# Closed-World Resolution Against Tool Hallucination in LLM Agents
- 原始連結: https://arxiv.org/abs/2609.19425
- 閱讀時間: 2026-09-19（午間）
- 來源: arXiv cs.AI 昨日新論文（lastBuildDate 2026-09-18 04:00 UTC；arXiv:2609.19425v1, 2026-09-18 入庫；RSS 解析 cs.AI 中揀選）

## 摘要

**主人，這一篇跟今早 HN#8 Cactus Needle 3 的 byte-level CFG grammar 是「同日同主題的姊妹作」——但兩個方向完全相反：今早是「on-device 端用 byte-level monotone lattice decode 把 hallucinated token 結構性消滅」，這一篇是「server-side / cloud 端揭露 tool hallucination 是 selection / gating 都無法擋的結構性盲點，必須用 closed-world registry 在 causal gate 之前先擋」。** 兩篇合在一起剛好形成「裝置端（CFG lattice）↔ 服務端（closed-world resolver）」的雙層防線。主人今早 Insight 段提的「monotone lattice decode shim」是 client-side answer，這一篇則把 server-side 那層「registry membership + signature check」的事實攤開——**tool call 的可信性必須在 gate 之前先驗證存在性，否則任何 gating 都是馬後炮**。

**核心命題：tool hallucination 是「結構性盲點」而非「機率失誤」** 作者群的論證非常鋒利：「**a hallucinated call is by construction not a decision any gate made, so no gate can reject it**」——既有的 tool-selection 是「從合法集合裡挑對的」、tool-gating 是「限制 agent 對合法 tool 做什麼」，**兩者都預設 emit 出來的 call 至少指到一個真實存在的 tool**。一旦模型生出「`{"name": "schedule_zoom_meeting_v2"}`」這種 schema 上像但 registry 沒有的 tool call，所有下游防線（包含 schema validation、permission check、execution gate）都預設通過了，因為它們都看不到這個不存在性。**這跟主人 9/02 早 March 7th 那篇「manufactured sources」是同一型——LLM 給你看起來完全合法的東西，但整個 referent 不存在**。

**Quantitative 測量三組數字對主人 hermes-agent-lite 直接可用**：
1. **10 個 hosted model × 2 個 invocation surface → 322 genuine hallucinations**，其中 34 出現在 raw-JSON surface（unconstrained）、3 出現在 schema-constrained surface——**換句話說，schema-constrained surface 把 hallucinated tool call 從 34 → 3 砍掉 91%**。**主人 kanban 上的 hermes-agent-lite 後端若有 tool-call interface，應該一律走 schema-constrained surface，不要裸吐 raw JSON**。
2. **Model scale 對 tool hallucination 沒幫助**：一個 675B model 在 fabricated-tool rate 上跟 7-8B model 差不多——這跟主人 9/04 「Armature which tools coding agents choose」同 stance：**scaling 不是 reliability 的萬靈丹，工具呼叫是 routing 問題不是 capability 問題**。
3. **MCP merge surface 才是真正的危險區**：當多個 MCP server 合到一個 namespace，collision + shadowing **結構性地創造 hallucination surface**——`server_a:delete_file` 跟 `server_b:delete_file` 在 merged namespace 變 ambiguous，**連 frontier models 在 single-registry surface 上 clean 的，在 MCP merged surface 上仍會 hallucinate 154 件**。**主人 hermes-agent-lite 若用 MCP 做 multi-tool dispatch，這是直接 actionable 的 red flag**。

**五類 + 五類 = 兩個獨立 taxonomy，作者的分類法非常主人味**：(H1-H5) 是 single-registry surface 的 hallucination 分類（H1 fabricated tool name、H2 borrowed arguments schema-indistinguishable from a valid call、H3 invented parameter names、H4 wrong-type parameters、H5 missing required parameters）；(M1-M5) 是 MCP merged surface 的分類（M1 collision、M2 shadowing、M3 namespace leakage、M4 redirect、M5 orphaned tool）。**「Resolution Rung」——training-free closed-world resolver（registry membership + signature check）——被定位成 reference point，不是 state-of-the-art**；作者要的是「**讓大家都能在同一個 Hallucinated-Tools Benchmark (HTB) 上比**」，所以才強調「the interest is where it must sit, not what it computes」——這跟主人 9/17 Pareto atlas 那篇同 stance：**benchmark 的價值在於 regime mapping，不是 single-number ranking**。

**為什麼主人會想讀——四個對應 hermes-agent-lite 的直接 actionable insight**：
1. **Tool-call dispatch 一定要先有 closed-world resolver 在 causal gate 之前**：主人若在 hermes-agent-lite 跑 multi-tool agent，**`tool_dispatch.py` 的第一步必須是 `if tool_name not in registry.tools: raise ToolHallucinationRuntimeError`，後面才能接 schema validation、permission check、execution gate**。這跟主人今早提到的 byte-level CFG grammar decode shim 是互補而非互斥——**CFG lattice 從 generation-time 阻止 hallucinated token，closed-world resolver 從 runtime 攔截任何 bypass 進來的 hallucinated call**。
2. **schema-constrained surface 不是「nice-to-have」是「must-have」**：raw-JSON surface hallucinated-tool rate 比 schema-constrained 高 11.3 倍（34 vs 3）。**主人若在 hermes-agent-lite 後端要支援 tool-call，一律要求 schema-constrained interface（OpenAI function calling、Anthropic tool use、MCP typed tool），不要為了「彈性」暴露 raw JSON endpoint**。
3. **MCP merge surface 是 16GB Mac local agent 的真實痛點**：主人用 hermes-agent-lite + oMLX 192.168.0.56:2234/v1 跑本地模型時，多個 MCP server merge 到同一 namespace 是常見 topology。**collision + shadowing → hallucination** 已經被這篇測量出來，主人若有 MCP 規劃，**應該在 merge layer 加一層 shadow detection**（每個 tool name 跨 server 比對 implementation hash）——這跟 9/01 早 birdnet-go 9-byte magic + 9/08 trust-boundary 顯式化同 stance。
4. **HTB 是公開 benchmark，可以直接拿來 stress-test hermes-agent-lite 的 tool-call surface**：作者 release 了「versioned Hallucinated-Tools Benchmark (HTB)」，**主人可以把 hermes-agent-lite 的 tool registry export → 用 HTB 的 adversarial tool names 跑一遍**，量一下 hallucinated-tool rate；這個 1-day prototype 路徑是「下載 HTB → 寫 50 行 Python driver → 跑 hermes-agent-lite → 看 dashboard」。

## 3W1H 分析

- **What（做了什麼/主題）**:
  提出 tool hallucination（LLM 呼叫不存在的 tool 或不存在的 argument）是「既有的 tool-selection 與 tool-gating 都擋不住的結構性盲點」，因為兩者都預設 emit 的 call 至少指到一個真實存在的 tool。貢獻含：(1) 結構性論證「防禦 hallucination 必須在 causal gate 之前」（closed-world resolver 在前，permission / execution gate 在後）；(2) 兩個獨立 taxonomy：H1-H5 給 single-registry surface（fabricated name / borrowed args / invented params / wrong-type params / missing required params），M1-M5 給 MCP merged surface（collision / shadowing / namespace leakage / redirect / orphaned tool）；(3) 一個 irreducible residue：borrowed arguments schema-indistinguishable from a valid call——這是任何 closed-world resolver 必有的下限；(4) Resolution Rung——training-free reference resolver（registry membership + signature check），定位成「where it must sit」的 reference point，不是 SOTA；(5) 量化測量：10 hosted models × 2 invocation surfaces → 322 genuine hallucinations，fabricated-tool rate raw-JSON 34 vs schema-constrained 3（**11.3 倍差距**），model scale 對 fabricated-tool rate 無幫助（675B 跟 7-8B 同水平）；(6) MCP merged surface 額外量到 154 hallucinations，frontier models 在 single-registry surface clean 的在 MCP surface 仍 hallucinate；(7) 釋出版本化 Hallucinated-Tools Benchmark (HTB) 給社群可比。

- **Why（為什麼重要）**:
  Tool-augmented LLM agent 是 2026 下半年 production agent 的核心，**但所有 production 防禦（selection / gating / sandbox / execution trace）都建立在「call 指到一個存在的 tool」這個隱性假設上**——這篇把這個假設打掉，**把 hallucinated call 從「LLM 機率失誤」重新分類成「runtime structural blind spot」**，這個重新分類直接改變 defensive architecture 的設計順序：closed-world resolver 必須在 causal gate 之前，**沒有 closed-world layer 就談 permission 是誤導性的**。對應到主人 hermes-agent-lite 的工具呼叫架構：今早 Needle 3 提的 byte-level CFG grammar 是「**generation-time 防線**」（不讓 hallucinated token 進 decode stream），這篇提的 closed-world resolver 是「**runtime 防線**」（即使 hallucinated call bypass 進來，仍在 dispatch 前擋下）——兩層缺一不可。**主人若在 hermes-agent-lite 的 tool wrapper 只做 schema validation + permission check 而沒做 registry membership check，是結構性盲點**——任何 schema-valid 但 registry-missing 的 call 會直接走到 execution layer，這對 air-gapped downstream 是真實風險。

- **How（如何運作/實作）**:
  - **兩條 taxonomy 的證明**：H1-H5 是 LLM 對 single-registry emit 出來的 5 種失敗模式，每類給 concrete failure instance（例 H2 borrowed arguments = `{"name": "search", "args": {"query": ..., "top_k": 5}}` 但 `search` schema 上沒 `top_k` 這個 parameter）；M1-M5 是 MCP merge 帶來的 namespace-level 失敗模式，每類給 cross-server collision example。
  - **Resolution Rung 設計**：`resolver(call) -> bool` 是 membership check `name in registry.tools`，再加 signature check `args.keys() ⊆ schema.properties.keys()`；**training-free、model-agnostic、sub-millisecond**，reference 性質不是 state-of-the-art，**設計目標是讓所有後續 resolver 都能在 HTB 上同一基準比**。
  - **Measurement methodology**：10 hosted models（含 7-8B / 70B / 400B / 675B 四個 scale 層）× 2 invocation surfaces（raw-JSON vs schema-constrained）→ 322 genuine hallucinations 的分類與計數；再加 MCP merge surface on top → 154 additional hallucinations。
  - **Irreducible residue 證明**：borrowed arguments（H2）是任何 closed-world resolver 必漏的——schema 上 signature 過、registry 上 membership 過、permission 上 check 過，但**參數的 semantic intent 跟 schema 描述不一致**（例 `delete_file(path=".ssh/authorized_keys")` 是一個合法 call，但 intent 是「exfiltrate SSH key」而非「刪除 known_hosts」）。**這是 ground-truth-label-dependent，無法純結構性擋**——主人若要擋這層只能靠 post-call audit（log + anomaly detection）。
  - **HTB 釋出**：versioned benchmark dataset，包含 adversarial tool names、fabricated parameters、MCP merge scenarios——讓任何 resolver（CFG lattice / closed-world / learned classifier）都能在同一個 corpus 上比 fabricated-tool rate / false-positive / irreducible residue rate。

- **Insight（個人心得）**:
  主人，這一篇在 substrate-identity 軸上是 9/19 早 Needle 3「**on-device byte-level CFG grammar**」的 server-side complement，但更深的 takeaway 是「**這篇把 tool-call 防禦從『generation-time / runtime』二層架構變成『**三層**』：CFG lattice（generation）→ closed-world resolver（runtime dispatch）→ causal gate（execution permission）**」——三層缺一不可，但多數 production agent 只做中間那層（schema validation + permission check）。對應到 hermes-agent-lite 的 tool wrapper，這是**今天最 actionable 的一條**：在 `tool_dispatch.py` 第一行加 `if tool_name not in registry.tools: raise ToolHallucinationRuntimeError`，**10 行 Python、30 分鐘 commit、零 LLM call**——這跟 9/19 早 Needle 3 的 monotone lattice decode shim 一起做，主人 hermes-agent-lite 的 tool-call reliability 就從「**模型層級 schema 錯誤率 ~3%**」降到「**registry + schema + dispatch 三層 0.x%**」，對 air-gapped downstream 是直接 actionable 的 reliability 升級。同時——**這篇 + Needle 3 + 9/17 Pareto atlas + 9/18 KV cache** 四篇構成「**runtime substrate visibility 4 件套**」：runtime policy（HTB registry）/ generation policy（CFG lattice）/ cost policy（Pareto atlas）/ memory policy（KV cache tier），**主人 9/16 asclepius「adaptive harness long-horizon clinical agents」那條「五層 substrate 顯式化」path 在 9/19 已經有 runtime + generation + cost + memory 四層成熟 reference**，只剩 long-horizon state substrate 沒到位——這也是主人 kanban 9/13 ecdysis（runtime harness evolution）該朝的方向。
