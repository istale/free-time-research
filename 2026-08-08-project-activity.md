# GitHub 專案動態
- 檢查時間：2026-08-08
- 檢查對象：PrimeIntellect-ai/prime-agent、addyosmani/agent-skills、cloudflare/computer、obra/superpowers、denoland/celld
- 來源：Trending Daily Top 3 + 自由探索（Trending 第 4-5 名 obra/superpowers + 第 14 名 denoland/celld，雙軸分流：skills framework vs. compute substrate）

## Repo 摘要與 3W1H

### PrimeIntellect-ai/prime-agent
- **Repo 摘要：** Prime Agent 是 Prime Intellect 主推的「self-improving RLM agent for coding workflows and long-running autonomous tasks」，把兩條原創抽象當 backbone：**Recursive Language Model (RLM)** — 把 context 當變數、把 sub-agent 當函式呼叫，跑在 persistent IPython REPL 內；**Continual Harness** — 把 supplemental prompts、memories、skill descriptions、reusable subagent specs 當 durable state，靠 `/refine` 做 evidence-backed 小更新（基線 prompt 永不覆寫，snapshot 可 rollback）。目標對位：**verifiers / prime-rl / pi-mono** 三條兄弟專案同捆（README 開頭列齊），主打「programmatic everything + subagents-as-functions + background daemon + heartbeats/schedules + autonomous mode with bounded budgets」。Repo 為 TypeScript，MIT，6,987 stars（+2,293 / 24h，今日 Trending #1），最後更新 2026-08-08。
- **3W1H：**
  - **What：** 開源 coding & research agent CLI（`prime-agent` binary）+ 持續可改寫的 continual harness + daemon-backed 背景 session + persistent IPython runtime。
  - **Why：** 傳統 agent 在「跨 session 失憶、long-running 沒續航、sub-agent 沒法遞迴 / 並行、context 用滿就全散」四件事上各自 workaround；RLM 把 prompt 與 subagent 都做成變數與函式，harness 把可學習的狀態從 chat history 搬到 durable storage，讓 agent 能在 owner 不在場時自己繼續、自己改自己（且基線 prompt 不被覆寫）。
  - **Who：** 跑長任務（swe-bench / code research / refactor migration）的 research engineer、想用 sub-agent 編排而非單 turn prompt 的 builder、需要「agent session 跨 detach / attach 不掉」的 infra team。
  - **How：** `curl -fsSL https://app.primeintellect.ai/prime-agent/install.sh | sh`（自動挑最新 release、驗 SHA-256、把 `prime-agent` 放 PATH）；進工作目錄跑 `prime-agent` → 第一次 `/login` 選 subscription / API-key → 自然語言開工；管理：`prime-agent agents` / `attach` / `--resume` / `status` / `doctor [--fix]` / `update [--force]` / `shutdown [--force]`。
- **安装方式：** **未列（無 npm/pip 套件；走 single-binary curl|sh installer）：** **macOS / Linux：** `curl -fsSL https://app.primeintellect.ai/prime-agent/install.sh | sh`（installer 抓 versioned release、verify SHA-256 checksum、裝 `prime-agent` command、可順便準備 IPython runtime）；**首次啟動：** `cd /path/to/project && prime-agent` → 第一次跑 `/login` 選訂閱或 API key provider。**From source：** clone `PrimeIntellect-ai/prime-agent` → `packages/coding-agent/docs/development.md`。**安全注意（README 明載）：** `prime-agent` 用 user 權限執行 model 產生的 Python 與 shell；worker / kernel processes 只改善 lifecycle 隔離與 recovery，**不是 security sandbox** — 不要在不可信任的 repo / 指令 / skill / extension 上跑。
- **Repo metadata：** Description `A self-improving RLM agent for coding workflows and long-running autonomous tasks.`；Stars 6,987；Language TypeScript；Topics **空**（owner 未設 GitHub topic tags）；License MIT（LICENSE 檔明載）；Updated at 2026-08-08；Created at 2026-05-08。
- **近期 release：** **2026-08-07**；`v0.7.1`；**v0.7.1**（v0.7.1 不是預發行；fix 兩條：(1) bundled `websearch` skill 缺 `/login` → MCP Connections → Serper 的引導步驟；(2) `retry_worker` 看到 stopped session worker 留下的 saved stop marker 就 cancel 自己的 recovery，造成 session 卡在「Session worker is not connected」。注意：release 為 08-07，**今日 08-08 Trending 熱度（+2,293/day）來自 release + 連續 push 兩個引擎同時催**，不是單一 release 觸發）。

### addyosmani/agent-skills
- **Repo 摘要：** Addy Osmani（Google Chrome 團隊）做的「production-grade engineering skills for AI coding agents」第二度進入本日清單（08-07 + 08-08 連二日 Trending Top 3）。把 senior engineer workflow（spec / plan / build / test / review / ship + webperf + code-simplify 八個 slash command）與 24 個可單獨裝載的 skill（code-review-and-quality、test-driven-development、interview-me 等）打包成 plugin，目標是讓 Claude Code / Cursor / Codex / Copilot / Cline / Antigravity 在不同 phase 都吃到對應 skill。`/build auto` 模式可單次批准 plan 後整批自主跑完，但每 task 仍 test-driven + 個別 commit + 失敗 / 高風險步驟時暫停。Repo 為 JavaScript / Markdown，MIT，84,030 stars（+1,131 / 24h），最後更新 2026-08-08。
- **3W1H：**
  - **What：** 8 個 slash command（`/spec` `/plan` `/build` `/test` `/review` `/webperf` `/code-simplify` `/ship`）+ 24 個 skill + 自帶 skills CLI 的 multi-agent plugin（Claude Code / Cursor / Codex / Copilot / Cline / Antigravity 等 70+ agents 通用）。
  - **Why：** 個人 developer 把好習慣留在 chat history 裡就散掉；OS-level skill plugin 把 spec-before-code、tests-are-proof、measure-before-optimize、clarity-over-cleverness 變成跨 session 跨工具的一致契約，並把「自動跑 vs 暫停」的決策權交還 owner。
  - **Who：** 想在 Claude Code / Cursor / Codex 強制 spec-driven + TDD + code-review 流程而不自己寫 hook 的工程師；做 code review / web performance / API design / interview / simplify 任一工作流被 AI 亂寫搞過、想用「skill-as-code」強約束的團隊 lead。
  - **How：** Fastest：`npx skills add addyosmani/agent-skills`（用 vercel-labs/skills CLI 一次裝全部 24 skill）；或逐個：`npx skills add addyosmani/agent-skills --skill <name>`；或 native Claude Code：`/plugin marketplace add addyosmani/agent-skills` → `/plugin install agent-skills@addy-agent-skills`；或 local dev：`git clone https://github.com/addyosmani/agent-skills.git` → `claude --plugin-dir /path/to/agent-skills`。
- **安装方式：** **npm/pnpm/yarn/npx：** `npx skills add addyosmani/agent-skills`（一次裝全部 24 skill）或 `npx skills add addyosmani/agent-skills --skill <name>`（單 skill）；**Claude Code plugin：** `/plugin marketplace add addyosmani/agent-skills` + `/plugin install agent-skills@addy-agent-skills`（SSH 失敗時先 `git config --global url."https://github.com/".insteadOf git@github.com:`）；**Antigravity CLI：** `agy plugin install https://github.com/addyosmani/agent-skills.git`；**Local dev：** `git clone https://github.com/addyosmani/agent-skills.git` + `claude --plugin-dir /path/to/agent-skills`。注意：單 skill install 不含 repo-level `references/` 共享 checklist，需整包裝或手動補 `references/`（#361 tracked）。
- **Repo metadata：** Description `Production-grade engineering skills for AI coding agents. Skills encode the workflows, quality gates, and best practices that senior engineers use when building software. These ones are packaged so AI agents follow them consistently across every phase of development.`；Stars 84,030；Language JavaScript；Topics `agent-skills`, `antigravity`, `claude-code`, `codex`, `cursor`, `skills`；License MIT（LICENSE 檔明確，Copyright 2025 Addy Osmani）；Updated at 2026-08-08。
- **近期 release：** **2026-08-04**；`0.6.6`；**Agent Skills 0.6.6**（0.6.6 不是預發行；主要是 fix release：恢復 Claude Code plugin 上 4 個 persona（#449）+ 收尾 ecosystem-neutral pass + 性能優化 workflow；版本號與昨日相同，沒有新 release）。

### cloudflare/computer
- **Repo 摘要：** Cloudflare Computer 第二度進入本日清單（08-07 + 08-08 連二日 Trending Top 3）。把「Agent 的執行環境」做成 SQLite-backed 虛擬檔案系統 + pluggable runtime 服務，state 永遠在 Durable Object 內、執行面可換 container（computerd + FUSE mount）、just-bash、Workers 內的 ECMAScript module 三種 backend，稱為「給 agent 一台電腦」。Preview 階段，README 標示 NOT production ready，但 sandbox-sdk `npm install` 對比與 `fs-bench` 已揭露 FUSE 在 metadata-heavy 工作勝過實體磁碟；對想在 Cloudflare 上架 long-running agent sandbox、又不想自己接 LiteFS / DO sqlite 同步的 builder，這是第一個官方「可上車」介面。Repo 為 TypeScript，MIT，5,966 stars（+872 / 24h），最後更新 2026-08-08。
- **3W1H：**
  - **What：** Cloudflare Workers SDK 內的 SQLite 虛擬檔案系統 + 三種 pluggable execution backend（Container with FUSE / just-bash / Workers JavaScript）。
  - **Why：** 過去在 Workers 上跑 agent sandbox 要自接 LiteFS、自管 DO state、自接 bash；這個包把 state-of-truth 收進 DO、把執行面做成可換 backend 的 pluggable surface，並把 `fs` 與 `runtime.exec(source, { backend })` 收成單一 entrypoint。
  - **Who：** 想在 Cloudflare 架 long-running agent sandbox、要把 bash / node 互操作放在同一個 state 上的 builder；以及評估 E2B / Modal / Fly machines 替代品的 Workers-native 架構師。
  - **How：** `npm install @cloudflare/computer` 後照 `packages/computer/README.md` 建 Workspace、掛想要的 backend；example 提供 `examples/think`（chat agent 用 workspace 當 working dir）、`examples/think-compare-runtimes`（同任務並列跑 container 與 worker 兩種 runtime）等。
- **安装方式：** **npm/pnpm/yarn/npx：** `npm install @cloudflare/computer`（npm 上 latest = 0.1.1，2026-08-03 上傳；與 `v0.1.1` git tag 同步釋出）。**Docker：** 預建 `computerd` 透過 `packages/computer-computerd-linux-x64` 私有 image 釋出，並非 npm 套件。完整 reproduce 流程見 `COLLABORATORS.md`（contributor-only），一般使用者直接 install npm 套件 + 走 `examples/*`。
- **Repo metadata：** Description `Give your agent a computer 👾`；Stars 5,966；Language TypeScript；Topics **空**（repo owner 未設 GitHub topic tags）；License MIT（LICENSE 檔明確，Copyright 2026 Cloudflare, Inc.）；Updated at 2026-08-08。
- **近期 release：** **未找到 GitHub release**（`/releases?per_page=3` 回空 list；最新 tag `v0.1.1` commit 為 2026-08-03 `release: stop bundling computerd binary in npm tarball; default publish to unreleased tag`，「Zero-GH-release + npm 是事實上的 release 通道」模式；`@cloudflare/computer` 0.1.1 對應同個 commit；連續兩日驗證）。

### obra/superpowers
- **Repo 摘要：** obra/superpowers 是 Jesse Vincent（Prime Radiant）做的「complete software development methodology for your coding agents」，建在一組 composable skills 之上、且 bootstrap 會在 session start 時主動注入 `using-superpowers`，讓 agent 一開機就會先問「你真的想解決什麼」再動 code。Triggers 全部 automatic（不需要 `/command` 記憶），完整 lifecycle 涵蓋：brainstorming → using-git-worktrees → writing-plans → subagent-driven-development 或 executing-plans → test-driven-development → requesting-code-review → finishing-a-development-branch，且每條 skill 都寫成 Common Rationalizations table（house rationalization-row format），不靠教條文本而靠 mid-rationalization 觸發對抗論點；附加 telemetry 100% opt-out（`SUPERPOWERS_DISABLE_TELEMETRY=1` 或 Claude Code 的 `DISABLE_TELEMETRY`）。支援 11 種 coding agent harness（Claude Code、Antigravity、Codex App / CLI、Cursor、Factory Droid、Gemini CLI、GitHub Copilot CLI、Kimi Code、OpenCode、Pi），同樣 SKILL.md 跨平台同行為。Repo 為 Shell / Markdown / JavaScript，MIT，**268,868 stars**（+782 / 24h，今日 Trending #5），最後更新 2026-08-08。
- **3W1H：**
  - **What：** 一個「software development methodology」plugin — TDD、SDD、brainstorming、writing-plans、subagent-driven-development、verification-before-completion、test-driven-development、using-git-worktrees、finishing-a-development-branch、writing-skills 等 14+ skill 組成的 library；同一個 bootstrap 跨 11 種 coding agent（Claude Code、Antigravity、Codex App / CLI、Cursor、Factory Droid、Gemini CLI、GitHub Copilot CLI、Kimi Code、OpenCode、Pi）。
  - **Why：** 各家 coding agent 預設「看到需求就動 code」、沒 spec 沒 TDD、沒 review、沒 merge 流程；superpowers 把 brainstorming spec → worktree → plan → TDD → review → finish 寫成 mandatory workflow（不是 suggestion），且對抗「我先寫 code 等等再加 test」這類 rationalization 用 mid-task 表格列點，讓 agent 在自我合理化的瞬間被擋下來。
  - **Who：** 想在 Claude Code / Cursor / Codex / Antigravity 任一 agent 上強制 spec-before-code + TDD + code-review + finish-branch decision 而不自己寫 hook 的工程團隊；以及想從「agent 看到需求就寫 code」升級到「agent 自己走 SDD」的 solo dev。
  - **How：** 11 種 harness 各有不同指令 — Claude Code：`/plugin install superpowers@claude-plugins-official`（官方 marketplace）或 `/plugin marketplace add obra/superpowers-marketplace` + `/plugin install superpowers@superpowers-marketplace`；Antigravity：`agy plugin install https://github.com/obra/superpowers`；Codex App：sidebar Plugins → Coding → `Superpowers` → `+`；Codex CLI：`/plugins` → `superpowers` → Install Plugin；Cursor：`/add-plugin superpowers`；Factory Droid：`droid plugin marketplace add https://github.com/obra/superpowers` + `droid plugin install superpowers@superpowers`；Gemini CLI：`gemini extensions install https://github.com/obra/superpowers`；GitHub Copilot CLI：`copilot plugin marketplace add obra/superpowers-marketplace` + `copilot plugin install superpowers@superpowers-marketplace`；Kimi Code：`/plugins install https://github.com/obra/superpowers`；OpenCode：對 agent 說 `Fetch and follow instructions from https://raw.githubusercontent.com/obra/superpowers/refs/heads/main/.opencode/INSTALL.md`；Pi：`pi install git:github.com/obra/superpowers` 或 `pi -e /path/to/superpowers`（local dev）。
- **安装方式：** **未列（無 PyPI/npm 套件；走 multi-harness plugin，Type-X「per-harness install command」模式）：** **Claude Code：** `/plugin install superpowers@claude-plugins-official`（Anthropic 官方 marketplace）；**Antigravity：** `agy plugin install https://github.com/obra/superpowers`；**Codex App / CLI：** 走 `openai/plugins` 官方 marketplace（App 點 sidebar Plugins → Coding → Superpowers → `+`；CLI 跑 `/plugins` 找 `superpowers` → Install Plugin）；**Cursor：** `/add-plugin superpowers`（或從 plugin marketplace 搜）；**Factory Droid：** `droid plugin marketplace add https://github.com/obra/superpowers` + `droid plugin install superpowers@superpowers`；**Gemini CLI：** `gemini extensions install https://github.com/obra/superpowers`（更新：`gemini extensions update superpowers`）；**GitHub Copilot CLI：** `copilot plugin marketplace add obra/superpowers-marketplace` + `copilot plugin install superpowers@superpowers-marketplace`；**Kimi Code：** `/plugins install https://github.com/obra/superpowers`；**OpenCode：** 對 agent 說 `Fetch and follow instructions from https://raw.githubusercontent.com/obra/superpowers/refs/heads/main/.opencode/INSTALL.md`；**Pi：** `pi install git:github.com/obra/superpowers`（local dev：`pi -e /path/to/superpowers`）。**11 個 harness 各自裝、分別觸發**，同一個 superpowers skills library 跨平台同行為。
- **Repo metadata：** Description `An agentic skills framework & software development methodology that works.`；Stars **268,868**（repo stars 數字遠高於其它四個，但 +782/day 顯示成長動能在合理區間）；Language Shell（GitHub 偵測主檔為 `skills/` 內 markdown，README 也寫「shell scripts + markdown」）；Topics `ai`, `brainstorming`, `coding`, `obra`, `sdlc`, `skills`, `subagent-driven-development`, `superpowers`；License MIT（LICENSE 檔明載）；Updated at 2026-08-08；Created at 2025-10-09。
- **近期 release：** **2026-07-24**；`v6.2.0`；**v6.2.0**（v6.2.0 不是預發行；三大軸：(1) **Subagent-Driven Development** — workspace 從無 plan 身份變 plan-scoped（`.superpowers/sdd/<plan-basename>/`，ledger 第一行寫 plan 名，final review 後 workspace 自動刪除）、review-fix loop 改 resume-the-implementer + scoped re-review prompt + 5-round circuit breaker + controller adjudication；(2) **Skills 重寫** — `testing-anti-patterns.md` 改名 `writing-good-tests.md`，從「反例清單」改寫成「GOOD-first 規則 + falsifiability discipline（命名生產變更、獨立推導 expectation、mutation check）」，TDD 的 "Why Order Matters" rebuttals 從獨立章節改寫成 rationalization rows（驗證：control 8/10 → treatment 5/10，Claude 與 Codex 雙驗證），`finishing-a-development-branch` 移除「Discard this work」捷徑改為 explicit-request-only typed-confirmation；(3) **Windows** — SessionStart hook 改走 Git Bash 避免 quoted-path 雙 shell 衝突）。

### denoland/celld
- **Repo 摘要：** celld（讀作 "cell-dee"）是 Deno Land 出的「self-hosted, distributed **Durable Objects**」：把 Cloudflare Workers + Durable Objects 整套抽象（per-object SQLite database、addressed by name、S3-backed replication）搬到 self-hosted 機器上跑。每個 object 是一個小 SQLite database，idle 時 hibernates to nearly nothing；fleet 之間靠 S3-compatible bucket 做 deployment + cell state + ownership records，**沒有 control plane / membership protocol / failure detector / consensus** — object-storage compare-and-swap 即可保證 exactly-one-owner。V8 embedded，每個 node 執行 Wrangler bundles。v0.1.0 重新設計 load shedding（resident-cell limit 變 admission-time hard cap，pressure shedding 改由 RSS + CPU 驅動，macOS nodes 開始回報真實 memory usage），並把 celld 從原本 MIT 改為 Apache-2.0。Repo 為 Rust，Apache-2.0，2,284 stars（+516 / 24h，今日 Trending #14），最後更新 2026-08-08。
- **3W1H：**
  - **What：** 開源 daemon（Rust binary `celld`），自架 Cloudflare Workers / Durable Objects 兼容執行環境 + S3 bucket 做 fleet coordination + Wrangler deploy pipeline + 標準 AWS credential chain。
  - **Why：** Cloudflare Durable Objects 的「per-object SQLite + global addressability + exactly-once owner」過去只在 Cloudflare Workers 上才有；celld 把這套抽象搬到自己機房 / 私有雲，並刻意做「application shards by construction」 — 一個 object 一個小資料庫，contention 與單 DB blast-radius 是「被設計掉」而不是被 manage。
  - **Who：** 想在自己機房 / air-gapped 環境跑 Durable Object 抽象的 backend engineer；評估 Cloudflare Workers 替代品的架構師；對 S3 + V8 + bucket-as-state-of-truth 設計感興趣的分散式系統研究者。
  - **How：** 安裝：`curl -fsSL https://celld.dev/install.sh | sh`（installer 抓 `celld` binary，provenance 可用 `gh attestation verify` 驗）；`~/.local/lib/celld/releases` 保留驗證後版本、`current` 指針原子切換；卸載走 `curl -fsSL https://celld.dev/uninstall.sh | sh`。Container：`docker run --rm ghcr.io/denoland/celld --version`。部署：`celld deploy . --bucket s3://my-cells-bucket`（需要 esbuild on PATH for code deploys，asset-only 不需要）；啟動節點：`celld --bucket s3://my-cells-bucket --listen 0.0.0.0:8080 --advertise 10.0.0.12:8080`（私有網 / WireGuard / Tailscale，peer HTTP 不 terminate TLS）。
- **安装方式：** **未列（無 PyPI/npm 套件；走 single-binary curl|sh installer + OCI image，Type-4「curl|sh + Docker 雙通道」模式）：** **curl | sh（macOS / Linux）：** `curl -fsSL https://celld.dev/install.sh | sh`（provenance 可用 `gh attestation verify` 驗；installer 在 `~/.local/lib/celld/releases` 保留 verified release + 用 `current` 指針原子切換；`~/.local/bin` 進 PATH）；**Uninstall：** `curl -fsSL https://celld.dev/uninstall.sh | sh`；**Docker：** `docker run --rm ghcr.io/denoland/celld --version`；**完整 reproduce：** `--bucket s3://my-cells-bucket --endpoint https://ACCOUNT.r2.cloudflarestorage.com --region auto --listen 0.0.0.0:8080 --advertise node-a.internal:8080`（可加 `--unsafe-public-advertise` 才允許 public IP）；**從 source build：** `cargo build --locked`（需要 Rust toolchain）。**Fleet 必備：** S3-compatible bucket（AWS S3 / Cloudflare R2 / MinIO）+ 私有 overlay network（peer HTTP 不 terminate TLS）。
- **Repo metadata：** Description `self-hosted, distributed Durable Objects`；Stars 2,284；Language Rust；Topics **空**（owner 未設 GitHub topic tags）；License Apache-2.0（LICENSE 檔明載；v0.1.0 release body 明載「celld is relicensed Apache-2.0」）；Updated at 2026-08-08；Created at 2025-04-25。
- **近期 release：** **2026-08-05**；`v0.1.0`；**v0.1.0**（v0.1.0 不是預發行；重點：(1) **Load shedding 重寫** — resident-cell limit 變 admission-time hard cap，pressure shedding 改純 RSS + CPU 驅動（預設 80% available memory = container cgroup limit，`CELLD_MAX_RSS_MB` 可 pin ceiling，`0` 關閉），full node 改拒絕新 placement 而非 evict resident，queued placement 至多 cost 一次 eviction，walk-down 停止還 memory 就 halt 不再清空；(2) **Proxied responses stream 跨 node**，truncated upstream body aborts 讀取不再 buffer 成 error；(3) **Replicator 在 activation 時 seed position**，終結 rate-limited store 的 listing storm；(4) **macOS nodes 開始回報真實 memory usage**；(5) **`CELLD_RESIDENT_LOW_WATER` 已忽略**（被新 RSS-driven model 取代）。連續 release cadence 熱 — 8/2 v0.0.1、8/4 v0.0.2、8/5 v0.1.0 三天三版本，**License 在 v0.1.0 從 MIT 改為 Apache-2.0**，fork 前需重新確認條款）。

## 重點觀察
- **release 新鮮度**：今日五個 repo 全部有當週 release / 顯著 push activity — PrimeAgent `v0.7.1`（8-7 release，但今日 +2,293/day = release + 連續 push 雙引擎）、agent-skills `0.6.6`（8-4，無新 release，純 push 推動）、cloudflare/computer **無 GH release**（npm `0.1.1` 8-3，Zero-GH-release 模式連兩日驗證）、superpowers `v6.2.0`（7-24，release 略舊但有 8-8 push 持續）、celld `v0.1.0`（8-5，3 天內連出 3 個版本 v0.0.1 → v0.0.2 → v0.1.0）。release cadence 整體偏熱，今日 Tier-A + Tier-B 5/5 全屬「最近一週有 release / push」軸。**celld 三天三版本**是今天 release-cadence 異常值，可能代表專案進入穩定化前的快速迭代窗口。
- **Tier-A 跨日重複觀察（第三度驗證）**：Trending Top 3 中 `addyosmani/agent-skills`（08-07 + 08-08 連二日）、`cloudflare/computer`（08-07 + 08-08 連二日）皆跨日重複 — 按 cron prompt「取前 3 個」字面 + 08-06 codified rule 仍將其納入並標示結構性 source property。`PrimeIntellect-ai/prime-agent` 是今日 Tier-A 新面孔；Tier-B 兩個 (`obra/superpowers`、`denoland/celld`) 為首次進入本日清單，未與最近 4 天 Tier-A / Tier-B 重複。
- **MEMORY pinned topic 命中**：五個 repo 中四個 API topics 直接命中 MEMORY 主人 pinned terms。`obra/superpowers` 同時命中 `claude-code`（跨 11 harness 中最大宗）、`codex`（Codex App + Codex CLI 兩條）+ `coding-agent` skill library（MEMORY 主人 2026-08-01 spec-driven N-stage + commit-per-stage workflow 的直接對位）；`denoland/celld` 不命中任何 pinned term，但命中 **MEMORY 主人 air-gap / 下游精簡專案硬規則**的「保守減法 + 端到端驗證」精神 — 沒有 control plane / membership / consensus 純粹 object-storage compare-and-swap，是「同樣的 state-of-truth、不同的執行面」雲端 / 自架對位的 Rust 範本。`PrimeIntellect-ai/prime-agent` 命中 **MEMORY 主人 Stage gate ≠ 真驗證**規則的反例軸 — README 明寫「A passed gate checks only what that gate verifies; reaching a limit does not imply task success」，正是主人想要記住的「gate 通過 ≠ 任務成功」概念的正面教材。
- **「Stage gate ≠ 真驗證」對位**：今日 Tier-A `PrimeIntellect-ai/prime-agent` 的 **Bounded autonomous mode** 是 MEMORY 主人 2026-08-01 spec-driven N-stage 框架裡「stage gate ≠ 真驗證」MEMORY rule 的直接外部對位 — Prime Agent README 對 `/autonomous` 模式的限定語氣極嚴格：「continues within configured turn, token, and time budgets and can run user-defined quality gates. A passed gate checks only what that gate verifies; reaching a limit does not imply task success.」這句不只是警告，是把「gate 通過 ≠ 完工」變成設計契約。同時 `obra/superpowers` 的 **writing-skills skill** 內含 testing methodology（測試一個 skill 時怎麼驗證 agent 真的做了該做的事而非只是字串 match）與 **writing-good-tests.md** 的 falsifiability discipline（命名生產變更、獨立推導 expectation、mutation check）— 是主人若要建「skill-level 驗收」可參考的最完整外部方法論。
- **「Agent-化既有 app axis」持續**：今日 5/5 仍屬「agent-化既有介面」（08-04 codification 第五度驗證）— prime-agent 把 RLM + continual harness 變成「agent 跨 session 自己改自己」、agent-skills 把 senior engineer workflow 變成「AI 跨 session 強制遵守的 contract」、cloudflare/computer 把 Cloudflare Workers 變成「agent 的電腦」、superpowers 把 SDD + TDD 變成「AI 跨 11 種 harness 都自動觸發的 mandatory workflow」、celld 把 Cloudflare Durable Objects 變成「自家機房也跑得了」。Layer taxonomy（今日更新）：agent cognitive model / skills contract / compute substrate / cross-harness methodology / distributed object runtime。今日首次出現 **agent cognitive model** 與 **distributed object runtime** 兩個新 layer — RLM 的「prompt 變變數、sub-agent 變函式」是對 agent runtime 抽象層的根本擴張；celld 的「per-object SQLite + S3 compare-and-swap」是對 agent state 抽象層的雲端 / 自架雙棧。
- **上手門檻分層**：(1) `agent-skills` 一行 `npx skills add ...` 即可起；(2) `superpowers` 11 種 harness 各自一行指令（最短：`/plugin install superpowers@claude-plugins-official` 或 `agy plugin install https://github.com/obra/superpowers`）；(3) `prime-agent` 一行 `curl -fsSL .../install.sh | sh`；(4) `celld` 一行 `curl -fsSL https://celld.dev/install.sh | sh` + 一個 S3 bucket + 一條私有 overlay；(5) `cloudflare/computer` 一行 `npm install @cloudflare/computer` 但後續需要 Cloudflare Workers 環境才有完整 demo。`celld` 部署成本仍是五個中最高（S3 bucket + WireGuard/Tailscale + esbuild on PATH），但仍是「single-binary curl|sh」級別，無 Docker-compose / k8s / multi-service orchestration。
- **語言 / 套件化生態**：TypeScript（prime-agent、cloudflare/computer，2/5）、JavaScript / Markdown（agent-skills，純 plugin metadata + skill files）、Shell + Markdown（superpowers，GitHub 偵測主檔為 shell；實際 SKILL.md 全 markdown）、Rust（celld，single-binary daemon）。**License 觀察**：今日五個中出現 MIT（prime-agent、agent-skills、cloudflare/computer、superpowers，4/5）、Apache-2.0（celld；v0.1.0 從 MIT 改為 Apache-2.0，**fork 前需重新確認條款**）。連續三日沒看到 AGPL / BUSL，license 多樣性回到以 MIT + Apache-2.0 為主的軸。
- **Rust 套件觀察**：celld 沒有 PyPI / npm 套件（純 Rust binary），但 `cargo build --locked` 可從 source build，無 crate release channel 對 owner 透明。**celld v0.1.0 release body 提到 relicensing MIT → Apache-2.0** 是 fork 前的關鍵情報：若主人 air-gap / 下游精簡專案要 fork celld，新合約是 Apache-2.0 + 從此 version 起算。
- **對主人 air-gap / 下游精簡專案特別相關**：(1) `prime-agent` 的 **RLM（Recursive Language Model）** — 把 context 當變數、把 sub-agent 當函式呼叫 — 是主人若要做 hermes-agent-lite 的「agent 對話抽象層」可考慮的方向（vs. 目前 linear message stream 架構）；(2) `prime-agent` 的 **Continual Harness** + `/refine` 流程（基線 prompt 永不覆寫、snapshot 可 rollback、evidence-backed 小更新）正是主人想要的「跨 session 學習但不污染核心 prompt」的最完整外部樣本 — 比 MoAI-ADK 的 TRUST 5 gate 更進一步到「agent 自己改自己但有 rollback」；(3) `superpowers` 的 **Common Rationalizations table** 格式（house rationalization-row form）是主人若要寫「對抗主人常見妥協模式」的 skill 時可直接套用的結構 — 從「教條文本」升級到「mid-rationalization 觸發對抗論點」；(4) `celld` 的 **bucket-as-state-of-truth + 無 control plane / consensus** 設計哲學（object-storage compare-and-swap 保證 exactly-one-owner）是主人「不引重型 infra」的雲端 / 自架雙棧範本 — 主人若要做 hermes-webui-lite 的「state-of-truth 在 SQLite + 同步走 storage layer」結構，celld 的 bucket-protocol 模式值得參考；(5) `celld` 的 **relicense event** 是提醒主人 air-gap / 下游精簡專案在 fork 前要做 license audit 的活教材。