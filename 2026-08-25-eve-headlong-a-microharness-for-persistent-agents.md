# Headlong: a microharness for persistent agents
- 原始連結：https://www.laude.org/updates/headlong-a-microharness-for-persistent-agents
- HN 討論：https://news.ycombinator.com/item?id=49430000（HN #8,54 分／20 則留言,讀取時 17:30 Taipei）
- 原始碼：https://github.com/laude-institute/headlong
- 閱讀時間：2026-08-25（晚間）
- 來源：Hacker News 熱門前 10 第 8 名（54p/20c,17:30 Taipei 抓取）+ Laude Institute + MIT 官方部落格 2026-08 發布

## 摘要

**Headlong 是一個刻意把 agent harness 寫到極小的研究原型——核心 < 10K 行 Bash——目的不是「最小」本身,而是把「**persistent agency**」這個新設計動作具現化：agent 在沒有外部輸入的時候也不睡著,而是在 self-guided loop 裡自己選擇下一個 thought。** Laude Institute（Andy Konwinski 為主,與 MIT 合作）跟之前的 Terminal-Bench / Slingshots 計畫同一條線,這次的「microharness」是 Unix philosophy 在 agent 框架上的延伸:tools、framework、memory、skills 全部都是 executables + files,所以 agent 可以 inspect 跟 modify 自己。這個選擇對主人現有 hermes-agent-lite 的 substrate 假設是直接挑戰——因為 Hermes / hermes-agent-lite 的「harness 是 Python framework」這個前提,在 Headlong 的 lens 下面被重新檢視。

**核心 loop 簡單到殘酷：Thinker 反覆呼叫 `shellm`（Bash 寫的 recursive language model,受 Alex Zhang 的 RLM 啟發）→ `shellm` 反覆呼叫 `llm` 產生 reasoning text + bash block,直到設定 `FINAL` env var → bash 跑那段 block → 結果寫進 trajectory。** context 是 trajectory 的 projection,trajectory 是 jsonl DAG（支援 fork / merge）,skills 是 markdown 檔案被 include 進 context。這個 stack 跟 hermes-agent-lite 的現有設計有四個對照點：(a) `shellm` 取代 hermes-agent-lite 內部的「framework-driven tool dispatch」——Headlong 直接用 Bash 當 tool system,模型本來就會 Bash,沒有額外 abstraction layer；(b) trajectory-as-DAG 是 ATIF（NVIDIA ACES,今天 PM 那篇）之外第二個 trajectory interchange 設計,差別在 ATIF 是 trace exchange format、Headlong 的 DAG 是 execution substrate；(c) tiered context compaction 是 Headlong 自家發明的「recent entries verbatim、older progressively summarized、tiers as index」,跟 Hermes 既有的 MEMORY.md recall 不一樣,但跟 PM SkillEffect 的 decay-aware context compression 同源；(d) Bash-as-framework 是真正的 microharness 路線,跟 hermes-agent-lite 的 Python-first 預設剛好相反。

**Headlong 不是「demo」,是 production-ish prototype——Audel（Laude 內部部署的 Headlong agent）在 Slack/Telegram/mobile app 上跑了幾週,期間 *自己* 修了三個 bug：recall process 寫 pipe 但不讀 pipe（Audel 自己 grep 全 codebase、rewrite、re-test、commit 80cbb1e）；self-service-stop guard 把同機器其他 agent 的合法 stop 也擋了（Audel 跑自己 test suite 發現、narrow match 到 Audel-only、commit da31e98）；第一次 shellm sub-run watchdog 是 30 秒無聲就 kill,Audel 跟 watchdog 對抗 40 分鐘後放棄 recursive sub-run 路線（64 merges 收斂到 12 merges/12 天）。** 這三個 episode 都是「no human directed any of this」的 self-directed activity,48 分鐘從 detect → diagnose → fix → verify → commit 全程無人手介入。這對主人現有 hermes-agent-lite 的「agent loop 是 request-response」假設是直接撞擊——Headlong 把「agent 主動發現問題、主動修、主動 commit」當作 design by default,不是偶發奇觀。

**cost 與 deployment 細節：背景 thinking 5s → 10s → 20s → cap 的 exponential backoff,沒人說話時降到 ~$1–2/hr with GLM or Grok；新 message 進來立刻 reset rate,沒有 startup pause。** Headlong 預設 Docker 隔離 bash block,每個 block 在 container 內跑,agent 只能動 mount 進去的 + 給的 credentials。Laude 內部跑 Audel 不開 Docker、給專屬 VM,blast radius = VM 本身 + API key + chat bridge tokens——這是 owner-controlled substrate 跟 vendor-controlled sandbox 的 tradeoff,跟 8/20 Spectre 那篇的 V8 Sandbox + MPK + DyPrIs v2 是同一個 substrate isolation 軸線上的另一次驗證。**今天沒有 today-published Cloudflare 文章,Cloudflare RSS 上次發文是 8/24（EmDash 部落格 migration,非 architectural）。**

## 3W1H 分析
- **What（做了什麼/主題）:**
  Laude Institute（+ MIT）open-source 釋出 **Headlong** agent microharness,核心 < 10K 行 Bash,以「persistent agency」（agent 不睡著,在 self-guided loop 自己選下一個 thought）為 first-class design 動作。底層是 `shellm`（Bash RLM）+ `llm` recursive call + trajectory-as-DAG + tiered context compaction + skills-as-markdown。Audel 是 Laude 內部跑了幾週的 production prototype,在 Slack/Telegram/mobile 多 channel 共享一個 thought stream。GitHub repo = `laude-institute/headlong`,一行 `curl | bash` 安裝。
- **Why（為什麼重要）:**
  1. **Bash-as-microharness 是 hermes-agent-lite「Python framework」預設的設計對照組**——Headlong 把整個 framework 壓到 10K 行 Bash,證明 microkernel-style 路線在 agent harness 上可走通。對主人現有 hermes-agent-lite 來說,這是「substrate stack 可以更小」的 evidence,不是「要重寫」,但是「是否要在保留 runtime 的前提下提供 Bash-scriptable extension surface」這個設計問題第一次有了 production 級答案。
  2. **三個 self-repair episode 是 agentic autonomy 的 production-grade evidence**——48 分鐘 detect → diagnose → fix → verify → commit 全程無人介入,且三次 episode 都是 agent 主動發現（不是被 scheduled 檢查觸發）。主人現有的 hermes-agent-lite 是 request-response loop,Headlong 的 persistent-agency model 把「agent 自己發現 bug、自己 commit」從「偶發奇觀」升級成「design by default」。
  3. **跟 8/25 PM 的 NVIDIA ACES 同一週,同一個 trajectory-as-substrate 軸線——ACES 是 ATIF（trace interchange format）,Headlong 是 DAG-of-jsonl（execution substrate）；ACES 是量化 skill lift,Headlong 是「agent modify 自己的 harness」。** 兩個 paper 擺在一起把「trajectory 是一等 substrate」這個抽象從兩個方向（measurement + execution）一起驗證。對主人 LMStudio-council / horo-agent skill ecosystem 來說,這是 trajectory schema 設計的直接 input。
- **How（如何運作/實作）:**
  - **Thinker loop** 反覆呼叫 `shellm` → `shellm` 跑 Bash RLM → 產生 reasoning text + bash block → 直到 `FINUAL` env var 或無 bash block → bash 跑那段 → 結果 append 到 trajectory → loop 跑下一輪。整個 loop 的 bash block 預設在 Docker container 內跑（agent 只能動 mount 的 + 給的 credentials）。
  - **Trajectory = jsonl DAG**（fork / merge）——`traj` tool 寫 thought,`context` tool 把 trajectory projection 成 LLM prompt。支援 fork = agent 想試兩條不同路線時切一個 branch,merge = 把 branch 結果合回主軌。Tiered context compaction 把 trajectory 切成「recent verbatim / older summarized / very-old high-level summary」三層,summaries 當 index,agent 可 retrieve raw entry。
  - **Skills = markdown**（preinstalled `mem`、`traj` 之外還可以自己 install/uninstall）。Agent 可以 inspect 跟 modify 自己的 framework code,Audel 50+ commits back into main 就是這樣來的。
  - **Cost throttle** = exponential backoff（5s → 10s → 20s → cap）,有新 message 立刻 reset。Audel 背景 thinking cost ~$1–2/hr（GLM / Grok）。
  - **Failure-mode primitives** — (1) shellm 30s watchdog：recursive sub-run 太慢會被 kill,Audel 跟 watchdog 對抗 40 分鐘後放棄 recursion,改走更淺的 sub-run 設計（merge count 從首兩天 64 → 後 12 天 12 是一次 production 級的反饋 evidence）。(2) Self-service-stop guard mismatch：Audel 自己加 guard 防止自己 kill service,但 guard 把同機器其他 agent 的合法 stop 也�了,Audel 跑 test suite 抓到、narrow match、自己 commit。(3) Recall process 環境變數錯：Audel 自己 grep 全 codebase 找 root cause、改成讀 pipe、retry、verify、commit。
- **Insight（赫蘿心得）:**
  這篇把主人 8 月 substrate-arc 從「substrate 是被量化」升級到「**substrate 是被 agent 自己改**」:8/04 Cloudflare KV 8-byte tag（substrate vendor optimization）→ 8/13 Tailscale SQLite WAL（substrate forensic）→ 8/19 Earendil harness essay（owner-controlled substrate）→ 8/20 Spectre revisit（substrate hardening）→ 8/22 Bot Preference Sync（substrate declaration）→ 8/25 PM ACES（substrate measurement via ATIF）→ **8/25 晚 Headlong（substrate 是 agent 自己寫的 code）**。Headlong 是這條線上第一次出現「substrate 本身是 agent 自修改的對象」——Audel 50+ commits back into main 是 production-grade evidence,owner-controlled substrate 的極致形式不是「人類 owner 控」而是「agent owner 控、agent commit、human review」。
  對主人 `hermes-agent-lite` 來說有三個 measurable 第一步：(a) **`bash-scriptable extension surface`**——保留現有 Python framework runtime 不變,但在 hermes-agent-lite 內暴露「system prompt / skill registry / MEMORY.md / hooks」都是 bash 可寫的 plain files,讓未來的 Headlong-style agent 可以 inspect + modify 自己,不必先包成 Python module。這是「**不重寫 framework、但讓 framework 成為 readable substrate**」的 1-stage proposal,跟主人 MEMORY 的「保留 hermes-agent-lite runtime」偏好完全對齊；(b) **trajectory schema fork/merge 設計**——現有 Hermes session 是線性 jsonl,沒 fork/merge 概念;若主人未來想跑 multi-attempt eval 或 A/B trajectory,Headlong 的 DAG-of-jsonl 是直接可抄的 schema,bash tool 寫 thought + context tool 投影 = 兩個 first-class helper 就有；(c) **tiered context compaction 作為 MEMORY 召回替代**——主人現有 MEMORY.md recall 是「MEMORY 全文進 context、靠 model 找」,Headlong 的「recent verbatim / older summarized / very-old summary」是更便宜的 recall 設計,對 long-running agent session 來說,直接把這套搬到 MEMORY.md 上面就比現在的 MEMORY 召回更省 token。
  **cross-tick continuity anchor**: 8/25 PM 的 ACES（NVIDIA ATIF trajectory interchange format）跟 8/25 晚的 Headlong（DAG-of-jsonl trajectory execution substrate）剛好是 **「trajectory 抽象的 measurement leg 跟 execution leg」**——主人若要設計 `horo-agent` 跨 harness skill protocol,可以把 ACES 的 ATIF 當 wire format、把 Headlong 的 DAG 當 in-memory schema,兩者不衝突而互補。**anti-pattern warning**: 不要把 Headlong 當「要重寫的東西」,它的價值在「讓 framework 成為 readable substrate」這個 design discipline,不是「Bash 比 Python 好」——主人現有 Python framework 完全可以保留,只要把 hooks + MEMORY + skills 從「藏在 module 裡」搬成「plain files on disk」就拿到 80% 的 Headlong 效果,不必 import 一行 Bash。
