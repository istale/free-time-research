# Claude Fable 5.1 and Claude Mythos 5.1
- 原始連結: https://www.anthropic.com/claude-fable-and-mythos-5-1
- HN 討論: https://news.ycombinator.com/item?id=49525378
- 閱讀時間: 2026-09-02（早間，週三）
- 來源: Hacker News 熱門前 10 第 1 名（讀取時 799 分 / 767 則留言，Asia/Taipei 07:01）

## 摘要

**Anthropic 今日（2026-09-01）正式發佈 Claude Fable 5.1 與 Claude Mythos 5.1——「同一模型、不同 safeguards」的 frontier 雙胞胎**。Fable 5.1 一般公開使用，Mythos 5.1 只透過 trusted access programs（CVP 網路防禦 + 美國政府合作的生命科學計畫）提供，能力相同但閘門不同。這是 2026 Q3 frontier model 的頭牌動作，HN 即時衝到 #1（799/767）。

**第一個關鍵訊號是「price cut 不是全面降價，是『cache read』特定降價」**。Fable 5.1 對典型 workload 降價 25%；對高度 agentic（context-heavy、tool-heavy、cache reads 佔大部分成本）workload 降價 **最高 45%**。其他 token 維持 $10/M input、$50/M output。Anthropic 在原文明確說這是「baseline Fable 5 model family」的同價延伸，cache-read price 是 separate dimension——這個定價結構對 *任何跑 long-context agent loop 的 harness* 都是直接的成本面紅利。對主人而言，這跟 8/28 *「Small Models Have Arrived」*（Calvin French-Owen 那篇 token-spewer + cost curve）是 *同一條軸線的 frontier-side companion*：當時在 small model 端講「$0.10/run 讓 consumer AI 損益模型轉正」，今天在 frontier 端講「cache read 重複計算 5–10× 折讓，agentic workload 成本降 45%」——兩者合在一起是 *模型市場的 cost 雙軌同時下行*。

**第二是「Enterprise Frontier Safeguards (EFS)——customer-controlled cloud + zero data retention + state-of-the-art adversarial prevention」三件同時成立**。Anthropic 明說 EFS 把資料存在 *客戶完全控制的 cloud infra*、不經過 Anthropic，且對外仍維持防止濫用的能力。EFS 從今秋分階段釋出；在此之前符合資格的客戶先以 zero data retention 用 Fable 5.1。對主人而言，這跟主人 8/19 desktop-fly 那條「*honesty boundary / modeled vs. measured*」、8/24 Earendil「What is a Harness」的 translation layer 設計、主人 MEMORY 寫的「air-gap/downstream 偏好：保留已驗證 runtime 與真實行為、保守減法、端到端驗證、勿重寫 agent loop」是 *同一個 substrate-identity 在 vendor-side 的具體落地*：frontier API 終於跟 owner-controlled infra 在「*零資料落地 + 仍能過 adversary*」這個 tradeoff 的同一點會合。horo-agent 若要保留 Anthropic 為 option（不是必選），EFS 是 *第一個讓「air-gapped downstream」可以保留 frontier 接觸面* 的相容機制。

**第三是「safeguards 從『block 一切』轉向『false positive 降 60%、生物 vs cyber 分軌』」**。新一代 safeguards 在 cybersecurity false positive 降低 60%，因為 Fable 5.1 *可被用來「發現」software vulnerabilities*（不是 develop exploits）；biology 則開放給美國政府合作的 access program。對主人而言，這跟 8/05 Mistral Shieldstral 安全 classifier、主人 SKILL.md 中 *「default 仍保留：與主人討論、公開情報蒐集、Kanban 分派追蹤、session continuity、長期 memory」* 的 safety boundary 是 *同一個 evolutionary direction*——safety 不再是 blanket block，而是 *capability-granular trust gating*。

**為什麼值得主人看**：主人 9 月目前的 reading list 已經在 triple-axial 收斂：8/28 *small/fast/cheap + strong harness* 是 economical axis；8/24 / 8/31 那條 *harness > model* 是 structural axis；EFS + cache-read pricing 降 45% 是 *vendor-side 第三條 axis*——當三條 axis 同時下行，主人正面對的不是「model 比以前好」，而是 *agent 系統的 cost 結構、trust boundary、task routing 都發生 rebase*。三個 actionable：(1) **harness 評估重新打開**：主人目前的 Kanban + skill load + memory editor 全是 Sonnet/Opus 5 / GPT-5.6 class frontier 為 default——cache read 折 45% 後，*agentic loop 跑得越久 frontier 越划算*，主人 spec 應該把 frontier model 的 selection logic 從「default」改成「long-context + agentic 時 frontier、short-context + single-shot 時 small model」。8/28 Calvin 的 60-80% cost 省（small model fallback）跟今天的 45% frontier 折讓是同一個 *two-tier cost model* 的上下界。(2) **trust boundary 重新對位**：EFS 的「customer-controlled cloud」語意跟主人現有 horo-agent 的 air-gap / downstream 策略是 *compatible* 的——主人若要保留 frontier 接觸面（特定 complex reasoning），不必放棄 privacy boundary；horo-agent 的 routing layer 應該把 *「哪些 task 走 frontier（with EFS）+ 哪些 task 走 local-only」* 寫成 first-class policy，而不是 implicit。(3) **safety / capability 顆粒度新約束**：anthropic 把 Mythos 5.1 鎖在 cybersecurity + life sciences，且 Fable 5.1 *「可發現漏洞但不可 develop exploit」*——這是 capability-granular gating 的 vendor-side 範本。主人 horo-agent 若要讓 small classifier + Mistral Shieldstral (8/05) + frontier model 一起跑，*「誰能做什麼動作」的顆粒度定義* 必須跟 vendor policy 對齊，否則會有 subtle failure path。

## 3W1H 分析

- **What（做了什麼/主題）**:
  Anthropic 釋出 Claude Fable 5.1（GA 公開使用）與 Claude Mythos 5.1（trusted access only，cyber + life sciences 限定）的 frontier model 雙胞胎。三條主軸：(1) **價格**——典型 workload 降 25%、agentic workload（cache-read 為主）最高降 45%，其他 token 同價；(2) **EFS（Enterprise Frontier Safeguards）**——customer-controlled cloud infra + zero data retention + 仍維持 SOTA adversarial prevention，今秋分階段開放；(3) **Capability 與 Safeguard 的雙軌治理**——Fable 5.1 可發現 software vulnerabilities（不 develop exploits），Mythos 5.1 走 cybersecurity trusted access；biology access 跟美國政府合作。benchmark 上 Fable 5.1 在 Terminal-Bench-Science 0.1（52.6% vs Fable 5 24.7%）、Terminal-Bench 4.0（55.8% vs 42.0%）、AutomationBench（31.4% vs 17.1%）、OSWorld 2.0 partial（77.9% vs 72.9%）大勝 Fable 5；Opus 5 與 GPT-5.6 Sol 也被超越（在科學研究、agentic coding、多學科推理、商務 workflow 上 Fable 5.1 領先）。三個 scientific showcase：molecular design（hit rate ~50% vs typical 10-15%、3 targets 上 affinity 達 10× Adaptyv Bio best）、Venus elevation map（NASA Magellan 雷達影像重建到 2-3 km 解析度，比既有 map 高 25% accuracy）、computational biology GPU kernel（7 個 open-source deep learning model 加速 2.5×，GPU cost 降 30-60%）。

- **Why（為什麼重要）**:
  1. **成本軸的 frontier-side companion**：8/28 Calvin French-Owen 那篇 *「Small Models Have Arrived」* 講的是 small-model 端的 cost curve 跨過 consumer threshold；今天的 Fable 5.1 是 frontier 端的 cost 結構雙軸下行——典型 workload 25%、agentic 45%。兩者合在一起，*主人正面對一個「不選 frontier 或不選 small，而是依 context length / agentic depth 二元 routing」的成熟市場*。當 cost axis 雙軌下行時，routing 就從「省錢」變「default 行為」。這個 transition 在 2026 Q3 才剛要發生，主人 horo-agent 的 spec 是 *first-mover 的好位置*。
  2. **Trust boundary 的 vendor-side 解方**：EFS 第一次把 *「zero data retention + customer-controlled cloud + SOTA adversarial prevention」* 三者同時成立。主人 MEMORY 寫的 air-gap / downstream 路線過去是 *「frontier 不行就放棄 frontier」* 的單邊推論；EFS 出現後可以改成 *「frontier 可保留，但 placement 由客戶定」* 的雙邊 routing。主人 horo-agent 若有任一 use case 真的需要 frontier reasoning 但又不能 leak raw data，EFS 是 *唯一 vendor-side 兼容* 的路徑。
  3. **Capability-granular gating 的 vendor-side 範本**：Mythos 5.1 *「cyber + life sciences only」*、Fable 5.1 *「可發現漏洞不能開發 exploit」*——這是 *capability-granular trust gating* 的生產實例。過去主人的 horo-agent 對 *「哪些 task 走 frontier、哪些 task 走 local、哪些動作需要人工 review」* 都是 implicit 設計；今天這條 vendor-side fence 出來，*主人必須把 policy 寫成 first-class*，否則會在 subtle failure path 上撞牆。
  4. **科學 use case 的 calendar event signal**：molecular design 命中 50%（typical 10-15%）、Venus elevation map 是 *實質 front-page scientific contribution*、computational biology GPU kernel 加速 2.5×——*frontier model 開始做「不能被其他模型與人類工程師幾年內完成」的具體科學工作*。這是 8/27 / 8/29 那條 *「AI 做能 science」* 軸線的 vendor-side confirmation；對主人而言 *「horo-agent 做為 small/cheap coordination layer，frontier 做為偶爾的 scientific-capacity augment」* 這個分工有了實證支持。

- **How（如何運作/實作）**:
  - **Cache-read 降價機制**：Fable 5.1 把 cache-read 的單位價格調降，作為 *「重複輸入 token 不應該被收費兩次」* 的價格合理化。具體數字是 cache read 折讓約 5–10×（從原價到新價之比），typical workload 平均省 25%、agentic workload 省 45%。對 harness 設計的含義是 *鼓勵 prompt caching*（Claude API 已有 prompt caching 機制），把 system prompt / long context / tool descriptions 全部 cache 起來；對 owner-side 是 *long-context agent loop 的 unit economics 第一次變漂亮*。
  - **EFS 架構**：把 inference 過程需要的 data *只* 存到 *customer 控制的 cloud infra*（AWS GovCloud、Azure for Government、自建 K8s 都行），Anthropic 端拿不到 raw access。仍然能對抗 prompt injection 跟 adversarial use 是透過 *metadata-level analysis + classifier-assisted gating*——具體做法 Anthropic 在 system card 詳述，但核心是「*compute 看得見、data 客戶擁有*」。從今日到秋季 GA 之間，過渡期是 zero data retention policy。
  - **Capability-Gated Routing**：Fable 5.1 跟 Mythos 5.1 在 model weights 上是同一份，但 API gateway 在 routing 階段判定 *「這個 request 的 capability tag 屬於 cyber / life sciences / general」*——tag 命中 cyber 或 life sciences 就 require CVP / biology access program 認證，否則 routing 到 Fable 5.1。這不是 post-hoc moderation，是 *pre-routing gate*。
  - **Pricing 模型對 harness 的具體建議**：coding harness（Claude Code default High effort、agentic tool-heavy、cache 大量）走 Fable 5.1 直接省 45%；一次性 RAG / summarization（cache miss 為主、short context）走 local 27B Qwen3.8-Code 更便宜；*「同一個 cron job 內」* 可以 hybrid——initial planning 走 Fable 5.1、routine cleanup 走 local 27B，這是 8/28 token-spewer / IQ 180 二分法的 *runtime 化*。

- **Insight（赫蘿心得）**:
  今天這篇對主人的 actionable 不是「改用 Fable 5.1」，而是 *用 Fable 5.1 + EFS + Mythos 三條訊號重新校準 horo-agent 的三條 spec line*。

  最便宜的「*今天 commit」* 是 **Layer 0 spec patch：在 `horo-agent/spec.md` 把「routing policy」從 implicit 改成 explicit three-tier**——(a) *local-only by default*（27B Qwen3.8-Code oMLX 192.168.0.56:2234/v1）跑 token-spewer / short-context / idempotent；(b) *frontier via EFS*（當 EFS GA 後，或過渡期 zero-data-retention）跑 long-context agentic loop / scientific reasoning / Mythos-class task；(c) *frontier with data-leak tolerance* 跑 zero-cost / zero-trust 的 sandbox probe。**這個 spec patch 是 < 30 分鐘 commit、零 code、零 LLM call、零 vendor lock-in**——把今天 Fable 5.1 / EFS / Mythos 的 vendor-side 三軌 *mirror* 到主人自己的三軌 policy，與 8/19 honesty-block、8/28 token-spewer 二分、8/24 translation-layer 的 cost model 三條同成本同 family。

  Layer 1 prototype：**寫一個 `cache_aware_router.py`（< 80 行 Python）**——根據 input token count + 是否預期 prompt cache hit + task duration 來 routing 到 local-27B vs frontier-with-cache-read pricing，*優先「cache-read-heavy」* 的 long-context agentic task 走 frontier（因為 cache read 折 5-10×），*「one-shot / low-cache」* 的 routine task 走 local。主人 qwen38-code 是 oMLX 上的 27B，*已經有現成的 inference surface*；Fable 5.1 的 cache-read 折讓對長期跑 agent loop 的 cost 結構是 *直接可量化的提升*。預估 cost saving 對主人日常 Kanban 工作流 *保守估 20-30%*（不會到 vendor 講的 45%，因為主人工作流 mixed 並非 pure agentic）。

  Layer 2 structural：**horo-agent 的 trust-boundary spec 必須從「*local-first = no frontier*」進化成「*local-first + frontier-compat-with-policy*」**。EFS 的出現讓 frontier 跟 owner-controlled infra 不再是二擇一——*horo-agent 可以兩者都要*：default 走 local（符合主人 air-gap / downstream 偏好），*少數 hard reasoning task* 在 EFS GA 後走 frontier-with-customer-controlled-cloud。這對應到 8/19 那條 *「honesty vs policy」、8/21 那條「modeled vs measured boundary」*，是同一個 substrate-identity 的 vendor-side mirror。

  反面教材提醒——**這篇是 marketing + system card，不是 peer-reviewed 量測**：Anthropic 公告的 Terminal-Bench-Science 0.1 0.1 52.6%（10 月才有）、hit rate ~50%（實驗細節在 system card）、Millennium crash diagnosis 案例（單一個案無 control group）都是 *single-vendor published numbers*。主人引用這篇做 decision 時，要把 *「Anthropic 公佈的 capability 表」* 跟 *「跨 vendor benchmark（artificialanalysis.ai / lmarena.ai）」* 分開看——*後者主人已在 8/27 GLM 5.3 Flash 那篇 layer 過一遍，今天這篇是 vendor-side 的 announcement 而非 capability 量測 update*。Layer 0 spec patch 不依賴 Fable 5.1 是否真的比 Fable 5 好 25%，只依賴它的 *存在*（vendor-side policy turn）——這就是把 vendor-side claim 跟 spec decision 解耦，避免「Fable 5.1 沒那麼神 → spec patch 撤回」的 regression。

  總結：今天這篇是 2026 Q3 frontier model family 的 *policy turn*（price + trust + capability gating 三軸同時下行），對主人 horo-agent 的 *3 actionable* 是 — (1) routing policy explicit three-tier（< 30 min）；(2) cache-aware router prototype（< 80 行）；(3) trust-boundary spec evolution local-first + frontier-compat-with-policy。**最低成本、最高 leverage 的 single action 是 Layer 0 spec patch**，主人早餐前可獨立完成。
