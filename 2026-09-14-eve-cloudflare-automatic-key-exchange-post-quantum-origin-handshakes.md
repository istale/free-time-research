# Cloudflare Automatic Key Exchange — 用測量代替猜測，把 45 億次每日 origin handshake 推到後量子 + 單 round-trip

- 原始連結: https://blog.cloudflare.com/automatic-key-exchange-for-origins/
- Cloudflare Radar PQ 工具: https://radar.cloudflare.com/post-quantum
- 開發者文件 (Automatic Key Exchange): https://developers.cloudflare.com/ssl/origin-configuration/automatic-key-exchange/
- 上一篇 PQ 軸 (DNS layer): 2026-09-11-eve-cloudflare-1111-post-quantum-dnssec.md (1.1.1.1 ML-DSA-44 DNSSEC)
- 閱讀時間: 2026-09-14（晚間 17:30 Asia/Taipei）
- 來源: Cloudflare Blog · 2026-09-08 13:10 GMT（cron 觸發當下為最近 6 天內、唯一未被寫過的 PQ axis architectural post）

## 摘要

**Cloudflare 把「TLS 1.3 origin handshake 的 key agreement 算法」從「永遠先猜 X25519」改寫成「先 probe 每個 origin 支援什麼、然後優先用 PQ hybrid X25519MLKEM768」**。動機直接：TLS 1.3 規範要求 client 在「第一個封包」就 commit keyshare，猜錯就要 HelloRetryRequest (HRR) 重新來——多花一個 round-trip。Cloudflare 量化這個浪費：「52% 的 origin 連線觸發 HRR」，p90 多 150ms。Automatic Key Exchange 把 HRR 比例壓到 3.7%，且把 PQ-capable origin 的「單 round-trip 內完成 PQ handshake」從 0% 推到 99.2%。目前 PQ origin TLS 1.3 流量已經從 25 億/日 長到 45 億/日，且這數字還在漲。

**「測量代替猜測」(measure instead of guess) 是這篇真正的 primitive — 不是後量子。** Automatic Key Exchange 的核心不是「我們終於自動 enable X25519MLKEM768」，而是 Cloudflare 把「每個 origin 支援什麼 key agreement 算法」這件事 **從 passive inference（觀察 traffic 推測）轉成 active probing（每個 origin 跑一系列 lightweight TLS handshake，每個只 offer 一個 algorithm，掃完整個支援矩陣）**。這跟 09-11 1.1.1.1 PQ DNSSEC 那篇的「default-on 提早啟用蒐集營運數字」是同一個 substrate — 但 advance 一階：09-11 講的是「量到真實營運成本做未來決策依據」，今天這篇講的是「量到真實 capability 做當下 routing 決策」。兩者合在一起是 Cloudflare PQ 路線的 **「測量驅動遷移」(measurement-driven migration) 雙層 blueprint**：DNS layer 量 verification cost → 為 root zone 切換準備；TLS layer 量 per-origin capability → 為當下 handshake 選 best 算法。

**對位主人 SOUL.md 的「hermes-agent / air-gap / 保守裁切」是有兩層直接交集的。** 第一層：**horo-agent multi-profile routing 的「per-profile capability detection」缺口**。主人 multi-agent 是 default Qwen + qwen38-code (oMLX 192.168.0.56:2234) executor + GPT reviewer，**profile 的 capability 是假設靜態的**（model name = capability set）。Cloudflare 今天是「per-origin 主動 probe → 動態決定用什麼算法」，對應主人 substrate 是「per-profile 主動 probe → 動態決定該 profile 能接什麼 task」。第二層：**horo-agent enterprise-lite / air-gap downstream 的「內部 handshake PQ-safe」deadline**。Q-Day = 2029（CNSA 2.0 + NIST timeline），horo-agent 若要保留 enterprise 客戶，內部 TLS origin handshake 必須 PQ-safe——否則 harvest-now, decrypt-later 風險等於「今天所有 enterprise 客戶的 LLM API traffic 都被收，未來量子電腦解密」。這兩個軸對位的具體 wiring 詳見下方 Insight。

**Substrate-arc continuity（NEW 09-14，PQ migration dual-layer pair）**：09-11 EVE = DNS layer PQ migration（ML-DSA-44 DNSSEC, 2,420-byte signature）；09-14 EVE = TLS layer PQ migration（X25519MLKEM768 origin handshake, 1,216-byte keyshare）。兩個 picks = **同一 primitive（post-quantum migration）在兩個 substrate（DNS layer + TLS layer）**，對應 08-20 cross-substrate isolation primitive bridge pattern。09-11 那篇講「break once, forge everywhere」根密鑰風險；今天這篇講「45B daily connections, harvest-now-decrypt-later 大規模被收」風險——同一個 Q-Day deadline 撞兩個不同 layer，這是主人企業-lite 路線的 **「crypto migration roadmap dual-track」** 樣板。

## 3W1H 分析

- **What（做了什麼/主題）**: Cloudflare 推出 **Automatic Key Exchange (AKE)** 為 Automatic SSL/TLS 的延伸，把 TLS 1.3 origin handshake 的 keyshare 從「靜態預設 X25519」改成「主動 probe 每個 origin 支援哪些 algorithm（含 X25519 / P-256 / P-384 / P-521 / X25519MLKEM768），然後 per-domain 自動 prefer 最強的：PQ hybrid (X25519MLKEM768) 優先、fallback 到最快 classical。Probe 流程 5 步：(1) lightweight TLS handshake 探每個 algorithm 一輪；(2) per-subdomain + 加權 traffic volume 評估；(3) strict priority order 選最強候選；(4) 漸進式 rollout（先小比例 traffic，HRR/failure rate 異常就 rollback）；(5) daily rescan 跟隨 origin configuration 變化。Compliance requirements 新增 Post-quantum hybrid 跟 FIPS 兩個 toggle，雙勾選必須有交集算法否則拒絕設定（避免「強制 PQ 但 origin 不支援」造成 100% TLS 失敗）。

- **Why（為什麼重要）**: 對主人有 4 個 actionable 軸：(a) **測量代替猜測的 primitive 通用性** — Cloudflare 把這套用在 TLS key agreement，但 pattern（probe → score → prefer → rollback on regression）可以套到任何「靜態假設 + 動態現實」落差的情境，主人 hermes-agent-lite 的多 profile routing 就是這種落差；(b) **Q-Day 2029 deadline 是企業-lite 路線的 hard constraint** — CNSA 2.0 要求 2030/2033 之間 classic 算法退役，horo-agent 如果要保留 enterprise 客戶，內部 TLS 必須 PQ-safe，今天 Cloudflare 把 PQ handshake 從 25B/day 推到 45B/day = 90 天內 80% 成長，是「業界已經在 PQ transition」的硬證據；(c) **Substrate-arc 雙層 PQ 推進** — 09-11 DNS + 09-14 TLS = 同一 primitive 兩層 substrate，主人可以借這個 pattern 規劃自己的 PQ roadmap（先量 verification cost，再量 per-instance capability）；(d) **「static 假設 + dynamic reality」失敗模式在 AI agent substrate 的普遍性** — Cloudflare 撞到的是「static X25519 + legacy middlebox failure」；主人 撞到的是「static profile capability + 任務需求 dynamic」，兩者都需要 active measurement 而非 passive inference。

- **How（如何運作/實作）**: 技術實作 4 個層次：(a) **主動 probing pipeline** — 每個 TLS 1.3-capable origin 跑 5 輪 lightweight handshake（每輪只 offer 一個 algorithm），scan 在 production traffic 路徑外，確認 origin 跟路徑都能 handle 才 upgrade；(b) **Per-domain 加權 scoring** — single domain 可能 front 多個 subdomain + 不同 origin，每個 subdomain 獨立評估 + traffic volume 加權，避免 dormant subdomain 跟 busy endpoint 等權；(c) **漸進式 rollout with automatic rollback** — 新 preference 先到小比例 traffic，monitor HRR rate，異常就 rollback（跟 Automatic SSL/TLS 加密模式升級的 rollback 邏輯共用）；(d) **Compliance requirements 兩條 toggle** — Post-quantum hybrid（強制 PQ only）跟 FIPS（強制 FIPS-compliant algorithms）可單獨或同時啟用，雙啟用必須有交集 algorithm 否則拒絕設定（防止 misconfiguration 把所有連線打掉）。底層 TLS 1.3 規範要求 predictive key exchange（client 第一個封包就 commit keyshare），這是 AKE 的 protocol-level 限制前提。

- **Insight（個人心得）**: 本文最值得主人拿走的是 **「測量代替猜測」(measure-instead-of-guess) 作為 static-vs-dynamic substrate 落差的 universal primitive**——Cloudflare 把這套用在 TLS key agreement，但 pattern 完全可移植到主人 hermes-agent-lite / horo-agent 的 multi-profile routing。具體落地建議——對主人的 multi-agent orchestration 工作：**(a) Layer 0 (text-rule, < 5 min)**：在 `~/.hermes/SOUL.md` 的「多代理偏好」段加一條「per-profile capability detection gate: 任何 profile (default-Qwen / qwen38-code / GPT-reviewer) 在 routing 前必須 probe 一次當下 capability（model name = capability 是 stale assumption），不 probe = 視為 static-default routing」；**(b) Layer 1 (< 1 hr prototype)**：在 horo-agent-lite 加一個 `profile_capability_probe.py` 腳本 (< 150 行 Python)，給每個 profile 跑 lightweight probe（每個 profile 送 5 個 standard task variants，分別量 latency / token cost / failure rate），根據結果算 score (e.g. `score = latency_p90_inverse × (1 - failure_rate) × cost_efficiency`)，router 根據 score 選 best profile，verify 步驟跑 3 個 toy case 測 approval/handoff rate ≥ 80%；**(c) Layer 2 (< 4 hr)**：把 Layer 1 的 probe 接到 hermes-agent-lite 的 multi-profile routing loop（default-Qwen 協調 → qwen38-code executor → GPT reviewer），讓每次 task routing 前自動 probe + score，預期能避免「profile 看起來 capability 跟實際 capability 不符」的 routing 失敗。**成本估計：Layer 0 + Layer 1 合計 < 70 分鐘，可在下一個 multi-profile task routing 週期一次到位**。**Substrate-arc 推進 (NEW 09-14, PQ migration dual-layer pair)**：(a) 09-11 EVE DNS layer = verification cost measurement for root zone decision；(b) **09-14 EVE TLS layer = per-instance capability measurement for handshake routing decision** — 兩篇是同一個「measurement-driven migration」primitive 在兩個 substrate（DNS protocol vs TLS protocol）的兩面鏡子。對位主人的 enterprise-lite 路線是 **「crypto migration roadmap dual-track」**：先量 horo-agent 內部 TLS handshake PQ-capability（Layer 1 probe 加上 `pq_safe_algorithms_supported` dimension），再量 DNS-over-TLS / DoH 的 PQ-readiness，兩個 layer 都量到再決定是否進到「強制 PQ-only」compliance state。**Operational borrow**: Cloudflare Radar 的 PQ-readiness checker（輸入 hostname 看 PQ 支援）= 主人可借鑑的「self-diagnostic tool」reference——可以寫一個 `hermes probe tls-pq-readiness --host <hostname>` CLI 對位主人 SOUL 的「保守裁切 + enterprise-lite」軸，verify horo-agent 連到的任何 external service 都在 PQ-capable 清單內。**Cost figure (NEW 09-14)**：PQ origin TLS 1.3 traffic 從 25B/day → 45B/day（80% 成長 / 90 天），這是「業界真的在 PQ transition」的硬量化證據——主人 SOUL 寫的 Q-Day 2029 deadline 不是 vendor marketing，是已經在跑的 migration 軌跡。
