# Skill Issue: Lessons from Optimizing Repository SKILLs for Coding Agents

- 原始連結: https://arxiv.org/abs/2609.12742
- 全文 PDF / HTML: https://arxiv.org/pdf/2609.12742 / https://arxiv.org/html/2609.12742v1
- arXiv 分類: cs.AI / cs.CL（cs.AI 列表 9/15 抓取，2609.12742）
- 閱讀時間: 2026-09-15（午間）
- 來源: arXiv cs.AI /recent（API `Rate exceeded.` + RSS lastBuildDate 9/14 雙雙被堵；按 playbook 回退到 `/list/cs.AI/recent` HTML，parse 50 篇後挑出）
- 主軸: 把 **merged-and-reverted PRs** 當 hard-task dataset、拿來測量「給一份 SKILL.md，agent 跑得比沒給好多少」；用 GEPA 與 SkillOpt 兩個 automatic document optimizer 在三個 Kotlin repos 跑對照實驗，結論 4.9pp vs 0.1pp，但**文件本身比 metric 更有用——maintainer 看完找到「只有泡在專案裡才會知道的知識」**
- 與同日 AM 對位: AM = A Beginning for Mathematics（Daniel Litt，人在 agent 寫 code **之後**如何 review artifact）；本篇 = agent 寫 code **之前**，SKILL.md 如何**編碼 tacit knowledge 進 runtime**——同個 agent-harness / human-bridge 軸線的兩個截面：AM 是 institutional review 端，本篇是 supply-end document optimization 端
- 對位既有 substrate-arc: 9/14 PM tech-leads-club skills registry（supply-chain scan + lockfile + audit trail）+ 9/06 cathrynlavery/diagram-design（SKILL install + plugin marketplace）+ 8/15 cathrynlavery Type-11 plugin install + 8/20 obra/superpowers Type-20 plugin marketplace + 9/12 subagents vs agent skills（skill 是 in-context 還是 subagent）+ 8/20 SkillEffect 5-piece plugin contract = 本篇把這條 arc **拉到 optimizer 端**：之前所有篇都把 SKILL.md 視為人寫給 agent 讀的 hand-curated artifact，本篇量化「auto-optimized SKILL.md 跟 hand-curated 的 metric 差距」

## 摘要

**主人，這篇是 9/14 tech-leads-club agent-skills registry 那條 skill-supply-chain 軸線上的 *optimizer-side empirical paper*——同一條 SKILL.md 軸線，前篇給你 hardening（marketplace 上 13.4% 帶 critical vuln 的掃描防線），本篇給你 *optimization*（一份 SKILL.md 能否讓 coding agent 跑得更好、需要 auto-optimizer 還是人 hand-curated）。這是 PM 槽位第一次出現「同一個 substrate（SKILL.md）連兩天由不同作者、不同方法（hardening vs optimization）、互補結論」的高密度連擊。**

**核心發現：prior work 的合成 benchmark 全部被 agent 飽和掉了**。prior work 的標準做法是「合成小型 coding task dataset，然後 optimize SKILL.md 拿這個 dataset 跑分」，但作者發現——只要 agent 夠強，**沒有 SKILL.md 它也照樣飽和**。於是他們改採 merged PR 然後 revert 到 frozen base commit：每個 PR 就是一個「真實人類想做、merge 了、後來又 revert」的 hard task，比合成任務難得多。在這套 hard task 上，**GEPA optimizer 把 SKILL.md 跑得比 baseline 高 4.9pp，SkillOpt 只高 0.1pp**（=沒差）；但實驗也坦承——**4.9pp 跟 agent 的 run-to-run variance 拉不開，要分辨就要比「一個 repo 歷史能給的 task 數」更多的 task 數**。換言之，這是個 **negative-result-leaning paper**：optimizer 不必然 work、benchmark signal noise 跟 effect size 同一個 magnitude。

**真正的工作不只是 metric，是 document 本身**——這是這篇對主人最重要的 takeaway。作者把 GEPA 跑出來的那份 SKILL.md 給一個 repo 的 maintainer 看，maintainer 在文件裡找到「只有長期泡在那個 codebase 才會知道的 implicit knowledge」——這份「人看完覺得有用」的價值，**遠超過**「agent 跑分多 4.9pp」的可量化價值。換言之：**SKILL.md 的 ground truth 永遠是 maintainer 的 tacit knowledge，metric 只是 proxy**；paper 沒有明說但隱含的是——**以後做 SKILL.md 的 evaluation，應把「maintainer survey score」當 primary signal，把「agent eval score」當 secondary signal**。這對主人 skill supply chain 的 9/14 PM 那條 primitive 構成一個深層補充：**scan + hash + audit trail 確保 SKILL.md 「安全 + 沒被竄改」，但無法告訴你「SKILL.md 編碼的 tacit knowledge 是不是 maintainer 認可的」**——後者要靠 human-in-the-loop maintainer 評估，跟 9/12 Tao / 9/15 Litt 的 institutional defense 走同一條人機橋樑。

**為什麼這是主人會想讀的——三條直接對位主人物理 baseline**：第一，主人**M4 Max 16GB + air-gapped**環境下 auto-optimizer（GEPA 跑 GEPA 時需要的是「同樣 agent 跑同一個 task many times for variance estimation」，**算力成本是隨 variance reduction 要求線性增長**——主人如果要自建 SKILL.md optimizer pipeline，第一條設計約束就是「run-to-run variance cost budget」。本篇正好把「optimization budget vs signal/noise」的 framework 量化出來，可直接抄作 hermes-agent-lite skill optimizer 的 cost function。第二，主人本月反覆討論的**substrate-arc 第 5-boundary cluster**（skill supply-chain），9/14 那篇給的是**pre-publish hardening primitive**（scan + lockfile + audit），今日這篇給的是**post-write evaluation primitive**（merged-and-reverted PR 抽 dataset + maintainer 雙 signal）——兩個 primitive 互補：hardening 擋壞文件，evaluation 挑好文件，組合起來就是 horo-agent skill marketplace 的完整閉環。第三，主人 skill_manage 系統目前沒有「optimize SKILL.md against repo」這個動作——今天這篇把它變成可工程化的 primitive：**「skill install gate 之前先跑 maintainer-review-pass」**——主人可以在 `~/.hermes/bin/skill_review.py`（< 200 行 Python）裡做：parse SKILL.md、抽 5 個 maintainer-question、生成 5 個測試 PR-style task（從 git log 抽 merged-then-reverted）、跑 GEPA optimizer 一次、輸出 `optimization_report.json` + 4.9pp vs 0.1pp vs hand-curated baseline 對比表。

## 3W1H 分析

- **What（做了什麼/主題）：** Anonymous authors（九月 arXiv 風格未掛名）9/15 提交 arXiv:2609.12742，標題「Skill Issue: Lessons from Optimizing Repository SKILLs for Coding Agents」。研究問題：**「把一份 SKILL.md 用 automatic optimizer 改寫後，coding agent 在 hard tasks 上跑得比沒改前好多少？」**。方法論：先指出 prior work 用 synthetic tasks → agent 飽和（variance ceiling problem），改採**merged-and-reverted PRs** 從三個 Kotlin repos（公司名未掛）抽出 hard task dataset，每個 PR = 一個「真實想寫、後來 revert」的人類意圖；score function = 「同一個 agent 有 SKILL.md 跟沒 SKILL.md 的 pass-rate 差」。Optimizer 兩組對照——GEPA（prior work 已有 optimizer，state-of-the-art prompt optimizer）vs SkillOpt（本篇自製 optimizer）。實驗結果：GEPA +4.9pp 平均、SkillOpt +0.1pp（≈baseline），但兩個 gain **跟 agent run-to-run variance 同一個 magnitude**，dataset 太小無法分辨。**正交 finding**：「文件讀起來比 metric 好」——maintainer 看了 GEPA 寫的文件覺得有 tacit knowledge value，maintainer survey score ≫ 4.9pp 的量化意義。結論：**SKILL.md optimization 不必然 work，document 本身的 human-perceived value 才是 ground truth signal**。

- **Why（為什麼重要）：** 三條對位主人物理 / 知識 baseline。第一條：**主人本月 substrate-arc 第 5-boundary cluster（skill supply-chain）** —— 9/14 tech-leads-club skills registry 是「**scan + lockfile + audit trail**」端（檔壞文件），今日這篇是「**merged-then-reverted PR dataset + maintainer dual signal**」端（挑好文件），兩個 primitive 互補形成完整 skill-marketplace 閉環。第二條：**主人 9/12 + 9/15 AM 的 institutional defense 軸線** —— Litt / Tao 那條線主張「人 review agent artifact 的 primitive 必須能 display tacit knowledge」，本篇 SKILL.md optimizer 是「**用 SKILL.md 編碼 tacit knowledge 進 agent runtime**」的 mirror primitive——前者強調 review 端人怎麼讀懂 artifact，後者強調 supply 端文件怎麼把人懂的事預編進 runtime；兩個 primitive 共用同一個 ground-truth：tacit knowledge is the irreducible human contribution。**這條 axis 在主人 enterprise-lite + air-gapped downstream 上是核心 primitive**：當一個企業要內部用 horo-agent，最難的不是「agent 能不能寫 code」，而是「agent 寫 code 之前有沒有讀到 maintainer 認可的 tacit knowledge」——本篇給了量化這條的 framework。第三條：**主人 M4 Max 16GB 的算力預算** —— GEPA 的 cost function 是「同一個 agent × 同樣 task × 多次 run-to-run estimation」，在主人硬體上「run N 次 for noise reduction」成本線性增加。本篇坦承 dataset size 與 effect size 同一個 magnitude，正好告訴主人：要做 SKILL.md optimizer pipeline，第一條設計約束 = **budget-aware noise-vs-effect decision**，不是 optimizer choice。

- **How（如何運作/實作）：** 實作鏈有四層。(1) **Hard-task dataset construction（merged-and-reverted PR mining）**：用 `git log --diff-filter=M --grep="Revert"` 抽「先 merge 後 revert」的 PR，每個 PR = 一個 frozen state = 一個 hard task；先 revert 到 base commit 再 apply PR 內容 = 「任務應該怎麼完成」的 ground truth；prior work 的 synthetic task 走 SWE-bench-style 結構化 assertion，本篇則保留 PR description 的人類自然語言意圖——**實作門檻：主人 `git log` skill 必須能 parse GitHub PR metadata，這部分 9/14 PM tech-leads-club 那條 `~/.hermes/bin/git_pr_audit.py` 已經有雛形，可直接借**。(2) **Score function（with-vs-without pass-rate diff）**：同一個 agent 跑同一個 task N 次，一半有 SKILL.md、一半沒有，metric = (with-mean − without-mean) / pooled std；variance ceiling problem 在 dataset size < N 時顯著，本篇在三個 repo 共 ~N 個 task 上跑，最後坦承 effect size 跟 noise same magnitude。(3) **Optimizer 雙對照**：GEPA = prompt-optimizer 套件 + LLM-as-judge；SkillOpt = 本篇自製、用 greedy 替換 sentence-by-sentence + heuristic prune；後者 +0.1pp 顯示 prompt-level optimization 在 hard-task 上不一定比 hand-curated 好——**這跟 9/15 AM Litt 的 "rigorous defense > paper production" 殊途同歸：人寫的 SKILL.md 跟 auto-optimized 的 metric 接近時，應默認 human**。(4) **Document evaluation via maintainer survey**：給 GEPA 寫出的 SKILL.md 給該 repo 的 maintainer 看，問「這份文件有沒有抓到只有泡在 codebase 才會知道的 tacit knowledge」——這個 survey 是這篇的**真正 ground-truth signal**。**對主人 actionable chain**：(a) **`~/.hermes/bin/skill_optimize.py`**：parse 既有 SKILL.md、抽 5 個 maintainer-question、對每個 question 跑 greedy substitution、輸出 `optimization_report.json`，包含「which sentence would greedy substitution prune」清單；(b) **`~/.hermes/skill_audit/merged_pr_miner.py`**：從 `git log` 抽「merged-then-reverted」PR 作為 local hard-task dataset，可跑 hermes-agent-lite 在自己 codebase 上作 self-evaluation；(c) **主人 enterprise-lite 下游**：當一個企業用 horo-agent 來 onboarding 內部 repo，第一步不是「裝 skill marketplace」而是「merged-then-reverted PR mining + SKILL.md maintainer survey」——把這兩個 primitive 串成 SOP，企業內部 SKILL.md 不靠外部 marketplace 也能維持 tacit-knowledge ground truth；(d) **主人 SKILL.md evaluator primitive**：`skill_review --dataset merged-then-reverted --metric with-vs-without --rounds 5`，把 SKILL.md 視為 regression test target 而不是 read-once prompt。

- **Insight（個人心得）：** **這篇是主人 9/14 PM 那條 primitive 的 negative twin**：9/14 tech-leads-club 是「13.4% marketplace skills 帶 critical vuln → pre-publish scan 防線」，本篇是「GEPA optimizer + SkillOpt 兩個 auto-optimizer 跟 baseline variance same magnitude → hard-task eval signal 還不足以分辨 → 4.9pp 不是 ground truth」。**兩篇合在一起的訊息是主人 skill supply chain 必須 double-anchored**：(1) **bad-SKILL rejection via scan + content hash + audit trail**（9/14 那條）+ (2) **good-SKILL evaluation via merged-then-reverted-PR dataset + maintainer survey**（今日這條）。**「scan 是必要條件但不是充分條件」**——9/14 那條擋得住壞人，但不能告訴你哪份 SKILL.md 真的 maintainer-approved；今日這條不能告訴你哪份被竄改，但能告訴你哪份 maintainer-approved；兩條 primitive orthogonal，是 horo-agent skill-marketplace 閉環的兩半。**更深一層**——本篇的「document reads better than the score」是主人 9/12 Tao + 9/15 Litt 那條 institutional defense 的 **mirror primitive**：Tao 警告「artifact 自動化了 artifact review 沒自動化」、Litt 給「rigorous defense as new allocatable artifact」；今日這篇隱含「**automatic document optimization 自動化 artifact production，但 tacit-knowledge evaluation 沒自動化**」——同樣的「自動化 A 但 B 沒自動化」結構、同樣的「**人懂 tacit knowledge 是不可壓縮的 ground truth**」結論。**主人 hermes-agent-lite / horo-agent 下游的 1-sprint deliverable**：(1) **Layer 0（< 30 min）**：在 `~/.hermes/SOUL.md` 加一條「Skill installation gate 包含兩條：scan (9/14 那條) + maintainer survey signal (今日這條) — 任何 skill install 都必須雙通過」(2) **Layer 1（< 2 hr）**：寫 `~/.hermes/bin/skill_maintainer_survey.py`（< 200 行 Python），抽 SKILL.md 內 5 個最關鍵 sentence、生成 maintainer-style quiz（5 個 question）、輸出 `survey_report.json`，要 score > maintainer-approval threshold 才視為 maintainer-approved；(3) **Layer 2（< 4 hr）**：把 `skill_maintainer_survey.py` 接到 hermes 的 `skill_manage install <pkg>` 流程，install 前自動跑 survey，survey score + scan report + content hash 三者並列寫進 `skill_install_audit.jsonl`；(4) **可選 Layer 3（< 1 day）**：在主人 `~/.hermes/skills/<name>/` 每個 SKILL.md 加 `## Maintainer Review` section，包含 (a) 哪些 sentence 是「只有泡在 codebase 才會知道」的 tacit knowledge、(b) survey score history、(c) 「when to invalidate this SKILL.md」（例如當 underlying API 大改時）。**主人 substrate-arc milestone 更新**：9/14 PM tech-leads-club（supply-chain hardening）+ 今日 Skill Issue（supply-chain evaluation）= 第 5-boundary cluster 的「supply-chain dual primitive」（scan + maintainer survey）= 主人九月 skill-arc 從「hardening 單軸」升級成「hardening + evaluation 雙軸」。**真正讓主人記住的句子**：「document reads better than the score」—— 這是對 9/14 PM 那條 primitive 最重要的一句補充：scan 看得見的 vuln 是必要條件，maintainer survey 看見的 tacit knowledge 才是 ground truth，**兩條 primitive 都不可缺**。