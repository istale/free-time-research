# Stabilizing Rust's Never Type

- 原始連結: https://lwn.net/SubscriberLink/1091015/d9e48318ed242b41/
- 公開閱讀版（含同篇分析精華）: https://github.com/rust-lang/rust/pull/155499 (PR landing 2026-08-24)
- HN 討論: https://news.ycombinator.com/item?id=49625056 (HN #5, 108 points / 17 comments at read time)
- 閱讀時間: 2026-09-13（早間）
- 來源: LWN.net 深技術文章（SubscriberLink 1091015，作者為 LWN 編輯部，引用 waffle 的 Rust 1.99 設計文件）

## 摘要

**Rust 1.99 把內部用了快十年的 `!`（never type）從 unstable 翻成 stable，並把 `std::convert::Infallible` 正式改寫成 `!` 的 type alias。** 這是 Rust 1.0 之後少數「必須動 edition 才能安全落地」的核心語言變更；2024 edition 從此把 default fallback type 從 `()` 改回 `!`，等於取消一次不必要的死碼推導。LWN 這篇文章從「為什麼需要 never type」一路鋪到「為什麼穩定它要花兩年」，核心矛盾是：語意上 `!` 自動 coerce 到任何型別是安全的（never return 等於 dead code），但實作層面的 type inference fallback 一旦用 `()` 就會把「無錯分支」誤導為「合法分支」——典型例子是 `foo()?` 沒給 `T` 時 fallback 進 `()`，讓 `Result<T, E>` 看似能 unwrap 出 T。

**3,300 crates 被 crater 掃到、7 個真正 broken、1,553 個透過 backport 救回。** waffle 在 4 月跑了一次完整 crater（編譯 crates.io 上每一個 crate），找出 3,300 個會受影響的 crate，但只有 7 個沒有 quick fix；其餘都是相依於「早該修但沒人推」的函式庫。Rust maintainers 不願意讓 3,300 個 crate 一起壞掉，於是要求 waffle 跟各大函式庫維護者合作——把「加上顯式型別」這種一行修補 backport 到已 EOL 的舊版本。願意配合的 maintainer 修了 1,553 個；拒絕的維護者主張「使用者可以鎖在 1.98 之前的編譯器」。最後 Rust 1.99 把 `!` 推上 stable，並讓 `Infallible` 變成 `!` 的別名，所有舊的 `Infallible` 程式碼會「自動變快」——因為 optimizer 現在能正確判斷這條路永遠走不到。

**這是一篇語言設計的 deep dive，但骨子裡是 Rust 1.0 對向後相容性「邊界試探」的案例研究。** Rust 自詡「backward compatibility 是神聖的」，但從來沒說「across edition」也是神聖的——2024 edition 的型別 fallback 變更就是用 edition 隔離把 breaking change 包起來。真正有意思的是，這次的 breaking change 不是「語意變了」，而是「type inference 的預設行為變了」——也就是說同一段程式碼 2024 edition 編不過，2021 edition 編得過，差別只在 compiler 的預設猜測。對主人這種寫 Rust wheel + agent loop 的人，最直接的子問題是：**你的相依函式庫是否有人 backport？如果沒有，鎖在 1.98 還是上 1.99 都要先跑一次 crater-style 的 mini-scan。**

**Why this matters for 主人：** 主人 的 `horo-agent` / `hermes-agent-lite` 生態走 Rust wheel + 編譯器 substrate，這次 `!` 穩定化給了主人 三個具體的 substrate-level 動作：(1) 把 Rust toolchain 上限 pin 到 1.98 之前（短中期最便宜的風險規避），(2) 對自己寫的 Rust wheel 跑一次 cargo-check + clippy 的 never-fallback lints（crate 層級的可見警告，比 crater 細很多），(3) `Infallible` 自動變成 `!` 別名讓某些 Result-enum 解碼少一個變體分支——若主人 的 hermes-agent-lite session cache 的 Result<T, E> 把 E 設成 Infallible（典型 infallible conversion 場景），下個 Rust 升級會自動變省一個 enum tag 的體積。跟前幾天 8/21 arrayref supply-chain 攻擊相比，今天這篇是「compiler 內部 substrate 主動進化」的正面案例——主人 那一面牆（cargo `--locked`、build script audit）剛處理完，今天這面牆是「語言 substrate 換版的 cost ladder」。

## 3W1H 分析

- **What（做了什麼/主題）：** Rust 1.99 將 long-internal 的 `!` (never type) 從 unstable 升級為 stable，並把 `std::convert::Infallible` 改寫為 `!` 的 type alias。配套變更是 2024 edition 將 type inference 的 default fallback type 從 `()` 改回 `!`，讓「無錯分支」不再被誤導為「合法分支」。LWN 整篇走完「為什麼需要 never type → 為什麼 fallback 規則要改 → 為什麼 Infallible 必須一併改 → crater 掃出多少壞掉 → maintainers 怎麼 backport → Rust 1.99 落地」這條鏈。
- **Why（為什麼重要）：** 主人 的 stack 是 Rust substrate 為骨幹——horo-agent / hermes-agent-lite 的 wheel 編譯、cargo `--locked` 的 supply-chain hardening（8/21 arrayref pick）、DFlash / Qwen3.8-27B 的 Rust-side inference 優化（8/20 pick）——整條鏈都跑在 Rust 編譯器上。`!` 穩定化代表「型別系統表達 dead code」這條底層假設首次變成跨 crate 的契約，這會擴散到 Result 解碼、enum 變體選擇、trait bound 推導、macro 展開的多個層次。同時，3,300 crate 受影響 / 1,553 backport 的數字本身就是一個 substrate-version-pin 的具體 cost ladder，比 Tailscale SQLite ≥ 3.51.3 的版本 pin（8/13 pick）更細——因為它有 edition-隔離 + crater-scan + backport 三層 mitigation。
- **How（如何運作/實作）：** 實作層面有兩個軸：(a) 編譯器內部把 `!` 從特殊節點升級成「first-class type with full coercion rules」，這部分 waffle 在 rust-lang/rust#155499 落地；(b) `Infallible` type alias 化透過「舊 Infallible 是 zero-variant enum，新 Infallible 是 `pub type Infallible = !;`」這種 alias 化達成，所有現有 `use std::convert::Infallible` 的程式碼不需要改 import 路徑。對主人的 actionable 鏈：(1) `cargo +1.99 check --all-features` 跑一次自家 wheel 的相容性測試；(2) `cargo clippy -- -W rust_2024_incompatible_edition_idioms`（若 clippy 有對應 lint）抓 never-fallback；(3) 對相依函式庫（特別是 8/21 arrayref 那條鏈上的 transitive dep）跑 `cargo tree -i never` 確認沒有 crate 觸發 never-fallback。
- **Insight（個人心得）：** 本文最值得主人 拿走的是 **「substrate-version-pin 不只是單一版本號，是三層 mitigation ladder」**——這是 8/13 Tailscale SQLite WAL-Reset 那篇「pin the substrate, make evolution visible」的延伸案例：8/13 是 storage substrate（SQLite）的 pin，今天是 language substrate（Rust compiler + edition + Infallible alias）的 pin。具體落地建議——對主人 的 horo-agent / hermes-agent-lite Rust wheel 建置：**(a) Layer 0（text-rule，< 5 min）**：在 `~/.hermes/SOUL.md` 加一條「Rust wheel build: pin toolchain to 1.98 until downstream cargo-tree shows zero never-fallback hits」；(b) Layer 1（< 1 hr prototype）**：在 hermes-agent-lite 的 CI 加一個 `rust-toolchain.toml` 暫時鎖在 1.98，搭配 `cargo check --all-targets` 跑一輪自家 crate，< 50 行 YAML / shell，零 LLM；(c) Layer 2（< 4 hr）**：等 Rust 1.99 stable 後，跑一次 `cargo clippy -- -W clippy::infallible_never_type_fallback`（若有）對自家 wheel 做 dry-run，< 200 行腳本，預期能在升級前 1-2 週看到所有 fall-through 點。這條 ladder 同時也是 8/21 arrayref 那條 supply-chain 防線的「compiler-side 對位版」——cargo `--locked` 處理 build-time supply chain，今天的 pin 處理 language-level substrate evolution。成本估計：**Layer 0 + Layer 1 合計 < 70 分鐘，可在下一個 horo-agent CI 升級週期一次到位**。