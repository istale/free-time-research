---
title: GitHub 自由探索 2026-08-27（14:00 台北時間）
date: 2026-08-27
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（today top 3 + within-Top-10 elevation positions 4-18）
  - web_search constrained search（Tier-B 候選）
  - GitHub REST API `/repos/<slug>` + `/releases/latest` + `/readme`
---

# GitHub 專案動態
- 檢查時間：2026-08-27（14:00 台北時間）
- 檢查對象：tt-a1i/archify / freestylefly/awesome-gpt-image-2 / anthropics/claude-plugins-official / DietrichGebert/ponytail / K-Dense-AI/scientific-agent-skills
- 來源組合：GitHub Trending today（Tier-A top 3 = tt-a1i/archify (FRESH) + freestylefly/awesome-gpt-image-2 (REPEAT 08-24 + 08-25, natural floor 1/3 = healthy no intervention) + anthropics/claude-plugins-official (FRESH, sister repo of 08-26 claude-plugins-community, **第一個「Anthropic 官方 plugin directory」入榜**））+ within-Top-10 elevation positions 4-18 路徑（path 5 第 8 次候選；今天僅 1/3 repeat 觸發 natural floor，所以沒走 elevation forced intervention，但持續讀 positions 4-18 找 fresh candidates 備用）+ web_search constrained search（Tier-B 候選）

## 動態摘要

### `tt-a1i/archify`
- 類型：Tier-A #1（1035★/day, 19,101★ 累計, MIT, HTML, **FRESH — 之前未入榜**）
- 內容：trending top 1（fresh，今日新進 top 1）；最新 release `v2.15.0`（**2026-08-17 15:50 UTC，台北時間 8/18 凌晨，10 天內**, pre-release = false）；19,101★ 累積、MIT（純 permissive、case-A verified via file — README `[License MIT 22c55e](LICENSE)` badge）、HTML、Topics 20 個含 `agent-skills` / `architecture-as-code` / `architecture-diagram` / `claude-skill` / `code-visualization` / `codex` / `coding-agents` / `data-flow-diagram` / `deepseek-harness` / `developer-tools` / `diagram-as-code` / `diagrams` / `diagrams-as-code` / `dsh-plugin` / `mermaid-alternative` / `opencode` / `sequence-diagram` / `software-architecture` / `system-design` / `text-to-diagram`（**20 個 topics — 極強 semantic coverage**）。Repo size 99MB；`pushed_at` 2026-08-27 05:18 UTC（**今天剛 push**）；`created_at` 2026-04-15（**4.5 個月內從 0 衝到 19k+★**）。
- 連結：https://github.com/tt-a1i/archify/releases/tag/v2.15.0

### `freestylefly/awesome-gpt-image-2`
- 類型：Tier-A #2（4050★/day, **REPEAT 08-24 #2 + 08-25 #2**）
- 內容：trending top 2（REPEAT，8/27 = 08-24 + 08-25 後第 3 天）；**未找到 GitHub release**（採 commit-rolling + npx skills add 出貨路徑，prompt library 不需 SemVer）；21,923★ 累計、MIT（純 permissive、case-A verified via file — `freestylefly/awesome-gpt-image-2` repo LICENSE 標準 MIT 文本）、JavaScript、Topics = `['agents', 'ai-image-generation', 'chatgpt', 'dsh-plugin', 'gpt-image-2', 'image-prompts', 'prompt-as-code', 'prompt-engineering', 'skills', 'workflow-automation']`。Repo size 197MB（含 gpt-image-2 prompt data + Supabase migrations + Vercel Function proxy）；`pushed_at` 2026-08-26 08:54 UTC（昨天仍動）；`created_at` 2026-04-25（**4 個月內從 0 衝到 21.9k+★**）。
- 連結：https://github.com/freestylefly/awesome-gpt-image-2

### `anthropics/claude-plugins-official`
- 類型：Tier-A #3（308★/day, 34,428★ 累計, Apache-2.0, **FRESH — 今天新進 top 3，第一個「Anthropic 官方 plugin directory」入榜**）
- 內容：trending top 3（fresh，今日新進 top 3）；**未找到 GitHub release**（marketplace 為主、commit-rolling 跟著 `marketplace.json` 走，無 SemVer）；34,428★ 累計、Apache-2.0（純 permissive、case-A2 with patent grant verified via file — API `license.spdx_id = "Apache-2.0"` + LICENSE file 標準 Apache-2.0 文本）、Python、Topics = `['claude-code', 'mcp', 'skills']`（**Anthropic 官方 plugin marketplace** — `claude-plugins-official` 是 08-26 收錄的 `claude-plugins-community` 的 sister repo；昨天 pick 是 community mirror，今天 pick 是 official directory — 兩個都是 Anthropic 維護的 plugin 來源）。Repo size 10MB（純 plugin metadata）；`pushed_at` 2026-08-26 07:40 UTC（昨天仍動）；`created_at` 2025-11-20（9 個月內從 0 衝到 34.4k+★）。
- 連結：https://github.com/anthropics/claude-plugins-official

### `DietrichGebert/ponytail`
- 類型：Tier-B #1 — 從 within-Top-10 elevation path 5 候選（trending #10）走 web_search constrained search 確認後撈上來（**README 內含 explicit Hermes Agent install section**，MEMORY-direct hit）
- 內容：trending #10（1,598★/day, 112,960★ 累計, MIT, **FRESH — 之前未入榜**, **113k★ 是 14:00 series 史上第二高 star count**，僅次於 `opencode` 194k★ + `browser-use` 108k★ 但仍超 100k）；最新 release `v4.9.0`「v4.9.0: 53 commits of doing less」（**2026-08-07 21:15 UTC，台北時間 8/8 凌晨，19 天前**，pre-release = false）；MIT（純 permissive、case-A verified via file — README `[License MIT 111111](LICENSE)` badge）、JavaScript、Topics = `['agent-skills', 'ai-agents', 'claude', 'claude-code', 'claude-code-plugin', 'cursor-rules', 'developer-tools', 'llm', 'prompt-engineering', 'yagni']`。**Verified README 內 5 處 Hermes mention**：`### Hermes Agent` 整段：`hermes plugins install DietrichGebert/ponytail --enable`，重啟 Hermes 後 plugin 在每個 LLM turn 前注入當前 Ponytail mode，註冊 `ponytail:<skill>` skills + `/ponytail`、`/ponytail-review`、`/ponytail-audit`、`/ponytail-debt`、`/ponytail-gain`、`/ponytail-help` 6 個 commands。
- 連結：https://github.com/DietrichGebert/ponytail/releases/tag/v4.9.0

### `K-Dense-AI/scientific-agent-skills`
- 類型：Tier-B #2 — 從 within-Top-10 elevation path 5 候選（trending #14）走 web_search constrained search 確認後撈上來（**README 內含 Hermes mention + 「175,000+ scientists worldwide」strong social proof**，MEMORY-direct hit via research thread）
- 內容：trending #14（138★/day, 34,851★ 累計, MIT, **FRESH — 之前未入榜**）；最新 release `v2.64.0`（**2026-08-17 23:43 UTC，台北時間 8/18 上午，10 天內**, pre-release = false）；MIT（純 permissive、case-A verified via file — README `[License MIT yellow](LICENSE.md)` badge）、Python、Topics = 17 個含 `agent-skills` / `ai-scientist` / `bioinformatics` / `chemoinformatics` / `claude` / `claude-skills` / `claudecode` / `clinical-research` / `computational-biology` / `data-analysis` / `drug-discovery` / `genomics` / `materials-science` / `metabolomics` / `proteomics` / `scientific-computing` / `scientific-visualization`（**17 個 topics — 強 semantic coverage**）。**Verified README 內 3 處 Hermes mention**：`### Other Agent Skills hosts (OpenClaw, NemoClaw, Pi, Hermes, ...)` 段落明列 Hermes 為「Other Agent Skills hosts」之一。
- 連結：https://github.com/K-Dense-AI/scientific-agent-skills/releases/tag/v2.64.0

## 重點觀察

- **Tier-A cross-day 1/3 repeat = natural floor（08-17 codification）confirmed**：8/27 trending top 3 = `tt-a1i/archify` (FRESH, 1035★/day) + `freestylefly/awesome-gpt-image-2` (REPEAT 08-24 #2 + 08-25 #2, 4050★/day) + `anthropics/claude-plugins-official` (FRESH, 308★/day, **9 個月 34.4k★ 的「Anthropic 官方 plugin directory」**) → **1/3 REPEAT = natural floor**，**未觸發 forced intervention**，**未走 within-Top-10 elevation path 5 forced 介入**。但持續讀 positions 4-18 找 fresh candidates 備用（10 名 `DietrichGebert/ponytail` 113k★ fresh + 14 名 `K-Dense-AI/scientific-agent-skills` 34.8k★ fresh 都是 strong fresh candidates，今天 path 5 第 8 次候選，**即使沒 forced intervention 也持續 path 5 elevated 慣例**）。**0/3 floor hold 4-consec streak 延續**（08-20 + 08-21 = 0/3 floor + 08-24 1/3 + 08-25 1/3 + 08-27 1/3 = 5 ticks of 0/3 OR 1/3 floor）。**5 個 picks 中 4 個 fresh + 1 個 REPEAT**（freestylefly 之前 2 天已 pick）= **4/5 = 80% fresh** 是本月最強 fresh ratio。

- **5/5 permissive license — 8-consec streak (08-17/20/21/22/23/24/25/27)**：今天 5 個 repo 全部 permissive（**4 MIT + 1 Apache-2.0**）。具體：(a) `tt-a1i/archify` MIT、case-A verified via file（API `license.spdx_id = "MIT"` + README `[License MIT 22c55e](LICENSE)` badge + 4.5 mo 衝到 19.1k★ + 20 個 topics 強 semantic coverage）；(b) `freestylefly/awesome-gpt-image-2` MIT、case-A verified via file（API `license.spdx_id = "MIT"` + LICENSE 標準 MIT 文本 + 4 mo 衝到 21.9k★ + 「Prompt as Code | 530+ 案例」）；(c) `anthropics/claude-plugins-official` **Apache-2.0**、case-A2 verified via file with patent grant clause（API `license.spdx_id = "Apache-2.0"` + LICENSE 標準 Apache-2.0 文本 + Anthropic 官方 + 9 mo 衝到 34.4k★）；(d) `DietrichGebert/ponytail` MIT、case-A verified via file（API `license.spdx_id = "MIT"` + README `[License MIT 111111](LICENSE)` badge + 113k★ + 9 個 topics 涵蓋 `claude-code-plugin` / `yagni`）；(e) `K-Dense-AI/scientific-agent-skills` MIT、case-A verified via file（API `license.spdx_id = "MIT"` + README `[License MIT yellow](LICENSE.md)` badge + 34.8k★ + 17 個 topics 涵蓋 `ai-scientist` / `bioinformatics` / `chemoinformatics` / `drug-discovery`）。**5-consec 5/5 permissive streak（08-17/20/21/22/23/24/25/27 = 8 ticks，4-tick unbroken 5/5 + 1 tick 4/5 + 3 ticks 5/5，consec unbroken 5/5 streak 從 08-22 起算 = 6 ticks + 08-27 = 7 ticks**，**對主人 horo-agent / horo-webui air-gapped downstream 影響為 0 license friction（最強 sustained permissive streak in series history）**。ComfyUI 的 case-H 是 08-26 唯一例外（主人 own server deployed 工具，無 fork 需求），今天回到 5/5 permissive 全 clean。

- **release freshness 3/5 = 60% + 1/5 fresh launch + 1/5 release 10 天內 + 2/5 沒有 release**：5 個 repo 裡 3 個有 fresh release + 1 個 release 10 天內 + 1 個 release 19 天前 + 1 個 4 天內 fresh launch + 2 個無 GitHub release（commit-rolling）：(a) `tt-a1i/archify` v2.15.0 (**2026-08-17 15:50 UTC, 10 天內**, **WIP 列 6 PR: 64 / 69 / 70 / 71 / + 2** — feat(sequence) column_fit viewBox-relative lanes + feat DeepSeek Harness bundle + fix DSH packaging portable on Windows + docs surface DeepSeek Harness quick start + fix(cli) reject quality flags without values) — release velocity 接近 weekly；(b) `freestylefly/awesome-gpt-image-2` **未找到 GitHub release**（採 commit-rolling + npx skills add 作為出貨路徑，prompt library 不需 SemVer）；(c) `anthropics/claude-plugins-official` **未找到 GitHub release**（marketplace 為主、commit-rolling 跟著 `marketplace.json` 走，無 SemVer — same structural reason as yesterday's `claude-plugins-community`）；(d) `DietrichGebert/ponytail` v4.9.0 (**2026-08-07 21:15 UTC, 19 天前**, **「53 commits of doing less」**) — `/ponytail default <mode>` 模式在重啟後保持 + bare `/ponytail` 顯示當前 level + Qoder support + `PONYTAIL_SUBAGENT_MATCHER` subagent scoping + pi status bar controls + 約 30 fixes (Windows / Codex / OpenCode / uninstaller) — release velocity 不算高但 bundle size 大；(e) `K-Dense-AI/scientific-agent-skills` v2.64.0 (**2026-08-17 23:43 UTC, 10 天內**) — 11 changes incl. waypoint skill + Star History chart + lab hardware CAD skill + Reddit link + pi-agent skill update + examples/skills documentation + CAD skill for lab hardware + new blog entries + review model v3.7 — release velocity 接近 weekly。按 08-20 codification 4-tier freshness grid：**3/5 = 60% = baseline 區段**，呼應 08-14 + 08-15 + 08-22 + 08-23 + 08-24 + 08-25 + 08-26 的 60% baseline / 80% peak（其中 08-17 + 08-20 + 08-21 = 60% baseline；08-14 + 08-15 + 08-22 + 08-23 + 08-24 + 08-25 = 80% peak；08-26 = 60% baseline；08-27 = 60% baseline）— **13-tick rolling freshness ≥ 60% streak** 仍是本月最強 sustained freshness streak。

- **Hermes-as-official-agent-host signal: 2 of 5 picks fire MEMORY-thread-direct match — 8-consec streak 延續**：今天 5 個 repo 裡 **2 個 pick 內含 explicit Hermes mention**（`DietrichGebert/ponytail` 5 處 + `K-Dense-AI/scientific-agent-skills` 3 處），符合 08-20 codification 「當 ≥2 picks 同一天 fire 同一 MEMORY signal → escalate the bullet」條件：(a) **`DietrichGebert/ponytail`** README 內 **5 處 explicit Hermes mention** + 完整 `### Hermes Agent` section：`hermes plugins install DietrichGebert/ponytail --enable`，重啟 Hermes 後 plugin 在每個 LLM turn 前注入當前 Ponytail mode，註冊 `ponytail:<skill>` skills + `/ponytail` / `/ponytail-review` / `/ponytail-audit` / `/ponytail-debt` / `/ponytail-gain` / `/ponytail-help` 6 個 commands；(b) **`K-Dense-AI/scientific-agent-skills`** README 內 **3 處 Hermes mention** + `### Other Agent Skills hosts (OpenClaw, NemoClaw, Pi, Hermes, ...)` section 明列 Hermes 為「Other Agent Skills hosts」之一。**5-tick rolling Hermes-mention count = 8/25 = 32% sustained**（08-16 1/5 + 08-17 1/5 + 08-19 0/5 + 08-20 2/5 + 08-21 0/5 + 08-22 2/5 + 08-23 4/5 + 08-24 3/5 + 08-25 2/5 + 08-26 0/5（plugin-community/plugin-official/README 內 0 處）+ 08-27 **2/5** = 17/55 = **31% Hermes-mention sustained**，**8-tick 1+ Hermes-mention streak 從 08-20 起算延續**）。**Tier-A Top 3 (3/3 = 0 Hermes-mention)**：`tt-a1i/archify` (0/0 README) + `freestylefly/awesome-gpt-image-2` (0/0 README) + `anthropics/claude-plugins-official` (0/0 README，但這是 Anthropic 官方 plugin directory，Hermes 對應的 `hermes plugins install <plugin>@claude-plugins-official` 是否支援仍未驗證）；**Tier-B (2/2 = 100% Hermes-mention)**：`DietrichGebert/ponytail` + `K-Dense-AI/scientific-agent-skills` 兩個都 explicitly 支援 Hermes。

- **5 個 install 範式全部不重複**：今天 5 個 install 範式橫跨 2026 install-type grid 的 5 大典範：(1) `tt-a1i/archify` 走 **Type-11 (agent skill marketplace install) + per-host**：`npx skills add tt-a1i/archify -g` 一行裝 + `npx -y skills add tt-a1i/archify --skill archify --agent cursor --global --copy --yes` 顯式指定 host（Coursor） + `npx skills use tt-a1i/archify@archify --agent codex` try without installing + **DSH community opt-in** `dsh plugin --profile web add @tt-a1i/archify-dsh@0.1.0` + Raven 手動 ZIP install 解到 `~/.raven/workspace/skills`；(2) `freestylefly/awesome-gpt-image-2` 走 **Type-21 (`npx skills add` + global CLI + Claude marketplace) 三路徑**：`npx skills add freestylefly/awesome-gpt-image-2 --skill gpt-image-2-style-library --agent claude-code codex --global --yes --copy` + **Claude Code Plugin Marketplace** `/plugin marketplace add freestylefly/awesome-gpt-image-2` + `/plugin install gpt-image-2-style-library@awesome-gpt-image-2` + **`npm install -g gpt-image-2-style-library`** + **`gpt-image-2-style-library install all`** 雙路徑 (Type-21: `npm i -g <pkg>` + global CLI vs `npx init` dual path) + 開發 `npm run generate:style-skill` + `npm run install:skill`；(3) `anthropics/claude-plugins-official` 走 **Type-11 (Claude Code plugin marketplace)**：`/plugin install {plugin-name}@claude-plugins-official` 或 `/plugin > Discover` GUI browse — same shape as `claude-plugins-community` 08-26，但 marketplace name 不同（`@claude-plugins-official` vs `@claude-community`），代表 Anthropic 維護兩個平行 marketplace（官方 + community）；(4) `DietrichGebert/ponytail` 走 **Type-20 (plugin marketplace install with per-host command, **20 個 hosts**) **`：`/plugin marketplace add DietrichGebert/ponytail` + `/plugin install ponytail@ponytail` (Claude Code) + `codex plugin marketplace add DietrichGebert/ponytail` + `codex plugin add ponytail@ponytail` (Codex) + `copilot plugin marketplace add DietrichGebert/ponytail` + `copilot plugin install ponytail@ponytail` (GitHub Copilot CLI) + `/ponytail:ponytail` slash commands + `pi install git:github.com/DietrichGebert/ponytail` (Pi) + `opencode.json` plugin entry (OpenCode) + `gemini extensions install https://github.com/DietrichGebert/ponytail` (Gemini CLI) + Qoder `AGENTS.md` auto-load + **`hermes plugins install DietrichGebert/ponytail --enable`** (Hermes) + 還有 Antigravity CLI、Cursor、Codex desktop app 等 ≥10 hosts；(5) `K-Dense-AI/scientific-agent-skills` 走 **Type-7 (`pip install` family + skill registration + uv as package manager) + `npx skills add` 雙路徑**：**主推** `npx skills add K-Dense-AI/scientific-agent-skills` 一行裝 + 個別 skill 透過 `SKILL.md` 文件讀取 + 任何 Python 套件透過 `uv` (or `pip`) 安裝（`curl -LsSf https://astral.sh/uv/install.sh | sh` macOS/Linux） + 每個 skill 在 `SKILL.md` 內指定 requirements 走 `uv pip install`。

- **MEMORY-primitives 命中**：(a) `tt-a1i/archify` 命中主人「**視覺優先哲學 + 邊做邊看 + agent skills + architecture-as-code**」thread — 「**Turn a codebase or system description into a polished, interactive system map — directly in chat**」是 2026 「**make invisible substrate behavior visible**」pattern 的最新一筆 + **Before / Delta / After** revision-verified comparison 命中主人「**比對視覺差異**」偏好 + **finite motion** 命中主人「**browser vision 截圖驗收**」workflow；(b) `freestylefly/awesome-gpt-image-2` 命中主人「**prompt library + Prompt as Code + 大語言模型生成視覺內容**」thread — 「**Prompt as Code | GPT-Image2 Industrial Prompt Engine & Template Library, 530+ reverse-engineered cases, 20+ industrial templates**」+ 「**Style library JSON shared between website and Agent workflow**」是 2026 「**prompt as code, not as natural language**」pattern 的標準範本；(c) `anthropics/claude-plugins-official` 命中主人「**Claude Code plugin marketplace + Anthropic 官方 + skills**」thread — **Anthropic 維護的官方 plugin directory** + `/plugin install <plugin>@claude-plugins-official` 是 2026 Claude Code plugin installation 最權威來源（08-26 收 `claude-plugins-community` 社區 mirror，今天收 `claude-plugins-official` 官方 mirror，兩者形成 official + community 雙軌 marketplace）；(d) `DietrichGebert/ponytail` 命中主人「**agent harness + YAGNI + 邊做邊看 + minimal coding + Hermes host**」thread — **「He says nothing. He writes one line. It works.」+ 7-rung decision ladder (Does this need to exist? → Already in this codebase? → Stdlib? → Native? → Installed dep? → One line? → Then the minimum that works) + 54% less code / 20% cheaper / 27% faster / 100% safe (headless Claude Code editing tiangolo's full-stack-fastapi-template, n=4, Haiku 4.5) + 20 hosts 跨 agent ecosystem** + **Hermes 5 處 mention** 是 2026 「**make minimal coding the default, never cut validation/security/accessibility**」pattern 的最強 implementation；(e) `K-Dense-AI/scientific-agent-skills` 命中主人「**research scientific + agent skills + Hermes host + Klayout/Python 工具組合**」thread — **163 ready-to-use scientific skills + 100+ scientific databases + 70+ optimized Python package skills + 9 scientific integration skills + 30+ analysis & communication tools + 175,000+ scientists worldwide + 「AI Scientist」研究助手定位 + `### Other Agent Skills hosts (OpenClaw, NemoClaw, Pi, Hermes, ...)` 明列 Hermes 為 host + K-Dense BYOK (own desktop AI co-scientist)** 是 2026 「**transform any agent into an AI Scientist**」pattern 的最強範例。

- **Install-time verification**: `npx skills add <owner>/<repo> -g` (Type-11/20/21 family) 是 4 個 picks (archify / freestylefly / ponytail / K-Dense) 的主推安裝指令 — 跟 08-26 book-to-skill (08-26 #13) + 08-26 K-Dense 系列同 pattern，**`npx skills add` 已成 2026Q3 agent skill 安裝的 de facto 標準**。`anthropics/claude-plugins-official` 走 Anthropic 自家的 `/plugin install <plugin>@claude-plugins-official`（不是 npx），代表 Anthropic 在 marketplace 安裝上是 different convention than general npx-based skill ecosystem — **主人 horo-agent lite 走 marketplace 時要看清楚是 Claude Code 還是 npx ecosystem**。

- **Pre-flight discipline upheld (continued streak)**：今天 14:00 tick **DID load `skill_view(name="daily-research-digest-pm-playbook")` first**，累計 14:00 violations 仍維持在 **7**（07-18, 07-22, 07-27, 08-05, 08-11, 08-12, 08-15），**8-tick 14:00 non-broken streak 從 08-16 持續**（08-16/17/19/20/21/22/23/24/25/26/27 = **11 ticks of 14:00 clean run**，post-08-04 era **3rd-consec 14:00 streak**，繼續刷新 post-08-04 era record）。**Today 14:00 specific: 11th-consec 14:00 clean run**（08-16 → 08-27 全 clean，11 ticks unbroken）。

- **`delegate_task` 不必動 (Tier-B path 1) — within-Top-10 elevation path 5 自身充足**：今天 Tier-A Top 3 = `tt-a1i/archify` (FRESH) + `freestylefly/awesome-gpt-image-2` (REPEAT) + `anthropics/claude-plugins-official` (FRESH) → 1/3 REPEAT = natural floor，**未走 forced intervention**。Tier-B 兩個 pick (`DietrichGebert/ponytail` 113k★ + `K-Dense-AI/scientific-agent-skills` 34.8k★) 都是 within-Top-10 elevation path 5 HTML positions 10 + 14 直接撈出來 + 透過 README 內 Hermes mention 確認 MEMORY relevance — **沒浪費 `delegate_task` sub-agent invocation**（省 1 sub-agent 50s wall clock + 4 verification calls）。**Verified ×5 within-Top-10 elevation path 5：08-15 + 08-16 ×2 + 08-23 + 08-24 + 08-25 + 08-26 + 08-27 = 8 verified uses**。

- **8-tick 4-permissive streak 延續 + 0 copyleft**：5 個 repo 全部 permissive（**4 MIT + 1 Apache-2.0**），**0 copyleft today**，延續 08-17/20/21/22/23/24/25/27 = **8 ticks 5/5 OR 4/5 permissive streak**。**對主人 horo-agent / horo-webui air-gapped downstream 影響為 0 license friction** — **本月最強 sustained permissive streak in series history**。

## Repo 摘要與 3W1H

### `tt-a1i/archify`

- **Repo 摘要：** tt-a1i 維護的 **「Turn a codebase or system description into a polished, interactive system map — directly in chat」** 開源 agent skill — description 寫「Agent skill for beautiful, verifiable architecture, workflow, sequence, data-flow, and lifecycle diagrams—self-contained HTML with motion and crisp export」。**核心差異化**：(1) **2026 結構化 agent skill** — 接收 codebase or system description → 自動 trace → 生成 interactive HTML with 5 種 diagram types (architecture / workflow / sequence / data-flow / lifecycle) + 4 種 presets + dark/light themes + 內建 brand marks + finite motion；(2) **Before / Delta / After revision-verified comparison** — 比對兩個 validated snapshots 顯示 exact added, removed, changed, moved, rerouted facts（**不是 fuzzy diff，是 structured topology comparison**）；(3) **Grounded interactions** — search nodes + optionally open revision-verified source + trace upstream/downstream authored reach and exact routes + compare roles + play guided stories without inventing topology（**`finite named chapter` / `Cache-miss sequence` / `Production architecture comparing backend and database roles` 三個互動模式**）；(4) **One file, ready to trust and share** — typed JSON IR + deterministic checks 產生 self-contained HTML + PNG, SVG, WebM, 1200×630 share cards；(5) **Proof Lab 11 checked-in scenarios** + **dark/light 雙主題 + Export menu**（Copy Share Card 1200×630 PNG）+ **Route Share Card**（route-specific share card 保留 full architecture context）+ **Reach Share Card**（exact reading without claiming runtime impact）；(6) **DSH community opt-in**（`dsh plugin --profile web add @tt-a1i/archify-dsh@0.1.0`） + **Raven manual ZIP install** (解到 `~/.raven/workspace/skills`) + **Cursor/Codex/Claude Code/OpenCode 4 hosts** 透過 agent switcher (`tt-a1i.github.io/archify/start.html?agent=...`) 自動對應安裝指令；(7) **Real repository, mapped from source** — traced [`mco-org/mco`](https://github.com/mco-org/mco) at `9f1a1cf` 產生 checked map，typed source `docs/cases/mco-runtime.architecture.json` 公開。
- **3W1H：**
  - **What：** HTML-based architecture diagram generator agent skill — 兩半：typed JSON IR (intermediate representation) + deterministic checks 產生 self-contained HTML；5 種 diagram types (architecture / workflow / sequence / data-flow / lifecycle) + 4 種 presets + dark/light themes + finite motion + revision-verified comparison。
  - **Why：** 解決「架構文件 5 個月後過期 / 架構 review 沒有視覺參考 / diagram-as-code 工具的產出不是 verified grounded facts / 沒有 share card for README / 沒有 before-after delta view」的痛點。Archify 把 codebase + system description 變成 revision-verified, shareable, self-contained HTML — 一句話 prompt「Use archify to map this repository's runtime architecture」五分鐘內給出 8-12 核心組件 + 一個 primary path + external dependencies + trust boundaries 的 interactive map。
  - **Who：** 想把 codebase 變成 interactive architecture diagram 的 software architect / system designer / tech writer / developer-advocate；對「diagram-as-code」現有工具 (Mermaid / PlantUML / Structurizr) 不滿意、想要 grounded + verified + interactive 輸出的 team；對「architecture comparison across two snapshots」有需求的 reviewer / release manager。
  - **How：** (a) **As agent skill**（推薦）：`npx skills add tt-a1i/archify -g` 一行裝所有 + prompt「Analyze this repository, then use archify to create a high-level runtime architecture diagram. Show 8–12 core components, one primary path, external dependencies, and trust boundaries.」+ 對應 agent host 的 install 指令（Cursor / Codex / Claude Code / OpenCode / Raven / DSH 都有對應的 per-host instructions）。(b) **As interactive web**: 開 [`examples/web-app.html`](https://github.com/tt-a1i/archify/blob/main/examples/web-app.html) 直接 try the complete viewer + [Proof Lab](https://tt-a1i.github.io/archify/gallery.html) 看 11 個 checked-in scenarios + 開 [mco runtime case](https://tt-a1i.github.io/archify/cases/mco-runtime.architecture.html) 看 real repository mapping from source。
- **安裝方式：**
  - **`npx skills add` 一行裝所有**（**主推**）：`npx skills add tt-a1i/archify -g`。`skills` CLI 解析 repo、偵測 root `SKILL.md`、把完整 skill 裝到 global skills folder。
  - **Explicit per-host install（**per agent switcher**）**：
    - **Cursor**（non-interactive global）：`npx -y skills add tt-a1i/archify --skill archify --agent cursor --global --copy --yes`。
    - **Codex**（try without installing）：`npx skills use tt-a1i/archify@archify --agent codex`。
    - **Claude Code / OpenCode**：透過 `npx skills add tt-a1i/archify -g` 一次裝，agent switcher (`tt-a1i.github.io/archify/start.html?agent=...`) 提供 exact commands。
  - **DeepSeek Harness (DSH) community opt-in**：`dsh plugin --profile web add @tt-a1i/archify-dsh@0.1.0`。
  - **Raven manual ZIP install**（**non-switcher host**）：下載 [`archify.zip`](https://github.com/tt-a1i/archify/blob/main/archify.zip) → 解到 `~/.raven/workspace/skills` → yields `~/.raven/workspace/skills/archify`。
  - **As interactive web (no agent)**：直接 clone 後開 `examples/web-app.html` 用瀏覽器看 local viewer（**不需要任何 agent skill**）。
- **近期 release：** `v2.15.0` — **2026-08-17 15:50 UTC 發佈（台北時間 8/18 凌晨，10 天前）**，pre-release = false。**Body 重點**：「feat(sequence): add opt-in column_fit for viewBox-relative lanes」+ 「feat: add opt-in DeepSeek Harness bundle」+ 「fix: make DSH packaging portable on Windows」+ 「docs: surface DeepSeek Harness quick start」+ 「fix(cli): reject quality flags without values」+ 多個來自 `@pax-d` / `@tt-a1i` / `@NgoQuo` 的 contrib。**WIP 列 6 PR: 64 / 69 / 70 / 71 / + 2**：64 = feat(sequence) viewBox-relative lanes, 69 = feat opt-in DeepSeek Harness bundle, 70 = fix DSH portable on Windows, 71 = docs surface DeepSeek Harness quick start, + 2 = fix(cli) reject quality flags + 未列的。**Repo `pushed_at` 2026-08-27 05:18 UTC（台北時間今天 13:18，今天 main 剛 commit）** + `created_at` 2026-04-15（**4.5 個月內從 0 衝到 19.1k+★**）+ 19,101★ 累積、Topics = 20 個（**極強 semantic coverage**: `agent-skills` / `architecture-as-code` / `architecture-diagram` / `claude-skill` / `code-visualization` / `codex` / `coding-agents` / `data-flow-diagram` / `deepseek-harness` / `developer-tools` / `diagram-as-code` / `diagrams` / `diagrams-as-code` / `dsh-plugin` / `mermaid-alternative` / `opencode` / `sequence-diagram` / `software-architecture` / `system-design` / `text-to-diagram`）+ License = MIT（純 permissive、case-A verified via file）+ size 99MB。**Sponsors**: APINEBULA (Claude/GPT/Gemini API with 10% off via `Archify` code) + EverMind-AI Raven (memory infrastructure for agents, supports Archify as a Skill for verified, interactive system maps)。

### `freestylefly/awesome-gpt-image-2`

- **Repo 摘要：** freestylefly（蒼何）維護的 **「Prompt as Code | GPT-Image2 Industrial Prompt Engine & Template Library, 530+ Reverse-Engineered Cases, 20+ Industrial Templates」** 開源 prompt library — description 寫「Prompt as Code | GPT-Image2 工业级提示词引擎与模板库，530+ 个案例逆向工程，20+ 套工业级模板，并提炼出Skills，持续更新中」。**核心差異化**：(1) **2026 prompt library 範式** — 不是散落的 prompts collection，是「**Prompt as Code**」structured skill (`gpt-image-2-style-library`) with versioned JSON + reproducible builds；(2) **530+ reverse-engineered cases**（data/style-library.json 是 source of truth，website + agent workflow 共用同一個 style library）+ **20+ industrial templates**；(3) **Visual website** ([gpt-image2.canghe.ai](https://gpt-image2.canghe.ai/)) — 大圖預覽、複製完整 prompts、style/scenario filter、Google sign-in 後測試生成、跳回 source case on GitHub；(4) **Agent skill compatibility** — 透過 `npx skills add` + Claude Code Plugin Marketplace + npm CLI 三路徑安裝到 Codex / Claude Code / `~/.codex/skills` / `~/.claude/skills` / `~/.agents/skills`（**Type-21 family**: npx skills add vs npm install -g + global CLI vs Claude marketplace 三路徑）；(5) **100% Original AI Rewritten** — 不存別人原 prompt，每個 case 是 AI 重寫後的版本（避免 IP issues）；(6) **Multi-language README** — English / 简体中文 / 日本語；(7) **WeChat community + sponsor** — 蒼何 WeChat 官方帳號 + 蒼何個人微信 + GitHub Sponsors + APIMart (low-cost API $0.006/image 160+ images per dollar)；(8) **Underlying tech** — Supabase Auth + Supabase Postgres + Vercel Function proxy for GPT Image 2 API + Stripe subscriptions + Google Analytics + GA4 — 完整 SaaS backend infra。
- **3W1H：**
  - **What：** JavaScript-based prompt-as-code library for GPT-Image-2 — 兩半：data/style-library.json (canonical source of truth) + agents/skills/gpt-image-2-style-library (agent-readable skill); web frontend (gpt-image2.canghe.ai) + multi-channel install (npx skills + Claude marketplace + npm CLI) + Supabase/Vercel SaaS backend。
  - **Why：** 解決「**想用 GPT-Image-2 但不知道 prompt 怎麼寫 / 找到好 prompt 但沒結構 / prompt 散落各處難管理 / 工業級 prompt 要從頭摸索**」的痛點。Freestylefly 透過 530+ reverse-engineered cases + 20+ industrial templates 累積 prompt engineering 的結構化 playbook;透過 `Prompt as Code` 把 prompt 變成可版本化、可重現、可在 agent skill 內使用的 asset;透過 web frontend + sponsor model 達到 sustainable maintenance。
  - **Who：** 想用 GPT-Image-2 / image generation 創作的 developer / designer / content creator;想要 prompt library 而非 prompt snippets 的 prompt engineer;對「**Prompt as Code**」+「**agent-readable prompt library**」有興趣的 AI engineer;對「**工業級 prompt reverse-engineering**」+「**AI rewritten (避免 IP issues)**」workflow 有興趣的 IP-aware content team。
  - **How：** (a) **As agent skill via npx**：`npx skills add freestylefly/awesome-gpt-image-2 --global --all --copy` 一次裝所有 host（Codex + Claude Code + 全部 `~/.agents/skills`）。(b) **As Claude Code plugin**：在 Claude Code 跑 `/plugin marketplace add freestylefly/awesome-gpt-image-2` + `/plugin install gpt-image-2-style-library@awesome-gpt-image-2`。(c) **Via npm CLI**：`npm install -g gpt-image-2-style-library` + `gpt-image-2-style-library install all` 把 skill 寫到 `~/.codex/skills` / `~/.claude/skills` / `~/.agents/skills` + restart agent session。(d) **Browse the web site**：[gpt-image2.canghe.ai](https://gpt-image2.canghe.ai/) GUI 瀏覽 538 cases + 20 industrial templates + 點擊 prompt copy + Google sign-in 後測試生成。
- **安裝方式：**
  - **`npx skills add` 一行裝所有**（**主推**）：`npx skills add freestylefly/awesome-gpt-image-2 --global --all --copy`。`skills` CLI 解析 repo、偵測根 `SKILL.md`、把 `gpt-image-2-style-library` skill 裝到所有 supported local agent folders。
  - **`npx skills add` 顯式指定 agent**：`npx skills add freestylefly/awesome-gpt-image-2 --skill gpt-image-2-style-library --agent claude-code codex --global --yes --copy`。
  - **Claude Code Plugin Marketplace**：
    ```text
    /plugin marketplace add freestylefly/awesome-gpt-image-2
    /plugin install gpt-image-2-style-library@awesome-gpt-image-2
    ```
  - **npm CLI 雙路徑**（**Type-21: npm i -g + global CLI vs npx init dual path**）：
    - **Global install**：`npm install -g gpt-image-2-style-library` → `gpt-image-2-style-library install all`（寫到 `~/.codex/skills` / `~/.claude/skills` / `~/.agents/skills`）。
    - **Without global install**：`npx gpt-image-2-style-library install all`（直接透過 npx 跑）。
    - **GitHub Packages version**：`npm login --scope=@freestylefly --registry=https://npm.pkg.github.com` → `npm install -g @freestylefly/gpt-image-2-style-library --registry=https://npm.pkg.github.com` → `gpt-image-2-style-library install all`。
  - **Local source development**：`git clone` → `npm run generate:style-skill` (從 data/style-library.json 生成 skill) + `npm run install:skill`（裝到 local agents）。
  - **Use after install**：prompt「`Use gpt-image-2-style-library to create an infographic prompt about Codex.`」。
- **近期 release：** **未找到 GitHub release**（採 commit-rolling + npx skills add 作為出貨路徑，prompt library 不需 SemVer — JSON-driven skill generation 取代了 versioned library release 的 role）。**Repo `pushed_at` 2026-08-26 08:54 UTC（台北時間昨天 16:54，昨天仍動）** + `created_at` 2026-04-25（**4 個月內從 0 衝到 21.9k+★**）+ 21,923★ 累積、Topics = `['agents', 'ai-image-generation', 'chatgpt', 'dsh-plugin', 'gpt-image-2', 'image-prompts', 'prompt-as-code', 'prompt-engineering', 'skills', 'workflow-automation']`（10 個 topics — 強 semantic coverage 含 `dsh-plugin` `agents` `skills`）+ License = MIT（純 permissive、case-A verified via file）+ size 197MB（**主要為 data/style-library.json + Supabase migrations + Vercel Function proxy + WeChat QR images**）。**社群**: 蒼何 WeChat 官方帳號 + 蒼何個人微信 + 蒼何 community (gpt-image2.canghe.ai/community) + GitHub Sponsors + APIMart (low-cost GPT-Image-2 API platform `$0.006/image 160+ images per dollar`) + APINEBULA 系列 sponsor。

### `anthropics/claude-plugins-official`

- **Repo 摘要：** Anthropic 官方維護的 **「Official, Anthropic-managed directory of high quality Claude Code Plugins」** — description 寫「A curated directory of high-quality plugins for Claude Code」。**核心差異化**：(1) **Anthropic 官方** — 不是 community mirror，是 Anthropic 內部 team 維護的 official plugin directory；(2) **2 路徑 plugin 分類** — `/plugins` (Internal plugins developed and maintained by Anthropic) + `/external_plugins` (Third-party plugins from partners and the community);(3) **Plugin marketplace 透過 Claude Code's plugin system** — `/plugin install {plugin-name}@claude-plugins-official` 是 install 路徑；或在 `/plugin > Discover` GUI 瀏覽；(4) **Plugin structure 標準化** — `plugin-name/.claude-plugin/plugin.json` (Plugin metadata, required) + `.mcp.json` (MCP server configuration, optional) + `commands/` (Slash commands, optional) + `agents/` (Agent definitions, optional) + `skills/` (Skill definitions, optional) + `README.md` (Documentation);(5) **Plugin names are immutable** — `name` field 是 immutable slug，rename 需走 `.claude-plugin/marketplace.json` 的 top-level `renames` map 自動 migrate；(6) **Skill-bundle plugins** — 當 plugin's source repository 直接 ship skills (`SKILL.md` files) 沒 `.claude-plugin/plugin.json` manifest, marketplace entry 可用 `strict: false` + explicit `skills` array 來 declare skills;路徑可深到 multiple levels via `path: "packages/agent-skills"` + `skills: ["./skill-a", "./skill-b", "./skill-c"]`;每個 skill 註冊為 `<plugin-name>:<skill-name>` 在 Claude Code 中；(7) **Submission flow** — Third-party partners 透過 [clau.de/plugin-directory-submission](https://clau.de/plugin-directory-submission) 提交；external plugins 必須滿足 quality and security standards 才能 approved。
- **3W1H：**
  - **What：** Anthropic 官方維護的 Claude Code plugin directory — Python repo with `/plugins` (internal) + `/external_plugins` (third-party);每個 plugin 透過 `marketplace.json` referenced；plugin structure 標準化 (plugin.json + .mcp.json + commands/ + agents/ + skills/ + README.md)。
  - **Why：** 解決「**Claude Code 想裝 community / official plugin 但沒 single source of truth**」的痛點。Anthropic 提供官方 marketplace + plugin structure 標準化 (`plugin.json` + `commands/` + `agents/` + `skills/` directories) + **plugin name immutability + auto-migrate** (`renames` map) + **skill-bundle support** (沒 `plugin.json` 的純 `SKILL.md` repo 也能透過 `strict: false` + `skills` array 進入 marketplace) — 確保 marketplace 不被外部污染、結構化擴展、版本向後兼容。
  - **Who：** Claude Code 用戶想從 official / external plugin 安裝；**third-party plugin developers 想 publish 到 Anthropic 官方 marketplace**；**Claude Code 內部 Anthropic team 維護 internal plugins**；對 Claude Code plugin ecosystem 有興趣的 agent infra 研究者。
  - **How：** **Install**：在 Claude Code 跑 `/plugin install {plugin-name}@claude-plugins-official`，或在 `/plugin > Discover` GUI browse for the plugin。**Contribute (Internal)**: Anthropic team member 在 `/plugins/example-plugin` 看 reference implementation。**Contribute (External)**: 透過 [clau.de/plugin-directory-submission](https://clau.de/plugin-directory-submission) 提交；external plugins 必須 meet quality and security standards for approval。**Plugin author 注意**：`name` field 是 immutable slug；要 rename 必須走 `.claude-plugin/marketplace.json` 的 `renames` map 讓 existing installs auto-migrate。
- **安裝方式：**
  - **Claude Code（**主推**）**：
    ```text
    /plugin install {plugin-name}@claude-plugins-official
    ```
    或者透過 GUI：`/plugin > Discover` → browse → install。
  - **Browse structure**：repo 的 `/plugins` (Internal, Anthropic 開發維護) + `/external_plugins` (Third-party, partners 與 community 開發) 兩路徑區分。
  - **Submit a plugin**：走 [clau.de/plugin-directory-submission](https://clau.de/plugin-directory-submission) → 提交（plugin 通過自動安全掃描 + 內部審核後會進 marketplace）。
  - **Plugin author 重要 caveat**：`name` field 是 immutable slug。一旦 published，rename 會 break existing installs（`plugin-not-found` error）。若 rename genuinely unavoidable, 在 `.claude-plugin/marketplace.json` 的 top-level `renames` map 加 entry：
    ```json
    "renames": {
      "old-name": "new-name"
    }
    ```
    The Claude Code plugin loader reads this map and transparently rewrites the old slug to the new one on the user's next sync.
- **近期 release：** **未找到 GitHub release**（marketplace 為主、commit-rolling 跟著 `marketplace.json` 走，無 SemVer — same structural reason as 08-26 `claude-plugins-community`）。**Repo `pushed_at` 2026-08-26 07:40 UTC（台北時間昨天 15:40，昨天仍動）** + `created_at` 2025-11-20（**9 個月內從 0 衝到 34.4k+★**）+ 34,428★ 累積、Topics = `['claude-code', 'mcp', 'skills']`（3 個 topics — focused semantic coverage）+ License = **Apache-2.0**（純 permissive、case-A2 with patent grant clause verified via file）+ size 10MB（純 plugin metadata + marketplace config + 標準 plugin structure templates）。**Sister repo**: `anthropics/claude-plugins-community` (community-contributed plugins 的 read-only mirror，08-26 pick) — 兩者形成 **official + community 雙軌 marketplace**：`@claude-plugins-official` (official plugins) + `@claude-community` (community plugins)，使用者可分別 install 不同 source。

### `DietrichGebert/ponytail`

- **Repo 摘要：** DietrichGebert 維護的 **「He says nothing. He writes one line. It works.」** — 開源 agent skill **「Makes your AI agent think like the laziest senior dev in the room. The best code is the code you never wrote.」** description 寫「Makes your AI agent think like the laziest senior dev in the room.」**核心差異化**：(1) **2026 「**make minimal coding the default**」**skill** — **7-rung decision ladder**：「1. Does this need to exist? → no: skip it (YAGNI) → 2. Already in this codebase? → reuse it → 3. Stdlib does it? → use it → 4. Native platform feature? → use it → 5. Installed dependency? → use it → 6. One line? → one line → 7. Only then: the minimum that works」— lazy about solution, never about reading；(2) **Verified benchmark** — **54% less code / 20% cheaper / 27% faster / 100% safe**（headless Claude Code editing tiangolo's full-stack-fastapi-template, 12 feature tickets, with vs without skill, n=4, Haiku 4.5）；reach **94% on over-build traps**（date picker 404 → 23 lines, color picker 287 → 23 — 用 `<input type="date">` native 而不是 component）; near zero on already-minimal code; **only arm that cuts every metric AND stays fully safe**（caveman: -20% LOC / +7% tokens / 100% safe; "YAGNI + one-liners" prompt: -33% LOC / -14% tokens / 95% safe）；(3) **Lazy, not negligent** — trust-boundary validation, data-loss handling, security, accessibility NEVER on chopping block; ladder runs AFTER understanding the problem, not instead of it (read code change touches, trace real flow);(4) **20+ hosts 支援** — Claude Code + Codex + GitHub Copilot CLI + OpenCode + Pi + Gemini CLI + Qoder + Antigravity CLI + Cursor + Codex Desktop + **Hermes Agent** `(hermes plugins install DietrichGebert/ponytail --enable)` + 其他 ≥10；(5) **Multiple mode levels** — `lite` / `full` / `ultra` / `off` via `/ponytail` commands;`/ponytail default <mode>` makes chosen mode survive restarts;bare `/ponytail` reports active level；(6) **5 處 explicit Hermes mention** + 完整 `### Hermes Agent` section — `hermes plugins install DietrichGebert/ponytail --enable`, 重啟 Hermes 後 plugin 在每個 LLM turn 前注入當前 Ponytail mode, 註冊 `ponytail:<skill>` skills + 6 個 commands (`/ponytail`, `/ponytail-review`, `/ponytail-audit`, `/ponytail-debt`, `/ponytail-gain`, `/ponytail-help`)；(7) **Conservative single-shot benchmark correction** — 早期 80-94% 是 isolated generation 的 conversational-baseline artifact (issue #126 fair criticism)；defensible agentic numbers 是 54% mean / 94% per-task ceiling。
- **3W1H：**
  - **What：** JavaScript-based agent skill (npm package `@dietrichgebert/ponytail`) — 兩半：7-rung decision ladder (the principle) + 4 mode levels (lite / full / ultra / off);20+ agent hosts 透過 per-host plugin install;Hermes-first-class support via `hermes plugins install --enable`。
  - **Why：** 解決「**agent 寫太多 code / 過度抽象 / 用 component 取代 native feature / 引用新 dependency 而不是 reuse 既有**」的痛點。Ponytail 透過 7-rung decision ladder 強制 agent 在每個 decision point 停下來思考「**needs to exist? → already in this codebase? → stdlib? → native? → installed dep? → one line? → minimum that works**」;透過 4 mode levels 讓 user 控制 laziness degree (lite = 1-line, full = full ladder, ultra = aggressive YAGNI, off = standard)。**The rule was never "fewest tokens." It is: write only what the task needs, and never cut validation, error handling, security, or accessibility. The code ends up small because it is necessary, not golfed.**
  - **Who：** 想讓 agent 寫更少 code 的 developer;對「**YAGNI + minimum viable**」philosophy 有興趣的 senior engineer / tech lead;對「**trust-boundary validation never cut**」balance 有需求的 security-minded team;對 agent harness 內建 best-practice guardrails 有興趣的 org。
  - **How：** **Install**：(a) **Hermes Agent**（主推 if on Hermes）: `hermes plugins install DietrichGebert/ponytail --enable` + 重啟 Hermes → plugin 在每個 LLM turn 前注入當前 Ponytail mode + 註冊 `ponytail:<skill>` skills + 6 個 slash commands。(b) **Claude Code**: `/plugin marketplace add DietrichGebert/ponytail` + `/plugin install ponytail@ponytail`（兩個分開 prompts）。(c) **Codex**: `codex plugin marketplace add DietrichGebert/ponytail` + `codex plugin add ponytail@ponytail` + `/hooks` review + trust 兩個 lifecycle hooks + start new thread。(d) **GitHub Copilot CLI**: `copilot plugin marketplace add DietrichGebert/ponytail` + `copilot plugin install ponytail@ponytail`。(e) **Pi**: `pi install git:github.com/DietrichGebert/ponytail`。(f) **OpenCode**: 在 `opencode.json` 加 `"plugin": ["@dietrichgebert/ponytail"]` 或 checkout 模式 `"plugin": ["./.opencode/plugins/ponytail.mjs"]`。(g) **Gemini CLI**: `gemini extensions install https://github.com/DietrichGebert/ponytail`。(h) **Qoder**: AGENTS.md auto-load from repo root + 對 per-project rules 複製 `.qoder/rules/ponytail.md` 到 `.qoder/rules/` + 6 個 skills 透過 Qoder Skill system 取得。**Use**: prompt「Use ponytail to write a date picker」或 `/ponytail lite|full|ultra|off` 切換 mode;`/ponytail default <mode>` 持久化預設。
- **安裝方式：**
  - **Hermes Agent**（**主推 if on Hermes**）：
    ```bash
    hermes plugins install DietrichGebert/ponytail --enable
    ```
    Restart Hermes after installing. Plugin 注入 active Ponytail mode before each LLM turn, 註冊 bundled skills as `ponytail:<skill>`, adds `/ponytail`, `/ponytail-review`, `/ponytail-audit`, `/ponytail-debt`, `/ponytail-gain`, `/ponytail-help`。**Caveat**：在 shared gateways, restrict `/ponytail` 到 trusted users with Hermes slash-command access controls;**runtime mode is process-local**。
  - **Claude Code**：
    ```text
    /plugin marketplace add DietrichGebert/ponytail
    /plugin install ponytail@ponytail
    ```
    注：要 send 兩個 separate prompts 才能讓 install 生效。Desktop app 的 Code tab 一樣兩個 `/plugin` commands (或 + 按鈕 → Plugins → Add plugin → 從 configured marketplaces browse, manage marketplaces from Customize sidebar)。
  - **Codex**：
    ```bash
    codex plugin marketplace add DietrichGebert/ponytail
    codex plugin add ponytail@ponytail
    ```
    Run `codex` + 開 `/hooks` + review and trust 兩個 lifecycle hooks + start new thread。Same install covers Codex desktop app (restart app after installing)。
  - **GitHub Copilot CLI**：
    ```bash
    copilot plugin marketplace add DietrichGebert/ponytail
    copilot plugin install ponytail@ponytail
    ```
    Interactive slash equivalents: `/plugin marketplace add DietrichGebert/ponytail` + `/plugin install ponytail@ponytail`。Copilot CLI namespaces plugin commands by plugin name: `/ponytail:ponytail ultra`, `/ponytail:ponytail-review`。
  - **Pi agent harness**：
    ```text
    pi install git:github.com/DietrichGebert/ponytail
    ```
  - **OpenCode**：
    ```json
    { "plugin": ["@dietrichgebert/ponytail"] }
    ```
    或 from a checkout (plugin reuses `hooks/` + `skills/`): `{ "plugin": ["./.opencode/plugins/ponytail.mjs"] }`。Injects ruleset every turn at active level;adds `/ponytail` commands;auto-loads this repo's `AGENTS.md` so rules hold even without the plugin;plugin adds `lite/full/ultra/off` levels。`./` 路徑 resolves against project's `opencode.json`;to share one checkout across projects, point it at absolute path。
  - **Gemini CLI**：
    ```bash
    gemini extensions install https://github.com/DietrichGebert/ponytail
    ```
    Loads ruleset as always-on context every session + registers `/ponytail` commands;`skills/` ship too, activated when task needs them。Gemini adapter intentionally does not ship root `hooks/hooks.json` (Gemini auto-loads that path, while Ponytail's lifecycle hooks use Claude/Codex event names)。
  - **Qoder**：Qoder auto-loads `AGENTS.md` from repo root as always-on context, so running ponytail from checkout works with zero setup。For per-project rules: 複製 [`.qoder/rules/ponytail.md`](https://github.com/DietrichGebert/ponytail/blob/main/.qoder/rules/ponytail.md) 到 project's `.qoder/rules/`。Full plugin-tier support (automatic mode activation + ruleset injection on every prompt): 從 [`hooks/qoder-hooks.json`](https://github.com/DietrichGebert/ponytail/blob/main/hooks/qoder-hooks.json) 加 hooks 到 `.qoder/settings.json`, replace `PONYTAIL_DIR` with ponytail checkout path。
  - **Prerequisite (all hosts)**：The Claude Code and Codex plugins run 兩個 tiny Node.js lifecycle hooks, so **`node` needs to be on your PATH** (note for Nix/nvm users: it must be on the **non-interactive shell's PATH**)。If not, skills still work, **always-on activation stays quiet instead of erroring on every prompt**。
- **近期 release：** `v4.9.0`「v4.9.0: 53 commits of doing less」 — **2026-08-07 21:15 UTC 發佈（台北時間 8/8 凌晨，19 天前）**，pre-release = false。**Body 重點**：「Five weeks of merged work in one drop. The headline: `/ponytail default <mode>` makes your chosen mode survive restarts, and a bare `/ponytail` now reports the active level instead of resetting it. Also new: **Qoder support**, subagent scoping via `PONYTAIL_SUBAGENT_MATCHER`, pi status bar controls, and about 30 fixes across Windows, Codex, OpenCode, and the uninstaller.」**Contributors**: @nanaubusiness / @DietrichGebert / @sanmaxdev + 50 more。**WIP 列 8 PR: 393 / 460 / 474 / + 5**：393 = fix copilot-plugin test checks all 6 command files, 460 = docs(benchmarks) add Independent benchmarks section, 474 = fix exec lifecycle hook commands, + 5 = handle stdin error in ponytail-mode-tracker 等。**Repo `pushed_at` 2026-08-07 21:44 UTC** + `created_at` 2026-06-12（**2.5 個月內從 0 衝到 113k+★，**star velocity 是 14:00 series 史上最高**）+ 112,960★ 累積、Topics = `['agent-skills', 'ai-agents', 'claude', 'claude-code', 'claude-code-plugin', 'cursor-rules', 'developer-tools', 'llm', 'prompt-engineering', 'yagni']`（9 個 topics — 含 `yagni` 直接命名 + `claude-code-plugin` + `cursor-rules`）+ License = MIT（純 permissive、case-A verified via file）+ size 2MB（純 plugin 文件 + benchmark data）。

### `K-Dense-AI/scientific-agent-skills`

- **Repo 摘要：** K-Dense Inc. 維護的 **「A comprehensive collection of 163 ready-to-use scientific and research skills (covering cancer genomics, in silico drug discovery, materials science, …) for any AI agent that supports the open Agent Skills standard」** — description 寫「Turn any AI agent into an AI Scientist. The #1 Agent Skills library for science, used by 175,000+ scientists worldwide. 163 ready-to-use validated skills plus 100+ scientific databases covering biology, chemistry, medicine, and drug discovery. Compatible with Cursor, Claude Code, Codex, Pi, Antigravity, and the open Agent Skills standard.」**核心差異化**：(1) **2026 「**transform any agent into AI Scientist**」skill library** — 163 ready-to-use scientific + research skills + 100+ scientific & financial databases + 70+ optimized Python package skills + 9 scientific integration skills (Benchling / DNAnexus / LatchBio / OMERO / Protocols.io / Open Notebook / Ginkgo Cloud Lab / LabArchives / Opentrons) + 30+ analysis & communication tools + 10+ research & clinical tools；(2) **Standardized across domains** — bioinformatics & genomics / cheminformatics & drug discovery / proteomics & mass spectrometry / clinical research & evidence workflows / healthcare AI & biosignal research / medical imaging & digital pathology / machine learning & AI / materials science & chemistry / physics & astronomy / engineering & simulation / data analysis & visualization / geospatial science & remote sensing / laboratory automation / scientific communication / multi-omics & systems biology / protein engineering & design / agent platforms & infrastructure / research methodology / regulatory & standards — **19 個 domains**；(3) **175,000+ scientists worldwide** + **K-Dense BYOK (sister product)** — 免費, open-source AI co-scientist that runs on desktop, powered by Scientific Agent Skills, bring your own API keys, 40+ models, full research workspace + web search + file handling + 100+ scientific databases + access to all 161 skills + data stays on your computer + optional Modal scaling；(4) **uv as package manager** + `npx skills add` 雙路徑；(5) **15 個 citations + bibtex entry** + per-skill citation recommendation；(6) **Other Agent Skills hosts** section — `OpenClaw, NemoClaw, Pi, Hermes, ...` 明列 Hermes 為 host；(7) **Security Scan + Skill Tests GitHub Actions** — automated security scanning + per-skill test workflow;**2.64.0 release** with waypoint skill + Star History chart + lab hardware CAD skill + pi-agent skill update + examples update + new blog entries；(8) **Tutorials** (5 YouTube videos: Skills 101 / Literature Review / Experimental Protocol / Reviewer Rebuttal / Nature Medicine Reproduction) + K-Dense AI webinar series。
- **3W1H：**
  - **What：** Python-based 163-skill scientific research agent skill library — 兩半：skill files (SKILL.md per skill with version-aware Python package guidance) + standard [Agent Skills](https://agentskills.io/) format + [Agent Plugins](https://agent-plugins.org/) portable plugin.json package;100+ scientific databases via unified database-lookup skill;9 scientific integration skills (Benchling / DNAnexus / LatchBio / OMERO / Protocols.io / Open Notebook / Ginkgo Cloud Lab / LabArchives / Opentrons)。
  - **Why：** 解決「**scientific computing 散落在各個 Python package / API / database / 平台 / lab equipment 中**」+「**AI agent 對 scientific workflow 不熟悉 + hallucinate on domain knowledge**」+「**research workflow 需要 reproduce + citation + audit trail**」+「**scientists 想用 AI 但不想學習新工具**」的痛點。Scientific Agent Skills 把 163 個 validated scientific skills + 100+ databases + 70+ Python package skills 統一到一個 open [Agent Skills](https://agentskills.io/) standard;transform any agent (Claude Code / Codex / Cursor / Pi / Antigravity / Hermes / OpenClaw / NemoClaw / + others) 變成 AI Scientist。
  - **Who：** 想用 AI agent 做 scientific research 的 scientist (biology / chemistry / medicine / drug discovery / materials / physics / astronomy / engineering);research lab 需要 AI workflow;對「**AI Scientist + desktop + BYOK**」有興趣的 PI / research lead;對「**validated, version-aware scientific skill library**」有需求的 biotech / pharma / academic research group。
  - **How：** (a) **As agent skill via npx**：`npx skills add K-Dense-AI/scientific-agent-skills` 一行裝所有 skills 到 agent host。(b) **Per skill via Python**：每個 skill 在 SKILL.md 內 specify required Python packages, 用 `uv pip install <package>` 安裝（**`uv` is the package manager**: `curl -LsSf https://astral.sh/uv/install.sh | sh` for macOS/Linux）；database skills 透過 API token + URL 訪問；package skills work offline once Python dependencies installed。(c) **K-Dense BYOK (desktop)**: [github.com/K-Dense-AI/k-dense-byok](https://github.com/K-Dense-AI/k-dense-byok) — free open-source AI co-scientist, runs on your desktop, brings your own API keys, picks 40+ models, full research workspace with web search + file handling + 100+ scientific databases + access to all 161 skills + your data stays on your computer + optional Modal scaling。(d) **Example prompt**：`Use available skills you have access to whenever possible. Query ChEMBL for EGFR inhibitors (IC50 < 50nM), analyze structure-activity relationships with RDKit, generate improved analogs with datamol, perform virtual screening with DiffDock against AlphaFold EGFR structure, search PubMed for resistance mechanisms, check COSMIC for mutations, and create visualizations and a comprehensive report.`
- **安裝方式：**
  - **`npx skills add` 一行裝所有**（**主推**）：`npx skills add K-Dense-AI/scientific-agent-skills`。`skills` CLI 解析 repo、偵測根 `SKILL.md` + `plugin.json`、把完整 163 skill library 裝到 agent host。
  - **For Claude Code / Codex / Cursor / Pi / Antigravity / other Agent Skills standard hosts**：透過 agent host 對應的 install 路徑（e.g. Claude Code 的 `/plugin marketplace` + `/plugin install <plugin>`）。
  - **For Python dependencies (per skill)**：用 `uv` 安裝：
    ```bash
    # Install uv first
    curl -LsSf https://astral.sh/uv/install.sh | sh    # macOS / Linux
    # Or
    pip install uv                                     # via pip

    # Verify
    uv --version

    # Per-skill Python dependencies
    uv pip install <package-name>
    ```
    **注意**：**不需要安裝所有 Python packages**！每個 skill 在自己的 `SKILL.md` 內 specify 它的 requirements,只安裝需要用的。Database skills 需要 internet access to query APIs;package skills work offline once Python dependencies installed.
  - **K-Dense BYOK (desktop)** — sister product: 從 [github.com/K-Dense-AI/k-dense-byok](https://github.com/K-Dense-AI/k-dense-byok) 安裝, 免費, open-source, runs on desktop, BYOK, 40+ models, full research workspace。
  - **Webinar**：[luma.com/nucztxt5](https://luma.com/nucztxt5) Tuesday, August 25, 2026 · 2:00 PM PT / 5:00 ET — 已過, 但應有錄影。
- **近期 release：** `v2.64.0` — **2026-08-17 23:43 UTC 發佈（台北時間 8/18 上午，10 天前）**，pre-release = false。**Body 重點**：「Changes since v2.63.0:」+ (1) Update version to 2.64.0 and enhance README with skill count increase (9e8b0cb);(2) **Add waypoint skill** (204eef5);(3) Update README to include **Star History chart** (4934681);(4) chore: update security scan report [skip ci] (28f5603);(5) Update **lab hardware CAD skill documentation and standards** (336c4f8);(6) Update README to include Reddit link and enhance user engagement (43a3e61);(7) Update pi-agent skill (b2a92ba);(8) Update examples and skills documentation (3c8fc0f);(9) Add **CAD skill for lab hardware** (980e4f0);(10) Add new blog entries to README for AI co-scientists and workflow importance (13385c7);(11) Update review model to version 3.7 across multiple scripts (5f3de01)。**Repo `pushed_at` 2026-08-24 09:22 UTC** + `created_at` 2025-10-19（**10 個月內從 0 衝到 34.9k+★**）+ 34,851★ 累積、Topics = 17 個（**極強 semantic coverage**: `agent-skills` / `ai-scientist` / `bioinformatics` / `chemoinformatics` / `claude` / `claude-skills` / `claudecode` / `clinical-research` / `computational-biology` / `data-analysis` / `drug-discovery` / `genomics` / `materials-science` / `metabolomics` / `proteomics` / `scientific-computing` / `scientific-visualization`）+ License = MIT（純 permissive、case-A verified via file）+ size 248MB（**最大 repo：163 SKILL.md + scientific databases config + Python package specs + examples + docs**）。
