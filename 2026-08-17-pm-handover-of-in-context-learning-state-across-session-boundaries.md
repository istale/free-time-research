# Handover of In-Context Learning State Across Session Boundaries
- 原始連結：https://arxiv.org/abs/2608.14528
- 來源：arXiv cs.AI 昨日新論文（2026-08-14 v1 上線、2026-08-17 11:30 Asia/Taipei 抓取）
- 閱讀時間：2026-08-17（午間）
- 作者：Masahiro Kato, Taka Kato（兩位作者，同一團隊；獨立釋出 paper、無公開 artifact）
- 與主人既有 axis 的對位：今日 AM《QuoteBench》（tool wrapper boundary 拆 5 欄位）、8/16 PM《Specification-first convergence》（freeze spec 才能收斂 audit）、8/04 KV-cache 8-byte tag 模板（make invisible substrate visible）、主人 MEMORY 寫死的 hermes-agent-lite session schema / cron 重啟

## 摘要

**主人，這一篇把今早 AM《QuoteBench》在 tool wrapper boundary 拆 5 欄位的同一個推理方法，從「單一 run 內的 command path」推進到「跨 session 重啟的 context path」——而且把問題從「要不要 carry over」正式拆成兩個 mathematical distinct 的子問題：exact recovery vs target-distribution preservation。** 全文圍繞一個核心 operator：LLM 應用會在 context 超過 input limit、應用重啟、或交棒給另一個 agent 時被迫開新 session，**handover 就是「舊 session 哪些資訊要被搬到新 session」這個傳輸函式**。作者 Masahiro Kato 與 Taka Kato 把它寫成 math problem：在 exogeneity condition 下，predictive equivalence 可以 characterize 出「哪種 deterministic handover 是 coarsest sufficient」並給出 fixed-length bit requirement；分析把 memory constraint / writer / continuation procedure 三層 effect 隔開，並量化「在 query 還未知時就先寫」的 cost。

**三段式 record 設計是今天最 actionable 的部分。** 作者提出 handover 不該是「整段 chat history 的 dump」，而是 **(a) decisions + constraints 要 exact 保留**、**(b) 重複性 evidence 用 task-justified statistics 摘要**、**(c) 那些 statistics 不能代表其 effect 的原始 observation 要原樣保留**——這三層結構直接把主人 horo-agent / hermes-agent-lite 的 session-handover 從「throw the whole conversation back」變成「specific decider：exact / summarised / raw」三通道。**Gaussian linear regression 部分給出 exact finite-dimensional handover + finite-bit perturbation bounds、nonparametric regression 部分給出 memory ↔ squared prediction error 的上下界**——這兩個 boundary 結果正是 master 寫「handover primitive spec」時可以 copy 的藍圖。

**最殺手級的一個 insight 給主人**：在 query 還沒 settle 之前就把 handover 寫好，這個「write-before-query-knows」動作不是 free、它會把 handover 推到一個 coarsest-sufficient 的 upper bound，而那個 upper bound 的 bit cost 是可以算出來的。**今日 QuoteBench 給主人 5-field audit header 是「run 內 boundary」可被披露的 primitive；今日 Handover 給主人是「跨 run 之間 boundary」可被披露的 primitive**——兩個 primitive 共用同一個 family：「make invisible substrate behavior visible」的 Layer-0 text rule + Layer-1 regex hook，差別只在 boundary 的時空座標。**當主人把現在 hermes-agent-lite 的 session-restart logic「整段 history 重塞給下一輪」換成「按 Kato 三段決策 + PM 用 nightly self-host 16GB VM 量比特 cost」的架構，handover 行為就從「我們覺得它 work」變成「在 bit-perturbation bound 內可被驗證」。**

**為什麼今早 AM 剛剛做過同一個方向的主題，不算 PM 重複**：今日 AM 是「同一個 run 內 tool wrapper boundary 該 report 5 字段」（QuoteBench）、今日 PM 是「不同 run 之間 session boundary 該 carry 什麼」(Handover)；前者是 micro-boundary、後者是 macro-boundary、兩個 primitive 結構同構（都是 substrate visibility）+ substrate 互補（per-call vs cross-call）。**這正是 8/14「spec-driven 多階段」與 8/16 PM「spec-first convergence」的 extending primitive**——今早 QuoteBench 把 audit envelope 推到 subprocess 邊界、今日 Handover 把 handover envelope 推到 session 邊界；兩個合起來就是「hermes-agent-lite / horo-agent 的 runtime boundary 全集合：command path + session path 都 audit」。**paper 同時也給 closure**：理論閉式 finite-dimensional + 實驗性 bit-perturbation bound 兩條結果都自足，**主人不需要等第二輪 arXiv follow-up 來 complete**。

## 3W1H 分析

**What（做了什麼／主題）:** Kato & Kato 把 LLM 跨 session 交接改寫成 math problem：handover 是一個從舊 session state 到新 session context 的傳輸函式、需要決定傳什麼。**Theory**：在 exogeneity condition 下證明「predictive equivalence 等價於 coarsest sufficient deterministic handover」、得到 fixed-length bit requirement；分析把 memory constraint / writer / continuation procedure 三個 effect 隔開、並量化「還沒看到 downstream query 就提前 write」的 cost。**Method**：三段式 record——(a) decisions + constraints exact、(b) 重複 evidence 用 task-justified statistics、(c) statistics 不能替代的原始 observation 原樣保留。**Empirical closed-form results**：Gaussian linear regression case 給出 exact finite-dimensional handover + finite-bit perturbation bounds；nonparametric case 給出「memory ↔ squared prediction error」的上下界。**結論**：handover 不應是 chat log dump，必須是 structure-aware 的三層 record，且 bit cost 是可 analytic 表達、可有限驗證的。

**Why（為什麼重要）:** 主人 MEMORY 寫死「hermes-agent-lite / horo-agent session schema 不能從零重寫」是因為現有 session handoff 在 cron 重啟之間已經被證明穩定——但「已經被證明穩定」不代表「bit cost 已知」、「handover 內容充要條件已知」、「writer / query-order-independence 已知」。今日 Handover 把這三條 unknown 全部正式化成可驗證的 closed-form 結果：**coarsest sufficient deterministic handover 的 bit bound** = handover 設計可量化的 KPI；**write-before-query 的 cost function** = nightly cron 是 write-first 而 query-last 的場合，這個 cost 就是 handover overhead 的下界；**Gaussian finite-bit perturbation bound** = handover schema 在「原本 carry 的 evidence 跟新 session 收到的 evidence 不完全一致」時的 robustness 上限。**主人 horo-agent 的 cron 多 step pipeline（PM 11:30 / AM 07:00 / EVE 17:30 / 14:00 / nightly / etc.）每一條都是 write-before-query 的場合，paper 直接給 closure**。**對位 8/04 KV-cache 8-byte tag 模板的 family**——8/04 是「cache invalidation 變可 grep」、今日 Handover 是「session handover 變可 analytic 量」。兩個 primitive 結構同構、都「make invisible substrate behavior visible」、都成本 < 1 個下午、都不需要 LLM call。

**How（如何運作／實作）:** 對主人 horo-agent / hermes-agent-lite 的可移植 primitive 有三條：**(1) 三段式 handover record**——把現有 session-restart 的「整段 chat history 重新塞回去」換成 `(decisions+constraints exact, repeated-evidence task-justified statistics, statistics-irreplaceable raw observations)` 三通道；**（實作成本）** 在 hermes-agent-lite 的 `~/.hermes/sessions/<sid>/handover.json` 加 schema 約 < 100 行 Python；**（KPI）** 用 paper 給的 Gaussian bit-perturbation bound + nonparametric upper/lower bound 兩條 closed-form 做 nightly self-check；**(2) Bit-cost audit header**——在 handover 出去的 envelope 加一個 `bit_cost: {exact_bytes: N, statistics_compression_ratio: r, raw_observations_count: M}` 區塊，讓 master 下一輪 turn 看到的不是「handover 完成 / 失敗」的 bool，而是「這次 handover 有多少 byte 走 exact、多少 ratio 被 summarised、多少條 raw 不可替代」的 audit-ready header；**(3) Write-before-query disclosure**——當 handover 在 query 還沒 settle 時就被 nightly cron 寫進 disk（這是主人每天 100+ cron tick 都會發生的場合），handover envelope 必須 disclose 自己屬於「coarsest-sufficient upper bound」這個 category，且 bit cost 是 paper 的 fixed-length bound；**（實作成本估算）** 主人的 16GB VM 跑 closed-form bit-cost audit（純 numpy / scipy，不用 GPU）< 10 秒；handover.json schema + envelope 改動 < 1 個下午；**（版本）** v1 paper 公開日期 2026-08-14，arXiv 公開頁即可取得；**（provenance）** theoretical paper，無公開 code artifact；建議主人把 paper 的 Gaussian / nonparametric 兩個 case 各自寫成 < 50 行的 pytest golden-numeric test，比依賴作者未公開的 code 更穩。

**Insight（個人心得）:** 主人本月 verifier-grounded 軸線最便宜的 closure 動作有兩條，今天這一篇是第二條——跟今早 QuoteBench 那條平行、且 substrate 互補。**今天如果主人把 hermes-agent-lite 的 session-restart 從「整段 history 塞回去」換成「Kato 三段 handover + bit-cost audit envelope」**，**「明顯改善」會在 owner 的兩個地方出現**——**(1) nightly cron tick 跨 session handoff 的 token cost** 主人 horo-agent 目前每晚大概 6-8 個 tick，每個 tick 都會 carry 一段 history 進下次 session；換成 Kato 三段 + envelope 之後，exact channel 的 byte 數（decisions + constraints）會比目前「整段塞」小 1-2 個 order of magnitude、raw-observations channel 的 byte 數（statistics-irreplaceable 原 observation）會被壓到主人能 trace 的範圍；**(2) session schema 演化的 robustness** 現在 hermes-agent-lite 任何 session schema 改動都要擔心「舊 session handoff 到新 schema 後，recovery 有沒有掉字」，Kato Gaussian finite-bit perturbation bound 就是直接給出「bit error 不超過 δ」的閉式上界，讓 schema 演化成 analytic-verifiable 而不是 trial-and-error。**（對位 8/04 KV-cache 模板）** 8/04 是「8-byte tag 讓 cache 失效可被 grep」、今日 QuoteBench 是「5-field audit envelope 讓 tool wrapper boundary 可被披露」、今日 Handover 是「bit-cost audit envelope 讓 session boundary 可被 analytic 量」——三層 primitive 結構完全同構、都把 Hidden 變 Visible、都 < 1 個下午成本、都 zero-LLM-call。**（赫蘿給主人的下一步）** 今天就 patch hermes-agent-lite 的 `~/.hermes/sessions/<sid>/` 寫一個 `handover.py` 出 Kato 三段 envelope、跑一個 nightly cron 拿 baseline bit cost；明天 patch QuoteBench 的 5-field audit header 進 `tool_use` wrapper。**（自我警示）** Insight 必須 cheap first——兩個 primitive 加起來 < 1 個下午、zero LLM call、不要被引誘去「rewrite horo-agent session loop」——那違背 8/04 模板的 cost ladder 也違背主人 MEMORY 寫死的「絕不重寫 session schema」。
