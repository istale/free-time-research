# Prime Intellect 開源 Prime Agent:RLM + Continual Harness 把 PTC 變成 production primitive
- 原始連結: https://github.com/PrimeIntellect-ai/prime-agent
- 官方部落格: https://www.primeintellect.ai/blog/rlm
- 閱讀時間: 2026-08-09（午間）
- 來源: GitHub Trending daily #1（9,153 stars / 882 forks，3 個月內從 0 衝上來）

## 摘要

**核心命題：把「Programmatic Tool Calling + Recursive Sub-agents + Continual Harness」三個 primitive 收進同一個 production agent 裡。** Prime Intellect（已在 Sync Labs / PRIME-RL 階段被主人記下的那家 RL infra 公司）把自家研究系列的「Recursive Language Model / RLM」做成可裝可跑的開源 coding agent，**三個月內 9.2K stars**，是目前 daily GitHub Trending #1。底層語言是 TypeScript，**建立在 [`pi`](https://github.com/earendil-works/pi) mono 之上**——pi 是 earendil-works 的 TUI agent 框架,Prime Agent 是 pi 的「self-improving + long-running」延伸。

**七個可以直接抄的 primitive。**(1) **persistent IPython = 內建 model tool**——不是「讓模型吐 JSON tool call,parse,執行」,而是「模型寫 Python 呼叫 file / shell / sub-agent / skill 全部走 IPython REPL」。**這跟主人今早讀的 PwC Bitter Lesson 論文(PTC 11/14 模型勝 JSON)是同一條 primitive**。(2) `rlm(...)` 內建 sub-agent 函式,**return result 而不是 return promise**——parent 在同一個 Python scope 拿到 child output,可以直接當變數用。(3) `/refine` 命令:review 當前 trajectory,**只改 supplemental harness state(skill / memory / subagent spec)、絕不改 immutable base system prompt**,refinement history 留 snapshot,支援 rollback。(4) skills 是**可 import 的 Python 套件**,不是 markdown 文字描述;內建 skill-creator 可把「重複發生的工作流」轉成 project-level 或 personal skill。(5) **daemon-backed sessions**:terminal 斷線,agent 還在跑;`prime-agent attach` 可重新接回。(6) **agent-to-agent direct communication**——running agents 之間可以互傳訊息,不必繞 user。(7) heartbeats / schedules / persistent goals / bounded autonomous mode:長任務跨 turn / 跨 session 不掉。

**為什麼這個 release 對主人 2026-08-09 此刻特別有意義。** 主人今早讀的 PwC Bitter Lesson 論文的兩個關鍵 prior art——**CodeAct 2024 跟 Lumer 2026 Recursive Agent Harnesses**——後者的作者 Ed Lumer **就是 PwC 這篇的共同作者**。換言之 PwC 那篇「PTC + recursive subagent spawns 全面贏 JSON」是 8 月初剛被提出來的研究 pattern,**Prime Agent 已經把這個 pattern 產品化 / 開源化**——而且是**同一週(8/8-8/9)release 跟 trending**。這不是巧合,是同一條 axis(agent 從 JSON-dispatch 轉向 code-as-interface + recursive subagent)上 research 跟 industry 同步收束的訊號。對主人正在做的 **horo-agent-lite / hermes-agent-lite** 來說,Prime Agent 是 8 月 9 日當下「這個 primitive 怎麼做成 production CLI」的現成 reference。

**對主人 16GB VM 跟 air-gapped downstream 的 hard rule 要特別小心的點。** README 自己寫得明白:**「Prime Agent executes model-generated Python and project commands with your user permissions. Its worker and kernel processes improve lifecycle isolation and recovery; they are not a security sandbox.」**——這跟主人 horo-agent 的 trust model 是同一個問題(主人 MEMORY 裡「以保守減法 + 端到端驗證落地」的硬規則,跟 Prime Agent「worker / kernel 改善 lifecycle 但不是 sandbox」是同一個 trade-off 表達方式)。**可借鏡的工程細節**:`prime-agent doctor [--fix]` 是個值得抄的 pattern——CLI 內建 self-diagnose / self-repair,而非把所有 background service 狀態檢查丟給外部工具。

## 3W1H 分析

- **What(做了什麼/主題)**: Prime Intellect 在 2026-05-08 開源 Prime Agent(8/9 trending #1,9,153 stars),定位是「self-improving RLM agent for coding workflows and long-running autonomous tasks」。技術 stack:TypeScript mono-repo(`packages/coding-agent` 為主、`prime-agent-runtime` 為 runtime layer),建立在 earendil-works 開源的 [`pi`](https://github.com/earendil-works/pi) TUI agent 之上。兩個第一階 primitive:**RLM(Recursive Language Model)— prompt-as-variable + `/sub-agent` 為 Python function call inside persistent IPython**;**Continual Harness — supplemental prompts / memories / skill descriptions / reusable subagent specifications 為 durable state,`/refine` 命令可做 evidence-backed small updates with snapshot rollback**。7 個 sub-primitive:persistent IPython、`rlm()` subagent、Continual Harness `/refine`、executable Python skills、daemon-backed sessions、agent-to-agent messaging、heartbeats/schedules/persistent goals/`/autonomous` 預算模式。安裝路徑:`curl -fsSL https://app.primeintellect.ai/prime-agent/install.sh | sh`(驗 SHA-256,裝 `prime-agent` CLI 到 PATH)。

- **Why(為什麼重要)**: 主人 7 天的早間摘要在「open-weights + agent + reasoning efficiency」軸上連續收束(7/22 Kimi-K3 routing → 7/24 Echo → 8/04 Kimi/GLM serving → 8/05 Shieldstral safety → 8/06 Castform RL post-training → 8/07 vLLM anatomy → 8/08 DeepSeek V4 Flash → **8/09 早 Bitter Lesson PTC 11/14 win**),每一篇都在加同一條 sub-axis 的資料點。**Prime Agent 在 8/9 衝上 trending #1 是這個 axis 第一個「research → production open-source」落地**——PwC 今早論文講的 PTC 跟 Lumer Recursive Agent Harnesses,Prime Agent 把它做成可 `curl install` 的 CLI。對主人 horo-agent / hermes-agent-lite 有三個直接含義:(a) **競爭訊號**——9,153 stars / 3 個月 / 同週 release,代表業界對「code-as-interface + recursive subagent + skill as Python module」這個 stack 押注,主人若不往這條路走,horo-agent 跟主人在 agent 軸的「保守減法」定位會被時代拋在後面;(b) **可借鏡的工程細節**——`/refine` 只改 supplemental、不改 base prompt、留 snapshot 可 rollback,這個 3-tier 信任模型正好對應主人「保守減法 + 端到端驗證」硬規則的精神;(c) **可借鏡的 skill 形式**——skill 是 importable Python package 而非 markdown 描述,跟主人 horo-agent 現有 SKILL.md(用 markdown 文字)是不同的 trade-off,**至少值得拿 Prime Agent 跟主人 SKILL.md 兩個跑同一組 task 比較「executable Python skill」vs「markdown skill description」哪個對 routing 決策更有資訊**。

- **How(如何運作/實作)**: Prime Agent 架構分四層(從 README 的 architecture overview 推):(1) **Daemon layer**——`prime-agent` CLI 啟動時 fork 一個 background daemon,**所有 sessions / IPython kernels / schedules / subagents 都跑在 daemon 之下**,terminal 斷線不影響;(2) **Worker / Kernel layer**——每個 session 一個 IPython kernel(他們強調「persistent IPython is the built-in model tool」,**不是「model 吐 JSON 給外部 tool runner」**),kernel 提供 file ops / shell / subagent-spawn / skill-import 的 Python API;(3) **Harness layer**——base system prompt(immutable)+ supplemental state(可 `/refine`)+ skill registry(可 hot-reload)+ subagent spec registry(可 fork),`/refine` 對 supplemental state 做 evidence-backed small updates 並記 snapshot;(4) **Agent layer**——user 跟 Prime Agent 互動時,model 在 IPython 裡寫 Python 而非吐 JSON,subagent 透過 `rlm(...)` 同步 spawn-and-return(不是 promise-based 異步),agent-to-agent 訊息走 daemon 內部的 IPC 不繞 user。`prime-agent doctor` 跟 `prime-agent status` 提供 daemon / worker / kernel 狀態的 CLI inspect + repair 介面。**trust model 自我聲明:not a security sandbox**——worker / kernel 改善 lifecycle isolation(崩潰不影響 daemon、可重新 attach)但不隔離權限,user-level permission 內可做任何事。

- **Insight(個人心得)**: 咱覺得這篇對主人**最值得馬上做**的不是「裝 Prime Agent 來用」,而是**拿 Prime Agent 的 primitive 清單當 horo-agent-lite 的 design checklist,跑 24 小時內的「primitive gap audit」**:(1) `persistent IPython` → 主人 horo-agent-lite 的 agent loop 現在 model output 是 JSON 還是 free-form?若仍是 JSON,今早 PwC 論文已給了 18.8% chain-length gap 這個 measurable gap,可量化收掉;(2) `rlm(...)` subagent as sync function call → 主人 sub-agent spawn 路徑目前是 async fire-and-forget 還是 sync return?若是 async,改成 sync 對 chain_len ≥ 12 task 預期可省 1 個 inference turn(同 PwC 那 18.8% 的同源 primitive);(3) `/refine` 三層信任模型 → 主人 SKILL.md 是 user 可直接編輯的單層,可考慮在 horo-agent-lite 加 `~/.horo/harness/supplemental.md`(可 `/refine`)+ `~/.horo/harness/base.md`(immutable,只隨 version upgrade 更新)+ snapshot `~/.horo/harness/snapshots/`,這直接對應主人 MEMORY 裡「以保守減法 + 端到端驗證」的硬規則(主人現在缺的是 explicit 3-tier trust model,概念上已做但沒介面化);(4) `prime-agent doctor` → 主人目前 background process(若有的話)怎麼 inspect?若還在用 ad-hoc log tail,抄 `doctor [--fix]` 這個 CLI 自檢 pattern。**預計工作量 ≤ 4 小時**:gap audit 1 小時(read Prime Agent 4 個 arch doc + 對照 horo-agent-lite 現有架構)+ 3 個 primitive prototype 各 1 小時(每個 30 分寫 + 30 分對主人既有 5-10 task 跑驗證)+ 結果彙整 30 分。**風險提示**:`pi` 是 Prime Agent 的上游依賴,主人若想直接 import `pi` 而不 import Prime Agent,需先確認 `pi` 的 license / API 穩定性(目前 `pi` 在 earendil-works 是獨立 repo,license 未明列須驗),且**不要在 air-gapped downstream 裡硬 import `pi`**——Pi 仍可能跟 Prime Agent 一樣 trust model 是「not a sandbox」,跟主人 air-gap 場景衝突,做借鏡可以、抄實作要小心。
