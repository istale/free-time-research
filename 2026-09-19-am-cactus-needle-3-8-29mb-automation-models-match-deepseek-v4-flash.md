# Show HN: Cactus Needle 3 — 8-29MB automation models can match DeepSeek V4 Flash
- 原始連結: https://cactuscompute.com/needle
- HN 討論: https://news.ycombinator.com/item?id=49748553
- 程式碼: https://huggingface.co/cactus-compute/needle-3 (engine binary via HF)
- 閱讀時間: 2026-09-19（早間）
- 來源: Hacker News 熱門前 10 第 8 名（144 分 / 71 則留言，讀取時 2026-09-19 07:01 Asia/Taipei）

## 摘要

**主人，這一篇是「把整個 8-29 MB 的 on-device automation foundation model 連同 structured-tool-call runtime 一次性 ship 出來」的工程級 demo——同時是 8/04 以來 inference-optimization 軸線第 6 個 pick，但跟前 5 個（serving-engine / shared-scheduling）完全不同，今日打開的是「niche-task on-device laddered networks」這個全新子軸線。** 文章作者群（Cactus Compute）在 144 分 / 71 則留言的 HN#8 釋出 Needle 3：整支模型（2L→20L subnetwork 同一份 binary）+ 結構化工具呼叫 runtime 全部打包進一個 8-29 MB 的單一檔案，靠 CQ2 quantization + Monarch Hadamard FFN + Engram fusion 三層 substrate trick，把 Raspberry Pi 5 的 decode throughput 推到 400-4k tok/s、prefill 1-10k tok/s。HN 留言區 71 則裡 50+ 是 substantive 機制討論（small-model routing、on-device agent stack、tool-call grammar 的 byte-level 設計），不是 vendor release cluster，也不是 swag thread。

**核心機制：Laddered Simple Attention Networks + byte-level tool grammar + Engram fusion 三件套** 文章把模型架構拆成 4 個 substrate-aware 子設計：(1) **Laddered subnetworks**——同一份 weights 從 2L 到 20L 任意 depth 都是合法子網路（刪掉 layer 18-19 之間的 residual lane 不破壞 inference graph），4L subnetworks 在 360B tokens 訓練後 fine-tune 一個 epoch 就能匹配 DeepSeek V4 Flash 在 BFCL v4 mobile-tool-call 上；(2) **CQ2-bit quantization**——codebook quant 把 121M 參數的整個 FFN 壓到 8.0-29 MB（取決於 layer count），目標是 MCU 級快閃記憶體能直接放得下；(3) **Engram fusion**——把 hashed 2-gram / 3-gram 的 lookup table 嵌進 attention 之外的 residual lane（site 3, 7, 11, 15, 19，18,432 slots），讓模型用 70.8M engram 參數省下 2× MFLOPs/token；(4) **byte-level CFG grammar + monotone lattice decode**——tool-call 的 JSON schema 在 token 層強制驗證，每個 decode step 都符合 typed schema。這組合起來單 binary 跨平台 iOS / Android / Raspberry Pi 5 / MCU 直接跑，無 GPU、無 server round-trip。

**為什麼主人會想讀——這是 16GB Mac + local-LLM + Hermes-stack 的「offline first-mile agent」範式骨架** 三個 transferable primitive 直接落在主人手上：

1. **Laddered subnetworks as cost-vs-effort curve**——同一份 binary 從 2L（2 MB, MCU 等級）到 20L（29 MB, Raspberry Pi 等級）任意深度可換，相當於把 8/08 cost-curve 那個 Max/High/Low 三階 band 從「三個獨立模型」壓成「同一份 weights 的三個切片」。對應到 hermes-agent-lite 的 routing-tier band：主人不必同時部署三個模型，只要根據 device RAM 動態切 ladder layer，就能在 16GB Mac Mini（20L）→ iPad（8L）→ 樹莓派（2L）三段硬體梯度維持同一份 agent behavior。
2. **Byte-level CFG grammar as dispatch-gate**——文章用 monotone lattice decode 強制 tool-call schema 結構，相當於把「模型能不能正確生成 tool call」從「LLM 機率分布」轉成「CFG 接受性」。對應到 hermes-agent-lite 的 tool wrapper：今天主人驗 tool-call 正確性靠 LLM judge 或 regex grep，未來可以在 tool wrapper 加一層 `decode_with_grammar(json_schema, bytes)` 的 bytecode shim，把 dispatch-gate 變成 schema-level 而不是 text-level。
3. **Confidence gating + suppressed_calls envelope**——每個 tool call response 帶 0.1 floor 的 confidence score + suppressed_calls 陣列；模型主動放棄低信心呼叫時 envelope 仍然回傳，但 function_calls = []。對應到主人 kanban 的「executor self-report」問題：這就是 8/04 KV-cache 8-byte tag + 9/17 anchored verifier 的同型 envelope——把「不敢 call」跟「不知道 call 什麼」分開回報，下游 routing 才能區分。

**與 9/17 同軸連擊的關係** 9/17 Bansal 的 qorl 把 4B Qwen3.5 蒸餾 + anchored GRPO RL 推到「niche 子領域 1.81× Postgres」、今日 Needle 3 把 29-121M（**比 4B 小 33-138 倍**）小模型 + laddered-network 推到「tool-call / extraction 匹配 2-3× bigger models」——兩者共同驗證了**「niche-task small-model optimization」** 這個 substrate-identity 在 2026 年下半年已經從「實驗室 demo」變成「生產級 deployable」。同時——文章把 `pg_hint_plan` / `prime-rl` / `vLLM` 這些同類 substrate-aware 設計哲學（pin 版本、把 RCE-shaped boundary 顯式化）原封不動搬進 laddered network 的 on-device 設定，所以這也是 8/21 arrayref 「trust boundary 顯式化」+ 9/8 Trusting-Trust 「toolchain bootstrap 是 6th boundary」同族「make invisible substrate behavior visible」的延伸。

## 3W1H 分析

**What（做了什麼/主題）:** Cactus Compute 9/18 在 HN#8 釋出 Needle 3：一份 8-29 MB 單 binary（含整個 2-20 layer laddered Simple Attention Network + Monarch Hadamard FFN + Engram fusion + CQ2-bit quantization + byte-level CFG grammar decode engine），加上 Python `needle.tool` 裝飾器 runtime（自動從 function signature 推導 JSON schema、從 docstring 推導 tool description）。benchmark 在 BFCL v4（Berkeley function calling，3,641 rows，AST match）、DSTC8（dialogue state tracking，1,813 turns，field F1）、SNIPS（slot filling，700 rows × 2 tasks，field F1）、DroidCall（200 rows，exact call in order）、Mobile Actions（961 rows Android intents，exact call）五套 suite 上，**Needle 3 的 4-layer subnetwork 跑 match DeepSeek V4 Flash**（cloud API baseline），20-layer subnetwork 在 mobile tool-call 上**打敗比自己大 10× 的 model**、在 extraction 上 match 2-3× bigger。decode speed 400-4k tok/s、prefill 1-10k tok/s、peak RAM 28.5 MB（4L 模式）。每個 tool-call response 帶 confidence score + suppressed_calls envelope + reasoning trace + prefill_tps + decode_tps + peak_ram_mb，zero-cost structured output。

**Why（為什麼重要）:** 三個 transferable insight 直接掛上主人 stack——
(a) **整支模型 + runtime + CFG grammar 一檔 ship**——把「雲端 LLM + Python orchestrator + JSON schema validation」三層降到「一份 29 MB binary 跑在 MCU 上」，相當於把 agent runtime 的「boundary count」從 3 降到 1（單一 file、單一 ABI、單一 decode engine），這對 air-gapped downstream / hermes-webui 的「< 200 MB single-binary serve」目標是直接驗證；
(b) **Laddered subnetworks 是 on-device 版的 8/08 cost-curve band**——同一份 binary 從 2L 到 20L 可切，等於把「routing-tier band」從「server-side multi-model」壓縮到「同一檔案的多層切片」，硬體梯度（16GB Mac → iPad → Pi 5 → MCU）對應模型梯度（20L → 8L → 4L → 2L），這是 routing 範式從「model A vs model B」變成「model ladder slice」；
(c) **Confidence gating + suppressed_calls envelope** 是 8/04 KV-cache 8-byte tag + 9/17 anchored verifier 的同型 envelope——把「LLM 不敢 call」與「LLM 不知道 call 什麼」分開回報，給下游 routing 一個結構化 hint；這對主人 kanban task-completion 驗證直接可用。

這跟 9/17 qorl 的「niche-task small-model optimization」、9/15 Litt 的「rigorous defense as allocatable unit」、8/13 Tailscale SQLite「pin substrate version」、9/8 Trusting-Trust「bootstrap toolchain is 6th trust boundary」共同構成「**在 16GB 個人硬體上重塑 agent 範式**」軸線——今日 Needle 3 用一份 binary 把這條軸線從「資料中心」拉到「口袋裝置」。

**How（如何運作/實作）:** 四階段 pipeline：(1) **Laddered Simple Attention Network 架構**——mHC lane read/write（Sinkhorn-normalized sandwich projection 4 residual lanes）、Monarch Hadamard FFN（D4M3D3M2 silu(D2c(x)M1D1x + b)，32×32 blocks）、Engram fusion（hashed 2-3-gram lookup，70.8M engram 參數 site 在 layer 3/7/11/15/19）、GQA attention 12q:2kv、3-tap causal conv per channel、ZCRMSNorm + Sinkhorn normalization；(2) **CQ2-bit quantization**——codebook quant 把整個 FFN 壓到 2-bit，121M 完整模型 29 MB、4L 8 MB；decode throughput 400-4k tok/s 在 Raspberry Pi 5 取決於 ladder depth；(3) **Byte-level CFG grammar + monotone lattice decode**——tool-call JSON schema 編譯成 byte-level CFG，每個 decode step 從合法 lattice 內採樣，**不可能生成不符合 schema 的 token**；byte-level grammar 比 BPE-level 更嚴格（避免 BPE 邊界切到 schema token 中間）；(4) **`needle.tool` Python runtime**——裝飾器讀 function signature 推導 JSON schema、讀 docstring 推導 tool description、執行後把 result 餵回 model、產出 `{"type": "call", "function_calls": [...], "confidence": 0.94, "prefill_tps": 4300, "decode_tps": 850, "peak_ram_mb": 28.5, "reasoning": "..."}` envelope。三個 substrate-aware 細節：(a) **Triggers regex**——`@needle.tool(triggers=[r"\b(turn|switch)\b.*\b(on|off)\b"])` 把 decode lattice 限制在 matched tools，避免 hallucinate；(b) **`extract(schema=BaseModel)`**——給 Pydantic model 直接回傳 typed object，繞過 JSON 解析；(c) **Confidence floor 0.1**——below 0.1 自動 withhold 並填 suppressed_calls，下游 routing 收到空 envelope 才能 fallback。

**Insight（個人心得）:** 主人，這一篇在 substrate-identity 上是 8/04 → 8/13 → 8/21 → 9/8 → 9/17 → **9/19（今日）** 「niche-task small-model + substrate-aware envelope」家族的**第 6 個 instance**，但跟前 5 個在 compute substrate 上根本翻轉——從「data-center shared scheduling surface」變成「on-device single-binary laddered network」。文章最強的 portable primitive 是 **byte-level CFG grammar**（不可能生成不符合 schema 的 token），對應到 hermes-agent-lite 的 tool wrapper：今天主人驗 tool-call 正確性靠 regex post-validation（要麼靠 LLM judge re-call，要麼靠 grep「`{\"name\": ...}`」），未來可以在 tool wrapper 加一層 **monotone lattice decode shim**——把 JSON schema 編譯成 byte-level CFG，每次 tool-call decode 強制限在 schema lattice 內，**結構性消滅 schema mismatch**，不再靠 regex 補救。**1 小時 prototype 路徑**：在 `~/.hermes/profiles/default/skills/` 下新增 `lattice-decode-tool-wrapper` skill，提供 `decode_with_grammar(json_schema: str, model_output: bytes) -> bytes` 函式（包裝 `lark` 或 `pycfg`），tool wrapper 在 LLM decode 後跑一次 `decode_with_grammar` 把不符 schema 的 token 強制替換成最近的合法 token；這跟 8/04 KV-cache 8-byte tag + 9/17 anchored verifier 是同一族「make invisible LLM output behavior observable + constrainable」，< 100 行 Python、< 2 hours commit、no LLM call。同時——8-29 MB single binary + 400-4k tok/s decode on Pi 5 的 envelope，**直接驗證了 horo-webui 的「< 200 MB single-binary serve」目標可行性**——下一個 horo-webui 的 prototype 可以把 Needle 3 binary embed 進 webui 的 static asset、讓 webui 在 air-gapped 環境（無 oMLX 192.168.0.56:2234/v1）下也能跑 tool-call agent，預期 < 50 MB webui binary 增量、16GB Mac 上 8L subnetwork 跑在 28.5 MB peak RAM 還有 15.5 GB 留給 webui 本體。
