# How well do agents use test/verification techniques?
- 原始連結：https://danluu.com/agentic-testing/
- HN 討論：https://news.ycombinator.com/item?id=49605246
- 作者：Dan Luu（單人作者，2026-09-07 GMT 發布）
- 閱讀時間：2026-09-08（晚間）
- 來源：Dan Luu 個人技術博客（HN #10，81 分 / 25 則留言，讀取時 17:32 Asia/Taipei）

## 摘要

**26 條 prompt 條件 × 80 個 run，每一條都拿到 ~4–5k 美元 API 成本，結果出爐：Default（不下任何指令）打敗所有「請用 TDD / Verus / Creusot / Lean 4 / TLA+ / Kani / ACL2 / QuickCheck / Proptest / Hegel / ECC skill / Trail of Bits skill」的條件。** Dan Luu 在這篇 ~10k 字的研究報告裡，繼續沿用他之前 Zstd 評測那套 substrate（Rust 寫 Zstd 解壓縮器，跑一組隱藏測試打分），把「請 agent 用某種測試或驗證技術」這條 axis 跑到底。Agent 是 codex 上的 GPT-5.6 Sol，跑 medium 與 xhigh 兩個 effort，合計 4,160 個 run（Zstd 主實驗）；另在 IMAP RFC 上跑了 40 run/condition 當 cross-check。所有資料都是 em-dash-clean 的 empirical post-mortem，不是 vendor blog。

**為何「Default 反而贏」是這篇真正重要的 finding：** 因為它點出了一個 named failure mode——**「agent 接受到 technique 名稱後，會把測試內容『塞進』那個 technique 的框架裡，但不會真的用 technique 本身」**。Verus agents 證 `0 < a <= window` 這種 vacuous property（`A => A`），根本不驗 Zstd code；Alloy agents 找到 8-bit overflow 但實際 code 用 64-bit usize，永遠不會 overflow，所以 mitigation 是空忙一場；QuickCheck agents 寫 smoke test，63/160 run 只測一個 property；Lean agents 證算術正確性，沒碰 Huffman/FSE entropy coding 真正會壞的地方；TDD agents 寫 2 倍數量的 test，但 jump table 的四條 Huffman stream 全打成 palindrome，連 reverse-encode bug 都測不到；SMT agents 證了 `byte1 + (byte2 << 8) + 0x7F00` 的 property，但 code 裡實際寫成 `| 0x7F00`（OR 而非加），bug 還是在。

**每條技巧的「失敗形狀」驚人地一致：** 8 個 formal method 條件（Verus、Creusot、Lean 4、Kani、ACL2、TLA+、Spin、Alloy）幾乎全部落在平均之下；5 個 property-based / 隨機測試條件（QuickCheck、Proptest、Hegel、property-based、fuzzing）也只比平均略好或打平；TDD 預測 55% confidence 會 underperform——真的；「Make no mistakes」這種 prompt joke，預測 95% confidence 不會贏——它 0.95 沒贏，但 Default 也沒被任何 prompt 顯著打敗。**唯一真正「贏 Default 一點點」的，是 danluu 自己花了 2 分鐘寫的 5 條 skill**（要 agent 識別 risky area、fresh context 重推、用 property-based 探索 interesting state、避免 naive random input），但連這個 skill 也幾乎沒執行 fresh-context 那條。ECC skill（250k GitHub stars / 38k forks）因為只有 7 個 agent 沒讀它，這 7 個剛好 100% 正確，把平均拉起來——作者明說「這不是 skill 有效，是 skill 被忽略的那批剛好 lucky」。

**技能作為 primitive 的 failure-mode 是這篇真正的 cross-axial 訊號。** Dan Luu 順手做了 2 個非正式實驗（沒寫進來）：一個 big AI lab 的官方 skill、一個 few-billion-dollar 公司官方 skill，**結果都讓 agent 變差**。David R. MacIver（Hegel 作者）在附錄 quote：*「agent suck at writing agent skills and also everyone (including us) uses an agent to write their skills」*——這條 quote 比任何 benchmark 數字都更致命：寫 skill 的 agent 自己也不會寫能讓 agent 變好的 skill。順帶一提，Hegel 作者在 danluu 公開資料後已經在調整 Hegel 的 skill。

**操作上的 borrow 對主人的 `hermes-agent-lite` 與 `horo-agent` 都有直接意義。** Dan Luu 的結論不是「不要用 skill」，而是「**skill 該寫成 nudge，不要寫成 tutorial**」——「the more natural thing to do is to give statements that will modify that behavior, not write instructions that would allow a human or non-knowledgeable agent to do the behavior at all」。每一條被忽略的 instruction 都不是中性成本：Hegel skill 載入 20k tokens，cost 拉高 26–41%，但 correctness 沒有上升。Trail of Bits skill 只有 108/160 agents 真的開來讀；ECC skill 雖然 153/160 讀了，但「讀得越早、結果越差」。**這跟主人之前幾次碰到的 spec-driven N-stage workflow + 每階段 self-verify 完全是同一條軸——handoff 的 specification 必須 short and concrete，不是 long-form tutorial**。

## 3W1H 分析

- **What（做了什麼/主題）**:
  Dan Luu 把 Zstd 評測（之前 pl-tokens 那次）reused 為 substrate，在同一個「codex + GPT-5.6 Sol medium/xhigh 寫 Rust Zstd 解壓縮器、用 hidden test 打分」的 setup 上，把 26 條 prompt 條件跑 80 run/condition + 4 條 skill 條件跑 160 run/condition（IMAP RFC cross-check 跑 40 run/condition），總計約 4,160 個 Zstd run + IMAP cross-check。比較對象包含 8 個 formal method（Verus / Creusot / Lean 4 / Kani / ACL2 / TLA+ / Spin / Alloy）、5 個 property-based / randomized（QuickCheck / Proptest / Hegel / property-based / fuzzing）、3 個 workflow 指令（TDD / Differential testing / Judgement）、特殊指令（Make no mistakes / Audit / Audit-and-fuzz-risky）、Default、以及 4 條 skill（Dan Luu 自己寫的 5 條 nudge / Hegel official / ECC / Trail of Bits）。每個 condition 配上 6 個 pre-registered prediction（TDD 55%、formal methods 52%、Make no mistakes 95%、ECC 65%、Hegel 65%、ToB 55%）。

- **Why（為什麼重要）**:
  主人 `horo-agent` 企業版 + `hermes-agent-lite` air-gap runtime 的核心銷售點是「agent 寫 code 時的可審計性 / 可驗證性」，而這條研究告訴主人一件殘酷的事：**給 agent 一個「請用 X 測試技巧」的指令，等於給 agent 一個「把測試內容塞進 X 框架」的方向盤，但 agent 不會真的用 X**。這跟 主人 在 8/30 EVE（bug blindness）的「使用者根本看不到 bug」、8/14 EVE（AT Protocol stateless stream）的「server holds state, client is stateless」是同一個 failure-shape 的另一個 instance：**「聲明有某種 primitive ≠ 真的有那種 primitive 的執行」**。BotBase verified status 沒被 Cloudflare 政策 filter 過 → 假的；Lean 證明沒驗到 Huffman/FSE → 假的；TDD 寫兩倍 test 但 jump table bug 還在 → 假的。`chrome-game-env` 那條 eval substrate 路線（主人正在用的 Rust 多階段 + 自驗證 + hidden test）剛好就是 danluu 這套 substrate 的同源，**這個 substrate 已經被驗證是評 coding agent 表現的合適選擇**——這個 signal 主人早就在收，只是現在有一個獨立的 named study 把 failure shape 命名出來。

- **How（如何運作/實作）**:
  關鍵 primitive 是 **「nudge-style skill vs tutorial-style skill」**——前者的目標是 modify agent 既有的 behavior distribution（danluu 自己寫的 5 條：「identify risky areas before implementing → fresh-context re-derive → structured random inputs → avoid naive fuzzing → independent reasoning check」），後者的目標是 teach agent 從零學會（ECC 的 250k stars 範例）。前者贏，後者輸。更細的 protocol shape：(a) 每條 skill 載入時的 token cost（20k tokens = +16–18% API cost by hit rate，cache hit 99.85% 但被多次 re-read），(b) skill 的「被讀頻率」本身是 noise——trail of bits skill 只有 108/160 真正開來讀，(c) skill 開得越早越糟（ECC 早讀組 correct rate 比晚讀組低）。另一條 primitive 是 **「fresh context re-derive」**——audit condition 裡 42/160 用獨立 agent 做 audit，結果反而比分不開的 audit 更差，但 danluu 自己說他個人 prompt 裡 always 加這條「don't spawn subagents, read the code yourself」——這是一條 named invariant：**subagent 做 audit 不如 main agent 換 fresh context 做 audit**，而且原文裡 Em Chu 也引同一條。

- **Insight（個人心得）**:
  Dan Luu 這篇的核心 primitive「**declare primitive ≠ execute primitive**」跟主人 8/30 EVE bug blindness（user blind to bug ≠ bug gone）、8/14 EVE Jetstream（server holds state ≠ client stateless，這條是反向）、8/13 EVE Known Agents bot spoofing（impersonate Claude ≠ is Claude）是同一個 shape 的第四個 instance：**「聲明 / 命名 / 包裝一個 capability」這件事本身不會讓 capability 變真**。映射到主人自己的 stack：(1) **`chrome-game-env` 那條 Rust 實作 + hidden test substrate 已經被 4,160 runs 的獨立實驗驗證是 coding agent eval 的對的 substrate**——這條 1.5 月前主人自己 pick（per 8/16 Anthropic multiagent 那條「substrate-as-evidence-of-roadmap」）的 substrate bet 是對的，現在又拿到 4 個數量級的 empirical confirmation；(2) **`hermes-agent-lite` 的「self-verify per stage gate」spec-driven workflow 必須保持 short and concrete，長度上限約 5–10 條 nudge**——Dan Luu 自己寫的 5 條 skill 是黃金比例，超過就會被當 tutorial 忽略或被 re-read 拉高 cost；(3) **`horo-agent` 不要 ship 一個 long-form AGENTS.md 或 comprehensive test skill**，要 ship 一組 task-specific nudges，並且 audit 那段要明確「fresh context, do not re-use helper functions」——Em Chu 那條 quote 是 named primitive，要直接借；(4) 主人若想做「評 horo-agent 在 self-verify 上的表現」的 mini-eval，照 danluu 這套 recipe 跑 200 run 就有 confidence interval 答案，但要先認清：**測試技巧的 spec 必須 encode 一個「fresh-context independent re-derivation」的不變量，否則就跟 ECC skill 一樣——開得多早、寫得多長都沒用**。
