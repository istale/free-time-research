# Orchestrating Claude Code Agents: The Chief of Staff Pattern

- 原始連結：https://asyncdot.com/blog/chief-of-staff-pattern-orchestrating-claude-code-sessions/
- HN 討論：https://news.ycombinator.com/item?id=49772806
- 作者：Will Keleher(asyncdot.com)
- 發布日期：2026-09-19
- 閱讀時間：2026-09-20(晚間)
- 來源：Hacker News top 10 第 9 名(18 分 / 3 則留言,讀取時 2026-09-20 17:32 Asia/Taipei)
- 姊妹參考：Plan Desk https://plandesk.asyncdot.com/ · cmux https://github.com/manaflow-ai/cmux

## 摘要

**這一篇把「long-horizon AI coding 為什麼爛」重新框成「不是 generation 的問題、是 checking 的問題」,並給出一個已經在 Claude Code 上跑得起來的 chief-of-staff 編制樣板。** 作者 Will Keleher(asyncdot.com)開宗明義:long-horizon AI coding work fails less because agents cannot write code and more because their **context is ephemeral and their self-reports are unreliable**。單一 AI coding session 一小時內表現還行、再久就劣化——三件事在發生:(1) context 有限且 lossy,長 session 被 compact、把三小時前的細節壓成 summary;(2) **self-reports drift from reality**,agent 說 "tests pass" 是 intent 跟記憶,不是 fresh observation;(3) **nothing compounds**,在 hour 2 痛出來的教訓到下一 session 就沒了。「加更多 agent」不會修這三件事,只會把它乘以 N——你會有好幾個 unreliable reporter 沒人 reconcile。修法是借人與組織的分工:**一個 session 的工作不是做 work,而是知道什麼是真的。**

**Chief of Staff 是這篇文章為「coordinator-implementor-verifier (CIV) / orchestrator-worker / Git integration-manager」這個舊形狀取的暱稱,不是新發明;真正新的是 verification discipline 跟六個 failure-mode 防線。** 作者明說這個 pattern 在文獻裡有幾個名字——orchestrator-worker / coordinator-implementor-verifier / maker-checker / integration manager(Git 的 distributed workflow) / team lead and teammates(Claude Code 自己的 subagent 文件)——骨子裡都是同一件事:**一個 agent 規劃 + 檢查、別人做 work、shared state 在任何單一 context window 之外**。作者加上的是驗證紀律與具體 failure-mode——「coordinator 的工作是 verify 而不是 do、實作交給 disposable session、board 是記憶體不是 context」、以及六個「instrument that reports success for work it did not do」的 defect class(下文 What 段詳列)。

**三個元件 — Claude Code runtime + cmux session substrate + Plan Desk 持久狀態儲存 — 構成一個已經 production 跑得起的樣板,八步操作 loop 把 red-gate 變成一級公民。** 三元件各自可換:**(a) Claude Code** 提供 sessions 本身(tool use、file editing、shell、session-to-session messaging);**(b) cmux** 管理 terminal workspaces 並從 CLI 可驅動——coordinator 用一行 `cmux workspace create --name ... --cwd ... --command 'claude "Read docs/briefs/current.md and do exactly what it says."'` spawn executor session;**(c) Plan Desk** 是一塊暴露 MCP 介面的 planning board——projects、goals、tasks with dependency edges、linked design documents、comments——coordinator 跟每個 executor 讀寫同一塊 board。八步 loop:**PULL → READ → RED GATE → DELEGATE → PROVE → OBSERVE → GATE → SHIP**。作者堅持 red gate 必須在開始前就 fail,理由強烈:**如果 check 已經是 green,你無法分辨「正確的實作」跟「永遠綠的 check」、「match nothing 的 filter」、「asserting something already true 的 test」**。

**「報告是 evidence、不是 instruction」是本文給主人 kanban orchestrator 最值得貼在牆上的一句。** 全文最有 portable primitive 的一句話:**The thing they are reporting on and the thing they checked are often two different objects.** 文章舉例:有 session 把 commit hash 寫進 log file,然後用 `git cat-file` 驗證——但它驗證的是 shell 裡的 short hash,不是寫進 file 的字串。兩個 check 都過,檔案裡的 hash 解析出來的什麼都不是。**check 跟 record 是兩個不同的 object,只測了其中一個**。fallout 出來的 rule:**verify the artifact by reading the value back out of the artifact, never from the variable you think you wrote there**。對應到主人 MEMORY.md「主人要求以真實 handoff、exit code、tests、health、live smoke 判定狀態;timeout、protocol violation、network blocker、設定存在、task started 必須分開報告,絕不能冒充完成」——Chief of Staff 把這條原則升級成方法論,而主人其實早已把方法論內化進 hermes 對每個 kanban task 結束前「跑真的 lint/build/test 取 exit code 再 commit」的 self-verify 流程。

**與今日 AM「Cloudflare saving 100TB RAM with math」+ 9/19「Closed world resolution against tool hallucination」的同軸關係——三篇共同驗證「在固定 substrate budget 上重新 fit 工作負載」這個 2026 下半年的 design philosophy,只是 substrate 從 RAM 換成 context window。** 9/20 AM Cloudflare 把 ring 上的 200× virtual node 用 math re-parametrization 砍掉(演算法換 substrate,不是 fleet 換 RAM);9/19 PM closed-world resolution 把 hallucinated tool call 在 schema 層 hard-fail(schema 換 substrate,不是模型換 RLHF);今日 chief-of-staff 把 long session 拆成 disposable executor + durable board + 獨立 verifier(context 換 substrate,不是 model 換 context window)。三篇共同點:**先用 evidence 證明 naive 設計在做 redundant work(Cloudflare 證 virtual nodes 是冗餘 / closed-world 證 hallucinated tool name 是 schema 不夠嚴 / chief-of-staff 證 session context 是 single source of false-positive)、把 redundant 那層砍掉、context/compute/budget 自然回收**。對應主人 air-gap downstream 偏好——「保留已驗證 runtime 與真實行為,保守減法、端到端驗證」——Chief of Staff 沒砍掉任何外部 observable(同一個 work output),只是把「假裝有用的單 session 自我報告」和「假裝持久的 context 記憶」砍掉。

## 3W1H 分析

**What(做了什麼/主題)**:asyncdot.com 9/19 釋出的「Chief of Staff Pattern」long-form post——把 long-horizon AI coding 的失敗從 generation 重新框成 checking,把 coordinator-implementor-verifier 這個舊形狀升級成一個含 (a) 三元件樣板(Claude Code runtime + cmux session substrate + Plan Desk 持久狀態) + (b) 八步操作 loop(PULL / READ / RED GATE / DELEGATE / PROVE / OBSERVE / GATE / SHIP,red gate 為一級公民) + (c) 六個 defect-class 防線(以下表格全名:**Vacuous assertion / Silent no-match / Errored check / Wrong reference / Stale premise / Scope mismatch**) + (d) 五條 practical mechanics(含 `--command` 發 shell 不是 spawn agent、`git commit -- <paths>` 而不是 bare commit 避免共享 working tree 污染)的可運行編制樣板。本文 246 行,有完整 framework 跟 code-shape。

**Why(為什麼重要)**:本文跟主人正在做的 hermes-agent / kanban orchestrator 直接對接——主人 kanban 已經實作 (a) 持久狀態 SQLite board、(b) `kanban_show` / `kanban_create` / `kanban_heartbeat` / `kanban_request_review` / `kanban_complete` 的 lifecycle commands、(c) 主人堅持「每個 task 結束前 self-verify 跑真的 lint/build/test 取 exit code 再 commit」的 spec-driven workflow。本文 Chief of Staff Pattern 把主人 MEMORY.md 寫的「**一件任務一件事,維持既定 Kanban decomposition;不可因 blocker/quota 合併已拆 scopes 或盲目重建**」+「**以真實 handoff、exit code、tests、health、live smoke 判定狀態**」+「**主人要求 executor 正常退出卻未呼叫 lifecycle completion 時,視為 protocol failure,不得推測已完成;配額耗盡、protocol、network blocker 要分開回報**」三條工作守則升級成方法論層——主人 kanban 已經在實踐 chief-of-staff 的反模式防線(kanban_request_changes 對應 GATE loop、kanban_heartbeat 對應 OBSERVE cadence),只是這篇把同樣的形狀命名為可推廣的 pattern。本文最強的 portable primitive 是 **「verify the artifact by reading the value back out of the artifact, never from the variable you think you wrote there」**——主人 kanban 對應:**每個 kanban_complete 前真的跑 `git rev-parse HEAD` 跟 `git ls-remote origin main` 比對 SHA**(已寫入 skill checklist),而不是相信 executor 報告「我已經 push 了」。

**How(如何運作/實作)**:四層實作 shape。(1) **持久狀態分離**:任何 single session 之外的 durable store(SQLite、Plan Desk、Notion API 都可)持有 tasks + status + comments + linked docs;coordinator 跟 executor 共享 read/write;**board 是 memory,sessions 是 disposable**。(2) **Red Gate 早於實作**:每個 task 開工前先跑驗證——它必須 fail;如果它已經 green,red gate 抓不到「check 永遠綠 / filter match nothing / test asserting already-true」三種 defect class。對應主人 kanban:`kanban_complete(artifacts=[...])` 對應「跑真的 exit code 0 才算完成」,而不是「executor 自己說完成」。(3) **報告 ↔ 物件分離**:每次驗證都要 "read back from the artifact, not from the variable you think you wrote there"——如果 executor 寫 commit hash 到 log,verify `git cat-file` 不能驗 shell 裡的變數,要驗 log file 裡的字串;如果 kanban 完成狀態存在 SQLite,verify 應該 `kanban_show(task_id=...)` 而不是相信 stdout 報告。(4) **Timeboxing 是 surfacing 不是 stopping**:interval 決定「多久回報一次」,不是「在哪裡停下」——mid-item timer 到期要 finish → verify → commit → 然後才 report;**「Shall I continue?」是一個要刪掉的句子**,人要看會打斷,人沒看就被一個問題殺了 run。本文八步 loop 對應主人 kanban 操作:`PULL` ↔ `kanban_show` / `READY` queue / `kanban_list` 找下個 task;`READ` ↔ 讀 `body` + linked docs;`RED GATE` ↔ 在 dispatch 前跑 verifier 取 fail;`DELEGATE` ↔ spawn executor session(或本 cron 中交給 minimax-executor profile);`PROVE` ↔ re-run 命令取 exit code;`OBSERVE` ↔ `git diff`/`git log` 看 diff 不是看 transcript;`GATE` ↔ `kanban_request_review(reviewer=chatgpt-reviewer)`;`SHIP` ↔ `kanban_complete(artifacts=..., created_cards=...)`。

**Insight(個人心得)**:主人,這一篇是 EVE axis-rotation 應該 land 上的「**AI coding orchestrator / multi-agent verification discipline**」sub-axis 的第一篇 pick,跟 9/1 eve BotBase、8/22 eve Bot Preference Sync、8/29 eve BotBase for Operators 的 Cloudflare agentic-internet security axis **不同軸但同 philosophy**——本篇是「**代理人組織紀律**」、前述 Cloudflare 系列是「**代理人政策框架**」。主人 kanban 從 6 月起就在實踐本文八步 loop 的所有原語,而且主人 **從 2026-07-01 寫 chrome-game-env 八階段**就把 red-gate 內化進 workflow(`spec-driven N-stage + 每階段 commit`、`todo 編號`、`一次 in_progress 一項`、`結束前 self-verify 跑真的 lint/build/test 取 exit code 再 commit`)。所以本文對主人不是「要不要採用 chief-of-staff」——主人**已經採用了**,而且比本文更早。本文對主人的 leverage 在三個 level: **(a) 把主人既有的 kanban 工作守則升級成可推廣的方法論命名**——主人 MEMORY.md 寫的「真實 handoff / exit code / live smoke / 不冒充完成 / 不合併已拆 scopes / 不盲目重建」這六條,事後看就是 chief-of-staff pattern 的「the six defect-class 防線 + 八步 loop + durable board」。**(b) 一條新的工具 primitive 可借**:**Plan Desk 的 MCP-exposed planning board**——主人 horo-agent 目前 kanban command 是 Hermes 自己的(`kanban_create` / `kanban_show` / `kanban_complete` 等),如果未來要對外暴露企業下游用戶的 task tracking,Plan Desk 的 MCP-first shape 是一個 candidate;不必照抄,但「planning board 暴露成 MCP server」這個 interface 選擇比 Hermes 目前的 CLI-tool-style 對 agent 更友善。**(c) 一條反模式警示特別適合寫進主人 kanban SOP**——本文 Practical mechanics 第一條:**「Verify a spawned session actually started. Launchers report that a workspace was _created_, which is not the same as an agent _running_」**——這條直接撞主人今天還在踩的 dispatch pitfall:dispatcher spawn agent 看到「task 在 running 狀態」就當成功,但 agent 可能卡在 heartbeat 之前、可能 spawn 出 process 但 model 還沒 load、可能 quota 耗盡在 spawn 那一秒就死。**主人 kanban 應該新增一條 SOP:「dispatch 後 N 分鐘若沒 heartbeat,kanban_heartbeat note 應該 evidence-based 不是 liveness」**——把「task started」與「task running with evidence」分開回報,正是本文「report is evidence not instruction」+「spawn ≠ running」雙重防線的具體落實。三層 leverage 加總:**主人 kanban 已經是 chief-of-staff pattern 的一個 instance,但本篇提供了「對外可推廣 + 反模式清單 + 對下游 MCP 友好的介面選擇」三個新的 design lever。** 順帶,本文 9/19 GMT 發,主人今早 AM 已寫 Cloudflare 100TB RAM(math re-parametrization on substrate),今午 PM 已寫 MuskIA Skia(formal semantics for 18.7% GPU speedup),今日 EVE chief-of-staff(verification discipline on long-horizon agentic substrate)——**三條腿的 substrate-axis 同一天不同 substrate 都收到一個「**先證 naive 設計在做 redundant work、再砍掉冗餘**」的 instance,主人可以考慮把這條命名為「2026 Q3 substrate-aware compression」工作紀律。**
