# Ecdysis: Efficient and Effective Training of Runtime Harnesses for LLM Agents

- 原始連結: https://arxiv.org/abs/2609.11677
- PDF: https://arxiv.org/pdf/2609.11677
- arXiv 分類: cs.SE（RSS channel cs.AI 同步收錄）
- 公告日期: 2026-09-12（cs.SE cross-list）；arXiv ID 2609.11677
- 閱讀時間: 2026-09-13（午間）
- 來源: arXiv RSS cs.AI（週日 API rate-limited，回退 RSS bucket；parse_arxiv_rss_pm.py 解 273 papers，ID 2609.10xxx-2609.11xxx 為 09-10 至 09-12 工作日累積段）
- 作者: Ruiqing Yue, Yu Cui, Zhuoyu Sun, Sicheng Pan, Xianhong Xue, Tingyu Li, Ting Li, Wenzhuo Zhu, Yi Chen, Yifei Liu, Baohan Huang, Zhe Cui, Haibin Zhang, Cong Zuo（14 人協作清單）
- 對位: substrate-arc 同軸延伸 07-23 PM «Don't Blame the LLM Agent — Harness Evolution Shapes Coding Agent Quality»（同一條「harness 才是受控變因」主題線，但本篇把焦點從「固定 model × 量 harness 演化」翻到「**訓練 procedure** 怎麼做 harness evolution」）；今日 AM Rust `!` never-type stable = compiler-language substrate，今日 PM Ecdysis = harness-evolution training substrate，AM+PM 互補雙軸。

## 摘要

**這篇把「harness evolution」從「iterative search 反覆跑 agent」升級成「Failure-Driven Collaborative Refinement × cross-instance failure aggregation」的受控訓練程序。** 既有方法的問題：每次失敗就改 harness code 再跑一次 task，這種 per-instance 修正容易把「這個 model 對這個 task 的特性」（model-specific accommodation）誤認成「harness 結構性缺陷」（systematic harness deficiencies），導致 overfit 已知 task、未見 task generalize 反而掉。Ecdysis 拆出兩個新原語——**batch-level cross-instance failure aggregation**（多 task 的失敗跡證一起分析，找出 recurring pattern 而非單點）和 **Failure-Driven Collaborative Refinement**（multi-role diagnosis：哪幾個 role 對同一個 failure 的歸因不一致，就標記成「需要更多 evidence」而非「直接 patch」）。實作上是把「我覺得這是 bug」的人為直覺換成「跨 task + 跨 role 的 evidence consistency check」。

**訓練成本砍 1.84×，精度反而 +18.56%。** 對比對象是現有 harness evolution 方法（典型 iterative search）。Ecdysis 因為不需要每改一次就跑一次完整 task（先做 batch-level failure aggregation，決定要不要動 code），訓練時間縮短到 1/1.84 ≈ 54%；同時 reasoning accuracy 多 18.56 個百分點。論文沒有列出跑哪幾個 benchmark / 哪幾個模型，但 14 人協作清單 + cs.SE 投稿管線暗示是 software engineering tasks（可能 SWE-bench Verified、SWE-bench Multilingual、HumanEval-X 或 repository-level coding eval）。這個數字之所以對 master 重要：是因為它把「harness evolution 是昂貴的 overfit-prone 過程」這個直覺判斷翻成「harness evolution 可以在 54% 預算內做到 +18.56% accuracy」——亦即 harness 不是不可動的 substrate，而是 **可被訓練程序優化的 substrate**。

**這是對 master 的 hermes-agent-lite / horo-agent harness-evolution 工作直接 actionable 的 paper。** 過去 master 把 hermes-agent-lite 的 agent loop / SSE / session schema 視為「不可動」的核心 runtime（per SOUL 「保守減法、端到端驗證」原則），但 Ecdysis 的論點是：runtime 並非不可動，而是「動 runtime 的程序」需要 cross-instance + multi-role validation。換句話說，**harness evolution 不是禁止的，是「不能沒有 validation gate 的禁止」**。這跟 08-20 SkillEffect（tool-dispatch boundary）、08-21 Adversarial Review（agent-review boundary）、08-26 Recuris（memory-evolution boundary）的 3-boundary cluster 形成 **同一 pattern 的第 4 個 boundary**——本篇是 **harness-evolution boundary**，primitive = 「batch-level cross-instance failure aggregation + multi-role diagnosis」。

**Why this matters for 主人：** 主人 的 hermes-agent-lite / horo-agent 在 SOUL.md 寫著「保守裁切 runtime，WebUI 另裁 UI/navigation」，並把 agent loop / SSE / session schema 標記為不可改核心。但 Ecdysis 顯示：如果「harness evolution 程序」自帶 validation gate（cross-instance + multi-role），那「動 runtime」這件事可以從「禁止」變成「在受控程序下可執行」。具體的 substrate-level 動作：(1) 在 horo-agent-lite 的 plugin contract 設計（08-20 SkillEffect 5-piece contract）加上「plugin 變更必須通過 cross-task failure aggregation」這一層 gate——把 SkillEffect 的 arena-bound function 從「靜態容量上限」升級成「動態驗證通過的容量上限」；(2) 把 hermes-agent-lite 的 memory substrate 變更（08-26 Recuris 5-component loop）加上 multi-role diagnosis（哪些 role 對「要不要寫入」看法不一致，就 hold 該 write 而非 commit）——把 Recuris 的 validation gate 從「單一 Meta-Agent」升級成「multi-role + cross-instance」；(3) 把 agent review 的 R–C inner loop（08-21 AR）加上 batch-level failure aggregation 而非 per-instance——AR 現有的 cap 5 rounds 是 *time* budget，加了 aggregation 就是 *evidence* budget。對比 8-21 AM arrayref supply-chain 攻擊，今天這篇是「harness evolution 程序的安全性」正面案例——主人 那面牆（cargo `--locked`、build script audit）處理 build-time supply chain，今天這面牆是「runtime evolution 程序也要 audit」。

## 3W1H 分析

- **What（做了什麼/主題）：** 提出 Ecdysis 框架，目標是「更便宜、更泛化」的 LLM agent runtime harness evolution。核心兩個新原語：(a) batch-level cross-instance failure aggregation——不再逐 task 修正 harness，而是先把多個 task 的失敗 evidence 一起分析，找出 recurring pattern；(b) Failure-Driven Collaborative Refinement——multi-role diagnosis，幾個 role 對同一 failure 給的歸因不同時，標記為「需要更多 evidence」而非「直接 patch」。最終實驗結果：1.84× training speedup + 18.56% reasoning accuracy 提升（相較既有 harness evolution 方法）。

- **Why（為什麼重要）：** 主人 的 stack 走 hermes-agent-lite / horo-agent 作為 air-gapped + 16GB Mac 友好的下游 runtime。SOUL 寫「保守裁切 runtime」，意味著「動 runtime = 高風險」。但 Ecdysis 顯示「動 runtime」這件事的 cost 與 risk 可以被「cross-instance + multi-role validation」程序大幅壓低——把 runtime evolution 從「禁止」重新定義為「在 validation gate 下可執行」。具體到 substrate-arc：本篇是 harness-evolution boundary，跟 08-20 SkillEffect（tool-dispatch boundary）、08-21 Adversarial Review（agent-review boundary）、08-26 Recuris（memory-evolution boundary）形成 **同一 skill-shape + validation gate pattern 的第 4 個 boundary**——把 08-26 的 3-boundary cluster 升級為 **4-boundary cluster**，代表「runtime primitive 是可以跨 boundary 統一表達的 substrate」這個發現現在有 4 個獨立證據點。

- **How（如何運作/實作）：** 實作層面有兩個層次：(a) **batch-level cross-instance failure aggregation**——把 N 個 task 的失敗 log 收集起來做 pattern matching（recurring failure 標記為 candidate harness deficiency，non-recurring 標記為 model-specific accommodation）；(b) **multi-role diagnosis**——同一個 failure 由多個 role（典型可能是 reviewer / executor / verifier / critic）獨立給 patch proposal，當 patch proposals 不一致時進 freeze-then-aggregate loop。對主人 的 actionable 鏈：(1) 在 horo-agent-lite 的 plugin contract 設計（08-20 SkillEffect 5-piece contract）加一層「plugin 變更必須通過 N=10 task 以上的 cross-instance aggregation」gate——把 SkillEffect 的 arena-bound function 從「靜態容量上限」升級成「動態驗證通過的容量上限」；(2) 把 hermes-agent-lite 的 memory substrate 變更（08-26 Recuris 5-component loop）的 Meta-Agent 升級成 multi-role diagnosis（哪些 role 對「要不要寫入」看法不一致，就 hold 該 write 而非 commit）；(3) 把 agent review 的 R–C inner loop（08-21 AR）加上 batch-level failure aggregation 而非 per-instance——AR 現有的 cap 5 rounds 是 *time* budget，加了 aggregation 就是 *evidence* budget。

- **Insight（個人心得）：** 本文最值得主人 拿走的是 **「harness evolution 的 cost ladder 從『要不要動』升級到『在什麼 validation 程序下可以動』」**——這是 08-13 Tailscale SQLite WAL-Reset 那篇「pin the substrate, make evolution visible」+ 07-23 PM Harness Evolution Shapes Coding Agent Quality「把 harness 從透明中介層拉出來做受控縱貫」的雙重延伸：08-13 是 storage substrate 的 pin、07-23 是「harness 是變數不是常數」的縱貫實證，今天 Ecdysis 是「harness evolution 的 cost 程序」。具體落地建議——對主人 的 horo-agent-lite / hermes-agent-lite runtime evolution 工作：**(a) Layer 0（text-rule，< 5 min）**：在 `~/.hermes/SOUL.md` 加一條「Runtime evolution gate: 任何 agent loop / SSE / session schema 變更必須通過 N ≥ 10 task 的 cross-instance failure aggregation + 至少 2 個 role 的 diagnosis 一致」；**(b) Layer 1（< 1 hr prototype）**：在 horo-agent-lite 加一個 `harness_evolution_gate.py` 腳本（< 80 行 Python），接收 N 個 task failure JSON + K 個 role diagnosis，輸出「approve / hold / reject」三態 verdict，verify 步驟跑 3 個 toy task（naming + classification + arithmetic）測 verdict 與人類標註的一致性 ≥ 80%；**(c) Layer 2（< 4 hr）**：把 Layer 1 的 gate 接到 hermes-agent-lite 的 `hermes plugin install` / `hermes plugin update` 流程（已是 08-20 SkillEffect plugin contract 的一部分），讓「裝 plugin / 升 plugin」這兩個動作自動觸發 cross-instance + multi-role validation，預期能在 1-2 個 plugin 升級週期看到所有 fail-through 點。這條 ladder 同時也是 8-21 PM arrayref supply-chain 那條防線的「runtime-side 對位版」——cargo `--locked` 處理 build-time supply chain，今天的 gate 處理 runtime evolution procedure。成本估計：**Layer 0 + Layer 1 合計 < 70 分鐘，可在下一個 horo-agent plugin 升級週期一次到位**。**Substrate-arc 推進（NEW 09-13）**：3-boundary cluster（08-20 → 08-21 → 08-26）→ **4-boundary cluster**（加入今天 Ecdysis 的 harness-evolution boundary），代表「skill-shape + validation gate」這個 pattern 已經有 4 個獨立 boundary 證據點；對 master 而言這是「runtime primitive 統一表達」的 **structural milestone**，可以考慮把 4 個 primitive 整合成一份「horo-agent Runtime Evolution Protocol」single-source-of-truth doc（< 30 頁），用於下游 + air-gapped + 16GB Mac 的 runtime evolution 治理。