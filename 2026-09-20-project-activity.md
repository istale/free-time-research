---
title: GitHub 自由探索 2026-09-20（14:00 台北時間）
date: 2026-09-20
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 7 天 repeat）
  - web_search 當日 AI 開源新聞（Tier-B web 探索 2）
  - GitHub REST API `/repos/<slug>` + `/releases` + raw `README.md` + PyPI / npm registry
---

# GitHub 專案動態

- 檢查時間：2026-09-20（14:00 台北時間，週日）
- 檢查對象：`cloudflare/security-audit-skill` / `trycua/cua` / `addyosmani/agent-skills` / `volcengine/OpenViking` / `semantica-agi/semantica`
- 來源組合：GitHub Trending today 解析後 Tier-A top 3（**`cloudflare/security-audit-skill` 3,155 ★ today** #1、`trycua/cua` 859 ★ today #2、`addyosmani/agent-skills` 556 ★ today #3，4-9 名依序為 `coder/coder` 402、`anthropics/claude-code` 483、`Open-Dev-Society/OpenStock` 472、`higgsfield-ai/higgsfield` 196、`cactus-compute/needle` 234、`yynxxxxx/Codex-X` 32；前 7 天 Tier-A 內無重複 owner）。Tier-B web_search 命中兩個 strong leads：**`volcengine/OpenViking`**（Self-evolving Context Database for AI Agents、Python AGPL-3.0、38,119★、最新 release `v0.4.20` 2026-09-14 + `python-sdk@0.1.12` 2026-09-18、Active 9 月連續發版、`pip install openviking` 進入 DSV context-db 主戰場）+ **`semantica-agi/semantica`**（Graph-Native Infrastructure for Context and Accountable AI Systems、Python MIT、13,284★、release `v0.6.8` 2026-09-05、OWL/SHACL/SKOS W3C 標準 + 決定性 reasoning engine + W3C PROV-O provenance、鎖定「regulated enterprises 不可用 black box SaaS」、`pip install semantica`）。

---

## Repo 摘要與 3W1H

### `cloudflare/security-audit-skill`

- **Repo 摘要：** 「**A coding-agent skill for multi-phase security audits with independently verified, machine-readable findings.**」——Cloudflare 開源 3 個月的「**multi-phase security audit orchestration**」Claude Code skill、是 [blog.cloudflare.com/build-your-own-vulnerability-harness](https://blog.cloudflare.com/build-your-own-vulnerability-harness) 文章中描述的多階段 fleet 弱點發現系統的單 repo 起點。16,796★、JavaScript、MIT、**topics 為空陣列**、`created` 2026-06-18（**3 個月 = young-but-Cloudflare-blessed**）、`pushed_at` 2026-09-14 19:29 UTC（6 天前仍 push）、`updated_at` 2026-09-20 06:00 UTC、`open_issues_count` 41。**完全沒有 GitHub release**（releases API 回空陣列）—— 屬於「**技能 bundle 走 main + skills.sh、release pipeline 不公開**」型態。**六階段 audit pipeline**：(1) Reconnaissance → `architecture.md` + `coverage-ledger.json`、(2) Coverage-led hunting（isolated hunters + coverage critics）、(3) Candidate validation（每個 unique candidate 給 fresh verifier 反證）、(4) Structured output（`findings.json` 三 verdict：`confirmed` / `needs_validation` / `rejected` + `report-schema.json` 驗證）、(5) Independent record verification（fresh agents verify）、(6) Target-neutral reporting（`REPORT.md` + `FINDINGS-DETAIL.md` + `NEEDS-VALIDATION.md`）。**Adversarial validation 原則**：「check agent ≠ found agent」是核心 contract。**15 個 hunting class files**：attack-classes / memory-safety / AI-LLM / web-protocol-auth / client-side / supply-chain / cloud-deployment / protocols-rpc / resource-exhaustion / data-isolation / desktop-mobile / validation-reporting。**對主人的對位：跟 9-15 `alibaba/open-code-review` + 9-18 `Tencent/BrowserSkill` 同類「agent skill bundle」，但這個獨佔「**security audit methodology-as-skill**」 niche。對主人 horo-agent 的對位是「主人若要搞 security-audit / threat-modeling / compliance-checking pipeline、可以直接拿這個 SKILL.md 框架當本，改寫成 `horo-security-audit` skill」。
- **3W1H：**
  - **What：** Markdown + YAML frontmatter SKILL.md skill collection（15 個 hunting class + workflow files）+ Node.js 零相依 validator scripts（`validate-findings.cjs` + `validate-coverage-ledger.cjs` + 對應 .test.cjs 檔）+ JSON schema（`report-schema.json`）。Skills.sh CLI installer 為官方 install path。
  - **Why：** 傳統 security audit 三大病灶：(1) **agent 自己找、自己驗** → 自我確認偏差嚴重；(2) **checklist-only** → 偏向 vulnerability hunting 而非 attack surface coverage；(3) **no audit trail** → 同一個 repo 重跑結果不一致。**六階段 pipeline 直接破解三病灶**：(a) 找的 agent 跟驗的 agent 強制分離、(b) coverage-ledger 量化「找過什麼」、(c) `findings.json` 結構化可 machine-read + 多輪 additive。**Cloudflare 自己拿這個 skill 跑了 production 弱點發現**，seeded from real Cloudflare bug bounty = 「battle-tested 不是 demo」。**近期受關注原因**：今天是 GitHub Trending #1（3,155★ today）、同 Cloudflare 上週 blog post launch 一起 viral、Owner 為 Cloudflare 旗下 security-ai-research team（security-ai-research@cloudflare.com 直接列在 README 底部）。
  - **Who：** Claude Code user 想跑完整 security audit 在自己 codebase 的；**enterprise security team 想把「security review」標準化為可重複 agent pipeline 的**（每個 PR 跑一次 audit、產出可比對的 findings.json）；**Cloudflare-style vendor 想 ship security-as-skill 進 marketplace 的**（model template）。
  - **How：** **`npx skills add https://github.com/cloudflare/security-audit-skill --skill security-audit`**（skills.sh 是官方 install path），加 `--global` 變成 user-level install。**啟動**：在欲 audit 的 codebase 起 coding agent、貼 trigger：「security audit this codebase」/「find security vulnerabilities in ./src」/「do a security review, output to ~/audits/my-project」。**Requirements**：coding agent 支援 tool use + parallel sub-agents、Node.js（給 validator scripts）、**OS-enforced sandbox**（disable external networking + sanitized env + resource limits，否則 lead 永遠停在 `needs_validation` 不執行 target code）。
- **安裝方式：**
  - **npx skills.sh（最快路徑，官方推薦）：** **`npx skills add https://github.com/cloudflare/security-audit-skill --skill security-audit`** —— 透過 skills.sh CLI 拉 SKILL.md + workflow files 進當前目錄。加 `--global` 變 user-level。
  - **Source clone：** `git clone https://github.com/cloudflare/security-audit-skill` → 手動將 `SKILL.md` + 各 `.md` 拷到 `~/.claude/skills/security-audit/`（繞過 skills.sh、需手動 update）。
  - **npm / pip / uv / poetry：** **不適用**（這不是 package library、是 Claude Code skill collection）。`npx skills add` 是主要 install 入口。
  - **Skills CLI 額外指令：** `npx skills --help` 看 agent-selection + non-interactive 選項。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可跑 `npx skills add https://github.com/cloudflare/security-audit-skill --skill security-audit --global`（需 Node 18+）；裝完 trigger prompt「security audit this codebase」即可。HORO-Agent 對位需自寫 `SKILL.md` schema parser 或直接 import 整個 skill bundle 進 agent config。
- **近期 release：** **未找到 GitHub release**（`/repos/cloudflare/security-audit-skill/releases` 回空陣列）—— 官方 release pipeline 不公開在 GitHub。`pushed_at` 2026-09-14 19:29 UTC（6 天前仍 push）表示 main 仍在 active 開發；**release cadence 不是「半年才一版」，是「ship via main + skills.sh marketplace update」型態**。類似 9-19 `fmtlib/fmt`（半年 stable + main 持續推）的對位但這個是「技能 bundle 不需 semver 化」—— **skill bundle 通常不需要 semver，因為 SKILL.md 內容本身是 source-of-truth、版本鎖定由 skills.sh marketplace 處理**。

### `trycua/cua`

- **Repo 摘要：** 「**Scale computer-use 2.0 with open-source drivers, cross-OS fleets, and benchmarks for training, evaluation, and data generation.**」——trycua 開源 1 年 8 個月的「**Computer-Use 2.0 開源 framework**」、給 AI agent 「真實可用電腦」的 desktop automation + isolated cloud desktop + Apple Silicon 本地 macOS VM + small specialist decision model + evaluation benchmark 五件套 monorepo。24,646★、HTML、MIT、topics 18 個（`agent / ai-agent / apple / computer-use / computer-use-agent / containerization / cua / desktop-automation / hacktoberfest / lume / macos / manus / operator / swift / virtualization / virtualization-framework / windows / windows-sandbox`）、`created` 2025-01-31（**1 年 8 個月**）、`pushed_at` 2026-09-20 02:14 UTC（**今天仍在 push = 仍在 hot-fix**）、`updated_at` 2026-09-20 06:00 UTC、`open_issues_count` **1,032**（viral + 大量 bug/feature request）。**Latest stable release `sandbox-v0.8.0` published 2026-09-15 16:48 UTC（5 天前）**，prior 4 個 release 為 nightly `cua-driver-rs-v0.28.3-nightly.*`（2026-09-19/18/17/16 連 4 天 nightly）。**5 個 sub-project**：(1) **Cua Fleets**（[run.cua.ai](https://run.cua.ai) = managed cloud desktops Sandbox SDK，provision Linux desktop 跑命令 + screenshot）、(2) **CUA-S1**（small specialist System-1 decision model、forms-focused、HuggingFace `cua-ai/cua-s1-forms` 開源 weights）、(3) **Cua Driver**（macOS / Windows / Linux desktop inspection + control via CLI + MCP + typed SDK，background delivery 不搶 focus）、(4) **Lume**（Apple Silicon 上用 Apple Virtualization.Framework 開 macOS Tahoe / Linux VM，SSH 連）、(5) **Cua Bench**（computer-use task 建構 + evaluate + export trajectories）。**對主人的對位：跟 9-19 `fmtlib/fmt`（system-level C++ lib）有點像「**主人 HORO Python stack 不直接對位、但屬 reference observation**」—— 但 trycua/cua 對主人「Apple M4 Max VM」特別有對位價值：**Lume 能在 Apple Silicon 上開 macOS VM、跟主人 HORO-Agent multi-VM workflow 可結合**：例如用 Lume 開 isolated macOS VM、跑 HORO-Agent 在 guest、host 監控 driver/screenshot。對主人 horo-agent 對位是「**Computer-Use 是 multi-modal agent 必備 substrate、trycua 是 open-source leader**」。
- **3W1H：**
  - **What：** Monorepo（5 sub-project）+ Python SDK + Rust `cua-driver-rs` + Swift (Apple Virtualization.Framework bindings) + Node.js / MCP integration + HuggingFace model weights。**HTML 主要 language** 是因為 docs site 主檔。License MIT、但 optional `cua-agent[omni]` extra 帶 ultralytics（AGPL-3.0）、Kasm（MIT）、OmniParser（CC-BY-4.0）三個 third-party。**5 install pattern**：(a) **Cua Driver** macOS/Linux：`curl -fsSL https://cua.ai/driver/install.sh | bash`、(b) Windows PowerShell：`irm https://cua.ai/driver/install.ps1 | iex`、(c) **Lume**：Apple Silicon 本地 VM、`curl -fsSL https://cua.ai/lume/install.sh | bash`、(d) **Cua Bench**：`uv tool install 'cua-bench[browser]'` + `playwright install chromium`、(e) **Cua Fleets**：managed service at run.cua.ai。
  - **Why：** 2025-2026 「Computer-Use 2.0」三個新基礎：(1) **Lume** —— Apple Silicon 上本地 macOS VM（不是 docker-in-docker、是真正的 Apple Virtualization.Framework VM），(2) **Cua Driver** —— 多 OS desktop automation with **background delivery**（不搶 user focus、parallel agents 不互相打架），(3) **CUA-S1** —— **small specialist System-1 models**（不是 huge general model，是 forms-focused fast decision model、decision in <100ms）。**對位 OpenAI Operator + Anthropic Computer-Use + Manus**：trycua 是 open-source counterpart、`Lume` 是 Apple Silicon 獨佔差異化（雲端 vendor 不能 easy replicate on-prem Apple VM）。**近期受關注原因**：(a) Lume 對位 Apple Silicon M4 個人開發者、(b) CUA-S1 forms-focused model 對位 enterprise RPA 自動化、(c) Computer-Use benchmark 訓練資料需求暴增 → 這個提供 `cua-bench` task creator。
  - **Who：** **Apple Silicon 開發者** 想跑 isolated macOS VM 做 AI agent testing 的（Lume）；**enterprise automation team** 想 ship computer-use agent 進 production 的（Cua Driver + Fleet）；**AI training team** 需要 computer-use trajectory data 的（CUA-S1 + Cua Bench）；**researcher** 想 reproduce OpenAI Operator / Anthropic Computer-Use 結果的。
  - **How：** 5 sub-project 各自獨立 install（見上面）。**典型 workflow**：(1) `curl -fsSL https://cua.ai/lume/install.sh | bash` 開 macOS VM、(2) `curl -fsSL https://cua.ai/driver/install.sh | bash` 裝 driver、(3) coding agent（Claude Code / Codex / Cursor / OpenClaw）透過 MCP 接 driver、(4) agent 自動化 desktop app、(5) `uv tool install 'cua-bench[browser]'` 跑 evaluation。**managed path**：直接用 [run.cua.ai](https://run.cua.ai) 跳過 local setup。
- **安裝方式：**
  - **Cua Driver macOS / Linux（最簡單路徑）：** **`/bin/bash -c "$(curl -fsSL https://cua.ai/driver/install.sh)"`** — 一行 bash installer。
  - **Cua Driver Windows（PowerShell）：** **`irm https://cua.ai/driver/install.ps1 | iex`**。
  - **Lume（Apple Silicon 本地 macOS / Linux VM）：** **`/bin/bash -c "$(curl -fsSL https://cua.ai/lume/install.sh)"`**。
  - **Cua Bench（evaluation + task creator）：** 需 Python 3.12 / 3.13 + uv，**`uv tool install 'cua-bench[browser]'`** + **`uv tool run --from 'cua-bench[browser]' playwright install chromium`**。
  - **Python SDK：** `pip install cua` 或 `uv pip install cua`（底層 bindings 是 Python wrapping Rust `cua-driver-rs`）。
  - **Cua Fleets：** managed service → [run.cua.ai](https://run.cua.ai) 註冊 + 拿 API key，**無 local install**。
  - **npm / poetry：** 不適用（Cua Driver / Lume 是 shell install、Cua Bench 是 uv tool、Cua Fleets 是 SaaS）。
  - **主人硬體可行性：** 主人在 **Apple M4 Max VM（16 GB RAM）** + macOS 26.3.1 —— **Lume 對位極好**：Apple Virtualization.Framework 在 Apple Silicon 上 native support、可開 macOS Tahoe VM guest。**注意**：Lume 需要 host 是真 Apple Silicon（不是 VM 內的 virtualized Mac）。**主人在 VirtualMac2,1 VM 內跑、Lume 可能無法在 VM guest 內再開 nested VM** —— 但可作為 reference design、用於其他 Apple Silicon 主機。主人 HORO-Agent Python 端要接 cua 走 `pip install cua` + MCP stdio bridge。
- **近期 release：** **`sandbox-v0.8.0` published 2026-09-15 16:48 UTC（5 天前）** = latest stable release。Prior 4 release 全為 nightly `cua-driver-rs-v0.28.3-nightly.20260919.35421378483` (2026-09-19) / `.20260918` (09-18) / `.20260917` (09-17) / `.20260916` (09-16) —— **連 4 天 daily nightly、加上 sandbox stable release、是非常 active 雙 train release engineering**。nightly 跟 stable 分流：nightly = driver-rs Rust hot-fix、stable = Sandbox SDK API stabilization。**Release cadence 不規則** —— stable 偶爾出（不固定週期）、nightly 每天出，main 仍 active push。**跟 9-19 `deepseek-ai/deepseek-harness` 「alpha + rc + next + latest 4-tag npm train」同類多-train release engineering**，但這個是「nightly + stable」對位。**1,032 open issues** 屬「viral + 大量 community ask」、跟 9-19 `DietrichGebert/ponytail` 277 issues 同型「popularity metric 不是 quality metric」。

### `addyosmani/agent-skills`

- **Repo 摘要：** 「**Production-grade engineering skills for AI coding agents.**」——Addy Osmani（Google Chrome team 工程總監、`Learning JavaScript Design Patterns` 作者）開源 7 個月的「**25 個 senior engineer workflow skill**」Claude Code skill bundle、走 **lifecycle（DEFINE → PLAN → BUILD → VERIFY → REVIEW → SHIP）** 完整鏈。97,189★、JavaScript、MIT、topics 6 個（`agent-skills / antigravity / claude-code / codex / cursor / skills`）、`created` 2026-02-15（**7 個月**）、`pushed_at` 2026-09-18 03:32 UTC（2 天前仍 push）、`updated_at` 2026-09-20 06:00 UTC、`open_issues_count` 111。**Latest release `0.6.10` published 2026-09-18 03:32 UTC（2 天前）**—— release cadence **6 個 release 在 45 天內**（`0.6.10` 09-18 / `0.6.9` 09-05 / `0.6.8` 08-28 / `0.6.7` 08-14 / `0.6.6` 08-04 / `0.6.5` 07-22 左右）= 約每 9 天出一版的 active cadence。**25 個 skill 全 lifecycle 化**：(1) Meta: `using-agent-skills`、(2) Define: `interview-me` / `idea-refine` / `spec-driven-development` / `constraint-driven-development`、(3) Plan: `planning-and-task-breakdown`、(4) Build: `incremental-implementation` / `test-driven-development` / `context-engineering` / `source-driven-development`、(5) Verify、Review、Ship 各 2-3 個 skill。**裝法極廣** —— 官方支援 12 個 agent platform：Claude Code / Codex / Cursor / OpenCode / GitHub Copilot / Kiro / Command Code / Antigravity / Gemini CLI / Windsurf / Cline / Cody —— 每個平台都有專屬 install 指令。**對主人的對位：跟 9-15 `alibaba/open-code-review` + 9-16 `addyosmani/agent-skills` 之前已出現、9-18 `Tencent/BrowserSkill` + 9-19 `mattpocock/skills` 同類「marketplace-driven skill collection」 —— 但這個獨佔「**lifecycle completeness + multi-agent-platform reach**」 niche**。跟 mattpocock/skills 對位 = 「25 個 lifecycle skill」vs「7 個 opinionated skill」、跟 ponytail 對位 = 「production-grade full lifecycle」vs「YAGNI single-style」、跟 Tencent/BrowserSkill 對位 = 「vendor-neutral engineer」vs「vendor browser-control」。對主人 horo-agent 是「**如果主人想要一個 full lifecycle skill collection reference、addyosmani/agent-skills 是最大最完整的**」。
- **3W1H：**
  - **What：** Markdown + YAML frontmatter SKILL.md skill collection（25 個）+ 每個 skill 一個目錄 + docs/ 子目錄（每個 agent platform 一個 setup doc）+ `.claude-plugin/` / `.codex-plugin/` 等 12 個 agent platform plugin manifests + adoption guide + Anti-rationalization tables（每個 skill 內建「**為什麼 agent 會跳過這步**」對照表）。
  - **Why：** 現有 skill collection 三大對位：(1) **`mattpocock/skills`** —— 7 個 opinionated 個人 engineering style、(2) **`DietrichGebert/ponytail`** —— YAGNI single prompt-engineering skill、(3) **`Tencent/BrowserSkill`** —— vendor browser-control skill。**今天 `addyosmani/agent-skills` 提供的是「**lifecycle 完整覆蓋、從 DEFINE 到 SHIP 都給 skill**」+「**12 個 agent platform 都支援、auto-detect**」** —— 不是某一個 phase 強、是全 phase 都強 + multi-platform reach 強。Addy Osmani 是 Google Chrome engineering lead + `Learning JavaScript Design Patterns` 暢銷書作者、personal brand 強。**Anti-rationalization tables** 是這個 bundle 的 critical UX 設計：每個 skill 內建「agent 為什麼會跳過這步 + 為什麼不該跳」對照 —— 解決 agent 收到「write test before code」會偷懶的問題。
  - **Who：** **AI coding agent user**（Claude Code / Codex / Cursor / OpenCode / GitHub Copilot 等）想升級到 senior-engineer-level workflow 的；**tech lead** 想 ship 一致性 engineering practice 給全 team 的（裝同一個 skill bundle）；**enterprise developer** 想 vendor-neutral + multi-platform skill bundle 但要 active maintenance 的（addyosmani 是 Google employee = reliable steward）。
  - **How：** **Multi-platform install**（12 個 platform）：(a) **Claude Code**：plugin marketplace、`/plugin install addyosmani-agent-skills` 或 `npx skills add addyosmani/agent-skills`；(b) **Codex CLI** v0.122+：`codex plugin marketplace add addyosmani/agent-skills` + `codex plugin add agent-skills@agent-skills`；(c) **Cursor**：拷到 `.cursor/rules/` 或 `.cursor/skills/`；(d) **OpenCode**：拷到 `.opencode/skills/` + project `AGENTS.md`；(e) **Command Code**：`cmd skills add addyosmani/agent-skills`（native 指令，clone repo + auto-discover SKILL.md）；(f) 其他 7 個 platform 各有 setup doc。
- **安裝方式：**
  - **Claude Code plugin marketplace（最快路徑，auto-update）：** **`claude plugins install addyosmani-agent-skills`**（marketplace 命令）或 session 內 **`/plugin install addyosmani-agent-skills`**。
  - **skills.sh 路徑（editable 拷貝）：** **`npx skills@latest add addyosmani/agent-skills`** — 拷貝進 project、可改。
  - **Codex CLI v0.122+：** **`codex plugin marketplace add addyosmani/agent-skills`** + **`codex plugin add agent-skills@agent-skills`**。
  - **Command Code（native 指令）：** **`cmd skills add addyosmani/agent-skills`** 或 **`cmd skills add addyosmani/agent-skills --global`** 或 **`cmd skills add addyosmani/agent-skills -s spec-driven-development`**（裝特定 skill）。
  - **Cursor：** 手動拷 `.cursor/rules/` 或 `.cursor/skills/`、加 `AGENTS.md`。
  - **OpenCode：** 拷到 `.opencode/skills/` + `AGENTS.md`、用 built-in `skill` tool。
  - **GitHub Copilot：** `agents/` 內 persona + `.github/copilot-instructions.md`。
  - **Kiro IDE/CLI：** `.kiro/skills/`、project 或 global level。
  - **Source clone：** `git clone https://github.com/addyosmani/agent-skills` 後手動安裝。
  - **pip / uv / poetry：** **不適用**（這不是 Python library、是 skill collection）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM + macOS 26.3.1 可跑 `npx skills@latest add addyosmani/agent-skills`（需 Node 18+）；裝完 25 個 skill 進 Claude Code。HORO 是其他 stack、要將 skill bundle 鑽孔去 HORO 需自寫 SKILL.md schema parser。
- **近期 release：** **`0.6.10` published 2026-09-18 03:32 UTC（2 天前）** = latest stable。Release history：`0.6.10` (09-18) / `0.6.9` (09-05) / `0.6.8` (08-28) / `0.6.7` (08-14) / `0.6.6` (08-04) = **45 天內 5 版、9 天一版 active cadence**。**`pushed_at` 09-18 03:32 UTC（2 天前仍 push）**、main 持續 preview 開發。**Latest `0.6.10` 主要是 command-code + codex plugin 適配 + skill 內容 polish**。**跟 9-19 `fmtlib/fmt` 「半年 stable + main 預備下一版」相反**：這個走「active bi-weekly stable cadence」、是 enterprise-grade open-source project 該有的 release rhythm。對位 9-19 `zhayujie/CowAgent`「2-週 stable」是同類 cadence、但這個更簡潔（semver `0.6.x` 跳版、不像 CowAgent 走 narrative `v2.1.x`）。

### `volcengine/OpenViking`

- **Repo 摘要：** 「**OpenViking: The Context Database for AI Agents — Self-evolving Context Database for AI Agents. Unify Agent Memory, Knowledge RAG and Skills.**」——字節跳動旗下火山引擎（ByteDance's cloud）開源 8.5 個月的「**Context Database for AI Agents**」、**Viking URI virtual filesystem** (`viking://`) 把 agent memory + RAG + skills 全統一成 filesystem semantics、**L0/L1/L2 三層 on-demand context loading** + 「**VikingBot enabled `ov compile`**」自動把 session 編譯成 wiki / knowledge graph / report。38,119★、Python、**AGPL-3.0**、topics 6 個（`agent-memory / agent-plugins / agentic-rag / context-database / dsh-plugin / self-evolving`）、`created` 2026-01-05（**8.5 個月 = mature 但仍在 viral**）、`pushed_at` 2026-09-20 04:10 UTC（**今天仍在 push = 仍在 hot-fix**）、`updated_at` 2026-09-20 05:46 UTC、`open_issues_count` **722**。**Latest release `v0.4.20` published 2026-09-14 05:57 UTC（6 天前）**—— 但 **`python-sdk@0.1.12` published 2026-09-18 10:17 UTC（2 天前）** 是更新 SDK release、**`cli@0.4.20` 09-14**、**`sdk/go/v0.0.2` 09-10**、**`python-sdk@0.1.11` 09-14**。**多 SDK 多 train release engineering**：core (`v0.4.20`) + python-sdk (`@0.1.12`) + go-sdk (`v0.0.2`) + cli (`@0.4.20`) = 4 條 train。**架構核心 4 件**：(1) `viking://` virtual filesystem (resources / user memories / skills / peers)、(2) **L0/L1/L2 三層 context loading**（Abstract / Overview / Details，agent 先看 L0/L1 再 on-demand load L2）、(3) **Context compilation**（`ov compile` 把 source material 編成 wiki / KG / report，背後 VikingBot skill）、(4) **12 個 native integration**：Claude Code / Codex / Cursor / Trae / OpenClaw / Hermes Agent / OpenCode / pi / DeerFlow / Claude Code CLI / **Hermes Agent (`Built-in`)** — **Hermes Agent 是 `Built-in` 不是 Hooks+Plugin、其他都是 Plugin+MCP**。**Benchmark on 0.3.22 LoCoMo + tau2-bench**：LoCoMo accuracy OpenClaw 24.20% → 82.08% with OpenViking、Hermes 33.38% → 82.86%、Claude Code 57.21% → 80.32%（**3 個 agent 跳 +24-58pp**），tau2-bench Retail 70.94% → 77.81%、Airline 54.38% → 66.25%（**+6.87pp / +11.87pp**），input tokens 降 34.3-91.0%、query latency 降 58.45-66.10%。**對主人的對位：跟 9-19 `fmtlib/fmt` 一樣是「**主人 HORO Python-stack 直接整合可見**」 —— 但因為 Hermes Agent 是 `Built-in` integration、**OpenViking 是主人 HORO-Agent 的 native partner**：可直接拿 `pip install openviking` + 設 `openviking-server init` + 連 Hermes Agent = 自動跨 session memory + VikingBot compile session → wiki/KG/report。**主人之前已有 Hermes Agent + now2 + Hermes-Agent 自家 agent、OpenViking 是給主人 agent 跨 session memory 的最對位 substrate**。
- **3W1H：**
  - **What：** Python monorepo（核心）+ Python SDK + Go SDK + TypeScript SDK + HTTP API + `ov` CLI + openviking-server daemon + Web Studio (`web-studio/`) + VikingBot skill + 12 個 agent integration module（Claude Code / Codex / Cursor / Trae / OpenClaw / **Hermes Built-in** / OpenCode / pi / DeerFlow / 等）。License **AGPL-3.0**（**不是 MIT** —— enterprise 自架要注意 copyleft）。
  - **Why：** 現有 agent memory/RAG 有三條路：(1) **Vector DB + RAG**（無結構、無 provenance、無 governance）、(2) **Plain LLM Memory**（token window 限、無 history、無 decision trail）、(3) **`volcengine/OpenViking`** —— 「**filesystem abstraction for agent context**」：memory + knowledge RAG + skills 全統一到 `viking://` URI、L0/L1/L2 三層 on-demand loading 解 token 浪費、Context compilation (`ov compile` + VikingBot) 自動組織 session → wiki。**Benchmark 證明跨 agent 整合可達 80-83% accuracy vs 24-57% native memory、token 省 34-91%、latency 省 58-66%** = 不是 demo、是 production measurable improvement。**近期受關注原因**：(a) viral 38k★、(b) 9 月連續出 5+ release、(c) 主人 Hermes Agent `Built-in` integration 是跟主人最直接相關的 selling point。
  - **Who：** **Agent framework developer** 想 ship 跨 session memory 給 end user 的（裝 OpenViking + integration plugin）；**enterprise developer** 想自架 AGPL-3.0 context DB（不送資料到 vendor cloud）的；**AI agent researcher** 想 quantify memory 改善的（benchmark 用）；**主人 HORO-Agent user** —— **Hermes Agent 已是 Built-in integration、裝了就直接跨 session memory + VikingBot wiki compile**。
  - **How：** **Server + client two-process 架構**：(1) **`pip install openviking --upgrade`** + **`openviking-server init`**（設 providers and models，支援 Volcengine / OpenAI / Codex OAuth / Kimi / GLM / local Ollama）+ **`openviking-server doctor`**（驗配置）+ **`openviking-server`**（起 server）。(2) 另一個 terminal 用 `ov` CLI：`ov add-resource https://github.com/volcengine/OpenViking` → `ov task status TASK_ID` → `ov ls viking://resources/` → `ov tree viking://resources/volcengine -L 2` → `ov find "what is openviking"` → `ov grep "openviking" --uri viking://resources/volcengine/OpenViking/docs/en`。**Connect to agent**：(a) Claude Code：Hooks + MCP、(b) Hermes Agent：**Built-in**（不用另設）、(c) Cursor：Hooks + MCP、(d) OpenClaw：Context engine。
- **安裝方式：**
  - **pip（最簡單路徑，server + SDK + CLI）：** **`pip install openviking --upgrade`** 或 **`uv pip install openviking`**。需要 Python 3.10+ + 一個 embedding model + 一個 VLM（cloud 或 local）。
  - **pip + uv：** **`uv pip install openviking`**（如果用 uv 管理）。
  - **Server init：** **`openviking-server init`** → 寫 `~/.openviking/ov.conf`，設定 provider（Volcengine / OpenAI / Codex OAuth / Kimi / GLM / Ollama local）。
  - **CLI commands：** `openviking-server`（起 server）、`openviking-server doctor`（驗配置）、`ov status` / `ov add-resource <url>` / `ov task status <TASK_ID>` / `ov ls <viking_uri>` / `ov tree <viking_uri> -L 2` / `ov find <query>` / `ov grep <pattern> --uri <viking_uri>`。
  - **Hermes Agent 整合（主人對位，Built-in）：** **主人 HORO-Agent 已內建 OpenViking 支援** —— 在 hermes config 設 `openviking.enabled = true` 即可（具體需查 Hermes 文件）。
  - **Docker：** Railway one-click deploy 按鈕（README 有 badge）。
  - **npm / poetry：** **不適用**（這是 Python server + SDK，TypeScript / Go SDK 另從 source build）。**`poetry add openviking`** 理論上可（如果發 PyPI wheel）。
  - **Source build：** `git clone https://github.com/volcengine/OpenViking` + Python SDK 在 `sdk/python/`、Go SDK 在 `sdk/go/`、TS SDK 在 `sdk/typescript/`。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可走 **`uv pip install openviking`** + **`openviking-server init`**（設 provider = OpenAI 或 Codex OAuth）+ **`openviking-server`** 起 daemon。Hermes Agent 內建整合 = 直接 `pip install openviking` + 重啟 HORO 即可。**注意 AGPL-3.0 license**：主人若要 distribute 改版 OpenViking、需 open source 改版。主人 HORO 是 Python + Hermes Agent、`pip install openviking` 是 native install path。
- **近期 release：** **`v0.4.20` published 2026-09-14 05:57 UTC（6 天前）** = latest core release。**Multi-train release engineering**：core `v0.4.20` 09-14、python-sdk `@0.1.12` 09-18（**2 天前更新**）、cli `@0.4.20` 09-14、python-sdk `@0.1.11` 09-14、sdk/go `@0.0.2` 09-10。**跟 9-19 `deepseek-ai/deepseek-harness`「alpha + rc + next + latest 4-tag」** 對位是「core + python-sdk + go-sdk + cli 4 train」、更分散的 train 管理。**Main 仍 active push**（pushed_at 2026-09-20 04:10 UTC 今天仍 push）。**典型「active multi-SDK release engineering + main 持續 preview」**，跟 9-19 `Tencent/BrowserSkill`「release 切了 main 繼續推」型成熟 cadence 接近。

### `semantica-agi/semantica`

- **Repo 摘要：** 「**Graph-Native Infrastructure for Context and Accountable AI Systems — Developer-first, knowledge infrastructure for AI, alternative to expensive enterprise platforms.**」——Semantica AGI 開源 1 年 3 個月的「**Graph-Native Context Infrastructure**」、把 enterprise data 建成 Context Graph + knowledge graph（OWL / SHACL / SKOS / W3C 標準 ontology）、**deterministic reasoning engine**（forward chain + Rete + Datalog + SPARQL、**no LLM required** for graph construction + reasoning + provenance）、**W3C PROV-O provenance** on every fact、**Polyglot graph storage**（RDF: Oxigraph / Blazegraph / Jena / RDF4J；LPG: Neo4j / FalkorDB / AGE / Neptune）+ **native enterprise data platform connectors**（Databricks Unity Catalog + Delta Lake / Snowflake / SAP OData）。13,284★、Python、MIT、**topics 20 個**（`agent-memory / ai / ai-governance / ai-infrastructure / artificial-intelligence / context-engineering / context-graphs / data-engineering / decision-intelligence / developer-tools / explainable-ai / generative-ai / graph-rag / knowledge-graph / llm / ontology / provenance / python / reasoning / semantic-search`）、`created` 2025-06-25（**1 年 3 個月**）、`pushed_at` 2026-09-19 18:03 UTC（昨天仍 push）、`updated_at` 2026-09-20 05:59 UTC、`open_issues_count` 75。**Latest release `v0.6.8` published 2026-09-05 14:01 UTC（15 天前）** —— release cadence 約 2 週：`v0.6.8` 09-05 / `v0.6.7` 08-28 / `v0.6.6` 08-20 / `v0.6.5` 08-11 / `v0.6.0` 07-21。**Built-in integrations**：Agno / CrewAI / LangChain + **full MCP server** + CLI + REST API + plugins across major editors。**Deterministic KG pipeline**：multi-source ingestion + entity-aware chunking + NER / relation / event extraction + graph construction + semantic dedup + provenance-preserving merges。**Decision provenance as first-class object**：every decision traceable + searchable by precedent + causally linked。**對主人的對位：跟 `volcengine/OpenViking` 同「context database」substrate 但哲學不同 —— **OpenViking 是「filesystem + L0/L1/L2 tiered loading + 火山引擎自家 engine」、Semantica 是「graph + W3C standards + deterministic reasoning + zero LLM for graph ops」**。Semantica 對主人 HORO-Agent 的對位是「**如果主人 HORO 要 ship enterprise-grade compliance / regulated industry agent、Semantica 是更對位的 substrate** —— 因為它 W3C PROV-O 可 export 給 regulator 接受、SHACL 可表達 compliance rule、deterministic reasoning 不需 LLM 解釋就 auditable」。對主人「HORO Python + Hermes Agent + enterprise positioning」對位是「如果 HORO 要做 compliance / regulated vertical、需要 deterministic reasoning + W3C provenance substrate」。
- **3W1H：**
  - **What：** Python monorepo（核心 KG + reasoning engine + ingestion + decision intelligence）+ PyPI `semantica` wheel + CLI + REST API + MCP server + Agno / CrewAI / LangChain integration + Neo4j / FalkorDB / AGE / Neptune / Oxigraph / Blazegraph / Jena / RDF4J graph backend drivers + Databricks / Snowflake / SAP OData enterprise connectors + visualizer (browser workbench) + 4 個 i18n README（English / Deutsch / Français / Español / Italiano / Português / العربية / اردو / हिन्दू / 中文 / 日本語 / 한국어 = 12 種語言）。
  - **Why：** 現有 enterprise AI 三大痛點：(1) **Vector DB + RAG** 只存 embedding similarity、無結構、無 provenance、無 governance；(2) **Plain LLM Memory** 只存 token、無 decision history、無 audit trail；(3) **「為什麼 AI 這樣決策」** 在 regulated industry 是 regulator 必問、目前 LLM 是 black box。**Semantica 解三痛點**：(a) **graph-native** = 結構化 + 可 query + 可 traverse、(b) **W3C standards** = OWL ontology / SHACL constraints / SKOS vocabularies / PROV-O provenance = regulator 接受、(c) **deterministic reasoning** = forward chain + Rete + Datalog + SPARQL = 不需 LLM 就 auditable。**Why Semantica 比較表** 直接列出 9 行 vector DB + plain LLM memory 都不行、Semantica 全有。**近期受關注原因**：(a) regulated industry AI 需求（金融 / 醫療 / 法律 / 政府 / 國防）、(b) EU AI Act + 美國 AI 行政命令需要 auditable AI、(c) Databricks / Snowflake 企業用戶想 self-host KG 不送 SaaS。
  - **Who：** **AI/ML platform team** 想 ship 結構化 queryable context 給 agent 的（不是 vector index）；**enterprise data team on Databricks / Snowflake / SAP** 想把 lakehouse / warehouse 變 lineage-tracked KG 的；**compliance / risk / audit team** 需要 regulator-acceptable「why AI 這樣決策」；**regulated enterprises**（金融 / 醫療 / 法律 / 政府 / 國防）不能 ship black box、不能送資料到 SaaS 的；**platform engineer** 想 self-host + swappable KG + reasoning + provenance stack 的；**data / knowledge engineer** 想建 KG from messy multi-source data、conflict detection + dedup 的。
  - **How：** **`pip install semantica`** + Python API：`from semantica.context import ContextGraph` + `graph = ContextGraph(advanced_analytics=True)` + `decision_id = graph.record_decision(category="vendor_selection", scenario="...")`（every agent decision becomes queryable auditable knowledge node）。**Configurable graph backend**：Neo4j / FalkorDB / AGE / Neptune / Oxigraph / Blazegraph / Jena / RDF4J（**全部 swappable 不改 code**）。**Integration 入口**：Agno / CrewAI / LangChain 都有 drop-in integration、MCP server 全功能、CLI 全功能、REST API 全功能。
- **安裝方式：**
  - **pip（最簡單路徑）：** **`pip install semantica`**（PyPI wheel、**Python 3.8+**）。也可 `uv pip install semantica` 或 `poetry add semantica`。
  - **From source：** `git clone https://github.com/semantica-agi/semantica` + `pip install -e .`（for contributor / customization）。
  - **CLI：** `pip install semantica` 後有 `semantica` CLI 指令（具體 subcommand 看 docs）。
  - **MCP server：** Semantica 自帶 full MCP server、可被 Claude Code / Hermes Agent / 其他 MCP client 直接連。
  - **Graph backend install（optional）：** Neo4j / FalkorDB / Oxigraph / Blazegraph / Jena / RDF4J 各需獨立 install、Semantica 透過 driver 連線。**default backend 是 in-memory + filesystem = 不需另外裝**。
  - **Enterprise data platform connector（optional）：** Databricks / Snowflake / SAP OData 各需 service credential。
  - **Docker：** 未在 README 強調、可能需自寫 Dockerfile（base image `python:3.11-slim` + `pip install semantica`）。
  - **npm：** **不適用**（這是 Python library、無 JS binding）。**`pip install semantica` 是唯一官方 install path**。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM + macOS 26.3.1 可走 **`pip install semantica`**（需 Python 3.8+）。**注意** Semantica 對系統需求比 OpenViking 重（KG + reasoning engine + 多 backend driver）、建議主人先試 in-memory backend 再考慮 Neo4j / FalkorDB。
- **近期 release：** **`v0.6.8` published 2026-09-05 14:01 UTC（15 天前）** = latest stable。Release history：`v0.6.8` (09-05) / `v0.6.7` (08-28) / `v0.6.6` (08-20) / `v0.6.5` (08-11) / `v0.6.0` (07-21) = **約 2 週一次 stable cadence**。Main 持續 preview 開發（pushed_at 2026-09-19 18:03 UTC 昨天仍 push）。**Release cadence 屬「bi-weekly stable + main 持續 preview」型**、跟 9-19 `addyosmani/agent-skills` 9 天一版相近、但這個稍微長（2 週一版）。**跟 OpenViking 對位是「**release 較少 + 每次 release 較重**」（OpenViking 4 train 同步推、Semantica 單 train）；背後 reason：Semantica 的「deterministic reasoning」要嚴謹測試、不能像 OpenViking filesystem 那樣快速加 feature。

---

## 重點觀察

- **Trending top 3 全押「AI agent skill bundle / framework」+ 兩個 Tier-B 全押「context database / knowledge graph substrate」—— 14:00 slot 連續第 2 個 tick 出現「AI infra substrate 雙主流」模式**：今天 Tier-A top 3（`cloudflare/security-audit-skill` security-audit skill + `trycua/cua` computer-use framework + `addyosmani/agent-skills` 25-skill lifecycle bundle）+ Tier-B top 2（`volcengine/OpenViking` context database + `semantica-agi/semantica` knowledge graph）= **3 個 skill/framework + 2 個 substrate、5 個 pick 全部 AI agent 對位、零 C++ library / 零 generic tool**。跟 9-19「3/5 不是 vendor AI agent」（fmtlib/fmt + mattpocock/skills + ponytail）相比、今天 5/5 100% AI agent 對位 —— 14:00 slot 重新回到「AI agent 主導」狀態。**Substrate 雙主流 pattern**：OpenViking = 「filesystem + tiered loading」風格（火山引擎自家 design）、Semantica = 「graph + W3C standards」風格（W3C 標準本體）—— **同一個 niche 兩個對位 design choice、是 substrate-market 進入「second wave」的訊號**：第一 wave 是 vector DB（Chroma / Pinecone / Weaviate）、第二 wave 是 context database with reasoning + provenance。對主人 horo-agent 對位是「**substrate 選擇 = 哲學選擇**：filesystem + LLM-augmented vs graph + deterministic reasoning = 不同 enterprise positioning」。**OpenViking 對主人 Hermes Agent 是 Built-in、Semantica 是 generic Python library 需自己接 Hermes** —— 對主人最 immediate value 是 OpenViking。

- **5/5 都有實際「上層框架 / 上層協議 / 上層 marketplace」整合 story —— 上層 infra 競爭已經從「功能」移到「整合密度」**：今天 5 個 pick 全有 explicit integration story：(a) **cloudflare/security-audit-skill**：skills.sh marketplace 1st-class、(b) **trycua/cua**：5 sub-project 整合（Fleets / Driver / Lume / S1 / Bench）+ Claude Code / Codex / Cursor / OpenClaw 4 agent 整合、(c) **addyosmani/agent-skills**：12 個 agent platform 整合 + 全有專屬 setup doc、(d) **volcengine/OpenViking**：12 個 native agent integration（含 Hermes Built-in）、(e) **semantica-agi/semantica**：Agno / CrewAI / LangChain + MCP server + CLI + REST API + 8 graph backend driver + 3 enterprise data connector。**整合 density 排行**（per repo）：addyosmani/agent-skills 12 platform > volcengine/OpenViking 12 agent > trycua/cua 4 agent + 5 sub-project > semantica-agi/semantica 8 graph + 3 enterprise > cloudflare/security-audit-skill 1 marketplace。**對主人 horo-agent 的對位是「**ship 一個新 Python lib、若沒 explicit MCP server / Claude Code plugin / Hermes integration 設計，會被 trending tier 邊緣化**」** —— 整合設計是 OSS viral 的必要條件、不是 nice-to-have。

- **Release freshness 跨 spectrum：「無 release」到「5 train daily nightly」5 種 cadence 全部出現 + skill bundle 通常「無 release」**：今天 5 個 pick release freshness 跨 5 種極端：(a) **cloudflare/security-audit-skill**：**無 release**、靠 main + skills.sh update（**skill bundle 不需 semver 化**）、(b) **trycua/cua**：stable 5 天前 + nightly 連 4 天（**multi-train daily + stable bi-weekly**）、(c) **addyosmani/agent-skills**：2 天前 0.6.10（**9 天一版 active cadence**）、(d) **volcengine/OpenViking**：6 天前 core + 2 天前 python-sdk + 6 天前 cli + 10 天前 go-sdk（**4 train 分散 release engineering**）、(e) **semantica-agi/semantica**：15 天前 0.6.8（**bi-weekly stable cadence**）。**5 種 cadence 各對應不同生態 niche**：(a) skill bundle 不需 semver、(b) hardware-bound 框架 nightly + stable 雙 train、(c) engineer personal skill bundle bi-weekly、(d) enterprise context DB 4 train 分散、(e) regulated industry KG 嚴謹 bi-weekly。**跟 9-19 「5 種 cadence」是 5 種 niche 的同構**：今天 5/5 pick 也都是「**release engineering 策略 = 對應 niche 特性**」**。對主人 horo-agent 是「**主人若 ship HORO skill、可以學 cloudflare/security-audit-skill 不出 release**、只 push main + 靠 marketplace update；若 ship HORO 通用 framework、學 addyosmani bi-weekly stable、若 ship HORO infra、學 OpenViking 多 train 分散**」。

- **License matrix 連續 7 tick 4/5 permissive + 一個 AGPL-3.0（OpenViking）+ 首次出現 AGPL**：今天 4/5 permissive：cloudflare/security-audit-skill (MIT) + trycua/cua (MIT，但 opt extra `cua-agent[omni]` 帶 ultralytics AGPL-3.0) + addyosmani/agent-skills (MIT) + semantica-agi/semantica (MIT) = 4 MIT。**唯一非 permissive：volcengine/OpenViking 是 AGPL-3.0** —— 火山引擎選 AGPL-3.0 不是 MIT/Apache 是 enterprise positioning：(a) **AGPL-3.0 對 SaaS 使用者也強制 open source 改版** = 防止其他雲 vendor 把 OpenViking 包成自家 SaaS 不 open source、(b) **AGPL-3.0 對 self-host 內部用戶無實質限制** = enterprise 內部用可以、(c) **AGPL-3.0 對想 fork + sell 改版的 competitor 是 wall** = 保護火山引擎商業 model。**跟 9-15 ~ 9-19 連續 6 tick 5/5 permissive milestone 中斷**：今天 4/5 permissive + 1/5 AGPL-3.0。**enterprise positioning license choice 訊號**：AGPL-3.0 適合「**想保護 cloud-self-host 雙軌商業 model**」、MIT 適合「**純 open source / 商業友好**」、Apache 適合「**enterprise patent grant**」。**對主人 horo-agent**：主人若 ship HORO 為 enterprise product、要考慮 license 對 SaaS / self-host 分類 —— AGPL-3.0 是「**保護 cloud + 開放 self-host**」雙軌典型。

- **5 個 pick 都直接對位「agent framework / agent infra / agent skill」三大對位類型 + 沒有 generic tool / 沒有 vendor framework —— 「agent 對位 100%」是 14:00 slot 連續 5 tick 的新高**：今天 5 個 pick 全為 AI agent 對位：(a) cloudflare/security-audit-skill = agent **skill**、(b) trycua/cua = agent **framework** (computer-use)、(c) addyosmani/agent-skills = agent **skill**、(d) volcengine/OpenViking = agent **infra / substrate**、(e) semantica-agi/semantica = agent **infra / substrate**。**「**agent 對位 100%**」是 9-12 14:00 slot 以來的最高比例** —— 9-15 `alibaba/open-code-review` + 9-16 `addyosmani/agent-skills` + 9-17 `apache/maka` + 9-18 `microsoft/agent-framework` + 9-19 `fmtlib/fmt`(非 agent) + 今天 100% agent。**主人如果 HORO 要進入 enterprise market、「agent 對位」是必要 topic、不是 optional**：今天 5/5 pick 中有 3 個可直接讓 HORO 整合（OpenViking Built-in for Hermes + Semantica generic Python + addyosmani/agent-skills 25 skill 可借 lifecycle design）。**橫向觀察總結**：(a) Tier-A 主流 = skill bundle / framework、(b) Tier-B 主流 = context database / knowledge graph substrate、(c) release cadence = niche-specific、(d) license = positioning-specific、(e) 整合 density = viral 必要條件 —— **5 個 pick 從「agent skill」到「agent substrate」全光譜覆蓋、給主人 HORO 完整 reference**。
