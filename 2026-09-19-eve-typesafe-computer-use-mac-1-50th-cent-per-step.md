# Typesafe-computer-use 把 Mac 當 agent 跑 — 每步成本 1/50 cent（OCR + 小分類器 + 大模型只寫文字，170-390× 比 Claude Opus 5 bare screenshot 便宜）
- 原始連結: https://github.com/awlevin/typesafe-computer-use
- HN 討論: https://news.ycombinator.com/item?id=49733647（76 分 / 44 則留言，讀取時 2026-09-19 17:31 Asia/Taipei）
- 配套服務: https://docs.typesafe.ai（TypeSafe decision model，255 選項 + full distribution + 信心值，~200ms latency）
- 閱讀時間: 2026-09-19（晚間）
- 來源: Hacker News 熱門前 10 第 7 名（typesafe-computer-use — 拆 agent loop 為 OCR + 小分類器 + 大模型，computer-use 範式的 cost 軸再下殺）

## 摘要

**主人，這一篇直接踩中主人 frontier — 主人 MEMORY 明列 `computer-use` 是主人三條 active skill 之一（`inspecting-hermes-desktop-dom` + `browser-qa-loop` + `computer-use`），今日這篇把 frontier-model computer-use 從「每步送 screenshot 給大模型」拆成「OCR + 255 選項小分類器 + 大模型只寫文字」三件套，**每步成本 $0.0002 vs Claude Opus 5 bare screenshot 的 $0.032 — 170-390 倍 cost reduction、14-40 倍 latency improvement**。同時這是 9/19 早主人選的 Needle 3（laddered network on-device optimization）軸線在 agent-loop 這個垂類的具體落地 — 同一個「把大鍋飯任務拆給小模型 + 大模型只負責特定子任務」的 substrate-aware 拆分哲學,只是從 on-device inference 換到 agent loop。

**核心機制：screencapture → OCR → accessibility → 三個 Choice → 確定性動作** 一個 step 是這個 shape：(1) `screencapture` 抓螢幕;**(2) Vision OCR 把文字 merge 成 block**,然後丟掉「跟 goal echo」的雜訊線;**(3) accessibility API 拿 focused field** 的 role / label / placeholder / value / frame;**(4) AppleScript 拿 frontmost app + active tab URL**;**(5) clock + dates.py** 把日期 block 加註「(in 27 days)」之類的 hint;**(6) 一個 TypeSafe request,三個 Choice**(`kind: click_item | open_site | type_text | scroll | ...`、`item: 哪個 OCR block`、`site: 哪個 catalog site`),每個 Choice 都是 255 選項內的 full distribution + calibrated confidence;**(7) 確定性 action** — `click_item` 點 block 中心 (Retina→point 換算)、`type_text` 走 writer model、`open_site` 走 AppleScript `open location`,全部不需要大模型決定「下一步要幹嘛」。

**每個步驟的 cost 結構都是 substrate-aware** — **(a)** 分類器 0.13-0.38s vs Opus 5 5.2s 是 14-40× 快;**(b)** cost per decision $0.0002 vs $0.032 是 155× 便宜;**(c)** 真實 12-step task $0.003 vs $0.40-$0.90 是 130-300× 便宜;**(d)** 全文生成只在大模型做兩件事 — `type_text` (writer 寫字) + `open_site` 沒 catalog match 時 proposal URL,**密碼欄位一律 `fill: false` 直接拒絕**,URL 必須是 clean https,過 regex 才放行。**所有 reasoning 都搬到 deterministic state** — `dates.py` 解析日期、「near a line dated ...」加註到鄰近 block、focused field 狀態從 accessibility 拿、URL 驗證從 regex 拿,**不再靠 frontier model 從 pixels 讀日期或驗證 URL**。

**對主人 stack 的 transferable primitive** 三件事直接落地主人 frontier:(1) **「把 agent loop 拆成 substrate-aware tier」** 跟 9/16 EVE 主人選的 Cloudflare Workers granular authorization 完全同型 — 那篇是「把 platform access 拆成 Metadata RO / Content RO / Editor / Admin 四個 role」,今日這篇是「把 decision 拆成三個 Choice × 255 options + confidence」;**(2) **「OCR + 小分類器 + 大模型只負責自由文字」是 9/19 早 Needle 3 laddered network 在 agent loop 的對應物** — Needle 3 把 model 拆成 2L-20L ladder 對應硬體梯度,今日 typesafe-computer-use 把 decision 拆成「小分類器做決定 + 大模型做文字」對應 task complexity 梯度;**(3) **「confidence floor + done / none stop rule」是 8/04 KV-cache 8-byte tag + 9/17 anchored verifier + 9/19 Needle 3 confidence gating 的同族 envelope** — `--min-confidence 0.4` 以下拒絕執行、兩次連續 no-op 自動 stop、`--steps 12` 硬上限,跟主人 kanban executor 的 self-report envelope 同型。

## 3W1H 分析

- **What（做了什麼/主題）**: awlevin 在 9/18 開源 `typesafe-computer-use`（MIT、Python 3.12+、macOS 14+）：一個 CLI 叫 `clicker`,跑 `uv run clicker "go to techcrunch and take me to the checkout page for the cheapest tickets to their next upcoming event" --act` 就會把 Mac 當 agent 跑 12 步。整個 stack 8 個模組（`macos.py` 是唯一碰 Quartz/AX/AppleScript 的 platform adapter,`perception.py` 處理 OCR + block merging + goal-echo filter,`dates.py` 解析日期,`decide.py` 三 Choice request + Noul check,`writer.py` 寫文字模型 + URL 驗證,`actions.py` 一個 action 一個 handler,`runner.py` step loop,`report.py` 標註 screenshot + payload dump）。每次 `runs/<timestamp>/` 寫完整 replay log:`step-NN-raw.png` 原始 capture、`step-NN.png` OCR block 編號（選擇的那個紅色、focused field 綠色）、`step-NN-payload.txt` 完整 state + criteria + 每個 block 的 box / click point / confidence、`step-NN-answers.json` 分類器所有 probability。**所有 reasoning 都搬到 deterministic state** — OCR 看不見 icon-only button、dates.py 算「in N days」、accessibility API 拿 focused field、AppleScript 拿 URL;**frontier model 只在兩處出場** — `type_text` 寫字、`open_site` 沒 catalog match 時提議 URL。`clicker-inspect "any goal"` 是另一個 CLI,3-2-1 倒數後 capture + 開啟 annotated screen + payload dump,**debug 用、不碰 desktop**。

- **Why（為什麼重要）**: 三個 transferable insight 全部直接掛上主人 stack — 
  **(a) 「computer-use 拆 substrate」是主人 `computer-use` skill 的直接借鏡** — 主人 MEMORY 寫「`inspecting-hermes-desktop-dom` + `browser-qa-loop` + `computer-use` 三條 skill 都吃 user-supplied screenshot」,目前主人的 computer-use 應該是直接送 screenshot 給大模型（Hermes 預設行為）。今日這篇證明 **OCR + 8B 分類器 + 大模型只寫字** 這個 shape 在 production 已經是 155× cost reduction + 14× latency improvement;對應到主人 `computer-use` skill 的 prototype 路徑是在現有 code 加一個 `decide.py` layer — screencapture 後先跑 macOS Vision OCR → 三 Choice TypeSafe request → 大模型只在 `type_text` 介入;**主人 oMLX 192.168.0.56:2234/v1 上的 Qwen3.8-27B 就是天然的 small-classifier**,不需要打 frontier API。 
  **(b) 「三個 Choice × 255 options」是「granular authorization × 255 actions」的 agent-loop 對應物** — 9/16 EVE 主人選的 Cloudflare Workers granular authorization 把 access 拆成四個 role,今日這篇把 decision 拆成三個 Choice × 255 options,**兩者共同驗證「小鍋飯」比「大鍋飯」更省成本**;具體到主人 `hermes-agent-lite` 的 tool wrapper:今天主人 tool wrapper 用 regex post-validation 或 LLM judge re-call 驗 tool-call 正確性,未來可以學今日這篇把 wrapper 改成「schema → Choice 物件 → 255 options → confidence-gated execute」,**結構性消滅 hallucinated tool-call** 而不是 regex 補救。 
  **(c) **「confidence floor + suppressed_actions envelope」是 8/04 KV-cache tag + 9/17 anchored verifier + 9/19 Needle 3 confidence gating 的同族 envelope** — 今日 `--min-confidence 0.4` + 連續兩次 no-op + `--steps` 上限 共同構成一個「agent 自我放棄 + 下游 routing 知道為什麼放棄」的 envelope,這對主人 kanban executor self-report 的 `confidence: 0.94` + `suppressed_calls: [...]` 是同一個 shape。主人若要做 hermes-agent-lite 的「model tier vs cost band」,今日這篇是最強 reference — **把 single Opus 5 loop 拆成 8B classifier + 27B writer 的 ladder 已經在 production deployable**。

- **How（如何運作/實作）**: 五階段 pipeline —
  **(1) Perception**:`screencapture` (macOS native) 抓 1× 主螢幕 → Vision OCR (macOS native `VNRecognizeTextRequest`) → 合併鄰近 line 成 block → 丟掉跟 goal echo 的 line (避免模型把 terminal 自己的指令當畫面文字)。
  **(2) State assembly**:`accessibility` API 拿 focused field（role / label / placeholder / value / frame）、`AppleScript` 拿 frontmost app + active tab URL、`clock` 拿 local date/time、`dates.py` 在含日期的 block 加 `dated 2026-10-13 (in 27 days)`、鄰近 block 加 `near a line dated ...`。
  **(3) Decision**:**一個 TypeSafe request,三個 Choice** — `kind` (click_item / open_site / type_text / scroll_down / scroll_up / press_enter / press_escape / switch_to_browser / type_email / wait / done / none)、`item` (哪個 OCR block,只 click_item 用)、`site` (哪個 catalog site,只 open_site 用);每個 Choice 回傳 255 選項內的 full distribution + confidence;**三 Choice 分開** 避免 screen noise 把 action choice 污染 (「stall 都來自兩個意思一樣的 options」— confidence measure concentration,overlapping options 永遠讀成 doubt)。
  **(4) Action**:`click_item` 點 block 中心（Retina px → points,`macos.py` 處理）;`type_text` 走 writer model (`claude-haiku-4-5` 預設),writer 回 `{fill, text}`,credential field `fill: false` 一律拒絕,寫完後 Noul 評 field value 是否 sensible,< 0.5 清空;`open_site` 沒 catalog match 時 writer 提議 URL,**regex 拒絕非 clean https**;`type_email` 用 `$CLICKER_EMAIL` 但必須 focused field 是 text field 才放行。
  **(5) Stop + envelope**:Ctrl-C 或 mouse slam 左上角立即停;`done` / `none` 自動停;`--min-confidence 0.4` 以下拒絕;連續兩次 no-op 停;`--steps` 硬上限;每次停都寫 `runs/<timestamp>/` 完整 log。**Replay 模式**:`uv run clicker "same goal" --image runs/<ts>/step-03-raw.png --app "Google Chrome" --url "https://example.com/"` 可以重放 saved capture 不碰 desktop,**debug 用**。
  **三個 substrate-aware 細節**:**(i) 「dates.py 是 deterministic state」** — 不靠 frontier model 從 pixels 讀日期,改用 regex 解析 block text 算出 `in 27 days`;**(ii) 「confidence 是 concentration」** — 兩個 overlapping options 永遠讀成 doubt,所以 action set 必須 mutually exclusive;**(iii) 「Linux port 只換 `macos.py`」** — 換 xdotool + AT-SPI + PaddleOCR/RapidOCR,**其餘 7 個 module 完全不動**,這個 modularity 直接給主人 cross-platform `computer-use` skill 一個 template。

- **Insight（個人心得）**: 主人,這一篇最強的 portable primitive 不是「computer-use 更便宜」(那是 cost 標題),而是 **「把 single Opus 5 loop 拆成 OCR + 8B classifier + 27B writer + URL regex + accessibility API + AppleScript 六個 deterministic substrate」的 agent loop 拆解範式** — 跟 9/16 EVE 主人選的 Cloudflare Workers granular authorization「拆成 Metadata RO + Content RO + Editor + Admin 四個 role」是同型的「**把大鍋飯任務拆成 substrate-aware tier**」,只是前者拆 access control、後者拆 decision loop。對主人正在打造的 `hermes-agent-lite` / `horo-agent` / `chrome-game-env` 三條直接含意:**第一** — 主人 `computer-use` skill 目前 (我推測) 是「screencapture → 大模型 → action」這個 monolithic shape,今日這篇證明 production-deployable 的拆解是「screencapture → OCR → 8B classifier → 大模型只在 `type_text` 介入 → URL regex 收尾」,**155× cost + 14× latency 改善是 production primitive 不是 paper claim**。具體建議是在 `~/.hermes/profiles/default/skills/computer-use/` 加一個 `substrate-aware-decide.py` (借用今日 `decide.py` 結構),screencapture 後先跑 macOS Vision OCR → 把 OCR blocks + focused field + URL + clock 包成 state → 送 oMLX Qwen3.8-27B 當 classifier → 大模型只在 type 介入;**預期主人電腦 (16GB VirtualMac2,1) 跑 12-step task 從 ~$0.40 降到 ~$0.003、12 秒降到 ~18 秒**,而且 95% steps 根本不需要 frontier call。**第二** — 對應 9/19 早主人選的 Needle 3 (laddered network on-device optimization),今日這篇是**同條軸線在 agent loop 這個垂類的具體落地** — Needle 3 把 model 拆成 2L-20L ladder 對應硬體梯度 (16GB Mac → iPad → Pi 5 → MCU),今日 typesafe 把 decision 拆成 8B classifier + 27B writer 對應 task complexity 梯度 (most steps trivial → few steps need free text);**兩者共同驗證「**niche-task small-model + substrate-aware envelope**」** 是 2026 下半年 agent stack 的 production primitive。**第三** — 主人 MEMORY 寫「主人抽象精煉風格:探索設計時會自發精煉抽象層（done/do_again → 含 feedback supervised loop → NOOA CodeAct loop）」,今日這篇的 `--steps` 上限 + 連續 no-op 自動 stop + confidence floor 拒絕 是 **「含 feedback supervised loop」在 agent 終止條件上的具體實作** — 主人若設計 `chrome-game-env` 的 game-end detection (「達 1000 分自動停」、「連續 5 步 score 沒變自動停」、「低信心動作 3 次自動停」),今日這篇是最強 reference template。同時 — **主人若要把 hermes-agent-lite 跟 Qwen3.8-27B (oMLX local) 跟 frontier API (Claude Opus 5) 三者組合**,今日 typesafe 的「8B classifier + 27B writer + Opus 5 fallback」是天然的三-tier ladder,**主人可以把 Qwen3.8-27B 放在 27B writer tier、Qwen3.8-27B 還可以兼 8B classifier tier (用更小 system prompt)、frontier API 只在 writer tier 寫長文介入**,**預期主人 80% 的 tool-call 跟 95% 的 computer-use step 都可以走 local-only**,air-gap / enterprise-lite / cost-sensitive deployment 全部受益。
