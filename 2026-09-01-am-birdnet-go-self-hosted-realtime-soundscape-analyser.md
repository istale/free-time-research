# BirdNET-Go: Self-Hosted Realtime Soundscape Analyser
- 原始連結: https://github.com/tphakala/birdnet-go
- 作者實戰文: https://jasontucker.blog/how-i-turned-my-security-cameras-into-an-automatic-bird-identification-system-with-birdnet-go/
- HN 討論: https://news.ycombinator.com/item?id=49511856
- 閱讀時間: 2026-09-01（早間，週二）
- 來源: Hacker News 熱門前 10 第 2 名（317 分 / 88 則留言，讀取時間 07:01 Asia/Taipei）

## 摘要

**BirdNET-Go 是一個把「RTSP 串流 / 音效卡輸入」持續送進多模型本地推理、把每次偵測 route 到 Discord / Slack / MQTT / Home Assistant / Webhook 的「24/7 soundscape 處理器」**——作者把三支原本用於保全的 IP camera 改成鳥類偵測器，靠 RTSP 直接取 mic 串流、配 BirdNET v2.4 + Google Perch v2 + BattyBirdNET + BirdNET Geomodel v3.0 的 multi-model gallery 在 Raspberry Pi 上跑。對主人而言，這篇不是「賞鳥」而是 *「local-first AI pipeline + 跨 webhook 多通道 routing + Homelab Docker」的範本* — 8/27 / 8/28 那條 agent / Cloudflare / Workers 軸的「AI 在自己機器上跑、不依賴外部 API」substrate-identity 又一次被 paper / 部落格驗證，而且這次是 *連續 streaming + multi-channel alert routing* 的具體落地。

**第二個關鍵訊號是「multi-model gallery 跟 parallel inference 是 owner-actionable 的」** — Repo 明確列出 BirdNET v2.4（6,500+ 物種，預設內嵌）、Google Perch v2（14,795 物種，ONNX runtime）、BattyBirdNET（11 區域蝙蝠模型，Linux + 超音波裝置）、BirdNET Geomodel v3.0（location-based range filter，12,012 物種）、自訂 TFLite 模型五個 *runtime-overridable 的 model slot*，並提供 *cross-model detection consensus*（多模型同意時強化信心、不一致時 flag 給人工 review）。對主人而言，這是 8/30 vLLM v0.28 thinking-token-budget 跟 8/22 Qwen3-TTS serving 那條 *「shared scheduling surface across heterogeneous compute modules」* 的新 instance：*「一個 inference surface 上跑 N 個 model，cross-model consensus 是 output 而不是 raw logits」*——主人若要在 hermes-agent-lite 上跑 Qwen3.8-27B + Mistral Shieldstral（8/05）的 safety classifier + 某個 small classifier，*同一個 dispatcher 框架就能複用*。

**第三是「local-only by default + opt-in telemetry + privacy filters」這條 substrate boundary** — Repo README 明確寫 *「Local-only by default. Optional Sentry telemetry is strictly opt-in」*，另有「privacy and dog-bark filters」、speech-cutout（mic 偵測到人聲自動切掉）、Deep Detection（15 秒內同物種重複確認才 confirm）、per-species dynamic threshold 等四到五層「false-positive 防線」逐一列在 README。對主人而言，這是 8/19 desktop-fly 那條「honesty boundary」（modeled vs. measured）的 *homelab-scale instance*：主人現有的 launchd 守護服務（8260 memory-editor / 8100 lmstudio-council / 8765 tailscale-curation-server / 8787 hermes-webui / 8000 ASMR Foley Lab）全部都是「local-only by default」風格，但 *沒有任何一個有 README 寫明「哪些 telemetry 是 opt-in、哪些 false-positive 是被動消除」*——可借 BirdNET-Go 的 honesty-block 結構當 template。

## 3W1H 分析

- **What（做了什麼/主題）**:
  BirdNET-Go（GitHub `tphakala/birdnet-go`，CC-BY-NC-SA-4.0，⭐ ~1.2k，Go 1.2 萬行 + Svelte 5 + TypeScript SPA）— 一個 self-hosted realtime soundscape analyser，核心抽象是「multi-source audio input（soundcard + RTSP / RTSPS streams，256 kHz 支援超音波）→ multi-model inference gallery（BirdNET v2.4 / Perch v2 / BattyBirdNET / Geomodel v3.0 + 自訂 TFLite）→ cross-model consensus + alert routing（Discord / Slack / Telegram / ntfy / Pushover / Gotify / Matrix / MQTT + Home Assistant discovery / Webhook / BirdWeather / Prometheus / Browser Push）→ SQLite / MySQL + PWA dashboard」。可裝在 Raspberry Pi 4 跑 24/7，Docker image 給 `linux/amd64` + `linux/arm64`，單一 static binary 把 BirdNET TFLite model 內嵌。

- **Why（為什麼重要）**:
  1. **Owner-actionability triple-hit**：HN #2（317/88）勝過 #1 UBO MV3 退役（253/198）跟 #3 Playa Phone（452/172），因為它同時打到主人的三條現有 primitive：(a) *homelab self-host 模式*（主人 8000 / 8260 / 8765 等 launchd 守護全是這個 shape）、(b) *local AI inference pipeline*（主人有 `qwen38-code` oMLX 192.168.0.56:2234/v1，Qwen3.8-27B，max_turns=20 在跑）、(c) *Svelte 5 + TypeScript SPA*（主人 tailscale-curation-server 的 threejs-style-ref 同 stack）。Playa Phone 是 swag 類、社群都在聊 Burning Man 經驗、不是技術機制；UBO MV3 是 vendor-policy 公告、無 transferable primitive。三條 primitive 命中是 8/19 desktop-fly 那條「triple-resident primitive hit beats higher HN score」rule 的新 instance。
  2. **8/30 vLLM v0.28 → 8/22 Qwen3-TTS → 今日 BirdNET-Go = inference-optimization axis 第 7 pick**，SKILL.md 8/22 saturation rule 寫明「6th pick 之後必須 extend 或 refute substrate-identity」——今日把 substrate-identity 從 *「LLM serving + TTS serving」* 推進到 *「multi-source streaming + multi-model parallel + cross-model consensus」*，是同一個 *shared scheduling surface* family 在 audio + vision 之外的延伸，仍 owner-actionable。
  3. **跨 webhook 多通道 routing 是 hermes-agent 直接可借的模式**：Repo 列出 Discord / Slack / Telegram / ntfy / Pushover / Gotify / Matrix / Bark / IFTTT 共 9 條 push channel，加 MQTT with Home Assistant discovery 加 webhook with custom template 加 shell-script hook——主人現有 hermes-agent cron / Discord 回報是 ad-hoc 的，這個 *alert rules engine + per-rule conditions + schedules + multi-target delivery* 是可以直接借的設計（特別是「per-rule conditions」這個 abstraction）。

- **How（如何運作/實作）**:
  BirdNET-Go 的核心算法分三層：
  - **Audio input layer**：soundcard capture + RTSP / RTSPS streams 平行 ingest，sample rate auto-probe 到 256 kHz（超音波蝙蝠偵測需求），audio liveness watchdog 在串流掛掉時 tiered recovery，per-source quiet hours / daylight filter / extended capture mode 控制噪音。
  - **Inference layer**：每個 audio source 配一個 model（BirdNET v2.4 預設內嵌、Perch v2 走 ONNX runtime、BattyBirdNET 11 區域模型走 Linux + 超音波 mic、Geomodel v3.0 做 location-based range filter），多模型跑同一 source 時 *cross-model consensus* 強化信心；false-positive 防線有四層：(a) Deep Detection（15 秒內同物種重複確認才 confirm）、(b) per-species dynamic threshold、(c) location-based range filter、(d) privacy / dog-bark filter、(e) speech-cutout（mic 偵測到人聲自動切掉）。Live spectrogram streaming + detection heatmap 用 ONNX-accelerated rendering 在瀏覽器直接畫。
  - **Alert + storage layer**：alert rules engine 配 per-rule conditions / schedules / delivery targets，走 `shoutrrr` library 一次送 9 條 push channel，加 MQTT with Home Assistant auto-discovery 加 Prometheus metrics endpoint；存 SQLite（預設）或 MySQL（含 retry-aware write paths for contention），automatic backups with real-time status polling，format-aware audio clip export。

  Repo 用 Go + Svelte 5 + TypeScript 寫成單 binary + PWA，hot-reload 設定（不用重啟就能換 model / 換 per-source model assignment），system health page 涵蓋 audio pipeline / models / network / datastore 四個子系統、database doctor 自動診斷 schema、help & support page 一鍵 support dump。

- **Insight（赫蘿心得）**:
  今天這篇對主人的 actionable 不是「裝一套 BirdNET-Go」而是 *借它的三個 abstraction 到 hermes-agent-lite*。最便宜的落地是 **Layer 0 SOUL.md / README honesty-block convention**：在主人 5 個 launchd 守護服務的每個 README 開頭加一段 `## Privacy / Telemetry Boundary`（< 10 分鐘 commit、零 code、零 LLM call），照 BirdNET-Go README 的結構寫「local-only by default」、「optional telemetry 嚴格 opt-in」、「false-positive / silent-failure 防線列舉（speech-cutout 對應主人的 session log redacted PII strip、deep-detection 對應主人的 idempotent retry、range-filter 對應主人的 allow-list）」——同一個 *honesty-boundary* 模板從 8/19 desktop-fly 的 *modeled vs. measured* 進化成 *runtime vs. telemetry vs. privacy-filter* 的三段式，最 family-friendly。Layer 1 prototype：寫一個 `infra_alert_rules.py`（< 50 行 Python）— 把主人現有的 hermes-agent Discord 回報 pattern 從「每次 cron job 直接呼叫 webhook」改成「rules engine with per-rule conditions + multi-target delivery」，參數照 BirdNET-Go 的 `shoutrrr` library 介面（condition = trigger event type，target = channel list，schedule = cron expression），cost **< 4 hr prototype、無 GPU 需求、可在主人 16GB Mac 直接跑**；主人多個 launchd 服務都能 plug in。Layer 2 是 multi-model inference gallery（Qwen3.8-27B + Mistral Shieldstral 8/05 safety classifier + 某個 small classifier）走同一 dispatcher 框架——*cross-model consensus* abstraction 是 8/30 vLLM v0.28 *「shared scheduling surface」* family 的下一站，可 paper-anchor（Hermes-side cost ceiling 估 *< 5% overhead*，比 8/04 KV cache integrity 0.1% 多一點點，因 dispatch 是 Python dict lookup 不是 GPU attention）。主人若今天吃飽前想 commit 一個 minimal primitive，**Layer 0 那個 `## Privacy / Telemetry Boundary` block 加進 5 個 launchd 服務的 README 是 < 10 分鐘 commit、跟 8/19 honesty-section primitive 同成本、同 family**，比 8/30 / 8/22 那種 routing-layer 改動輕很多。
