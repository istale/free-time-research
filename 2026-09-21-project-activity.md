---
title: GitHub 自由探索 2026-09-21（14:00 台北時間）
date: 2026-09-21
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 7 天 repeat）
  - web_search 當日 AI 開源新聞（Tier-B web 探索 2）
  - GitHub REST API `/repos/<slug>` + `/releases` + raw `README.md` + npm registry
---

# GitHub 專案動態

- 檢查時間：2026-09-21（14:00 台北時間，週一）
- 檢查對象：`affaan-m/ECC` / `BuilderIO/agent-native` / `vercel-labs/json-render` / `markfulton/ai-employees` / `yylo-dev/yylo`
- 來源組合：GitHub Trending today 解析後 Tier-A top 3（**`affaan-m/ECC` 826 ★ today #1**、**`BuilderIO/agent-native` 98 ★ today #2**、**`cloudflare/security-audit-skill` 2428 ★ today #3** —— 但 #3 是 9-20 報告 pick、跳過、#4 `trycua/cua` 1018 也是 9-20 pick、跳過，所以實際 Trending top 3 = **`affaan-m/ECC` + `BuilderIO/agent-native` + Trending #12 `vercel-labs/json-render` 291 ★ today**（#5-11 都跳過：`anthropics/financial-services` description 為空、`paperless-ngx` 文件管理、`anthropics/claude-code` 已是 viral、`mihail911/modern-software-dev-assignments` Stanford 課程、`higgsfield-ai/higgsfield` 是 9-20 報告 `#9` 的 pick、`Open-Dev-Society/OpenStock` 開源股票工具、`coder/coder` 是 9-18 報告 `#3` 的 pick、`addyosmani/agent-skills` 是 9-16 + 9-20 報告的 pick），前 7 天 Tier-A 內無重複 owner 為 `affaan-m` / `BuilderIO` / `vercel-labs`）。Tier-B web_search 命中兩個 strong leads：**`markfulton/ai-employees`**（8 scheduled AI Employees + 60 routines + 11 agent harnesses **含 Hermes Agent** + 9-19 release + MIT、JavaScript、239★、topics 13 個含 `hermes-agent` / `claude-code` / `openclaw` / `opencode` / `antigravity`、`npx ai-employees hire <role>` install 模式、`docs/HARNESSES.md` 列 Hermes 為 Ready + built-in cron with delivery to any platform）+ **`yylo-dev/yylo`**（YYLO AI coding-agent orchestration CLI、Python、MIT、60★、20 topics、`npm install -g '@yylo/cli@latest'`、`yy init --task ... --subagent pi` worktree-based parallel agents、typed task + validation + merge + release-readiness boundaries、Pi + Codex subagents）。

---

## Repo 摘要與 3W1H

### `affaan-m/ECC`

- **Repo 摘要：** 「**The agent harness performance optimization system. Skills, instincts, memory, security, and research-first development for Claude Code, Codex, Opencode, Cursor and beyond.**」——affaan-m（個人 maintainer、有 GitHub Sponsors + Pro subscribers $19/seat/mo + CodeRabbit / Greptile / Kimi / Itô Markets / SerpApi 等 sponsor）開源 8 個月的「**multi-harness agent operating system**」、給 Claude Code / Codex / OpenCode / Cursor / Gemini / Zed / Copilot / Antigravity / Qwen / **Hermes** / OpenClaw 等 11 個 harness 提供 68 個 specialist agents + 292 個 skills + 94 commands + AgentShield security scanner + memory + hooks + continuous learning。263,988★、JavaScript、MIT、topics 8 個（`ai-agents / anthropic / claude / claude-code / developer-tools / llm / mcp / productivity`）、`created` 2026-01-18（**8 個月 = mature personal project**）、`pushed_at` 2026-09-21 04:55 UTC（**今天仍在 push**）、`updated_at` 2026-09-21 06:00 UTC、`open_issues_count` 212、size 52 KB、homepage `ecc.tools`。**Latest GH release `v2.2.1` published 2026-09-08 17:52 UTC（13 天前）**、**npm `ecc-universal@2.2.1` 同步 published 2026-09-08 17:51:59 UTC（差 1 秒）** —— GH release 跟 npm latest 完全同步（**registry-lag check 反例**）、v2.2 系列已 ship 2.2.0/2.2.1/2.2.2 三個 patch。**3 個獨立 identifier**：`affaan-m/ECC`（GH source） + `ecc@ecc`（Claude marketplace/plugin identifier） + `ecc-universal`（npm package）—— 三者刻意不同：marketplace 要短 slug 給 Desktop/API validators、npm 留 universal naming。**核心 pipeline 文字**：`plan → test → implement → review → verify → remember → improve`（ECC 把整個 engineering workflow「持久化到 context window 之外」）、AgentShield 掃描「prompt / hooks / MCP config / permissions / secrets / agent files」。**對主人的對位：跟 9-15 `alibaba/open-code-review` + 9-16 `addyosmani/agent-skills` + 9-17 `cloudflare/security-audit-skill` + 9-18 `Tencent/BrowserSkill` + 9-19 `mattpocock/skills` + 9-20 `trycua/cua` + `addyosmani/agent-skills` 同類「agent skill bundle / framework」、但這個獨佔「**multi-harness 跨 11 個 runtime + 68 agents + 292 skills + AgentShield security scanning**」 niche + 「**personal maintainer + Pro subscription 商業 model**」 模式**。跟 `addyosmani/agent-skills`（9-16/9-20）對位是「**multi-harness reach**」vs「**12 platform setup doc**」、跟 `mattpocock/skills`（9-19）對位是「**全 stack（agents + skills + commands + memory + security）**」vs「**opinionated minimal（7 skills）**」、跟 `Tencent/BrowserSkill`（9-18）對位是「**multi-harness vendor-neutral**」vs「**vendor browser-control**」。對主人 horo-agent 對位是「**主人若要 ship 一個 agent operating system reference、ECC 是最大 + 最成熟的 single-maintainer 開源對位**」+ **Hermes Agent 是 ECC 11 個 advanced managed adapter 之一**（README 第 237 行明確列 `hermes` 為 advanced managed adapter），但 ECC 對 Hermes 是「still uses its documented `ecc install --target ...` paths until each adapter has passed the guided collision, update, repair, and uninstall lifecycle matrix」= ECC 對 Hermes 還沒跑完完整測試、**不是 first-class**。對主人 HORO-Agent 來說 ECC 的最大 reference value 是「**single-maintainer ship 11 harness + sponsor 商業 model 的成功 path**」+ 「**3 個 identifier（GH / plugin slug / npm）刻意不同是 marketplace install 的 UX 必要設計**」。
- **3W1H：**
  - **What：** 一個 monorepo（68 agents、292 skills、94 commands、AgentShield scanner）+ 11 個 harness adapter（Claude Code 是 native plugin + OpenClaw / Hermes / OpenCode / Antigravity 等是 advanced managed adapter）+ npm `ecc-universal` + GitHub Marketplace app `ecc-tools` + Discord community（1496644400590094540）。
  - **Why：** 現有 AI coding agent 使用者 5 大痛點：(1) **每次 prompt 都要重做 `plan → test → implement → review → verify → remember → improve`** 的 process、(2) **context window 爆掉**、記憶不持久、(3) **skills 是 ad-hoc markdown** 沒有結構化 hierarchy、(4) **security 沒有人掃**（惡意 prompt / 危險 MCP config / 暴露 secrets）、(5) **multi-harness 切換成本高**。**ECC 一個 install 解 5 痛點**：(a) 7-stage pipeline 寫進 skills 永久保留、(b) memory + hooks + continuous learning 把 session 內容存出 context window、(c) 68 agents + 292 skills 分 specialist 跟 general、(d) AgentShield 自動掃 prompt / hooks / MCP / permissions / secrets / agent files、(e) 一個 install 可推到 11 個 harness（但要單獨 install 不可 stack）。**近期受關注原因**：今天 GitHub Trending #1（826★ today）+ 9-08 連 ship 3 版 v2.2 + `ecc-tools` GitHub Marketplace app 上架（private repos from $19/seat/mo）+ Kimi Code 是新增的 guided setup target。
  - **Who：** **single maintainer coding agent user**（Claude Code 為主、其他 10 個 harness 為次）想 ship senior-engineer workflow 不每次重寫 prompt 的；**startup founder 兼 engineer** 想用同一個 agent operating system 跑 Claude + Codex + OpenCode 對比測試的；**enterprise security team** 想用 AgentShield 掃 agent 設定漏洞的（防止 malicious skill install）；**sponsor-supported open-source maintainer**（Pro + Sponsors 模式 reference 設計）。
  - **How：** **3 種 install path**：(1) **Universal guided setup（推薦，Claude Code / Codex / Kimi Code）**：`npx ecc-universal@2.2.2 setup`（npm/pnpm/yarn2+/bunx 都行）+ wizard 會自動偵測 marketplace + native install scope + 詢問 install / update / scope change。(2) **Multi-harness guided**：`npx ecc-universal@2.2.2 install --guided --harness claude --harness codex --harness kimi --claude-scope local --claude-hooks standard --profile core --yes`。(3) **Native Claude Code commands**：`/plugin marketplace add https://github.com/affaan-m/ECC` + `/plugin install ecc@ecc`。**驗證 dry-run**：`npx ecc-universal@2.2.2 install --guided --harness codex --dry-run` 或 `--target kimi --dry-run`。**Reset / Uninstall**：當多層 install 出問題、`node scripts/ecc.js doctor` + `repair` 通常能救回。**Trouble report**：`ecc feedback` 或 GitHub issue with template `install-problem.yml`。
- **安裝方式：**
  - **Universal guided setup（Claude Code / Codex / Kimi Code，最快路徑，官方推薦）：** **`npx ecc-universal@2.2.2 setup`** — wizard 自動偵測所有 native Claude install + 詢問 scope 跟 hook profile。npm 最新版是 `2.2.1`（Sep 8），README 推薦 `@2.2.2`（為什麼是 2.2.2 要看 repo issue tracker，但 README 把它當作「published ECC 2.2.2 release」）。**Pnpm**：`pnpm dlx ecc-universal@2.2.2 setup`。**Yarn 2+**：`yarn dlx ecc-universal@2.2.2 setup`。**Bun**：`bunx ecc-universal@2.2.2 setup`。
  - **Multi-harness install（推薦多 harness 用法）：** **`npx ecc-universal@2.2.2 install --guided --harness claude --harness codex --harness kimi --claude-scope local --claude-hooks standard --profile core --yes`** — preflight 每個 selection 然後一次性 confirm。
  - **Native Claude Code plugin（in-CLI）：** **`/plugin marketplace add https://github.com/affaan-m/ECC`** + **`/plugin install ecc@ecc`** — Claude Code 自己的 marketplace + plugin 安裝指令，裝完即可用 `/ecc:configure-ecc` 在 Claude Code session 內 reconfigure。
  - **Settings.json declarative（for CI/CD 或 declarative setup）：** 在 `~/.claude/settings.json` 加 `extraKnownMarketplaces.ecc` + `enabledPlugins.ecc@ecc: true` —— 等同兩個 `/plugin` 指令的 declarative 版本。
  - **Source clone（for manual rule 拷貝）：** **`git clone https://github.com/affaan-m/ECC.git`** + `mkdir -p ~/.claude/rules/ecc` + `cp -R rules/common ~/.claude/rules/ecc/` + `cp -R rules/typescript ~/.claude/rules/ecc/`（挑你要的 language pack）。
  - **Hermes Agent install（advanced managed adapter）：** **`ecc install --target hermes`**（依官方 HARNESSES.md 表）。**注意 Hermes 仍是 advanced managed adapter 階段**、未跑完完整的 guided collision + update + repair + uninstall matrix、**不是 first-class**，文件上寫「Those targets still use their documented `ecc install --target ...` paths until each adapter has passed the guided collision, update, repair, and uninstall lifecycle matrix」。
  - **pip / uv / poetry：** **不適用**（這是 npm TypeScript/JavaScript 安裝）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可跑 `npx ecc-universal@2.2.2 setup`（需 Node.js 18+）。HORO-Agent 對位需自寫 SKILL.md 68 agents 的 schema parser 或直接 import 整套 skill bundle 進 agent config。Hermes Agent 安裝走 `ecc install --target hermes` 但屬 advanced adapter、**HORO-Agent 不建議在生產用**、僅適合 study reference。
- **近期 release：** **`v2.2.1` published 2026-09-08 17:52 UTC（13 天前）** = latest stable GH release。**npm `ecc-universal@2.2.1` 同步 published 2026-09-08 17:51:59 UTC（差 1 秒）** —— GH release 跟 npm latest 完全同步（registry-lag check PASS、不是 lag 反例）。v2.2 系列已 ship 2.2.0 + 2.2.1 + 2.2.2 patch 三版（**2.2.2 在 README 出現但 npm latest 仍 2.2.1、可能是 README 提前 promote 或 2.2.2 即將 publish**）。**Main 仍 active push**（`pushed_at` 2026-09-21 04:55 UTC 今天仍 push）、`pushed_at` vs `published_at` 落差 13 天 = 仍在 main preview、穩定版已發。**Release cadence 不固定** —— 6 週內 4 個 major（v2.0 / v2.1 / v2.2 / v2.2.x patch）、**single-maintainer weekly cadence**。對位 9-19 `fmtlib/fmt`「半年 stable + main 持續推」是「大 library 慢 cadence」、ECC 是「personal project weekly cadence」、對位 9-19 `zhayujie/CowAgent`「2-週 stable」是 enterprise-grade 雙週節奏、ECC 比那更快。**跟 9-17 `cloudflare/security-audit-skill`「無 release」對位** = ECC 出 release、security-audit-skill 不出 release，因為 security-audit-skill 是 skill bundle 不需 semver 化、ECC 是 harness operating system + npm package 需要 semver 化。

### `BuilderIO/agent-native`

- **Repo 摘要：** 「**A framework for building agentic apps**」——Builder.io（visual CMS + headless content 開源老牌 SaaS，自家也做 MCP server `builder.io/mcp`）開源 6 個月的「**TypeScript framework for building agents that pair autonomous work with a purpose-built UI**」、**shared action pattern 為核心抽象**：一個 `defineAction()` 寫一次、agent 當 tool 用 + UI 從 code call + HTTP + MCP + A2A + CLI 六個 surface 共用同一個 function + validation + permissions。5,395★、TypeScript、**license 為 None（API 顯示無 license 字段）**，但 npm 顯示 `@agent-native/core@0.182.1` = MIT，topics 5 個（`agent-native / agents / ai / react / typescript`）、`created` 2026-03-12（**6 個月**）、`pushed_at` 2026-09-21 06:00 UTC（**今天仍在 push**）、`updated_at` 2026-09-21 06:00 UTC、`open_issues_count` 79、size **352 MB（大 monorepo）**、homepage `agent-native.com`。**Latest GH release `@agent-native/toolkit@0.20.4` published 2026-09-17 19:37 UTC（4 天前）**、**npm `@agent-native/core@0.182.1` 同步 published 2026-09-17 19:36:48 UTC（差 1 分鐘）** —— npm 跟 GH release 時間同分鐘級同步（registry-lag check PASS）。**6 大 included feature**：(1) **Agent chat** — 同一個 UI 內 delegate / ask / review、(2) **Authentication + permissions** — 控制誰能讀寫 shared work、(3) **Skills + memory** — reusable expertise + persistent context、(4) **Automations** — schedule + event-driven agent run、(5) **Agent teams** — delegate to specialist agents in same workspace or across connected、(6) **PostgreSQL backend** — production 用 PostgreSQL、local dev 用 PGlite (in-process WASM Postgres)。**Open-source agent apps**：Clips（record + understand meetings/screens/voice）+ Design（visual design agent）+ Chat 等。**對主人的對位：跟 9-15 `alibaba/open-code-review` + 9-16 `addyosmani/agent-skills` + 9-17 `cloudflare/security-audit-skill` + 9-18 `Tencent/BrowserSkill` + 9-19 `mattpocock/skills` + 9-20 `trycua/cua` + `addyosmani/agent-skills` 同類「agent framework / skill」、但這個獨佔「**shared action pattern（agent tool + UI + HTTP + MCP + A2A + CLI 6 surface 共用同一個 function）**」 niche + 「**PostgreSQL backend by default（vs SQLite / in-memory）**」 設計 choice**。跟 `trycua/cua`（9-20）對位是「**agent framework + UI + DB**」vs「**computer-use framework + VM + driver**」、跟 `microsoft/agent-framework`（9-18 報告）對位是「**TS + community-led**」vs「**Python + Microsoft-led**」、跟 `semantica-agi/semantica`（9-20）對位是「**PostgreSQL 預設 backend + no LLM required**」vs「**多 swappable graph backend + W3C standards**」。對主人 horo-agent 對位是「**shared action pattern 是可以直接抄的 abstraction**」+ 「**PGlite for local dev** 是可以直接抄的 setup（air-gapped 友好）」。
- **3W1H：**
  - **What：** TypeScript framework monorepo（352 MB = 含 docs site + framework source + agent sample apps）+ `@agent-native/core` + `@agent-native/react` + `@agent-native/next` + `@agent-native/mcp` 等 npm packages + Nitro server framework + PostgreSQL / PGlite drivers + React UI components + auth + automation engine + multi-agent team orchestrator。
  - **Why：** 現有 agent framework 三大對位：(1) **Agent 是「text box」** 的、UI 跟 agent 工作流分離（OpenAI ChatGPT / Anthropic Console 都是）、(2) **UI 跟 agent 是「兩個 codebase」** 的（agent 跑 Python、UI 跑 React、要 glue code）、(3) **NoSQL/SQLite 預設 backend** 對 enterprise 不足。**Agent-Native 解三對位**：(a) **shared actions** = 一個 `defineAction()` 寫六個 surface（agent tool + UI from code + HTTP + MCP + A2A + CLI）共用同一個 validation + permissions + implementation、(b) **shared data + state** = agent 寫的 work 在 UI 看得到、UI 寫的 work agent 也看得到、(c) **PostgreSQL 預設** = enterprise 友善、PGlite for local dev = WASM in-process Postgres（zero infra local）。**Why 敘述直接寫**：「Coding agents work with more than a text box. Their environment provides context, tools, files, tests, and previews that make their capabilities and results visible.」—— **強調「agent 在 context-rich environment 裡工作」不只是「在 text box 裡」**。**近期受關注原因**：(a) Builder.io 在 no-code / CMS 領域老牌、(b) shared action pattern 是 2026 開始流行的 abstraction（多家框架開始 follow）、(c) PGlite 是 WASM Postgres 創新。
  - **Who：** **Full-stack developer** 想 ship 一個 agent-driven app（agent 跟 UI 同一 codebase）給 enterprise 的；**startup founder** 想用一個 framework 取代「FastAPI + LangChain + React + shadcn」 4 stack 的；**Builder.io existing user** 想 ship agent UI 在 Builder.io CMS 上的；**research lab** 想跑 reproducible agent evaluation 環境的（PGlite + TS zero infra）。
  - **How：** **`npx --yes @agent-native/core@latest create my-agent --standalone --template chat`**（scaffold 一個 starter template，含 chat UI + agent + database）。**Quick Start 三步**：(1) `defineCatalog` 定義 components（Card / Metric / Button）+ actions（export_report / refresh_data）、(2) `defineRegistry` 定義 components rendering function（用 React / Vue / Svelte / Solid 等 renderer）、(3) 渲染 `<Renderer spec={ai_generated_spec} registry={registry} />`。**共享 action** 範例：`defineAction({description, schema: z.object({name: z.string()}), http: {method: "GET"}, run: async ({name}) => ({message: ...})})` —— 同一個 action 暴露給 agent tool、React `useActionQuery("hello", {name})`、HTTP GET、MCP、A2A、CLI 六 surface。
- **安裝方式：**
  - **Scaffold（最快路徑，官方推薦）：** **`npx --yes @agent-native/core@latest create my-agent --standalone --template chat`** — 在當前目錄產生 my-agent 專案（含 chat template + database + agent）。需要 Node.js（npm/npx/pnpm/yarn/bun 都可）。
  - **手動裝進既有專案：** **`npm install @agent-native/core @agent-native/react`** + 設定 PostgreSQL connection（production）或 PGlite（local dev）+ 寫 `defineAction` + `defineCatalog` + `defineRegistry`。
  - **Next.js 整合：** 用 `@agent-native/next` + Nitro server。
  - **MCP server：** `@agent-native/mcp` 暴露 agent 給 Claude Desktop / ChatGPT / Cursor / VS Code 等 MCP client。
  - **Source clone：** `git clone https://github.com/BuilderIO/agent-native`（352 MB monorepo）+ 看 `packages/core/` + `packages/react/` + `apps/`。
  - **pip / uv / poetry：** **不適用**（這是 TypeScript framework、無 Python binding）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可跑 `npx --yes @agent-native/core@latest create my-agent --standalone --template chat`（需 Node.js 18+）。HORO-Agent 對位需自寫 PGlite driver 或直接用 PostgreSQL docker-compose。**注意 352 MB monorepo clone 時間偏長**、建議先 npx scaffold、而非 source clone 整包。
- **近期 release：** **`@agent-native/toolkit@0.20.4` published 2026-09-17 19:37 UTC（4 天前）** = latest GH release。**npm `@agent-native/core@0.182.1` 同步 published 2026-09-17 19:36:48 UTC（差 1 分鐘）** —— npm 跟 GH release 分鐘級同步（registry-lag check PASS）。GH release tag 是 `@agent-native/toolkit` 而 npm latest 是 `@agent-native/core`、**兩個不同 scope 但同 framework family**、`toolkit` 應該是 wrapper / CLI / scaffold、`core` 是 runtime。**Main 仍 active push**（`pushed_at` 2026-09-21 06:00 UTC 今天仍 push）。**Release cadence 不固定** —— 從 release 歷史看是 irregular minor version bumps、`@agent-native/core@0.182.1` 是 6 個月內已 ship 0.18x 版號、daily/semi-daily cadence。對位 9-17 `NousResearch/hermes-agent`「每日 commit + 不規則 release」、9-19 `deepseek-ai/deepseek-harness`「alpha + rc + next + latest 4-tag npm」、9-20 `trycua/cua`「nightly + stable 雙 train」是同類「**active multi-train release engineering**」、但這個 release tag 跟 scope 不對齊（`toolkit` vs `core`）是個 minor confusing point。

### `vercel-labs/json-render`

- **Repo 摘要：** 「**The Generative UI framework**」——Vercel Labs（Vercel 旗下 experimental product division、曾出 Next.js / Turborepo / Svelte 等）開源 8 個月的「**Guardrailed Generative UI framework**」、**AI 從 prompt 生成 UI 但只能使用你預定義的 components + actions、JSON output 符合 schema**、**29 個 npm packages 跨 9 種 renderer**（React / Vue / Svelte / Solid / React Native / Remotion video / react-pdf / react-email / Ink terminal / R3F 3D）+ 36 個 pre-built shadcn/ui components。17,538★、TypeScript、**Apache-2.0**（LICENSE 檔確認）、topics 空陣列、`created` 2026-01-14（**8 個月**）、`pushed_at` 2026-09-18 18:53 UTC（**3 天前仍 push**）、`updated_at` 2026-09-21 06:00 UTC、`open_issues_count` 109、size 5.8 MB、homepage `json-render.dev`。**Latest GH release `v0.21.0` published 2026-09-18 18:53 UTC（3 天前）**、**npm `@json-render/core@0.21.0` 同步 published 2026-09-18 18:50:55 UTC（差 2 分鐘）** —— npm 跟 GH release 分鐘級同步（registry-lag check PASS）。**3 大哲學**：(1) **Guardrailed** = AI 只能使用你 catalog 內的 components、(2) **Predictable** = JSON output 永遠 match schema、(3) **Cross-platform** = 9 個 renderer 用同一個 catalog。**29 packages** = `@json-render/core` (schemas + catalogs + AI prompts + SpecStream utilities)、`/react` `/vue` `/svelte` `/solid` `/react-native` `/next` `/tanstack-start` `/remotion` `/react-pdf` `/react-email` `/ink` `/image` (OG/social via Satori) `/directives` ($format / $math / $t i18n) `/codegen` `/devtools` + 4 framework adapter + `/mcp` (MCP Apps for Claude/ChatGPT/Cursor/VS Code) + `/yaml` (streaming parser + AI SDK transform)。**對主人的對位：跟 9-20 `trycua/cua` 同「**Vercel Labs 出的開源 8 個月 framework**」對位、但 `trycua/cua` 是 computer-use framework（hardware-adjacent）而 `json-render` 是 generative UI framework（UI-adjacent）**。跟 9-15 `alibaba/open-code-review` + 9-18 `Tencent/BrowserSkill` + 9-20 `cloudflare/security-audit-skill` 同類「vendor-Labs 出品 agent skill / framework」，但這個獨佔「**Guardrailed generative UI + 9 renderer + 29 packages**」 niche。對主人 horo-agent 對位是「**主人若要 ship agent-driven dashboard / visualization（不是純 chat UI）、json-render 是 Vercel Labs 出的 framework、可直接借 9 renderer 抽象**」+ 「**MCP Apps integration（`@json-render/mcp`）**是 2026 開始流行的「agent 透過 MCP 渲染 UI」 standard protocol」+ 「**Apache-2.0 license + standard SPDX + LICENSE 檔** = case-A2 strict permissive」、對 horo-agent downstream 完全可商用。
- **3W1H：**
  - **What：** 29 個 npm packages under `@json-render/*` scope + `defineCatalog` / `defineRegistry` / `Renderer` 為核心 API + 36 個 pre-built shadcn/ui components + 9 renderer（React / Vue / Svelte / Solid / React Native / Remotion / react-pdf / react-email / Ink / R3F 3D）+ devtools panel + codegen utility + MCP Apps integration。
  - **Why：** 現有 generative AI UI 三大病灶：(1) **AI 直接生 HTML/JSX** → 不可靠、可能 XSS、可能結構錯誤、(2) **每個平台寫一份 UI**（Web / Mobile / Email / PDF / Video 各一份）、(3) **沒辦法限制 AI 行為**。**json-render 解三病灶**：(a) **AI 生 JSON、JSON match schema、JSON 餵進 renderer** → XSS-free、schema-validated、(b) **同一個 catalog → 9 renderer** → write once deploy everywhere、(c) **guardrail = 你定義 components 跟 actions、AI 只能 pick from them** = 限制行為 + 增 predictability。**跨 9 renderer 的 catalog 抽象** = 一個 `Card` 在 React / Vue / Svelte / Solid / React Native / Remotion video frame / react-pdf page / react-email email / Ink TUI / R3F 3D scene 都用同一個 schema 描述、render 時 swap renderer。**近期受關注原因**：(a) Vercel Labs brand、(b) generative UI 是 2026 AI 主流應用、(c) MCP Apps 是新 protocol、`@json-render/mcp` 直接吃這個 trend。
  - **Who：** **Frontend developer** 想 ship AI-generated UI 但要 schema-constrained 的（不想讓 AI 亂生 HTML）；**Full-stack developer** 想一份 catalog 跨 web / mobile / email / pdf / video 的；**Dashboard / BI 平台 builder**（Vercel 是主場）；**AI agent platform team** 想讓 agent 在 MCP session 內渲染 structured UI 的（`@json-render/mcp`）；**AI-native startup** 想快速 iterate 跨平台 UI 的。
  - **How：** **Step 1: Define Catalog**：`defineCatalog(schema, {components: {Card: {props: z.object({title: z.string()}), description: "..."}, Metric: {...}, Button: {...}}, actions: {export_report: {...}, refresh_data: {...}}})` —— 一個 catalog 描述所有 components 跟 actions。**Step 2: Define Components / Registry**：`defineRegistry(catalog, {components: {Card: ({props, children}) => <div className="card">...</div>, ...}})`。**Step 3: Render AI-generated specs**：`<Renderer spec={ai_generated_json} registry={registry} />` —— AI 生 JSON spec → 透過 catalog 跟 registry 渲染成 UI。**Stream + Progressive rendering**：`SpecStream` utilities 讓 spec streaming 進來時 partial render。
- **安裝方式：**
  - **React（官方預設，最快路徑）：** **`npm install @json-render/core @json-render/react`** — 裝 core schema + React renderer。
  - **React + shadcn/ui pre-built components（推薦）：** **`npm install @json-render/shadcn`** — 直接拿 36 個 pre-built shadcn components（Radix UI + Tailwind）。
  - **React Native：** **`npm install @json-render/core @json-render/react-native`**。
  - **Video（Remotion）：** **`npm install @json-render/core @json-render/remotion`**。
  - **PDF：** **`npm install @json-render/core @json-render/react-pdf`**。
  - **HTML Email：** **`npm install @json-render/core @json-render/react-email @react-email/components @react-email/render`**。
  - **Vue 3：** **`npm install @json-render/core @json-render/vue`**。
  - **Svelte 5：** **`npm install @json-render/core @json-render/svelte`**。
  - **SolidJS：** **`npm install @json-render/core @json-render/solid`**。
  - **Terminal UI：** **`npm install @json-render/core @json-render/ink ink react`**。
  - **Next.js full app：** **`npm install @json-render/core @json-render/react @json-render/next`**。
  - **3D scene（含 Gaussian Splatting）：** **`npm install @json-render/core @json-render/react-three-fiber @react-three/fiber @react-three/drei three`**。
  - **pnpm / yarn / bun：** 對應 `pnpm add` / `yarn add` / `bun add`。
  - **MCP Apps integration：** **`npm install @json-render/mcp`** — 透過 MCP protocol 渲染 structured UI 給 Claude / ChatGPT / Cursor / VS Code。
  - **pip / uv / poetry：** **不適用**（這是 TypeScript / JavaScript framework、無 Python binding）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可走 **`npm install @json-render/core @json-render/react`** + `@json-render/shadcn` 預設 UI components。HORO-Agent 對位需自寫 Python-side `@json-render/core` schema parser 或透過 `@json-render/mcp` 從 Python agent call MCP server。**Apache-2.0 license** 對主人 horo-agent downstream 完全 free + patent grant。
- **近期 release：** **`v0.21.0` published 2026-09-18 18:53 UTC（3 天前）** = latest GH release。**npm `@json-render/core@0.21.0` 同步 published 2026-09-18 18:50:55 UTC（差 2 分鐘）** —— npm 跟 GH release 分鐘級同步（registry-lag check PASS）。**Main 仍 active push**（`pushed_at` 2026-09-18 18:53 UTC 3 天前 push）。**Release cadence 屬「minor version active push」、3-7 天一版**（v0.21 3 天前、v0.20 之前）。對位 9-19 `addyosmani/agent-skills`「9 天一版 active cadence」是「bi-weekly stable」、json-render 是「**3-7 天一版、active push + frequent shipping**」= 是 framework 類「ship new renderer / new component feature」對位的「**minor-version daily**」 cadence。對位 9-20 `semantica-agi/semantica`「bi-weekly stable」是「嚴謹測試週期」、json-render 是「**rapid renderer addition**」。

### `markfulton/ai-employees`

- **Repo 摘要：** 「**Open source AI Employees. 8 scheduled business roles, 60 routines, on Claude Code and 10 other harnesses. They drive your browser the way you do and improve every run. You own the files.**」——Mark Fulton（Reinventing.AI founder、Facebook group Vibe Coding is Life 340,000+ members 創辦人、since August 27, 2026 用 Claude Code 跑自己的 club launch every weekday）9-19 release 的「**8 scheduled AI Employees + 60 routines + 11 agent harnesses**」開源 MIT。239★、JavaScript、MIT、topics 13 個（`ai-agents / ai-employee / ai-employees / antigravity / automation / claude-code / claude-skill / codex / hermes-agent / openclaw / opencode / scheduled-tasks / small-business`）、`created` 2026-09-02（**19 天 = brand new**）、`pushed_at` 2026-09-19 09:21 UTC（**2 天前 push = 9-19 release**）、`updated_at` 2026-09-21 06:00 UTC、`open_issues_count` 4、size 9 MB、homepage 空。**沒有 GitHub release**（releases API 回空陣列）—— 但 README 寫「released 2026-09-19」、**`pushed_at` 9-19 = 9-19 commit 為 release cut**、**npm `ai-employees@1.7.0` published 2026-09-19 09:29:20 UTC** = npm 跟 GH `pushed_at` 同分鐘級同步。**8 個 AI Employees**：GTM Engineer（launch positioning, outbound, weekly scoreboard, 8 routines）+ SEO/AEO Employee（keyword research, one article/weekday, publishing, 8 routines）+ Web Dev Employee（site health, error triage, 8 routines）+ Social Media Employee（posts in your voice per platform, 7 routines）+ Ad Manager Employee（account reads, creative sets, 7 routines）+ Sales Employee（prospect sweeps, first touches, 7 routines）+ Customer Satisfaction Employee（inbox sweep, replies drafted, 8 routines）+ Chief of Staff（reads every other employee's run log, names what quietly stopped, 7 routines）。**每個 employee 是一個 plain-text folder**：`INSTALL-PROMPT.md` (one-time setup) + `ROLE.md` (who + how thinks) + `SCHEDULE.md` (when each routine runs) + `RELEASES.md` (channels you released, shipped empty) + `routines/<id>/SKILL.md` (one per routine)。**11 agent harnesses 支援**（`docs/HARNESSES.md` 逐個列 status + scheduler + invocation + what to check first + adding connection）：Claude Desktop app (Windows / macOS) + Claude Code CLI (Windows / macOS / Linux) + OpenClaw + **Hermes** + OpenCode + Grok Bot + Codex + Antigravity + Pi + Cline + Qwen Code + DeepSeek = **12 entry 但實質 11 harness**（Claude Desktop app + Claude Code CLI 同 agent family）。**對 Hermes 的 HARNESSES.md entry 原文**：「Hermes | Ready | Built in cron with delivery to any platform | Point each job at the routine file as its prompt | Whether it reads files and runs a shell command; then the browser question | `mcp_servers` in `~/.hermes/config.yaml`, or `hermes mcp install <name>` from its catalog」。**對主人的對位：跟 9-15 `alibaba/open-code-review` + 9-16 `addyosmani/agent-skills` + 9-17 `cloudflare/security-audit-skill` + 9-18 `Tencent/BrowserSkill` + 9-19 `mattpock/skills` + 9-20 `trycua/cua` + `addyosmani/agent-skills` 同類「vendor-Labs 或個人 maintainer 的 agent skill collection」、但這個獨佔「**scheduled-routine AI Employees + 11 harness + Hermes Ready 顯式支援 + 小商家定位**」 niche + 「**personal maintainer + 9-module Masterclass + Product Pass resale license 商業 model**」 模式**。跟 `addyosmani/agent-skills`（9-16/9-20）對位是「**scheduled routines + 小商家定位**」vs「**lifecycle DEFINE→SHIP + engineer 定位**」、跟 `Tencent/BrowserSkill`（9-18）對位是「**small business automation**」vs「**browser-control vendor**」、跟 `cloudflare/security-audit-skill`（9-17/9-20）對位是「**ship to user machine**」vs「**ship to marketplace**」。對主人 horo-agent 對位是「**主人 Hermes Agent 已列為 Ready 顯式支援 + 主人若要 ship small-business automation、ai-employees 是 reference 設計**」+ 「**8 AI Employees 的 ROLE.md + SCHEDULE.md + RELEASES.md 三件式 design 是可以直接抄的 agent contract pattern**」。
- **3W1H：**
  - **What：** 一個 monorepo（8 employee folders）+ npm `ai-employees` CLI + docs/INSTALL.md + docs/PREREQUISITES.md + docs/HARNESSES.md（12 entry harness matrix）+ docs/HOW-EMPLOYEES-WORK.md + docs/GUARDRAILS.md + docs/COST.md + docs/FAQ.md + CONTRIBUTING.md + SECURITY.md + TRADEMARKS.md + LICENSE + 60 個 `routines/<id>/SKILL.md` plain-text routine files。
  - **Why：** 現有 AI agent 給 small business 三大對位：(1) **AI agent 沒有 scheduled routine** — 開瀏覽器就跑、不開就跑不了、(2) **AI agent 不能保證 draft-only** — 容易誤發 / 誤付、(3) **AI agent 不能跨 11 個 harness 一致執行**。**ai-employees 解三對位**：(a) **8 employees 60 routines 都是 scheduled jobs**（cron / launchd / Task Scheduler / openclaw automations / codex automations / agy job runner / Cline schedule / built-in cron in Hermes 等 native scheduler），(b) **Two guardrails** = 「**Every send, post, submit, publish and spend is held for your click**」 + 「**RELEASES.md 是你交 channel 的地方**」+ **never creates account / enters password / completes captcha / writes credential** = **三層 guardrail 保證 draft-only**，(c) **11 harness 共用同一個 ROLE.md contract** + 每個 harness 一個 invocation shim file（`run/<id>.cmd.example` for Windows Claude Code CLI 等）。**對位純「real-world business routine」**：從 GTM Engineer 的 weekly scoreboard 到 Chief of Staff 讀每個 employee run log 找出 silently stopped 的 = **「agent as full-time employee」不是「agent as one-shot prompt」**。**近期受關注原因**：(a) 9-19 release、(b) 11 harness **含 Hermes Ready**、(c) small-business 市場 narrative（SEO / Ad Manager / Sales / Customer Satisfaction 都是 SMB 痛點）。
  - **Who：** **Solo founder / freelancer / 1-10 人 startup operator** 想用 AI 取代 / 補充 GTM / SEO / Sales / Customer Satisfaction 等業務角色的；**AI agent platform team** 想 ship scheduled-routine capability 給 user 的；**SMB consultant** 想 ship client-facing AI Employee 的（MIT license、可 resale、改名即可）；**主人 Hermes Agent user** 想 ship Hermes Agent 為主的 business automation 給小商家客戶的（**Hermes 是 HARNESSES.md 第一個 Ready entry 之一**）。
  - **How：** **Install 兩步**：(1) **`npx ai-employees hire gtm-engineer --to <folder>`**（或從 GitHub `Code → Download ZIP` 解壓縮到任意非 OneDrive / Dropbox / iCloud folder）。(2) **在該 folder 開 AI agent、說「Install the GTM Engineer from this folder.」** —— agent 自己研究你 business 從 website、建 dashboard、schedule routines、停一次給你看 first drafts。**Hermes 安裝（主人對位）**：`mcp_servers` 在 `~/.hermes/config.yaml` 或 `hermes mcp install <name>` from catalog + 對每個 routine 用 Hermes built-in cron + delivery to any platform、point each job at routine file as its prompt。**Guardrails 設計**：「**Two rules hold on every harness**」：(a) **routines 是 scheduled jobs 不可拷進 global skills directory**（否則每個 session 都 load、不在 window 內就跑），(b) **keep one copy**（routine 末尾有 `## Corrections` section 你寫它讀、兩 copy 等於寫一份讀一份）。**驗證**：`npx ai-employees upgrade`（升級但不覆蓋你編輯過的檔）。
- **安裝方式：**
  - **Clone + 解壓縮（最簡單路徑，官方推薦）：** **`git clone https://github.com/markfulton/ai-employees`**（或 `Code → Download ZIP`） → 解壓到任意 folder（不在 OneDrive / Dropbox / Google Drive / iCloud）。每個 employee 是一個 self-contained folder、可只裝一個。
  - **npm CLI hire：** **`npx ai-employees hire gtm-engineer --to <folder>`** — 從 npm registry 自動下載 gtm-engineer kit 到指定 folder。
  - **Agent prompt install：** 在 folder 內開 AI agent（Claude Code / OpenClaw / Hermes / OpenCode / Grok Bot / Codex / Antigravity / Pi / Cline / Qwen Code / DeepSeek）、prompt「**Install the GTM Engineer from this folder.**」—— agent 會自動跑 install flow。
  - **Hermes Agent 安裝（主人對位，顯式 Ready）：** 依 `docs/HARNESSES.md`：**(1) `mcp_servers` in `~/.hermes/config.yaml`** 或 **`hermes mcp install <name>` from its catalog**；(2) 用 Hermes **built-in cron** + delivery to any platform；(4) 每個 job point at routine file as prompt。**First-run check**：whether Hermes reads files and runs shell command、then the browser question。**注意**：routines 是 scheduled jobs 不可拷進 `~/.hermes/skills/`（Hermes skills directory 會 load 到每個 session、且讓 routine 在 window 外被 invoke）。
  - **Upgrade：** **`npx ai-employees upgrade`** — leave every file you edited alone、只升級 kit 本體。
  - **Source-only：** **`git clone https://github.com/markfulton/ai-employees`** + 拷貝需要的 employee folder + 設定 schedule。
  - **pip / uv / poetry：** **不適用**（這是 npm/JS 開源 project、AI agent 透過 SKILL.md 跑）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可走 **`npx ai-employees hire gtm-engineer --to ~/projects/ai-employees`** + 在 Hermes Agent 內 prompt「Install the GTM Engineer from this folder」即可。Hermes 已 Ready 顯式支援（`docs/HARNESSES.md` Hermes row）、主人 horo-agent 對位極佳。**注意**：routines 是 scheduled jobs 不可拷進 Hermes global skills directory、要用 Hermes built-in cron 觸發。
- **近期 release：** **未找到 GitHub release**（releases API 回空陣列）—— 但 README 明確寫「released 2026-09-19」、**`pushed_at` 2026-09-19 09:21 UTC 為 release cut**、**npm `ai-employees@1.7.0` published 2026-09-19 09:29:20 UTC（GH push 8 分鐘後）** —— npm 跟 GH `pushed_at` 分鐘級同步、但 GH 沒有 formal release tag。**「ship via main + npm publish、無 GitHub release」型態** —— 跟 9-17 `cloudflare/security-audit-skill`「無 release」對位、但這個是用 npm package 取代 GH release（npm latest = source of truth）、security-audit-skill 連 npm 都沒有。**Release cadence 不固定** —— 從 9-2 創立到 9-19 release = 17 天、9-21 仍在 push main（`pushed_at` 9-19、updated_at 9-21）。**Release 9-19 後**到現在（9-21）已過 2 天、main 仍在 active push、可能近期再 cut 一版。對位 9-17 `cloudflare/security-audit-skill`「無 release」、9-19 `mattpocock/skills`「無 release」同「**personal maintainer 開源 project 早期 + npm publish 作為 release 機制**」、但這個 npm publish 1.7.0 較 mature（已有 6 版 npm 版本）。

### `yylo-dev/yylo`

- **Repo 摘要：** 「**YYLO (why-lo): AI coding-agent orchestration CLI with equivalent yylo and yy launchers.**」——yylo-dev（個人 maintainer、`yylo.tools`、Sponsors `github.com/sponsors/yylo-dev`）1-06 創立的「**AI coding-agent orchestration CLI**」、給 developer 想跑 quick agent loop + project operator 想 typed task + validation + merge + release-readiness boundaries 的人。**`yylo` and `yy` 是 equivalent launchers**、`ypl` = `yy pi --live`（Pi subagent live terminal agent）。60★、Python、MIT、**topics 20 個**（`agentic-coding-approved / agentic-coding-rules / agentic-coding-tool / agentic-coding-tools / agentic-coding-workflow / ai-agent / ai-native-development / aider / autonomous-agents / claude-code / coding-agent / developer-tools / developer-tools-ai-agent / developer-tools-productivity / git-workflow / local-first / pi-agent / ralph-loop / ralph-wiggum / yylo`）、`created` 2026-01-06（**8 個月**）、`pushed_at` 2026-09-20 21:33 UTC（**昨天仍在 push**）、`updated_at` 2026-09-21 06:00 UTC、`open_issues_count` 10、size 16 MB、homepage `yylo.dev`。**Latest GH release `cli-v0.2.1-rc.1` published 2026-08-29 08:48 UTC（23 天前）**、但**npm `@yylo/cli@0.2.6` published 2026-09-19 02:05:35 UTC（2 天前）** + **next = `0.2.3-rc.3`**。**Registry-lag 反例**：GH release tag = `cli-v0.2.1-rc.1`（08-29）、npm latest = `0.2.6`（09-19）、GH release 落後 npm 21 天 —— **README 寫 stable 是 `@latest` (0.2.2)、但 npm latest 已升到 0.2.6、GH release 完全沒跟上**。**核心概念**：(1) **Worktree-based parallel coding agents** — 每個 task 在 dedicated branch + worktree 內跑、(2) **Risk-based merge queue** — review result with receipt-backed changes、(3) **Typed task + validation + merge + release-readiness boundaries** — task type 嚴格檢查 + merge 風險評估 + release readiness gate、(4) **Pi + Codex subagents** — built-in Pi agent + Codex CLI integration、(5) **7 user-intent-first skills** (`artifact-yylo / ledger-tasks-yylo / plan-ledger-tasks-yylo / ralph-loop-yylo / understand-project-yylo / wiki-yylo / workflow-yylo`)、(6) **YYLO Ledger** + **YYLO Benchmark** 是 independent Git-native record/task store + evaluation/evidence package、`yy ledger` / `yy benchmark` delegate 到 `yylo-dev/yylo-ledger` + `yylo-dev/yylo-benchmark`、不是 bundled alternate implementation。**對主人的對位：跟 9-15 `alibaba/open-code-review` + 9-16 `addyosmani/agent-skills` + 9-17 `cloudflare/security-audit-skill` + 9-18 `Tencent/BrowserSkill` + 9-19 `mattpocock/skills` + 9-20 `trycua/cua` + `addyosmani/agent-skills` 同類「agent skill / framework」、但這個獨佔「**AI coding-agent orchestration CLI + typed task + validation + merge + release-readiness boundaries + worktree-based parallel**」 niche + 「**personal maintainer + Sponsors + 3 sibling repos (`yylo-dev/yylo` + `yylo-dev/yylo-ledger` + `yylo-dev/yylo-benchmark`)**」 monorepo-by-organization 模式**。對主人 horo-agent + Kanban 的對位是「**主人用 Kanban 做 cross-agent task routing + release management、若要 ship 一個 worktree-based parallel coding agents CLI 給 team、yylo 是 cleanest reference design**」+ 「**typed task + validation + merge + release-readiness boundaries 4-stage gate 可以直接抄給 HORO-Agent Kanban 設計**」+ 「**YYLO Ledger (Git-native record/task store) + YYLO Benchmark (eval package) 是 reference 分拆 sibling repo 設計**」。
- **3W1H：**
  - **What：** Python monorepo + npm CLI `@yylo/cli`（雖然語言是 Python、CLI 包在 npm 是 dual-distribution pattern）+ `yylo-dev/yylo-skills`（independent skill versioning）+ `yylo-dev/yylo-ledger`（Git-native Record/task store）+ `yylo-dev/yylo-benchmark`（evaluation/evidence package）= **4-repo organization pattern**。CLI 提供 8 個 public surface: `yy pi` / `yy start` / agent aliases（agent run）+ `continue / clone / branches / switch / continuity`（session continuity）+ `watch status|await|follow`（optional observation）+ `evidence run|status|await`（validation evidence）+ `info / where / doctor workspace / integration`（repository topology）+ `task start|run|status|checkpoint|preflight|finish`（feature lifecycle）+ `merge status / merge land / merge project`（protected delivery）。
  - **Why：** 現有 AI coding agent workflow 三大對位：(1) **每個 agent run 都是 isolated experiment** — 沒 validation + merge gate、(2) **沒有 release-readiness** — agent 生產 code 不等於 production-ready、(3) **沒有 ledger / evidence trail** — 誰 run 了什麼改了什麼不知道。**yylo 解三對位**：(a) **typed task + validation + merge + release-readiness 4-stage boundary** = explicit gate、(b) **risk-based merge queue** + receipt-backed changes = auditable、(c) **YYLO Ledger** Git-native Record/task store + **YYLO Benchmark** evidence package = trail + eval。**對位「worktree-based parallel coding agents」**：每個 task 在 dedicated branch + worktree 跑 = 不污染 main、parallel agents 不打架。**近期受關注原因**：(a) personal maintainer model + sponsors、(b) YYLO Ledger + Benchmark 是 clean 4-repo split pattern、(c) Pi subagent 是 local-first 設計。
  - **Who：** **Solo developer** 想用 quick agent loop 跑 PI/Codex subagent 的；**Project operator** 想 typed task + validation + merge + release-readiness 4-stage gate 跑 team workflow 的；**Multi-agent team** 想 worktree-based parallel coding agents 不打架的；**Tool author** 想 study 4-repo split pattern（CLI + skills + ledger + benchmark 獨立 versioning）；**主人 horo-agent + Kanban** user 想 ship 同等 orchestration CLI 的。
  - **How：** **Install**: `npm install --global '@yylo/cli@latest'`（**需 Node.js 20.10+**、npm、Git）。**First run**: `yy --version` → `mkdir yylo-demo && cd yylo-demo && git init && yy init --task "Document the onboarding path" --subagent pi`。**Successful run** prints installed version、initializes `.juno_task/`、emits watch receipt with `state:COMPLETED`、`exit_code:0`、nonzero `log_bytes`。**Stable + prerelease channel**：`npm install --global '@yylo/cli@latest'` (stable 0.2.2 → latest 0.2.6) vs `'@yylo/cli@next'` (prerelease 0.2.3-rc.3) vs `npm install -g @yylo/cli@0.2.8` (exact)。**Install skills explicitly**（skills 不 bundled in CLI）：`yy skills install` + `yy skills install --version 2.0.2` + `yy skills update` + `yy skills status`。**Inspect**：`yy info --json` + `yy doctor workspace` + `yy --help`。
- **安裝方式：**
  - **npm install global CLI（最簡單路徑，官方推薦）：** **`npm install --global '@yylo/cli@latest'`** —— 需 Node.js 20.10+、npm、Git。**Stable channel = `@latest`**（從 README 0.2.2 升到 npm 0.2.6、Sep 19 published）。
  - **Prerelease：** **`npm install --global '@yylo/cli@next'`** = 0.2.3-rc.3（明確 prerelease 標記）。
  - **Exact pin：** **`npm install -g @yylo/cli@0.2.8`**（CI 場景）。
  - **Registry check：** **`npm view '@yylo/cli' version dist-tags --json`** + **`yy --version`**。
  - **Install agent skills explicitly（skills 不 bundled）：** **`yy skills install`** + 可加 `--version 2.0.2` pin specific version。**Skills versioning 在 independent `yylo-dev/yylo-skills` repo**（public version、CLI 要求 `^2.0.2`）。**Update**：`yy skills update`。**Status**：`yy skills status`。**List local**：`yy skills list`（offline、只讀 local bytes + receipts）。
  - **First run canary：** `mkdir yylo-demo && cd yylo-demo && git init && yy init --task "Document the onboarding path" --subagent pi`（no model provider contact、empty unborn Git repo）。
  - **Inspect initialized workspace：** `yy info --json` + `yy doctor workspace` + `yy --help`。
  - **pnpm / yarn / bun：** 對應 `pnpm install -g '@yylo/cli@latest'` / `yarn global add '@yylo/cli@latest'` / `bun install -g '@yylo/cli@latest'`。
  - **pip / uv / poetry：** **不適用**（CLI 是 npm distribution、雖然 monorepo 是 Python source、但發行走 npm）。**Poetry 用 Python source 可 clone `yylo-dev/yylo` 後 `poetry install`，但沒有 published PyPI package**。
  - **Source clone（for contributor）：** **`git clone https://github.com/yylo-dev/yylo`** + Python monorepo + 看 `yylo/` source。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可走 **`npm install --global '@yylo/cli@latest'`**（需 Node.js 20.10+）。HORO-Agent 對位需把 yylo 4-stage gate（typed task → validation → merge → release-readiness）抄進 Kanban workflow。**Registry-lag 反例提醒**：GH release `cli-v0.2.1-rc.1` 落後 npm latest `0.2.6` 21 天、**跟 README 對齊：裝 `npm @latest` 拿 0.2.6、不要等 GH release tag**。
- **近期 release：** **GH release `cli-v0.2.1-rc.1` published 2026-08-29 08:48 UTC（23 天前）** = GH last release tag。**npm `@yylo/cli@0.2.6` published 2026-09-19 02:05:35 UTC（2 天前）** + **next = `0.2.3-rc.3`** + **`@yylo/cli@0.2.2` 是 README 寫的 stable** —— **Registry-lag 反例**：GH release 落後 npm latest 21 天、stable channel 名稱（`@latest` = 0.2.2）跟實際 npm 0.2.6 對不上、prerelease channel `@next` = 0.2.3-rc.3（**prerelease 反而比 latest 還低 version number** = unusual prerelease convention）。**Main 仍 active push**（`pushed_at` 2026-09-20 21:33 UTC 昨天仍 push）。**Release cadence 推測**：GH release irregular（23 天前最後一版）、npm publish ~2 天一次（從 README 提的 0.2.2 → 0.2.6 gap）、main 持續 push。對位 9-19 `fmtlib/fmt`「半年 stable + main 持續推」、9-20 `trycua/cua`「nightly 連 4 天 + stable 5 天前」、9-19 `zhayujie/CowAgent`「2-週 stable」：yylo 是「**npm publish daily + GH release irregular**」是 release engineering 雙軌策略、對 master 最 immediate value 是「**README install command 是 `@latest` 不是 specific version、會自動跟上 npm daily publish**」+ 「**registry-lag 反例時 GH release tag 不是 source of truth**」。

---

## 重點觀察

- **5/5 全 permissive（4 MIT + 1 Apache-2.0）= 第 4 個 5/5 permissive tick in series、09-19 1/5 NOASSERTION 中斷後回歸**：今天 5/5 permissive：affaan-m/ECC (MIT) + BuilderIO/agent-native (MIT，npm `@agent-native/core` 顯示 MIT) + vercel-labs/json-render (Apache-2.0 = case-A2) + markfulton/ai-employees (MIT) + yylo-dev/yylo (MIT) = 4 MIT + 1 Apache-2.0。**連續 milestone**：09-17 (5/5 permissive) + 09-20 (4/5 permissive) → **09-19 1/5 NOASSERTION 中斷** → **09-21 5/5 permissive 回歸** = 本週 5-permissive-of-6 命中率 = 83%。**對主人 horo-agent downstream 影響為 0 license friction**、且 **`vercel-labs/json-render` 是 case-A2 (Apache-2.0) = 對主人 horo-agent downstream 有 patent grant bonus**（vs MIT 沒 patent grant）。**報告時若 5/5 permissive、應該明確報告「5/5 permissive、Apache-2.0 +1 patent grant bonus」給主人 horo-agent / horo-webui enterprise downstream team 知道**。

- **Hermes-as-official-agent-host signal 連續第 2 個 14:00 tick（09-20 2/5 → 09-21 2/5）+ 兩個 pick 各自以不同方式 mention Hermes**：今天 2/5 picks 顯式 mention Hermes Agent：(a) **affaan-m/ECC** README 第 237 行明確列 Hermes 為 advanced managed adapter 之一（11 個 harness list：cursor / antigravity / gemini / opencode / codebuddy / joycode / qwen / zed / **hermes** / openclaw）、install command 是 `ecc install --target hermes`、(b) **markfulton/ai-employees** `docs/HARNESSES.md` 對 Hermes row 明確標 **Ready** + 給出 **built-in cron + delivery to any platform** scheduler + **point each job at routine file as prompt** invocation + `mcp_servers` in `~/.hermes/config.yaml` or `hermes mcp install <name>` from catalog = **Hermes 是 12 entry 中唯一 small-business-focused 「horizontal integration」niche**。**對主人 horo-agent / horo-webui 直接利多**：兩個 pick 都把 Hermes Agent 當 first-class（ai-employees 顯式 Ready、ECC advanced adapter）、**主人若要 ship HORO-Agent 在 Hermes 上的應用、這兩個 pick 的 install pattern 是可以直接抄的 reference**（特別是 ai-employees 的 `mcp_servers` in `~/.hermes/config.yaml` 或 `hermes mcp install` pattern）。**MEMORY-direct-match 5-of-8-tick count (08-16 + 08-17 + 08-19 + 08-20 + 09-21)** — 連續 5 tick 中有 Hermes 提及的比例是 5/8 = 62.5%、**且今天兩個 pick 是不同 niche（operating system vs scheduled employees）獨立 fire Hermes-as-host signal = structural consolidation 持續**。

- **Release freshness 5/5 全在過去 2 週內 release、registry-lag check 4/5 PASS + 1/5 反例 = 100% fresh release 是本月新高**：今天 5 個 pick 全部都在 9-04 ~ 9-19 範圍內 release：**ECC `v2.2.1` Sep 8（13 天前）**、**agent-native `@agent-native/toolkit@0.20.4` Sep 17（4 天前）**、**json-render `v0.21.0` Sep 18（3 天前）**、**ai-employees `npm 1.7.0` Sep 19（2 天前、但無 GH release）**、**yylo `npm 0.2.6` Sep 19（2 天前）** —— **5/5 全 ≤ 13 天 release + 4/5 ≤ 4 天 release** = fresh-release rate 100%。**Registry-lag check**：4/5 PASS（GH release 跟 npm publish 分鐘 ~ 小時級同步）、1/5 反例是 **yylo `cli-v0.2.1-rc.1` (GH 08-29) vs npm `@yylo/cli@0.2.6` (npm 09-19) 落差 21 天** = yylo 是 personal maintainer 用 npm daily publish + irregular GH release tag 模式、**README install command 是 `@latest` 自動跟上 npm、但 GH release tag 不是 source of truth**。對位 09-20 是 1/5 zero-GH-release = 20% zero-release floor、09-21 是 **1/5 zero-GH-release** (ai-employees) = 20% zero-release floor (verify ×2)、其餘 4/5 都有 release、且 100% fresh = **`fresh-release rate 100% + zero-release rate 20%`** = 「**所有 pick 都有 release signal、且大多數在過去 4 天**」、這是本月最強 fresh signal 場景。

- **Multi-harness / Cross-runtime / Cross-renderer 設計是 2026-09-21 五個 pick 共同主題 = 「installation surface 標準化」訊號**：今天 5 個 pick 各自有「跨 X 個 runtime 抽象」：(a) **affaan-m/ECC** 跨 **11 個 harness** (Claude Code / Codex / OpenCode / Cursor / Gemini / Zed / Copilot / Antigravity / Qwen / Hermes / OpenClaw)、(b) **BuilderIO/agent-native** 跨 **6 個 surface** (agent tool + UI from code + HTTP + MCP + A2A + CLI)、(c) **vercel-labs/json-render** 跨 **9 個 renderer** (React / Vue / Svelte / Solid / React Native / Remotion / react-pdf / react-email / Ink / R3F 3D) + **MCP Apps protocol**、(d) **markfulton/ai-employees** 跨 **11 個 harness** (Claude Desktop/CLI / OpenClaw / Hermes / OpenCode / Grok Bot / Codex / Antigravity / Pi / Cline / Qwen Code / DeepSeek) + 3 OS (Windows / macOS / Linux)、(e) **yylo-dev/yylo** 跨 **Pi + Codex subagents** + YYLO Ledger + YYLO Benchmark 3-repo split + npm + Python source dual distribution。**共同抽象**：**「write once、deploy across N 個 target」** —— ECC 寫 skills 一次、11 個 harness 用；agent-native 寫 action 一次、6 surface 用；json-render 寫 catalog 一次、9 renderer 用；ai-employees 寫 ROLE.md 一次、11 harness 用；yylo 寫 task 一次、Pi + Codex + 多 sibling repo 用。**對主人 horo-agent 對位是「**主人若 ship HORO 通用 framework、應該明顯表達「跨 X 個 runtime」的 abstraction、這是 9-21 的 5 個 pick 共同傳達的訊號**」+ 「**HORO 應該選 1 個或多個 cross-runtime 抽象（multi-harness / cross-renderer / cross-surface）作為架構核心**」**。

- **Personal-maintainer + sponsor / Pro subscription 商業 model 在 5/5 pick 中有 3/5 採用 = 「single maintainer + paid product model」是 9-21 主流**：今天 5 個 pick 中 **3/5 是 single maintainer + paid product** 模式：(a) **affaan-m/ECC** = affaan-m 個人 + Sponsors + **ECC Pro $19/seat/mo + $19/seat/mo GitHub Marketplace App**、(b) **markfulton/ai-employees** = Mark Fulton 個人 + **Agent Ops Masterclass + Product Pass resale license + Lifetime $499**、(c) **yylo-dev/yylo** = yylo-dev 個人 + **Sponsors + 4-repo split (CLI + skills + ledger + benchmark 各自獨立 maintainer)**。**2/5 是 vendor-Labs**（BuilderIO agent-native = Builder.io 旗下 + json-render = Vercel Labs 旗下）。**owner 分布**：(a) ECC = 個人、(b) agent-native = Builder.io 公司、(c) json-render = Vercel Labs 公司、(d) ai-employees = 個人（Mark Fulton）、(e) yylo = 個人 = **3 個人 + 2 公司** = 個人 maintainer 比例 60% 是 9-21 主旋律。**Sponsorship structure**：(a) ECC = GitHub Sponsors + Discord、CodeRabbit / Greptile / Kimi / Itô Markets / SerpApi 是 sponsors、(b) ai-employees = Agent Ops Club ($499 lifetime)、(c) yylo = GitHub Sponsors、**3 個 pick 都走 sponsor 模式但產品 + 商業 model 各自不同**：ECC 是 SaaS monthly subscription、ai-employees 是一次性 lifetime + masterclass、yylo 是 open source + sponsor。**對主人 horo-agent 對位**：「**主人若 ship HORO 個人 maintainer model、有 3 個 reference 模式可以挑：monthly SaaS (ECC)、lifetime + masterclass (ai-employees)、pure open source + sponsor (yylo)**」、**3 個模式各有 trade-off**：ECC monthly 有 recurring revenue 但需每月 ship、ai-employees lifetime 有 front-loaded revenue 但需持續 masterclass content、yylo pure OSS 有最大 reach 但 revenue 不穩。**對主人 HORO downstream 對位是「**主人若 ship HORO 為 enterprise product、應該挑 monthly SaaS model；若 ship 為社群 / developer 產品、應該挑 pure OSS + optional commercial extension**」**。