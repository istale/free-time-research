# GitHub 專案動態
- 檢查時間：2026-08-04
- 檢查對象：lyogavin/airllm、zhaoxuya520/reverse-skill、firecrawl/pdf-inspector、trycompai/crm、microsoft/skill-recorder
- 來源：Trending Daily Top 3（https://github.com/trending?since=daily，article-block parser 取前三）+ 自由探索 Web Search（GitHub Search API by created:2026-07-28..2026-08-04, stars>200, sort=stars desc；web-search 工具不可用，改以 GitHub Search API 驗證）

## Repo 摘要與 3W1H

### lyogavin/airllm
- **Repo 摘要：** `AirLLM` 是把超大語言模型拆成「layer-shard + sparse-MoE expert streaming」、讓單卡消費級 GPU 就能跑 70B 以上的 inference library。最新進度是 2026-07 的 Kimi K3 (2.8T) 支援：在單卡 RTX 6000 Ada 上量到 3.72 GB VRAM 用量；v3.0 (2026-06) 起也加入 FP8、Qwen3-235B、DeepSeek-V3、Llama 3.x/4、Phi-4 等主流模型。它是 PyPI + Jupyter Notebook 雙形態交付、適合單機研究環境，不是雲端高吞吐推理平台。
- **3W1H：**
  - **What：** Python + Jupyter 的 single-GPU 大模型 inference library，核心是 layer / expert 切片與 lazy streaming。
  - **Why：** 用「分層載入 + sparse MoE expert streaming」把 GPU 記憶體需求壓到 4–12 GB 級，繞過 quantization / distillation / pruning 對模型品質的副作用；2026-07 加 Kimi K3 (2.8T) 單卡示範是這條路線目前最大的可重現成果。
  - **Who：** 受限於單卡 / 單機 VRAM 卻想跑 70B+ LLM 的研究者、想在 macOS / 4GB GPU 環境試 MoE 模型的工程師、做 inference 成本比較的 baseline 用戶。
  - **How：** `pip install airllm` → `from airllm import AutoModel` → 用 `AutoModel.from_pretrained("<hf-repo-id>")` 與 `model.generate(...)`，layer shard 可用 `layer_shards_saving_path` 指定；K3 額外需要 `pip install compressed-tensors flash-attn`、CUDA 12 build 的 torch、`transformers` 4.56.x；macOS 路徑見 README 的 `## MacOS` 段落。
- **安裝方式：** **pip/uv/poetry：** **`pip install airllm`**（PyPI 套件名為 `airllm`）；K3 路徑另需 **`pip install compressed-tensors flash-attn`**。**從 source：** `git clone https://github.com/lyogavin/airllm.git` → `cd airllm` → `pip install -e .`。MacOS / CPU inference、模型分層 / shard 機制與 example notebooks 另見 README。**npm/pnpm/yarn/npx：** 未找到方式。
- **Repo metadata：** Description `AirLLM 70B inference with single 4GB GPU`；Stars **27,488**；Forks **2,993**；Language **Jupyter Notebook**（實際上是 Python 為主，GH 主語言標記為 Jupyter Notebook）；Topics `chinese-llm`、`chinese-nlp`、`finetune`、`generative-ai`、`instruct-gpt`、`instruction-set`、`llama`、`llm`、`lora`、`open-models`、`open-source`、`open-source-models`、`qlora`；License **Apache-2.0**；Created 2023-06-12；Updated **2026-08-04 06:01:09 UTC**；Pushed **2026-07-29 01:08:32 UTC**；Default branch `main`。
- **近期 release：** **2026-07-29 01:08:32 UTC**；`v3.1.0`；**AirLLM v3.1.0 — Kimi K3 (2.8T) on a single card**。這次 release 的 headline 是把 Kimi K3 (2.8T) 跑在單卡上、VRAM 量到 3.72 GB，並把 per-expert streaming 寫進文件；同一天的 push 即 release，沒有 prerelease / alpha 通道，release 與 main branch 是同一刻。

### zhaoxuya520/reverse-skill
- **Repo 摘要：** `reverse-skill` 是一個 reverse engineering / 授權滲透測試 / CTF 的 AI 技能路由包：當 AI agent 拿到 APK、二進位、前端 JS 加密、CTF 或 pentest 目標時，會依 `RULES.md` 與 `MASTER-ROUTING.md` 把任務分派到正確的 skill（jadx / apktool / Frida / IDA / BurpSuite / radare2 / Ghidra），並在 `timeline + Evidence→Finding→Path` 結構下產出可重複的工作流與 field-journal。它刻意設計成跨 Claude Code、Codex CLI、Cursor 等多家 AI client，今天 README 第一句話就指向 `README_AI.md`，等於把 repo 寫成「給 AI 看的入口」。屬於 Type C「必須 clone + 跨 host 安裝」的路線。
- **3W1H：**
  - **What：** PowerShell / Bash / Python / Java 多語言 skill pack + routing + toolchain bootstrap，主入口是 `skills/MASTER-ROUTING.md`。
  - **Why：** 同一個 AI agent 在不同 case（APK / ELF / JS / PCAP / CTF）下該用哪個工具常常是猜的，作者把判斷收斂成 `MASTER-ROUTING.md` ladder，並以 `skills/ops/` 形式把 scope gate、evidence chain、field-journal 寫死。
  - **Who：** 想把 Claude Code / Codex CLI / Cursor 接到 reverse-engineering / pentest / CTF 的安全研究人員與 AI 工具整合者。
  - **How：** `git clone https://github.com/zhaoxuya520/reverse-skill.git` → 跑 `bash skills/scripts/refresh-tool-index.sh`（或 Windows 的 `.ps1`、Kali 的 `kali/scripts/refresh-tool-index.sh`）重建本地 tool index → 進 `skills/MASTER-ROUTING.md` 看 fast ladder → 對應場景打開 `skills/<scenario>/SKILL.md`；scope gate 在 `skills/ops/`，evidence chain 與 field-journal 都已結構化。
- **安裝方式：** **pip/uv/poetry：** 未找到 PyPI 套件（PowerShell 為主、非 Python library）。**npm/pnpm/yarn/npx：** 未列。**From source（必要路徑）：** `git clone https://github.com/zhaoxuya520/reverse-skill.git`。**前置：** Java/JDK（jadx + apktool）、Node.js 22.12+（JS toolchain + MCP servers）、Python 3.x（Frida + 輔助腳本）、一個 AI coding client（Claude Code / Codex CLI / Cursor 等）。**Refresh tool index：** `bash skills/scripts/refresh-tool-index.sh`（Linux/macOS）/ `powershell -File skills/scripts/refresh-tool-index.ps1`（Windows）/ `bash kali/scripts/refresh-tool-index.sh`（Kali）。**Platform docs：** Kali → `kali/README-kali.md`、Ubuntu/Debian → `docs/platforms/linux.md`、macOS → `docs/platforms/macos.md`。
- **Repo metadata：** Description `Reverse Engineering / Authorized Penetration Testing / Security Research Skill Router Pack AI-powered routing + On-demand toolchain bootstrapping + Self-evolving knowledge base Supports Claude Code, Codex CLI, Cursor, ...`；Stars **16,344**；Forks **2,288**；Language **PowerShell**；Topics **空**（repo owner 未設 GitHub topic tags）；License **MIT**；Created 2026-05-13；Updated **2026-08-04 06:00:42 UTC**；Pushed **2026-08-04 02:46:28 UTC**（同日有 `fix(case-init)`、Merge PR #48 等 commit，顯示仍在迭代）；Default branch `main`。
- **近期 release：** **2026-07-17 16:52:03 UTC**；`v1.0.0`；**v1.0.0 — First formal release**。這是正式 stable release；但 `GET /tags` 顯示還有一個 **`v2.0.0` git tag（commit date 2026-05-17）**，沒有對應的 GitHub Release entry — 屬於「tag-as-marker, no release-notes」模式，與 07-31 / 08-01 觀察到的 tag-without-release 現象一致。採樣時 `pushed_at = 2026-08-04 02:46:28 UTC`，表示實際程式仍在更新，但沒有對外 release 公告。

### firecrawl/pdf-inspector
- **Repo 摘要：** `pdf-inspector` 是 Firecrawl 開源的 Rust PDF 解析 / 分類 / 抽取 library：先偵測 PDF 是 text-based / scanned / image-based / mixed，再做 position-aware text extraction 與 clean Markdown 轉換，沒有 OCR、沒有 ML 模型、單一 `lopdf` 依賴；同步釋出 PyPI / npm / crates.io / WebAssembly 四種 binding。它主打「200ms 內把 text-based PDF 變 Markdown」的 local default，並在 README 的 opendataloader-bench（200 PDFs）對照中以 0.875 overall / 0.470s/200-docs 領先。屬於 Type A 一行 `pip install` / `npm install` / `cargo add` 的 library。
- **3W1H：**
  - **What：** Rust 寫的 PDF 解析 library，提供 CLI（`pdf2md`、`detect-pdf`）、Python（PyO3）、Node.js（napi-rs）、Browser WebAssembly bindings。
  - **Why：** 給規模化 PDF pipeline 一個 local default：text-based PDF 直接走 fast extraction，只有真的需要 OCR 的頁面才送 OCR service，省下 latency 與成本；benchmark 上同時贏 reading-order / table / speed 三項，比較對象是 LiteParse、OpenDataLoader、PyMuPDF4LLM、MarkItDown。
  - **Who：** 做 PDF ingestion / scraping pipeline 的工程師（Firecrawl 自己就是第一個客戶）、要把研究論文 / 報告 / 財報 / 法律文件變成 clean Markdown 的開發者、需要�覽器端 PDF parsing 的前端工程師。
  - **How：** Python 路徑 `pip install maturin` → `maturin develop --release`，或 Node 路徑 `npm install @firecrawl/pdf-inspector`、Browser 路徑 `npm install @firecrawl/pdf-inspector-wasm`、Rust 路徑 `cargo add pdf-inspector`；呼叫 `process_pdf(path)` 取得 `pdf_type` + `markdown`，CLI 端可用 `pdf2md document.pdf` 或 `detect-pdf document.pdf --json`。
- **安裝方式：** **pip/uv/poetry：** 走 PyPI 包名 `pdf-inspector`（PyPI 0.2.6 / 2026-07-31），README 標示本地 build 用 `pip install maturin` + `maturin develop --release`。**npm/pnpm/yarn/npx：** **`npm install @firecrawl/pdf-inspector`**（npm registry 1.12.0 / 2026-08-03，registry 與 GitHub 都已對齊 `firecrawl/pdf-inspector`）；Browser WebAssembly 走 **`npm install @firecrawl/pdf-inspector-wasm`**。**Rust：** **`cargo add pdf-inspector`**。**Source build：** `git clone https://github.com/firecrawl/pdf-inspector.git` → `cargo run --bin pdf2md -- document.pdf` 或 `cargo run --bin detect-pdf -- document.pdf`。
- **Repo metadata：** Description `Fast Rust library for PDF inspection, classification, and text extraction. Intelligently detects scanned vs text-based PDFs to enable smart routing decisions.`；Stars **8,726**；Forks **573**；Language **Rust**；Topics `markdown`、`nodejs`、`ocr-routing`、`pdf`、`pdf-classification`、`pdf-extraction`、`pdf-parser`、`python`、`rust`、`text-extraction`；License **MIT**；Created 2026-02-06；Updated **2026-08-04 06:00:47 UTC**；Pushed **2026-08-04 03:08:39 UTC**（同日 `fix(markdown): preserve redline edits in prose (#240)`、`fix(layout): preserve contextual digit runs (#201)`、`fix(deps): bump lopdf to 0.42.0 for nesting-depth DoS (#198)`，都是當日 commit）；Default branch `main`；Homepage `https://firecrawl.github.io/pdf-inspector/`。
- **近期 release：** **未找到 GitHub release**（`GET /releases/latest` 與 `GET /releases?per_page=5` 均回傳空陣列 `[]`）。`GET /tags` 顯示存在 **`v0.7.0`（commit date 2026-04-14）**、**`v0.6.0`**、**`v0.5.0`**、**`v0.4.3`**、**`v0.4.2`** 等 tag，但都沒有對應的 GitHub Release entry；屬於 07-31 / 08-01 的「tag-as-marker, no release-notes」模式 + PyPI-only 雙軌（PyPI 0.2.6 / 2026-07-31，npm 1.12.0 / 2026-08-03）。採樣時仍有同日 push，實際開發節奏並未停。

### trycompai/crm
- **Repo 摘要：** `trycompai/crm`（repo 標題為 `Comp AI / CRM`，README 標籤 `Built with eve`）是一個 open-source、agentic-first 的 CRM：NestJS + Prisma + Postgres 後端、Next.js + shadcn/ui 前端、Bun + Turborepo monorepo，核心是 Vercel 的 `eve` durable agent — 18 個 authored tools + 4 個 markdown skills + 1 個 schedule，自己跑在自有部署、自有排程、自有 work queue 上，自己決定下一步看誰、要不要追 research、何時停。它把「資料庫只是 agent 寫筆記的地方」當成設計主軸，並用 `deny-all` egress sandbox 加上不給沙箱 `DATABASE_URL` 兩條規則把外洩風險收緊。屬於 Type B `git clone + bun install + docker compose` 的 monorepo。
- **3W1H：**
  - **What：** TypeScript monorepo 的 self-hosted CRM，agent 是產品、DB 是 agent 的筆記本。
  - **Why：** 對抗「CRM = 資料庫 + 表單」、「AI CRM = 表單旁邊加 chat box」兩種主流；README 把「Intelligence never lives in the API」、「UI only from packages/ui」、「no organizations 三條規則」與 sandbox 設計明寫出來，agent 用 evidence ledger 而非 confidence score，弱證據才升成人類要 review 的 suggestion。
  - **Who：** 想用 AI agent 接手 research-heavy 銷售流程的小團隊、要 self-host CRM 又不想把資料交給 SaaS 的組織、要研究 durable agent / Vercel eve 框架的工程師。
  - **How：** 先裝 Bun + Docker，`git clone https://github.com/trycompai/crm.git && cd crm`，`cp .env.example .env` 後填 `BETTER_AUTH_SECRET`、`ALLOWED_SIGN_IN`、Google OAuth client（兩者皆設或皆不設），`bun install` → `docker compose up -d`（Postgres :5432）→ `bun run db:deploy` → 可選 `bun run db:seed` → `bun run dev`；前 3000、後 3001，啟動時 agent 會印出「哪些外部 key 有 / 沒有」。
- **安裝方式：** **npm/pnpm/yarn/npx：** 套件本身是 private workspace（`package.json` 標 `private: true`），沒有 npm registry；安裝走 **`bun install`**（root）→ `docker compose up -d` → `bun run db:deploy` → `bun run dev`。**From source：** `git clone https://github.com/trycompai/crm.git && cd crm`。**Engines：** Node ≥ 22。**Deploy：** Vercel（`AGENT_BRIDGE_SECRET` 要在前後兩端設成同值才會打開 contact 的 Agent tab）。**pip/uv/poetry：** 未找到方式。
- **Repo metadata：** Description `An open-source, agentic-first CRM.`；Stars **3,544**；Forks **384**；Language **TypeScript**；Topics **空**（repo owner 未設 GitHub topic tags，雖然 README 滿版都是 agent / CRM / evidence-ledger 關鍵字）；License **MIT**（`LICENSE` 檔存在，Copyright (c) 2026 Comp AI）；Created **2026-07-31**；Updated **2026-08-04 06:00:27 UTC**；Pushed **2026-08-03 22:58:57 UTC**（同日 3 個 `feat(api)` 與一個 `Merge PR #14` commit，agent / API 仍在密集迭代）；Default branch `main`。
- **近期 release：** **未找到 GitHub release**（`/releases/latest` 與 `/releases?per_page=5` 均回傳空陣列，`/tags` 也回傳空陣列）。這個 repo 是 2026-07-31 創立的新 repo，目前仍走 commit-driven 迭代，沒有 tag / release / changelog 公告；對這份報告來說採樣時的 push 就是「最新動態」，README 的 `## The agent` 段落是事實上的「release notes」。

### microsoft/skill-recorder
- **Repo 摘要：** `microsoft/skill-recorder` 是 Microsoft 開源的桌面 app：錄下你做一件任務的螢幕 / 視窗切換 / 點擊 /（可選）語音，送到 GitHub Copilot CLI 把它還原成「intent + ordered steps」，再把單次 run 變成 Agent 可重複用的 **Skill** 或排程觸發的 **Automation**。安裝方式是「從最新 release 的命令複製貼上，build 該 release commit 的 source，加進 `~/Applications`」，不是註冊 npm 或 pip 套件。它設計上偏好 agent 自己的 native tools（`gh` CLI / `web_fetch`）而不是 replay UI clicks，所以「教一次、推廣到所有同型表單」會自然 generalize。屬於 Type 4「`curl | bash` install + pinned commit + 本機 app」。
- **3W1H：**
  - **What：** Electron + TypeScript 的 desktop app，macOS 為主、Windows 11（x64 / ARM64）為輔，搭配 GitHub Copilot CLI 把錄影轉成 SKILL.md / 排程。
  - **Why：** 把「人做一次」變成「agent 重複做」，且不靠錄影 replay，而是要 agent 用 native tools 重新做；對「同類任務很多」的場景（例如填表單、跑固定 pipeline）特別划算，README 也提醒「不要錄到密碼 / token / API key」。
  - **Who：** 想把日常 desktop 工作轉成 reusable Copilot skill / automation 的開發者與 power user、Copilot / Scout / Cowork 的早期採用者。
  - **How：** 先確認 GitHub 帳號有 Copilot 存取權（Copilot CLI 會跟著 app 一起裝），從最新 release 頁複製對應平台命令：macOS / Ubuntu 走 `commit="<40-char-sha>"; curl -fsSL "https://raw.githubusercontent.com/microsoft/skill-recorder/$commit/install.sh" | SKILL_RECORDER_COMMIT="$commit" bash`（要常駐就再加 `SKILL_RECORDER_DETACHED=1`），Windows PowerShell 走 `$commit="<40-char-sha>"; $env:SKILL_RECORDER_COMMIT=$commit; irm ".../install.ps1" | iex`；首次啟動授權 Screen Recording 即可 `⌘⇧R` / `Ctrl+Shift+R` 開錄。
- **安裝方式：** **npm/pnpm/yarn/npx：** 未列（不是 npm 套件，是 desktop app）。**pip/uv/poetry：** 未找到方式。**Source release（必要路徑）：** 從 https://github.com/microsoft/skill-recorder/releases/latest 複製對應平台命令，pinned 到 40-char release commit。macOS / Ubuntu：`commit="<40-character-release-commit>"; curl -fsSL "https://raw.githubusercontent.com/microsoft/skill-recorder/$commit/install.sh" | SKILL_RECORDER_COMMIT="$commit" bash`（要常駐加 `SKILL_RECORDER_DETACHED=1`）。Windows PowerShell：`$commit="<40-character-release-commit>"; $env:SKILL_RECORDER_COMMIT=$commit; irm "https://raw.githubusercontent.com/microsoft/skill-recorder/$commit/install.ps1" | iex`。安裝位置：macOS 加到 `~/Applications` 的 `Skill Recorder (Source)` app；Ubuntu 加對應 application entry；Windows 加桌面 + Start Menu 捷徑。
- **Repo metadata：** Description `Desktop app that records your on-screen work session and uses the GitHub Copilot CLI to reconstruct it as an intent + ordered steps, then builds a reusable Skill or Automation for Microsoft Scout, Microsoft Copilot Cowork, GitHub Copilot CLI, or Microsoft Copilot Studio.`；Stars **1,510**；Forks **160**；Language **TypeScript**；Topics `agent-skills`、`ai-agents`、`automation`、`copilot`、`copilot-cli`、`copilot-cowork`、`copilot-studio`、`electron`、`microsoft-scout`、`screen-recording`；License **MIT**；Created 2026-07-29；Updated **2026-08-04 05:57:02 UTC**；Pushed **2026-08-04 05:35:14 UTC**（同日仍有 commit，開發持續）；Default branch `main`。
- **近期 release：** **2026-07-30 16:34:33 UTC**；`v0.3.1`；**Skill Recorder v0.3.1**。`GET /tags?per_page=5` 顯示 `v0.3.1`、`v0.3.0`、`v0.2.1`、`v0.2.0`、`v0.1.0` 都已是正式 GitHub Release，release 與 tag 對齊良好；採樣當日仍在 push 但沒有更新版本號。

## 重點觀察
- **Trending Top 3 對應到三種完全不同的「新進榜」訊號。** `lyogavin/airllm` 雖是 27,488★ 的成熟 repo（2023-06 創立），今天再次進榜是 v3.1.0（2026-07-29）把 Kimi K3 (2.8T) 跑在單卡上的 headline；`zhaoxuya520/reverse-skill`（08-01 已報過一次，3 天內第二次進榜）的 `v2.0.0` tag（commit date 2026-05-17）尚未開 release，採樣時同日 push；`firecrawl/pdf-inspector`（全新）今天同日有 3 個 commit 收 PDF 安全與 layout 修正。**「Trending 名次 ≠ release 新鮮度」這條規則今天再次成立**，並把 08-01 / 08-03 觀察到的「同一個 repo 在 4 天內反覆進榜」延續下來 — 今日 Trending Top 3 與最近 4 天的 2 份 `project-activity.md` 重疊了 2 個，是 prompt body-wins 而非 dedup 的結果。
- **Web-search 兩個 repo 都是「30 天內建立 + 已有完整部署文檔」的工程型專案，恰好把今天 5 個 repo 的 release 形態再演一遍。** `trycompai/crm`（2026-07-31 創立，無 tag 無 release，commit-driven）與 `microsoft/skill-recorder`（2026-07-29 創立，`v0.3.1` / 2026-07-30 release + 持續 tag 推進）是 release-less 與 release-healthy 的兩端；對比 Trending Top 3 中 `firecrawl/pdf-inspector` 的 tag-without-release、`zhaoxuya520/reverse-skill` 的 tag-without-release + 隔 1 個 commit 才出 release、`lyogavin/airllm` 的「release 即同日 push」。今天 5 個 repo 把 release 文化的全光譜（健康 release / tag-marker / release-less）一次走完 — 在沒有任何 release-less 純教學 repo 的情況下，這點比 08-02 / 08-03 更純粹。
- **安裝生態四型，今天全部都到齊。** Type 1 `pip install airllm` + Type 1 `pip install maturin` / `npm install @firecrawl/pdf-inspector` / `cargo add pdf-inspector`（同一 library 跨 4 個 package registry）是 library 套件化的極端正例；Type 2 `git clone + bun install + docker compose`（trycompai/crm）；Type 3 `git clone + bash skills/scripts/refresh-tool-index.sh`（zhaoxuya520/reverse-skill，PowerShell/腳本驅動，無套件）；Type 4 `curl | bash` pinned commit + Electron desktop app（microsoft/skill-recorder）。**沒有一個 repo 把「npm install 一行」當作最終使用者入口**，反而都把「要嘛 build / 要嘛 docker / 要嘛 desktop」的安裝複雜度擺到 README 第一段 — 反映 2026 年的開源正在把安裝從「套件管理員」移向「agent 怎麼用」。
- **跨語言與跨執行期的設計哲學。** `firecrawl/pdf-inspector` 把同一個 Rust library 編給 4 種 runtime（Python / Node / Browser WASM / Rust crate），是一行套件化最乾淨的版本；`lyogavin/airllm` 跨 Jupyter / Python / PyPI / Hugging Face 模型 ID 4 種入口，把「4GB GPU 跑 70B」當成單一賣點；`trycompai/crm` 走 TypeScript 全端 + Bun + Postgres + Vercel，把 AI agent 跟一般 web app 綁在同一個 monorepo；`microsoft/skill-recorder` 把 Electron desktop + Copilot CLI + pinned commit 串起來；`zhaoxuya520/reverse-skill` 把 PowerShell + Bash + Python + Java + 多 AI client 全部揉在同一個 skill pack。**跨執行期的設計在 2026 年幾乎是新規範，pure-language 的單執行期 repo 在今天的清單上缺席**。
- **「今天有兩條 release 軸可以觀察」。** 一條是 K3 軸：`lyogavin/airllm`（K3 2.8T 單卡 inference）、昨天的 `sqliteai/waste`（K3 NVMe streaming 從 RAM）、`FareedKhan-dev/kimi-k3-in-c`（K3 純 C inference 8.24 GB RAM） — 三種不同硬體路徑都在把 K3 拉進單機；另一條是「agent 化既有 app」軸：`trycompai/crm`（CRM）、`microsoft/skill-recorder`（desktop → skill）、昨天的 `usekaneo/kaneo`（PM）— 把傳統業務流程接上 durable agent / Copilot / agent skill marketplace。**這兩條軸不是同一件事，但都是「release-driven by AI 應用」**，可以視為今天自由探索的核心訊號。
