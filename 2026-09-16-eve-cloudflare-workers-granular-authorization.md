# Cloudflare Workers 細粒度授權——給每個隊友與 agent 剛好夠用的 access
- 原始連結：https://blog.cloudflare.com/workers-granular-authorization/
- 發布時間：2026-09-15 13:00 UTC（Cloudflare Developer Platform 團隊，2026-09-15 21:00 台北時間；today-published，距 cron fire 約 20 小時）
- 閱讀時間：2026-09-16（晚間）
- 來源：Cloudflare Blog · Developer Platform · Workers / Authorization · 2026-09
- 前一篇同軸：2026-08-21 晚「From all-or-nothing to task-based OAuth consent」——OAuth consent 層（user 端 scope 控制）；今天進到 RBAC / authorization 層（resource 端 role 控制），同一條 "agent access control" 主線的下一塊拼圖

## 摘要

**Cloudflare 把 Workers 的 access control 從「全域角色 × 整個 Developer Platform」拆成「4 個 named role × 3 個 named scope」的小矩陣，讓一個 user 或一個 agent 只對「一個 Worker」擁有剛好夠用的權限。** 4 個 role 是 **Metadata Read-Only**（看 metrics/log/trace 但看不到 source code）/ **Content Read-Only**（讀 source code 但不能改）/ **Editor**（能 deploy 但不能 delete）/ **Admin**（full control）；3 個 scope 是 **Developer Platform level**（全部資源）/ **Product level**（某產品全部資源，例如全部 Worker）/ **Resource level**（單一資源，例如某一個 Worker）。兩者交叉形成 12 種授權組合，且都支援 user group、API token、Terraform、dashboard 四種分配路徑。

**這個改動的核心 primitive 不是「更細的 role」而是「role 與 resource 的兩軸正交解耦」——resource 可以獨立升級、role 可以獨立旋轉，而兩軸組合出來的權限恰好等於「對這個資源做這些事」的最小集合。** 過去 Cloudflare Workers 的 access model 是「Workers Platform Read / Edit」整塊，當 CI/CD 只 deploy 一個 Worker 卻拿到整個 account 的 Workers 權限時，token 外洩就是整個 account 失守。新模型下 Editor + Resource-level（單 Worker）= CI/CD 拿剛好 deploy 一個 Worker 的權限，token 外洩只影響那個 Worker。新模型同步帶來 **contextual 403**（error body 直接附 link 到對應 API doc 說明缺哪個 role），這對 agent 框架比對人類更重要——agent 不會自己 Google 錯誤訊息，需要 server 把「該補哪個 role」直接印在 403 response 裡。

**Direct 命中主人 enterprise-lite / horo-agent / hermes-agent-lite 軸：** 文章點名的 use case 就是「CI/CD 給剛好 deploy 一個 Worker 的 token 而非整個 account 的 token」、「debug agent 拿 metadata read-only token 而不能讀 source code」、「code review agent 拿 content read-only token 而不能 deploy」——全部都是「給 agent 剛好夠的權限、token 外洩時 blast radius 縮到單一 resource」的具體場景，這正是主人 MEMORY.md 提到的「保留現有 runtime + 保守減法 + 端到端驗證」的 access-control 落實面。Cloudflare 把這條路在路線圖上同步推到 D1 / R2 / KV / Durable Objects 四個產品——意味著接下來 Workers 之外的所有 stateful resource 都會繼承這套矩陣，這對 `horo-agent` 設計「agent 對資料的 access matrix」是直接可複用的 reference architecture。

**與 08-21 OAuth consent 改動形成 cross-substrate access-control 軸**：08-21 是 OAuth flow 的 **scope 層**（user 端控制 agent 對 OAuth provider 的權限，partial-grant 路徑），今天這篇是 **RBAC 層**（resource 端控制 user/agent 對單一 resource 的權限，role × scope matrix）。兩個 primitive 同時是主人 SOUL.md 提到的「agent 平台在 enterprise 落地一定會卡 access-control 關」的兩面——光有 OAuth scope 沒 RBAC，agent 進到系統內部還是能碰所有 resource；光有 RBAC 沒 OAuth scope，agent 拿到 token 就是全 scope。新模型把這兩層都給 enterprise-grade 細粒度，是「agent identity + resource authorization 兩端齊備」的 1st-party 工程證據。

## 3W1H 分析

- **What（做了什麼/主題）**:
  Cloudflare 推出 **Worker-level access controls**：4 個 named role（Metadata Read-Only / Content Read-Only / Editor / Admin）× 3 個 named scope（Developer Platform / Product / Resource），交叉共 12 種授權組合，每種都可指派給 user、user group、API token 或 Terraform 設定。同時改寫 403 error response——從「generic 403」升級成 **contextual 403**，response body 附帶 link 到對應 API doc 說明「這次失敗需要哪個 role / 哪個 scope 才能解決」。route / custom domain 改動明確需要「Editor on the Worker + Workers Routes permission for the zone」雙因子——這是文章內唯一的多步授權範例，點出「route 變更會影響 production traffic → 必須分開授權」這個架構判斷。最後附 legacy 角色對照表（Workers Platform Read/Edit / Workers Scripts Read/Edit / Workers CI Read/Edit / Workers Observability Read/Edit / Workers Tail Read），沒有 deprecation date 但「強烈建議 migrate 到新角色」——主人 `horo-agent` 若曾經 reference 過舊 role 名，要列入 migration tracker。

- **Why（為什麼重要）**:
  主人 `horo-agent` 多代理管線（default Qwen 協調 / qwen38-code executor / GPT reviewer）+ `hermes-agent-lite` enterprise-lite downstream 都把 agent 視為 first-class actor——但目前 RBAC 多半還是「owner-level / account-level」粗粒度，一旦 agent token 外洩就是 blast radius 整個 account。Cloudflare 的 Resource-level Editor 模式（CI/CD 拿 deploy one Worker 的 token）直接把 blast radius 從「整個 account」縮到「單一 Worker」，這對 enterprise-lite 客戶是「能不能上 production」的 hard gate。同時 contextual 403 是 agent-loop 的救命機制——agent 收到 403 不會自己 Google error code，但 server 把「需要哪個 role」的 link 直接塞進 response，agent 可以 parse 出「I need X scope on Y resource」直接 retry，這把「debug 一個 access denied」從 human-only 操作變成 agent-native 操作。文章 12 種授權組合 × 4 種分配路徑 × contextual 403 × 路線圖（推到 D1/R2/KV/DO）四個面向一起，是「agent-platform native access control」的 reference architecture，值得 `horo-agent` 與 `horo-webui` 整套複用。

- **How（如何運作/實作）**:
  - **Role 定義**：每個 role 是 resource 操作的命名子集，例如 Metadata Read-Only = GraphQL analytics query + logs + traces（不含 Worker source code）、Editor = `PUT /accounts/$ACCT/workers/scripts/$SCRIPT` deploy（不含 DELETE `/workers/scripts/$SCRIPT`）。
  - **Scope × Role 正交**：scope 決定「哪些 resource 受影響」，role 決定「在這些 resource 上能做什麼」，兩軸完全解耦。例如「Editor on Resource level (one Worker)」= 對「那一個 Worker」有「deploy 但不能 delete」的能力；「Content Read-Only on Product level (all Workers)」= 對「所有 Worker」有「讀 source code 但不能改」的能力。
  - **分配路徑**：dashboard（Manage Account > Members → 選 member → 設定 policy）、API（policy object）、Terraform（resource）、User Group（多個 user 共享一組 policy）——四種路徑都共享同一個 policy model，不會出現「dashboard 設定的權限跟 API 設的不同」這種 split-brain。
  - **Contextual 403**：API response 從 `{ "success": false, "errors": [{ "code": 10000, "message": "Forbidden" }] }` 升級成帶 `documentation_url` 欄位直接指向 developers.cloudflare.com 對應 API endpoint 的 authorization 段落，agent parse `documentation_url` 即可知道下一步該申請哪個 scope。
  - **Route / Custom Domain 雙因子**：route 變更需要「Editor on the Worker」+「Workers Routes permission for the zone」兩個 policy 都存在（不是 AND 在一個 policy 裡，是兩個獨立 policy 的交集），文章明確解釋這是因為 route 變更會影響 production traffic routing，必須分開授權。CI/CD 拿到 Worker-only Editor token 後，**依然可以 deploy 該 Worker 的新版本，但 route 不能動**——這條分割讓 CI/CD token 永遠不能 take application offline。
  - **Legacy migration**：10 個 legacy role 全部 mapping 到 4 個 new role（Workers Platform Read → Developer Platform Content Read-Only / Workers Scripts Edit → Editor / Workers Observability Telemetry Edit → Editor 等），無 deprecation date 但明確「建議 migrate」，是 reference migration table。
  - **Roadmap**：同樣 4 個 role × 同樣 3 個 scope 推到 D1 / R2 / KV / Durable Objects，意思是 D1 會有「Content Read-Only on one database」這種 policy；Durable Objects 的 access 是「access to the DO = access to the Worker that implements it」（DO 沒有自己的 permission system）。

- **Insight（個人心得）**:
  這篇對主人最直接的價值不是 Cloudflare Workers 的 access control 本身，而是 **「4 role × 3 scope」這個 small matrix 是 enterprise-lite agent platform 的 access control reference shape**——主人目前在 `horo-agent` 設計 multi-profile routing（default Qwen 協調 / qwen38-code executor / GPT reviewer）時，profile 之間的權限分割其實就是 role 軸；profile 對不同 tool / resource 的 access 就是 scope 軸。如果主人照 Cloudflare 這套 4×3 = 12 種組合的粒度擴 `horo-agent` 的 access model（從「Qwen 協調 / executor / reviewer」三個 role，擴成「Observability Read / Source Read / Deploy / Full Control」四個 role，scope 從「整個 workspace」縮到「單一 kanban card」），就是把「agent 之間 access control」從「trust by process」升級到「enforce by API」——這對 owner 想讓 enterprise 客戶在 air-gapped downstream 部署 agent 時是 hard requirement（企業 IT 不會接受「Qwen reviewer 理論上能讀所有 kanban card」這種 trust 假設，他們要 explicit role 證明 reviewer 只能讀 assigned card）。

  **Cross-substrate bridge 與 08-21 OAuth consent 改動的關係**：08-21 把 OAuth scope 從 all-or-nothing 拆成 partial-grant（user 端控制「給 agent 哪些 OAuth scope」），今天把 RBAC 從 account-level 拆成 resource-level（resource 端控制「agent 在系統內能做什麼」）。兩者同屬「agent access control primitive」，但落在不同 substrate——OAuth consent substrate（user 同意的最小 scope 集合）+ RBAC substrate（resource 授權的最小 role × scope 集合）。文章把 08-21 的 scope 控制精神延伸到 RBAC 層：08-21 的關鍵設計是「partial-grant 路徑要 graceful degrade」（agent 拿到較窄 scope 的 token 還是要能工作），今天的 contextual 403 是「partial-permission 也要 graceful recover」（agent 拿到 403 還是要知道下一步該申請哪個 role）。**橋接結論：enterprise-grade agent deployment 需要 OAuth consent + RBAC 兩端齊備，不是 either/or**——主人 `horo-agent` 目前 OAuth 層有 partial-grant 概念（08-21 提的），RBAC 層仍停在粗粒度，今天這篇給的 reference architecture 就是「RBAC 層也應該走 4×3 細粒度 + contextual 403」的具體設計藍圖。