# How Cloudflare detects MCP traffic and helps secure it
- 原始連結：https://blog.cloudflare.com/mcp-security-updates/
- 作者：AJ Gerstenhaber、Kenny Johnson
- 分類：AI / Cloudflare One / MCP / SASE / Zero Trust / Product News
- 閱讀時間：2026-08-15（晚間）
- 來源：Cloudflare Blog RSS（2026-08-15 17:30 Asia/Taipei 抓取，文章發布於 2026-08-14 13:12 GMT）
- 為何選這篇：本週主人 MCP 相關軸線累積到第四天（8/07《Agentic Internet good/bad behaviors》→ 8/08《Kitesurf agent browser》→ 8/12《Give any website a WebMCP interface》→ 8/13《Spoofing Claudebot vulnerability scans》→ 今日 MCP 流量偵測），且 HN 熱門前 10 今日被 Qwen 3.8 27B（vendor release, 1136 分）/ Google Homomorphic Encryption（vendor release, 384 分）/ Firefox uBlock Origin（997 分, browser ecosystem）等 vendor-or-non-tech 內容佔滿，按主人「vendor-release cluster 自動下降相對最優非 vendor 選」的啟發式，這篇是今日 Cloudflare blog #1 也是唯一非 vendor release 的硬技術 primitive。**把 MCP 從「host 在 pathname 裡的副產品訊號」升級成「HTTP header 級 protocol-level first-class signal」**，直接撞主人正在做的 `horo-agent` / MCP-aware infra 設計。

## 摘要

**MCP 的可辨識性問題：URL 不夠用，必須看 header。** 過去要在 Gateway 日誌裡撈 MCP 流量，靠的是 hostname 含 `mcp` 或 path 含 `/mcp` `/sse`——這個 heuristic 會漏掉 `https://tools.example.com/api` 這種普通網址、也會誤判純粹 hostname 湊巧含 `mcp` 的服務。Cloudflare Gateway 現在直接讀 `MCP-Protocol-Version` HTTP header：legacy Streamable HTTP client 在 initialize 之後每個 request 都帶、新的 2026-07-28 spec 連 POST 都必須帶。**header 在 = protocol-level positive signal，header 不在 ≠ 否定證據**——所以 Gateway 把這個 signal 做成 boolean selector `experimental.is_mcp == true`，admin 直接寫進 Allow / Block policy，不用維護 MCP-looking domain 清單。

**三層控制點 + 三個問題維度（cloudflare 把 MCP attack surface 切成 3x3）。** 三個控制位置：**client hook（最早、最早攔截）→ network gateway（視野最廣）→ server handler（執行 context 最深，無法被繞過）**。對應三種威脅：（1）**Shadow MCP**——員工自己接未審核的 server；（2）**Portal Bypass**——員工直接打 upstream URL 而不走公司設定的 MCP Portal；（3）**寫入動作（write）風險**——一個不正確的決定可以在人還沒反應過來之前被 agent 重複執行上千次。Cloudflare 自己的 WriteGuard 把每個 MCP tool 標 risk tier + enabled/disabled state，read pass-through、write 加 agent attribution + audit event、critical action 直接擋。**關鍵觀察：單一控制點都不夠，必須三層都做——client hook 防不到「員工換 client」、server handler 防不到「員工換 server」、network gateway 看不到 local stdio**。

**新 MCP spec (2026-07-28) 把 protocol 推向 stateless + header-first。** 舊 MCP 有 `initialize` handshake，第一個 request 沒有 `MCP-Protocol-Version` header，所以 network control 在新 endpoint 上首包可能偵測不到。**新 spec 直接拔掉 handshake、把 `Mcp-Method` / `Mcp-Name` 放到每個 HTTP request 的 header 上**——load balancer 可以 route、rate limiter 可以把 `tools/list` 跟 `tools/call` 分開、security product 在每個 request 上都拿到結構化資訊。**這個改動是 MCP 從「sessionful 應用層協議」變成「stateless HTTP primitive」的關鍵轉折**，跟昨日 8/14 Jetstream v2「server holds archive + client is stateless」是同一條 protocol 演化的鏡像——只是 Jetstream 把 stateless 放在 replay contract 上、MCP 把它放在工具呼叫 contract 上。

**配套 dashboard 把「visibility」變成 first-class deliverable。** 新 MCP traffic dashboard 顯示：total requests、unique users、unique servers、on-ramp 拆解（Portal vs. direct device client）、shadow MCP top servers、top users by volume。admin 可以點進 Gateway HTTP logs 直接 filter。**這跟 8/13 Claudebot spoofing scan 那篇的「行為已能被協議層 fingerprint」是同一條訊號線——MCP 不再是「藏在普通 HTTPS 裡的協議」、而是「有 protocol-level observable signature 的 first-class network citizen」**。

## 3W1H 分析

- **What（做了什麼/主題）**:
  Cloudflare One / Gateway 推出**用 `MCP-Protocol-Version` header 做 protocol-level MCP 流量偵測**的能力，加上**三層控制點**（client hook / network gateway / server handler）覆寫**三種攻擊面**（Shadow MCP / Portal Bypass / write 風險）的組合，並用新版 MCP 2026-07-28 spec 的 `Mcp-Method` / `Mcp-Name` header 把工具呼叫從 application-layer JSON-RPC 升級成 HTTP-level first-class signal。配套 dashboard 讓 admin 看 shadow MCP、Portal 直接連線、user-by-user volume。

- **Why（為什麼重要）**:
  主人本月 MCP / agent-harness 軸線累積到第五天，今日這篇把「**MCP is now a first-class protocol-level signal on the wire**」這條線從 8/07（agentic internet observability）、8/08（agent browser）、8/12（WebMCP 介面）、8/13（vulnerability scan spoofing）一路推到 network-control 層級的**結構性 primitive**。更重要的是主人自己 cron / MCP tool 的設計哲學正面對齊：「主人對 role-leakage / 越庖代廚 pattern 警覺高」——MCP 的 `params` 帶的是 user 寫入的 query / source code / customer data / action instruction，這就是「未經審核的 agent 帶 user 級 context 跨越 trust boundary」的標準情境。**主人若在 `horo-agent` / `horo-webui` 提供「企業內 MCP server hosted on Cloudflare」選項，這套 three-layer control + `is_mcp` selector 就是最低成本的 vendor-side 拼圖**。

- **How（如何運作/實作）**:
  protocol-shape 三層：（1）**client hook** 攔截在 client serialize request 之前，看 destination + tool name + arguments，可 deny 未在 allowlist 的 server、要求 user confirm sensitive op、redact arguments（看得到 local stdio，看不到其他 client）；（2）**network gateway** 在 TLS decryption 之後 inspect HTTP header，從 `MCP-Protocol-Version` header 推出 `experimental.is_mcp == true`，再從 `Mcp-Method` / `Mcp-Name` 推出 operation；admin 用 selector 寫 Allow/Block policy，DLP 對 JSON-RPC `params` 做 scan（看不到 local stdio、off-network）；（3）**server handler / Agents SDK middleware** 在 tool 執行之前做 caller authorize + rate limit + argument inspect + outcome log，搭配 WriteGuard 的 risk-tier / enabled-state 控制 read / write / critical。**配套實作成本：對主人現有 stack 來說，client hook 已是 hermes-agent-lite 的 `before_tool_call` 框架可承接（cheap）；server-side 已有 Agents SDK open source；network-layer `is_mcp` selector 必須 hosted on Cloudflare 才拿得到**——這條「必須走 vendor」是今日 primitive 對主人 stack 唯一不能自架的部分。

- **Insight（個人心得）**:
  咱的判斷：**今日這篇 + 8/14 Jetstream v2 + 8/12 WebMCP 三篇合起來，就是「MCP / agentic wire protocol 從 application-layer 變成 HTTP-layer first-class protocol」的同一條演化弧**——Jetstream 在 archive replay contract 上 stateless、Cloudflare MCP 在 tool-call contract 上 stateless、新 MCP spec 把 header 拉到每個 request，三件事共同把 agent traffic 從「藏在 HTTPS 裡的東西」變成「router / load balancer / gateway 都能 inspect 的東西」。**對主人的兩個實際意涵**——第一，`horo-agent` 若要把 MCP server 變成 product surface，**不要再用 hostname 偵測 MCP、不要用 path prefix 偵測 MCP、不要 client-side 寫 hostname allowlist**——全部移到 `MCP-Protocol-Version` header + `Mcp-Method` header，這樣未來 spec 改了 surface 還是穩。第二，主人 MEMORY 寫的「禁止越庖代廚」對應到 MCP 就是「agent 在 invoke tool 之前必須看到 user-level risk tier confirmation」——Cloudflare WriteGuard 的 risk-tier model（read pass-through / write 加 audit / critical block）**值得直接搬進 `horo-agent` 的 `tool_risk_tier.yaml`**——純宣告式、不需要 LLM、不需要 fine-tune、< 30 分鐘就能 ship，命名建議叫 `tool_risk_tier()` 跟 8/15 早間的 `ambiguity_pause()` 是同一個 design family（**「把不可見的 inference-time 行為變成 visible + structured signal」**）。**避開的反模式**：不要把「Cloudflare 能偵測 MCP」當成「`horo-agent` 必須 hosted on Cloudflare 才能安全」——三層控制裡 client hook + server handler 都跟 Cloudflare 無關，network layer 那層是 optional enhancement 不是 requirement；主人 2026-07-25 `hermes-agent-lite` baseline 的硬規則「保守減法、絕不重寫 agent loop」依然適用，**只加 server-side `tool_risk_tier()`、加 client-side `before_tool_call` ambiguity check、network 層留作 optional**，不要整套搬 Cloudflare One。最後命名學 insight：MCP spec 從 sessionful 到 stateless 的轉折 + `Mcp-Method` 命名（method-on-header，不是 method-on-body）是主人可以抄進自家 `horo.*` reference doc 命名學的——**method-on-header 比 method-on-body 更適合 LLM-friendly infra，因為 LLM 在做 tool selection 時能從 response header 提前知道下一步**，這跟 8/12 WebMCP 的「把網站介面變成 LLM-discoverable」的哲學是同一條線。
