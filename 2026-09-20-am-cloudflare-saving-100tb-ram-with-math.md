# Saving another 100TB of RAM with math (and Rust)
- 原始連結: https://blog.cloudflare.com/saving-100-tb-of-ram-with-math/
- HN 討論: https://news.ycombinator.com/item?id=49758580
- 閱讀時間: 2026-09-20（早間）
- 來源: Hacker News 熱門前 20 第 15 名（460 分 / 108 則留言，讀取時 2026-09-20 07:01 Asia/Taipei）
- 作者: Kevin Guthrie / Mariia Iurchenko / Zaidoon Abd Al Hadi / Ivan Babrou（Cloudflare Performance team）

## 摘要

**這一篇是 Cloudflare 9/18 釋出的「math + Rust 把 Pingora Backend Router 的 memory footprint 砍 100 TB」substrate-aware engineering case study——跟前幾天 9/17 qorl（4B Qwen3.5 蒸餾 81% faster query plans than Postgres）+ 9/19 Needle 3（8-29 MB on-device laddered networks match DeepSeek V4 Flash）共同構成主人最近愛的「在 fixed hardware budget 上用算法/結構把工作負載重新 fit 進去」的同型軸線第三個 instance。** 切入點是一張 ticket：「pingora-ketama 記憶體用量超標」。pingora-ketama 是 Cloudflare 開源的 consistent-hashing library，負責把 cacheable requests 依 URL 路由到「環」上最近的 server，目標是 data center 內每份檔案只存一份、上下線時 migration cost 最小。PBR（Pingora Backend Router）是所有請求入口都會跑 hot-path 的服務，所以它的每一 byte RAM 都會被全球 fleet 放大成顯著的 footprint。

**核心機制：把 ring 上的 server 用「math 重新參數化」而不是「硬塞更多 entry」** 文章分三段把記憶體吃乾淨：(1) **Ring 上的「假 server」重複問題**——consistent hashing 的經典技巧是每個實體 server 在 ring 上放 N 個 virtual nodes（所謂 ketama），但 PBR 過去放 N 太多（每個實體 server 200 個 virtual node），N × world-wide server count 讓 lookup table 大到占掉 100+ TB RAM。(2) **數學式 re-parametrization**——Cloudflare 工程師沒有無腦縮減 N，而是觀察到：「同一個實體 server 在 ring 上的 N 個 virtual node 等距分佈（spacing = 2^32 / N），而每個 URL hash 取最近的 virtual node」這個演算法可以**完全等價 rewrite 成「對每個 URL hash 直接 mod ring_size 然後查表」**——ring_size 不必再是 prime、不必再放 N 個 virtual node，只要 hash 落在 ring 上的整數區間就用 binary search 找最近 server。整個 virtual-node table 的 RAM 用量直接消失。(3) **Rust 結構再瘦**——hash bucket struct 從 8 byte 砍到 2 byte、route lookup 從 hash map 改成 sorted slice + binary search、allocation count 從 O(server_count × N) 降到 O(server_count)。

**為什麼主人會想讀——這是 hermes-stack + 16GB Mac + air-gap downstream 的「把演算法從『假裝 O(N)』還原成『真正 O(log N)』」範式樣板** 三個 transferable primitive：
1. **「N virtual nodes」這種 trade-off 在 substrate-aware 視角下是 antipattern**——一致性雜湊教科書教學告訴你 N 越大越平衡，但忘了告訴你 N 越大 RAM footprint 越大。在「一台主機的世界」算 RAM cost 很小，在「Cloudflare fleet × 1M requests/sec」的世界每個 virtual node 都是真實的 byte cost。文章用數學證明這個 trade-off 在 ring 等距分佈假設下**根本是 free 的**——可以無成本同時拿平衡度與 O(server_count) RAM。
2. **Sorted slice + binary search 取代 HashMap 是 substrate-aware lookup 範式**——對 static、read-only 的 ring（server 加入/移除時 rebuild）來說，HashMap 的 O(1) 平均 + 巨大 constant factor + cache-unfriendly pointer chasing 比不上 sorted slice + binary search 的 O(log N) + tight cache line。文章用 `#[repr(C, packed)]` + 2-byte struct 把一個 entry 壓到 2 byte，讓整個 ring 全部塞進 L2 cache。
3. **「ticket-driven engineering」是 substrate-aware 第一步**——文章不是「我覺得 PBR 很肥所以來優化」，而是 Performance team 的 Ivan 開 ticket、Pingora team 接手、最後 reclaim 100 TB RAM。在主人 stack 對應到：**kanban task 應該以「資源/成本指標超出預期」為 ticket 出發點**，而不是「我想做 X feature」。每個 reclaim 都是 ticket；每個 ticket 都給出 before/after 的 hard number。

**與 9/17 qorl + 9/19 Needle 3 同軸的關係** 三篇共同驗證「**substrate-aware 演算法比 naive scaling 更有 leverage**」這個 2026 下半年的 design philosophy：9/17 qorl 把 4B 模型蒸餾 + RL 推到 1.81× Postgres（**模型換 substrate，不是 server 換規格**）；9/19 Needle 3 把 8-29 MB on-device laddered networks 推到 match DeepSeek V4 Flash（**quantization + Engram fusion 換 substrate，不是雲端 API 換 quota**）；今日 Cloudflare 把 ring 上的 200× virtual node 換成 sorted slice + binary search（**演算法換 substrate，不是 fleet 換 RAM**）。三者共同點：**先用 math 證明 naive 演算法在做 redundant work、然後把 redundant work 砍掉、RAM/CPU/compute budget 自然回收。** 對應到主人 air-gap downstream 偏好——「保留已驗證 runtime 與真實行為，保守減法、端到端驗證」——Cloudflare 沒砍掉任何外部 observable behavior（cache hit rate、routing distribution、fault tolerance 都不變），只是把「假裝有用的 virtual node」和「假裝快的 HashMap」砍掉。

## 3W1H 分析

**What（做了什麼/主題）:** Cloudflare 9/18 釋出 case study：把內部 load-balancing service「Pingora Backend Router (PBR)」的 pingora-ketama library（基於 consistent hashing 路由 cacheable requests by URL）做三階段瘦身——(a) **數學 re-parametrization**：發現 ketama 的「每個 server N 個等距 virtual node」在 hash 函數均勻假設下可完全等價 rewrite 為「hash mod ring_size + binary search sorted slice」，整個 virtual-node lookup table 從 RAM 消失，ring_size 從 prime 2^32-1 換成 2^32；(b) **Rust struct 縮小**：hash bucket entry 從 8 byte (`u64 server_id`) 砍到 2 byte (`u16 shard_id`)，route lookup table 從 `HashMap<u32, ServerId>` 換成 sorted `Vec<Entry>` + binary search；(c) **Allocation 消除**：每個 lookup 不再 allocate，全 stack-allocated。結果：單一服務 reclaim **>100 TB RAM**（全球 fleet 總計），加上 8 月 DNS team 的 100 TB，**兩個月合計 200 TB RAM 從生產 fleet 上卸下**。PBR 對所有 cacheable HTTP request 跑 hot-path，所以等於每個 Cloudflare 使用者的每個請求都受益。

**Why（為什麼重要）:** 三個 transferable insight 直接掛上主人 stack——(a) **虛擬節點 (virtual nodes) 是 consistent hashing 教科書的 silent RAM tax**——為了避免 hash skew，教科書教「每個實體 server 放 N 個 virtual node」；但這 N 個 virtual node 是「假裝存在」的 entry，**真正的環只需要 O(server_count) 個 entry 就夠**，N 是冗餘。文章用「在等距分佈假設下 hash 落到 virtual node 區間等價於 hash 落到整數區間」這個 proof 砍掉冗餘；對應主人 hermes-agent-lite：「LLM 用 N 個 prompt template 跑同一個 task」常常也是冗餘——多寫幾個 prompt template 不會讓結果變好，只會讓 context window 變大。(b) **Sorted slice + binary search 在 read-mostly workload 永遠贏 HashMap**——Cloudflare 的 ring 在 server 加入/移除時 rebuild，平時 read-only；`HashMap` 的 O(1) 平均 + pointer chasing cache miss + 8-byte entry 在這個 workload 下是 **constant factor 巨大的 O(log N)**，binary search 2-byte entry 的 L2-cache-friendly 版本直接打爆。(c) **「ticket-driven」勝過「feature-driven」engineering**——文章不是「我們想加新功能所以改 PBR」，而是 Performance team 的 Ivan 發現 memory 異常開 ticket、Pingora team 接手調查並 reclaim 100 TB。在 hermes kanban：ticket 應該以「某項指標超出預期」為單位（memory > X MB、latency > Y ms、confidence < 0.7 rate 過高），而不是「我想做 Z feature」。

**How（如何運作/實作）:** 四階段 pipeline：(1) **Problem diagnosis**——Performance team Ivan 用 `heaptrack`/`jemalloc stats` 抓到 PBR 佔用異常，挖到 pingora-ketama ring 上的 virtual node count × server count 算出 global RAM cost；(2) **Math re-derivation**——證明「對每個 URL hash 找最近 virtual node」在等距分佈假設下等價於「hash 落入 virtual node i 的整數區間 [(2^32-1)/N × i, (2^32-1)/N × (i+1)]」——所以只要 hash mod 2^32 就能直接用 binary search 找 server，省掉整個 virtual-node table；(3) **Rust rewrite**——把 `Vec<VirtualNode>` 換成 `Vec<Entry>`，`Entry` 是 `#[repr(C, packed)] struct { shard_id: u16, server_idx: u16 }`（2 byte），lookup 從 `HashMap::get` 換成 `slice::binary_search_by`，全 stack-allocated、零 heap allocation；(4) **Production rollout**——先把 PBR 跑 canary（10% traffic），對比 cache hit rate / routing distribution / p99 latency 確認 external behavior 完全不變，然後 100% rollout 然後 reclaim 100 TB RAM 還給 Performance team 重新分配。三個 substrate-aware 細節：(a) **`#[repr(C, packed)]`** 強制 struct 沒有 padding、2 byte 真的就是 2 byte（一般 Rust struct 預設會 padding 到 4-byte alignment）；(b) **`slice::binary_search_by`** 編譯器會自動 SIMD 化（`_mm_cmp_epi32` + branchless mask），比 `HashMap::get` 的 pointer-chasing 快一個數量級；(c) **Global fleet aggregate**——單機省 100 MB 沒人在意，但全球 1M+ servers × 100 MB = 100 TB，這是 Cloudflare 能這樣做的關鍵規模槓桿。

**Insight（個人心得）:** 主人，這一篇在 substrate-identity 上是 9/17 qorl + 9/19 Needle 3 + **9/20（今日）Cloudflare** 「演算法/結構換 substrate」軸線的**第三個 instance**。文章最強的 portable primitive 是 **「在等距分佈假設下，ketama 的 N virtual nodes 等價於 sorted slice + binary search」這個 math re-parametrization**——它不是新的演算法發明，而是**把教科書的「為了 handle skew 而放 N 個 virtual nodes」這層抽象還原成「其實 skew 在 hash 均勻假設下不存在」這個更深的真理**。對應到 hermes-agent-lite 的 tool wrapper：今天主人驗 tool-call 正確性時常寫「try 3 種 prompt template、挑最好的」——這個 N=3 是 silent compute tax；如果 tool call 真的失敗，**應該問「schema 是不是設計錯」而不是「prompt 不夠多」**。**1 小時 prototype 路徑**：在 `~/.hermes/profiles/default/skills/` 下新增 `ketama-binary-search-lookup` skill（不要真的做 consistent hashing，做的是 portable primitive：**把任何 read-only lookup 從 `HashMap<K,V>` 換成 sorted `Vec<(K, V)>` + `binary_search_by`** 的 Rust recipe），提供 `lookup<K: Ord, V>(slice: &[(K, V)], key: &K) -> Option<&V>` 函式 + `Entry` struct 的 `#[repr(C, packed)]` template + 簡單 benchmark（10K entry HashMap vs sorted slice binary search）。這跟 9/17 qorl 的「用 GRPO RL 重訓 planner model」、9/19 Needle 3 的「用 laddered subnetwork 換 model depth」是同一族「**在 fixed budget 上重新 fit 工作負載**」，< 200 行 Rust + < 2 hours commit + 無 LLM call。同時——「ticket-driven engineering」直接對應到主人 kanban 偏好：今日主人 kanban 裡若有任何 task 是「我想做 X feature」開頭的，可以考慮改寫成「指標 Y 超標 / 成本 Z 過高」開頭，讓 task 一開始就綁到可驗證的 before/after number，而不是抽象意圖——這也對齊主人「**以真實 handoff、exit code、tests、health、live smoke 判定狀態**」的 hard-evidence 哲學。