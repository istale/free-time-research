# Reverse engineering my e-scooter and rewriting the firmware in Rust

- 原始連結: https://bensimms.moe/reverse-engineering-scooter/
- HN 討論: https://news.ycombinator.com/item?id=49638071 (HN #10, 329 points / 124 comments at read time)
- 閱讀時間: 2026-09-14（早間）
- 來源: bensimms.moe 個人 deep-dive（作者 Ben Simms，2026-08-09 發表，原始 firmware repo: https://github.com/simmsb/scooter-display、can-flasher: https://github.com/simmsb/egret-can-flasher、AT32 HAL fork: https://github.com/simmsb/at32f4xx-hal）

## 摘要

**一個 6 個月的 end-to-end 韌體 reverse-engineering 案例：把 Egret GT 電動機車的顯示器從 C 廠版改寫成 Rust。** 作者從 USB-C 腳位的示波器 probe 開始，意外發現兩個 SBU pin 被當作 CAN bus 在用（明顯違反 USB-C 規範），接著自焊 ESP32-C6 + SN65HVD230 + MCP2515 的「混種」CAN sniffer，把 throttle / driving mode / motor speed 等 frame 全部解出來。透過 Sniffing-while-driving，他建立一份完整的 CAN message table（0x300 = display→controller 的駕駛模式、0x306 = throttle 9-bit unsigned、0x201 = motor speed），並用 egui 寫了個時間軸視覺化工具——這是這篇文章第一個「學得到」的 hack 點。

**第二階段是 hardware teardown + firmware extraction。** Controller 用的是 APM32E103xCxE（STM32F103 相容），作者用 Dremel 切開 potting compound（幸運地是軟膠），直接摸到四個 SWD pad，用 OpenOCD dump flash + RAM。顯示器用的是 AT32F415（同樣 STM clone，但 RTC 是 STM32F3 風格），加上 CH573 bluetooth MCU、W25Q128FV SPI flash、FM17520 NFC。firmware 結構是 bootloader（0x8000000）+ updater（0x8003000）+ main app（0x8006200），三者都可以透過 CAN bus 互相更新——重點是**完全沒有 authentication 或 cryptography**。作者用 Ghidra 反編譯，搭配 ArteryTek 官方 HAL source（沒開 LTO 所以 decompile 跟 source 幾乎一對一對應），逐步把 GPIO 與 peripheral configuration 全部映射回原始 pin 編號。

**最精彩的一段是「Rewrite it in Rust」。** 因為 AT32F415 不是任何 Embassy 支援的晶片，作者從 stm32-rs fork 出 at32f4xx-hal，先實作 embassy-time-driver timer，再依序 bring-up ADC、GPIO interrupts、UART、CAN、RTC。顯示器驅動用 mipidsi + 一個自寫的 `Bus<const P: char, const SHIFT: u8, const MASK: u16, MODE>` parallel-16 GPIO bus（透過 const-generics 把 mask / shift 編進 type）——這是這篇文章第二個「學得到」的 hack 點。boot 順序最 tricky：原廠 bootloader 啟動時已經 enable 一堆 clock，作者必須在 Rust 端用 `unsafe { hal::pac::Peripherals::steal() }` + 一連串 CRM register reset 把 clock tree 整個打掉重來，最後才用 `cfgr.use_hext(8.MHz()).sclk(96.MHz()).freeze()` 鎖定。這段「bootloader → application takeover」的 dance 是 MCU 客製 firmware 共同的痛點，跟昨天 9/13 Rust never type 那篇「substrate 變更的 cost ladder」直接呼應——差別是今天這篇是「自己重寫 substrate」，昨天是「被 substrate 變更推著走」。

**Why this matters for 主人：** 這篇正好接續主人 9/13 看的 Rust never type 那條主線。never type 是「語言 substrate 進化」，今天這篇是「在 MCU substrate 上自己重寫 firmware」——兩者都把 Rust 推到 substrate / embedded 的更底層。對主人的 horo-agent / hermes-agent-lite 棧有三個直接聯想：(1) 自寫 HAL + const-generics parallel bus 是「Rust type system 對位硬體位址」的標準玩法，跟主人 慣用的 wheel + substrate 抽象同一種語感；(2) Ghidra + decompile-to-source 比對、OpenOCD flash dump 是 reverse engineering 的「cargo clippy 對位版」——看 decompiled output 跟 vendor HAL source 的差異，是 substrate trust 的最低成本驗證手段；(3) CAN bus 0x384 firmware update loop 完全沒有 crypto / auth 的事實，是 supply-chain 與 IoT security 的反面教材——主人 的 horo-agent 走 air-gap / tailnet-only，但如果未來擴到 IoT agent 棧，這條「bootloader 信任鏈」就是必補的洞。

## 3W1H 分析

- **What（做了什麼/主題）：** 作者 Ben Simms 用 6 個月時間把 Egret GT 電動機車的顯示器 MCU（AT32F415，STM32 clone）從原廠 C firmware 完整 reverse-engineering，並改寫成一套使用 Rust + Embassy-style async runtime 的自駕 firmware。技術鏈涵蓋 USB-C 腳位示波器 probe、ESP32-C6 自製 CAN sniffer、Ghidra 反編譯、OpenOCD flash 提取、自寫 stm32-rs fork HAL（at32f4xx-hal）、embassy-time-driver bring-up、mipidsi 顯示器驅動，以及一段極其 tricky 的「bootloader → Rust app clock takeover」reset sequence。
- **Why（為什麼重要）：** 對主人 來說，這是 Rust 在「語言 substrate 之上更底一層」的具體展演。主人 9/13 才看了 Rust 1.99 never type 穩定化——那是語言表層的進化；今天這篇是「語言沒變、但你自己把整個 embedded substrate 用 Rust 重寫」。具體落點：(a) at32f4xx-hal 的 `Bus<const P, const SHIFT, const MASK>` 用 const-generics 把 GPIO mask 編進 type，這是 Rust 對位硬體最 natural 的寫法，主人 之後若要把 horo-agent 推到任何「自寫 driver / 對位實體裝置」場域，這個 pattern 可以直接抄；(b) 完全沒有 auth 的 CAN firmware update（0x384 message 直接 reset + 寫 flash）對主人 的 supply-chain trust model 是反面教材——主人 的 hermes-agent-lite 雖然是 software-only，但若 agent 棧延伸到 IoT（譬如 horo-agent on edge device），bootloader trust chain 必須重新設計；(c) 文章展示的「decompile-to-source 比對」是 substrate-level trust 的最低成本驗證手段，主人 的 cargo `--locked`（8/21 pick）處理 build-time supply chain，decompile-to-source 比對處理 runtime substrate trust——兩者是同一面牆的不同剖面。
- **How（如何運作/實作）：** 技術上有四個關鍵操作軸：(1) **Bus sniffing**——USB-C SBU1/SBU2 兩個腳被當 CAN bus，示波器確認後用 ESP32-C6 + SN65HVD230（CAN transceiver）+ MCP2515（CAN controller）的自焊板子 log frame，搭配 egui 時間軸 GUI 把 ID 對時間畫散佈圖，肉眼即可分辨 command vs response vs periodic；(2) **Firmware extraction**——OpenOCD 接 SWD pad，dump flash + RAM，Ghidra 開 decompile，靠 AT32 沒開 LTO 的特性讓 HAL function 跟 source 一對一對應，30 分鐘內把整個 GPIO + peripheral config 全部映射回去；(3) **自寫 HAL**——從 kossnikita 的 stm32-rs fork 出 at32f4xx-hal，timer 優先（要給 embassy-time-driver 用）、再 ADC → GPIO interrupt → UART → CAN → RTC，顯示器用 mipidsi + 自寫 parallel-16 GPIO bus；(4) **Boot takeover**——原廠 bootloader 已經 enable 各種 clock，Rust app 必須用 `unsafe { Peripherals::steal() }` 拿到 PAC，把 CRM（Clock Reset Module）的 ctrl / cfg / pll / apb1en / apb2en / ahben 一個一個 reset 掉，最後才用 `cfgr.use_hext(8.MHz()).sclk(96.MHz()).pclk1(48.MHz()).pclk2(48.MHz()).freeze()` 鎖定——CAN bus 在超過 96 MHz 會 fall over 是一個 embedded debug 的小坑。
- **Insight（個人心得）：** 本文最值得主人 拿走的是 **「Rust 在 MCU 上不只是語言改寫，是 substrate 整層的型別系統化」**——at32f4xx-hal 的 `Bus<const P, const SHIFT, const MASK>` 把 GPIO mask / shift 編進 type，讓編譯器替你檢查「哪些 pin 是合法成員」，這比 C macro / `#define` 表達的「硬體 layout」更早抓到 bug。這跟主人 抽象精煉的風格（done/do_again → 含 feedback supervised loop → NOOA CodeAct loop）完全同調。具體落地建議——對主人的 horo-agent / hermes-agent-lite 棧：**（a）< 5 分鐘（text-rule 級）**：在 SOUL.md 加一條「embedded / MCU-side code: prefer const-generics over macro for hardware layout」，對齊這篇的型別系統化哲學；**（b）< 1 小時 prototype**：若主人 未來要把 agent 推到 edge device，先在 horo-agent 的 wheel 寫一個 `Bus<const P, const SHIFT, const MASK>` mirror（哪怕只是 type stub），驗 const-generics 對位硬件 layout 的編譯期檢查威力，這個 mirror 之後真接到 MCU 時直接 reusable；**（c）< 4 小時**：若主人 想真玩一次「自寫 HAL + Embassy bring-up」，最便宜的入口是買一個 STM32F103 開發板（$5 級），跑 embassy-template 的 `embassy-time-driver` + `mipidsi` 那條鏈，預期 4 小時可以 bring-up 一個 SPI display + 兩顆 LED，順便驗證 const-generics Bus pattern 在自家 codebase 好不好用。成本估計：text-rule 級 < 5 分鐘、type-stub mirror < 1 小時、STM32F103 bring-up prototype < 4 小時，這三層 ladder 都可以在下一個 horo-agent CI 升級週期一次到位。跟前幾天 9/13 Rust never type 那篇對位：9/13 是「語言 substrate 主動進化」（要 pin toolchain），今天是「自己當 substrate 作者」（要懂 const-generics 對位硬體）——同樣是 Rust substrate 主題，一面是被動應對、一面是主動建構，主人的 Rust substrate 知識樹可以同時走這兩條分支。
