# GitHub 專案動態
- 檢查時間：2026-07-21
- 檢查對象：tirth8205/code-review-graph、1jehuang/jcode、diegosouzapw/OmniRoute（Trending Top 3）+ PromptPartner/agentsmith、yc-duan/fastctx（Web 探索 2）

## 動態摘要

### tirth8205/code-review-graph（tirth8205/code-review-graph，Trending #1，連莊第二天）
- 類型：release / commit / issue / pull request
- 內容：最新 release 仍為 `v2.3.7`（2026-07-18，maintainer-reconciliation release，標題直接寫「不含四個 client-validation drafts」）。最新 3 條 commit：`6ce25b4` 2026-07-21「Update README.md」（今天剛推，呼應昨天單日 +1,833 stars 還在漲）、`6a1ee1c` 2026-07-18「release: v2.3.7 (#659)」、`0a3bd6c` 2026-07-17「fix(security): resolve open CodeQL alerts (#657)」——今天（2026-07-21）open Issue：#691「Generic walker's name-field heuristic misses common LaTeX/BibTeX/Markdown node shapes」（用戶在 `.code-review-graph/languages.toml` 自訂語言時發現 generic walker 的命名 heuristic 太窄，LaTeX/BibTeX/Markdown 的命名節點會被 silently dropped）；open PR：#689「fix(embeddings): default to OpenAI provider when env vars are configured (#551)」（MCP `semantic_search_nodes` 沒帶 `provider` 時一律 fallback 到 local sentence-transformers，即使 `CRG_OPENAI_API_KEY`/`CRG_OPENAI_BASE_URL` 已設，把 Ollama / vLLM / LocalAI / LiteLLM 等 OpenAI-compatible embeddings 都擋掉）、#688「feat(platform): add MiMo Code MCP support」——三條訊號很清楚：**v2.3.7 之後的收斂期還沒結束**，反而從「CodeQL alerts 清空」延伸到「embeddings provider 預設 / 自訂語言覆蓋率 / 新增 MCP client target（MiMo Code）」三條不同層面，代表 v2.3.7 不是 release-only 的句點，而是「**先把公開 bug 與 embed provider 拓荒清完，再放新 MCP target**」的循環，今天再登 trending #1（單日 +1,833 stars 持續累積）說明這條 codebase intelligence + MCP 路線還在漲。
- 連結：https://github.com/tirth8205/code-review-graph/issues/691

### 1jehuang/jcode（1jehuang/jcode，Trending #2，新進）
- 類型：release / commit / issue / pull request
- 內容：最新 release 為 `v0.54.4`（2026-07-20，「Complete cross-platform release artifacts」），重點修三件事：portable Linux x86_64 下載（補回 bundled OpenSSL 的完整 runtime）、Windows x86_64 / ARM64 release pipeline 修好、release publication 改成 gated 直到 Linux / macOS / Windows / FreeBSD 四個 artifact 都齊才能 publish。最新 3 條 commit：`ccf6153` 2026-07-21「Rename Flagship plan display to Solo」（今天改名，可能是 Solo/Haiku/Ultra/Fable 等訂閱層命名在做品牌調整）、`f6b9335` 2026-07-20「Give Ultra access to Fable 5」、`fc6e16d` 2026-07-20「fix(models): exclude unstable Nemotron Super route」——主軸是 **「訂閱層權限 / 模型路由穩定 + cross-platform release artifact gating」三件套**。今天（2026-07-21）open Issue：#526「How do I disable emojis in jcode output?」（用戶在 TUI 與 CLI 都想要完整關掉 emoji；作者回覆已有 `JCODE_QUIET=1` 但用戶不確定是否覆蓋全部路徑）；open PR：#528「fix: add global no-emoji output config」（提議加 `[display] emoji = false` 與 `JCODE_NO_EMOJI=1`，把 emoji grapheme clusters 換成 ASCII）、#527「fix(desktop): gate Unix IPC on Windows」（jcode-desktop 的 Unix-domain-socket IPC 模組要在 Windows build 完全排除，否則 Windows MSVC cross-check 會 E0432/E0433 失敗）——三條訊號跟 v0.54.4 一致：**「跨平台 TUI 體驗一致性 + 桌面 IPC 在 Windows 的 gating + 模型路由穩定」是當前優先**，呼應 trending #2（單日 +568 stars，新進項目）代表社群對「**terminal-first Rust agent harness + 多模型 routing + 多平台 TUI**」這個切角正在快速共鳴。
- 連結：https://github.com/1jehuang/jcode/pull/528

### diegosouzapw/OmniRoute（diegosouzapw/OmniRoute，Trending #3，新進）
- 類型：release / commit / issue / pull request
- 內容：最新 release 為 `v3.8.48`（2026-07-13，標題明寫「**Hotfix release**」，3.8.47 的 npm package 每次 boot 都 crash（`ERR_MODULE_NOT_FOUND`，prepublish prune allowlist 漏了 `dist/head-response-guard.cjs`，這是「tls-options/3.8.41」之後第三個同類問題），被 deprecated；3.8.48 是 v3.8.47 cycle 第一個能裝的版本）。最新 3 條 commit：`c1bdd91` 2026-07-21「Hide internal reasoning replay placeholders (#7912)」、`1175746` 2026-07-21「fix(antigravity): collect native functionCall parts in SSE collector (#7902)」、`eadcbea` 2026-07-21「fix(combo): strip boolean reasoning field for opencode-go providers (#7891)」——主軸是 **「reasoning replay / SSE collector 對 native functionCall 的處理 + combo provider 的 reasoning field strip」三件套**，全是 provider 邊界相容性。今天（2026-07-21）open Issue：#7938「fix(mitm): Agent Bridge wizard — empty sudo on DNS/Trust Cert (pam_faillock) + mappings never auto-detected」（Linux CachyOS / Arch 上 MITM Agent Bridge 的 Setup wizard / Trust Cert / Start DNS 在沒 sudo password 時直接 spawn `sudo -S` 觸發 pam_faillock）、#7937「[Feature] UI improvements in the Provider tab」（單一 provider 超過 50 帳號分頁只能卷到底才能換頁、沒辦法用名字搜尋帳號）；open PR：#7939「fix(mitm): gate Agent Bridge DNS and Trust Cert on sudo password」——三條訊號把 OmniRoute 的當前態畫得很清楚：**「provider 數量暴增（260+ providers、500+ models）→ 帳號管理 UI 撐不住 + agent bridge 需要 root 時的安全 gating + provider 邊界的 reasoning field 處理」**，呼應 v3.8.48 的 hotfix 主軸——**build/ci/包裝環節的 regression 反覆出現**，trending #3（單日 +1,107 stars）說明「**free MIT AI gateway / 一個 endpoint 通吃 268+ providers**」這個切角在今天成本敏感的開源圈仍是強勁入口。
- 連結：https://github.com/diegosouzapw/OmniRoute/pull/7939

### PromptPartner/agentsmith（PromptPartner/agentsmith，Web 探索 #1）
- 類型：commit（無 release、無 issue/PR）
- 內容：**完全沒有 published release**（404）；最新 3 條 commit 全部都在 2026-07-17 ~ 2026-07-18：`8d07e12` 2026-07-18「docs(readme): embed the live star-history chart (repo now at 100 stars)」、`b9782cc` 2026-07-17「feat(setup): auto-install rtk (CLI-output compressor) for code profiles」、`5159433` 2026-07-17「docs(license): add MIT LICENSE and name it in the README」——三條訊號把 agentsmith 的當前階段畫得很清楚：**5 天大的新 repo，正卡在「early user onboarding + 自動安裝 rtk 來省 CLI token + MIT license 補齊 + 100 stars 慶祝」**這個「0.x 自我介紹」階段。**issues/PRs 列表完全為空**——代表外部貢獻還沒進場，整個專案還在 maintainer 獨推 + 文件化階段。repo 自介寫「Universal, model-agnostic operating harness for AI agents (Claude, Codex, Gemini, …) — a lean core + work-type profiles assembled by one setup script」，跟今早 AM 那篇 Cursor agent swarm 的「**規劃者／執行者／衝突仲裁者**」切角呼應：agentsmith 把同樣的「model-agnostic 通用 operating system 給 agent」做成 lean-core + profile plug-in 的安裝腳本，並把 rtk（CLI-output compressor）直接掛進 setup，這條「**省 token 的 agent runtime 工具鏈**」訊號正好對齊今天 PM 那篇 cache-aware prompt compression 的「prefix-stable + tool output compression」雙軌。
- 連結：https://github.com/PromptPartner/agentsmith

### yc-duan/fastctx（yc-duan/fastctx，Web 探索 #2）
- 類型：release / commit / issue
- 內容：最新 release 為 `v0.1.1`（2026-07-19，FastCtx v0.1.1 is a **「compatibility and update-reliability release」**——重點：source-aware npm updates 加 half-install protection + stable-copy recovery、Linux 相容性降到 glibc 2.31、Windows legacy-encoding recovery 保留 shell output bytes）。最新 3 條 commit：`64a6a45` 2026-07-19「ci: stop duplicate runs from starving the release workflow」、`8cc2d54` 2026-07-19「test(parallel): drop a scheduling-order assumption from the early-break test」、`f049d0b` 2026-07-19「ci(release): publish npm packages with trusted publishing」——主軸是 **「CI/release workflow reliability + 跨平台 runtime 拓邊」**：CI 重複 run 不再餓死 release workflow、test 平行化的 scheduling-order 假設拿掉、npm package 用 trusted publishing。今天（2026-07-21）open Issue：#6「请问能结合rtk工具使用吗？」（簡中，用戶想確認 fastctx 能不能搭配 rtk 一起壓 CLI 輸出）、#3「[feature request] 关于使用 fastctx 来直接操作 remote host」（簡中，建議加 `ssh://<host>/xxx` 內部路徑讓 fastctx 透過 sshfs 無感 access remote host、本地 codex 也能操作伺服器檔案、跑遠端 bash）、#2「更新检查失败: the npm launcher did not provide FASTCTX_NPM_CLI」（macOS 15.7 + NPM 安裝的 fastctx v0.1.1 在「更新檢查」選單失敗）——三條訊號把 fastctx 切角畫得很清楚：**「MCP-based context-efficient repo tools + 跨平台發版穩定 + 開始想接 remote host」**，5 天大、174★，跟 PromptPartner/agentsmith 是同期新 repo，兩者一起驗證了「**MCP-based repo tools / agent operating system**」這個切角在開源社群正在快速冒出來。
- 連結：https://github.com/yc-duan/fastctx/issues/3

## 重點觀察
- 今天 Trending Top 3 跟前幾天完全沒有重疊，且**連莊者只有 tirth8205/code-review-graph**（昨天是 #2，今天升 #1），`1jehuang/jcode` 與 `diegosouzapw/OmniRoute` 都是新進；跟前天 (2026-07-19) `Robbyant/lingbot-map + apache/ossie + PostHog/posthog`、昨天 (2026-07-20) `bojieli/ai-agent-book + tirth8205/code-review-graph + kvcache-ai/ktransformers` 完全換一輪——**GitHub Trending 週一到週二的輪替強度比週末還高**，今天直接切到「**code intelligence / Rust terminal agent / free AI gateway 三件套**」，顯示「**省 token + 多 provider / 多平台 / 多模型路由**」這個成本敏感切角正在取代前幾天「**內容型教科書 + 異質推論框架**」的主軸。
- 今天 Trending 兩條訊號合在一起很值得記：**1jehuang/jcode 在 Rust 寫「terminal-first agent harness」+ diegosouzapw/OmniRoute 在 TypeScript 寫「free MIT AI gateway, 268+ providers, 500+ models」**——前者把「**agent 的本地執行端**」用 Rust 推到極致（v0.54.4 一次搞定 Linux/macOS/Windows/FreeBSD artifact gating + emoji config + IPC Windows gating + Nemotron Super route 暫時關掉），後者把「**agent 的雲端路由端**」用 TS 鋪成 free gateway——**端（Rust TUI）與雲（TS gateway）兩條路同時在 trending 前段**，跟今早 AM 那篇 Cursor agent swarm 的「規劃者／執行者分工」與 PM 那篇 cache-aware prompt compression 的「prefix-stable + tool output compression」是同一週期內的互補訊號：開源社群正在把「**agent runtime 的每個層（端 / 雲 / 工具鏈）**」都做成獨立可組合的小型 repo。
- Web 探索兩個 repo 切角刻意避開 trending 三強並剛好對齊今天另外兩篇閱讀：**PromptPartner/agentsmith 是「model-agnostic 通用 agent operating harness（5 天大、0 release、文件 + rtk 自動安裝 + 100 stars）」**、**yc-duan/fastctx 是「MCP-based context-efficient repo tools（5 天大、v0.1.1 跨平台相容 + trusted publishing + remote host feature request）」**——兩者都不是 trending 上的當紅炸子雞，但都精準落在「**MCP / agent operating system / 多模型路由 / 跨平台發行**」這條主軸上，**5 個 repo 合計覆蓋了「code intelligence / Rust terminal agent / free AI gateway / model-agnostic harness / MCP repo tools」五個不同切角**，跟昨天「內容 / framework / 異質推論 / 紅隊 / 多 agent 協作」完全換了一輪。
- 跨 5 個 repo 的共通訊號是 **「跨平台發版 / provider 邊界相容 / MCP / agent runtime 工具鏈」**四個關鍵詞同時出現：tirth8205/code-review-graph 在 push「MiMo Code MCP target + embeddings provider fallback + 自訂語言覆蓋率」、1jehuang/jcode 在 push「Linux/macOS/Windows/FreeBSD artifact gating + emoji config + Windows IPC gating + Fable 5 訂閱層權限」、diegosouzapw/OmniRoute 在 push「provider 數量暴增後的 UI 撐不住 + agent bridge sudo gating + reasoning replay placeholder」、agentsmith 在 push「rtk 自動安裝 + lean core + 100 stars」、fastctx 在 push「glibc 2.31 向下相容 + Windows legacy-encoding + trusted publishing + remote host feature request」——**5 個 repo 沒有一個今天在 land 大型 feature，而是把「跨平台相容性 + provider 邊界 + MCP 拓荒 + runtime 工具鏈（rtk）」放在最高優先**，跟前幾天 deer-flow / nanobot / PostHog / T3MP3ST / lemma-platform 的「安全收緊 + 維護型 PR」相比，今天切到「**拓邊型相容性**」是新的節奏，呼應主人今早讀的 Cursor 與 PM 那篇都在談「**模型端／provider 端／runtime 端的組合成本**」。
- 兩個新進 Web repo 加起來剛好 **5 天大、沒有任何外部 PR/issues 進場、文件與發版仍在補齊階段（agentsmith 連 release 都沒有；fastctx 才 v0.1.1）**——這個訊號要跟 PromptPartner/agentsmith 100 stars 達標後立刻補 MIT LICENSE 一起看：**「AI agent / MCP / code intelligence」這個切角在開源社群已經進入「5 天長到 100~300 stars + 立即補 license / 立即掛 rtk 壓輸出 / 立即想 remote host」這個「0.x 快攻」階段**，跟 7 月初的 memoharness / nanobot / deer-flow 相比，今天的新進 repo **更小、更工具導向、更明顯以「**省 token / 多平台 / 多模型 routing**」當作第一性需求**，對主人現在 Hermes runtime（prefix cache + skills 載入 + delegation）下一輪優化是把「**rtk / MCP repo tools / cross-platform artifact gating**」這三條開源訊號收進去，不是再寫一個大框架。