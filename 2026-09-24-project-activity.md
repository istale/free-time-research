---
title: GitHub 自由探索 2026-09-24（14:00 台北時間）
date: 2026-09-24
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 7 天 repeat）
  - GitHub Search API `created:>2026-09-15` 排序 stars desc（Tier-B web 探索 2）
  - GitHub REST API `/repos/<slug>` + `/releases/latest` + raw `README.md`
---

# GitHub 專案動態

- 檢查時間：2026-09-24（14:00 台北時間，週四）
- 檢查對象：`google/ax` / `Open-Dev-Society/OpenStock` / `superdesigndev/treg` / `zai-org/ZCode` / `unreallabsai/unreal-agent`
- 來源組合：GitHub Trending today 解析後 Tier-A 17 row 排除前 7 天 repeat（9-17 `cloudflare/security-audit-skill` + `abue-ammar/tinycast` + `jamiepine/voicebox` + `NousResearch/hermes-agent` + `apache/maka`；9-18 `Tencent/BrowserSkill` + `TencentCloud/Octop` + `coder/coder` + `TencentCloud/TencentDB-Agent-Memory` + `microsoft/agent-framework`；9-19 `fmtlib/fmt` + `mattpocock/skills` + `DietrichGebert/ponytail` + `deepseek-ai/deepseek-harness` + `zhayujie/CowAgent`；9-20 `cloudflare/security-audit-skill` + `trycua/cua` + `addyosmani/agent-skills` + `volcengine/OpenViking` + `semantica-agi/semantica`；9-21 `affaan-m/ECC` + `BuilderIO/agent-native` + `vercel-labs/json-render` + `markfulton/ai-employees` + `yylo-dev/yylo`；9-22 `anthropics/skills` + `JuliusBrussee/caveman` + `blader/humanizer` + `magnitudedev/magnitude` + `Fission-AI/OpenSpec`；9-23 `agent-substrate/substrate` + `dream-num/univer` + `davila7/claude-code-templates` + `browser-use/jev-ultrafast` + `tamaratran/fast-jev-compaction`；**Tier-A 排除後 fresh top 3 = `google/ax` rank 2 = 9,358★ + 1,543 stars today + **`v0.3.0` GH release 2026-09-20 = 4 天前** + Apache-2.0 + Go + 「AX 是 Agent Substrate 上層 declarative orchestrator（Substrate = runtime layer / AX = control plane + manifest API），agent workload 不像 stateless microservice 也不是 run-to-completion batch job，AX 提供 Task + Workspace + Gateway + Model 4 primitives 走 `kubectl`-shaped CLI（`ax apply / get / watch / ssh / suspend / resume`）」+ **`agent-substrate/substrate` 是 9-23 pick #1，AX 是 Substrate 上的 control plane 層 = Substrate → AX 同一條 ecosystem 兩週內連續 pick 2 次 = ecosystem pair**、1,543 stars today = **agent runtime/control plane 主題本日最熱**）+ **`Open-Dev-Society/OpenStock` rank 7 = 18,946★ + 344 stars today** + **NO GH release（社區 stock app 走 `pnpm dev` 沒有 SemVer 紀律）** + AGPL-3.0（自述 `© Open Dev Society. This project is licensed under AGPL-3.0; if you modify, redistribute, or deploy it (including as a web service), you must release your source code under the same license and credit the original authors` = **case-E AGPL-3.0 標準條款 + README 內顯式標示 = case-E 嚴格 copyleft**）+ TypeScript + Next.js 15 + App Router + Tailwind v4 + shadcn/ui + Better Auth + MongoDB + Finnhub + TradingView widgets + 「open-source alternative to expensive market platforms, track real-time prices, set personalized alerts, and explore detailed company insights」+ `pnpm install` / `pnpm dev` / `pnpm test:db` install path + docker-compose 一鍵起）+ **`superdesigndev/treg` rank 11 = 2,827★ + 506 stars today** + **NO GH release（fresh launch < 60 天）** + **NOASSERTION license（README 內 `Join community here: https://discord.gg/6mQYYfFMAn` + `treg.to` hosted SaaS 但 LICENSE 沒 file = case-G，custom non-standard terms 警告）** + Python + 88,254 KB + topics 11 個（agents/api-keys/cli/credentials/developer-tools/dsh-plugin/mcp/proxy/python/registry/secrets）+ 「OpenRouter for agent tools, 3,000+ catalogued endpoints across 60+ providers, priced per call from a cent, no provider signup」+ **`curl -fsSL https://treg.to/install.sh | sh` + `treg login` + `treg catalog search` install path + Claude Code `/plugin marketplace add superdesigndev/treg` + `/plugin install treg@treg` + `npx skills add superdesigndev/treg -s treg` 三向 install**。Tier-B web 探索命中兩個 strong leads：(a) **`zai-org/ZCode`**（Z.ai 的 coding agent harness、ZCode v3.14.3 = 9-23 update、TypeScript + Apache-2.0、`pushed_at` 2026-09-23 13:25 UTC = 昨天、6,593★、`created_at` 2026-09-20 12:01 UTC = **only 4 天 fresh launch**、README 中文 `ZCode 是 AI 编程工作台，提供桌面应用、浏览器界面和终端 Agent`、需要 `mise.toml` 鎖 Node.js **24.14.0** + pnpm **10.33.2**、`pnpm bootstrap` install、`pnpm dev:desktop` / `pnpm dev:web` / `pnpm dev:desktop:test` 開發路徑、`pnpm bundle:desktop -- --os mac --arch arm64` Electron 打包、發行包 `tar.gz` + `sha256.txt` + `latest.json` + `install.sh` 走 `~/.zcode/runtime` 安裝、`apps/zcode-cli/` 內含 Agent CLI 源碼、`pnpm --filter @zcode/cli... build` build path、`open_issues` 11、`size_kb` 40,147 = 中等 monorepo、`releases/<version>/` 下 zip、**GH release `NO GH release` 因為走自家 install.sh + tar.gz 不是 GitHub Releases**）+ (b) **`unreallabsai/unreal-agent`**（Unreal Labs 出的 async-first agent harness、Go + MIT、`v0.2.0` GH release **2026-09-23 19:15 UTC = 昨天**、1,847★、`created_at` 2026-09-21 23:14 UTC = **3 天 fresh launch**、README「harness/ = the library, cmd/ = executables, benchmarks/ = benchmark runners」、9 個 component glossary（Session inbox / Coordinator / Session store / Context builder / LLM Adapter / Tool registry / Tool translator / Operation manager）+ Session-store 持久化 + tool translator 同步不阻塞 event loop + operation versioning + proxy operations manager 跨 process 跨 sandbox 調用、README 簡短靠 code 自我表達、`size_kb` 1,067 = **極小 source-only Go library**、`open_issues` 8、`pushed_at` 2026-09-23 19:08 UTC = **昨天仍 active**、**GH release `v0.2.0` 2026-09-23 = 1 天前** = **5 picks 裡 release 最鮮**、**3 天 fresh launch + 2 個 release 已經 ship = GA discipline 起步**）。

## Repo 摘要與 3W1H

### `google/ax`

- **Repo 摘要：** 「**Declare an agentic task with workspaces and gateway specifications. AX sandboxes it, wires up its workspace, fences its network, and helps running it at scale.**」「**AX is a high-throughput, declarative orchestrator to run billions of autonomous agent workloads in a cluster. It runs on top of Agent Substrate for sandboxed execution and is built to run billions of tasks per cluster. If you have used Kubernetes, `ax` will feel similar.**」——Google ATE team、2026-03-30 開源（**6 個月 mature**）、9,358★、Go（README badge + `cmd/ax` binary）、**Apache-2.0**、topics 空陣列、`created` 2026-03-30T17:55:01Z、`pushed_at` 2026-09-23T21:08:29Z（**昨天仍 push**）、`updated_at` 2026-09-24T05:59:52Z、`open_issues_count` 38、size 44.789 MB、`default_branch` main。**Latest GH release `v0.3.0` published 2026-09-20T03:33:20Z（4 天前）**、`created_at` 2026-09-20T03:27:51Z、**non-prerelease / non-draft** = 正式 SemVer bump、**`v0.3.0` 跟 `v0.1.0`（Substrate 9-23 pick）成 Substrate ecosystem pair — 兩週內連續 pick 同一個 ecosystem 的兩層**。**README WARNING 明確**：「**We are still actively refining our core concepts, protocols, and specifications. We will likely to introduce major breaking changes prior to a stable release.**」= **v0.3.0 是 "reference architecture 公開 + breaking changes ahead" 而非 "stable API"**、對位主人 horo-agent downstream 不能 ship 進 production runtime、可借 Task/Gateway/Workspace/Model 4-piece abstraction 做 reference。**4 大 primitives 走 `kubectl`-shaped CLI**：(1) **`Task`** — Run untrusted agent code in isolated sandbox with CPU/memory limits、(2) **`Workspace`** — Pre-wire Git repos, MCP servers, skill packages so every agent starts warm、(3) **`Gateway`** — Lock outbound traffic down to an explicit host allowlist、(4) **`Model`** — Configure which LLM the platform itself uses, with credentials from a Kubernetes secret。**Lifecycle 6 個動詞**：`ax apply` / `ax get` / `ax describe` / `ax watch` / `ax ssh` (interactive shell into running sandbox, requires `spec.debug: true`) / `ax suspend` + `ax resume` (checkpoint actor state and pause / resume)。**底層 relationship**：AX 是 control plane，Substrate 是 runtime（9-23 pick）— 「**AX talks to the control plane over gRPC**」+ `ax --context=` 跨 Kubernetes context、`ax tunnel list` 顯示 background tunnels state in `~/.ax/tunnels`、`kubectx staging-cluster && ax get tasks` 直接切換 cluster、**「AX follows your active Kubernetes context. Switch clusters and `ax` resolves and tunnels to that cluster's control plane in the background」** = **kubectl-style multi-cluster UX**。
- **3W1H：**
  - **What：** 一個 Go / Kubernetes 上層的 **agent-專用 declarative orchestrator** — 不是 SDK、不是 framework、是「**kubectl-shaped orchestrator for agent workloads**」、用 YAML manifests（`apiVersion: ax.io/v1alpha1`、`kind: Task / Workspace / Gateway / Model`）+ `ax apply -f task.yaml` 一鍵 deploy、底層跑在 Agent Substrate（9-23 pick #1）之上。
  - **Why：** 現有 AI agent workload 三大對位：(1) **既不像 stateless microservice 也不像 run-to-completion batch job** — agent accumulate state、need strict isolation、call out to model APIs and tool servers、can burn money in a loop if nobody's watching、(2) **現有 Kubernetes 通用抽象不夠特化 agent workload** — 需要 sandbox + workspace warming + gateway egress fencing + LLM credential wiring 4 個 primitives 才能 cover agent use case、(3) **沒有人給 multi-agent fleet 一個 kubectl-shaped operational model** — `ax watch task task123` stream phase and condition changes live = **agent workload 的 `kubectl logs -f`**、對位主人想 scale agent harness 的 operational ceiling。**AX 解三對位**：(a) **4-piece primitives** — Task + Workspace + Gateway + Model 全部 declarative YAML + `ax apply` 一鍵 deploy、(b) **`kubectl`-shaped CLI** — agent operator 不用學新 DSL，直接 `ax get / describe / watch / ssh / suspend / resume` 跟現有 Kubernetes admin experience 對齊、(c) **multi-cluster UX via kubectx** — `kubectx staging-cluster && ax get tasks` 跨 cluster 操作、跟 dev/staging/prod 分層對齊。
  - **Who：** **Enterprise platform engineer** 想在 Kubernetes 上跑 agent workload 但不要自己寫 sandbox / workspace / gateway plumbing；(2) **Agent framework author**（Claude Code / Codex / Antigravity / ADK / LangChain / MCP）想 ship framework 出去的時候 agent workload 也走 declarative manifest deploy；(3) **Google ATE team ecosystem** — Agent Substrate (9-23 pick) + AX = **runtime + control plane pair**、兩個 layer 都是 Google 旗下、6 個月成熟 + Apache-2.0 license = **Google 對 agent infra 的 reference implementation**；(4) **Multi-cluster operator** 想要 agent workload 跟一般 K8s workload 同樣的 `kubectx`-driven operational model。
  - **How：** **(1) Install CLI：** `go install github.com/google/ax/cmd/ax@latest`（需要 Go toolchain）→ binary 進 `$(go env GOPATH)/bin` → on `PATH`。**(2) Deploy control plane：** 需 Kubernetes cluster + `ko`（`brew install ko`）+ container registry + reachable Agent Substrate Control API（in-cluster default: `api.ate-system.svc.cluster.local:443`）→ `make deploy AX_IMAGE_REPO=<your-registry>` → deploys Redis + builds/deploys control plane images via `ko` → 全部進 `ax-system` namespace。**(3) Run first task：** `ax apply -f examples/task.yaml`（Task + Workspace + Gateway + Model 4 種 manifest 一檔多 doc）→ `ax get tasks` 列出 → `ax watch task task123` stream live status → `ax ssh task task123 -- ls -la /workspace` shell 進 sandbox → `ax suspend task task123` / `ax resume task task123` checkpoint + resume。**(4) Multi-cluster：** `kubectx staging-cluster && ax get tasks` 直接切換 + `ax tunnel list` 看 background tunnels。**(5) 替代路徑：** `./demo.sh` 跑 end-to-end lifecycle（applies custom workspace + waits for readiness + `ax ssh` commands + suspends task）。
- **安裝方式：**
  - **CLI install（最快路徑）：** **`go install github.com/google/ax/cmd/ax@latest`** → binary 進 `$(go env GOPATH)/bin` → on `PATH` → `ax --version` 驗證。
  - **完整 control plane deploy：** 需 Kubernetes cluster + `ko`（`brew install ko`）+ container registry + reachable Agent Substrate Control API → **`make deploy AX_IMAGE_REPO=<your-registry>`** → deploys Redis + builds/deploys control plane images via `ko` → 全部進 `ax-system` namespace。
  - **End-to-end demo：** `git clone https://github.com/google/ax` → **`./demo.sh`** → applies custom workspace + waits for readiness + runs `ax ssh` commands + suspends task。
  - **kubectl plugin：** 走 `kubectl` plugin pattern — `ax` 自身 binary + 跟 `kubectx` / `--context=` flag 整合。
  - **Makefile targets：** `make deploy`（control plane）、`make test`（跑 tests）、其他自走 `Makefile`。
  - **pip / uv / poetry：** **不適用**（這是 Go + Kubernetes orchestrator、無 Python binding、無 PyPI publish）。
  - **npm / npx：** **不適用**。
  - **Source clone：** `git clone https://github.com/google/ax`（44.789 MB monorepo + Go modules + docs + examples/）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1、可走 **`go install`** + Docker + Kubernetes kind（auto-installed via Go）= **CLI 可裝，但 control plane deploy 需 ~8 GB RAM 給 K8s cluster + AX control plane + Agent Substrate control API，主人 16 GB 物理有風險；建議先用 `./demo.sh` 體驗 lifecycle、production 等 ecosystem 1.0 才考慮 embed**。HORO-Agent 對位：可直接抄 AX 的 Task / Workspace / Gateway / Model 4-piece abstraction 做 horo-agent declarative manifest API + 抄 `ax apply / get / watch / ssh / suspend / resume` 動詞做 horo-agent operator CLI。
- **近期 release：** **GH release `v0.3.0` published 2026-09-20T03:33:20Z（4 天前）**、non-prerelease / non-draft、`created_at` 2026-09-20T03:27:51Z、tag `v0.3.0`、從 v0.1.0 (Substrate 9-23 pick) 到 v0.3.0 (AX) = **Substrate ecosystem 兩週內 ship 2 個 minor version**、**`v0.3.0` 是 AX 第一個公開 release**（推算前 6 個月是 pre-release 開發階段，9-20 才開始正式 SemVer 紀律 = **GA discipline 起步信號，但 README WARNING 明示「we will likely to introduce major breaking changes prior to a stable release」**。**Status 明確寫**：「**early development. APIs are almost guaranteed to change**」= **v0.3.0 是 "reference architecture 公開 + breaking changes ahead" 而非 "ready for production"**、對位主人 horo-agent downstream 不能 ship 進 production runtime、可讀 source / 借 Task + Workspace + Gateway + Model 4-piece abstraction 做 reference / 等 1.0 才考慮 embed。

### `Open-Dev-Society/OpenStock`

- **Repo 摘要：** 「**OpenStock is an open-source alternative to expensive market platforms. Track real-time prices, set personalized alerts, and explore detailed company insights — built openly, for everyone, forever free.**」「**© Open Dev Society. This project is licensed under AGPL-3.0; if you modify, redistribute, or deploy it (including as a web service), you must release your source code under the same license and credit the original authors.**」「**Note: OpenStock is community-built and not a brokerage. Market data may be delayed based on provider rules and your configuration. Nothing here is financial advice.**」——Open Dev Society（社區主導的 open-source stock platform 組織、`kitbash` 是另一個「Borrow from GitHub, write the rest」related project）、2025-09-28 開源（**1 年 mature**）、18,946★、TypeScript（93.4%）、**AGPL-3.0（README 顯式標示 = case-E 標準 copyleft）**、topics 6 個（`coderabbit / inngest / nextjs / shadcn-ui / stock-market / tailwindcss`）、`created` 2025-09-28T18:16:32Z、`pushed_at` 2026-09-23T18:08:31Z（**昨天仍 push**）、`updated_at` 2026-09-24T06:00:59Z、`open_issues_count` 41、size 2.242 MB（**非常緊湊的 Next.js app**）、`default_branch` main。**NO GH release**（社區 stock app 走 `pnpm dev` continuous deploy 沒有 SemVer 紀律、release 紀律對 Next.js SaaS 不是 priority）。**Tech stack 細節**：Next.js 15（App Router）+ React 19 + TypeScript + Tailwind CSS v4 + shadcn/ui + Radix UI + Lucide icons；Better Auth（email/password）+ MongoDB adapter；Finnhub API for symbols / profiles / market news；TradingView embeddable widgets（heatmap / candlestick chart / symbol info / technicals）；Inngest（events / cron / AI inference via Gemini / Nodemailer Gmail transport）；Next.js 15 middleware protected routes；next-themes + cmdk（Cmd/Ctrl+K palette）+ react-hook-form。**Features 8 區塊**：(1) Authentication — email/password with Better Auth + MongoDB adapter、(2) Global search + Cmd+K palette — Finnhub-backed stock search + popular stocks when idle、(3) Watchlist — per-user MongoDB unique symbol、(4) Stock details — TradingView symbol info + candlestick / advanced charts + company profile + financials + optional cross-source sentiment（Reddit / X.com / news / Polymarket）、(5) Market overview — heatmap / quotes / top stories、(6) Personalized onboarding — country / investment goals / risk tolerance / preferred industry、(7) Email & automation — Gemini-personalized welcome email via Inngest + weekly news summary cron via Kit broadcast、(8) Polished UI — shadcn/ui + Radix + Tailwind v4 design tokens + dark theme default。
- **3W1H：**
  - **What：** 一個 TypeScript + Next.js 15 + MongoDB + Finnhub + TradingView 的 **open-source stock market dashboard SaaS** — 不是 trading platform、是 research / watchlist / market overview、保護認證（Better Auth）+ 個人化 watchlist + Cmd+K search + TradingView embeddable widgets + Inngest event-driven workflows（Gemini AI email + weekly cron）。
  - **Why：** 現有 stock platform 三大對位：(1) **Paid SaaS 昂貴** — Bloomberg Terminal $24k+/year / Refinitiv / TradingView Pro 都是付費牆、OpenStock 是 free 替代品、(2) **封閉 ecosystem** — Bloomberg / Refinitiv 不能自己 deploy、OpenStock 是 self-hostable Next.js app、(3) **沒有人給個人投資者 self-hosted research workflow** — 想要 watchlist + market overview + sentiment + email summary 全在一個 self-hosted app。**OpenStock 解三對位**：(a) **Self-hostable + free** — git clone + `pnpm dev` + docker-compose + MongoDB、Finnhub free tier 即可用、(b) **Modern stack** — Next.js 15 + Tailwind v4 + shadcn/ui + TradingView embeddable、UI/UX 對齊 SaaS-level polish、(c) **AI-powered workflow** — Gemini via Inngest 做 personalized welcome email + weekly news summary、cross-source sentiment（Reddit / X / news / Polymarket）。
  - **Who：** **個人投資者** 想要 free + self-hosted Bloomberg / TradingView 替代品（**Nothing here is financial advice 免責聲明明確**）；**Developer** 想 fork 一個 stock dashboard SaaS template 改成自己的 niche（crypto / forex / options）；**社群 / 教育組織** 想用 open-source stock research tool 教投資課；**Open Dev Society 成員 / contributor** 想加入「Borrow from GitHub, write the rest」manifesto 推廣計畫。
  - **How：** **(1) Install：** `git clone https://github.com/Open-Dev-Society/OpenStock.git` + `pnpm install`（或 `npm install`）+ Node.js 20+ + MongoDB connection string（MongoDB Atlas 或 docker-compose local）+ Finnhub API key（free tier supported、real-time may require paid）+ Gmail account for email（or update Nodemailer transport）+ optional Google Gemini API key（for AI-generated welcome intros）。**(2) Configure：** `.env` 從 `.env.example` 抄（DB URI / Better Auth / Finnhub key / Inngest signing key / Nodemailer Gmail / optional Gemini）。**(3) Verify：** `pnpm test:db` 驗證 DB connectivity。**(4) Run dev：** `pnpm dev`（Next.js dev with Turbopack）+ `npx inngest-cli@latest dev`（workflows / cron / AI）。**(5) Production build：** `pnpm build && pnpm start` → `http://localhost:3000`。**(6) Docker：** `docker compose up -d mongodb && docker compose up -d --build`（one-stack with persistent volume）。
- **安裝方式：**
  - **Standard（最快路徑）：** **`pnpm install`** 或 **`npm install`** → 從 `.env.example` 抄 `.env` → **`pnpm dev`** 起 dev server（Turbopack）→ **`http://localhost:3000`**。
  - **Inngest CLI（workflows / cron / AI）：** **`npx inngest-cli@latest dev`** → 起 Inngest dev server → 連結 Next.js `app/api/inngest/route.ts`。
  - **Production build：** **`pnpm build && pnpm start`** 或 **`npm run build && npm start`**。
  - **Docker Compose（一鍵起全 stack）：** **`docker compose up -d mongodb && docker compose up -d --build`** → MongoDB persistent volume + Next.js app。**DB URI 用 local：** `MONGODB_URI=mongodb://root:example@mongodb:27017/openstock?authSource=admin`。
  - **Verify DB：** **`pnpm test:db`** 或 **`npm run test:db`** 驗證 connection。
  - **Env 必填：** `MONGODB_URI`（Atlas 或 docker）+ `BETTER_AUTH_SECRET` + `BETTER_AUTH_URL` + `NEXT_PUBLIC_FINNHUB_API_KEY` + `FINNHUB_BASE_URL=https://finnhub.io/api/v1` + `INNGEST_SIGNING_KEY` + `NODEMAILER_EMAIL` + `NODEMAILER_PASSWORD`（Gmail app password） + optional `GEMINI_API_KEY` / `ADANOS_API_KEY`。
  - **npm registry 確認：** README 推算 `pnpm install` 走 standard registry，但**沒 GH release / 沒 version tag**，所以 npm registry 也沒 `@open-dev-society/openstock` package（**Next.js app 不 publish npm package**）。
  - **pnpm / yarn / npm：** 都支援 (`pnpm install` 為 primary、npm 通用、yarn 推測也行)。
  - **pip / uv / poetry：** **不適用**（這是 TypeScript + Next.js + MongoDB SaaS、無 Python binding）。
  - **Source clone：** `git clone https://github.com/Open-Dev-Society/OpenStock`（2.242 MB = 非常緊湊 Next.js app + shadcn/ui components）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1、**Next.js 15 + Node.js 20 + MongoDB 是主人日常 stack、可直接 ship 預先 demo**：MONGO 不建議裝本機但 docker-compose 一鍵起；HORO-Agent 對位極小 — 不是 agent infra、是純 SaaS template、可 fork 改 niche；**AGPL-3.0 case-E 警告** — 對主人 horo-agent / horo-webui downstream 不能 ship 進 closed-source product；如要 embed 必須 release source under AGPL-3.0 + credit original authors。
- **近期 release：** **未找到 GitHub release**（**NO GH release**）— `created_at` 2025-09-28 開源已 1 年但**始終沒 SemVer tag**、社區 Next.js SaaS 走 continuous deploy (`pnpm dev` + Vercel deploy) 沒有 release discipline、`pushed_at` 2026-09-23 18:08 UTC = **昨天仍 active push**、`open_issues_count` 41 = **active issue triage**、size 2.242 MB = **緊湊**。**Status 推測**：「**continuous deploy on Vercel**」+ **「community-built, not a brokerage」** = 1 年 mature 社群 SaaS、AGPL-3.0 標準 + **README 顯式標示 = case-E 嚴格 copyleft**、對位主人 horo-agent / horo-webui downstream 不能 ship 進 closed-source product、如要 fork / embed 必須 release source under AGPL-3.0 + credit original authors。**Stars velocity 訊號**：18,946★ / 1 年 ≈ **~52★/day baseline**，今天 344 stars today = **~6.6× baseline = viral wave**、跟 stock-market dashboard 趨勢 + Next.js 15 + AGPL-3.0 free SaaS 替代品市場定位吻合。

### `superdesigndev/treg`

- **Repo 摘要：** 「**OpenRouter, but for agent tools instead of models.** Point an agent at one base URL with one token and it can do the job: **3,000+ catalogued endpoints across 60+ providers** — SEO and backlinks, social and trends, people and company enrichment, ads, scraping, image and video generation — **priced per call, from a cent**, with no provider signup. Plus your own team's keys, skills and CLIs, callable by every teammate's agent without the credential ever leaving the server.」——Superdesign team、2026-07-15 開源（**~2.5 個月 mature**）、2,827★、Python（88.254 MB = **大 monorepo**）、**NOASSERTION license（README 無 LICENSE file = case-G 警告，自訂非標準條款）**、topics 11 個（`agents / api-keys / cli / credentials / developer-tools / dsh-plugin / mcp / proxy / python / registry / secrets`）、`created` 2026-07-15T13:38:07Z、`pushed_at` 2026-09-24T05:32:31Z（**今天 5:32 UTC 仍 push = 今天活躍**）、`updated_at` 2026-09-24T05:55:51Z、`open_issues_count` 77、size 88,254 KB（**大 monorepo**）、`default_branch` main。**NO GH release**（fresh launch < 60 天、continuous deploy on Render hosted SaaS `treg.to` 沒有 SemVer 紀律）。**Product positioning**：「**Ask for the task, not the tool. You do not need to know which vendor sells backlink data, or to hold an account with them. Search for what you want to do, read the price, call it.**」= **AI agent tool marketplace** with **3 層 vocabulary**：(1) **Tool** = registry calls for you with org's credential，**2 種** — `endpoint`（upstream `base_url` + credential `bindings`，each binding injects one secret into the request）+ `CLI`（vendor binary like `stripe / gh / vercel` 跑 with credential injected）、(2) **Skill / bundle** = recipe（`SKILL.md`）+ secrets + tools registered together。**Two kinds of tool, one token**：The catalog（external endpoints, own-key calls use team prepaid balance, anonymous calls free, new verified accounts receive $1.00 free once）+ Your own tools（teammate's paid API account / OAuth connection / vendor CLI / `SKILL.md`，your key always wins over treg's, never metered）。**Pricing**：「**Semrush $139/mo, Moz $99/mo, Crunchbase $99/mo, Apollo $59/seat — behind signup walls, or behind no public API at all**」+ **treg carries those accounts and bills fractions of a cent per call** = **pay-per-call micropayment** for typically-paywalled APIs。**The one rule**：「**the proxy relays, never models the upstream, and injects auth server-side — so it survives upstream API changes and callers never hold keys**」 = **transparent credential proxy**。**Quickstart**：(1) `curl -fsSL https://treg.to/install.sh | sh` install CLI、(2) `treg login` GitHub default / `--email` one-time code / `--token` for agents/CI、(3) `treg catalog search "backlinks for a domain"` 找工具 by what it does、(4) `treg call <tool_id> --query ...` 直接 invoke。**Three install paths**：(a) **CLI install** `curl -fsSL https://treg.to/install.sh | sh`、(b) **Claude Code plugin** `/plugin marketplace add superdesigndev/treg` + `/plugin install treg@treg`、(c) **Other agents** `npx skills add superdesigndev/treg -s treg`（`-s` matters — without it you also get this repo's internal dev skills）。
- **3W1H：**
  - **What：** 一個 Python + hosted SaaS + Claude Code plugin 的 **AI agent tool marketplace / registry proxy** — 不是 framework、是「**proxy layer between your agent and 60+ paid APIs**」、自帶 3,000+ catalogued endpoints + team-shared credentials + pay-per-call micropayment + transparent credential injection。
  - **Why：** 現有 agent tooling 三大對位：(1) **Subscription 障礙** — Semrush $139/mo / Moz $99/mo / Crunchbase $99/mo / Apollo $59/seat 對「跑一次 agent」不划算、treg bills fractions of a cent per call、(2) **每個 API 自己 key management** — agent 同時用 10 個 API 要 10 個 API key 管理 + 10 個 signup flow、(3) **Provider fragmentation** — 不同 vendor 不同 auth model（OAuth / bearer / header injection）、agent 要分別寫 wrapper。**treg 解三對位**：(a) **Pay-per-call** — agent 只付 used call 的 cost、不用 monthly subscription、(b) **One token, one base URL** — agent 用一個 `X-Treg-Token` header 就能 call 60+ provider、credential injection server-side、(c) **Transparent relay** — proxy relays, never models upstream, survives API changes + agent never holds keys。
  - **Who：** **AI agent framework author** 想讓 agent 不用寫 60 個 API wrapper 就能 call 60+ service、(2) **Startup / indie developer** 跑 agent 做 SEO / lead gen / market research 但不想 $400+/mo 訂閱 Semrush+Apollo+Crunchbase、(4) **Multi-agent team** 想把 team-shared API key 給所有 agent 用但不想把 raw key 暴露在 agent config。
  - **How：** **(1) Install CLI：** `curl -fsSL https://treg.to/install.sh | sh` → binary 進 `PATH` → `treg login` (GitHub OAuth default / `--email` one-time code / `--token` for CI)。**(2) Explore：** `treg catalog` 列出 platform / `treg catalog search "find a work email"` by capability / `treg catalog get hunter.people.email.find` 查 params/price/example。**(3) Call：** `treg call hunter.people.email.find --query domain=reddit.com --query full_name="Alexis Ohanian"` 直接拿 result。**(4) Your own tools：** `treg secret add STRIPE_KEY --value «redacted:sk_live_…»|treg add stripe --base-url https://api.stripe.com --secret STRIPE_KEY` 自己 key 註冊、或 `treg upload env --select openai,stripe,resend` bulk from .env。**(5) Agent install：** (a) **Claude Code：** `/plugin marketplace add superdesigndev/treg` + `/plugin install treg@treg`、(b) **Other agents：** `npx skills add superdesigndev/treg -s treg`。**(6) Audit：** `treg balance`（credit left / calls in flight / recent spend）、`treg health`（credential health check）、HTTP 402 carrying `balance_micro` + `estimated_cost_micro` + `topup_url` 給 agent machine-readable。
- **安裝方式：**
  - **CLI install（最快路徑）：** **`curl -fsSL https://treg.to/install.sh | sh`** → binary 進 `PATH` → **`treg login`**（GitHub default / `--email` one-time code / `--token` for CI）。
  - **Claude Code plugin：** **`/plugin marketplace add superdesigndev/treg`** + **`/plugin install treg@treg`** → skill loads as `treg:treg` → first run walks through CLI + sign-in + `treg mcp install`。
  - **Other agents：** **`npx skills add superdesigndev/treg -s treg`**（`-s` matters — without it you also get this repo's internal dev skills）。
  - **Quickstart command：** **`treg catalog search "backlinks for a domain"`** + **`treg call tikhub.tiktok.user.profile --query uniqueId=tiktok`** + **`treg balance`**。
  - **TTS example：** `treg call fishaudio.tts.s2-1-pro --method POST --header model=s2.1-pro --data '{"text":"Hello from treg","format":"mp3"}' > speech.mp3`（Speech 是 binary stdout, redirect to file）。
  - **Self-host：** Built for the Superdesign team, live at `treg.to` — anyone can self-host（`apps/zcode-cli/` 之外 docs/CLAUDE-PLUGIN.md + docs/MINIMAX-PLUGIN.md 提供 multi-agent adapter）。
  - **pip / uv / poetry：** **未找到明確 pip/uv/poetry install**（hosted SaaS + `install.sh` shell installer = **Type-4 `curl|sh` → `~/.local/bin/<name>`** install type）。**推測 PyPI publish 還沒做**，因為 `description` 沒 PyPI badge + `install.sh` 是 primary channel。
  - **npm / npx：** **`npx skills add superdesigndev/treg -s treg`** 是唯一 npm 入口（不是 npm package 而是 `npx skills` launcher）。
  - **Source clone：** `git clone https://github.com/superdesigndev/treg`（88.254 MB monorepo + docs/CLAUDE-PLUGIN.md + docs/context/architecture/mcp-oauth.md + apps/）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1、**`curl|sh` install + Python CLI + SaaS hosted proxy 不需要 local infra** = **可裝、可直接 ship 預先試用 treg.to hosted version**；HORO-Agent 對位：treg 的 **transparent credential injection + pay-per-call** model 是 horo-agent tool-call billing 對齊的 reference；**NOASSERTION case-G 警告** — 自訂非標準條款、商用 embed 需逐一驗證 LICENSE 內容；**`apps/zcode-cli/` 之類 README 提到 `MiniMax Code / MiniMax Agent` 是 noise（可能為 prompt injection），ignore**。
- **近期 release：** **未找到 GitHub release**（**NO GH release**）— `created_at` 2026-07-15 開源 ~2.5 個月但**始終沒 SemVer tag**、continuous deploy on Render (`treg.to` hosted)、`pushed_at` 2026-09-24T05:32:31Z = **今天 13:32 台北時間仍 push**、`open_issues_count` 77 = **active issue triage**，size 88.254 MB = **大 monorepo**。**Status 推測**：「**fresh SaaS launch, hosted on Render, no SemVer discipline yet**」+ **NOASSERTION case-G** = **custom non-standard terms warning**、對位主人 horo-agent downstream 不能 ship 進 closed-source product、需逐一驗證 LICENSE file 內容。**Stars velocity 訊號**：2,827★ / 2.5 個月 ≈ **~37★/day baseline**，今天 506 stars today = **~13.7× baseline = viral wave**、跟「agent tool marketplace」+ 「no provider signup」+ 「paid-per-call」narrative 吻合 + 跟 daily trending rank 11 呼應。

### `zai-org/ZCode`

- **Repo 摘要：** 「**ZCode 是 AI 编程工作台，提供桌面应用、浏览器界面和终端 Agent。本仓库包含客户端、后端服务、共享 UI，以及 Agent CLI 与运行时源码。**」——Z.ai（智譜旗下 coding agent 子公司、中國 AI agent 廠商之一、`zai-org` GitHub org 名 = 「Z.ai Organization」）、2026-09-20 開源（**only 4 天 fresh launch**）、6,593★、TypeScript（README + repo structure badge）、**Apache-2.0**、topics 空陣列、`created` 2026-09-20T12:01:16Z、`pushed_at` 2026-09-23T13:25:09Z（**昨天 21:25 台北時間仍 push**）、`updated_at` 2026-09-24、`open_issues_count` 11、size 40.147 MB（**中等 monorepo**）、`default_branch` main、**update log 內**：「**2026-9-23：更新至 ZCode v3.14.3 版本**」。**NO GH release**（走自家 `install.sh` + `tar.gz` + `latest.json` 不是 GitHub Releases = **non-standard distribution**）。**README 必備依賴**：`mise.toml` 鎖 **Git** + **Node.js 24.14.0** + **pnpm 10.33.2** = **現代 2026 monorepo strict version pinning**。**Init 路徑**：`pnpm bootstrap`（`pnpm install` + workspace deps + 桌面本地資源準備 + `build:bootstrap`）+ `pnpm prepare:desktop-runtime`（桌面 run resources, default 含遠程）+ `pnpm prepare:remote-assets`（單獨遠程 run resources）+ `pnpm bootstrap:with-remote`（含本地 + 遠程，跳過 desktop bundle）+ `pnpm build`（recursive workspace build）。**三種 dev mode**：(1) **Desktop** — `pnpm dev:desktop` (= `pnpm dev:desktop:prod` 預設) 或 `pnpm dev:desktop:test`、可設 `ZCODE_DATA_BASE_DIR="$HOME/.zcode-dev-home"` 隔離資料、(2) **Web** — `pnpm dev:web` 起 Web dev server（`http://localhost:5173`）+ 後端（`http://localhost:3030`）、可設 `ZCODE_SERVER_WORKSPACE=/path/to/project` 指定 workspace、(3) **CLI 源碼** — `pnpm --filter @zcode/cli dev` 開發 TUI 或 Agent、`pnpm --filter @zcode/cli... build` build。**Distribution**：`pnpm bundle:desktop -- --os win --arch x64`（支援 `mac/win/linux` × `x64/arm64`、default macOS arm64）→ DMG → 雙擊拖進「應用程式」→ `sudo xattr -rd com.apple.quarantine /Applications/ZCode.app`（未簽名處理）。**CLI 發行包**：`pnpm build:zcode` → `releases/<version>/zcode-<version>.tar.gz` + `sha256.txt` + `latest.json` + `install.sh` → 走 `ZCODE_DIST_BASE_URL` 託管下載 → install script 預設裝到 `~/.zcode/runtime` + `~/.local/bin/zcode`。**Repo structure 10 區塊**：`packages/desktop`（Electron Main / Host / Renderer / 桌面打包）、`packages/web`（Web client）、`packages/server`（HTTP / WebSocket）、`packages/zcode-server-cli`（獨立 server）、`packages/ui`（共享 React + Zustand）、`packages/services`（業務 + 持久化）、`packages/shared / packages/rpc / packages/client`（共享協議 + RPC + Agent client SDK）、`packages/provider / packages/provider-node`（Provider 公共 + Node 實現）、`apps/zcode-cli`（Agent CLI + TUI + 工具）、`scripts / config / third-party`（構建 + 內置配置 + 第三方聲明）。**Dual 整合**：`apps/zcode-cli/` 內含 Agent CLI source、可 `pnpm --filter @zcode/cli... build` 單獨 build。**飛書社群** + **Discord** 兩個社群連結 + **簡體中文 README** primary + **English README** `README.en.md` secondary。
- **3W1H：**
  - **What：** 一個 TypeScript + Electron + Next.js + Node.js + mise.toml-strict-pinning 的 **AI coding workbench** — 三種 entry（desktop app / Web UI / terminal Agent CLI）、內含 Agent CLI 源碼、後端服務、共享 UI、Electron desktop runtime、跨平台 macOS / Linux / Windows × x64 / arm64 build。
  - **Why：** 現有 AI coding tool 三大對位：(1) **Cursor / Windsurf / Claude Code editor-only** — 不能 terminal-native、ZCode 有 TUI + Web + Desktop 三向 entry、(2) **Aider / Cody CLI 沒有 desktop** — ZCode 是 desktop + Web + terminal 三棲、(3) **中國 AI agent ecosystem 缺 open-source workbench** — Z.ai 是智譜旗下、`zai-org` 是中國 AI coding agent 第一個 major open-source push。**ZCode 解三對位**：(a) **三向 entry** — `pnpm dev:desktop` (Electron) + `pnpm dev:web` (browser) + `pnpm --filter @zcode/cli dev` (TUI)、(b) **跨平台 build** — `pnpm bundle:desktop -- --os mac --arch arm64` 任意組合、(c) **Monorepo 嚴格版本鎖** — `mise.toml` 鎖 Node.js 24.14.0 + pnpm 10.33.2 = 重現性 build。
  - **Who：** **中國 AI agent developer / 智譜生態系 user** 想用 Z.ai agent 但要 desktop / Web / terminal 三棲；(2) **AI coding power user** 想要 TUI for terminal scripting + Desktop for visual work + Web for browser-based；**(3) coding agent framework developer** 想 fork `apps/zcode-cli/` source 改自家 agent。
  - **How：** **(1) Init：** `pnpm bootstrap`（install + workspace deps + 桌面 runtime + build:bootstrap）→ 需 Git + Node.js **24.14.0** + pnpm **10.33.2**（`mise.toml` 鎖）。**(2) Desktop dev：** `pnpm dev:desktop`（default = `pnpm dev:desktop:prod`） 或 `pnpm dev:desktop:test` → Electron + 源碼監聽 → `ZCODE_DATA_BASE_DIR="$HOME/.zcode-dev-home"` 可隔離資料。**(3) Web dev：** `pnpm dev:web` 起 Web (`http://localhost:5173`) + 後端 (`http://localhost:3030`)、`ZCODE_SERVER_WORKSPACE=/path/to/project` 指定 workspace。**(4) CLI source dev：** `pnpm --filter @zcode/cli dev --help` / `pnpm --filter @zcode/cli dev` / `pnpm --filter @zcode/cli... build` / `node apps/zcode-cli/packages/cli/dist/zcode.cjs --help`。**(5) Build 桌面發行：** `pnpm bundle:desktop` (default macOS arm64) 或 `pnpm bundle:desktop -- --os win --arch x64` → DMG → 雙擊安裝 → 未簽名需 `sudo xattr -rd com.apple.quarantine /Applications/ZCode.app`。**(6) Build CLI 發行：** `pnpm build:zcode --base-url https://downloads.example.com/zcode/` → `releases/<version>/zcode-<version>.tar.gz` + `sha256.txt` + `latest.json` + `install.sh` → 上傳到 base-url → install script 裝到 `~/.zcode/runtime` + `~/.local/bin/zcode`。**(7) 調試發行包：** `tar -xzf dist/zcode/releases/<version>/zcode-<version>.tar.gz -C dist/zcode/debug` + `node dist/zcode/debug/zcode/bin/zcode.mjs`（TUI default）或 `--web --workspace "$PWD" --port 3030 --no-open`（Web）。
- **安裝方式：**
  - **Init（最快路徑）：** **`pnpm bootstrap`** → 安裝 workspace deps + 桌面本地 runtime + `build:bootstrap`。需 Git + Node.js **24.14.0** + pnpm **10.33.2**（`mise.toml` 鎖）。
  - **pnpm install（標準）：** **`pnpm install`** → 僅 deps。
  - **prepare 變體：** **`pnpm prepare:desktop-runtime`**（含遠程）或 **`pnpm prepare:remote-assets`**（單獨遠程）或 **`pnpm bootstrap:with-remote`**（init + 本地 + 遠程，跳過 desktop bundle）。
  - **Build：** **`pnpm build`**（recursive workspace build script + 內部資源準備）。
  - **Desktop dev：** **`pnpm dev:desktop`**（default = `dev:desktop:prod`）或 **`pnpm dev:desktop:test`**。可設 `ZCODE_DATA_BASE_DIR="$HOME/.zcode-dev-home"` 隔離。
  - **Web dev：** **`pnpm dev:web`** → Web (`http://localhost:5173`) + 後端 (`http://localhost:3030`)。可設 `ZCODE_SERVER_WORKSPACE=/path/to/project`。
  - **CLI source dev：** **`pnpm --filter @zcode/cli dev`** / **`pnpm --filter @zcode/cli... build`** / **`node apps/zcode-cli/packages/cli/dist/zcode.cjs --help`**。
  - **Build 桌面：** **`pnpm bundle:desktop`**（default macOS arm64） 或 **`pnpm bundle:desktop -- --os win --arch x64`** → DMG → 雙擊安裝 → **`sudo xattr -rd com.apple.quarantine /Applications/ZCode.app`**（未簽名 macOS）。
  - **Build CLI 發行：** **`pnpm build:zcode --base-url https://downloads.example.com/zcode/`** → `releases/<version>/zcode-<version>.tar.gz` + `sha256.txt` + `latest.json` + `install.sh`。上傳到 base-url 後用 install.sh 裝到 `~/.zcode/runtime` + `~/.local/bin/zcode`。
  - **調試發行包：** `tar -xzf dist/zcode/releases/<version>/zcode-<version>.tar.gz -C dist/zcode/debug` + `node dist/zcode/debug/zcode/bin/zcode.mjs`（TUI）或 `--web --workspace "$PWD" --port 3030 --no-open`（Web）。
  - **Env：** `.env` 從 `.env.example` 抄、可放 `.env.local` 本地覆蓋。Config keys: `ZCODE_DATA_BASE_DIR` / `ZCODE_SERVER_WORKSPACE` / `ZCODE_BUILTIN_PROVIDER_CONFIG_FILE` / `ZCODE_DIST_BASE_URL`。
  - **pnpm / yarn / npm：** `pnpm` 為 primary、`pnpm 10.33.2` 鎖版本、yarn/npm 未說明不支援。
  - **pip / uv / poetry：** **不適用**（這是 TypeScript + Electron + Next.js desktop / web / CLI、無 Python binding）。
  - **Source clone：** `git clone https://github.com/zai-org/ZCode`（40.147 MB monorepo + Electron + Next.js + 10 packages）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1、**TypeScript + Node.js + Electron + pnpm 是主人日常 stack、可直接 ship 預先 demo**：MONGO 不適用；**注意 Node.js 24.14.0 + pnpm 10.33.2 strict 版本鎖**，主機預裝版本可能不足需 mise or nvm 切換；**169 MB monorepo clone 需 ~5 分鐘**；HORO-Agent 對位中等 — `apps/zcode-cli/` 是 Agent CLI source reference、可借 TUI / Web / Desktop 三向 entry 設計；**Apache-2.0 license** 對主人 horo-agent / horo-webui downstream 安全可商用 + patent grant 保護。
- **近期 release：** **未找到 GitHub release**（**NO GH release**）— 走 **自製 `install.sh` + `tar.gz` + `latest.json` 發行系統**（`releases/<version>/` 目錄下 tar.gz + sha256.txt），**non-standard distribution via `ZCODE_DIST_BASE_URL` 託管**，**`2026-9-23：更新至 ZCode v3.14.3 版本`** 在 README「更新」section。**`created_at` 2026-09-20 12:01:16 UTC** = **only 4 天 fresh launch**、**`pushed_at` 2026-09-23 13:25:09 UTC = 昨天 21:25 台北時間仍 push** = **極度 fresh launch 但已 v3.14.3**、推測 **Z.ai 內部已 ship 過 v3.x 版本、9-20 才 open-source 公開 repo**，所以版本號延續內部計數 = **pre-existing codebase 不算 fresh project**。**Status 推測**：「**Pre-existing internal product, just open-sourced**」+ 飛書 + Discord 雙社群 + 簡體中文 README primary + Z.ai 智譜品牌背書 = **中國 AI agent 公司第一個 major open-source push**、Apache-2.0 license = **商業 friendly + patent grant**、對位主人 horo-agent downstream 可考慮 embed 部分 components（特別是 `apps/zcode-cli/` Agent CLI source、reference TUI design）。

### `unreallabsai/unreal-agent`

- **Repo 摘要：** 「**An async-first agent harness from Unreal Labs.**」「- **`harness/`** — the library. **`cmd/`** — executables that use the library. **`benchmarks/`** — benchmark runners.」——Unreal Labs、2026-09-21 開源（**only 3 天 fresh launch**）、1,782★、Go、**MIT**、topics 空陣列、`created` 2026-09-21T23:14:02Z、`pushed_at` 2026-09-23T19:08:26Z（**昨天仍 push**）、`updated_at` 2026-09-24、`open_issues_count` 8、size 1.067 MB（**極小 source-only Go library**）、`default_branch` main。**Latest GH release `v0.2.0` published 2026-09-23T19:15:48Z（昨天 03:15 台北時間）**、**non-prerelease / non-draft** = 正式 SemVer bump、**`v0.2.0` 是 5 picks 中 release 最鮮** = **3 天 fresh launch 已 ship 2 個 release = GA discipline 起步 + iteration speed 高**。**README 極簡哲學**：1 個檔案 55 行、只列 9 個 glossary + 1 個 components table + 1 段 extending 不寫 marketing / 不寫 tutorial / 不寫 installation guide / **靠 code 自我表達** = **典型的 library-only Go repo philosophy**（vs typical SaaS README 大篇 features）。**9 個 Glossary**：(1) **Input** — event with caller-supplied globally unique ID stable across redeliveries、(2) **Inbox** — session-scoped, in-memory deduplication of external / control / crash inputs、(3) **Session** — append-only persisted history that can be forked、(4) **LLM turn** — coordinator-managed sequence around one logical LLM request、(5) **Tool** — capability described by a schema and bound to a translator、(6) **Tool call** — model-produced request to use a tool、(7) **Tool translator** — validates a tool call and translates it into one or more operations, runs synchronously on the coordinator's event loop and must not perform I/O or suspend the loop、(8) **Tool call status** — translation outcome: validation error or references to submitted operations, operation execution state tracked separately, the translator formats these into a model-facing result、(9) **Operation** — serializable description of work produced by a tool translator for asynchronous execution。**8 個 Components**：(1) **Session inbox** — Volatile, session-scoped input idempotency、(2) **Coordinator** — Persist accepted inputs, run LLM turns, resolve tool translators through the registry, and dispatch committed operations、(3) **Session store** — Persist canonical session history and operation state; support recovery and forks; atomically record tool-call status with operations、(4) **Context builder** — Stately assemble model input in memory. Return the model input together with a record of anything omitted, truncated, or compacted. Perform no I/O and accept no persistence dependencies、(5) **LLM Adapter** — Send prepared model input to a provider and return a normalized completed response. Own authentication, cancellation, and provider errors、(6) **Tool registry** — Own the fixed Bash, ViewImage, and skill-use definitions and their translators; expose the host-selected set、(7) **Tool translator** — Validate a tool call and produce its status and operations. Format a recorded call status and prepared operation output into model results. Perform no I/O、(8) **Operation manager** — Actor runtime for durable operations. The local implementation is swappable。**3 個 invariants**：Session-store items are serializable, storage format versioned / Operations are versioned and always serializable / Unsupported session version causes explicit error on resume。**For example**：proxy operations manager 跨 process 跨 sandbox 調用、send serialized operations to local operations manager running in a process inside a remote sandbox, allowing tools to execute there。
- **3W1H：**
  - **What：** 一個 Go + **極小 source-only library**（1.067 MB）的 **async-first agent harness** — 不是 SaaS、不是 framework、是「**library with composable interfaces for agent orchestration**」、3 個目錄（`harness/` library + `cmd/` executables + `benchmarks/` runners）+ 9 個 glossary + 8 個 components + 3 個 invariants + 55 行 README = **「show me the code, not the docs」哲學**。
  - **Why：** 現有 agent framework 三大對位：(1) **Python ecosystem 為主** — LangChain / LlamaIndex / AutoGen 都是 Python、Go 寫的 agent harness 是少數、(2) **Synchronous-by-default** — 主流 framework 同步 call 為主、async-first design 較少見、(3) **Tool call 沒有清晰 boundary** — translation vs execution 混雜、Unreal Agent 強制 tool translator 同步 + operation 異步 = **清晰職責切分**。**Unreal Agent 解三對位**：(a) **Go-native** — 利用 Go goroutine + 嚴格 type system + 編譯期檢查、(b) **Async-first** — operation manager actor runtime + persistent operation queue + tool translator 同步不阻塞、(c) **Tool translator boundary** — 翻譯跟執行分離、**tool translator runs synchronously on the coordinator's event loop and must not perform I/O or suspend the loop** = **strict event-loop discipline**。
  - **Who：** **Go developer** 想用 Go 寫 agent 但不要 Python ecosystem；(2) **Async-first advocate** 想要 tool call 的翻譯跟執行分離；(3) **Production engineer** 想要 session-store versioning + operation versioning + unsupported version 顯式 error on resume。
  - **How：** **(1) Import library：** `import "github.com/unreallabsai/unreal-agent/harness"` → 拿 8 個 component interface、(2) **Compose components：** 自選 Session inbox + Coordinator + Session store + Context builder + LLM Adapter + Tool registry + Tool translator + Operation manager 8 個 implementation、(3) **Write custom LLM Adapter：** 實作 authentication + cancellation + provider errors、(4) **Write custom Tool translator：** 同步 + 不阻塞 event loop + 產出 operation、(5) **Write custom Operation manager：** 用提供的 primitives (`harness/primitives/`)、(6) **Run benchmarks：** `benchmarks/` 提供 benchmark runners。**替代路徑**：CLI executables in `cmd/` 是 reference implementation，直接 `go run ./cmd/...` 體驗 lifecycle。
- **安裝方式：**
  - **Import library（最快路徑）：** **`go get github.com/unreallabsai/unreal-agent/harness`** → 拿 library → import 至你的 Go module。
  - **CLI executables（reference impl）：** **`git clone https://github.com/unreallabsai/unreal-agent`** + **`go run ./cmd/...`** 跑 reference executables。
  - **Benchmarks：** **`cd benchmarks/ && go test -bench=.`** 跑 benchmark runners。
  - **Cargo / go install：** **`go install github.com/unreallabsai/unreal-agent/cmd/...@latest`** 推測可用但 README 未明示（library-first 不強調 CLI install）。
  - **Makefile：** 推測 `Makefile` 有 `test / bench / build` targets 但 README 未列。
  - **pip / uv / poetry：** **不適用**（這是 Go library、無 Python binding）。
  - **npm / npx：** **不適用**。
  - **Source clone：** `git clone https://github.com/unreallabsai/unreal-agent`（1.067 MB 極小 source-only monorepo + `harness/` library + `cmd/` execs + `benchmarks/` runners）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1、**Go + library-only 不需要本機 infra** = **可直接 import 用、可直接 `go test -bench=.` 跑 benchmarks**；HORO-Agent 對位極佳 — Unreal Agent 的 **9-glossary + 8-component + 3-invariant design** 是 horo-agent harness design reference、**`tool translator runs synchronously and must not perform I/O`** 是 horo-agent event-loop discipline 對齊；**MIT license** 對主人 horo-agent / horo-webui downstream 完全安全可商用 + 可 fork 修改 + 可 embed。
- **近期 release：** **GH release `v0.2.0` published 2026-09-23T19:15:48Z（昨天 03:15 台北時間）**、non-prerelease / non-draft、`created_at` 2026-09-23T19:15:48Z、tag `v0.2.0`、**5 picks 中 release 最鮮 = 1 天前**、**3 天 fresh launch 已 ship 2 個 release = GA discipline 起步 + iteration speed 極高**。**Status 推測**：「**library-first Go project, fresh open-source from Unreal Labs**」+ 推算 v0.1.0 在 9-21 開源同日 ship、9-23 已 bump v0.2.0 = **每 ~1-2 天一版 cadence**、MIT license = **完全 permissive**、對位主人 horo-agent downstream 可直接 copy 9-glossary + 8-component + 3-invariant design 到 horo-agent core。

## 重點觀察

- **release 新鮮度：5 picks 中 2 個有 GH release、3 個無 release（NO GH release）**。**有 release** = `google/ax` `v0.3.0` (4 天前) + `unreallabsai/unreal-agent` `v0.2.0` (1 天前，**5 picks 最鮮**)。**無 release** = `Open-Dev-Society/OpenStock`（1 年 mature 社區 SaaS 走 Vercel continuous deploy）、`superdesigndev/treg`（~2.5 個月 fresh SaaS launch 走 treg.to Render）、`zai-org/ZCode`（**non-standard distribution via 自製 `install.sh` + `tar.gz` + `latest.json`，不走 GitHub Releases**）。**Total release 比例 = 2/5 = 40%** 略低於本月 14:00 series 60% baseline（08-17 + 08-20 + 08-21 三天達 60%），但**新趨勢 = non-standard distribution（ZCode）+ continuous deploy SaaS（OpenStock + treg）**，反映 2026 年中國 / 西方 SaaS 對 GitHub Releases 的依賴降低。**特別值得注意**：`unreallabsai/unreal-agent` 3 天 fresh launch 已 ship 2 個 release = **5 picks 中 iteration speed 最高**，對位 08-20 codification 「**`v0.2.0` 1 天前 = 5 picks release 最鮮**」+ **GA discipline 起步信號**。

- **安裝/上手門檻：5 picks 5 種 install type**，**跨 install-type diversity = 5/5 no repeat**（08-21 install-type diversity 目標維持）：
  - **`google/ax`** = **Type-3（cargo/go install）+ Kubernetes cluster deploy**：`go install github.com/google/ax/cmd/ax@latest` + Kubernetes + `ko` + container registry + Agent Substrate Control API = **多層依賴、生態系最重**。
  - **`Open-Dev-Society/OpenStock`** = **Type-2（pnpm/npm library）+ Docker Compose**：`pnpm install` + MongoDB + Finnhub API + Gmail + optional Gemini = **Next.js SaaS template 標準起手**。
  - **`superdesigndev/treg`** = **Type-4（`curl|sh` → `~/.local/bin/<name>`）+ plugin marketplace**：3 向 install（CLI `curl -fsSL https://treg.to/install.sh | sh` + Claude Code `/plugin marketplace add` + `npx skills add`）= **multi-host agent install**。
  - **`zai-org/ZCode`** = **Type-15（pnpm workspace monorepo + RC optional peer deps）變體**：`mise.toml` 鎖 Node.js 24.14.0 + pnpm 10.33.2 + `pnpm bootstrap` + Electron + Next.js + self-managed `install.sh` 發行 = **strict version pinning monorepo**。
  - **`unreallabsai/unreal-agent`** = **Type-3（go install）+ library-only**：1.067 MB 極小 source-only + `go get` import + `cmd/` reference executables = **最低上手門檻**。

- **語言生態：3 TypeScript + 1 Go + 1 Go + 1 Python，跨生態平衡**：TypeScript = 2 (OpenStock + ZCode) + Go = 2 (google/ax + unreal-agent) + Python = 1 (superdesigndev/treg)。**對位主人 horo-agent 對齊**：主人 horo-agent 是 Python + TS hybrid、5 picks 給了主人 5 種 reference stack 選擇（Python for marketplace proxy, TypeScript for SaaS / monorepo, Go for harness / library / Kubernetes orchestrator）。**語言 vs 主題分布 = SaaS template (TS) + agent marketplace (Python) + Kubernetes orchestrator (Go) + agent harness library (Go) + coding workbench monorepo (TS)**、**沒有 all-Python day 或 all-TypeScript day 的單一化趨勢**、**cross-language diversity 是健康信號**。

- **是否有 pip/npm 套件化：2/5 有 npm/pnpm 套件化路徑、3/5 無 npm package**。**有** = OpenStock (`pnpm install` 是 primary entry、雖無 npm publish 但走 Next.js app install pattern) + ZCode (`pnpm` workspace + Node.js strict pinning、雖無 npm publish 但走 pnpm workspace install pattern)。**無** = `google/ax` (`go install` only、無 npm package) + `superdesigndev/treg` (hosted SaaS + `install.sh` + `npx skills` only、無 npm package publish) + `unreallabsai/unreal-agent` (Go library only、無 npm/JS binding)。**對位主人 horo-agent 對齊**：如果主人想跟哪個 pick 整合，`OpenStock` + `ZCode` 可直接 `pnpm install` 走 monorepo 模式、`google/ax` + `unreal-agent` 走 Go module sidecar via `cgo` 或 RPC、`treg` 走 SaaS API integration。

- **license 乾淨度：5 picks 中 3 permissive + 1 AGPL-3.0 + 1 NOASSERTION case-G**。**Permissive** = `google/ax` Apache-2.0 (case-A2) + `zai-org/ZCode` Apache-2.0 (case-A2) + `unreallabsai/unreal-agent` MIT (case-A)。**AGPL-3.0 case-E** = `Open-Dev-Society/OpenStock`（README 顯式標示 = 嚴格 copyleft、對主人 horo-agent / horo-webui downstream 不能 ship 進 closed-source product、如要 fork / embed 必須 release source under AGPL-3.0 + credit original authors）。**NOASSERTION case-G** = `superdesigndev/treg`（README 內無 LICENSE file + 自訂非標準條款 + hosted SaaS `treg.to` 計費機制 = **商用 embed 需逐一驗證 LICENSE file 內容**）。**對位主人 horo-agent downstream license friction 評估**：`google/ax` + `zai-org/ZCode` + `unreallabsai/unreal-agent` 為 0 friction、`Open-Dev-Society/OpenStock` 為 structural prohibition (AGPL)、`superdesigndev/treg` 為 unclear (case-G verify LICENSE)。**Memo 呼應 09-11 14:00 codification "case-A2 patent grant 結構性比 MIT 更安全"**：今天 2 個 Apache-2.0 = 雙倍 patent grant 保護。

- **Substrate ecosystem pair（跨 9-23 + 9-24 同 ecosystem 連續 pick）**：`agent-substrate/substrate`（9-23 pick #1, runtime layer）→ `google/ax`（9-24 pick #1, control plane layer）= **兩週內連續 pick 同一個 Google ATE ecosystem 的兩層**、Substrate 是 sandbox execution runtime、AX 是 declarative orchestrator running on top of Substrate、**兩層都 Apache-2.0 + 兩層都 Go + 兩層都 6 個月 mature** = **Google 對 agent infra 的 reference implementation 完整 pair**。對位主人 horo-agent downstream 可借 Substrate 的 actor/worker multiplexing + AX 的 Task + Workspace + Gateway + Model 4-piece abstraction 做 horo-agent declarative manifest API。**substrate-arc 11th pair**：**`runtime (Substrate) ↔ control-plane (AX)` = 同 ecosystem 不同 layer**、Substrate 提供 sandbox execution primitives、AX 提供 declarative orchestration manifest、對位 08-04 KV-cache 8-byte tag + 08-17 boundary-visibility + 08-19 fidelity + 08-20 cost-axis + 08-26 3-boundary cluster pattern family = 「**把隱形 substrate 行為變可工程化**」思路的 ecosystem-pair 應用。**Verified ×1 (9-23 + 9-24 cross-day chain)**、promote to "verified ×2" after next ecosystem-pair lands。