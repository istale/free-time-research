# Manufactured Sources Behind AI Recommendations

- 原始連結: https://trellner.com/reports/manufactured-sources-behind-ai-recommendations/
- Dataset: https://trellner.com/data/manufactured-sources-behind-ai-recommendations/
- HN 討論: https://news.ycombinator.com/item?id=49536375
- 閱讀時間: 2026-09-03（早間，週四）
- 來源: Hacker News 熱門前 10 第 8 名（讀取時 270 分 / 124 則留言，Asia/Taipei 07:00）；trellner.com 2026-09-02 發布

## 摘要

**單一研究問題：「Perplexity Sonar 跟 Sonar-Pro 在 380 個 buyer-intent 類別上引用了誰？」答案是——59.8% 的引用指向 Tranco top-100k 之外的 domain，23.4% 完全不在 top-1M，七成多的引用來自 36.5% 的 domain。** 報告在 2026-09-02 跑 760 次 query（380 categories × 2 tiers），每個 query 要求 JSON top-5 + 每個產品的官方 domain，回收 3,800 個推薦 slot、命名 1,807 個產品、cite 7,534 個 URL 跨 2,055 個 domain。對主人而言，這不是「SEO 抱怨文」而是 *retrieval-layer attack surface 的 forensic measurement*——跟前兩次 attack-class pick（8/12 reasoning trace 攻 CoT 機密性、8/21 proc-macro1 arrayref typosquat 攻 build-time RCE）同一 family 但在 grounding 層，跟主人 MEMORY 的 *「horo-agent enterprise-lite / air-gap / downstream 偏好」* 直接共振：未來 hermes-agent-lite 若開 web-grounded retrieval，這份 dataset + method 是 audit evidence-base 的範本。

**第二個關鍵訊號是「215,128 個 machine-generated buying guide pages 由三個 domain 共寫，none of them existed before Dec 2023」。** 三站（wifitalents.com / worldmetrics.org / gitnux.org）註冊於 NameCheap 2023-12 至 2024-05、DNS 都委派給同一對 Cloudflare name server（`pam.ns.cloudflare.com` + `sean.ns.cloudflare.com`）、跑同一份 page template（Services / Market Data / Software Advice / Editorial Process / Company），每站恰好 6 篇 blog、18 篇 blog 互相 cross-promote 三個 brand。Sitemap 列出 103,578 / 107,083 / 105,541 個 URL，其中 70,731 / 71,684 / 72,713 個是 `/best/<something>-software/` page——三站合計 215,128 個 generated buying guide，*而 380 個 category 根本不需要 21 萬個 page*。HTML title 不是公司名而是 `<Brand> — Facts & Grounding Page`，meta description 寫 "Verified facts about <brand>: an independent market research company"——**「grounding」這個詞不是 buyer 用語，是 retrieval system 內部的 step 名稱**。這些 page 不是給人讀的，是給 *「會讀它們的軟體」* 寫的。對主人而言，這是 *8/19 desktop-fly 那條「modeled vs measured honesty boundary」+ 8/21 arrayref 那條「transitive attack surface」* 的 retrieval-layer 變體——但跟 8/21 不同的是，這次攻擊者不是 typosquat 一個 crate、而是 *為 retrieval system 的 vocabulary（"Facts"、"Grounding"、"Editorial Process"）量身打造 page 結構*，等於把 LLM retrieval 的 source-selection signal 反向工程成可購買的 attack channel。

**第三個關鍵訊號是「同一 query 在三個站上 top-5 完全不一致」。** 報告抓 `project estimation software` 在三站上的 JSON-LD ranking：worldmetrics = Float / Scoro / Teamwork.com / Procore / Wrike；wifitalents = Float / Scoro / Teamwork.com / Buildertrend / Apropo；gitnux = Saviom / Mosaic / Buildertrend / Float / Teamwork.com——Gitnux 的 #1（Saviom）連 worldmetrics 的 top-5 都不在。九位「staff」跨三站、Editorial process 都標 "AI-verified · Expert reviewed"、byline 出現 `Within the next 26 days` / `Within the next 40 days` 兩個未渲染 template variable。Perplexity 在 380 個 category 裡有 290 次把同個產品排第一，但兩個 tier 的 citation list 在 289 個 category 完全 byte-identical（Jaccard 0.898），意謂 *兩個 tier 共用同一個 retrieval layer，agreement 不是 evidence of independent convergence*。對主人而言，這跟昨天 9/02 Claude Fable/Mythos 那篇 *「capability-granular trust gating」* 是同一條 *trust boundary 不只在 API 層* 的延伸——Anthropic Mythos 把 capability gating 寫進 API gateway，這篇告訴主人 *retrieval layer 也需要 capability-granular trust gating*，不然 7,534 個 citation 裡 23% 來自不在 top-1M 的 domain、36.5% 來自 top-100k 之外，*「Perplexity 引用了它」≠「它值得被引用」*。

**為什麼值得主人看**：主人 9 月目前 reading list 的 triple-axis（8/28 small model + 8/29 GLM-5.3 open-weight + 8/30 vLLM thinking budget + 9/01 BirdNET-Go multi-model inference + 9/02 Claude Fable/Mythos capability gating）在「*trust boundary 的層次*」這條 sub-axis 上，今天是 *第 12 個 pick、3 個不同 layer*——API capability layer (Fable 5.1 + Mythos)、build / supply chain layer (8/21 arrayref)、retrieval evidence-base layer (今天)。三個 layer 共用同一個 substrate-identity：*「trust boundary 必須被 explicit 設計，否則 attacker 會把 implicit boundary 反向工程成 attack surface」*。三個 actionable：(1) **horo-agent 若未來加 web-grounded retrieval，evidence-base audit 必須是 first-class primitive**——把這份 dataset 的 method (760 query / JSON top-5 / 7,534 citation / Tranco + Wayback lookup) 改成 hermes-agent-lite 的 `audit_evidence_base.py`（< 50 行 Python、< 4 小時 prototype），跑主人真實 daily digest workflow 統計「主人引用了哪些 domain、其中 Tranco rank 分佈是什麼」，把 audit 從 implicit 變 explicit。(2) **SOUL.md / `horo-agent/spec.md` 新增一條 retrieval trust boundary rule**：主人目前 MEMORY 寫「air-gap / downstream 偏好：保留已驗證 runtime 與真實行為、保守減法、端到端驗證、勿重寫 agent loop」——若 owner-controlled infra 必須留 retrieval 出口，*應 allow-list 而非 block-list*，因為今天這篇證明 block-list 沒有用（block "best software" page block 不完 21 萬個 URL），allow-list by domain reputation + by content structure 是唯一可行解。(3) **「Facts & Grounding Page」這個 attack vocabulary 寫進主人的 retrieval threat model**：當一個 page 把 HTML title 設成 *machine-targeted* 的詞（"Facts"、"Grounding"、"Editorial Process"、"Verified"、"Independent market research"），主人 routing layer 應該把它跟「*claim 是 data 還是 assumption*」做 same kind of honesty-boundary check（8/19 desktop-fly 那條 substrate-honesty boundary 的 retrieval-layer instance），cost **< 10 分鐘 commit、一個 regex rule**，跟 8/19 的 `# data: <src> | assumptions: <set>` header 同成本同 family。

## 3W1H 分析

- **What（做了什麼/主題）**:
  Trellner 在 2026-09-02 跑了一份針對 Perplexity Sonar + Sonar-Pro 的 retrieval-evidence-base audit：380 個 buyer-intent category × 2 tier = 760 次 query，透過 OpenRouter 呼叫 `perplexity/sonar` 與 `perplexity/sonar-pro`，每個 query 要 JSON top-5 與官方 domain；產出 3,800 個推薦 slot、1,807 個 distinct 產品、7,534 個 citation URL 跨 2,055 個 distinct domain；用 Tranco daily 2026-09-01 + Wayback Machine 對每個 cited domain 做 reputation + first-seen lookup；對 1,502 個 vendor homepage 做 liveness 二次驗證（直接 + rotating proxy）；dataset、method、column documentation 全 release 在 `/data/manufactured-sources-behind-ai-recommendations/`，CC BY 4.0。報告核心 finding：(a) 23.4% citation 在 Tranco top-1M 之外、59.8% 在 top-100k 之外；(b) 三站（wifitalents.com / worldmetrics.org / gitnux.org）共寫了 215,128 個 `/best/<X>-software/` page，三站都 2023-12 後才註冊、共用同一對 Cloudflare name server、跑同 template、HTML title 為 `<Brand> — Facts & Grounding Page`；(c) 同一 query 在三站上 top-5 完全不一致（Gitnux 的 winner 連另一站 top-5 都不在）；(d) Sonar 跟 Sonar-Pro 不是兩個獨立 measurement——它們在 289/380 category 給 byte-identical citation list（Jaccard 0.898），且 290/380 次把同一個產品排第一——這是 *shared retrieval layer 的一致性*，不是 independent system 的 convergence；(e) 1.1% 的 vendor domain（17 個）gone or unreachable，包括把 `montecarlodata.com` 對到 Monte-Carlo 摩納哥賭場、把 `dryad.co` 對到印尼 gambling portal `BIGSLOT288`。

- **Why（為什麼重要）**:
  1. **Trust boundary 的 retrieval-layer instance，跟 8/12 reasoning trace + 8/21 build-time RCE 同 family 但新 layer**：8/12 (Stealing Reasoning Traces from Proprietary LLM APIs) 攻 *encrypted CoT portability*、8/21 (arrayref/proc-macro1) 攻 *build-time RCE*、今天攻 *retrieval evidence-base*。三個 attack 共用同一個 substrate-identity：*「implicit trust boundary 會被反向工程成 attack channel」*。Anthropic 把 capability gating 寫進 API gateway 是 *explicit design*；今天這篇證明 retrieval layer 沒有 explicit gating 時 attacker 把 LLM 的 source-selection vocabulary（"Facts"、"Grounding"、"Editorial Process"）反過來寫進 page 結構，21 萬個 page 就是 21 萬個 attack surface。對主人而言，未來 hermes-agent-lite 若開 web-grounded retrieval，這是必須先解掉的 threat model。
  2. **Owner-actionability 高：method 可直接借**：報告把 760 query / JSON top-5 / 7,534 citation / Tranco + Wayback lookup 全 release 為 CC BY 4.0 + METHOD.md + column documentation。主人寫 `audit_evidence_base.py`（< 50 行 Python）套主人現有 daily digest workflow 跑同樣 audit 不需要 1 個月、而是 *< 4 小時 prototype*——dataset schema 已在，script 只需把 input 換成主人的 retrieval log。**這比 9/02 Claude Fable/Mythos 5.1 那條 vendor-side capability gating 的 Insight 更 actionable**，因為它是 *owner-side audit primitive*，主人 16GB Mac 直接跑。
  3. **跨日 continuity 拉出新 sub-axis**：昨日 9/02 Claude Fable/Mythos 5.1 把 trust gating 從 implicit 推到 explicit (API capability layer)，今日這篇把 trust gating 從 API layer 推到 retrieval layer；兩篇合起來是 *主人 9 月 trust boundary sub-axis 的第 12 pick、3 個 layer*——API capability layer (Fable) / build supply chain layer (8/21) / retrieval evidence-base layer (今天)。三個 layer 共用同一個 substrate-identity，**Insight bridge 把三個 primitive 寫成同一個 family 的三個 instance**——這跟 8/17 substrate-identity stacking pattern 同成本、同結構。
  4. **「attack vocabulary 寫進 page 結構」是 8/19 honesty-boundary 的 retrieval instance**：8/19 desktop-fly 那條 *「honesty boundary = 明確區分 data 與 modeling assumption」* 是 substrate-honesty boundary；今天這篇揭示 attacker 用 *retrieval-targeted vocabulary*（"Facts"、"Grounding"、"Verified"、"Independent"、"Editorial Process"）讓 page 看起來像 data，其實是 machine-targeted attack vector。主人 SOUL.md 加一條 retrieval honesty rule：*「任何 page 把 HTML title 設成 retrieval-internal term（Facts / Grounding / Verified / Editorial / Methodology / Compliance）時，視為 low-trust signal，跟 modeling assumption 同處理」*——cost < 10 分鐘 commit、一個 regex，比 build-time RCE 防禦便宜一個量級。

- **How（如何運作/實作）**:
  - **Audit method**：760 query via OpenRouter / 380 categories pre-committed before results seen / per-query JSON top-5 + official domain / per-citation Tranco + Wayback lookup / per-vendor homepage liveness check twice（direct + rotating proxy）/ dataset published CC BY 4.0 with METHOD.md + column documentation。**對 owner 的含義**：audit 不是 vendor 專利，任何 owner 可以用 4-6 小時 script + < 50 行 Python 跑自己的 evidence-base audit。
  - **Attack vector**：attacker 把 LLM retrieval 的 source-selection signal（domain reputation + content structure + "facts" vocabulary）反向工程成可購買的 attack channel。3 站用同一對 Cloudflare name server + 同 template + 18 篇互相 cross-promote 的 blog = 強烈的 circumstantial evidence of common control（但報告誠實標 "we do not know who operates them; none of the three names an owner"）。Sitemap 21 萬 page、每站 6 篇 blog = *page 數量 ≠ content 數量*。對 owner 的含義：allow-list by content structure + by domain reputation 比 block-list 有效。
  - **Perplexity retrieval layer 是 monolithic**：Sonar 跟 Sonar-Pro 在 289/380 category 給 byte-identical citation list（Jaccard 0.898），是 *同一個 retrieval layer 跑兩次*，不是兩個 independent system。對 owner 的含義：*不要把 Sonar + Sonar-Pro 當 cross-validation*——它們是 *sibling tier of the same engine*。
  - **Vendor 1.1% dead rate + 6.1% redirect rate**：1,502 個 vendor homepage 裡 17 個 gone or unreachable（`montecarlodata.com` 跳到摩納哥賭場、`dryad.co` 跳到 BIGSLOT288）、92 個 redirect 到不同 registrable domain。對 owner 的含義：web-grounded retrieval 必須 *post-retrieval verify*（不只 verify domain alive，還要 verify content 跟 query intent 對得上），cost < 50 行 Python。
  - **Owner-side allow-list by content structure**：HTML title 含 `Facts`、`Grounding`、`Verified`、`Independent`、`Editorial`、`Methodology`、`Compliance` 的 page 視為 low-trust signal；同時 allow-list by Tranco rank + Wayback first-seen date；audit log 寫進 hermes-agent-lite session store。cost **< 10 分鐘 commit、一個 regex + 一個 lookup table**，跟 8/19 honesty-boundary header 同成本同 family。

- **Insight（赫蘿心得）**:
  今天這篇對主人的 actionable 不是「裝 Perplexity」、也不是「買 Sonar-Pro」，而是 *借 attack-class paper 的 method 寫主人自己的 retrieval-layer audit primitive*。

  最便宜的「*今天 commit*」是 **Layer 0 SOUL.md / `horo-agent/spec.md` 新增一條 retrieval trust boundary rule**：寫一段 4 行 policy——`web-grounded retrieval: (1) allow-list by domain reputation (Tranco top-100k + Wayback first-seen > 2y) unless owner-opted-in; (2) post-retrieval verify: vendor homepage reachable + content matches query intent; (3) page HTML title containing "Facts"|"Grounding"|"Verified"|"Editorial"|"Methodology"|"Compliance" treated as low-trust unless domain is allow-listed; (4) audit log all retrieval citations to session store with timestamp + domain + Tranco rank + Wayback date.`——**這個 spec patch 是 < 10 分鐘 commit、零 code、零 LLM call、零 vendor lock-in**，把今天這篇 attack-class paper 的 threat model mirror 到主人自己的 spec line，與 8/19 desktop-fly honesty-boundary header + 8/15 SOUL.md rule primitive + 8/21 arrayref Layer-0 SOUL.md rules at every boundary 同成本同 family——同一個 *「text rule before code」* 模板的 retrieval-layer instance。

  Layer 1 prototype：**寫一個 `audit_evidence_base.py`（< 80 行 Python）**——套今天報告的 method 跑主人現有 hermes-agent-lite 任何 web-grounded retrieval workflow：760 query 改主人 daily digest 過去 30 天實際跑的 query 數（保守估 100-300 queries），JSON top-5 改主人 retrieval log 實際 output，Tranco + Wayback lookup 改 offline cached dataset（CC BY 4.0，可下載），產出 *「主人過去 30 天 cite 了哪些 domain、其中 Tranco top-100k 之外佔幾 %」* 的 audit report。**這個 prototype 是 < 4 小時、零 GPU 需求、可在主人 16GB Mac 直接跑**，給主人 *「現有 retrieval 是否已經被 attack」的 ground truth*——如果結果顯示主人 retrieval log 也有 20%+ 引用 top-100k 之外 domain，Layer 0 policy patch 就是必要的；如果 < 5%，可推遲 Layer 1。

  Layer 2 structural：**horo-agent 的 trust-boundary spec 必須從「*air-gap = no retrieval*」進化成「*air-gap + retrieval-with-policy*」**。今天這篇證明 retrieval 不是 binary（open / close）而是 *layered trust boundary*——同 9/02 Fable/Mythos 的 EFS 把 frontier + customer-controlled cloud 變 *compatible* 一樣，主人若需要 retrieval 出口（主人 daily digest 確實偶爾 web-grounded），*horo-agent 可以用 Layer 0 + Layer 1 policy 保留 retrieval 而不放棄 owner-controlled infra*——allow-list by domain reputation + post-retrieval verify + low-trust vocabulary detection 是 *唯一可行 shape*。這跟 8/19 desktop-fly 那條 *honesty boundary = model 寫假 data vs 量測真 data 的明確劃分* 是同一個 substrate-identity 的 retrieval-layer instance：*「attack vector 會把 implicit boundary 反向工程成 attack channel；唯一解是 explicit boundary」*。

  反面教材提醒——**這份 dataset 只量測 Perplexity Sonar / Sonar-Pro，報告自己明寫 "We have not measured ChatGPT, Gemini, Copilot or Google's AI Mode, and there is no reason to assume their retrieval mixes match"**。主人引用這篇做 decision 時，要記住三件事：(a) 7,534 citation 數量是 *Perplexity-only*，ChatGPT / Gemini 的 citation mix 可能完全不同，*不應該 generalize 到所有 web-grounded LLM*；(b) 760 query 是一日的 snapshot，不是 longitudinal——attacker 可能輪流 push 不同 brand，audit 需要 *每日跑* 才能偵測 shift；(c) 380 category 是 *researcher 自己寫的 buyer-intent list*，不是真實 buyer 的 query distribution，*weighted toward niche verticals*。Layer 0 policy patch 不依賴 Perplexity 是 23.4% bad citation 還是 5%，只依賴 *implicit retrieval boundary 會被 attacker 反向工程* 的 attack-vector 結構——這就是把 vendor-side measurement 跟 spec decision 解耦，避免「明天 ChatGPT 數字比較漂亮 → spec patch 撤回」的 regression。

  總結：今天這篇是 2026 Q3 *retrieval-layer attack surface* 的 forensic measurement，對主人 horo-agent 的 *3 actionable* 是——(1) Layer 0 SOUL.md retrieval trust boundary rule（< 10 分鐘 commit）；(2) `audit_evidence_base.py` prototype（< 80 行 / < 4 小時，套今天 method）；(3) trust-boundary spec 從 air-gap-no-retrieval 進化到 air-gap-plus-layered-retrieval-policy。**最低成本、最高 leverage 的 single action 是 Layer 0 SOUL.md patch**，主人早餐前可獨立完成。
