---
title: GitHub 自由探索 2026-09-02（14:00 台北時間）
date: 2026-09-02
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 5 天 repeat）
  - Web search（HKUDS 系列 — agent harness + personal agent 8 月底 / 9 月初 release）
  - GitHub REST API `/repos/<slug>` + `/releases/latest` + raw `README.md`
  - PyPI JSON API（nanobot-ai / openharness-ai / manimgl）
  - npm registry（@gitlawb/openclaude）
---

# GitHub 專案動態

- 檢查時間：2026-09-02（14:00 台北時間）
- 檢查對象：`Gitlawb/openclaude` / `Imbad0202/academic-research-skills` / `3b1b/manim` / `HKUDS/nanobot` / `HKUDS/OpenHarness`
- 來源組合：GitHub Trending today Tier-A top 3 排除前 5 天 repeat（**fresh top 3 = `Gitlawb/openclaude` rank 1, 31519★ TypeScript + 5 topics (ai/ai-agent/ai-tools/cli/coding), "open-source coding-agent CLI for cloud and local model providers" — but **LICENSE 警訊**：「This repository contains code derived from Anthropic's Claude Code CLI ... Modifications and additions ... under MIT License where legally permissible ... The underlying derived code remains subject to Anthropic's copyright」MIT NOASSERTION 但 LICENSE 內含法律灰色聲明** + `Imbad0202/academic-research-skills` rank 2, 45122★ Python, "Academic Research Skills for Claude Code: research → write → review → revise → finalize" (12-stage pipeline, 1 federal-7-mode integrity gate, **CC BY-NC 4.0**, NOT permitted for commercial fork — 含 繁體中文版 README.zh-TW + 簡體中文 + 日本語 + 한국語), v3.21.1 release 8/24 (9 天前 fresh) + `#782`-`#797` 10 PRs in last release incl sealed preregistration for Promotion Bakeoffs + `gpt-5.6-sol` ChatGPT-subscription citation transport validation** + `3b1b/manim` rank 6 (跳過 rank 3 `THU-MAIC/OpenMAIC` REPEAT 08-30 + 08-31 + rank 5 `jingyaogong/minimind` REPEAT 09-01), 92672★ Python (founded 2015-03-22, 11 年經典 = 3Blue1Brown math animation engine)，MIT, **`pip install manimgl`**（不是 `manim`/`manimlib`！需注意二個不同 package，manimgl 是 3b1b 原版 / `manim` 是 community edition `@ManimCommunity/manim`），v1.7.2 release 2024-12-13 (622 天前 stale 但 `pushed_at` 9/1 = today still active + 4 topics (3b1b-videos/animation/explanatory-math-videos/python) **viral-tail 持續中**, Trending today #6 = classic project 重新興起**），排除 `THU-MAIC/OpenMAIC` (REPEAT 08-30 + 08-31) + `K-Dense-AI/scientific-agent-skills` (REPEAT 08-30) + `iv-org/invidious` (Crystal + AGPL-3.0, YouTube alt frontend 是 niche 不是 AI agent 主軸) + `jingyaogong/minimind` (REPEAT 09-01) + `THU-MAIC/OpenMAIC` (REPEAT 08-30 + 08-31)），Web-search 2 picks = **`HKUDS/nanobot`** + **`HKUDS/OpenHarness`** 兩者皆由 HKUDS 維護且有 concept-level 互補，**`HKUDS/nanobot`** 47628★ Python + 20 topics (agent-framework/ai-agent/chatbot/chatops/discord-bot/llm-agents/local-llm/mcp/multi-agent/openclaw/personal-ai-assistant/python/self-hosted/telegram-bot/webui/workflow-automation), self-hosted + WebUI + 6 messaging channels (Telegram/Discord/Slack/Feishu/Email/Mattermost) + MCP + multi-agent delegation + scheduled automation + OpenAI-compatible API, MIT, v0.3.0 release `2026-07-25` (38 天前 中階 stale 但 **`pushed_at` 2026-09-02 (今天 9/2 = today active)**), **`pip install nanobot-ai`** + 1 launch command `nanobot webui` 啟動 WebUI + 繁體中文 i18n 文件 (nanobot.wiki/zh-Hant/) + Discord + X @nanobot_project + WeChat/Feishu community, **OpenClaw-coexist-friendly** (topic 包含 `openclaw`)；**`HKUDS/OpenHarness`** 15620★ Python + 0 topics, "Open Agent Harness with Built-in Personal Agent -- Ohmo!" + 1 CLI command `oh` + supports CLI agent integration **including OpenClaw, nanobot, Cursor** + 43 tools (file/shell/search/web/MCP) + on-demand skill loading (.md) + plugin ecosystem + **compatible with anthropics/skills & plugins** + Persistent context & memory + 114 pytest passing + 6 E2E suites + React-Ink TUI + Python ≥3.10 + Feishu/WeChat 社群 + 中文 (English/简体中文/-- 繁中缺) + 提供 `pip install openharness-ai` (PyPI v0.1.9) + 一鍵 install script `curl -fsSL https://raw.githubusercontent.com/HKUDS/OpenHarness/main/scripts/install.sh | bash`, MIT, v0.1.9 release `2026-05-07` (117 天前 stale 但 `pushed_at` 2026-06-04 = 3 個月前停滯，**active development 信號較弱**)

---

## Repo 摘要與 3W1H

### `Gitlawb/openclaude`

- **Repo 摘要：** **OpenClaude — open-source coding-agent CLI for cloud and local model providers** — 5 個月內從 0 衝到 31,519★，TypeScript + 5 topics (`ai`/`ai-agent`/`ai-tools`/`cli`/`coding`) 的 **Multi-provider terminal coding agent**，標語是「runs anywhere. uses anything」。**支援 OpenAI-compatible APIs + Gemini + GitHub Models + Codex OAuth + Codex + Ollama + Atomic Chat 等 backend**，**統一 terminal-first workflow** = prompts + tools + agents + MCP + slash commands + streaming output。**核心差異化**：**(1) Multi-provider abstraction layer** — 一個 CLI 同時 manage OpenAI/Anthropic/Gemini/Codex OAuth/Codex/Ollama/Atomic Chat 等後端，**focused LLMTR hybrid gateway + live model lists for OpenRouter and OpenGateway** (v0.30.0 PR #2150 + PR #2084)；**(2) Claude Code-compat feature set** — Terminal-first + prompts + tools + agents + MCP + slash commands + streaming，**bypass Anthropic 官方 Claude Code 限制**（openclaude v0.30.0 多處 hotfix 都在「managed AIMLAPI attribution」「scoped Anthropic attribution」「plugins marketplace hostPattern against lookalike hosts」這類 Claude-Code-compat 細節）；**(3) MIT PERMISSIVE on modifications only** — LICENSE file 內明說「This repository contains code derived from Anthropic's Claude Code CLI ... The original Claude Code source is proprietary software ... Modifications and additions by OpenClaude contributors are offered under the MIT License where legally permissible ... The underlying derived code remains subject to Anthropic's copyright」 — **GitHub API `license.spdx_id = NOASSERTION`**（不是 MIT-clear），對 air-gapped downstream fork 來說**合法風險比純 MIT repo 高很多**；(4) **npm 套件** `@gitlawb/openclaude` + npm downloads badge + Discord `k68zFR6AcB` + X `@gitlawb` + Trendshift badges（含 daily/monthly/badge-trendshift-25807）；(5) **active development**：v0.27.0 (7/31) → v0.28.0 (8/11) → v0.29.0 (8/19) → v0.29.1 (8/20) → **v0.30.0 (8/31 = 2 天前 fresh)** + `pushed_at` 9/2 today，**~每 1-2 週一版**；(6) **Partners** = GitLawb + Bankr.bot + Atomic Chat + Xiaomi MiMo + Atlas Cloud + AIMLAPI（README 開頭 logo grid）；(7) **VS Code extension + Setup Guides** 一應俱全。

- **3W1H：**
  - **What：** TypeScript + Node.js CLI 寫的 multi-provider terminal coding agent。支援 OpenAI / Anthropic / Gemini / Codex OAuth / Codex / Ollama / Atomic Chat 等 backend，統一 prompts + tools + agents + MCP + slash commands + streaming workflow。npm `@gitlawb/openclaude` 套件 + VS Code extension + Discord 社群。
  - **Why：** 解決「想用 Claude Code 風格的 terminal coding agent 但不想被 Anthropic 帳號綁死 / 想用 OpenAI-compatible + Gemini + Ollama 等任意 backend / 想在 atomic.chat 內 embed coding agent」的需求。OpenClaude 把「Claude Code-style workflow」port 成 open-source + multi-provider。
  - **Who：** 想用 Claude-Code-style CLI 但需要 multi-provider 自由度的 developer；對 atomic.chat 平台 binding 有興趣的 Web3 / onchain developer（partner bankr.bot + atomic.chat）；想繞過 Anthropic Claude Code 付費方案的 freelancer；對**法律灰色風險**（NOT pure MIT, derived from proprietary）有自評能力的 power user（**對 horo-agent air-gapped downstream 不建議直接 fork**）。
  - **How：** (a) **npm 裝（主推）**：`npm install -g @gitlawb/openclaude` → `openclaude` 進互動 CLI → 透過 `provider` 指令切換 backend（OpenAI OAuth / Gemini / GitHub Models / Codex OAuth / Ollama）；(b) **Setup Guides** 連結：vscode extension + 各 provider setup；(c) **Slash commands + MCP** 跟 Claude Code 類似；(d) **Discord** `https://discord.gg/k68zFR6AcB` + **X @gitlawb** + GitHub Discussions。

- **安裝方式：**
  - **npm 全域裝（主推）**：
    ```bash
    npm install -g @gitlawb/openclaude
    openclaude
    ```
    npm package: `@gitlawb/openclaude` (latest = `0.30.0`, released 2026-08-31)。**Node.js 18+**（參考 sister package `paperclipai` requires Node 24.11+ — openclaude 不特定，但建議 18+）。
  - **Provider 設定**（OpenAI / Gemini / Codex OAuth / Ollama 等）：
    ```bash
    openclaude provider add openai --api-key <your_key>
    openclaude provider add ollama --base-url http://localhost:11434
    openclaude provider add gemini --api-key <your_key>
    # ... etc
    ```
  - **VS Code Extension**：透過 README「VS Code Extension」section 安裝。
  - **手動 clone**：`git clone https://github.com/Gitlawb/openclaude.git` → `cd openclaude && npm install`。
  - **未找到明確 `pip install` 安裝方式**（TypeScript / Node.js 生態，PyPI 不可用）。
  - **⚠️ LICENSE 警訊**：本 repo LICENSE file 內明說「underlying derived code remains subject to Anthropic's copyright」 — **GitHub API `spdx_id = NOASSERTION`**（不是 MIT-clear）。**MIT modification-only license is legally murky**（Anthropic 並未授權 redistribution of derived Claude Code 原始碼）。Closed-source fork / air-gapped downstream commercial use 前**務必法務 review**。

- **近期 release：** `v0.30.0` — **2026-08-31 02:25 UTC 發佈（台北時間 8/31 10:25, **2 天前 fresh**）**, pre-release = false, draft = false。Body 主標題 `[0.30.0] (2026-08-31)` — release 由 [release-please](https://github.com/googleapis/release-please) 自動產生 conventional-commits changelog。**Features (2)**：(1) **`providers:` add focused LLMTR hybrid gateway** ([#2150](https://github.com/Gitlawb/openclaude/issues/2150))；(2) **`providers:` live model lists for OpenRouter and OpenGateway** ([#2084](https://github.com/Gitlawb/openclaude/issues/2084)) — **讓 providers 列表即時 refresh 而不是 cache**。**Bug Fixes (9)**：(a) **`api:` scope Anthropic attribution to compatible request paths** ([#2147](https://github.com/Gitlawb/openclaude/issues/2147)) — **avoid 將 Anthropic attribution 套用到 non-Anthropic requests**；(b) `bg: identify sessions with persisted process markers` ([#2163](https://github.com/Gitlawb/openclaude/issues/2163))；(c) `effort: preserve known model exclusions when force-enabled` ([#2148](https://github.com/Gitlawb/openclaude/issues/2148))；(d) **`integrations:` keep managed AIMLAPI attribution over caller headers** ([#2179](https://github.com/Gitlawb/openclaude/issues/2179)) — **partner integration 屬性優先 over user 自訂 headers**；(e) `openai-shim: drop synthetic tool-results marker and guard echoes` ([#2039](https://github.com/Gitlawb/openclaude/issues/2039)) + ([#2153](https://github.com/Gitlawb/openclaude/issues/2153))；(f) **`plugins:` anchor marketplace hostPattern against lookalike hosts** ([#2177](https://github.com/Gitlawb/openclaude/issues/2177)) — **security: 防範 spoofed marketplace hosts**；(g) `settings: preserve concurrent updates` ([#2137](https://github.com/Gitlawb/openclaude/issues/2137)) — **race-condition fix**；(h) `settings: stop proto-named permission rules from aborting validation` ([#2170](https://github.com/Gitlawb/openclaude/issues/2170))；(i) **`tui:` proper Unicode/IME input handling for composed sequences** ([#2018](https://github.com/Gitlawb/openclaude/issues/2018)) + ([#2154](https://github.com/Gitlawb/openclaude/issues/2154)) — **中文 / 日文 / Korean input fix**。**對 horo-agent / air-gapped downstream 影響評估**：**NOT recommended as direct fork basis 因為 LICENSE 含 Anthropic proprietary 殘留**。**Repo `pushed_at` 2026-09-02 00:15 UTC（台北時間 9/2 08:15, **今天 main 仍 commit**）** — `pushed_at` 顯著比 release 新鮮得多，**main branch 仍 active 開發中**（v0.30.1 可能即將出）。**Release-cadence**：v0.27.0 (7/31) → v0.28.0 (8/11 = 11 天) → v0.29.0 (8/19 = 8 天) → v0.29.1 (8/20 = 1 天 hotfix) → v0.30.0 (8/31 = 11 天) = **~每 1-2 週一版** = active 1.0-pre。**Pre-relase cadence `pre-release_stale`** = **2 天 release tag + 0 天 master push today = 100% fresh by both metrics**。**npm `@gitlawb/openclaude` 0.30.0** 存在（latest = `0.30.0`, published 2026-08-31, prev: 0.27.0/0.28.0/0.29.0/0.29.1） + **License 文件內明說 NOT pure MIT**（MIT for modifications only, underlying Anthropic proprietary）。**Repo `created_at` 2026-04-01（**5 個月內從 0 衝到 31,519★**, viral-tail 早期)** + 8,965 forks + 35,646KB size（含 multi-provider CLI + plugins + VS Code extension + docs + Discord setup）+ 73 open issues + **`License: NOASSERTION` GitHub API**（LICENSE file 是 MIT-modifications-only + Anthropic proprietary 殘留）+ **Homepage**: `https://openclaude.gitlawb.com` + **Mirror**：`https://gitlawb.com/node/repos/z6MkqDnb/openclaude`（**mirror 提示此 mirror-style distribution model**）。**完整 changelog**：見 [Releases](https://github.com/Gitlawb/openclaude/releases)。

### `Imbad0202/academic-research-skills`

- **Repo 摘要：** **ARS — Academic Research Skills for Claude Code: research → write → review → revise → finalize** — 6 個月內從 0 衝到 45,122★，Python + 8 topics (`academic-pipeline`/`academic-writing`/`ai-research`/`claude`/`claude-code`/`literature-review`/`peer-review`/`prompt-engineering`)，以 **Claude Code plugin marketplace** 形式發佈的 **12-stage academic pipeline skill pack**。**核心差異化**：**(1) Human-in-the-loop, not full automation** — Built on Lu et al. (2026, Nature 651:914-919) 對 The AI Scientist 的 Limitations 分析（implementation bugs / hallucinated results / shortcut reliance / bug-as-insight reframing / methodology fabrication / frame-lock / citation hallucinations），**Stage 2.5 + Stage 4.5 integrity gates 跑 7-mode blocking checklist**；(2) **Citation trust-chain + L3 gap closure (v3.7-3.8)** — Audited 111M references across 2.5M papers on arXiv / bioRxiv / SSRN / PMC (Zhao et al. 2026-05) 估計 146,932 hallucinated citations for 2025 alone，**v3.7.1 trust-chain frontmatter for source provenance** + **v3.7.3 locator infrastructure (three-layer citation anchors)** + **v3.8 opt-in audit pass `ARS_CLAIM_AUDIT=1` 跑 5 個 HIGH-WARN classes** (`claim-not-supported`, `negative-constraint-violation`, `fabricated-reference`, `anchorless`, `constraint-violation-uncited`) **gate-refuse output through the formatter terminal hard gate** + shipped **20-tuple gold set calibration with FNR<0.15 + FPR<0.10 acceptance thresholds**；(3) **Multi-language README** — 簡體中文 + **繁體中文版** + 日本語 + 한국語（含 `README.zh-TW.md` 對中文學術使用者友善）；(4) **Task Type Annotation (v3.3.2+)** — every skill declares `task_type` (`open-ended` or `outcome-gradable`)；(5) **Companion: Experiment Agent** — `Imbad0202/experiment-agent` 跑 python/R experiments with 11-type fallacy detection + Teaching Skills + ARXIV/bioRxiv integration；(6) **Latest 10-stage pipeline run artifacts** — `examples/showcase/` 全套真實 artifacts（peer review reports + integrity verification reports + final paper）；(7) **Integrity boundary disclosure** — README 內明寫「ARS does **not** establish that procedures were actually performed, raw data are authentic, or results reproduce; a consistently reported fabrication can pass these checks」**誠實揭露 limitation**；(8) **Cross-model handoff envelope (#527, v3.17+)** — owner→dispatcher→owner blind-checkpoint 透過 machine-stable `[CROSS-MODEL-HANDOFF v1]` envelope + `scripts/cross_model_handoff.py` normative grammar（不用 prose-only enforcement）；(9) **Model Tiering (#517, v3.16+)** — `ARS_MODEL_TIERING` 兩方向：`economy` (execution-tier agents one tier below session model, floor Opus-class) + `quality-boost` (judgment-tier agents 升到 frontier tier)。

- **3W1H：**
  - **What：** Claude Code plugin marketplace 上的 academic research skill pack（12-stage pipeline：RESEARCH → WRITE → REVIEW → REVISE → PUBLISH），含 8 種 Deep Research modes + 11 種 Academic Paper modes + cross-model handoff envelope + citation trust chain + L3 claim audit。Multi-language README（簡中 + **繁中** + 日 + 韓）+ DOI `10.5281/zenodo.20696614` + CITATION.cff + GOVERNANCE.md。
  - **Why：** 解決「想做 academic paper 但怕 hallucinated citations / 怕 methodology fabrication / 想要 7-mode integrity gate / 需要 systematic-review + PRISMA / 想要 stage-based progression 而非 autonomous-The-AI-Scientist」的痛點。ARS 把學術寫作流程變成可被 Claude Code 呼叫的 deterministic skill pipeline，**讓 AI 成為 copilot 而非 pilot**，保留 human 對 question definition / method choice / data interpretation / "I argue that" sentence 的主控權。
  - **Who：** 做 academic paper 的 researcher / PhD student / postdoc；需要 systematic literature review + PRISMA 的 medical / social science 研究者；對 citation hallucination / claim-source alignment 有顧慮的嚴謹 researcher；想用 Claude Code 跑學術 workflow 但要 human-in-the-loop 而非 fully autonomous 的指導者；想做中文 / 日文 / 韓文學術寫作的 user（readme 有繁中）。
  - **How：** (a) **Claude Code Plugin 安裝（主推）**：`/plugin marketplace add Imbad0202/academic-research-skills` + `/plugin install academic-research-skills`；(b) **Quick start**：`/ars-plan` 走 Socratic dialogue 規劃論文結構；(c) **Stage 1 RESEARCH**：跑 deep research 8 modes (full / quick / systematic-review / socratic / fact-check / lit-review / three-way-scan / review)；(d) **Stage 2 WRITE**：用 11 modes 寫論文 (full / plan / outline-only / revision / revision-coach / abstract-only / lit-review / format-convert etc.)；(e) **Stage 2.5 + 4.5 integrity gates**：7-mode blocking checklist + opt-in `ARS_CLAIM_AUDIT=1` audit pass；(f) **Stage 3 peer review**：Journal-Fit Reviewer + 3 Reviewers + Devil's Advocate；(g) **Stage 3' Re-review**：verification after revisions + Round 2；(h) **optional Stage 0.5 Experiment Agent**：`Imbad0202/experiment-agent` 跑實驗。

- **安裝方式：**
  - **Claude Code Plugin 安裝（主推, 30 秒）**：
    ```text
    /plugin marketplace add Imbad0202/academic-research-skills
    /plugin install academic-research-skills
    ```
    Claude Code CLI / VS Code / JetBrains 都支援（**v3.7.0+**）。然後 `/ars-plan` 試 Socratic dialogue 規劃論文。
  - **傳統 symlink 安裝**：
    ```text
    # Clone
    git clone https://github.com/Imbad0202/academic-research-skills
    # 把 skills 連結到 Claude Code skills 目錄
    ln -s "$(pwd)/academic-research-skills/academic-pipeline" ~/.claude/skills/
    # 詳見 README Quick install section
    ```
    需要 Claude Code + 設定 skills 目錄 (`~/.claude/skills/`) + permissions。
  - **未找到明確 `pip install` / `npm install` 安裝方式**（Claude Code plugin 是主路，PyPI/npm 個別 package 不適用 — 整個 skill pack 透過 `.claude-plugin/` directory + `marketplace add` 安裝）。
  - **Companion Experiment Agent**：另安裝 [`Imbad0202/experiment-agent`](https://github.com/Imbad0202/experiment-agent) — 跑 Python/R experiments with 11-type fallacy detection + `Teaching Skills` ([`YujxZJCN/teaching-skills`](https://github.com/YujxZJCN/teaching-skills)) — 同樣透過 Claude Code plugin marketplace 安裝。
  - **⚠️ LICENSE 警訊**：**`CC BY-NC 4.0`** — **NOT permitted for commercial use**。`SPDX-License-Identifier` not OSI-approved commercial-friendly。**Closed-source commercial fork 不可用**。

- **近期 release：** `v3.21.1 — Bounded workflow substrates, sealed bakeoffs, and transport hardening` — **2026-08-24 11:21 UTC 發佈（台北時間 8/24 19:21, **9 天前 fresh**）**, pre-release = false, draft = false。Body 主標題 `## What's Changed` + 10 PRs：**(1) Add OrcaRouter to the community directory — #782**；(2) **Update cross-model recommendation surfaces for generation currency — #784**；(3) **`Repair the Codex ChatGPT-subscription citation transport for codex-cli 0.147.0 — #786`** — **fix contained ChatGPT-subscription citation transport for Codex CLI 0.147.0**：stderr authentication attestation + provider-incompatible schema keywords + code-mode-host/web-search interaction；(4) **`Record the first Promotion Bakeoff and validate gpt-5.6-sol for that transport — #788`** — **first counterbalanced 180-call bakeoff 驗證 `gpt-5.6-sol` for ChatGPT-subscription citation transport**（**transport-qualified**：only 對 ChatGPT-subscription 通路有效，first-party API route 仍 provisional）；(5) **Consolidate Markdown parsing helpers and harden link/heading semantics — #791**；(6) `Register write-scope guard launcher degradations — #792`；(7) `Re-derive data_access_level for standalone paper and reviewer skills — #793`；(8) **`Add sealed bakeoff and default-off workflow-profile contracts — #795`** — **sealed preregistration for future Promotion Bakeoffs**；(9) **`Add the opt-in inquiry ledger, alternative-register design freeze, and bounded review-criteria proving set — #796`** — opt-in `ARS_INQUIRY_LEDGER=1` alpha + bounded 1 個 MSR 2027 review-criteria proving set with exact-axis resolution + three-consumer digest binding；(10) `Release preparation and version alignment — #797`。**Important boundaries** 5 條誠實揭露：(a) research-workflow profile ships as **default-off, offline-only substrate**（no manuscript inference / pipeline hook / family-specific shipped profile / behavioral usefulness `NOT_RUN`）；(b) `ARS_INQUIRY_LEDGER=1` opt-in defaults **OFF**（usefulness/unusability evidence remains `NOT_RUN`）；(c) alternative register 是 **design-only** — no runtime implementation ships；(d) review-criteria fixture 是 **1 個 illustrative proving set**，not general venue/discipline coverage nor real-author attestation — `#575` remains open pending `#684`'s two independent blinded human experts；(e) **`gpt-5.6-sol` result is transport-qualified** — validated for ChatGPT-subscription citation route only，first-party API route 仍 provisional。**對 horo-agent / air-gapped downstream 影響**：**CC BY-NC 4.0 = NON-commercial only**。**Closed-source commercial fork 不可用**，**但學術 / 教育 / 個人使用 OK**。對主人「中文 academic writing with Claude Code」興趣高度對齊（**繁中 README 完整 + `Zenodo DOI 10.5281/zenodo.20696614`**）。**Repo `pushed_at` 2026-09-01 17:57 UTC（台北時間 9/1 13:57, **1 天前 master 仍 commit**）** — `pushed_at` 顯著比 release 新鮮得多，**main branch 仍 active 開發中**。**Repo `created_at` 2026-02-26（**6 個月內從 0 衝到 45,122★**, viral-tail 持續中）+ 3,558 forks + 21,475KB size（含 12-stage pipeline + 8+11 mode skills + showcase artifacts + companion experiment-agent + Teaching Skills + 4 languages READMEs + governance + integrity contracts）+ 19 open issues + License = **CC BY-NC 4.0**（**NON-commercial-only**, 不是 OSI-approved opensource license — 對 owner commercial downstream 不適用）+ **`SPDX-License-Identifier: CC-BY-NC-4.0`**（case-A verified via LICENSE file）+ **`Homepage: https://buymeacoffee.com/crucify020v`**（個人 sponsor 連結）。**完整 changelog**：[CHANGELOG.md](https://github.com/Imbad0202/academic-research-skills/blob/v3.21.1/CHANGELOG.md) + **technical details**：[POSITIONING.md](POSITIONING.md)。

### `3b1b/manim`

- **Repo 摘要：** **Manim (ManimGL of 3b1b) — Animation engine for explanatory math videos** — **11 年經典項目從 2015 持續至今衝到 92,672★**，MIT + Python + 4 topics (`3b1b-videos`/`animation`/`explanatory-math-videos`/`python`)，**3Blue1Brown Grant Sanderson 親自維護** + **2020 起 Manim Community Edition** 分支出去（`ManimCommunity/manim`，目標「更穩定、更好測試、更友善」）。**核心差異化**：**(1) Programmatic math animation engine** — Python API 寫 scene + animation + camera + text + LaTeX → render 成可解釋數學影片；(2) **Two versions 分清楚**：(a) **本 repo `3b1b/manim` = ManimGL**（`pip install manimgl` 是本版，不是 `manim` 或 `manimlib`）；(b) `ManimCommunity/manim` = Community Edition（`pip install manim` 是 Community Edition），**兩個 package name 不一樣不能混用**；(3) **System requirements**：Python 3.10+、FFmpeg、OpenGL、Pango（with headers）、可選 LaTeX distribution（建議 lightweight `texlive-science` + `texlive-fonts-extra` + `texlive-latex-extra`，不要裝 `texlive-full`）；(4) **Linux/Ubuntu/Debian detailed setup**：`apt install ffmpeg python3-pip libpango1.0-dev` + optional `texlive-science` + `git clone && python3 -m pip install -e .`；(5) **3Blue1Brown videos**：[github.com/3b1b/videos](https://github.com/3b1b/videos) 提供 video-specific code 供學習者 fork；(6) **Docs**：3b1b.github.io/manim/ + Manim Subreddit + Manim Discord `bYCyhM9Kz2`；(7) **Active maintainer** (3Blue1Brown 主理, 但 release cadence 慢)。

- **3W1H：**
  - **What：** Python 寫的 math animation engine（ManimGL of 3b1b，3Blue1Brown 親自維護）。輸入 Python scene definition → output explanatory math video。Sys requires Python 3.10+ + FFmpeg + OpenGL + Pango headers + optional LaTeX。
  - **Why：** 解決「想用程式碼（不是 After Effects 手 K）做 math video / 想重現 3Blue1Brown 風格的可解釋視覺化 / 想要 deterministic + reproducible 的科學 / 教學動畫」的痛點。ManimGL 把「explanatory math animation」變成 declarative Python code。
  - **Who：** 想要做 math / physics / CS 教學影片的教師 / 教授 / YouTuber；做科學解釋視覺化的研究員；想 fork 3Blue1Brown 風格個人教學頻道的 content creator；對「programmatic animation + LaTeX rendering + OpenGL real-time preview」有興趣的 Python 開發者；viral-tail project 想了解如何 v1.7.2 還能 Trending #6 的經典項目研究。
  - **How：** (a) **PyPI 裝（主推）**：`pip install manimgl`（**注意：不是 `pip install manim`**，那是 Community Edition）；(b) **試跑**：`manimgl` 直接進互動；(c) **範例**：`manimgl example_scenes.py OpeningManimExample` 或 `manim-render example_scenes.py OpeningManimExample`；(d) **從 source hack**：`git clone https://github.com/3b1b/manim.git` → `cd manim` → `python3 -m pip install -e .` → `manimgl example_scenes.py OpeningManimExample`；(e) **Linux/Ubuntu detailed setup**：見上方「安裝方式」。

- **安裝方式：**
  - **PyPI 直裝（主推, pip）**：
    ```sh
    # Install manimgl（注意是 manimgl 而不是 manim）
    pip install manimgl
    # Try it out
    manimgl
    ```
    **Python 3.10+**。
  - **從 source hack（給想改 manimlib 本體的人）**：
    ```sh
    git clone https://github.com/3b1b/manim.git
    cd manim
    pip install -e .
    manimgl example_scenes.py OpeningManimExample
    # 或
    manim-render example_scenes.py OpeningManimExample
    ```
  - **Linux (Ubuntu/Debian, 詳細步驟)**：
    ```sh
    sudo apt update
    sudo apt install ffmpeg python3-pip libpango1.0-dev
    # 可選 lightweight LaTeX distribution
    sudo apt install texlive-science texlive-fonts-extra texlive-latex-extra
    git clone https://github.com/3b1b/manim.git
    cd manim
    python3 -m pip install -e .
    manimgl example_scenes.py OpeningManimExample
    ```
    **推薦用 venv**：`apt install python3-venv` + `python3 -m venv venv` + `source venv/bin/activate` + `pip install -e .`。
  - **macOS**：見 README。安裝 FFmpeg + LaTeX + `pip install manimgl`。
  - **Windows**：FFmpeg + MiKTeX + `git clone` + `pip install -e .`。
  - **Sys requirements**：Python 3.10+ + FFmpeg + OpenGL + Pango headers + 可選 LaTeX（lightweight `texlive-science` + `texlive-fonts-extra` + `texlive-latex-extra`，**NOT** `texlive-full` 因為太大）。
  - **⚠️ 重要警告**：**不要把 ManimGL 與 ManimCommunity Edition 混用**：`pip install manim` ≠ `pip install manimgl`！Community Edition 用 `manim` 指令，本 repo (3b1b) 用 `manimgl` 指令。**先確定你要哪版再裝**。
  - **System Python on Linux**：`python3.12-venv` 是分開 package（Ubuntu/Debian 24.04+），如 `python3-venv` 不可用才改裝。
  - **Docker** (community contribution, 非官方)：搜 "manim docker" 找 community image; 官方未提供 Dockerfile。
  - **PyPI 套件**：`manimgl` latest `1.7.2` (released 2024-12-13) + License = **MIT**。

- **近期 release：** `v1.7.2` — **2024-12-13 02:58 UTC 發佈（台北時間 12/13 10:58, **622 天前 = ~20 個月前 large stale**）**, pre-release = false, draft = false。Body 5 條 changes：(1) **Added reload functionality by @Splines**；(2) **Updated how configuration is handled**；(3) **Change how Tex (and Text) caches generated svg data**；(4) **Fix incorrect arg injection in Riemann rectangles by @InakiRaba91**；(5) **Misc. other bug fixes**。**對 horo-agent / air-gapped downstream 影響評估**：無直接應用場景（horo-agent 是 coding agent，不是 math animation engine），但 **3Blue1Brown-style programmatic animation** 是 **explanatory visualization** 的標竿 — **如果主人想擴 horo-agent 視覺化報告能力（scientific paper explainer / blog post animated diagrams / Hebrew-tutor-style 教學 video）, ManimGL 是第一個要評估的 toolchain**。**Repo `pushed_at` 2026-09-01 20:13 UTC（台北時間 9/1 04:13, **今天 master 仍 commit active**）** — `pushed_at` 顯著比 release 新鮮得多，**main branch 仍 active 開發中**（雖然 release tag cadence 慢 ~11 個月，但 commit 仍持續）。**Release-cadence**：v1.6.0 (2022-04) → v1.6.1 (2022-04) → v1.7.0 (2024-10) → v1.7.1 (2024-10) → v1.7.2 (2024-12) = **~每 release 間距 2-12 個月**。**viral-tail class project** — Trending today #6 (★+86 today) 跟 v1.7.2 622 天前的 stale release 形成鮮明對比，**說明「commit + visibility」是 viral-tail project 的關鍵**（主人可參考 `pushed_at` 當 active liveness signal 而非 release tag）。**Pre-relase cadence `pre-release_stale`** = **622 天 release tag + 1 天 master push today = `pushed_at` 救回 stale release 的最佳 example**。**PyPI `manimgl` 1.7.2** 存在 + **License = MIT case-A verified via file**（純 permissive，closed-source fork 完全 safe）。**Repo `created_at` 2015-03-22（**10 年經典, viral-tail post-stable**）+ 7,625 forks + 77,262KB size（含全 codebase + example_scenes + 3b1b/videos 對應代碼 + docs）+ 492 open issues + License = **MIT**（case-A verified via file 含 copyright notice clause）+ **分支 `master`**（不是 `main`，這是 2015 project 慣例）。**完整 changelog**：見 [Releases](https://github.com/3b1b/manim/releases)。

### `HKUDS/nanobot`

- **Repo 摘要：** **🐈 nanobot — Ultra-lightweight, open-source, self-hosted personal AI agent framework in Python with WebUI, tools, memory, MCP, multi-agent workflows, automation, and chat apps** — **6 個月內從 0 衝到 47,628★**，MIT + Python + **20 topics** (`agent-framework`/`ai-agent`/`ai-agents`/`chatbot`/`chatops`/`discord-bot`/`llm-agents`/`llms`/`local-llm`/`mcp`/`model-context-protocol`/`multi-agent`/`openai-compatible`/`openclaw`/`personal-ai-assistant`/`python`/`self-hosted`/`telegram-bot-ai-assistant`/`webui`/`workflow-automation`)，定位為「OpenClaw, Claude Code, Codex 精神下的 ultra-lightweight personal AI agent in Python」。**核心差異化**：**(1) Persistent workflows**：goals + memory + tools + chat context 跨 long-running work survive；(2) **Chat-native reach**：WebUI + API + Telegram + Feishu + Slack + Discord + Teams + Email + Mattermost（**9 channels**）；(3) **Model freedom**：OpenAI-compatible APIs + local LLMs + image generation + search + fallback models；(4) **Small core**：可讀 internals 配 MCP + memory + deployment + channel abstractions；(5) **`/goal` long-running goals** v0.2.0+ (2026-05-15 + v0.3.0 enhanced) — sustained objectives across turns；(6) **Dream memory system** — session memory + two-stage memory；(7) **WebUI included** — `pip install nanobot-ai` 後跑 `nanobot webui` 即可（**published package already includes WebUI, no separate frontend build**；(8) **OpenClaw-adjacent**: topic 含 `openclaw` + **OpenCode provider** integrated v0.3.0 — 雖然不是直接 fork 但 spirit 接近 OpenClaw；(9) **Production-ready runtime**：live configuration reload without process teardown + cleaner cancellation ownership + more reliable scheduled work + faster CI；(10) **繁體中文 i18n**：`nanobot.wiki/zh-Hant/` 完整 docs（與主人 horo-agent 中文導向偏好對齊）。

- **3W1H：**
  - **What：** Python + FastAPI framework + WebUI + 9-channel messaging bridge 的 self-hosted personal AI agent。`pip install nanobot-ai` + `nanobot webui` 一鍵起 WebUI，9 個 chat channels + MCP + memory + multi-agent delegation + scheduled automation + OpenAI-compatible API。
  - **Why：** 解決「想做 personal AI agent 但只想在 Python 上搞（不學 TypeScript）、想要 chat-native 介面（Telegram / Discord / Slack / Feishu）、想用 WebUI 而不是 raw CLI、想 local-first 而不是 cloud、想用 MCP 而不只是 function calling」的痛點。nanobot 把「personal AI agent」整個 stack 降到 Python + 1 pip install + 1 launch command。
  - **Who：** 想自架 personal AI agent 但只要 Python 環境的 developer；想用 Telegram / Feishu / Discord 等 chat app 接入 AI agent 的 user；對「memory persistence + long-running goals + scheduled automation」有需求的 power user；想避開 cloud-only AI agent SaaS 的 privacy-concern user；對 OpenClaw 精神有興趣但不想碰 OpenClaw 整個 stack 的 builder（**spirit-adjacent**，不是直接 fork）。
  - **How：** (a) **PyPI 1 行裝（主推）**：`pip install nanobot-ai` → `nanobot webui` 啟動 local WebUI（含 guided setup，幫助 first-time user 選 provider）；(b) **CLI 模式**：直接 `nanobot` 進 terminal 互動；(c) **Chat channels**：`nanobot` 連 Telegram / Discord / Slack / Feishu / WeChat / Email / Mattermost 等；(d) **Configuration**（provider / fallback / Langfuse / MCP / web tools / security）：`nanobot configure` 或讀 `docs/configuration.md`；(e) **Development**：[`docs/development.md`](docs/development.md) + [`docs/architecture.md`](docs/architecture.md)；(f) **Deployment**：本地 + server + Docker + Render；(g) **Multilingual docs**：English / 簡體中文 / **繁體中文** / Español / Français / Bahasa Indonesia / 日本語 / 한국어 / Русский / Tiếng Viện 在 [nanobot.wiki](https://nanobot.wiki)。

- **安裝方式：**
  - **PyPI 1 行裝（pip, 主推）**：
    ```bash
    pip install nanobot-ai
    nanobot webui
    ```
    **Python ≥3.11**。WebUI package 與 CLI 同一 wheel（**no separate frontend build**）。
  - **CLI mode**（不需要 WebUI）：
    ```bash
    pip install nanobot-ai
    nanobot
    ```
  - **Git clone + 開發模式**：
    ```bash
    git clone https://github.com/HKUDS/nanobot
    cd nanobot
    pip install -e .
    nanobot webui
    ```
  - **Docker / Render deployment**：
    ```bash
    # Docker (推導範例, 詳見 docs/deployment.md)
    docker run -it ghcr.io/hkuds/nanobot nanobot webui
    # 或 Render — 詳見 docs/deployment.md
    ```
  - **Provider 設定**：
    ```bash
    nanobot configure
    # 互動式 wizard 設 OpenAI / Anthropic / OpenAI-compatible / Ollama / local LLM
    # Langfuse observability（v0.1.5+）/ MCP web tools / security
    ```
  - **Channel 設定**（Telegram / Discord / Slack / Feishu / WeChat / Email / Mattermost）：
    ```bash
    nanobot channel add telegram --bot-token <your_token>
    nanobot channel start telegram
    ```
    詳見 [`docs/chat-apps.md`](docs/chat-apps.md)。
  - **CLI commands**：`nanobot webui` (啟 WebUI) + `nanobot` (CLI) + `nanobot channel` (channel mgmt) + `nanobot configure` (provider/config) + `nanobot mcp` (MCP servers) + `nanobot memory` (Dream 2-stage memory inspect)。
  - **PyPI 套件**：`nanobot-ai` latest `0.3.0` (released 2026-07-25) + Python ≥3.11 + **MIT License confirmed via PyPI classifier**（含 `License :: OSI Approved :: MIT License`）。
  - **Discord**：[`discord.gg/MnCvHqpUGB`](https://discord.gg/MnCvHqpUGB) + **X**：`@nanobot_project` + **WeChat/Feishu**：[`COMMUNICATION.md`](./COMMUNICATION.md)。

- **近期 release：** `v0.3.0` — **2026-07-25 08:08 UTC 發佈（台北時間 7/25 16:08, **38 天前 中階 stale**）**, pre-release = false, draft = false。Body 主標題 `🐈 nanobot v0.3.0 is here: 260 PRs merged and 38 new contributors. The agent gained agency.` + lead-in `The fastest way to experience the release is one command: nanobot webui`**。**9 大 highlights**：(1) **One command to the workbench** — `nanobot webui` prepares local channel + starts gateway + opens browser + leads first-time users into guided setup；(2) **A stronger browser workbench** — Topic-style chats + composer model switching + quoted follow-ups + document attachments + native folder selection + responsive settings + clearer fallback state；(3) **Agent coordination without core sprawl** — Inline subagents 可以 consult 在 active task 內 + authorized work 可以 continue 通過 verification + typed runtime context keeps provider-owned state out of ad hoc loop logic；(4) **Explicit long-running goals** — Sustained execution gated behind `/goal` with **clearer permission / cancellation / timeout / heartbeat / local-trigger behavior**；(5) **Files and tool activity you can inspect** — Syntax-highlighted previews + live file-edit diffs + better code-copy behavior + structured tool errors + richer MCP artifacts + preserved multimodal tool output；(6) **A more operational runtime** — **Live configuration reloads without process teardown** + bounded session and exec state + safer restart/shutdown + cleaner cancellation ownership + more reliable scheduled work + faster CI；(7) **Broader providers and search** — **OpenCode Zen/Go** + **Kimi Coding** + **Grok OAuth** + provider-scoped proxies + Copilot Enterprise overrides + **Parallel Search** + Serper + steadier authentication fallback；(8) **Channels as maintainable extensions** — **Native Neonize WhatsApp** + Mattermost + improved Feishu/Telegram/Discord/Matrix/QQ behavior + optional channel dependencies + clearer setup flows；(9) **Security tightened at the edges** — Chained-command allowlist（**read full body** 連結 v0.3.0 release notes）。**對 horo-agent / air-gapped downstream 影響**：**MIT permissive + clean archetype**（No AGPL obligations，closed-source fork safe）。**Functional parallels to horo-agent**：(a) `nanobot webui` ≡ `horo webui`（browser-based local workbench）；(b) `nanobot channel add telegram` ≡ `horo agent-teamworkflow-task`；(c) `nanobot /goal long_running_objective` ≡ `horo goal_mode: true` 的 Kanban cards；(d) Dream 2-stage memory ≡ kanban_cycle_memory + session_search。**Spirit-adjacent**，**不是直接 fork**，**horo-agent 官方進程參考價值高**（架構啟發）。**Repo `pushed_at` 2026-09-02 05:05 UTC（台北時間 9/2 13:05, **今天 master 仍 commit active**）** — `pushed_at` 顯著比 release 新鮮得多（38 天落差），**main branch 仍 active 開發中**（v0.3.1 / v0.4.0 正在醞釀）。**Release-cadence**：v0.1.5 (2026-04-05) → v0.1.5.post2 (2026-04-21) → v0.1.6 (?) → v0.2.0 (2026-05-15) → v0.3.0 (2026-07-25) = **~每月一版 + 中間 hotfix** = active 1.0-pre。**Pre-relase cadence `pre-release_stale`** = **38 天 release tag + 0 天 master push today = `pushed_at` 救回 stale release 的好 example**。**Repo `created_at` 2026-02-01（**7 個月內從 0 衝到 47,628★**）+ license = **MIT**（case-A verified via LICENSE file）+ **Homepage** PyPI + Discord `MnCvHqpUGB` + X `@nanobot_project`。**完整 release notes**：v0.3.0 release page。

### `HKUDS/OpenHarness`

- **Repo 摘要：** **`oh` — OpenHarness & `ohmo` — Open Agent Harness with Built-in Personal Agent "Ohmo!"** — **5 個月內從 0 衝到 15,620★**，MIT + Python ≥3.10 + React-Ink TUI + 0 topics 的 **personal AI agent framework** + 「one command `oh` to launch OpenHarness and unlock all agent harnesses」。**核心差異化**：**(1) Built-in personal agent Ohmo** — Not another chatbot, but an assistant that actually works for you over long sessions — **chat with ohmo in Feishu / Slack / Telegram / Discord, and it forks branches, writes code, runs tests, and opens PRs on its own**；(2) **Use existing subscription** — ohmo runs on user's existing Claude Code or Codex subscription, **no extra API key needed**；(3) **CLI agent integration** — Supports CLI agent integration **including OpenClaw, nanobot, Cursor, and more** — **spirit-adjacent to OpenClaw ecosystem 但自有定位**；(4) **43+ tools** (file / shell / search / web / MCP) + on-demand skill loading (.md) + plugin ecosystem (Skills + Hooks + Agents) + **compatible with anthropics/skills & plugins**；(5) **Persistent context & memory**（視覺化 badge "Persistent-8B5CF6"）+ streaming tool-call cycle + API retry with exponential backoff + parallel tool execution + token counting & cost tracking；(6) **114 pytest passing + 6 E2E suites**（CI 完整）；(7) **Output formats**: text | json | stream-json；(8) **Provider flexibility** — Supports `Kimi/Moonshot` + `MiniMax` + `z.ai/GLM` + `Azure OpenAI` + `OpenAI` + `Anthropic` 等；(9) **MCP** — gracefully handles disconnected servers in `call_tool` / `read_resource`；(10) **Security** — Built-in sensitive-path protection in `PermissionChecker` + hardened `web_fetch` URL validation；(11) **Stability** — EIO crash recovery in Ink TUI + `--debug` logging + Windows cmd flash fix；(12) **`oh --dry-run`** — Dry-run safe preview 解析 resolved runtime settings, auth state, skills, commands, prompts, and channel config flow；(13) **Multi-platform** — Linux / macOS / WSL 一鍵 install script + Windows 原生支援 + **`pip install openharness-ai`** 另一路；(14) **Feishu Group + WeChat Group** + 中文社群（English + 简体中文，**繁中未明列**）。

- **3W1H：**
  - **What：** Python + React-Ink TUI 寫的 Open Agent Harness with built-in personal agent "Ohmo"。CLI command `oh` 啟動整個 harness + 整合 OpenClaw / nanobot / Cursor 等 CLI agents + 43+ tools + on-demand skills + plugins（compatible with anthropics/skills & plugins）+ MCP + persistent memory + Feishu/Slack/Telegram/Discord 9+ channels（ohmo 連）+ Git workflow（fork branches / 寫 code / 跑 tests / 開 PRs on its own）。
  - **Why：** 解決「想用 Claude Code 或 Codex 等 coding agent 但要自己維護 CLI lifecycle / 想讓 agent 在 Feishu/Slack/Telegram 等 chat platform 自動回應 / 想用 Claude Code/Codex subscription 而不是另開 API key / 想整合多個 CLI agents（OpenClaw + nanobot + Cursor）做 orchestration / 想用 anthropic skills & plugins 格式但不要被 vendor 綁死」的痛點。OpenHarness 把「personal AI agent + multi-agent harness」整個 stack 降到 Python + 1 `oh` command。
  - **Who：** 想用 Claude Code/Codex subscription 做 personal AI agent 但想要 multi-channel + open-source harness 的 developer；對「整合多個 CLI agents (OpenClaw + nanobot + Cursor)」有需求的 power user；想用 anthropic-skills & plugins 格式但開源實作的 skill author；想研究「Personal Agent with Persistent Session」架構的研究員；做繁中需求的用戶（OH 不像 nanobot 有繁中 i18n，主人可能需要 zh-TW community contribution）。
  - **How：** (a) **一鍵 install script（主推）**：`curl -fsSL https://raw.githubusercontent.com/HKUDS/OpenHarness/main/scripts/install.sh | bash`（Linux/macOS/WSL）；(b) **`pip install` 另一路**：`pip install openharness-ai`（PyPI 0.1.9 + Python ≥3.10）；(c) **Windows 原生**：另一行 install command (見 README)；(d) **`oh` launch** — One command to launch OpenHarness + 啟 harness + connect Ohmo（personal agent）+ CLI agents；(e) **`oh --dry-run`** 預覽 resolved runtime settings, auth state, skills, commands, prompts, channel config flow；(f) **`oh setup`** 設定 providers（含 API key update for already-configured provider profile，v0.1.9 fix）；(g) **`oh provider add/edit/list`** 管理 providers；(h) **Skills**：on-demand .md files + `skill-creator` skill（v0.1.9 bundled）+ slash-invoked skills with arguments + optional model override；(i) **Channels**：Feishu Group / Slack / Telegram / Discord / Matrix 等。

- **安裝方式：**
  - **一鍵 install script（Linux/macOS/WSL, 主推）**：
    ```bash
    curl -fsSL https://raw.githubusercontent.com/HKUDS/OpenHarness/main/scripts/install.sh | bash
    ```
  - **PyPI 另一路**：
    ```bash
    pip install openharness-ai
    # 或指定版本
    pip install --upgrade openharness-ai==0.1.9
    ```
    **Python ≥3.10**。
  - **Windows 原生**：見 README（PowerShell 或 `install.ps1`）。
  - **從 source clone + 開發**：
    ```bash
    git clone https://github.com/HKUDS/OpenHarness.git
    cd OpenHarness
    pip install -e .
    oh --help
    ```
  - **Provider 設定**：
    ```bash
    # 新增 provider (with API key stored at same time)
    oh provider add openai --api-key <your_key>
    # 編輯既有 provider (update API key)
    oh provider edit openai --api-key <new_key>
    # 互動式 setup
    oh setup
    ```
    支援 `Kimi/Moonshot` + `MiniMax` + `z.ai/GLM` + `Azure OpenAI` + `OpenAI` + `Anthropic` 等 providers。`OPENAI_BASE_URL` env override + profile-scoped credential priority + fixed provider-switching key mismatch（v0.1.7 fix）。
  - **CLI commands**：`oh` (launch) + `oh --dry-run` (preview) + `oh setup` (configure providers) + `oh provider` (provider mgmt) + `oh skill` (skill mgmt) + `oh channel` (channel mgmt) + `oh --debug` (debug logging)。
  - **PyPI 套件**：`openharness-ai` latest `0.1.9` (released 2026-05-07) + Python ≥3.10 + **MIT License 但 PyPI classifier 未明示（License: None in PyPI metadata，repo `license.spdx_id = MIT`）**。**PyPI license metadata 不一致**：repo LICENSE 是 MIT，PyPI metadata 標 None，**建議安裝後直接 verify LICENSE file**。
  - **社群**：[`HKUDS/.github` profile](https://github.com/HKUDS/.github/blob/main/profile/README.md) 上有 **Feishu Group** + **WeChat Group** + 中文 / 英文 README。

- **近期 release：** `v0.1.9 — Skill Workflows and Provider Key Updates` — **2026-05-07 11:10 UTC 發佈（台北時間 5/7 19:10, **117 天前 stale** = ~4 個月 stale**）**, pre-release = false, draft = false。Body 主標題 `# v0.1.9 — Skill Workflows and Provider Key Updates`。**Highlights (3)**：(1) **Bundled skill creator** — Added bundled `skill-creator` skill for creating / improving / verifying OpenHarness/ohmo skills；**makes repeatable workflows easier to capture as first-class skills**；(2) **User-invocable skill slash commands** — Skills marked as user-invocable can now be triggered directly with slash commands；**Slash-invoked skills support user arguments and can request a model override through skill metadata**；(3) **Provider API key update fix** — `oh setup` now lets users update the API key for an already-configured API-key provider profile + `oh provider edit <profile> --api-key <key>` can replace a saved profile key directly + `oh provider add ... --api-key <key>` can store a key while creating a provider profile。**Fixes (1)**：Fixed issue **#238** — 「users could change a configured provider model but had no supported path to update the saved API key」。**Install**：`pip install --upgrade openharness-ai==0.1.9`。**Contributors**："primarily a maintainer follow-up release"（主要 maintainer）。**對 horo-agent / air-gapped downstream 影響**：**MIT permissive + clean archetype**。**Spirit-adjacent to horo-agent**：OpenHarness 主張「personal agent ohmo that works over long sessions」+ `oh provider add <provider>` vs horo-agent multi-profile orchestration + **整合 nanobot + OpenClaw + Cursor** 而不是取代 = **multi-harness orchestration layer** — **對主人 horizon-agent 進程有 architecture reference 價值**。**Repo `pushed_at` 2026-06-04 02:42 UTC（台北時間 6/4 10:42, **90 天前 stale** = 3 個月滯後）** — `pushed_at` 比 release 新鮮 30 天（v0.1.9 5/7 → push 6/4 = 28 天活躍窗口，6/4 後停止 push 持續 90 天），**main branch 開發信號收斂**（active development 進入 pause 階段）。**Release-cadence**：v0.1.7 (2026-04-18) → v0.1.8 (2026-05-06) → v0.1.9 (2026-05-07) = **~4 月底密集，5 月初最後一版，6 月初停**。**Pre-relase cadence `pre-release_stale`** = **117 天 release tag + 90 天 stale `pushed_at` = 雙 stale**（**少見雙 stale sign, 顯示 mature-but-paused, not dead** — 因為 release cadence 短暫密集 → 5 月後 stop commit，是「feature-complete version 0.1.x 等下個開發週期」的 pattern）。**Repo `created_at` 2026-04-01（**5 個月內從 0 衝到 15,620★**）+ License = **MIT**（case-A verified via LICENSE file，含 Copyright (c) 2026 HKUDS）+ **測試規模**：114 pytest passing + 6 E2E suites。**完整 changelog**：見 [Releases](https://github.com/HKUDS/OpenHarness/releases)。

## 重點觀察

- **Tier-A top 3 = 3/3 全 FRESH by `pushed_at` (cross-day 0/3 repeat floor ×5 VERIFIED)**：今天 Tier-A 排除 3 個 repeat (`THU-MAIC/OpenMAIC` 8/30 + 8/31 + `K-Dense-AI/scientific-agent-skills` 8/30 + `jingyaogong/minimind` 9/1) 後，**3 個 pick 都是 today / 1 天內 `pushed_at`**：`Gitlawb/openclaude` (pushed_at 9/2 today) + `Imbad0202/academic-research-skills` (pushed_at 9/1 = 1 天前) + `3b1b/manim` (pushed_at 9/1 = 1 天前)。**0/3 cross-day repeat floor 已達 ×5（08-20 + 08-21 + 08-31 + 09-01 + 09-02）** — 符合 08-20 codification「0/3 = natural floor = structurally optimal」標準連續 5 天。**Tier-A top 3 與 Web-search 2 (HKUDS/nanobot + HKUDS/OpenHarness) 今天都是 MIT permissive 且 `pushed_at` active** — `nanobot` pushed_at 9/2 today，`OpenHarness` pushed_at 6/4 stale 3 個月。**5 picks = Python ×4 (ARS + manim + nanobot + OpenHarness) + TypeScript ×1 (openclaude) = 1 語言 dominant**，**比 09-01「2 語言 3 生態」（Python ×3 + Node.js ×1 + Markdown ×1）語言更集中** — Python 是主軸（4/5 picks），TypeScript 是 open-source ports of Claude Code CLI 的邊緣。**注意**：`3b1b/manim` v1.7.2 release 是 622 天前 stale (2024-12-13) 但 `pushed_at` 9/1 = 1 天前 active；`ARS` v3.21.1 release 是 9 天前 fresh；`openclaude` v0.30.0 release 是 2 天前 fresh — **3/3 都是「release cadence 對得上 master push」**，nanobot 是 38 天落差的中階，`OpenHarness` 是 117 天 + 90 天雙 stale。**5/5 = 100% `pushed_at` active LATER 或 AT LEAST 2 天內 stale** 是本月 tier-A freshness 上半段。

- **License 警訊 split = 1 permissive-but-CLARIFIED + 1 NON-commercial-only + 1 MIT + 2 MIT (web-search)**：今天 pick 內 `Gitlawb/openclaude` 有獨特 LICENSE 警訊：**LICENSE file 內明說「This repository contains code derived from Anthropic's Claude Code CLI ... The underlying derived code remains subject to Anthropic's copyright. This project does not have Anthropic's authorization to distribute their proprietary source.」** — `GitHub API spdx_id = NOASSERTION`（不是 MIT-clear），**MIT modifications-only 是 legal murky**。**對主人 horo-agent / horo-webui air-gapped downstream 影響 = ❌ NOT recommended as direct fork basis**。**Imbad0202/academic-research-skills` = `CC BY-NC 4.0`** — **NON-commercial only** — 學術 / 教育 / 個人使用 OK 但 closed-source commercial fork 不可用。**剩餘 3 picks 都 MIT 純 permissive** (`3b1b/manim` + `HKUDS/nanobot` + `HKUDS/OpenHarness`) — **3/5 = 60% pure permissive**（含 patent grant clause TODO verify），**比 09-01 `5/5 = 100% permissive` 下降 40pp** — 唯一一次 Trending #1 有 legal murky 警訊，**值得對主人 explicit flag**。**對 horo-agent air-gapped downstream 影響**：3 picks 可直接 fork 入庫，2 picks 需法務 review / 排除（openclaude + ARS-NON-commercial）。

- **5 picks 5 種 install type 全 distinct（install type diversity is the new bar ×5 VERIFIED）**：今天命中：**Type-1 PyPI one-liner (`pip install manimgl`) + System deps (FFmpeg/OpenGL/Pango/LaTeX)**（**3b1b/manim** `pip install manimgl` + `apt install ffmpeg python3-pip libpango1.0-dev` + optional `texlive-science`） + **Type-21 npm install globally + multi-provider CLI**（**openclaude** `npm install -g @gitlawb/openclaude` + `openclaude provider add` 設 OpenAI/Anthropic/Gemini/Codex OAuth/Ollama） + **Type-23 Claude Code Plugin Marketplace install**（**ARS** `/plugin marketplace add Imbad0202/academic-research-skills` + `/plugin install academic-research-skills` — **這是第 1 次命中 Claude Code plugin install type**）+ **Type-1 PyPI one-liner (`pip install nanobot-ai`) + `nanobot webui` bundled WebUI**（**nanobot** `pip install nanobot-ai` + `nanobot webui` 1-launch command + published package 含 WebUI，無 separate frontend build） + **Type-2 curl-pipe-to-bash + pip alternative**（**OpenHarness** Linux/macOS/WSL `curl -fsSL https://raw.githubusercontent.com/HKUDS/OpenHarness/main/scripts/install.sh | bash` **OR** `pip install openharness-ai` — 兩條路都 expose，**dual-mode install 是 power user 友善 pattern**）。**5 picks = 5 distinct install paths** 達 **install type diversity ×5 VERIFIED（08-20 + 08-21 + 08-31 + 09-01 + 09-02）**。**新增 install type**: **Claude Code Plugin Marketplace** 與 **dual-mode install (curl OR pip)** 是過去 5 天未觀察到的新 pattern。

- **Release 新鮮度 tier-split（5 picks）**：超 fresh (≤1 天) = **0**、fresh (≤14 天) = **3** (`ARS` v3.21.1 9 天前 + `openclaude` v0.30.0 2 天前 + `manim` 雖 release 622 天前 stale 但屬於 viral-tail)、**Tier-A fresh by `pushed_at` (≤1 天)** = **4** (`ARS` 9/1 + `manim` 9/1 + `nanobot` 9/2 + `openclaude` 9/2) + `OpenHarness` 90 天 stale、stale (>30 天) = **2** (`OpenHarness` 117 天 + `nanobot` 38 天 邊界)。**4/5 = 80% `pushed_at` fresh** 是本月 tier-A freshness 上半段，**3/5 = 60% fresh by release tag** (`ARS` + `openclaude` ≤14 天 + `manim` viral-tail class）。**0/5 zero-GH-release** = 0 picks 沒 release = **5/5 = 100% release-tag cadence** 是 14:00 系列本月 **10 個 100% release-tag milestone**（08-12/13/14/15/17/20/21 + 08-31 + 09-01 + 09-02 共 10 個 100% milestone, 09-02 是最新）。**Operational reading**：今天 2 個 stale release (`OpenHarness` 117 天 + `nanobot` 38 天) 都是 **post-stable-like cadence**（`pushed_at` 收斂或滯後，release cadence 慢但 commit 尚 active in nanobot / OpenHarness stopped）。**3b1b/manim viral-tail extreme example** = v1.7.2 release 622 天 stale 但 `pushed_at` 今天仍 commit + Trending today #6 — **commit + community visibility 才是 viral-tail project 的真正 vitality**，**不是 release tag**（主人可參考 `pushed_at` + trending appearance 當 dual liveness signal，release tag 是次要）。

- **Master MEMORY signal 命中 (HKUDS 雙 hit)**：今天 **2/5 picks fire Hermes-as-host-adjacent signal**：(a) **`HKUDS/nanobot`** topic 包含 **`openclaw`** + **20 topics** (`agent-framework`/`ai-agent`/`llm-agents`/`multi-agent`/`openclaw`/`personal-ai-assistant`/`self-hosted`/`webui`/`workflow-automation`) + **Spirit-adjacent to OpenClaw/horo-agent 架構**（`nanobot webui` ≡ `horo webui`、`nanobot channel add telegram` ≡ `horo agent-teamworkflow-task`、`nanobot /goal long_running_objective` ≡ `horo goal_mode`）+ Dream 2-stage memory ≡ kanban_cycle_memory + **繁體中文 i18n** (nanobot.wiki/zh-Hant/) 對主人 horo-agent 預期繁中導向對齊 — **multi-channel + WebUI + personal agent + memory 的完整 archetype reference**；(b) **`HKUDS/OpenHarness`** 內 README 明寫 **「Supports CLI agent integration including OpenClaw, nanobot, Cursor, and more」** + 主標題 `oh — OpenHarness & ohmo — Open Agent Harness` + **integrates 自身為 multi-harness orchestration layer**（不取代 OpenClaw + nanobot + Cursor 而是 host them） — **對主人「多代理偏好 default（Qwen）直接建立／路由 Kanban task 給 executor/reviewer」+ 多-profile 架構是直接案例 in production**。**2/5 picks fire MEMORY-adjacent signal** 略高於 5-tick rolling average ≈ 1.4/5（08-27 = 1/5, 08-28 = 1/5, 08-29 = 2/5, 08-30 = 1/5, 08-31 = 2/5 + 09-01 = 1/5 + 09-02 = 2/5, **5-tick avg = 1.6/5**），09-02 在 avg 之上。**Master MEMORY 顯著對齊**：HKUDS 兩個 pick 同時提供（i）multi-channel messaging（Feishu + Slack + Telegram + Discord）、（ii）memory persistence + Dream + long-running goals、（iii）multi-agent delegation + Claude Code/Codex subscription reuse、（iv）MCP + provider flexibility（OpenAI/Ollama/Kimi/Anthropic/MiniMax/z.ai/GLM），是 **horo-agent **`將公開情報蒐集、Kanban 分派追蹤、當前 checklist、session continuity、長期 memory`** 落到 production 的 reference 雙拼** — **值得 deep dive 比較**（paperclip 那天 1/5 也是同類 high-impact MEMORY-adjacent pick，加上 nanobot + OpenHarness 雙 hit 累積 3 個這類 pick 短短 2 天）。今天其他 3 picks 雖然沒直接命中 MEMORY 對齊：`ARS` 對齊主人 Claude Code plugin 生態 + 繁中 README + 學術興趣；`openclaude` 對齊主人 Claude Code CLI 用法但有 license 警訊；`3b1b/manim` 對齊主人「視覺化 + 邊做邊看」興趣（math animation 是 deterministic + reproducible 可解釋動畫的最佳範例）。
