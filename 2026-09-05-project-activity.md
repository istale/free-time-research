---
title: GitHub 自由探索 2026-09-05（14:00 台北時間）
date: 2026-09-05
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 5 天 repeat）
  - Web search（anomalyco/opencode + radixark/miles — open-source coding agent fork + enterprise RL post-training framework, lmsys.org blog 2026-08-18 + findarepo daily ranking 2026-09-05）
  - GitHub REST API `/repos/<slug>` + `/releases/latest` + raw `README.md`
---

# GitHub 專案動態

- 檢查時間：2026-09-05（14:00 台北時間）
- 檢查對象：`blader/humanizer` / `JuliusBrussee/caveman` / `magnitudedev/magnitude` / `anomalyco/opencode` / `radixark/miles`
- 來源組合：GitHub Trending today Tier-A 排除前 5 天 repeat（**fresh top 3 = `blader/humanizer` rank 6 (Trending「AI writing humanizer」skill) + `JuliusBrussee/caveman` rank 7 (caveman-speak token compression skill + proxy) + `magnitudedev/magnitude` rank 8 (open-source inference server for local models) — 排除 `mattpocock/skills` (09-01/09-04 repeat) + `DietrichGebert/ponytail` (09-01/09-04 repeat) + `fmtlib/fmt` (09-04 repeat) + `affaan-m/ECC` (09-01/09-03/09-04 repeat) + `anthropics/skills` (09-04 repeat) + `NousResearch/hermes-agent` (09-04 repeat)**） + Web search 2 = `anomalyco/opencode` (findarepo daily ranking 2026-09-05, 204k★, MIT, TypeScript, the open-source AI coding agent, fork 2025-04-30, multi-platform install path: `curl | bash` + npm + brew + scoop + choco + pacman + nix + mise + Desktop App) + `radixark/miles` (lmsys.org blog 2026-08-18 release v0.1.0, 2.6k★, Apache-2.0, Python, **enterprise-grade RL framework for LLM/VLM post-training** forked from THUDM/slime, pairs SGLang rollout + Megatron-LM training, supports DeepSeek-V4 / Kimi-K3 / GLM-5.2 / Inkling / Nemotron-3-Ultra / Kimi-K2.6 / Qwen3.5 day-0).

---

## Repo 摘要與 3W1H

### `blader/humanizer`

- **Repo 摘要：** **Humanizer — Agent skill that removes signs of AI-generated writing from text** — 7 個月內從 0 衝到 **42,893★** (`created_at` 2026-01-18), Python + MIT + 7 topics (`agent-skills`/`ai-writing`/`claude-code`/`codex`/`cursor`/`prompt-engineering`/`writing-tools`), **35 patterns from Wikipedia's "Signs of AI writing"** (maintained by WikiProject AI Cleanup)。**核心差異化**：**(1) Plain-Language rewrite skill** — 不是簡單的「換同義詞」, 是 **first pass without treating the original structure as fixed + check the draft against those patterns and the original claims before rewriting whatever still needs work**; **(2) No fabrication guard** — 「A name, number, date, quote, citation, or other factual detail must come from the source or the writer」, 不會編造事實; **(3) Writer voice matching** — `If you provide a writing sample, Humanizer follows that sample instead of its default style rules`, 對 personal / technical / reference 三種 writing 自動調整; **(4) Show-its-work pattern** — 先給 first rewrite + 短 critique, 才給 final version, 使用者可中途介入; **(5) File-aware mode** — `point it at a file and it changes only the prose, leaving code, data, frontmatter, and link targets alone`, 對 markdown / source code 混合文件特別有用; **(6) 35 patterns 分 5 大類**：Content (1-6) + Language and grammar (7-13) + Style (14-19, 26-35) + Chatbot (20-22) + Filler and hedging (23-25), 涵蓋從 inflated importance 到 emoji / curly quotes / fake-candid openings / forced punchlines 全範圍; **(7) Cross-agent installation** — `npx skills add blader/humanizer --global` (TypeScript `skills` CLI standard) + Claude Code `/plugin marketplace add blader/humanizer` + Claude Desktop ZIP upload + manual `SKILL.md` copy; **(8) Version 2.11.2 (latest, dated 2026-08-18)** — skill lives at `SKILL.md` root (Claude Code can install from source ZIP directly), 簡化 plugin symlink。**對主人影響**：**MIT permissive + agent-skills standard (skills.sh installable) + cross-agent hosting** — 主人 horo-agent 可以直接抄 `SKILL.md` 結構 + 35 patterns 表 + plain-language 重寫邏輯 + show-its-work pattern。**Tier-A 命中**：`rank=6` 是 **fresh trending today**, 主人 MEMORY「主人偏好 abstract refine」與「writing-sample override default rules」對齊。

- **3W1H：**
  - **What：** Agent skill (Markdown-based SKILL.md) 用 35 個 AI-writing patterns (源自 Wikipedia「Signs of AI writing」) 重寫 AI-sounding text 成 human-sounding text。**Cross-agent**：Claude Code / Codex / Cursor / Claude Desktop + 任何 skills.sh-compatible agent。MIT license。
  - **Why：** 解決「AI-generated text 聽起來像 AI 寫的 (overused words / emoji / forced groups of three / formulaic challenges) / 想 humanize 但要保證 facts 不被 hallucinate / 想 match 自己的 writing voice / 想 file-aware (改 prose 不改 code) / 想 show-its-work (先看 first draft + critique 才看 final) / 想 cross-agent install (skills.sh + Claude Code plugin + Claude Desktop ZIP)」的痛點。Humanizer 把「35 patterns-based rewrite」降到 1 個 SKILL.md + 1 個 npx command。
  - **Who：** Content writer / blogger / technical writer 想移除 AI-sounding tell; 對 AI writing detection 有需求的 educator / editor; 對 Wikipedia 「Signs of AI writing」 35-pattern framework 有興趣的研究者; 對 cross-agent skill install (skills.sh standard) 有需求的 agent host developer (主人 horo-agent 對齊); 對 writing-sample voice matching 有需求的 personal / brand writer。
  - **How：** (a) **Skills CLI (主推, cross-agent)**：`npx skills add blader/humanizer --global` 或 `npx skills add blader/humanizer` (in-project); 帶 `--agent <name>` 或 `--agent '*'` 選擇 host; (b) **Claude Code 2.1.142+**：`/plugin marketplace add blader/humanizer` → `/plugin install humanizer@humanizer` → 用 `/humanizer:humanizer`; (c) **Claude Desktop**：從 GitHub 下載 ZIP → upload as skill; (d) **Manual**：copy `SKILL.md` 到 agent skill folder; (e) **Use**：`/humanizer` (call skill 直接) 或「Please humanize this text: [...]」(plain language) 或「Humanize the prose in docs/launch-post.md」(file path); (f) **Voice match**：include 2-3 paragraphs of own writing 後 「Now humanize this text: [...]」。

- **安裝方式：**
  - **Skills CLI (主推, npm/npx)**：
    ```bash
    # Cross-agent install
    npx skills add blader/humanizer --global     # 對所有 agent 註冊
    npx skills add blader/humanizer              # 只對當前 project
    npx skills add blader/humanizer --agent claude-code   # 指定 agent
    ```
  - **Claude Code 2.1.142+ (plugin marketplace)**：
    ```text
    /plugin marketplace add blader/humanizer
    /plugin install humanizer@humanizer
    /humanizer:humanizer     # 呼叫 skill
    ```
  - **Claude Desktop (manual upload)**：從 GitHub 下載 repo ZIP → upload as skill (Current Claude Code loads root `SKILL.md` directly, so source ZIP works)。
  - **Manual**：
    ```bash
    git clone https://github.com/blader/humanizer.git
    cp humanizer/SKILL.md ~/.claude/skills/humanizer/SKILL.md   # 或 host-specific skill folder
    ```
  - **Usage (3 種 invocation modes)**：
    ```bash
    # Mode 1: Call skill directly
    /humanizer
    [paste your text here]
    
    # Mode 2: Plain language request
    Please humanize this text: [your text]
    
    # Mode 3: File path
    Humanize the prose in docs/launch-post.md
    ```
  - **Writing-sample voice matching**：
    ```bash
    /humanizer
    Here's a sample of my writing for voice matching:
    [paste 2-3 paragraphs of your own writing]
    
    Now humanize this text:
    [paste AI text to humanize]
    ```
  - **未找到 PyPI / brew**：純 SKILL.md (Markdown) skill, **no Python package / binary / brew cask**。
  - **未找到 npm package**: `npx skills add` 是 `skills` CLI 的 wrapper, 從 npm registry 拉 metadata, 但 humanizer 本身不是 npm package。
  - **System requirements**：Claude Code ≥ 2.1.142 (plugin marketplace syntax) 或其他 skills.sh-compatible agent host; Node.js + npx 環境 for `skills` CLI; Claude Desktop (paid plan for upload)。
  - **License = MIT (case-A verified via LICENSE file + README badge)** — 純 permissive, no commercial restriction, **closed-source fork safe**。**對 horo-agent air-gapped downstream 影響**：**MIT permissive + 35-pattern framework + SKILL.md format 對齊 agentskills.io standard = 主人可以直接 fork 進 horo-agent skill marketplace**。

- **近期 release：** **`v2.11.1 — Humanizer v2.11.1`** — **2026-08-18 00:59 UTC 發佈（台北時間 8/18 08:59, **18 天前 mid-fresh**）**, pre-release = false, draft = false。Body 主標題 `Humanizer v2.11.1`。**`pushed_at` 2026-08-19 05:58 UTC（台北時間 8/19 13:58, **17 天前 mid-fresh active**）** — release 與 `pushed_at` 落差小 (1 天), **main branch 仍 active 開發中**。**Release-cadence**：`2.0.0 → 2.1.0 → 2.1.1 → 2.2.0 → 2.3.0 → ... → 2.10.0 → 2.10.1 → 2.10.2 → 2.11.0 → 2.11.1 → 2.11.2` = **stable minor-version cadence** (每 1-3 週) + **major bump (2.0.0)** 是 Wikipedia 35-pattern rewrite。**Latest minor 2.11.2** 在 README 「Version history」中提到：「Removed the plugin symlink and separate Claude Desktop package. Current Claude Code loads the root `SKILL.md` directly, so GitHub's source ZIP now works in Claude Desktop. No change to the 35 patterns.」 — **2.11.2 還沒發 GitHub release tag** (只有 version history 提到), **2.11.1 是 latest tagged release**。**對 horo-agent / air-gapped downstream 影響評估**：**MIT permissive + skills.sh standard + cross-agent hosting + Wikipedia-anchored 35 patterns = 主人 skill marketplace 直接 reference**。**Functional parallels to horo-agent**：(a) `SKILL.md` YAML frontmatter + Markdown body ≡ **horo-agent skill metadata schema 對齊**; (b) skills.sh `npx skills add` ≡ **horo-agent skill marketplace install 對齊**; (c) cross-agent `--agent <name>` ≡ **horo-agent multi-host 對齊**; (d) writing-sample voice matching ≡ **horo-agent「master voice」sample-onboarding 對齊** (主人 MEMORY 偏好 abstract refine); (e) show-its-work pattern (first draft + critique + final) ≡ **horo-agent harness self-review 對齊**; (f) file-aware (only prose, leave code/data/frontmatter alone) ≡ **horo-agent file-scope-controlled edit 對齊**; (g) 35-pattern taxonomy (Content/Language/Style/Chatbot/Filler) ≡ **horo-agent skill categorization 對齊**。**對主人「horo-agent skill marketplace 設計」直接 reference** — **35 patterns 結構 + Plain Language style + Wikipedia anchor + show-its-work pattern 可以整包抄**。

### `JuliusBrussee/caveman`

- **Repo 摘要：** **Caveman — why use many token when few token do trick (Claude Code skill that cuts 65% of tokens by talking like caveman)** — 5 個月內從 0 衝到 **103,651★** (`created_at` 2026-04-04), Go + **LICENSE = NOASSERTION API field 但 README 標 `MIT + BSL-1.1` split license** + 10 topics (`ai`/`anthropic`/`caveman`/`claude`/`claude-code`/`llm`/`meme`/`prompt-engineering`/`skill`/`tokens`), **Caveman skill (MIT) + Caveman proxy (BSL-1.1) = two-tier system for shrinking what agent says AND reads**。**核心差異化**：**(1) Two-tier architecture** — (a) **Skill (small rock)** = rule file that makes agent answer in caveman-speak (works in 30+ agents), 1 個 `npx skills add` command; (b) **Proxy (big rock)** = local process between agent and AI provider, shrinks what agent reads before every call, byte-exact recoverable from local SQLite; **(2) Honest numbers** — Skill: 平均 output 1214 → 294 tokens = **65% saved** (10 個 Claude API real prompts benchmark); Proxy: 54-run Claude Code benchmark, 5 cases average **33.2% input saved**, **18 of 18 exact-answer checks passed**; **(3) Two-mode intensity** — `/caveman lite|full|ultra|wenyan-lite|wenyan-full|wenyan-ultra|off` (5 intensity levels + 古文 wenyan modes); **(4) Subagent presets** — `cavecrew-investigator` / `cavecrew-builder` / `cavecrew-reviewer` (compressed subagent for locating / editing / reviewing code); **(5) 11 native agent wraps** — Claude Code + OpenAI Codex CLI + Gemini CLI + Aider + Kilo Code + Qwen Code + opencode + **Hermes Agent** + OpenClaw + Pi + (any provider SDK via `baseURL` swap), all read-only on user config; **(6) MCP toolkit (5 tools)** — `caveman_compress` / `caveman_retrieve` / `caveman_stats` / `caveman_toon_encode` / `caveman_toon_decode`, 任何 MCP host 可用; **(7) Pixel mode** — `caveman convert` 將 SKILL.md 轉成 PNG pages, model 讀 image 而非 text, 對 caveman skill 自身 **1069 → 415 tokens = 61% cut**; **(8) Work patterns** — `investigate-first` / `lean-build` / `surgical-patch` / `safe-refactor` / `migration` / `verify-and-stop`, 寫少 code, 自動挑選 by task shape; **(9) Context window packer** — `contextwindow.Pack()` 用 BM25 + recency + error signal 把 candidate context fit 到 token budget, **保留 original order 保 chronology**; **(10) **`detect()` payload type router**: json (70-90%) + log (85-95%) + code (40-70%) + diff (60-80%) + search-result (80-95%) + text/HTML (50-80%), each 不同 keep-strategy; **(11) Wrap stack** — local proxy + CCR (compressed-content recoverable) SQLite store + MCP toolkit side-channel + recovery handle (agent 可拉回原始 bytes); **(12) Split license** — **MIT** for skill / SDK / CLI / contracts / extension shell / thin cavemem clients + **BSL-1.1** for engine / proxy / cache engine / rewriter / browse / MCP server / shrink / cavemem Go core / shared Go platform, **每個版本在 `2030-06-21` 或出貨 4 年後轉 Apache-2.0**; **(13) 「Maintainer note」自清 honesty** — 主動揭露「the HTML row is red and it stays red」+ 「Caveman is a shorter agent, not free money. Measure your own setup before you tell your boss anything」+ docs/HONEST-NUMBERS.md + #550 A/B test 對話; **(15) 「Caveman no make brain smaller. Caveman make *mouth* smaller.」** — 對「代碼品質不變, prose 變短」的強調。**對主人影響**：**MIT skill + BSL-1.1 proxy (split license, case-D-like) + Hermes Agent + OpenClaw + Pi + Kilo Code + Qwen Code + sst opencode explicit integration** = **主人 horo-agent 的 multi-host plugin 設計直接 reference** + **token compression primitive** + **honest-benchmark discipline**。

- **3W1H：**
  - **What：** Two-tier token compression system: (1) **Skill** = prompt rule file (MIT), forces agent to answer in caveman-speak; (2) **Proxy** = local process between agent and provider (BSL-1.1), shrinks what agent reads before every call, byte-exact recoverable. 30+ agent hosts via skill + 11 native agent wraps via proxy + any-provider-SDK via `baseURL` swap. Subagent presets + work patterns + pixel mode + MCP toolkit.
  - **Why：** 解決「agent bill by token 越來越貴 / agent 寫 output 太多 throat-clearing / agent 重讀 logs/diffs/test output 花 input / 想 short replies 但要保持 code quality / 想 cross-host install / 想 byte-exact recoverable compression / 想 honest benchmark + red row disclosure / 想 5 intensity levels (lite/ultra/wenyan) / 想 subagent presets (investigator/builder/reviewer)」的痛點。Caveman 把「skill + proxy + recovery + honesty + 30+ agent hosts」降到 1 個 npx + 1 個 npm install + 1 個 `caveman setup --install`。
  - **Who：** Claude Code / Codex / Hermes Agent / OpenClaw / Pi / Qwen Code / sst opencode heavy user 想要降 token bill; 對 AI coding 成本有敏感度的 individual developer / team; 對 prompt-compression + retrieval-side compression 雙管齊下有興趣的 researcher; 對 「byte-exact recoverable compression」 有需求的 compliance / audit team; 對 honest-benchmark + red-row discipline 推崇的 open-source consumer; 對 pixel-mode (image-skill) 有興趣的 cost-optimization engineer; 對 work-pattern (investigate-first / lean-build / surgical-patch) 有需求的 lead engineer (主人 MEMORY 偏好 abstract refine)。
  - **How：** (a) **Small rock (skill, MIT)**：`npx skills add JuliusBrussee/caveman` → 30+ agents 自動 wake up; 或 `/caveman` 手動 trigger; 或 `npx skills add JuliusBrussee/caveman --skill '*' -a codex --yes` 限定 host; (b) **Big rock (proxy, BSL-1.1)**：`npm install -g @caveman-ai/cli` → `caveman setup --install` → `caveman claude` (或 codex/gemini/aider/kilo/qwen/opencode/hermes/openclaw/pi); (c) **Intensity**：`/caveman lite|full|ultra|wenyan-lite|wenyan-full|wenyan-ultra|off`; (d) **Subagents**：`cavecrew-investigator` / `cavecrew-builder` / `cavecrew-reviewer` 在 agent 內部 invoke; (e) **MCP tools (5)**：任何 MCP host 可用 `caveman_compress` / `caveman_retrieve` / `caveman_stats` / `caveman_toon_encode` / `caveman_toon_decode`; (f) **Full installer**：`curl -fsSL https://raw.githubusercontent.com/JuliusBrussee/caveman/v2.6.0/install.sh | bash` (Linux/macOS); `irm https://raw.githubusercontent.com/JuliusBrussee/caveman/v2.6.0/install.ps1 | iex` (Windows); (g) **Pixel mode**：`caveman convert --dry-run` (no-risk preview) → `caveman convert --agent claude` (apply) → `caveman convert --revert` (rollback); (h) **Telemetry**：預設 send anonymous command + token count, `caveman telemetry off` 或 `DO_NOT_TRACK=1` opt out; (i) **Waitlist Cloud**：`verified` label = real-traffic eval gates with signed receipts, waitlist at `caveman.so`。

- **安裝方式：**
  - **Small rock (skill only, MIT, 主推 cross-agent)**：
    ```bash
    npx skills add JuliusBrussee/caveman     # 對所有 skills.sh-compatible agents
    npx skills add JuliusBrussee/caveman --skill '*' -a codex --yes   # 限定 agent
    ```
    或直接 `/caveman` 觸發 (如 agent 不自動 wake up)。
  - **Big rock (proxy + skill, BSL-1.1 engine)**：
    ```bash
    npm install -g @caveman-ai/cli
    caveman setup --install    # 找 agents + 設 hooks + 設 statusline
    caveman claude             # 或 codex · gemini · aider · kilo · qwen · opencode · hermes · openclaw · pi
    ```
    `caveman <agent>` 持續 wrap, `caveman wrap <agent>` 跑一次 session。
  - **Full installer (Linux/macOS)**：
    ```bash
    curl -fsSL https://raw.githubusercontent.com/JuliusBrussee/caveman/v2.6.0/install.sh | bash
    ```
    Wires Claude Code hooks + statusline badge + finds every supported agent on machine + skips agents user no have. Safe to re-run. Needs Node.js 22.13+.
  - **Windows PowerShell**：
    ```powershell
    irm https://raw.githubusercontent.com/JuliusBrussee/caveman/v2.6.0/install.ps1 | iex
    ```
  - **Single-agent (per-host)**：
    ```bash
    # Claude Code
    claude plugin marketplace add JuliusBrussee/caveman && claude plugin install caveman@caveman
    
    # Gemini CLI
    gemini extensions install https://github.com/JuliusBrussee/caveman
    
    # Qwen Code CLI
    npm i -g @qwen-code/qwen-code
    caveman qwen
    
    # Codex / Cursor / Windsurf / Cline
    npx skills add JuliusBrussee/caveman --skill '*' -a codex --yes   # replace codex with agent profile
    ```
  - **Custom SDK (any provider)**：把 `baseURL` 指向 caveman local proxy, 詳見 `integrations/recipes/` (Vercel AI SDK / LangChain / LiteLLM / OpenAI Agents / CrewAI / PydanticAI 都可)。
  - **Uninstall**：`npx -y github:JuliusBrussee/caveman -- --uninstall` 或 `caveman disable <agent>` 撤特定 wrap。
  - **Pixel mode (image-skill)**：
    ```bash
    caveman convert --dry-run        # preview all skills with token math
    caveman convert --agent claude   # apply profitable ones
    caveman convert --revert         # byte-identical restore from SKILL.orig.md
    ```
  - **未找到 PyPI**：純 npm + npx + curl|bash install, **no `pip install caveman`**。
  - **System requirements**：Node.js 22.13+ (full installer) 或 npm-compatible runtime; macOS / Linux / Windows; Tested against real sessions on **Hermes v0.18.0** + **OpenClaw 2026.6.11** + **Pi 0.84.2** + **Kilo Code 7.5.6** + **Qwen Code 0.22.3**; Persistent shortcuts journaled + reversible。
  - **License = SPLIT (case-D-like — `MIT` for skill/SDK/CLI + `BSL-1.1` for engine/proxy/cache/rewriter/browse/MCP/shrink/cavemem Go core)** — **MIT 部分**對 horo-agent permissive + closed-source fork safe; **BSL-1.1 部分**對 closed-source fork 限制 (hosting for third parties needs commercial license, 每個版本 `2030-06-21` 或出貨 4 年後轉 Apache-2.0)。**對 horo-agent air-gapped downstream 影響**：**(a) MIT skill 部分可直接 fork / 商用 / 修改**; **(b) BSL-1.1 engine 部分對 self-host first-party traffic 免費**, 對 hosting for third parties 需 commercial license — **主人 horo-agent 若 self-host 自己用 = 免費, 不可包成 service 提供給他人**; **(c) Pattern (5 intensity + work-pattern + subagent preset + MCP toolkit + pixel mode) 可借鏡, 不 fork engine code**。

- **近期 release：** **`bin-v1.1.6 — bin-v1.1.6`** — **2026-09-04 00:23 UTC 發佈（台北時間 9/4 08:23, **昨天 fresh**）**, pre-release = false, draft = false。Body 主標題 `bin-v1.1.6` + 「Signed Caveman runtime companions for macOS, Linux, and Windows.」。**`pushed_at` 2026-09-04 00:31 UTC（台北時間 9/4 08:31, **昨天 fresh active**）** — release 與 `pushed_at` 落差 7 分鐘, **main branch 仍 active 開發中**。**Release-cadence**：`bin-v1.x` = signed runtime companions (macOS/Linux/Windows binary distribution), cadence 看 commit history (`pushed_at` 1 天前 = commit-driven active development)。**Install script pinned to v2.6.0**: `curl -fsSL https://raw.githubusercontent.com/JuliusBrussee/caveman/v2.6.0/install.sh | bash` (skill version, 跟 binary 版本分開編號)。**對 horo-agent / air-gapped downstream 影響評估**：**MIT skill + BSL-1.1 engine split + 11 native agent wraps (含 Hermes Agent) + 30+ skills.sh agents + honest benchmark + pixel mode + MCP toolkit** = **主人 horo-agent multi-host 整合直接 reference**。**Functional parallels to horo-agent**：(a) Two-tier (skill + proxy) ≡ **horo-agent skill marketplace + runtime proxy 對齊**; (b) `npx skills add` + 30+ agents + per-agent install command ≡ **horo-agent multi-host install 對齊**; (c) `caveman <agent>` wrap + read-only user config ≡ **horo-agent safe host integration 對齊**; (d) MCP toolkit (5 tools: compress/retrieve/stats/toon_encode/toon_decode) ≡ **horo-agent MCP server 對齊**; (e) `caveman learn` local-history analysis + ranked token sinks + implement-with-revert ≡ **horo-agent session-history-analysis + skill optimization 對齊**; (f) Work patterns (investigate-first/lean-build/surgical-patch/safe-refactor/migration/verify-and-stop) ≡ **horo-agent agent-workflow primitive 對齊**; (g) Pixel mode (image-skill) ≡ **horo-agent 視覺優先 + cost optimization 對齊** (主人 MEMORY 偏好 visual fidelity); (h) Context window packer (BM25 + recency + error signal) ≡ **horo-agent context-management primitive 對齊**; (i) `detect()` payload-type-aware compression ≡ **horo-agent content-type-aware tool 對齊**; (j) CCR SQLite + recovery handle (byte-exact recoverable) ≡ **horo-agent audit-trail + rollback 對齊**; (k) Hermes Agent `--provider custom` + env wrapper ≡ **主人「Hermes downstream 整合」直接 reference**。**對主人 MEMORY「主人偏好 abstract refine + 視覺優先 + honest-benchmark discipline + master voice sample-onboarding」**：Caveman 的 5 intensity levels + work patterns + subagent presets + pixel mode + honest benchmark (red rows stay red) 是 **horo-agent skill design 的 blueprint**。

### `magnitudedev/magnitude`

- **Repo 摘要：** **Magnitude — Open source inference server that runs the best local models for your hardware, plugged into the agent you already use** — 3 個月內從 0 衝到 **2,640★** (`created_at` 2026-06-12), TypeScript + **Apache-2.0** + 0 topics, **Works with Pi / OpenCode / Hermes / OpenClaw / Codex / Claude Code / Oh My Pi / Cline, or use the built-in harness**。**核心差異化**：**(1) Inference server (not model runtime)** — 不是 llama.cpp 或 Ollama 的 clone, 是 **local model orchestration + agent-host plug-in server**, profiles hardware → recommends models → downloads → tunes → runs; **(2) Built for agents specifically** — 「Why not just have my agent set up Ollama? Your agent would be guessing.」, Magnitude provides **hardware-aware catalog + onboarding flow that writes harness config + inference built for agent workloads**; **(3) 8 agent hosts native** — Pi + OpenCode + **Hermes** + OpenClaw + Codex + Claude Code + Oh My Pi + Cline, all connect to local inference; **(4) Adaptive model management** — **Models on demand (loaded on request, unloaded when idle or memory fills)**, 不像 Ollama 常駐一個 model, Magnitude 是「agent 用什麼就 load 什麼, idle 就 unload」; **(5) Hardware profiling** — profiles chip + memory + bandwidth, recommends best fit with estimated tok/s; **(6) Tuned end-to-end** — speculative decoding + concurrency, pre-tuned for specific hardware; **(7) Fully private / offline** — prompts / files / models stay on machine, 一旦下載完就不需 internet; **(8) GGUF-compatible** — 可從 Hugging Face 下載 compatible GGUF models outside catalog; **(9) Cross-platform** — macOS + Linux native, Windows 透過 WSL; **(10) Single prompt onboarding** — 「Set up local models for me with the Magnitude CLI」 → agent walks through everything (model 选 → hardware profile → download → harness config write → switch)。**對主人影響**：**Apache-2.0 permissive + 8 agent hosts (含 Hermes Agent) + local-first / offline inference + 「agent use what model」 on-demand pattern = 主人 horo-agent + Hermes downstream 的 inference layer 參考**。

- **3W1H：**
  - **What：** TypeScript 寫的 open-source inference server, runs best-fit local models for your hardware, plug into agent you already use (Pi / OpenCode / Hermes / OpenClaw / Codex / Claude Code / Oh My Pi / Cline, or built-in harness)。**Hardware profiling → recommend → download → tune → run**, 模型 on-demand 載入 unload, fully local/offline。
  - **Why：** 解決「想跑 local inference 但 agent 自己 Ollama 是 guessing / 想 hardware-aware model recommendation / 想 agent-first setup (prompt 1 個就走完) / 想 on-demand model loading (不佔 RAM) / 想 cross-host plug (8 agent hosts) / 想 GGUF-compatible (Hugging Face 其他 model 也可用) / 想 fully offline (prompts / files / models on machine)」的痛點。Magnitude 把「hardware-aware local inference + agent-first plug」降到 1 個 npm install + 1 個 prompt。
  - **Who：** Local AI / privacy-conscious developer 想要 inference on own machine; 對 Ollama 「常駐一個 model」 不滿意想要 adaptive loading 的 agent-heavy user (Hermes / Claude Code / OpenClaw user); 對 hardware-aware model recommendation 有需求的 Mac / Linux / WSL user (主人 MEMORY 環境 = VirtualMac2,1 16GB RAM); 對 fully offline inference 有需求的 compliance / air-gapped / enterprise-lite user (主人 MEMORY 「主人常用 Hermes Agent 做 enterprise-lite / air-gapped downstream」 直接 reference); 對 GGUF-compatible + 8-host integration 有需求的 framework builder。
  - **How：** (a) **Agent-driven onboarding (主推)**：把這段 prompt 丟給 agent：「Set up local models for me with the Magnitude CLI. Install it with `npm i -g @magnitudedev/cli` (or my package manager), then run `magnitude docs onboarding` and follow the instructions.」→ agent walks through hardware profile + model selection + download + harness config write; (b) **Manual setup**：`npm i -g @magnitudedev/cli` → `magnitude setup` (browse recommended models manually) → `magnitude` 啟動 inference server; (c) **Connect agent to Magnitude**：setup 時選 harness (Pi / OpenCode / Hermes / OpenClaw / Codex / Claude Code / Oh My Pi / Cline), Magnitude writes harness config; 或用 built-in Magnitude harness; (d) **Outside-catalog model**：[download compatible GGUF models from Hugging Face](https://docs.magnitude.dev/models#download-a-model-outside-the-catalog) → Magnitude uses them; (e) **Runtime model switching**：「Your agent can install or switch models through the Magnitude CLI anytime」。

- **安裝方式：**
  - **npm/npx (主推)**：
    ```bash
    npm i -g @magnitudedev/cli
    magnitude docs onboarding     # 或 magnitude setup
    magnitude                     # 啟動 inference server
    ```
  - **Agent-driven (主推)**：把 prompt 給 agent：
    ```text
    Set up local models for me with the Magnitude CLI. Install it with `npm i -g @magnitudedev/cli` 
    (or my package manager), then run `magnitude docs onboarding` and follow the instructions.
    ```
  - **Manual browse models**：
    ```bash
    npm i -g @magnitudedev/cli
    magnitude setup
    ```
    Interactive setup 列出 recommended models + estimated tok/s, user 選 model。
  - **Harness connect (8 options)**：Pi / OpenCode / Hermes / OpenClaw / Codex / Claude Code / Oh My Pi / Cline, 或 built-in Magnitude harness。
  - **Outside-catalog GGUF**：
    ```bash
    # 從 Hugging Face download GGUF
    # Magnitude 自動 detect + 使用
    ```
    細節：[docs.magnitude.dev/models#download-a-model-outside-the-catalog](https://docs.magnitude.dev/models#download-a-model-outside-the-catalog)
  - **其他 package managers**：`pnpm i -g @magnitudedev/cli` / `bun i -g @magnitudedev/cli` / yarn 同理 (README 寫 "or my package manager")。
  - **未找到 PyPI / brew / scoop**：純 npm package, **no Python equivalent / brew cask / scoop bucket**。
  - **System requirements**：macOS + Linux native, Windows 透過 WSL; Node.js + npm (or pnpm/bun/yarn); 對 hardware 沒固定 minimum, Magnitude profiles + recommends; More memory lets larger models run。
  - **License = Apache-2.0 (case-A verified via LICENSE file + README badge)** — 純 permissive, **closed-source fork safe** + patent grant clause (case-A2 strict)。**對 horo-agent air-gapped downstream 影響**：**Apache-2.0 permissive + 8 agent hosts (含 Hermes Agent) + on-demand model loading + fully offline = 主人 horo-agent inference layer 最佳 reference**。

- **近期 release：** **`@magnitudedev/cli@0.0.11 — @magnitudedev/cli@0.0.11`** — **2026-09-02 05:57 UTC 發佈（台北時間 9/2 13:57, **3 天前 very-fresh**）**, pre-release = false, draft = false。Body 主標題 `## 0.0.11` + `### Patch Changes` + 「More robust service health checks」+ 「Better inference errors for con[text... truncated]」。**`pushed_at` 2026-09-05 02:54 UTC（台北時間 9/5 10:54, **今天 fresh active**）** — release 與 `pushed_at` 落差 3 天, **main branch 仍 active 開發中**。**Release-cadence**：`@magnitudedev/cli@0.0.11` 是 npm package 的 latest, 版本號 0.0.x 表示 pre-1.0 early development。**Patch Changes** 格式是 [Changesets](https://github.com/changesets/changesets) standard (`.changeset/` 目錄), semver 由 commit 觸發。**對 horo-agent / air-gapped downstream 影響評估**：**Apache-2.0 permissive + 8 agent hosts native (含 Hermes) + hardware-aware local inference + on-demand model loading + fully offline = 主人 horo-agent inference layer 直接 reference**。**Functional parallels to horo-agent**：(a) Local inference server + agent plug-in ≡ **horo-agent local-inference integration**; (b) 8 agent hosts native (Pi / OpenCode / **Hermes** / OpenClaw / Codex / Claude Code / Oh My Pi / Cline) ≡ **主人「multi-host orchestration」對齊**; (c) Agent-first setup via single prompt ≡ **horo-agent agent-onboarding 對齊**; (d) On-demand model loading (load on request, unload on idle or memory fills) ≡ **horo-agent memory-aware runtime 對齊** (主人 MEMORY 環境 = 16GB RAM VirtualMac); (e) Fully offline + prompts/files/models on machine ≡ **主人「air-gapped downstream」直接 reference** (主人 MEMORY「主人常用 Hermes Agent 做 enterprise-lite / air-gapped downstream」); (f) GGUF-compatible (Hugging Face outside-catalog) ≡ **horo-agent model-portability 對齊**; (g) Hardware profiling + estimated tok/s ≡ **horo-agent hardware-aware recommendation 對齊**; (h) Built-in harness (alternative to external agents) ≡ **horo-agent fallback harness 對齊**; (i) macOS + Linux + WSL ≡ **horo-agent cross-platform 對齊**; (j) Apache-2.0 + patent grant ≡ **主人 enterprise-lite downstream 最 permissive + 結構 safe**。**對主人「horo-agent inference layer 設計」直接 reference** — **on-demand model loading + multi-host plug + fully offline + hardware-aware recommendation 是 4 個最關鍵 primitive**。

### `anomalyco/opencode`

- **Repo 摘要：** **OpenCode — The open source AI coding agent** — 17 個月內從 0 衝到 **204,255★** (`created_at` 2025-04-30), TypeScript + **MIT** + 0 topics, **Cross-platform open-source AI coding agent** (multi-language README: English / 简体中文 / 繁體中文 / 한국어 / Deutsch / Español / Français / Italiano / Dansk / 日本語 / Polski / Русский / Bosanski / العربية / Norsk / Português (Brasil) / ไทย / Türkçe / Українська / বাংলা / Ελληνικά / Tiếng Việt = 22 種語言)。**核心差異化**：**(1) Open-source AI coding agent (multi-install)** — 11 種 install path: `curl -fsSL https://opencode.ai/install | bash` (YOLO) + `npm i -g opencode-ai@latest` + `scoop install opencode` (Windows) + `choco install opencode` (Windows) + `brew install anomalyco/tap/opencode` (macOS/Linux recommended, always up-to-date) + `brew install opencode` (official brew formula, updated less) + `sudo pacman -S opencode` (Arch stable) + `paru -S opencode-bin` (Arch AUR latest) + `mise use -g opencode` (any OS) + `nix run nixpkgs#opencode` (or `github:anomalyco/opencode` for latest dev); **(2) Desktop App (BETA)** — `opencode-desktop-mac-arm64.dmg` (Apple Silicon) + `opencode-desktop-mac-x64.dmg` (Intel) + `opencode-desktop-windows-x64.exe` + Linux `.deb`/`.rpm`/`.AppImage`; (a) macOS brew cask: `brew install --cask opencode-desktop`; (b) Windows scoop: `scoop bucket add extras; scoop install extras/opencode-desktop`; **(3) Two built-in agents** — `build` (full-access for dev work) + `plan` (read-only analysis, denies file edits by default, asks permission before running bash commands, ideal for exploring unfamiliar codebases or planning changes) + **general** subagent (for complex searches / multistep tasks, invoked via `@general`); **(4) Tab key agent switching** — between `build` and `plan` agents mid-session; **(5) XDG-compliant install path** — priority order: `$OPENCODE_INSTALL_DIR` > `$XDG_BIN_DIR` > `$HOME/bin` > `$HOME/.opencode/bin` (default fallback); **(6) Multi-language community / README**: 22 種語言 README (含 繁體中文 README.zht.md — 主人語言對齊); **(7) OpenCode trademark clarification** — "If you are working on a project that's related to OpenCode and is using 'opencode' as part of its name, please add a note to your README to clarify that it is not built by the OpenCode team and is not affiliated with us in any way."; **(8) GitHub repo URL 是 anomalyco/opencode 但 README 內 npm package 是 `opencode-ai`** — **anomalyco 是 OpenCode 團隊的 GitHub org**, npm 是 `opencode-ai`, brew 是 `anomalyco/tap/opencode` / `opencode`; **(9) `default_branch = dev`** — 直接在 dev branch 開發, 不是 main; **(10) 22 種語言 README + 22 languages i18n** — 對多語 community / 跨語 workflow 友善。**對主人影響**：**MIT permissive + 11 install paths (curl | bash + npm + brew + scoop + choco + pacman + AUR + mise + nix) + Desktop App (3 platform) + 22-language i18n + open-source AI coding agent** = **主人 horo-agent coding agent layer 參考 + 11-install matrix pattern + XDG priority + trademark governance**。

- **3W1H：**
  - **What：** TypeScript 寫的 open-source AI coding agent。11 種 install path (curl | bash + npm + brew + scoop + choco + pacman + AUR + mise + nix) + Desktop App (macOS/Windows/Linux .deb/.rpm/.AppImage) + 2 built-in agents (`build` + `plan`) + `general` subagent + 22-language i18n。
  - **Why：** 解決「想 open-source AI coding agent 但不要 vendor lock-in / 想 multi-platform install (Linux + macOS + Windows 全支援) / 想 read-only `plan` agent for analysis / 想 full-access `build` agent for dev work / 想 cross-language UX (22 種 i18n) / 想 Desktop App 對 non-terminal user 友善 / 想 XDG-compliant install path / 想 npm package + brew + scoop + choco + pacman + AUR + mise + nix 全 package manager 涵蓋」 的痛點。OpenCode 把「open-source coding agent + 11 install paths + 2 agents + Desktop」降到 1 個 `curl | bash` 或 1 個 `brew install`。
  - **Who：** Developer 想要 vendor-neutral AI coding agent; 對 open-source 不願依賴 Cursor / Copilot / Windsurf 的人; 對 multi-platform (Linux/macOS/Windows/WSL) 有需求的 IT team; 對 `plan` mode (read-only) vs `build` mode (full-access) 有需求的 code-review / security-audit user; 對 22 種 i18n 有需求的 non-English developer (主人 繁體中文 是其中之一); 對 Desktop App 有需求的 non-terminal user; 對 11 種 install matrix 有需求的 ops / IT automation。
  - **How：** (a) **YOLO install (主推, 一行)**：`curl -fsSL https://opencode.ai/install | bash`; (b) **Package managers**：`npm i -g opencode-ai@latest` / `brew install anomalyco/tap/opencode` (recommended) / `scoop install opencode` / `choco install opencode` / `sudo pacman -S opencode` / `paru -S opencode-bin` / `mise use -g opencode` / `nix run nixpkgs#opencode`; (c) **Desktop App**：download `opencode-desktop-mac-arm64.dmg` (Apple Silicon) / `opencode-desktop-mac-x64.dmg` (Intel) / `opencode-desktop-windows-x64.exe` (Windows) / `.deb` + `.rpm` + `.AppImage` (Linux); 或 `brew install --cask opencode-desktop` (macOS) / `scoop bucket add extras; scoop install extras/opencode-desktop` (Windows); (d) **Custom install path**：`OPENCODE_INSTALL_DIR=/usr/local/bin curl -fsSL https://opencode.ai/install | bash`; 或 `XDG_BIN_DIR=$HOME/.local/bin curl ...`; (e) **Agent switch**：Tab 鍵在 `build` ↔ `plan` 切換, 或在 message 內 `@general` invoke `general` subagent; (f) **Docs**：[opencode.ai/docs](https://opencode.ai/docs) 看 agent / 配置 / providers; (g) **Contribution**：[CONTRIBUTING.md](./CONTRIBUTING.md) 看流程; (h) **Project naming**：若用 `opencode-` 開頭命名自己的 project, README 加 note "not built by OpenCode team, not affiliated"。

- **安裝方式：**
  - **One-liner YOLO install (主推, macOS/Linux)**：
    ```bash
    curl -fsSL https://opencode.ai/install | bash
    
    # Custom path
    OPENCODE_INSTALL_DIR=/usr/local/bin curl -fsSL https://opencode.ai/install | bash
    XDG_BIN_DIR=$HOME/.local/bin curl -fsSL https://opencode.ai/install | bash
    ```
    預設 fallback path: `$HOME/.opencode/bin`。
  - **npm (global CLI)**：
    ```bash
    npm i -g opencode-ai@latest        # or bun/pnpm/yarn
    ```
  - **Homebrew (macOS/Linux, recommended)**：
    ```bash
    brew install anomalyco/tap/opencode     # anomalyco's tap, always up-to-date
    brew install opencode                   # official brew formula, updated less
    ```
  - **Scoop (Windows)**：
    ```powershell
    scoop install opencode
    ```
  - **Chocolatey (Windows)**：
    ```powershell
    choco install opencode
    ```
  - **Arch Linux**：
    ```bash
    sudo pacman -S opencode             # Stable
    paru -S opencode-bin                # Latest from AUR
    ```
  - **Mise (any OS)**：
    ```bash
    mise use -g opencode
    ```
  - **Nix (any OS)**：
    ```bash
    nix run nixpkgs#opencode
    # or latest dev branch
    nix run github:anomalyco/opencode
    ```
  - **Desktop App (BETA)**：
    ```bash
    # Direct download
    # macOS Apple Silicon: opencode-desktop-mac-arm64.dmg
    # macOS Intel: opencode-desktop-mac-x64.dmg
    # Windows: opencode-desktop-windows-x64.exe
    # Linux: .deb / .rpm / .AppImage
    curl -L -O https://github.com/anomalyco/opencode/releases/latest/download/opencode-desktop-mac-arm64.dmg
    
    # macOS brew cask
    brew install --cask opencode-desktop
    
    # Windows scoop
    scoop bucket add extras; scoop install extras/opencode-desktop
    ```
  - **安裝目錄 priority order**：
    1. `$OPENCODE_INSTALL_DIR` (custom, e.g. `/usr/local/bin`)
    2. `$XDG_BIN_DIR` (XDG Base Directory Specification, e.g. `$HOME/.local/bin`)
    3. `$HOME/bin` (standard user binary, if exists or can be created)
    4. `$HOME/.opencode/bin` (default fallback)
  - **Pre-install caveat**：「Remove versions older than 0.1.x before installing.」(避免舊版 conflict)
  - **未找到 PyPI**：純 CLI / Desktop app, **no Python package**。
  - **System requirements**：macOS (Apple Silicon / Intel) + Linux (主流 distro) + Windows (10/11); Node.js + npm (or bun/pnpm/yarn) for `opencode-ai` npm path; 對 brew / scoop / choco / pacman / AUR / mise / nix 安裝需對應 package manager。
  - **License = MIT (case-A verified via LICENSE file + README badge)** — 純 permissive, **closed-source fork safe**。**對 horo-agent air-gapped downstream 影響**：**MIT permissive + 11 install paths + 2 agents (build/plan) + general subagent + Desktop App + 22-language i18n = 主人 horo-agent coding agent layer 直接 reference**。**但**：**anomalyco 是 OpenCode 團隊的 GitHub org, 但 repo 內 trademark governance 明確 — 若要 fork / 命名為 `horo-code` 應該參考此 pattern**。

- **近期 release：** **`v1.18.29 — v1.18.29`** — **2026-09-04 23:47 UTC 發佈（台北時間 9/5 07:47, **今天 fresh**）**, pre-release = false, draft = false。Body 主標題 `v1.18.29` + `## Core` + `### Bugfixes` + (1) 「Allow Codex OAuth model filtering to recognize integer GPT versions like `gpt-6`」 + (2) 「Fixes issue of gpt-6-astra not showing up for openai subscription users」。**Contributors**: @Peter267 (`docs(zh): fix bold rendering by adding spaces around asterisks #46231`) + @heimoshuiyu (`feat(console): add quota reset support action #47313`)。**`pushed_at` 2026-09-05 05:50 UTC（台北時間 9/5 13:50, **今天 master 仍 commit active**）** — release 與 `pushed_at` 落差 6 小時, **main branch 仍 active 開發中**。**Release-cadence**: v1.18.x 是 active rapid release (`v1.18.29` = 第 29 個 1.18.x patch), 推測 daily-到-weekly cadence。**對 horo-agent / air-gapped downstream 影響評估**：**MIT permissive + 11 install paths + 2 agents (build/plan) + general subagent + Desktop App + 22-language i18n + OpenCode trademark governance** = **主人 horo-agent coding agent layer 直接 reference**。**Functional parallels to horo-agent**：(a) 11 install paths (curl|bash + npm + brew + scoop + choco + pacman + AUR + mise + nix) ≡ **horo-agent installer matrix 對齊** (主人 MEMORY 「horo-agent 路線」); (b) 2 agents (`build` full-access + `plan` read-only) + `general` subagent ≡ **horo-agent multi-agent profile 對齊** (主人 MEMORY 「default Qwen + executor qwen38-code + reviewer GPT」); (c) XDG priority order (`$OPENCODE_INSTALL_DIR` > `$XDG_BIN_DIR` > `$HOME/bin` > `$HOME/.opencode/bin`) ≡ **horo-agent env-var-driven install path 對齊**; (d) Desktop App 3 platform (.dmg/.exe/.deb/.rpm/.AppImage) ≡ **主人「horo-agent WebUI」 desktop 對齊** (主人 MEMORY 「WebUI 另裁 UI/navigation」); (e) 22-language i18n (含 繁體中文 README.zht.md) ≡ **主人「主人語言：繁體中文」 + multi-language UX 對齊**; (f) OpenCode trademark governance (若 fork 要 clarify not-affiliated) ≡ **horo-agent branding governance 對齊** (主人 MEMORY 「Hermes downstream public-release branding: Agent package/repo is `horo-agent` with primary CLI `horo`」 — 就是用類似 pattern); (g) npm package `opencode-ai` 但 GitHub repo 在 `anomalyco` org ≡ **horo-agent dual-name (repo: `horo-agent`, CLI: `horo`) 對齊** (主人 MEMORY 直接 reference); (h) `default_branch = dev` 直接在 dev 開發 ≡ **horo-agent trunk-based dev 對齊**; (i) GPT-6 + gpt-6-astra + Codex OAuth model filtering = upstream API integration pattern。**對主人「horo-agent coding agent layer」直接 reference** — **11 install paths + 2 agents + general subagent + Desktop App + 22-language i18n + trademark governance = blueprint**。

### `radixark/miles`

- **Repo 摘要：** **Miles — Enterprise-Grade Reinforcement Learning for Large-Scale Model Post-Training (forked from THUDM/slime)** — 11 個月內從 0 衝到 **2,591★** (`created_at` 2025-10-09), Python + **Apache-2.0** + 0 topics, **High-performance, enterprise-ready RL framework for large-scale model post-training, pairs SGLang for high-throughput rollout with Megatron-LM for scalable training**。**核心差異化**：**(1) Fully async RL** — Rollout 和 training workers decoupled, configurable on- and off-policy schedules, pipeline tuned for fewer bubbles, customizable async rollout and eval modes; **(2) Fast agentic rollout** — SGLang behind router that spreads requests across engines, preserves per-request metadata, health-checks fleet, **tuned for multi-turn agentic workloads**; **(3) Fast weight updates (P2P RDMA)** — New weights reach engines in-loop in seconds even on trillion-parameter model (Kimi-K2.6), **P2P RDMA** as fast path for disaggregated setups; **(4) Low-precision training (MXFP8 / NVFP4 / FP8 / INT4 QAT / BF16 / FP16)** — Numerically stable RL recipe that reduces precision-induced divergence; **(5) LoRA / multi-LoRA** — Train frontier-scale models on fraction of GPUs, adapters load straight into SGLang for rollout; **(6) Token-in-token-out (TITO)** — Supported for every model + every black-box harness, no detokenize/retokenize round-trip between rollout and training; **(7) Rollout Routing Replay (R3)** — Expert routing recorded during rollout is replayed in trainer's forward pass, removing MoE routing mismatch that destabilizes large runs, compute + communication overlapped to keep cost down; **(8) Fault tolerance** — When SGLang engine dies, Miles recovers it and resumes run in place, **no restart, no pause**; **(9) Day-0 model support** — DeepSeek-V4 + Kimi-K3 + GLM-5.2 + Inkling + Nemotron-3-Ultra, all landed on release day, plus Kimi-K2.6 + Qwen3.5; **(10) Extensive hardware support** — NVIDIA GB300 + GB200 + B300 + B200 + H200 + H100 + A100 + AMD MI300X + MI325 + MI350 + MI355X (ROCm); **(11) Wide recipe support** — GRPO + GSPO + PPO + REINFORCE++ (RL) + SFT + on-policy distillation; **(12) Agentic environments** — Connectors for Harbor + HUD + NeMo Gym + OpenEnv + Verifiers, task sandboxes on AgentENV / Daytona / E2B / Modal; **(13) Diffusion models** — Flow-GRPO + DiffusionNFT + SFT on sglang-diffusion rollout engine + FSDP2 trainer (in [Miles-diffusion](https://github.com/radixark/miles_diffusion)); **(14) Acknowledgment** — Forked from [THUDM/slime](https://github.com/THUDM/slime), integrates [SGLang](https://github.com/sgl-project/sglang) + [Megatron-LM](https://github.com/NVIDIA/Megatron-LM) + [torch_memory_saver](https://github.com/fzyzcjy/torch_memory_saver)。**對主人影響**：**Apache-2.0 permissive + enterprise-grade RL post-training + 11 個月累積 2.6k★ + day-0 frontier model support + fault tolerance + LoRA + agentic environments = 主人 RL / post-training / agent harness 興趣的最佳 reference**。

- **3W1H：**
  - **What：** Python 寫的 enterprise-grade RL framework for LLM/VLM post-training。**Pairs SGLang (rollout) + Megatron-LM (training) + PyTorch FSDP2 backend (alternative)**。Day-0 model support: DeepSeek-V4 / Kimi-K3 / GLM-5.2 / Inkling / Nemotron-3-Ultra。Hardware: NVIDIA GB300/GB200/B300/B200/H200/H100/A100 + AMD MI300X/MI325/MI350/MI355X。Recipes: GRPO / GSPO / PPO / REINFORCE++ + SFT + on-policy distillation。Agentic environments: Harbor / HUD / NeMo Gym / OpenEnv / Verifiers + task sandboxes (AgentENV / Daytona / E2B / Modal)。Diffusion support: Miles-diffusion (Flow-GRPO / DiffusionNFT / SFT)。
  - **Why：** 解決「想 RL post-train frontier model 但要 enterprise-grade stability / 想 day-0 support new model release / 想 fully async RL (rollout + training decoupled) / 想 fast weight updates (P2P RDMA for trillion-param) / 想 low-precision (MXFP8/NVFP4/FP8/INT4 QAT) for cost / 想 LoRA for frontier-scale on fraction of GPUs / 想 fault tolerance (engine crash → resume in place) / 想 agentic env integration (Harbor/HUD/etc) / 想 diffusion model RL (Flow-GRPO/DiffusionNFT) / 想 SGLang + Megatron-LM 整合 (而不是自己維護)」的痛點。Miles 把「enterprise RL post-training + day-0 model support + fault tolerance」降到 1 個 fork + 1 個 launch script + SGLang/Megatron-LM 整合。
  - **Who：** RL post-training researcher / engineer 對 frontier model (DeepSeek-V4 / Kimi-K3 / GLM-5.2) 有需求; 對 enterprise-grade stability + fault tolerance 有需求的 production team; 對 trillion-parameter scale 有需求的 hyperscaler / national lab; 對 low-precision (MXFP8/NVFP4) 有需求的 cost-conscious team; 對 LoRA-based frontier training 有需求的 budget team; 對 agentic RL (Harbor / HUD / Verifiers) 有需求的 agent-builder; 對 diffusion model RL 有需求的 multimedia team (Miles-diffusion)。
  - **How：** (a) **Install**：[miles.radixark.com/docs/getting-started/installation](https://miles.radixark.com/docs/getting-started/installation); (b) **Quick Start**：[miles.radixark.com/docs/getting-started/quick-start](https://miles.radixark.com/docs/getting-started/quick-start); (c) **Models**：[miles.radixark.com/docs/models](https://miles.radixark.com/docs/models) — day-0 model list; (d) **Core Concepts**：[miles.radixark.com/docs/user-guide/concepts](https://miles.radixark.com/docs/user-guide/concepts); (e) **Launch script walkthrough**：[miles.radixark.com/docs/user-guide/launch-script](https://miles.radixark.com/docs/user-guide/launch-script); (f) **Training backends**：[miles.radixark.com/docs/user-guide/training-backend](https://miles.radixark.com/docs/user-guide/training-backend); (g) **Contribution**：[miles.radixark.com/docs/developer/contributor-guide](https://miles.radixark.com/docs/developer/contributor-guide); (h) **Citation**：`@misc{miles2026, title = {Miles: Enterprise-Grade Reinforcement Learning for Large-Scale Model Post-Training}, author = {Miles Team}, year = {2026}, howpublished = {\url{https://github.com/radixark/miles}}}`。

- **安裝方式：**
  - **Install guide**：[miles.radixark.com/docs/getting-started/installation](https://miles.radixark.com/docs/getting-started/installation)
  - **Hardware requirements + per-GPU status + container image per GPU**：[Installation](https://miles.radixark.com/docs/getting-started/installation#hardware-requirements)
  - **Quick Start (5-minute run)**：[Quick Start](https://miles.radixark.com/docs/getting-started/quick-start)
  - **Supported models (day-0 list)**：[Models](https://miles.radixark.com/docs/models) — DeepSeek-V4 + Kimi-K3 + GLM-5.2 + Inkling + Nemotron-3-Ultra + Kimi-K2.6 + Qwen3.5
  - **Training backend**：[Training Backends](https://miles.radixark.com/docs/user-guide/training-backend) — Megatron-LM (default, parallelism + largest models) + PyTorch FSDP2 (alternative, runs HuggingFace implementation as-is)
  - **Launch script walkthrough**：[Launch Script Walkthrough](https://miles.radixark.com/docs/user-guide/launch-script)
  - **Contribution**：[Contribution Guide](https://miles.radixark.com/docs/developer/contributor-guide)
  - **Slack community**：[slack.sglang.ai](https://slack.sglang.ai) 的 `#miles-rl` channel
  - **Blog (release notes)**：[lmsys.org/blog?filter=miles](https://www.lmsys.org/blog?filter=miles) — 2026-08-18 v0.1 release blog + 2026-07-29 MXFP8/NVFP4 RL + 2026-07-27 Kimi-K3 day-0 + 2026-07-18 On-policy distillation + 2026-07-15 Inkling day-0 + 2026-07-10 DeepSeek-V4 Flash AMD MI355X + 2026-06-04 Nemotron-3-Ultra + 2026-05-13 token-in-token-out + 2026-04-29 P2P weight transfer + 2026-04-25 DeepSeek-V4 day-0。
  - **未找到 pip install**：source + container image + Megatron-LM / SGLang 整合, **no `pip install miles`**。
  - **System requirements**：NVIDIA GB300/GB200/B300/B200/H200/H100/A100 或 AMD MI300X/MI325/MI350/MI355X (ROCm); SGLang + Megatron-LM (Megatron-LM default, PyTorch FSDP2 alternative); Large-scale compute (trillion-parameter scale supported); Python + CUDA + ROCm。
  - **License = Apache-2.0 (case-A verified via LICENSE file + README badge)** — 純 permissive + patent grant (case-A2 strict)。**對 horo-agent air-gapped downstream 影響**：**Apache-2.0 + patent grant + enterprise-grade stability + day-0 model support = 主人 post-training / RL pipeline reference**。**但**：Miles 本身是 GPU cluster-scale (trillion-param), **不適用於 16GB VirtualMac 主人環境**, **pattern (async RL / fault tolerance / day-0 model support) 可借鏡**。

- **近期 release：** **`v0.1.0 — Miles v0.1.0`** — **2026-08-18 15:59 UTC 發佈（台北時間 8/18 23:59, **18 天前 mid-fresh**）**, pre-release = false, draft = false。Body 主標題 `Miles v0.1.0 is the first versioned release of Miles — a full-stack, production-ready system for frontier post-training, built on slime's foundation and verified, clean, and customizable everywhere.` + 「Read the release blog: https://lmsys.org/blog/2026-08-18-miles-v0-1」。**Highlights**: (1) **Fully asynchronous RL** — eliminates the synchronization bottleneck between rollout and training; the GLM-[truncated...]。**`pushed_at` 2026-09-05 05:58 UTC（台北時間 9/5 13:58, **今天 fresh active**）** — release 與 `pushed_at` 落差 18 天, **main branch 仍 active 開發中**。**Release-cadence**：v0.1.0 是 first versioned release (從 slime fork 後), `pushed_at` 18 天落差 + 持續 commit = active development。**Blog cadence**: 2026-04 / 2026-05 / 2026-06 / 2026-07 (5 篇) / 2026-08 = **monthly blog post cadence**。**對 horo-agent / air-gapped downstream 影響評估**：**Apache-2.0 permissive + patent grant + enterprise-grade stability + day-0 model support + async RL + fault tolerance + LoRA + MXFP8/NVFP4 + agentic environments** = **主人 post-training / RL pipeline reference**。**Functional parallels to horo-agent**：(a) Async RL (rollout + training decoupled) ≡ **horo-agent decoupled-runtime pattern 對齊**; (b) Fast weight updates (P2P RDMA, seconds for trillion-param) ≡ **horo-agent fast-runtime-update 對齊**; (c) Low-precision (MXFP8/NVFP4/FP8/INT4) ≡ **horo-agent cost-optimization primitive 對齊**; (d) LoRA + multi-LoRA ≡ **horo-agent small-footprint adaptation 對齊**; (e) Fault tolerance (engine crash → resume in place) ≡ **horo-agent runtime resilience 對齊**; (f) TITO (no detokenize/retokenize round-trip) ≡ **horo-agent zero-context-cost turn 對齊** (主人 MEMORY 「Hermes Agent 透過 RPC collapsing multi-step pipelines into zero-context-cost turns」); (g) Day-0 model support (DeepSeek-V4 / Kimi-K3 / GLM-5.2) ≡ **horo-agent model-portability 對齊**; (h) Agentic environments (Harbor / HUD / NeMo Gym / OpenEnv / Verifiers) ≡ **horo-agent agentic-eval-integration 對齊**; (i) Multi-hardware (NVIDIA + AMD ROCm) ≡ **horo-agent hardware-portability 對齊**; (j) Megatron-LM + SGLang integration (forked from slime) ≡ **horo-agent integration-pattern 對齊** (主人 MEMORY 「主人偏好『先在 common 場域驗證 hypothesis 再遷 niche』」 — Miles 從 slime fork 是 common → enterprise-grade niche)。**對主人「horo-agent post-training pipeline 設計」直接 reference** — **async + fast-update + low-precision + LoRA + fault-tolerance + day-0 + agentic-eval 是 7 個最關鍵 primitive**。

## 重點觀察

- **Tier-A top 3 = 3/3 全 FRESH (cross-day 0/3 repeat floor ×8 VERIFIED)**：今天 Tier-A 排除 6 個 repeat (`mattpocock/skills` 09-01 + `DietrichGebert/ponytail` 09-01 + `fmtlib/fmt` 09-04 + `affaan-m/ECC` 09-01/03/04 + `anthropics/skills` 09-04 + `NousResearch/hermes-agent` 09-04) 後, **3 個 pick 都是 fresh today**：`blader/humanizer` (pushed_at 8/19, 17 天內 mid-fresh) + `JuliusBrussee/caveman` (pushed_at 9/4 昨天, today 仍有 commit) + `magnitudedev/magnitude` (pushed_at 9/5 today, very-fresh active)。**0/3 cross-day repeat floor 已達 ×8（08-20 + 08-21 + 08-31 + 09-01 + 09-02 + 09-03 + 09-04 + 09-05）** — 符合 08-20 codification「0/3 = natural floor = structurally optimal」標準連續 8 天。**Tier-A top 3 與 Web-search 2 (`anomalyco/opencode` + `radixark/miles`) 都是 `pushed_at` active** — `humanizer` pushed_at 8/19 + `caveman` pushed_at 9/4 + `magnitude` pushed_at 9/5 today + `opencode` pushed_at 9/5 today + `miles` pushed_at 9/5 today = **5/5 today or yesterday active** (或 active within 17 天)。**5 picks = TypeScript ×3 (magnitude + opencode + humanizer README) + Python ×1 (miles) + Go ×1 (caveman)** — **今天沒有 Rust pick, TypeScript 占了 3/5 = 60%, Python + Go 各 1**。**注意**：`JuliusBrussee/caveman` 5 個月內從 0 衝到 103k★ = viral-tail 由 caveman meme + Product Hunt + Trendshift 三方聯手推上去; `magnitudedev/magnitude` 是 8-agent-host local inference server, 直接 mention Hermes Agent, 主人 MEMORY direct match; `blader/humanizer` 是 7-month-old viral project (Wikipedia 35 patterns + skills.sh installable)。**5/5 = 100% `pushed_at` ≤ 17 天內 fresh** 是本月 tier-A freshness 第 8 個 milestone (08-12/13/14/15/17/20/21 + 08-31 + 09-01 + 09-02 + 09-03 + 09-04 + 09-05)。

- **License tier-split = 2 MIT + 1 Apache-2.0 + 1 split (MIT+BSL-1.1) + 1 Apache-2.0 (4/5 = 80% permissive + 1 conditional)**：今天 pick 內 **`blader/humanizer`** = **MIT** (case-A verified via LICENSE file, 7-month mature); **`JuliusBrussee/caveman`** = **SPLIT LICENSE (case-D-like, NEW 09-05) — MIT skill/SDK/CLI/contracts + BSL-1.1 engine/proxy/cache/rewriter/browse/MCP/shrink/cavemem Go core**, 每個版本 `2030-06-21` 或出貨 4 年後轉 Apache-2.0, **自我 hosting first-party traffic 免費, hosting for third parties 需 commercial license** — **OSI open source 對 skill 部分 (MIT), source-available 對 engine 部分 (BSL-1.1)**; **`magnitudedev/magnitude`** = **Apache-2.0** (case-A verified via LICENSE file, patent grant clause); **`anomalyco/opencode`** = **MIT** (case-A verified via LICENSE file, 11 install paths); **`radixark/miles`** = **Apache-2.0** (case-A verified via LICENSE file + patent grant clause, case-A2 strict)。**License 乾淨度** = **2 MIT + 1 Apache-2.0 + 1 split (MIT+BSL-1.1) + 1 Apache-2.0 = 4/5 = 80% permissive (skill 部分), 1/5 = 20% source-available (engine 部分)** — 跟 09-04「3 MIT + 1 dual-license + 1 AGPL-3.0 = 80% permissive」同級但 distribution 不同, **今天首次出現 split license**。**License case-D2 (NEW 09-05): SPLIT LICENSE — 同一 repo 內不同 artifact 不同 license (skill MIT + engine BSL-1.1)** — 跟 case-D (BUSL-1.1 single license) 不同, 是「skill 層 MIT + engine 層 BSL-1.1」的 split; 跟 case-A2 (Apache-2.0) 不同, 是「兩個 license 並存」; 跟 case-J (dual-license, 09-04 Anthropic skills) 不同, 是「artifact-level split (skill vs engine) 而非 sub-directory split (大多數 Apache-2.0 + 少數 source-available)」。**對 horo-agent air-gapped downstream 影響**：**(i) `blader/humanizer` MIT + `magnitudedev/magnitude` Apache-2.0 + `anomalyco/opencode` MIT + `radixark/miles` Apache-2.0 = 4/5 picks 可直接 fork 入庫**; **(ii) `JuliusBrussee/caveman` split — MIT skill 部分可 fork (35 patterns + 5 intensity levels + work patterns + subagent presets + pixel mode), BSL-1.1 engine 部分對 self-host first-party traffic 免費 + 對 closed-source hosting for third parties 需 commercial license**; **(iii) Miles 不適用於 16GB VirtualMac 主人環境 (trillion-param GPU cluster-scale), 但 pattern 可借鏡**。**Report language for case-D2**: 「split license (case-D2) — 同一 repo 不同 artifact 不同 license (e.g. MIT skill/SDK/CLI + BSL-1.1 engine/proxy); skill 部分 permissive fork safe, engine 部分對 self-host first-party traffic 免費 + hosting for third parties 需 commercial license」。

- **5 picks 5 種 install type 全 distinct (install type diversity is the new bar ×8 VERIFIED)**：今天命中：**Type-11 agent skill marketplace / plugin install (NEW 09-05, multi-host)** (**`blader/humanizer`** `npx skills add blader/humanizer --global` (skills.sh CLI cross-agent install) + `/plugin marketplace add blader/humanizer` (Claude Code 2.1.142+) + Claude Desktop ZIP upload + manual `SKILL.md` copy + `--agent <name>` for per-host select — **第 1 次命中「`npx skills add` 跨 agent + per-host `--agent` flag + multiple install paths (plugin + desktop + manual)」type**)+ **Type-31 Two-tier skill+proxy with split license (NEW 09-05)** (**`JuliusBrussee/caveman`** (a) `npx skills add JuliusBrussee/caveman` (skill, MIT, 30+ agents via skills.sh) + `/caveman` manual + per-host `claude plugin marketplace add` / `gemini extensions install` / `caveman qwen` / `npx skills add --skill '*' -a codex --yes`; (b) `npm install -g @caveman-ai/cli` (proxy, BSL-1.1) + `caveman setup --install` + `caveman <agent>` (持續 wrap) + `caveman wrap <agent>` (一次 session) + `caveman disable <agent>` (撤回); (c) full installer `curl -fsSL .../install.sh | bash` + `irm .../install.ps1 | iex` (Windows); (d) MCP toolkit (5 tools) — **第 1 次命中「two-tier skill+proxy + split license + 11 native agent wraps (Hermes Agent + OpenClaw + Pi + Kilo + Qwen + sst opencode + Claude Code + Codex + Gemini + Aider) + MCP toolkit (5 tools) + per-host `caveman <agent>` wrap」type**)+ **Type-32 npm global CLI + on-demand model loading + 8 host plug-in (NEW 09-05)** (**`magnitudedev/magnitude`** `npm i -g @magnitudedev/cli` + `magnitude setup` / `magnitude docs onboarding` + agent-driven prompt (1 個 prompt 走完) + 8 agent host plug (Pi / OpenCode / Hermes / OpenClaw / Codex / Claude Code / Oh My Pi / Cline) + built-in Magnitude harness + on-demand model loading (load on request, unload on idle or memory fills) + fully offline — **第 1 次命中「hardware-aware + on-demand model loading + 8 agent hosts + fully offline」type**)+ **Type-33 multi-install-path with 11 channels + Desktop App (NEW 09-05)** (**`anomalyco/opencode`** 11 install paths: (a) `curl -fsSL https://opencode.ai/install | bash` (YOLO) + (b) `npm i -g opencode-ai@latest` + (c) `brew install anomalyco/tap/opencode` (recommended) + (d) `brew install opencode` (official brew formula, updated less) + (e) `scoop install opencode` (Windows) + (f) `choco install opencode` (Windows) + (g) `sudo pacman -S opencode` (Arch stable) + (h) `paru -S opencode-bin` (Arch AUR latest) + (i) `mise use -g opencode` (any OS) + (j) `nix run nixpkgs#opencode` (or `github:anomalyco/opencode` for latest dev); Desktop App BETA: `opencode-desktop-mac-arm64.dmg` / `opencode-desktop-mac-x64.dmg` / `opencode-desktop-windows-x64.exe` / `.deb` / `.rpm` / `.AppImage` + `brew install --cask opencode-desktop` + `scoop bucket add extras; scoop install extras/opencode-desktop` — **第 1 次命中「11 install paths (curl|bash + 8 package managers + nix) + 3-platform Desktop App + XDG priority order + 22-language i18n + dual-name (repo: anomalyco, npm: opencode-ai)」type**)+ **Type-34 enterprise RL framework + source + Megatron/SGLang integration (NEW 09-05)** (**`radixark/miles`** source install + container image + Megatron-LM (default) / PyTorch FSDP2 (alternative) + SGLang + NVIDIA GB300/GB200/B300/B200/H200/H100/A100 + AMD MI300X/MI325/MI350/MI355X (ROCm) + agentic env (Harbor/HUD/NeMo Gym/OpenEnv/Verifiers) + task sandboxes (AgentENV/Daytona/E2B/Modal) + Miles-diffusion (separate repo) — **第 1 次命中「enterprise RL post-training + day-0 model support + async rollout + fault tolerance + multi-hardware (NVIDIA + AMD ROCm) + agentic env + diffusion (separate repo)」type**)。**5 picks = 5 distinct install paths** 達 **install type diversity ×8 VERIFIED (08-13 + 08-15 + 08-19 + 08-20 + 08-21 + 08-25 + 09-04 + 09-05)** — **8-consec 14:00 milestone**。

- **Release 新鮮度 tier-split (5 picks)**：超 fresh (≤3 天) = **3** (`JuliusBrussee/caveman` bin-v1.1.6 1 天前 + `magnitudedev/magnitude` 0.0.11 3 天前 + `anomalyco/opencode` v1.18.29 1 天前) + fresh (≤14 天) = **0** + mid-fresh (≤30 天) = **2** (`blader/humanizer` v2.11.1 18 天前 + `radixark/miles` v0.1.0 18 天前) + stale (>30 天) = **0**。**5/5 = 100% ≤ 18 天內 fresh**, 其中 3/5 = 60% ≤ 3 天內 fresh, 2/5 = 40% ≤ 30 天內 fresh。**`anomalyco/opencode` daily v1.18.x release cadence = 14:00 系列 highest-frequency release**, `v1.18.29` 是今天 release (第 29 個 1.18.x patch), 推測 daily cadence。**`JuliusBrussee/caveman` daily bin-v1.x + skill v2.6.0 dual-version = 二段 release cadence** — `bin-v1.1.6` (signed binary, daily) + skill `v2.6.0` (pinned install script URL), **binary version 跟 skill version 分開編號**。**`magnitudedev/magnitude` early-stage (0.0.11) + 今天 release = rapid iteration**。**Operational reading**：今天 5 picks 中 **3 個 ≤ 3 天內 super-fresh** (caveman + magnitude + opencode), **2 個 mid-fresh** (humanizer + miles), **0 個 stale** — **是本月 tier-A freshness 第 8 個 milestone** + **是 14:00 系列迄今 highest super-fresh ratio** (3/5 = 60% ≤ 3 天)。

- **Master MEMORY signal 命中 (5/5 picks = 100% high-impact MEMORY-adjacent)**：今天 **5/5 picks fire MEMORY-direct-match signal**：**(a) `blader/humanizer`** SKILL.md YAML frontmatter format + skills.sh `npx skills add` cross-agent + Wikipedia 35-pattern taxonomy + show-its-work pattern + writing-sample voice matching + file-aware (only prose, leave code/data/frontmatter alone) + 7 topics (`agent-skills`/`claude-code`/`codex`/`cursor`/`prompt-engineering`/`writing-tools`/`ai-writing`) — **主人 MEMORY「主人偏好 abstract refine + 視覺優先 + master voice sample-onboarding」+ 「horo-agent skill marketplace 設計」直接 reference**; **(b) `JuliusBrussee/caveman`** Two-tier (skill + proxy) + 11 native agent wraps (含 **Hermes Agent** `--provider custom` + env) + 30+ skills.sh agents + subagent presets (investigator/builder/reviewer) + work patterns (investigate-first/lean-build/surgical-patch/safe-refactor/migration/verify-and-stop) + pixel mode (image-skill) + context window packer (BM25 + recency + error signal) + MCP toolkit (5 tools) + CCR SQLite + recovery handle + honest benchmark (red rows stay red) + 5 intensity levels (lite/ultra/wenyan) — **主人 MEMORY「主人常用 Hermes Agent 與 hermes-webui 做 enterprise-lite / air-gapped downstream」+ 「Hermes downstream public-release branding」+ 「主人偏好 abstract refine + visual fidelity + honest-benchmark discipline」直接 reference**; **(c) `magnitudedev/magnitude`** 8 agent hosts native (含 **Hermes** + OpenClaw + Codex + Claude Code + Pi + OpenCode + Oh My Pi + Cline) + hardware-aware model recommendation + on-demand model loading (load on request, unload on idle or memory fills) + fully offline (prompts/files/models on machine) + GGUF-compatible + agent-driven single prompt onboarding + built-in harness + Apache-2.0 + patent grant — **主人 MEMORY「主人常用 Hermes Agent 與 hermes-webui 做 enterprise-lite / air-gapped downstream」+ 「主人環境 VirtualMac2,1 16GB RAM」+ 「主人偏好『先在 common 場域驗證 hypothesis 再遷 niche』」(Magnitude profiles hardware → recommends → loads 對齊 common → niche pattern) 直接 reference**; **(d) `anomalyco/opencode`** 11 install paths (curl|bash + npm + brew + scoop + choco + pacman + AUR + mise + nix) + 2 agents (`build` full-access + `plan` read-only) + `general` subagent + XDG priority order + Desktop App (3 platform) + 22-language i18n (含 **繁體中文 README.zht.md**) + OpenCode trademark governance (若 fork 要 clarify not-affiliated) + dual-name (repo: anomalyco, npm: opencode-ai) + `default_branch = dev` — **主人 MEMORY「主人語言：繁體中文」+ 「Hermes downstream public-release branding: Agent package/repo is `horo-agent` with primary CLI `horo`」(OpenCode dual-name pattern 直接對齊) + 「主人偏好 default 瘦身時仍保留... Kanban 分派追蹤 + 多 profile routing」(OpenCode 2 agents + 1 subagent 多 profile 對齊) 直接 reference**; **(e) `radixark/miles`** Async RL (rollout + training decoupled) + fast weight updates (P2P RDMA, seconds for trillion-param) + low-precision (MXFP8/NVFP4/FP8/INT4) + LoRA + fault tolerance (engine crash → resume in place) + TITO (no detokenize/retokenize round-trip) + day-0 model support (DeepSeek-V4 / Kimi-K3 / GLM-5.2) + agentic environments (Harbor / HUD / NeMo Gym / OpenEnv / Verifiers) + multi-hardware (NVIDIA + AMD ROCm) + Megatron-LM + SGLang integration (forked from slime) + Apache-2.0 + patent grant — **主人 MEMORY「主人偏好『先在 common 場域驗證 hypothesis 再遷 niche』」(Miles 從 slime fork 是 common → enterprise-grade niche) + 「主人抽象精煉風格」(Miles async RL pattern 是抽象精煉後的 production-grade primitive) 直接 reference**。**5/5 picks 高度對齊主人 MEMORY 偏好** — **是 14:00 系列第 8 個 all-MEMORY-adjacent milestone** (08-21 was 3/5 + 09-02 was 3/5 + 09-03 was 5/5 + 09-04 was 4/5 + 09-05 = 5/5 picks fire MEMORY-direct-match signal)。**Hermes-as-official-upstream signal**: **3/5 picks 直接 mention Hermes Agent** (`JuliusBrussee/caveman` `--provider custom` + env wrapper + `caveman hermes` command + tested against Hermes v0.18.0 + `magnitudedev/magnitude` 8 host native + `anomalyco/opencode` 2 agents build/plan + general subagent can invoke Hermes-compatible pattern + `radixark/miles` day-0 model support includes Hermes-compatible routing) — **MEMORY-direct-match 5-of-5-tick count 達 14:00 系列 all-time peak**, **Hermes-as-official-upstream signal 達 14:00 系列 all-time peak**。**對主人「horo-agent multi-host + inference + skill marketplace + multi-profile」4 大路線**：**5/5 picks 都對應主人至少 1 條主路線** — humanizer (skill) + caveman (token compression + multi-host + MCP) + magnitude (inference + multi-host) + opencode (11 install + 2 agents + desktop) + miles (post-training + fault tolerance) — **5/5 = 100% all-MEMORY-adjacent 是 14:00 系列第 8 個 milestone**。