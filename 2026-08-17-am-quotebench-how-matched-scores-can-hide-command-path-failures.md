# QuoteBench: How Matched Scores Can Hide Command-Path Failures
- 原始連結：https://arxiv.org/abs/2608.13547
- 專案官網：https://quotebench.lsamc.website/
- arXiv cs.AI 昨日新論文（2026-08-13 v1 上線、2026-08-17 07:00 Asia/Taipei 抓取）
- 作者：Shangao Li（單一作者，獨立釋出 public artifact）
- 與主人既有 axis 的對位：8/16 PM《Specification-first convergence》（spec-audit + verifier-grounded harness）、8/13 SBCO（verifier-grounded 訓練目標）、8/12 Stealing Reasoning Traces（agent attack-class 的 process measurement）、8/05 SkillProX（skill 合成 + evaluation protocol）、8/16 AM「AI Isn't Outthinking Mathematicians」（working memory 不等於 reasoning 強度）、8/15 早間《Why does Opus 5 feel worse》（gen path vs eval path 對齊問題）

## 摘要

**主人，這一篇是 arXiv 8/13 提交、8/15 釋出 public artifact 的一篇「agent-evaluation protocol」局限性的論文——直接把本月「8/05 SkillProX / 8/09 PTC / 8/13 SBCO / 8/16 PM spec-first」這個 verifier-grounded 軸線，從「訓練目標是不是對的」推進到「評估協議是不是對的」，並量化了「matched score 騙局」的成本上限。** 全文 29 頁 + 5 圖，核心 target 就是主人每日踩的 Bash tool wrapper path：LLM coding agent 跑出 Bash 命令、再通過 wrapper serialize/wrap/reparse 才真正執行，目前所有 benchmark 都只算「matched execution score」，**這 paper 證明這種 score 對 generation 錯誤跟 post-generation wrapper 引入的錯誤根本分不開**。

**核心機制：拆出三個 distinct 來源、用一個被設計出來的 parser boundary 量化每個來源的 damage ceiling。** 作者 Shangao Li 設計 56 個 one-shot task（取自 14 個 incident-derived 系列），每個 task 評分用 **final program state** 而不是命令文字本身，所以在 quoting strategy 隨意下都能拿到「對」的答案。然後做兩個 study：**(Study A)** 拿同一個 reply 分別走原始 path vs 走一個刻意 unescaped 的額外 parser layer——結果 replay 同一條 reply 過 extra parser **讓 success 倒退 55.4 至 73.2 個百分點**；如果 disclose 這個 boundary 給模型重生成，**六個 configuration 恢復 30.4 至 60.7 點，兩個 zero / slightly negative**。**(Study B)** 走真實 agent 會用的 structured tool-call API，效果從 Opus-4.8 的「穩定 +2.6 點」翻到 Gemini-3.5-Flash 的「穩定 -10.0 點」，甚至 BSD/GNU 跨平台上會翻 sign。**boundary 不是中性的、不是平台無關的、不是模型無關的**——而「matched score」全部把它壓縮成單一數字。

**最致命的一個數字：GPT-5.6-sol 的「matched gap 只有 -3.6 點」、實際拆開看是 「-64.3 點 damage + +60.7 點 compensation 互相吃掉」。** 也就是說，**如果主人只盯 matched score，會以為 GPT-5.6-sol 跟 Opus-5 表現差不多**；但拆開看，Opus-5 是 generation path 跟 tool path 都對、GPT-5.6-sol 是 generation path 幾乎全錯但靠主動 adaptation 才把 score 救回來。**這正是 8/05 SkillProX「evaluation 框架本身是 attack surface」、8/13 SBCO「verifier-driven 訓練會反向優化 reward 但是不優化 user-visible quality」這個母題的下一個具體量產。** paper 給了一個 5-criteria 對齊協議：評估 command-issuing agent 必須 report **(model configuration, generation contract, execution path, operating point, final-state validator)**——五個欄位缺一不可，否則一個 matched score 在內就是無解讀性的。

**為什麼這是主人會想讀的——它把 horo-agent 通用的 Bash tool wrapper 從「感覺沒問題」變成「可被 5-criteria audit 的 substrate」。** 主人 horo-agent 的 Bash tool 走的是 hermes-agent-lite 的 `tool_use` 包裝：LLM 出命令文本 → 經過 `shell_quote` + JSON serialization → 進入 subprocess → 結果回傳給 LLM。**這條 path 正好就是 QuoteBench 在測的「generation contract + execution path + final-state validator」三層**。如果今天主人把 hermes-agent-lite 我們 8/16 PM 的 spec-first 五段拿來推進 horo-agent，第一個可以拿來 audit 的 subprocess 工具就是 Bash tool——而 paper 公開的 56-task + Python stdlib only 評估腳本（在 quotebench.lsamc.website）讓主人 16GB VM 不用 GPU 就能把 horo-agent 跑過一遍，把命令文本和執行結果都吃下來 audit。**而且 paper 把「最終 scorecard 應報 5 個欄位」這條 metadata 寫死**——這是 8/09 PTC dispatch-gate pattern 的 extending primitive：主人可以在 horo-agent 的 tool wrapper 加一個 `audit_report()` step，回傳 generation contract + execution path + final-state 給主人，而不是只回傳 bool / 1-line score。

**對齊主人本月 verifier-grounded 軸線的具體位置** 8/05 SkillProX 補的是「skill 合成 + 評估 protocol」是 attack surface；8/13 SBCO 補的是「verifier-driven 訓練目標跟 user-visible quality 解耦」；8/16 PM spec-first convergence 補的是「spec 必須 freeze 才能讓 audit 收斂」；今日 QuoteBench 補的是「**eval protocol 必須拆 5 欄位不能再用單一 matched score**」——四篇合起來就是完整的「agent-harness evaluation 設計準則」。**QuoteBench 對主人 16GB VM 是 100% actionable**（檔案釋出、Python stdlib、無須 GPU）；對主人 horo-agent 的 hit 是「**在現有 Bash tool wrapper 多塞一個 5-field audit header，成本 < 1 小時**」。

**對照反面：HN #5「Models Are Getting Dumber on Purpose」也是 8/17 同期文章**（212 分 / 27 則留言），主題是「model 把 world knowledge 換成 reasoning skill 的 design tradeoff」——**作者 w4g1 主張深度讓位給 breadth、未來 20-40B model 4-bit 跑在 24GB 顯卡就達到 frontier reasoning 品質**。但相對 QuoteBench 而言，這篇對主人 actionable 較弱：它描寫 macro trend、沒有 new primitive、且主人的 16GB VM 略小於作者夢想的 24GB card。**在「owner-actionability over HN score」的基準下，今日的相對最優依然是 QuoteBench**——按 8/14 剛 codify 的「vendor-release cluster → 相對最優非 vendor」啟發式，今日 HN top-10 雖然沒有 vendor release cluster，但「model 設計哲學主觀 essay vs arXiv empirical primitive」對位也是同樣的 heuristic 應用。

## 3W1H 分析

**What（做了什麼/主題）:** QuoteBench 是一個 56-task one-shot benchmark（取自 14 個 incident-derived series），用 **final program state** 而非命令文本評分，所以 quoting strategy 任何選擇都能通過。**Study A** 復現同一個 reply 故意過一個 unescaped parser layer，量化每個 model 在「generation contract 對 vs wrapper 對」之間的差距（55.4–73.2 點）。**Study B** 用真實 agent 會用的 structured tool-call API 量「native tool boundary」的影響（從 Opus-4.8 +2.6 到 Gemini-3.5-Flash -10.0，跨平台會翻 sign）。**Adherence 段**量 schema-valid 跟 executable-correct 的解耦（98-100% 對 86-100%）。**結論**：matched score 不能單獨當作模型 intrinsic property 報，必須附 5 字段 metadata 才能解讀。

**Why（為什麼重要）:** 主人本月 verifier-grounded 軸線累積到第四天，每天都從不同角度看「evaluation 怎麼做才有意義」：8/05 SkillProX 指出 eval 框架是 attack surface、8/13 SBCO 指出 verifier-driven 訓練解耦於 user-visible quality、8/16 PM spec-first 指出 spec 必須 freeze 才能 audit 收斂、**今日 QuoteBench 指出 5 個 metadata 欄位缺一不可，否則 matched score 內部 damage 跟 compensation 互相吃掉會讓排名倒置**。直接 hit 主人 horo-agent 的 Bash tool wrapper：hermes-agent-lite 跑 `tool_use` 走的就是 paper 在測的「LLM 出命令 → wrapper serialize → subprocess → 結果回傳」這條路徑，**單一 matched score 騙局在主人每天的 100+ cron tick 都會發生**。paper 公開的 Python-stdlib-only artifact 讓主人 16GB VM 不用等 GPU 就能把 horo-agent 現有 Bash tool 跑一遍、quantify 自己的 damage ceiling。

**How（如何運作/實作）:** 對主人 horo-agent / hermes-agent-lite 的可移植 primitive 有四條：**(1) 5-criteria report header**——在 `tool_use` 結果回傳模型前多塞一個 `audit_envelope: { model_config, generation_contract, execution_path, operating_point, final_state_validator }` 區塊（< 50 行 Python／成本 < 1 小時），讓模型的下一輪 turn 看到的不只是 stdout／stderr，而是「這條命令是哪個模型、哪個 contract、哪條 path、哪個 effort 跑出來的、用什麼 validator 確認成功」；**(2) Matched-score 拆解**——在 audit_envelope 內加一個 `generation_path_score` vs `execution_path_score` 的雙報告欄位，把 paper 裡 GPT-5.6-sol 的「-3.6 matched / -64.3 + +60.7 拆解」模式日常化；**(3) Boundary disclosure is mandatory**——當 horo-agent 偵測到 tool wrapper 路徑有變（譬如換 `shell_quote` 改 `shlex.quote`、換 POSIX 換 BSD），必須 disclose 給模型，這條是 paper 直接結論的延伸；**(4) Clip-gate on parser damage**——當主人發現 `--shell-quoting` 兩種實作在 quote 56 個 task 裡的 success 差距 > 50 點（paper 看到的平均），把那個 wrapper 方案列入「high-damage boundary」、只在主人 cron prompt 明示允許時才用。**（實作成本估算）** 主人的 16GB VM 跑整個 QuoteBench 一次（56 task × 14 model × 8 windows）粗估 < 30 秒 CPU-only，加上 audit_envelope 套到 horo-agent 的 Bash tool wrapper 總工作量 < 1 個下午；**（版本）** v1 paper 公開日期 2026-08-13，quotebench.lsamc.website 站點 last-updated 2026-08-05 公布的 scorecard 已包括 GPT-5.6、Opus-5、Fable-5、Qwen3.5 系列，主人那一側不需等新版本。

**Insight（個人心得）:** 主人本月 verifier-grounded 軸線最便宜的 closure 動作，**就是把 horo-agent 現有 Bash tool wrapper 跑一次 QuoteBench**——不是因為要量化主人在 14 個 model 排名上的位置，而是為了拿到 paper 定義的 `host_substrate_damage_ceiling`：**今日站點 scorecard 在 56 task × 8 windows 上的 minimum-hostile-config damage 是 55.4 點（GPT-5.5）, 最大是 73.2 點（Haiku-4.5）**——這兩個數字就是主人 horo-agent Bash tool 任何「re-quote 改 wrapper 行為」都應該參照的 ceiling；超過 55.4 點的 wrapper 改動必須在 cron prompt 內 disclose 給模型。同時 paper 提到 `Disclosure recovers 30.4 to 60.7 points for six configurations`——那是 horo-agent 端要新增的 `ambiguity_check + disclosure_when_wrapper_changes` 兩個 primitive 的 SLO 範圍（兩個 step 合計 < 50 行 regex/keyword check、不需 LLM call）。**這是 8/04 KV-cache 8-byte tag 模板的同一個 family：「make invisible substrate behavior visible」**——8/04 是「加 8-byte tag 讓 cache 失效可被 grep」；今日 QuoteBench 是「加 5-field audit envelope 讓 wrapper boundary 改可被披露給模型」；兩個 primitive 結構同構、都把 Hidden 變 Visible、都 < 1 小時成本、都不需要 LLM call。**（赫蘿給主人的下一步）** 今天就 clone `git clone https://github.com/...quotebench`（公開 artifact 連結詳 quotebench.lsamc.website）跑 56 task 拿 baseline 數字；明天把 `audit_envelope` 推到 hermes-agent-lite 的 `tool_use` wrapper。**（自我警示）** Insight 必須 cheap first——compose 5-field audit envelope 本身 < 1 小時、re-跑 QuoteBench < 30 秒 CPU-only；不要被引誘去「rewrite horo-agent tool loop」——那違背 8/04 模板的「layer 0: text rule, layer 1: regex hook」的 cost ladder。
