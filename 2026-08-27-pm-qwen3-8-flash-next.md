# Qwen3.8-Flash-Next: A New Architecture, Towards Ultimate Cost-Efficiency
- 原始連結：https://qwen.ai/blog?id=qwen3.8-flash-next
- HuggingFace model card: https://huggingface.co/Qwen/Qwen3.8-Flash-Next
- 技術報告: https://github.com/QwenLM/Qwen3.8-Flash-Next/blob/main/tech_report.pdf
- HN 討論: https://news.ycombinator.com/item?id=49448210
- 閱讀時間：2026-08-27（午間）
- 來源：Hacker News 熱門第 6 名（讀取時 639 分／208 則留言，Asia/Taipei 11:31）；Qwen Team 官方部落格 8 月發布，HuggingFace 公開權重（Apache-style `qwen-community-1.0` license），GitHub 完整 tech_report

## 摘要

**Qwen3.8-Flash-Next 是 Qwen 團隊開出「Qwen4 架構預覽」的實驗性 preview release：125B 總參數 / 6B activated / 6B-on-token，搭配 51B n-gram embedding 跟 4B MTP，native 262K context、YaRN 可推到 1M。** 主軸是「沿 attention / residual / embedding / optimization 四個軸同時精煉架構」，把 Qwen3.7-Plus 的訓練成本壓到約 *1/9*、推論成本也對應降階，卻在 coding 與 office task 上拿到更好能力；商業版本 Qwen3.8-Flash 在 QwenCloud 上以 **$0.16 / 1M input + $0.47 / 1M output** 開賣 —— 直接把昨天 GLM-5.3-Flash「Opus 等級智力、十倍便宜」的軸線往下再推一階，Qwen 的版本是「接近 Opus 等級智力、Flash 系列原價再砍半」。這條路徑對主人直接相關 —— 主人多代理 default=Qwen（如 MEMORY「多代理偏好」段所示，qwen38-code 192.168.0.56:2234/v1 的 Qwen3.8-27B 就是今天最大可能的「下一棒 hardware」），這個 release 對 horo-agent lite 16GB Mac runtime 的 *本地 executor* 升級路徑是「有官方 hotspot」。

**Attention 軸：GDN（3/4 layers）+ QSA（full attention layers 用 Qwen Sparse Attention）。QSA 把 long-context attention 在 micro-block 等級做 sparse indexing，1M context 下 Prefill 7.6×、Decode 4.9× speedup；90% prefix-cache 場景下，1M context Prefill throughput 是 Qwen3.7-Plus 的 8.6×。** Qwen 的 hybrid attention 設計邏輯跟昨天的 GLM-5.3-Flash（linear + sparse + IndexPool）幾乎是同一條 Pareto frontier 上的兩個解 —— GLM 走 *weighted pooling 壓 indexer vectors*、Qwen 走 *micro-block level routing*。兩個設計都同意「attention economics 的世代躍進不在 KV cache 大小，而在 *把 attention 拆成本地 memory state + 全域 sparse retrieval*」。對主人的意義：(1) **多源交叉驗證 sparsity 是 attention 新 baseline** —— 不是某家實驗室的 demo，是 GLM 與 Qwen 兩個獨立團隊在八月同週拿出來的 production pattern。(2) **Qwen 用的 GDN（Gated DeltaNet）三層 out of 四層負責「持續壓縮歷史到固定大小 state」**，也就是 GDN = 記憶、QSA = 精準 retrieval；這跟昨天主人反芻的「memory-substrate 是 agent harness 的隱形 backbone」完全同源。

**Residual 軸：Gated Residual（GR）把 single residual stream 拓到 4 個平行 branches，每個 branch 都用 element-wise data-dependent read gate + per-branch scalar write gate 動態控制 info flow；同時 residual state 支援 FP8 儲存，把 memory-access overhead 又砍一截。** 這個設計的工程意義是 *「更深層的 LLM training stability」用 *細粒度 per-branch gating* 解，而不是靠 layer normalization 或 skip-connection magic number*。呼應主人 MEMORY「讓隱形 substrate 行為可量化」家族（08-19 Harness the Memory、08-24 Earendil、昨天 Recuris、今天 RENDER）的 pattern：主人 horizon 越長越關心 training stability、execution stability、memory stability，今天 Qwen 用 *4-branch gated residual* 把「stability 不是『不要壞』，而是『明確知道每條路徑要 fire 多少』」這件事寫成 architecture primitive —— 跟昨天 Recuris 的 *「Working Memory 永遠只反映當下 task state」* 是「*把不可見路徑做成可審計*」這個 design pattern 的兩個 independent example。

**Embedding 軸：51B N-gram Embedding 透過 local context（bigrams/trigrams）查表擴 model capacity；embedding table 可 offload 到 host memory、跟 model 計算以 async prefetching 重疊。** 51B n-gram embedding 不是 51B 全部要算 —— 是「用 2-/3-gram 索引到 20M entry 的 table，per token 只讀該位置的 vector」。這給主人的 insight 是 *MoE 不是唯一擴容方式，*embedding table 是另一個維度*。對 horo-agent 的 16GB Mac runtime 直接相關：主人 horizon 多長都需要「擴 LLM 知識但不想全塞 GPU」，Qwen 證明 *n-gram embedding + async host offload* 是當前最乾淨的 production path。這個設計跟「記憶是 substrate 的擴充」哲學直接對位 —— *embedding table 大 + 模型小 + async overlap* 就是 *「短 context LLM + 大 knowledge base + hidden prefetching」* 的硬體級版本。

**Optimization 軸：Muon optimizer 全程主線，搭配 *重新擬合 scaling law*、*Muon+AdamW 雙軌參數分組*、*正交化精度* 等實作選擇；直接從 target batch size 開練，省掉 batch-size warmup。** 這是 *「architecture co-design with optimization」* 路線 —— 不是先定 model 再挑 optimizer，而是從訓練開始就讓架構跟 optimizer 互相約束。對主人的 horo-agent 啟示：雖然主人不是訓練 LLM 的人，但這條 *「架構與訓練方式共同演化」* 跟主人 MEMORY「抽象精煉風格」段（done/do_again → 含 feedback supervised loop → NOOA CodeAct loop）直接同源 —— 系統到穩定後必須 *迭代地 refit 自己的 meta-config*，不能假設 initial design 是 final design。

**為什麼值得主人看**：今天這篇把主人 8 月的「attention economics」軸線、*「Nvidia 不是唯一選」* 軸線、與 *「多代理 default model 路線」* 全部交叉命中 — 而且這次 Qwen 出了一份能上 *horo-agent local executor* 跑生產的 production-grade 答案。(1) **「GDN + QSA + Gated Residual + N-gram + Muon」是 multi-axis 完整 stack**，不只是「我又多 flash 一個 model」；昨天 GLM-5.3-Flash 的 hybrid linear+sparse+IndexPool 是 *attention axis only*，今天 Qwen3.8-Flash-Next 是 *attention + residual + embedding + optimization* 四軸齊改，等於 GLM 自承的「再下一次就是 hybrid × 2」也跟上了。對主人有用的判斷：**horo-agent lite 的本地 executor 升級候選在 2026 Q4 應該是「Qwen3.8-Flash-Next + Apple Silicon sparsity-friendly inference stack」而非「更大的 dense model」**—— 主人 16GB Mac 的真實瓶頸是 *attention compute 與 memory bandwidth*，n-gram embedding + QSA + Gated Residual 每一件都對這個瓶頸有效。(2) **價格直接破盤**：Qwen3.8-Flash 報價 $0.16 input / $0.47 output per 1M tokens，比昨天 GLM-5.3-Flash（$0.045 任務成本，換算 input+output 後差距不大但定位不同）更明確「per-token 是 $0.1~0.5」這個價位帶；主人 horizon 多長都需要評估 *horo-agent 的 translation layer 是否能 route 到 Qwen3.8-Flash / GLM-5.3-Flash / Claude / OpenAI 之間任意切換*，這個版本讓那條 routing 路線的 *Qwen 端* 從「Qwen3-Plus OK」升級到「Qwen3.8-Flash 接近 Opus 智力、原價 1/40」是直接 production-grade。(3) **architecture-level message 是「擴 LLM 不再只能靠增加 parameters」**—— Qwen 用 n-gram embedding + Gated Residual 證明 *「用 embedding table + 細粒度 gating」* 也能擴容量與表現，而且這些都是 *memory-friendly*。主人 horo-agent 的 memory-editor（8260）與 long-horizon agent 戰略可以借鏡：*「容量擴充」不必靠 model size，可走 host-memory offload + fine-grained access pattern*。(4) **訓練成本的 1/9 不是 marketing language**：Qwen 明確寫「training takes only about 1/9 as much as Qwen3.7-Plus」是 Muon + 重新擬合 scaling law + 不需 batch-size warmup 三件齊下的結果，這呼應主人 MEMORY *抽象精煉風格* 段中「探索設計時自發精煉抽象層」的 path —— *system 不是『把模型做大』而是『把系統（含訓練法）一起優化』*。建議主人若要在 2026 Q4 評估 horo-agent 的 local executor 升級，這個 release 的 tech_report 是 first-class reference。最後呼應 *主人 MEMORY「視覺優先」與昨天 GLM-5.3-Flash 視覺 loop*：Qwen3.8-Flash-Next 也是 Causal LM **with Vision Encoder**，Vision2Web 拿到 88.1（這是 owner / coding 視覺重建評測），與昨天 Z.ai 的「visual self-judgment」是 *同一條軸線的兩個獨立 production example*：*vision = coder 的眼睛 = agent verify loop 的核心 sensor*。

## 3W1H 分析
- **What（做了什麼/主題）**:
  Qwen Team 釋出 Qwen3.8-Flash-Next —— 125B / 6B active MoE + 51B n-gram embedding + 4B MTP，GDN + QSA hybrid attention（3/4 layers 走 Gated DeltaNet，1/4 layers 走 Qwen Sparse Attention），Gated Residual（4-branch + read/write gate），n-gram embedding with host-memory async prefetch，Muon optimizer with refit scaling law + Muon/AdamW 雙軌。Training cost ≈ Qwen3.7-Plus 的 1/9；Coding benchmarks 在 multi-pair 上接近 Claude-Opus-4.6（Max），多模態 Vision2Web 88.1。Production 版本 Qwen3.8-Flash 跑在 QwenCloud，$0.16 input / $0.47 output per 1M tokens；HF 公開完整權重（qwen-community-1.0 license），可 vLLM / SGLang / TokenSpeed 部署。Tech report 同步公開於 QwenLM/Qwen3.8-Flash-Next GitHub repo。
- **Why（為什麼重要）**:
  1. **「Attention economics 新一輪」第二個獨立 production 驗證** —— GLM-5.3-Flash（昨天）走 IndexPool 壓 indexer、Qwen3.8-Flash-Next 走 micro-block level QSA；2 個獨立團隊同週拿出 production-grade hybrid linear+sparse attention 解，這不是某家實驗室的 demo，而是 *「long-context efficient inference」* 的 Pareto frontier 推進。
  2. **「N-gram embedding + host-memory offload」是 MoE 之外的另一條擴容路徑** —— 主人 horo-agent 16GB Mac runtime 的本地 executor 升級，過去 *只能靠「更大的 dense / MoE 模型硬塞」*，現在多了 *「模型小 + 大 embedding table + async overlap」* 這條 memory-friendly 路徑；對 16GB Apple Silicon 有直接 production-grade 支持。
  3. **「Gated Residual 用細粒度 per-branch gate 解 training stability」是主人 MEMORY「讓隱形 substrate 行為可量化」家族的新成員** —— 跟 08-19 Harness the Memory、08-24 Earendil、昨天 Recuris 的 *「把不可見行為做成可審計」* 是同一條 design pattern：主人的 long-running kanban task / multi-turn research workflow 都會受益於「每條路徑我都知道它在 fire 多少」這個 insight（映射成 harness level 就是 Recuris 的 Working Memory ↔ Experiential Memory 解耦 + 今日 GR 的 4-branch 解耦）。
  4. **「Training cost 1/9 不是行銷話術，是 Muon + 重新擬合 scaling law + 不需 batch warmup 三件齊下的工程結果」** —— 呼應主人 MEMORY 抽象精煉風格段（done/do_again → 含 feedback supervised loop → NOOA CodeAct loop）：「*system 不是把模型做大，而是把系統（含訓練法）一起迭代地 refit*」這個 insight 對 horo-agent 與 horo-webui 的內部 tuning cycle 同源。
  5. **「Qwen3.8-Flash 報價 $0.16 input / $0.47 output per 1M tokens」是 per-token pricing 直接破盤** —— 比 Claude-Opus-4.6（Max）便宜 *兩個數量級*，主人 horo-agent 與 horo-webui 若做 token-aware model router，這個 release 必須直接納入候選 default 路徑，不能再只靠「OpenAI / Anthropic / Z.ai」三選一。Qwen3.8-Flash production 的 vendor model 是「*Codex 支援、QwenCloud 公開 endpoint、qwen-community-1.0 license、IFEN / TokenSpeed / vLLM / SGLang 多 framework 兼容*」 —— production 路徑已經完整，不只是 research preview。
- **How（如何運作/實作）**:
  - **GDN + QSA Hybrid Attention**：3/4 layers 走 Gated DeltaNet（持續壓縮歷史到固定 state size），1/4 layers 走 QSA（Qwen Sparse Attention，micro-block level routing）。QSA 的 attention kernel 在 1M context 下拿到 7.6× Prefill / 4.9× Decode speedup。
  - **Gated Residual**：single residual stream 拓成 4 個 parallel branches，每個 branch 用 element-wise data-dependent read gate + per-branch scalar write gate 控制 info flow。FP8 storage 把 memory-access overhead 砍一截。Gate 自動 suppress activation outliers，這是 *training stability 不用 layer normalization 的 design trick*。
  - **N-gram Embedding**：51B 參數，平均每 token 6B activated 之外另用 bigram/trigram 索引 20M entry embedding table；embedding table 可 offload 到 host memory（CPU RAM），async prefetching 跟 GPU compute 重疊 —— 等於「*GPU 是 compute 加速器，CPU 是 knowledge base*」。
  - **Muon Optimizer**：主線用 Muon，搭配 (a) orthogonalization accuracy 升級、(b) Muon/AdamW 雙軌 parameter assignment、(c) fused-matrix splitting。Refit scaling law 讓 model 從 *target batch size 開始訓練*，省 batch-size warmup，因此 *optimizer step count 變少 → total training cost 1/9 of Qwen3.7-Plus*。
  - **MoE routing**：large expert pool + small number of routed experts per token + 1 shared expert；這是「*sparse 進 expert 層*」跟 *「sparse 進 attention 層 (QSA)」* 雙線 sparse 路線。
  - **Long-context**：native 262K token，YaRN factor 4.0 可推 1M；YaRN 實作細節公佈於 vLLM PR #34330 與 SGLang PR #18467。
  - **Integration**：已經整合進 QwenWork 的「Standard」模式（Qwen 即將推出的 coding agent 產品）；Codex 透過 `"slug": "qwen3.8-flash"` 與 `default_reasoning_level: xhigh` 公開列出，跨 vendor integration 已落地。
- **Insight（赫蘿心得）**:
  今天這篇把主人 8 月的「attention economics」軸線從 *單一實驗室* 升級成 *multi-lab production pattern*。昨天 GLM-5.3-Flash（IndexPool）+ 今天 Qwen3.8-Flash-Next（QSA + GR + n-gram + Muon）= 兩條完全不同的 *hybrid sparse attention* 解，同週同月同 production-grade 落地。三件值得主人內化的觀察：(1) **「擴 LLM 不再只能靠增加 parameters」** —— Qwen 用 n-gram embedding + Gated Residual 證明 *「用 embedding table + 細粒度 gating」* 也能擴容量與表現，而這些都是 *memory-friendly*。具體對主人 horo-agent lite 16GB Mac runtime 的意義：*本地 executor 升級路徑從「更大的 dense model」轉向「小模型 + 大 embedding table + async prefetch」的 memory-bound 解*，是 2026 Q4 的 first-class option。主人若要驗證，最小可行動作是 *把 Qwen3.8-Flash-Next 的 INT4-quantized 版本跑在 Apple Silicon 上*，確認 n-gram table host offload 在 16GB 內是否能穩定運作 —— 這比空談「換更大的 GPU」更有 ground-truth 證據。(2) **「architecture co-design with optimization」路徑** —— Qwen 明確證明 *「不是先定 model 再挑 optimizer，而是從訓練開始就讓架構跟 optimizer 互相約束」* 是 production 的必要姿勢。主人 MEMORY 抽象精煉風格段中寫到 *「探索設計時會自發精煉抽象層」*，這跟今天 Qwen 的 *「重新擬合 scaling law + 改 optimizer 路線 + 改 attention 設計 + 改 residual 設計」* 是 *同一個 design philosophy 的兩個 domain*（architecture 與 harness）的獨立 example。建議主人把這個 insight 套到 horo-agent 自己的 tuning 流程：*harness 不該只有一個「model + provider」選項，應該持續 refit 自己的「model + tooling + retrieval + dispatch」聯合 config*。(3) **「Qwen3.8-Flash production routing 在 $0.16/$0.47 per 1M tokens 是真正的 per-token 破盤」** —— 主人 horo-agent 與 horo-webui 的 translation layer 若還停在「OpenAI / Anthropic / 兩三個本地 model」的固定清單，今天這個 release 是 *必須納入* 的新成員。建議主人把 *Qwen3.8-Flash + GLM-5.3-Flash + Claude-Opus-4.6 + GPT-5.6 Terra + 自家 Qwen3.8-27B oMLX* 組成一個 5-provider × 2-substrate × 4-tooling 的二維 routing 矩陣，並用 *live token cost + quality delta + local latency* 三個 metric 動態評估。這比把「*哪個 model 最強*」當單一維度的判斷更接地氣。最後呼應主人 MEMORY *「視覺優先」* 與昨天 GLM-5.3-Flash *「visual self-judgment」*：Qwen3.8-Flash-Next 也是 Causal LM **with Vision Encoder**，Vision2Web 88.1。兩篇（昨天 + 今天）都在講 *「vision = coder 的眼睛 = agent verify loop 的核心 sensor」*，*multi-source cross-validation* 已經到了 *「不能再忽視」* 的程度 —— 主人 chrome-game-env 工作中「*改 code → Playwright + vision 截圖驗收*」的視覺優先哲學現在有兩個 production evidence back-up。**結論：今天 PM 給主人一個 4-axis architectural preview（attention + residual + embedding + optimization）+ 一個 production-grade price break（$0.16/$0.47 per 1M tokens）+ 一個 16GB-Mac memory-friendly 路徑（n-gram + host offload），三件都直接命中主人 horo-agent 與 horo-webui 的 strategic 路線**。最 actionable 的下一步：「*在 Apple Silicon 16GB 上跑 Qwen3.8-Flash-Next INT4 + Qwen3.8-Flash QwenCloud endpoint 的 dual-track evaluation*」，看本地 vs 雲端的 latency / cost / quality trade-off，這比空談 *「Qwen4 何時來」* 更有 ground-truth evidence。
