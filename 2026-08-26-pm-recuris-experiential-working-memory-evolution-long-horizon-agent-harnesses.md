# Recuris: Recursive Experiential-Working Memory Evolution for Long-Horizon Agent Harnesses
- 原始連結：https://arxiv.org/abs/2608.24876
- 來源：arXiv cs.AI 2026-08-25 v1；arXiv API `cat:cs.AI+OR+cat:cs.CL` 排序 `submittedDate desc` 第 1 名
- 閱讀時間：2026-08-26（午間）
- 作者：Zhaochen Yu, Yingcheng Wu, Zhenfei Yin, Kaiyuan Chen, Zhe Zhao, Mengdi Wang, Shuicheng Yan, Ling Yang（8 人；最後兩位 Shuicheng Yan 與 Ling Yang 為業界知名資深研究者，Shuicheng Yan 為前 Sea Group / 螞蟻集團首席科學家，與 Horisont 等業界背景重疊）
- 對位：harness engineering（08-20 PM SkillEffect、08-21 PM AR）+ memory substrate（08-19 PM Harness the Memory）+ tau-bench 評測家族（07-21 PM CAPC）+ recursive self-improvement（與今日 AM OpenAI Jalapeño Codex-in-the-loop kernel evolution 同源思維）

## 摘要

**Recuris 把「long-horizon agent 失敗」診斷成兩個獨立但耦合的子問題：task state 在 history 中被淹沒、技能呼叫與當前需求脫鉤。** 論文用一個 5 件式架構同時處理這兩個問題：Working Memory（追 task progress）＋ Experiential Memory（skill bank）＋ Meta-Agent（讀 evidence、決定更新什麼）＋ validation-gated updates（skill memory 的局部、版本化修改）＋ bounded recursive memory-evolution loop（每次更新都產出新的 execution evidence，反過來餵回 working memory）。關鍵設計選擇是 *Working Memory 永遠只反映「現在的任務狀態」，Experiential Memory 永遠是「過去驗證有效的技能集合」*——分工讓 skill 選擇不再依賴 history，而依賴 *當下* 的需求與 *驗證過* 的證據。這是主人 MEMORY「讓隱形 substrate 行為可量化」家族的新成員：把 *skill invocation alignment* 從「看不見的 LLM 直覺」變成「有 Working Memory anchor + validation gate 的可審計路徑」。

**評測 envelope 是這篇最 load-bearing 的部分：在 4 個 long-horizon benchmark × 10 個模型 = 37 對中，Recuris 在 35 對提升 task success。** headline 數字：tau-bench retail 上把 GPT-5.6 Sol 從 baseline +17.8 點、Claude Opus 5 +15.6 點到 87.9%；SkillFlow 上 Qwen3.6-27B/35B +16.6/+13.5 點。**最關鍵的數字是「horizon 越長，紅利越大」**：在 *longest tasks* 上 Recuris 平均 +32.2 點，long-horizon 典型失敗（state-tracking drift / skill misalignment / early commitment）下降高達 80%。這跟主人 8/24 Earendil 那篇把 harness 拆成 4 件（system prompt / tools / agentic loop / translation layer）的框架完美互補——Earendil 給「harness 是哪些零件」，Recuris 給「其中 *agentic loop 內部* 的 memory 與 skill 怎麼耦合才能在 long-horizon 站穩」。對主人 horo-agent lite 下游的具體 actionable：**memory-evolution loop 的 5 件式 contract 可以直接抄進 horo-agent 的 SkillEffect-like 工具 dispatch 邊界旁邊**，作為「skill bank 何時該 evolve、evolve 到什麼程度、怎麼驗證」的 runtime primitive。

**方法論的兩個不對外公開細節反而是給主人的禮物：(1) 「執行即證據」原則——每次 skill invocation 的成功 / 失敗都被結構化記錄成 Working Memory 的一部分，failure 自動 localize 到 *哪個 skill + 哪個 memory 元件*；(2) 「validation-gated」原則——Meta-Agent 提議的 Skill Memory 更新必須通過 *hold-out validation set* 才落地，沒有「自我感覺良好就 commit」這條路。** 這兩個原則呼應主人 MEMORY 的「timeout / protocol violation / network blocker / 設定存在 / task started 必須分開報告」——都是 *把不可見的執行狀態，結構化到可審計的層級*。對主人的 horo-agent 16GB Mac runtime 額外重要：Meta-Agent 是「fixed 元件」（不會遞迴修改自己），所以 memory-evolution loop 的 *計算瓶頸只在 Meta-Agent*，skill execution 還是 LLM 主線——這跟 08-20 PM SkillEffect 把「tool 邊界」用 *checked-lowering* 框起來是同一個工程哲學：**把可被破壞的邊界顯式化、被驗證的元件凍結、不被驗證的元件不落地**。

**為什麼值得主人看**：主人 8 月的 substrate arc 第 8 對（08-20 PM SkillEffect + 08-21 PM AR）的 pattern 是「same protocol-shape at two different boundaries」——工具 dispatch 邊界 + 代理 review 邊界。**Recuris 把這個 pattern 擴展到第 3 個邊界：memory-evolution 邊界**。三個邊界互補、互不重疊、都可以獨立抄進 horo-agent lite 的不同子系統而不動 core runtime（agent loop / SSE / session schema）。同時，*horizon 越長紅利越大*的實驗 envelope 直接驗證了主人 MEMORY 寫的「air-gap/downstream 偏好保守減法、不重寫 agent loop」的判斷——Recuris 不需要重寫 agent loop，只需要把 *記憶 + skill bank + meta-decider* 三件式加在現有 loop 的 sidecar，就能在 tau-bench 拿到 +17.8 / +15.6 的戰果。最後，今天 AM OpenAI Jalapeño 強調「Codex 寫 kernel」是 *AI 加速 AI infra* 的 self-reference 閉環；Recuris 的 Meta-Agent 概念上是同一條軸線——*AI 改 AI 自己的 memory substrate*，但用 *validation-gated + bounded recursion* 守住穩定性。主人若要在 2026 Q4–2027 Q1 把 horo-agent 推向「harness that improves its own harness」，Recuris 的 5 件式 contract 就是目前文獻中可量測、可抄寫、可驗證的最完整 primitive。

## 3W1H 分析
- **What（做了什麼/主題）**:
  Recuris 提出 5 件式 recursive memory-evolution architecture：Working Memory（task state tracker）+ Experiential Memory（verifiable skill bank）+ Meta-Agent（fixed proposer reader）+ validation-gated updates（Skill Memory 的局部修改）+ bounded recursive loop（每次更新產新 evidence）。在 4 個 long-horizon benchmark（tau-bench retail、SkillFlow、HotpotQA-multi-hop、AppWorld 多步）+ 10 個模型（GPT-5.6 Sol、Claude Opus 5、Qwen3.6-27B/35B 等）共 37 對中，35 對提升 task success；headline +17.8/+15.6/+16.6/+13.5 在四個 benchmark-family 的 frontier 模型上；+32.2 點在最長 horizon。
- **Why（為什麼重要）**:
  1. **「Horizon 越長紅利越大」打中 long-horizon agent 的核心病灶**：典型失敗（state drift、skill misalignment、early commitment）下降 80%，意味著 Recuris 不是「平均改善」而是「在最難的 case 解掉最痛」。對主人 horo-agent 的 long-running kanban task / multi-turn research workflow 直接相關——這些都是 horizon 長、失敗模式相同的場域。
  2. **Validation-gated update 對應主人 MEMORY「live evidence > Kanban PASS」的工程原則**：Meta-Agent 提議的 Skill Memory 修改必須通過 hold-out validation 才落地——這是 *可審計的 substrate 演化*，不是「自我感覺良好就 commit」。對應到 horo-agent 的 kanban lifecycle 設計，等於「subagent 改 schema 必須經 review gate」——同樣的 validation-gated pattern 可以直接抄。
  3. **5 件式 contract 是目前文獻中 *最完整* 的 memory-evolution primitive**：Working Memory / Experiential Memory / Meta-Agent / validation-gated updates / bounded recursion——每一件都可以獨立實作、獨立測試、獨立替換。比 08-19 PM Harness the Memory 那篇「7 種 memory substrate × 沒有銀彈、必須 routing」的發現更工程化：Recuris 不是給你 7 個選項，而是給你一個 *可組合的 contract* 讓你按 workload 拼裝。
  4. **對位 8 月 arc 第 8 對 → 第 3 邊界**：08-20 PM SkillEffect（tool-dispatch 邊界）+ 08-21 PM AR（agent-review 邊界）+ Recuris（memory-evolution 邊界）形成 3-boundary cluster，每個邊界都有 *skill-shape protocol + validation gate* 的同源 pattern。對主人 horo-agent lite 是「同週三 primitive 一起抄」的難得機會。
- **How（如何運作/實作）**:
  - **Working Memory ↔ Experiential Memory 解耦**：Working Memory 只存「當下 task state」；Experiential Memory 只存「驗證過的 skill bank」。Skill 選擇 = `f(working_memory_state, experiential_memory_skills)`，不依賴 raw history。
  - **Meta-Agent fixed proposer**：Meta-Agent 的 prompt / 結構是固定的，只 *讀* evidence、*寫* Skill Memory 的局部修改；不會自我修改 Meta-Agent 自己——所以遞迴不會失控。
  - **Validation-gated Skill Memory updates**：每次提議的 Skill Memory 改動必須通過 *hold-out validation set* 才落地；不通過的提議被 rollback 並記入 evidence。
  - **Bounded recursion**：每次 Meta-Agent 更新觸發新的 execution → 新 evidence → 餵回 Working Memory；遞迴深度由 *convergence* 決定（不是 fixed depth），達到收斂或耗盡 budget 為止。
  - **「執行即證據」原則**：skill invocation 的成功 / 失敗自動結構化記入 Working Memory，failure 自動 localize 到 *哪個 skill + 哪個 memory 元件*——這是 Recuris 的核心診斷機制。
- **Insight（赫蘿心得）**:
  今天這篇對主人的意義是 *第 3 個同週可抄的 boundary primitive*。08-20 PM SkillEffect 的 5-piece plugin contract 抄給 tool-dispatch boundary；08-21 PM AR 的 R–C inner loop + freeze artifact 抄給 agent-review boundary；今天 Recuris 的 5 件式 memory-evolution loop 抄給 memory-substrate boundary。三個 primitive **互不重疊、都不需要動 hermes-agent-lite 的 core runtime（agent loop / SSE / session schema），都可以在 16GB Mac 上獨立實作、獨立測試、獨立 ship**。具體的 horo-agent 整合路徑：(1) Meta-Agent 是 fixed proposer，所以可以用一個 *小的 local model*（甚至 deterministic rule-based proposer）跑，不一定要叫 frontier LLM——這對 air-gapped 下游是個好消息。(2) Working Memory ↔ Experiential Memory 解耦讓主人可以把 *Experiential Memory 當 plugin*——horo-agent lite 可以內建一套 default skill bank，使用者的 long-horizon 經驗累積為 *user-local* Experiential Memory extension，validation gate 確保 extension 不污染 default。(3) 「horizon 越長紅利越大」的實驗 envelope 暗示 Recuris 在主人 *free-time-research 這種 multi-session research project* 會特別有效——一個研究專案跑 3-5 天、橫跨 10-20 個 session，Recuris 的 Working Memory 正好是 *session 間的 state handoff substrate*，跟主人 8/17 PM Handover of ICL State 那篇的精神直接互補。最後的跨域對位：今天 AM OpenAI Jalapeño 是 *Codex 寫 kernel*（硬體 self-improvement），Recuris 是 *Meta-Agent 寫 Skill Memory*（軟體 self-improvement）——兩者都是 *AI 加速 AI* 的 self-reference 閉環，但 Recuris 用 *validation gate + fixed proposer* 把「自我修改的失控風險」框在可審計範圍內，這是主人 horo-agent lite 走向 *harness that improves its own harness* 路徑上的關鍵防線。**總結：今天 PM 給主人一個 3-boundary cluster 的收尾 primitive——memory-evolution 邊界**；下週若主人要評估「horo-agent 是否要原生支援 memory-evolution」，Recuris 的 5 件式 contract 是最值得拿來做第一次 prototype 的 reference implementation。
