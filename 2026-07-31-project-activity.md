# GitHub 專案動態
- 檢查時間：2026-07-31
- 檢查對象：huggingface/speech-to-speech、microsoft/AI-For-Beginners、paperswithbacktest/awesome-systematic-trading、gavamedia/deltafin、xikhar/persona
- 來源：Trending Daily Top 3 + 自由探索 Web Search（GitHub Search API by created:2026-07-28..2026-07-31, stars>200, sort=stars desc）

## Repo 摘要與 3W1H

### huggingface/speech-to-speech
- **Repo 摘要：** Hugging Face 官方推出的「模組化、低延遲、本地優先」語音代理 pipeline：VAD → STT → LLM → TTS 四段皆可熱抽換，透過 OpenAI Realtime 相容的 WebSocket API（`/v1/realtime`）對外服務。它內建 Parakeet TDT、Qwen3-TTS、Silero VAD v5，LLM 槽位可指向 OpenAI / HF Inference Providers / vLLM / llama.cpp，並在 Reachy Mini 機器人上以 production 等級實際運行；本機預設路徑就是 `pip install speech-to-speech` + `speech-to-speech` 一行啟動。Repo 為 Python、Apache-2.0，9,220 stars（已超越 9k），最後更新 2026-07-31；GitHub release 最新 `v0.2.10`（2026-06-11），但 PyPI 已默默上 `0.2.11`（2026-07-17）。
- **3W1H：**
  - **What：** 模組化、即時、本地優先的 speech-to-speech 伺服器（OpenAI Realtime 相容 WebSocket）。
  - **Why：** 多數 voice agent stack 要嘛鎖在雲端、要嘛把 VAD/STT/LLM/TTS 焊死；speech-to-speech 把每段都做成可換 backend 的 thread + queue，LLM 走 OpenAI 相容協議，可直接接 vLLM／llama.cpp 自架的本地模型，省去自寫 protocol 介接。
  - **Who：** 想在 Reachy Mini／自製硬體上跑 on-device 語音代理的 maker、要把自架 LLM 變成可通話應用的 builder、需要 Realtime API 標準介面但不想被單一雲端供應商綁死的產品工程師。
  - **How：** 預設路徑是 `pip install speech-to-speech` + `export OPENAI_API_KEY=...` + `speech-to-speech`（啟 `ws://localhost:8765/v1/realtime`）；要全本地 LLM 就先 `llama-server -hf ggml-org/gemma-4-E4B-it-GGUF -np 2 -c 65536 -fa on --swa-full`，再以 `--model_name ... --responses_api_base_url http://127.0.0.1:8080/v1` 指向它；客戶端可走 `python scripts/listen_and_play_realtime.py` 或任何 OpenAI Realtime 相容 client。
- **安裝方式：** **pip/uv/poetry：** `pip install speech-to-speech`（Python ≥ 3.10，PyPI 0.2.11 / 2026-07-17；GitHub release 最新 v0.2.10 / 2026-06-11）。`uv sync` 對 `pyproject.toml` 也通。**可選 extras（同一套 package）：** `pip install "speech-to-speech[kokoro]"`、`[pocket]`、`[chattts]`、`[facebook-mms]`、`[faster-whisper]`、`[whisper-mlx]`、`[paraformer]`、`[mlx-lm]`，依需求追加 STT/TTS backend。**Qwen3-TTS CUDA：** Linux 上若 CUDA runtime 與 PyPI 的 `qwentts-cpp-python` 預設 wheel 不符，先從 HF wheelhouse 指定 wheel（cu130／cu124／cpu）再裝 `speech-to-speech`。**From source：** `git clone` + `cd speech-to-speech` + `uv sync`。**CLI：** 裝完即可執行 `speech-to-speech`。README 未列 npm/pnpm/yarn/npx。
- **Repo metadata：** Description `Build local voice agents with open-source models`；Stars 9,220；Forks 1,141；Language Python；Topics `ai`, `assistant`, `language-model`, `machine-learning`, `python`, `speech`, `speech-synthesis`, `speech-to-text`, `speech-translation`；License Apache-2.0；Created 2024-08-07；Updated 2026-07-31；Pushed 2026-07-30；Size 3.1 MB。
- **近期 release：** GitHub Release：**2026-06-11**；`v0.2.10`；**v0.2.10**。補充：PyPI 上另有 `0.2.11`（2026-07-17）與 `v0.2.11` git tag（2026-07-17，tagger date 2026-07-17T12:04:50Z，message `Release v0.2.11`），但尚未在 GitHub Releases 開對應 release entry；實際可安裝版本以 PyPI 為準。

### microsoft/AI-For-Beginners
- **Repo 摘要：** 微軟 Azure Cloud Advocates 推出的 12 週、24 堂 AI 入門 curriculum：同時覆蓋 symbolic AI／GOFAI、神經網路／deep learning（TensorFlow + PyTorch 並陳）、CNN／NLP／GAN／RNN 等經典架構，以及遺傳演算法、多代理系統、負責任 AI 與 ethics。README 內建 50+ 種語言的翻譯導覽（含繁體中文 zh-TW），並提供 sparse-checkout 模式避免下載過大；課堂內容以 Jupyter Notebook 為主，但課程本身無 GitHub release 通道、僅以 commit 推進。Repo 為 Jupyter Notebook、MIT，54,397 stars（已逼近 5.5 萬），最後 push 2026-07-21，是今天清單裡最年長、維護最穩的「教育型 trending 熱點」。
- **3W1H：**
  - **What：** 12 週、24 堂、50+ 語言版本的 AI 入門 curriculum（Jupyter + 課文 + 配圖 sketchnote）。
  - **Why：** 對初學者而言，AI 領域教科書／論文一入門就掉進數學；微軟把 symbolic、neural、evolutionary、multi-agent、ethics 等多種典範編成一條「先觀念、後 code」的學習路徑，並用兩大主流框架（TF、PyTorch）並列呈現，避免框架綁架。
  - **Who：** 完全沒碰過 AI 的開發者、想從工程師轉進 ML 的 in-house 培訓團隊、需要 12 週結構化教材的高中／大學教師，以及任何想快速複盤 symbolic / evolutionary / multi-agent 等「教科書有、教學書少見」典範的工程師。
  - **How：** 直接打開 `lessons/` 內對應週的 notebook（課程附 quizzes、labs、sketchnote）；不想 clone 全部翻譯就走 sparse-checkout `git clone --filter=blob:none --sparse … && git sparse-checkout set --no-cone '/*' '!translations' '!translated_images'`；想動手就跑 `binder` badge 起的 JupyterHub。
- **安裝方式：** **pip/uv/poetry：** 未找到明確 pip 安裝方式（這是 curriculum，不是 Python library）。**Jupyter：** 從 `lessons/<week>/<lesson>.ipynb` 直接打開，binder badge 一鍵啟動線上環境。**從源碼（精簡 clone，不含翻譯）：** `git clone --filter=blob:none --sparse https://github.com/microsoft/AI-For-Beginners.git && cd AI-For-Beginners && git sparse-checkout set --no-cone '/*' '!translations' '!translated_images'`。**完整 clone：** `git clone https://github.com/microsoft/AI-For-Beginners.git`。**社群：** Microsoft Foundry Discord 與 Gitter。README 未列 npm。
- **Repo metadata：** Description `12 Weeks, 24 Lessons, AI for All!`；Stars 54,397；Forks 11,012；Language Jupyter Notebook；Topics `ai`, `artificial-intelligence`, `cnn`, `computer-vision`, `deep-learning`, `gan`, `machine-learning`, `microsoft-for-beginners`, `nlp`, `rnn`；License MIT；Created 2021-03-03；Updated 2026-07-31；Pushed 2026-07-21；Size 1.39 GB（含 50 種語言翻譯）。
- **近期 release：** **未找到 GitHub release**（無 `/releases/latest` 也無 git tag；推進靠 commit 與多語 PR，最後 push 2026-07-21）。

### paperswithbacktest/awesome-systematic-trading
- **Repo 摘要：** `paperswithbacktest.com` 維護的「系統化／量化交易 awesome list」：收錄 97 個 libraries／packages（backtest／live trading／broker APIs／data sources／ML／時間序列分析／visualization）、40+ 個 institutional 與學術策略、55 本書、23 個 videos／訪談、blog 與 courses，並有繁體中文版 README。Repo 為 Python、但無 LICENSE 檔（License 欄位為 `None`），11,248 stars，最後 push 是 2025-01-22，距今已 18 個月——今天上 trending 是「程式碼檔被大量 star／fork」造成的熱度衝上，**不是近期活躍開發**。
- **3W1H：**
  - **What：** 系統化／量化交易資源的 curated awesome list（libraries + 策略 + 書單 + 影片 + blog）。
  - **Why：** 量化入門者最大的痛點是「資源散在論壇、券商文件、arXiv、Substack 沒有入口」；這個 list 把 research、backtest、live-trading 套件與策略一次收齊，且很多條目附有 paperswithbacktest.com 的 implementation 連結。
  - **Who：** 想從零開始接觸量化交易、想找 backtesting framework（vector-based / event-driven / crypto）比較表的工程師、研究 alpha／mean-reversion／HFT 策略論文落地的 quant 學生。
  - **How：** 直接看 README；想看程式實作就從 `paperswithbacktest.com` 拿對應的 Python 範例；想自己 backtest 從「Libraries and packages」分類挑 framework（Backtesting.py、Zipline、vectorbt、Lean、CCXT 等）後照官方文件跑。
- **安裝方式：** **pip/uv/poetry：** 未找到明確 pip 安裝方式（這是 awesome list，不是 Python library）。**Clone：** `git clone https://github.com/paperswithbacktest/awesome-systematic-trading.git` 後讀 README。**配套網站：** `https://paperswithbacktest.com` 提供 implementation 程式與 exclusive content。**其他：** 條目內連結到的 library（Backtesting、vectorbt、CCXT、Lean 等）才需要 `pip install`——那是各自上游的事。README 未列 npm。
- **Repo metadata：** Description `A curated list of awesome libraries, packages, strategies, books, blogs, tutorials for systematic trading.`；Stars 11,248；Forks 1,431；Language Python（README 主檔）；Topics 20 個，含 `algorithmic-trading`, `algotrading`, `alpha`, `arbitrage-bot`, `awesome`, `awesome-list`, `book`, `finance`, `futures`, `quant`, `quantitative-finance`, `quantitative-trading`, `trading-algorithms`, `trading-bot`, `trading-strategies`；License **None（repo 內未放 LICENSE 檔）**；Created 2022-02-05；Updated 2026-07-31（外部 activity，例如 star／fork 觸發）；Pushed **2025-01-22**；Homepage `https://paperswithbacktest.com`。
- **近期 release：** **未找到 GitHub release**（無 release 也無 git tag；上次 commit 2025-01-22，18 個月未動；今天進 trending 是「質量＋中文版曝光」造成的 star 衝上）。

### gavamedia/deltafin
- **Repo 摘要：** Deltafin 是 GAVAMEDIA 在 Apple Silicon（macOS arm64）與 Linux（x86-64 / aarch64）上把 Moonshot 的 2.8T 參數 MoE frontier 模型 Kimi K3「跑在一台機器上」的研究專案：自動挑 MPS／CUDA／CPU、針對 Metal／CUDA／CPU 各做一份 MXFP4 expert kernel，並以 1.19 GB admission + 3.44 GB wide-proposal 的「target-verified draft」做投機解碼但**永遠以 K3 全量為唯一權威**。Maintainer 公布的 64 GB M1 Max reference 數字是 0.2660 token/s（3.76 s/token 精確 oracle 中位數），且提供 `--full`（~1.7 TB、3.76 s/token）與 `--stream`（~215 GB、~3 min/token 但免下載）兩種部署模式。Repo 為 Python、MIT（API 回 `NOASSERTION` 但 `LICENSE` 內容是 MIT），494 stars，3 天前（2026-07-28）建立，最後 push 2026-07-30，仍在快速迭代。
- **3W1H：**
  - **What：** 給 Kimi K3 (2.8T MoE) 寫的 on-device inference runtime（MPS／CUDA／CPU + MXFP4 native + target-verified speculative decoding + OpenAI 相容 API server）。
  - **Why：** 主流 frontier MoE 模型對單機是「太大放不下」或「放得下但慢到不可用」；Deltafin 透過「模型在 SSD、每次只取 16 routed experts × 92 layers = 25.8 GB 進 RAM」+ 嚴格 target-verified 投機解碼，把 reference decode 壓到 3.76 s/token（exact-oracle 中位數），且永遠不靠 draft 決定品質。
  - **Who：** 想在 M-series Mac / Linux 工作站 self-host Kimi K3 做本地 chat 與 coding agent 的研究者、想用 frontier model 但不想依賴雲端 API 的 builder、對 MoE offloading 與 MXFP4 kernel 有興趣的 ML systems 工程師。
  - **How：** clone + venv + `pip install torch` + `pip install -r requirements.txt` + `tools/build_native.py` + `tools/setup_k3.py --full`（下載 ~1.7 TB K3 + ~4.63 GB draft pair） + `tools/convert_spine_int8.py`；想先嘗鮮就 `setup_k3.py --stream`（~215 GB、~30 min）；升級靠 `tools/upgrade.py`；OpenAI 相容 API server 與指令細節見 README。
- **安裝方式：** **pip/uv/poetry：** 未找到 PyPI 上的 `deltafin` 套件（README 走 clone + venv + `pip install -r requirements.txt`）。**從源碼安裝（必要路徑）：** `git clone https://github.com/gavamedia/deltafin.git && cd deltafin && python3 -m venv venv && ./venv/bin/python -m pip install torch && ./venv/bin/python -m pip install -r requirements.txt && ./venv/bin/python tools/build_native.py && ./venv/bin/python tools/setup_k3.py --full && ./venv/bin/python tools/convert_spine_int8.py`。**macOS 前置：** `xcode-select --install`；**Linux 前置：** `build-essential` / GCC／Clang；**NVIDIA 前置：** 用 PyTorch 官方 selector 裝 CUDA-enabled PyTorch；`K3_CUDA_ARCH=sm_89` 為手動覆寫。**升級：** 既存安裝 `./venv/bin/python tools/upgrade.py`（只接受 fast-forward、不碰模型）。**npm：** 未列。
- **Repo metadata：** Description `Run full Kimi K3 on a single device. And an OpenAI-compatible API server for local chat and coding agents.`；Stars 494；Forks 52；Language Python；Topics `kimi`, `kimi-k3`, `local-ai`, `local-llm`；License `NOASSERTION`（實際 `LICENSE` 內容為 MIT，Copyright 2026 GAVAMEDIA Corporation）；Created 2026-07-28；Updated 2026-07-31；Pushed 2026-07-30；Size 755 KB（不含 model weights）。
- **近期 release：** **未找到 GitHub release**（API `/releases/latest` 回 404、亦無 git tag；版本節奏走 `tools/upgrade.py` + commit 推進，README 內以「this release」描述當下 HEAD）。

### xikhar/persona
- **Repo 摘要：** Persona 是一款「跨平台桌面角色存在層」：監聽指定 app 的語音輸出（macOS Core Audio process tap、Windows WASAPI process-loopback、Linux PipeWire process-stream），在桌面上用 VRM／VRMA 化身做出與工作並存的即時語音夥伴視覺。整個 listener 嚴格 scope 到「只聽」應用程式 playback 程序——不抓麥克風、不存音檔、不產生語音、不轉譯內容、不上傳音訊；並透過本地 MCP server（預設 `http://127.0.0.1:47831/mcp`）讓 Codex 等 agent 用 tool 觸發 idle／speaking／自訂動作。Repo 為 JavaScript（Electron）、MIT（`LICENSE` 為 MIT，API 端 spdx `NOASSERTION`），682 stars，2026-07-28 建立、今天仍持續 push。
- **3W1H：**
  - **What：** Electron-based 跨平台桌面 app：VRM/VRMA 化身 + 自動語音輸出監聽 + 本地 MCP server（給 Codex／agent 用）。
  - **Why：** Neuro-sama、AIRI 等 AI companion 多著重「會說話」這一面；Persona 反過來把焦點放在「你正在用的桌面 app 在說話」這件事，把聲音即時反映到一個站在旁邊的 VRM 角色上，給出「有人陪你工作」的視覺密度。
  - **Who：** 想把 voice AI 體驗從純音效提升到視覺陪伴感的個人開發者、Codex／Claude Code 等 agent 的「需要 animation 觸發」整合者、做桌面 AI companion UI 研究的設計師。
  - **How：** 安裝 Node.js 24+ 後 `git clone` + `npm install` + `cp public/assets/library.json.example public/assets/library.json` + `cp public/assets/manifest.json.example public/assets/manifest.json` + `npm run demo`；第一次啟動會進 Settings 讓你 import 一個 `.vrm`；連 Codex 用 `codex mcp add persona --url http://127.0.0.1:47831/mcp`。
- **安裝方式：** **pip/uv/poetry：** 未列。**npm/pnpm/yarn/npx：** `npm install`（從 clone 後的 repo）→ `npm run demo`（建置 + 啟動 Electron 桌面 app）或 `npm start -- --background`（背景啟動）。**原生打包（multi-OS）：** `npm run dist:linux`（AppImage + DEB）、`npm run dist:windows`（NSIS）、`npm run dist:mac`（DMG + ZIP，arm64 + x64），輸出到 `release/`；Windows 需 Visual Studio Build Tools C++ workload，macOS 需 Xcode CLT 與 macOS 14.2+ SDK。**前置：** Node.js 24 或更新、`npm`、有硬體加速 GPU 的 desktop session；Linux 還需 `pw-dump` / `pw-record` 在 `PATH`，Windows 10 需 build 20348+。**npm registry 上的 `persona` 是另一個 Persona Identities 公司 SDK（v5.8.0），與本 repo 無關。**
- **Repo metadata：** Description `Bringing real-time voice to life.`；Stars 682；Forks 56；Language JavaScript（Electron）；Topics 空（未填）；License `NOASSERTION`（實際 `LICENSE` 為 MIT，Copyright 2026 xikhar；應用程式碼 MIT，但「bundled character assets」不在 MIT 內，須自行替換並完成 license 欄位才能發佈）；Created 2026-07-28；Updated 2026-07-31；Pushed 2026-07-31；Size 1.3 MB（不含 character media，media 走 `.gitignore`）。
- **近期 release：** **未找到 GitHub release**（無 release、無 git tag；release 走 prerelease tag 形如 `v0.1.0-beta.0` 觸發 native build + checksum，文件見 `docs/RELEASING.md`）。

## 重點觀察

- **release 通道的「四種現實」光譜**：今天 5 個 repo 的 release 文化剛好把開源世界的四種節奏擺在一起。`huggingface/speech-to-speech` 是「健康 GitHub Release ＋ 偶爾 PyPI-only 補丁」型：v0.2.10 走 GitHub release（2026-06-11），但實務可用版本已上 0.2.11（PyPI 2026-07-17）且有 v0.2.11 git tag，只是還沒開 GitHub release entry，是用 PyPI 推 hotfix 的典型。`microsoft/AI-For-Beginners` 與 `paperswithbacktest/awesome-systematic-trading` 完全沒有 release 通道，前者靠 commit 與多語 PR 推進（push 2026-07-21 仍活躍），後者則 18 個月沒 commit，今天上 trending 全靠 star 衝刺。`gavamedia/deltafin` 與 `xikhar/persona` 是「剛出生的研究型／實驗型 release-less」：deltafin 用 `tools/upgrade.py` ＋ commit 做版本，persona 把 release 流程寫進 `docs/RELEASING.md` 等第一個 `v0.1.0-beta.0` tag 才會開 release。換言之，今天「GitHub Releases 看得到新東西」的只有 huggingface/speech-to-speech（v0.2.10），其餘四個用替代管道（commit、PyPI-only、prerelease gate）。
- **套件化路線呈現三種典型**：今天唯一「真正 `pip install` 一行就能用」的是 `huggingface/speech-to-speech`（`pip install speech-to-speech`，且 extras 矩陣分得很細：`[kokoro]`、`[pocket]`、`[chattts]`、`[faster-whisper]`、`[whisper-mlx]`、`[paraformer]`、`[mlx-lm]`）。第二種是「必須 clone 才能跑」的 research-grade Python／Electron 專案：`gavamedia/deltafin`（clone → venv → `tools/build_native.py` → `tools/setup_k3.py` → 1.7 TB 模型）與 `xikhar/persona`（clone → `npm install` → `npm run demo` + 自己 import `.vrm`），這類沒有 PyPI/npm 入口。第三種是「根本不是 library」的：AI-For-Beginners（curriculum + notebook）與 awesome-systematic-trading（curated list），下游要裝的是他們點名的第三方套件（Backtesting.py、vectorbt、Qwen3-TTS、Pocket TTS、Parakeet 等）。
- **語言與生態橫切**：5 個 repo 落在 4 個主語言：Python（speech-to-speech、deltafin、awesome-systematic-trading、microsoft/AI-For-Beginners 四個佔多）、JavaScript／Electron（persona）、Jupyter Notebook（AI-For-Beginners 的實際 cell 語言）、Shell／Markdown（curriculum 與 awesome list 的編排）。今天「infra 跑 C++／Rust，介面跑 Python／JS」的常見分工只在 deltafin 與 speech-to-speech 身上隱約可見：deltafin 把 MoE offload 與 MXFP4 kernel 寫成 C++／CUDA／Metal 透過 `tools/build_native.py` 在本機 build，Python 只負責 orchestration；speech-to-speech 則把 Qwen3-TTS 委外給 `qwentts-cpp-python`（CUDA 12.8 wheel），STT 走 nano-parakeet、Parakeet、Whisper 各自 Python bindings——顯示「Python 是表面、C++/CUDA 是瓶頸」這個 stack 已經是這類本地 AI 工具的標準姿態。
- **上手門檻分層清楚**：最低仍是 `speech-to-speech` 的 `pip install + speech-to-speech` 兩行；中段是 `AI-For-Beginners`（打開 notebook 即可，binder badge 一鍵）與 `awesome-systematic-trading`（讀 README 即可）；偏高的有 `xikhar/persona`（要 Node.js 24 + native build toolchain + 自行準備 VRM/VRMA assets + MCP server 整合）；最高是 `deltafin`（macOS arm64 或 Linux + 1.7 TB 磁碟或 215 GB streaming + 5–10 小時下載 + CUDA / Metal native build + int8 spine 轉換），它的 README 還特別聲明「speed must never come from reducing model quality」，把「研究型本地 frontier inference」的代價講得很白。今天 5 個 repo 加總起來剛好覆蓋從「學生入門」到「frontier 模型自架」的所有門檻。
- **值得追的兩個新訊號**：(1) 7 月底這波 GitHub 熱點繼續圍繞「本地優先 / on-device / open weights」主軸——speech-to-speech 衝到 9.2k stars 是「OpenAI Realtime 相容」這個介面形態在 Hugging Face 內部正式化的證據；deltafin 與上週的 Kimi-K3、AgentENV 一脈相承，把「frontier 開源模型 × 本地工作站」這條路在工具鏈上補到「可以真的用 `pip install`／`npm install` 啟動」的層級。(2) 兩個 web pick 都還沒發 GitHub release，deltafin 用 `tools/upgrade.py`、persona 用 prerelease tag——這代表 release 文化的界線正在重新洗牌：「沒 release」從「未維護」的標記變成「研究／實驗型交付」的合理選擇。下週值得再觀察的是 deltafin 是否會在 1.7 TB 安裝流程穩定後開第一個 GitHub release tag，以及 persona 的 `v0.1.0-beta.0` prerelease gate 何時觸發。
