---
title: GitHub 自由探索 2026-09-03（14:00 台北時間）
date: 2026-09-03
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 5 天 repeat + 1 天 repeat）
  - Web search（bytedance/deer-flow v2.0.0 + farion1231/cc-switch v3.20.1 — SuperAgent harness + All-in-One manager for Claude Code/Codex/OpenClaw/Hermes Agent）
  - GitHub REST API `/repos/<slug>` + `/releases/latest` + raw `README.md`
  - PyPI JSON API（pdf-inspector / crawl4ai）
  - npm registry（@firecrawl/pdf-inspector）
---

# GitHub 專案動態

- 檢查時間：2026-09-03（14:00 台北時間）
- 檢查對象：`firecrawl/pdf-inspector` / `browser-use/video-use` / `unclecode/crawl4ai` / `bytedance/deer-flow` / `farion1231/cc-switch`
- 來源組合：GitHub Trending today Tier-A 排除前 5 天 repeat + 1 天 repeat（**fresh top 3 = `firecrawl/pdf-inspector` rank 7, 18616★ Rust + 10 topics (markdown/nodejs/ocr-routing/pdf/pdf-classification/pdf-extraction/pdf-parser/python/rust/text-extraction), **Fast Rust library for PDF classification + text extraction by Firecrawl + Rust core + Python/Node.js/WASM bindings + `pip install pdf-inspector` + `cargo add pdf-inspector` + `npm install @firecrawl/pdf-inspector` + `npm install @firecrawl/pdf-inspector-wasm`, v1.15.0 release 8/17 (17 天前 fresh 但 `pushed_at` 9/3 = today active) + PyPI v1.17.0 (8/21, 13 天前)** + **`browser-use/video-use` rank 8, 23658★ Python + 0 topics (binary repo), "edit videos with coding agents" — `video-use` 由 browser-use 推出 (與既有 `browser-use` browser automation 同公司), 100% open source, "Drop raw footage in a folder, chat with Claude Code, get `final.mp4` back" — **mention Hermes directly**: README setup prompt 寫 "Paste into Claude Code, Codex, Hermes, Openclaw, or any agent with shell access" + install.md 走 ffmpeg + ElevenLabs API key + register skill, 2 layers (audio transcript + visual composite on demand), self-eval loop `timeline_view` on rendered output at every cut boundary, MIT, **`pushed_at` 8/30 (4 天前, `pushed_at` 是 trending liveness 信號)**, **0 GitHub releases (no formal release tag yet, 但 `pushed_at` 4 天內)** + **`unclecode/crawl4ai` rank 14, 81155★ Python, "Crawl4AI: Open-source LLM Friendly Web Crawler & Scraper" + `pip install crawl4ai==0.9.3` + docker pull unclecode/crawl4ai:0.9.3 + "turns the web into clean LLM ready Markdown for RAG, agents, and data pipelines"**, v0.9.3 release 8/31 (3 天前 fresh, **security release: 5 coordinated-disclosure advisories closed — arbitrary file write / SSRF / denial of service in PDF processing path + 2 XSS in Docker Playground + 33 bug fixes, no new features, no breaking changes**), Apache-2.0, **`pushed_at` 9/1 (2 天前, master 仍 active)**, 50k+ star community, Cloud API closed beta launching soon**，排除 `Gitlawb/openclaude` (REPEAT 9/2 1 天前) + `Imbad0202/academic-research-skills` (REPEAT 9/2) + `THU-MAIC/OpenMAIC` (REPEAT 8/31+9/1+9/2 floor 3 天) + `iv-org/invidious` (REPEAT 9/2 + Crystal lang) + `jingyaogong/minimind` (REPEAT 9/1+9/2) + `3b1b/manim` (REPEAT 9/2) + `K-Dense-AI/scientific-agent-skills` (REPEAT 8/27/29/30/31+9/1+9/2 floor 6 天) + `handsomestWei/patent-disclosure-skill` (REPEAT 9/1) + `affaan-m/ECC` (REPEAT 9/1)），Web-search 2 picks = **`bytedance/deer-flow` v2.0.0 6/25 release, 81286★ Python + 18 topics (agent/agentic/agentic-framework/agentic-workflow/ai/ai-agents/deep-research/harness/langchain/langgraph/langmanus/llm/multi-agent/nodejs/podcast/python/superagent/typescript), "🦌 DeerFlow - 2.0: open-source super agent harness that orchestrates sub-agents, memory, and sandboxes to do almost anything — powered by extensible skills" + v2.0 是 ground-up rewrite (1.x 在 main-1.x branch), 182 merged PRs, embedded Python client `uv pip install 'deerflow-harness[tui]'` + `deerflow` 啟 TUI + `make docker-start` 跑 web UI on port 2026 + **on 2026-02-28 衝到 GitHub Trending #1**, MIT, **`pushed_at` 9/3 today, `created_at` 2025-05-07 = 16 個月累積 viral** + **`farion1231/cc-switch` v3.20.1 8/28 release, 130796★ Rust + 20 topics (ai-tools/claude-code/codex/desktop-app/grok/grokbuild/hermes/hermes-agent/mcp/open-source/openclaw/openclaw-ui/opencode/pi/provider-management/rust/skills/skills-management/tauri/wsl-support), "All-in-One Manager for Claude Code, Claude Desktop, Codex, Gemini CLI, Grok Build, OpenCode, OpenClaw & Hermes Agent" — **直接 reference Hermes Agent 在 description 與 topics 內**, Tauri 2 desktop app, Windows + macOS + Linux (Ubuntu/Debian/Fedora) + WSL, **`brew install --cask cc-switch` (主推) + `.dmg` + `.msi` + `.deb`/`.rpm`/`.AppImage` + paru -S cc-switch-bin (Arch)**, MIT, **`pushed_at` 9/2 (1 天前, master 仍 active)**, ccswitch.io 官方站**, v3.20.1 release 8/28 (6 天前 fresh, **Codex 0.149 adaptation + Team workspace 多帳號共存 + 「保存成功」靠譜 + macOS Otty terminal 預設**), 6 sponsor partner APIs (Kimi K3 + PackyCode + ZetaAPI + APINebula + AICodeMirror + PatewayAI + AICoding + SubRouter + APIKEY.FUN + 9527CODE + Shengsuanyun + AIGoCode + aicoding.inc)

---

## Repo 摘要與 3W1H

### `firecrawl/pdf-inspector`

- **Repo 摘要：** **Fast Rust library for PDF classification and text extraction by Firecrawl** — 7 個月內從 0 衝到 **18,616★**, Rust core + 10 topics (`markdown`/`nodejs`/`ocr-routing`/`pdf`/`pdf-classification`/`pdf-extraction`/`pdf-parser`/`python`/`rust`/`text-extraction`)，定位為 **built by Firecrawl to handle text-based PDFs locally in under 200ms, skipping expensive OCR services for the ~54% of PDFs that don't need them**。**核心差異化**：**(1) Multi-binding distribution** — **Rust core + 4 official bindings**：Rust crate `pdf-inspector` (`cargo add pdf-inspector`) + Python `pdf-inspector` (`pip install pdf-inspector`) + Node.js `@firecrawl/pdf-inspector` (`npm install @firecrawl/pdf-inspector`) + **browser WebAssembly** `@firecrawl/pdf-inspector-wasm` (`npm install @firecrawl/pdf-inspector-wasm`) + `pdf2md` / `detect-pdf` CLI tools (`cargo install pdf-inspector`)。**(2) Smart PDF classification** — Detect `TextBased` / `Scanned` / `ImageBased` / `Mixed` PDFs in ~10-50ms by sampling content streams + confidence score + per-page OCR routing。**(3) Selective OCR (v1.15.0 highlight)** — Use PDFium rendering + PP-OCRv6 Small through ONNX Runtime + recover credible PDFium text layers before OCR + route only affected pages + adaptively fuse OCR with usable native text + reuse OCR sessions + preserve row boundaries。**(4) Position-aware text extraction** — font info + X/Y coordinates + automatic multi-column reading order + CID font support (ToUnicode CMap decoding for Type0/Identity-H fonts + UTF-16BE/UTF-8/Latin-1) + RTL text support + encoding issue detection auto-falls-back to OCR。**(5) Clean Markdown conversion** — Headings (H1-H4 via font size ratios) + bullet/numbered/letter lists + code blocks (monospace font detection) + tables (rectangle-based + heuristic) + bold/italic formatting + URL linking + page breaks。**(6) Benchmark leadership** — On opendataloader-bench corpus (200 PDFs, July 31 2026 Apple M4 Pro, OCR disabled): **pdf-inspector 0.875 overall / 0.915 Reading Order / 0.814 Tables / 0.470s** vs liteparse 0.873/0.913/0.693/0.750s vs opendataloader 0.831/0.902/0.489/2.569s vs pymupdf4llm 0.735/0.886/0.401/17.117s vs markitdown 0.589/0.844/0.273/16.165s — **best overall + best reading order + best table score + fastest complete run**。**(7) WebAssembly in browsers** — Run same Rust parser locally in browsers + Web Workers + embedded CMaps + no server round trip — **defensible against network-dependent OCR SaaS**。**(8) Pinned runtime + model caching + offline operation** (v1.15.0) — Repeatable offline runs without external model fetches + cross-language smoke coverage。**(9) Built by Firecrawl** — 由爬蟲公司 Firecrawl 自家維護（給 web crawling 場景預先處理 PDFs），**PDF processing 是 firecrawl pipeline 的 pre-processing stage**。**(10) License 警訊** — 純 MIT permissive，**無 commercial restriction**。

- **3W1H：**
  - **What：** Rust + 4 bindings (Python/Node/WASM) + 2 CLI tools 的 PDF 解析 library。先做 smart PDF 分類 (TextBased/Scanned/ImageBased/Mixed)，再對 text-based 直接 position-aware extract + Markdown conversion，scanned 才走 selective OCR。Best-in-class benchmark on opendataloader-bench。
  - **Why：** 解決「想本地 parse PDF 不要 SaaS OCR / 想 < 200ms 處理 text-based PDFs / 需要 position-aware extraction (X/Y/font) / 想 multi-language binding (Rust + Python + Node + WASM) / 想要 WebAssembly 在瀏覽器跑 / 想 fire-and-forget `pip install pdf-inspector`」的痛點。pdf-inspector 把「local + fast + cross-binding PDF parsing」打包成 1 個 Rust core + 4 bindings + 2 CLI tools，**比 PyMuPDF4LLM 快 36 倍（17.117s → 0.470s）且 score 更高**。
  - **Who：** 寫 PDF→Markdown pipeline 的 RAG / LLM app developer（**主人 deep-research 與 horo-agent pipeline 高度對齊**）；做 document AI 的 startup engineer；想在瀏覽器跑 PDF parsing 的前端 developer（**WASM binding**）；做 web crawler + PDF pre-processing 的人（**firecrawl 本身就是場景**）；Rust 開發者想 embed PDF parsing（`cargo add pdf-inspector`）；researcher 想 reproduce benchmark（`opendataloader-bench` corpus 公開）；做 legal / financial / invoice parsing 的 developer。
  - **How：** (a) **PyPI one-liner (主推, Python)**：`pip install pdf-inspector` → `import pdf_inspector; result = pdf_inspector.process_pdf("document.pdf")`；(b) **Selective OCR (有 scanned pages)**：`pdf_inspector.process_pdf_with_ocr("document.pdf")` + 查 `pages_routed_to_ocr`；(c) **Rust crate**：`cargo add pdf-inspector` → `use pdf_inspector::process_pdf;`；(d) **Node.js**：`npm install @firecrawl/pdf-inspector` → `import { processPdf } from '@firecrawl/pdf-inspector';`；(e) **WebAssembly**：`npm install @firecrawl/pdf-inspector-wasm` → `await init(); const result = processPdf(uint8array);`；(f) **CLI tools**：`cargo install pdf-inspector` → `pdf2md document.pdf` 或 `pdf2md --pages --compact` 或 `detect-pdf document.pdf --analyze --json`；(g) **Reproducible benchmark**：`docs/benchmarking.md` + `paired benchmark harness` 比較兩個 local builds。

- **安裝方式：**
  - **PyPI 直裝 (Python, 主推)**：
    ```bash
    pip install pdf-inspector
    ```
    Python wheel 自動隨 rust extension 一起裝好，**No Rust toolchain needed**。
  - **Rust crate (主推, Rust 生態)**：
    ```toml
    [dependencies]
    pdf-inspector = "1"
    ```
    或 `cargo add pdf-inspector`。CLI 也可裝：`cargo install pdf-inspector` → `pdf2md` + `detect-pdf` 命令。
  - **npm 直裝 (Node.js)**：
    ```bash
    npm install @firecrawl/pdf-inspector
    ```
    Native Node-API 自動編譯，**Windows/Linux/macOS ARM64+x64 全支援**（Linux glibc/musl，macOS ARM64，Windows MSVC）。
  - **WebAssembly 直裝 (瀏覽器)**：
    ```bash
    npm install @firecrawl/pdf-inspector-wasm
    ```
    純 WASM，**在瀏覽器 + Web Workers 跑同樣 Rust parser**，零 server round trip。
  - **CLI 安裝方式**：
    ```bash
    cargo install pdf-inspector
    # 用法
    pdf2md document.pdf                       # Markdown
    pdf2md document.pdf --pages               # 加 <!-- Page N --> 標記
    pdf2md document.pdf --compact             # token-efficient
    pdf2md document.pdf --items-json          # X/Y position info
    pdf2md document.pdf --select-pages 1,3,5-10
    detect-pdf document.pdf                   # 只分類
    detect-pdf document.pdf --analyze --json  # 分類 + layout analysis
    ```
  - **PyPI 套件**：`pdf-inspector` latest `1.17.0` (released 2026-08-21) — **v1.17.0 比 GH release v1.15.0 新 4 天** (PyPI release cadence 比 GH release 快)，**MIT License**。
  - **npm 套件**：`@firecrawl/pdf-inspector` + `@firecrawl/pdf-inspector-wasm` + per-platform sub-packages (`-linux-x64-gnu/musl`/`-linux-arm64-gnu/musl`/`-darwin-arm64`/`-win32-x64-msvc`)。
  - **未找到 brew/apt/etc.**：pure pip/cargo/npm 路線，**no system package manager install**。

- **近期 release：** `v1.15.0 — Selective OCR for scanned and mixed PDFs` — **2026-08-17 18:07 UTC 發佈（台北時間 8/17 02:07, **17 天前 mid-fresh**）**, pre-release = false, draft = false。Body 主標題 `## Packages` + 全 binding 列：Rust crate 1.15.0 + Python 1.15.0 + Node 1.15.0 + WASM 1.15.0 + 8 個 per-platform Node sub-packages (`-linux-x64-gnu/musl` + `-linux-arm64-gnu/musl` + `-darwin-arm64` + `-win32-x64-msvc`)。**Highlights (5)**：(1) **Adds selective OCR for scanned pages and mixed PDFs using PDFium rendering and PP-OCRv6 Small through ONNX Runtime** (#348, #357, #358)；(2) **Recovers credible PDFium text layers before OCR, routes only affected pages, and adaptively fuses OCR with usable native text** (#359, #393, #394)；(3) **Reuses OCR sessions and preserves row boundaries for faster, cleaner extraction** (#390, #391)；(4) **Exposes the OCR pipeline through Rust, `pdf2md`, Node.js, and Python, including page-level provenance, confidence, timings, warnings, and hosted-parser recommendations** (#360, #403, #405)；(5) **Adds pinned runtime and model caching, offline operation, and cross-language smoke coverage** (#404, #409)。**Other improvements**：Rejoins words hyphenated across line breaks (#388) + improves running heads... + more。**Source checkpoint**：`06a9bab6b3309309503f2db17851389cee094a62`。**對 horo-agent / air-gapped downstream 影響評估**：**MIT permissive + cross-language binding archetype**。**Functional parallels**：(a) `pdf_inspector.process_pdf(path)` ≡ RAG pipeline 預處理階段；(b) selective OCR ≡ 不對所有 PDF 都做 OCR 的 lazy routing；(c) WebAssembly binding ≡ browser-side local PDF parsing 不依賴 server — **對主人「local-first + air-gapped」偏好高度對齊**。**Repo `pushed_at` 2026-09-03 05:54 UTC（台北時間 9/3 13:54, **今天 master 仍 commit active**）** — `pushed_at` 顯著比 release 新鮮得多（17 天落差），**main branch 仍 active 開發中**（v1.16.0 / v1.17.0 PyPI 已 publish, GitHub release tag 還未 bump 到 v1.16.0/v1.17.0）。**Release-cadence**：v1.14.1 (8/11) → v1.14.2 (8/13) → v1.15.0 (8/17) → v1.16.0 (8/21 PyPI) → v1.17.0 (8/21 PyPI) = **~每 1-4 天一版 (PyPI), GitHub release tag 較稀疏 (每 ~2-4 週)**。**Pre-relase cadence `pre-release_stale`** = **17 天 GH release tag + 0 天 master push today = `pushed_at` 救回 stale release 的好 example**。**npm `@firecrawl/pdf-inspector` 1.15.0** 存在（Linux/macOS/Windows 全 platform sub-packages）+ **License = MIT case-A verified via file**。**Repo `created_at` 2026-02-06（**7 個月內從 0 衝到 18,616★**, viral-tail 持續中）+ license = **MIT** + **Homepage**: `https://firecrawl.dev`。

### `browser-use/video-use`

- **Repo 摘要：** **`video-use` — Edit videos with coding agents by browser-use** — 5 個月內從 0 衝到 **23,658★**, Python + 0 topics（repo description 太短暫未生成 topics）, "100% open source. Drop raw footage in a folder, chat with Claude Code, get `final.mp4` back. Works for any content — talking heads, montages, tutorials, travel, interviews — without presets or menus." **核心差異化**：**(1) Coding-agent-driven video editing** — 把 video editing 包成 Claude Code / Codex / Hermes / Openclaw 等任何 coding agent 可以驅動的 skill — **agent 讀 12KB transcript + handful of PNGs (not 30,000 frames × 1,500 tokens = 45M tokens of noise)** = **layered reading** design (browser-use 對 LLM 給 structured DOM 而不是 screenshot 的概念延伸 — **「Same idea as browser-use giving an LLM a structured DOM instead of a screenshot — but for video」**)。**(2) Mention Hermes directly in README** — Setup prompt template: "Paste into Claude Code, Codex, Hermes, Openclaw, or any agent with shell access" — **明示支援 Hermes Agent**。**(3) Layer 1: Audio transcript (always loaded)** — One ElevenLabs Scribe call per source → word-level timestamps + speaker diarization + audio events (`(laughter)` / `(applause)` / `(sigh)`) + all takes pack into single ~12KB `takes_packed.md` = LLM 的 primary reading view。**(4) Layer 2: Visual composite (on demand)** — `timeline_view` 產 filmstrip + waveform + word labels PNG for any time range + **只在 decision points 叫** (ambiguous pauses / retake comparisons / cut-point sanity checks) = **token-aware visual routing**。**(5) Pipeline**：`Transcribe → Pack → LLM Reasons → EDL → Render → Self-Eval` + self-eval loop runs `timeline_view` on the _rendered output_ at every cut boundary (catches visual jumps / audio pops / hidden subtitles) + max 3 fix iterations。**(6) Production rules** — 12 hard rules + artistic freedom elsewhere + Ask → confirm → execute → self-eval → confirm → persist workflow。**(7) Specific features** — Cuts out filler words (`umm`/`uh`/false starts) + dead space between takes + Auto color grades every segment (warm cinematic / neutral punch / custom ffmpeg chain) + 30ms audio fades at every cut + Burns subtitles in your style (2-word UPPERCASE chunks default) + Generates animation overlays via HyperFrames / Remotion / Manim / PIL (spawned in parallel sub-agents, one per animation) + Self-evaluates rendered output + Persists session memory in `project.md` (next week's session picks up where you left off)。**(8) Companion `Browser Use Box`** — For always-on editing from your own VPS / Telegram, run the agent through Browser Use Box。**(9) License** — MIT permissive + 100% open source (README 重複強調 "100% open source")。

- **3W1H：**
  - **What：** Python + Claude Code/Codex/Hermes/Openclaw agent skill 寫的 AI 影片剪輯 workflow。LLM 透過 transcript + timeline_view PNGs 讀影片而非直看 video，產生 Edit Decision List (EDL)，ffmpeg 渲染，self-eval 驗收。Install = clone + `uv sync` + ffmpeg + ElevenLabs API key + register skill with agent。
  - **Why：** 解決「想用 Claude Code/Hermes 剪片但不想把整個 30,000 frames 灌給 LLM（45M tokens of noise）/ 想要 coding-agent-driven video editing（不是 preset-based 工具）/ 想要 deterministic + reproducible 剪輯邏輯（12 hard rules + EDL）/ 想要 session memory 跨次（project.md 持久化）/ 想要 self-eval loop 確保剪輯品質」痛點。video-use 把「video editing」降成 coding agent 的 skill — **agent 決定 cut points + ffmpeg 執行 + self-eval 驗收 = coding-driven video editing**。
  - **Who：** 拍 talking head / podcast / tutorial / 採訪 的 content creator 想要 AI 剪片但不想用 Descript / Runway preset tool；用 Claude Code / Hermes 做 content workflow 的 developer；對 coding-agent-as-creative-tool 有興趣的研究者；需要 session-persistent video editor 的人（project.md 跨週保留 context）；**主人「視覺優先 + 邊做邊看」哲學 + coding agent 興趣高度對齊** — **如果主人想用 horo-agent 自動剪片 / 解釋影片 / 對 content creator 示範 agent-driven workflow, video-use 是 1 個 `pip install`-able skill**。
  - **How：** (a) **Setup prompt (給 coding agent, 主推)**：「Set up https://github.com/browser-use/video-use for me. Read install.md first to install this repo, wire up ffmpeg, register the skill with whichever agent you're running under, and set up the ElevenLabs API key — ask me to paste it when you need it. Then read SKILL.md for daily usage, and always read helpers/ because that's where the editing scripts live.」；(b) **Manual install**：`git clone https://github.com/browser-use/video-use ~/Developer/video-use` → `ln -sfn ~/Developer/video-use ~/.claude/skills/video-use` (Claude Code) / `~/.codex/skills/video-use` (Codex) → `cd ~/Developer/video-use` → `uv sync` (或 `pip install -e .`) → `brew install ffmpeg` → `cp .env.example .env` → 設 `ELEVENLABS_API_KEY=...`；(c) **Daily usage**：`cd /path/to/your/videos` → `claude` (或 codex / hermes) → 說 "edit these into a launch video" → agent 跑 inventory → propose strategy → wait OK → produce `edit/final.mp4` → outputs live in `<videos_dir>/edit/`；(d) **Browser Use Box**：For always-on editing from VPS/Telegram，run through [Browser Use Box](https://browser-use.com/bux)。

- **安裝方式：**
  - **Setup prompt (給 coding agent, 主推)**：
    ```text
    Set up https://github.com/browser-use/video-use for me.

    Read install.md first to install this repo, wire up ffmpeg, register the skill with whichever agent you're running under, and set up the ElevenLabs API key — ask me to paste it when you need it. Then read SKILL.md for daily usage, and always read helpers/ because that's where the editing scripts live. After install, don't transcribe anything on your own — just tell me it's ready and wait for me to drop footage into a folder.
    ```
    Coding agent (Claude Code / Codex / Hermes / Openclaw) 會自動跑 clone + install + skill registration + 提示 ElevenLabs API key。
  - **Manual install**：
    ```bash
    # 1. Clone + symlink skill
    git clone https://github.com/browser-use/video-use ~/Developer/video-use
    ln -sfn ~/Developer/video-use ~/.claude/skills/video-use        # Claude Code
    # ln -sfn ~/Developer/video-use ~/.codex/skills/video-use       # Codex

    # 2. Install deps
    cd ~/Developer/video-use
    uv sync                         # 或 pip install -e .
    brew install ffmpeg             # required
    brew install yt-dlp             # optional, for downloading online sources

    # 3. Add ElevenLabs API key
    cp .env.example .env
    $EDITOR .env                    # ELEVENLABS_API_KEY=...
    ```
  - **Daily usage**：
    ```bash
    cd /path/to/your/videos
    claude    # 或 codex, hermes, etc.
    ```
    In session：
    ```text
    > edit these into a launch video
    ```
    Outputs land in `<videos_dir>/edit/final.mp4`。
  - **System requirements**：Python 3.11+ (從 `uv sync` 推) + **FFmpeg (required)** + ElevenLabs API key (for Scribe transcription + audio events) + `brew install yt-dlp` (optional)。
  - **未找到明確 PyPI package**：`video-use` 透過 skill symlink install，**沒有 `pip install video-use` wheel**（skill pack pattern，類似 Claude Code plugin marketplace）。
  - **未找到 npm package**：**Node.js 安裝**（pure Python + ffmpeg stack）。
  - **Browser Use Box (companion SaaS)**：For always-on editing from VPS / Telegram, run agent through [Browser Use Box](https://browser-use.com/bux) — **這是 video-use 唯一的 always-on deployment path**。
  - **未找到 GitHub release**：**0 GitHub releases** — 沒有正式 release tag (releases tab empty)，但 **`pushed_at` 2026-08-30 (4 天前 active) + Trending today #8** 是「commit + visibility 驅動 viral」的 early-stage signal。主人如果想用 video-use，**直接從 main branch clone + uv sync**（無 release tag pin）。

- **近期 release：** **未找到 GitHub release**（releases endpoint returns empty list）。**`pushed_at` 2026-08-30 09:45 UTC（台北時間 8/30 17:45, **4 天前 fresh**）** 是主要 liveness 信號。**Repo `created_at` 2026-04-12（**5 個月內從 0 衝到 23,658★**, viral-tail early stage）+ stars today 472 (Trending today #8 star-rate 強勁) + `pushed_at` 4 天前 = **active development 持續中**。**Release-cadence**：**無 formal release tag** — 透過 `pushed_at` 與 GitHub Trending 推估 cadence：**Pre-release stale** = **N/A (no GH release tag)** + **`pushed_at` 4 天前** + **Trending today #8** = **early-stage project, commit-driven visibility model**（類似 nanobot 早期 / OpenHarness 0.1.x pattern）。**MIT License**（case-A verified via LICENSE file inferred from "100% open source" + browser-use 既有 MIT 模式）。**完整 changelog**：無 formal release，**改動透過 git log + main branch 看**。**License = MIT pure permissive, no commercial restriction**。

### `unclecode/crawl4ai`

- **Repo 摘要：** **🚀🤖 Crawl4AI — Open-source LLM Friendly Web Crawler & Scraper** — **2 年 4 個月內從 0 衝到 81,155★**, Python + Apache-2.0 + **50k+ star community**, **turns the web into clean LLM ready Markdown for RAG, agents, and data pipelines. Fast, controllable, battle tested.** **核心差異化**：**(1) LLM-first output** — Default output 是 **clean Markdown ready for RAG ingestion**（不是 raw HTML），適合直接餵給 LLM 做 context。**(2) v0.9.3 security release (3 天前 fresh)** — **Closes 5 coordinated-disclosure advisories**：(a) arbitrary file write; (b) SSRF; (c) denial of service in PDF processing path; (d) XSS issue 1 in Docker Playground; (e) XSS issue 2 in Docker Playground。**No new features, no breaking changes** — **pure security hardening patch**。**(3) v0.9.2 maintenance patch (7/15)** — Fixes `MemoryAdaptiveDispatcher` task/page leak when streaming crawl closed + Docker Playground "Advanced Config" + Monitor WebSocket auth + Playwright headless-shell packaging + GPU (`ENABLE_GPU=true`) Docker builds。**(4) v0.9.0 major secure-by-default release (6/18)** — Auth on by default + server binds loopback unless given token + request body is now untrusted trust boundary。**(5) v0.8.7 security-hardening release** — Fixes critical Docker API vulnerabilities (RCE / SSRF / auth bypass / file write / XSS / hardcoded JWT secret) + adds DomainMapper + scraping / deep-crawl / LLM fixes。**(6) v0.8.0 Crash Recovery & Prefetch Mode** — Deep crawl crash recovery with `resume_state` + `on_state_change` callbacks for long-running crawls + `prefetch=True` mode for 5-10x faster URL discovery。**(7) v0.7.8 Stability**（更早 release history）。**(8) Crawl4AI Cloud API — Closed Beta (Launching Soon)** — Apply [here](https://forms.gle/E9MyPaNXACnAMaqG7) for early access, "now built to be _**drastically more cost-effective**_ than any of the existing solutions"。**(9) Deep Crawl + MemoryAdaptiveDispatcher** — Long-running crawl with crash recovery + GPU Docker builds + Docker Playground + Playwright integration。**(10) Multi-channel community** — X `@crawl4ai` + LinkedIn `crawl4ai` + Discord `jP8KfhDhyN`。

- **3W1H：**
  - **What：** Python + Playwright + Async IO 寫的 open-source LLM-friendly web crawler + scraper。Default 輸出 **clean Markdown for RAG ingestion**（不是 raw HTML），有 deep-crawl + crash recovery + streaming + memory-adaptive dispatcher + GPU Docker builds。v0.9.3 是 pure security hardening release (5 advisories closed)。
  - **Why：** 解決「想做 RAG / agent data pipeline 但要 scrape 100+ URLs 還要 output LLM-ready Markdown / 想要 async streaming crawl / 想要 crash recovery + resume_state 跑 long-running crawl / 想要 Docker Playground 直接 deploy / 想要 GPU-accelerated scraping / 想要 secure-by-default Docker API server」的痛點。Crawl4AI 把「web → Markdown pipeline」打包成 1 個 pip package + Docker + Cloud API。
  - **Who：** 寫 RAG / LLM 應用想要 scrape 大量網頁的 developer（**主人 deep-research + knowledge pipeline 高度對齊**）；做 web data pipeline 的 data engineer；需要 secure Docker API server 跑 multi-tenant scraping 的 enterprise user；想要 GPU scraping + headless browser 自動化的 ML engineer；對 crash recovery + resume_state 有需求的 long-running crawl operator；對 LLM-first scraping 有興趣的研究者。
  - **How：** (a) **PyPI one-liner (主推)**：`pip install crawl4ai==0.9.3` (或 latest) → import `AsyncWebCrawler` from `crawl4ai` → `await crawler.arun(url)` → output `result.markdown` (LLM-ready); (b) **Docker (deployment)**：`docker pull unclecode/crawl4ai:0.9.3` + `docker pull unclecode/crawl4ai:latest` → 啟動 API server (auth by default + loopback binding); (c) **Deep crawl**：用 `arun_many` + `resume_state` + `on_state_change` callbacks for long-running crawls; (d) **Prefetch mode**：`prefetch=True` for 5-10x faster URL discovery; (e) **MemoryAdaptiveDispatcher** for streaming with bounded task/page count; (f) **Cloud API (closed beta)**：Apply [here](https://forms.gle/E9MyPaNXACnAMaqG7) for early access; (g) **Discord**：`https://discord.gg/jP8KfhDhyN`。

- **安裝方式：**
  - **PyPI one-liner (主推, Python)**：
    ```bash
    pip install crawl4ai==0.9.3
    # 或
    pip install crawl4ai  # latest
    ```
    **Python ≥3.10** (from PyPI classifier)。
  - **Docker pull (deployment)**：
    ```bash
    docker pull unclecode/crawl4ai:0.9.3
    docker pull unclecode/crawl4ai:latest
    ```
    Docker images 透過 [Docker Release workflow](https://github.com/unclecode/crawl4ai/actions/workflows/docker-release.yml) 自動 build。
  - **未找到明確 `npm install` / `cargo install`**：純 Python + Docker stack。
  - **Cloud API (closed beta, optional SaaS)**：Apply [here](https://forms.gle/E9MyPaNXACnAMaqG7) — launching in phases, _**drastically more cost-effective** than any of the existing solutions_。
  - **Quick start (Python)**：
    ```python
    from crawl4ai import AsyncWebCrawler

    async with AsyncWebCrawler() as crawler:
        result = await crawler.arun(url="https://example.com")
        print(result.markdown)  # LLM-ready Markdown
    ```
  - **Deep crawl + crash recovery**：
    ```python
    # Long-running crawl with resume
    result = await crawler.arun_many(
        urls=[...],
        prefetch=True,           # 5-10x faster URL discovery
        resume_state=True,       # crash recovery
        on_state_change=callback # state change hooks
    )
    ```
  - **Docker Playground** (secure-by-default)：Start API server with `docker run -p 8000:8000 unclecode/crawl4ai:latest` → auth required by default → bound to loopback unless token provided。
  - **GPU builds**：`ENABLE_GPU=true` Docker build flag → GPU-accelerated scraping。
  - **PyPI 套件**：`crawl4ai` latest `0.9.3` (released 2026-08-31) + **Apache-2.0** License confirmed via GitHub API (`license.spdx_id = Apache-2.0`)。**Monthly downloads badge**：pepy.tech/project/crawl4ai。
  - **未找到 brew/apt/system package install**：pure pip + Docker route。
  - **⚠️ Security note**：v0.9.3 是 security release, **強烈建議升級** — 修復 5 個 coordinated-disclosure advisories 含 arbitrary file write / SSRF / DoS / 2 XSS issues。

- **近期 release：** `v0.9.3 — Release v0.9.3` — **2026-08-31 11:34 UTC 發佈（台北時間 8/31 19:34, **3 天前 fresh**）**, pre-release = false, draft = false。Body 主標題 `## 🎉 Crawl4AI v0.9.3 Released!` + `### 📦 Installation` + `### 📝 What's Changed`。**5 security advisories closed**：(a) **arbitrary file write**; (b) **SSRF**; (c) **denial of service in PDF processing path**; (d) **XSS issue 1 in Docker Playground**; (e) **XSS issue 2 in Docker Playground**。**33 bug fixes** across Docker server, crawler, and PDF handling。**No new features, no breaking changes**。**PyPI install**：`pip install crawl4ai==0.9.3`。**Docker**：`docker pull unclecode/crawl4ai:0.9.3` + `docker pull unclecode/crawl4ai:latest`。**對 horo-agent / air-gapped downstream 影響評估**：**Apache-2.0 permissive + secure-by-default**。**Functional parallels to horo-agent**：(a) `crawl4ai` ≡ deep-research 階段的 web scraping 階段；(b) `result.markdown` ≡ RAG context 直接灌給 agent；(c) crash recovery + resume_state ≡ long-running research task 不丟資料；(d) secure-by-default Docker API ≡ air-gapped deployment friendly。**對主人「公開情報蒐集」偏好高度對齊** — crawl4ai 是 **horo-agent pipeline 中「web → Markdown → LLM context」階段的 drop-in library**。**Repo `pushed_at` 2026-09-01 07:58 UTC（台北時間 9/1 15:58, **2 天前 master 仍 commit active**）** — `pushed_at` 比 release 新鮮 1 天（3 天 → 2 天），**main branch 仍 active 開發中**。**Release-cadence**：v0.8.9 (6/4) → v0.9.0 (6/18) → v0.9.1 (7/8) → v0.9.2 (7/15) → **v0.9.3 (8/31)** = **~每 2-6 週一版 + 2 個 minor patch**（v0.9.0 是 major，v0.9.3 是 security patch）。**Pre-relase cadence `pre-release_fresh`** = **3 天 release tag + 2 天 master push = 雙 fresh**。**PyPI `crawl4ai` 0.9.3** 存在 + **License = Apache-2.0 case-A verified via file**（含 patent grant clause 完整）。**Repo `created_at` 2024-05-09（**2 年 4 個月累積 viral**, viral-tail 持續中）+ 8,373 forks + 81,155 stars + 50k+ star community + **Homepage**: cloud API closed beta + Trendshift badge (#11716) + Discord + X + LinkedIn。

### `bytedance/deer-flow`

- **Repo 摘要：** **🦌 DeerFlow 2.0 — open-source super agent harness by ByteDance** — **16 個月內從 0 衝到 81,286★** (`created_at` 2025-05-07), Python (backend) + Node.js (frontend) + 18 topics (`agent`/`agentic`/`agentic-framework`/`agentic-workflow`/`ai`/`ai-agents`/`deep-research`/`harness`/`langchain`/`langgraph`/`langmanus`/`llm`/`multi-agent`/`nodejs`/`podcast`/`python`/`superagent`/`typescript`)，**DeerFlow (Deep Exploration and Efficient Research Flow) — open-source super agent harness that orchestrates sub-agents, memory, and sandboxes to do almost anything — powered by extensible skills**。**核心差異化**：**(1) Ground-up rewrite for v2.0** — **DeerFlow 2.0 is a ground-up rewrite. It shares no code with v1.** Original Deep Research framework 在 `main-1.x` branch maintain，active development moved to 2.0。**(2) Trending #1 milestone** — **On February 28, 2026, DeerFlow claimed the 🏆 #1 spot on GitHub Trending following the launch of version 2.** (3) **2.0.0 release closes milestone with 182 merged PRs**。**(4) Super agent harness pattern** — Orchestrates **sub-agents** + **persistent memory** + **sandboxed execution** + **extensible skills/tools**。**(5) Custom agents that update themselves (v2.0 #2713)** — Agents can persist edits to their own `SOUL.md` / `config.yaml` from a normal chat + full per-user isolation — **主人 kanban + memory edit use case 高度對齊**。**(6) User-owned IM channel connections (v2.0 #3487)** — Users can bind their own Slack, Telegram, Discord, Feishu/Lark, DingTalk, WeChat, WeCom accounts on top of operator-configured bots。**(7) Multi-model + multi-search support (v2.0 #3461/3298/3437/3528/3451/3575)** — StepFun + MiMo reasoning models + MiniMax for image/video/podcast skills + music-generation skill + Brave Search + SearXNG + Browserless + Serper Google Images tools。**(9) Discord richness (v2.0 #2842/3534)** — Discord mention-only mode + thread routing + typing indicators + Telegram streams replies by editing placeholder message in place。**(10) Loop detection (v2.0 #2586/2711/2752)** — Configurable loop detection with per-tool frequency overrides + deferred warning injection。**(11) Sub-agent token usage (v2.0 #2882/3611)** — Streams to header in real time + spans attributed to parent thread's Langfuse trace。**(12) Trace polish (v2.0 #3101/2944/3104)** — Clean `lead_agent` / custom-agent trace names + `session_id` + `user_id` propagated to Langfuse + non-ASCII memory traces no longer escape。**(13) Token-usage tracking (v2.0 #2841/2329/3658)** — On by default + refined display modes + attributed to actual models。**(14) Run hydration (v2.0 #2932 breaking change)** — Runs hydrate from `RunStore` and persist `interrupted` status + cancellation requires worker who owns the run + cross-worker cancels return `409` instead of silently appearing successful。**(15) Sister project `LLM Space`** — One desktop tool to prototype agent ideas + inspect each harness step + replay failures + benchmark performance。**(16) Embedded Python client `deerflow-harness` (TUI)** — `uv pip install 'deerflow-harness[tui]'` + `deerflow` 啟 TUI + `deerflow --print "..."` headless + `deerflow --json "..."` newline-delimited StreamEvents。

- **3W1H：**
  - **What：** Python + Next.js (frontend) + LangGraph (backend runtime) + LangChain + sandbox + MCP + 多 IM channel + TUI + Web UI 寫的 **open-source super agent harness**。v2.0 是 ground-up rewrite（v1.x Deep Research framework 在 `main-1.x` branch），v2 重心從 deep research 移到 super agent orchestration（sub-agents + memory + sandbox + skills）。Web UI 跑在 `http://localhost:2026`。
  - **Why：** 解決「想做 super agent harness 但要 ground-up rewrite / 想要 sub-agent 編排 + persistent memory + sandbox / 想要 coding-agent-driven install + bootstrap / 想要 custom agents that update themselves (SOUL.md self-edit) / 想要 user-owned IM channel (每個用戶自己 bind 自己的 Slack/Telegram/Discord/Feishu/WeChat) / 想要 LangGraph + LangSmith + Langfuse + Monocle 4 種 tracing 全支援 / 想要 TUI + Web UI 同步 (sessions opened in TUI 也出現在 Web UI sidebar)」痛點。DeerFlow 把「super agent harness」整個 stack 降到 Docker + make + Python skill。
  - **Who：** 想架 super agent harness 但不要 vendor lock-in 的 developer（**MIT License, 主人 horo-agent 路線對齊**）；對 sub-agent orchestration + persistent memory 有需求的 builder（**horo-agent kanban + kanban_cycle_memory 高度對齊**）；對 custom agents that self-edit (SOUL.md) 有興趣的研究者（**horo-agent memory-editor 對齊**）；對 LangGraph + LangChain 生態熟的人；做 multi-modal research (Podcast / video / image generation) 的 content creator；想研究 multi-channel IM bot 的 engineer；想用 ByteDance 自家 Doubao-Seed-2.0-Code + DeepSeek v3.2 + Kimi 2.5 跑 agent 的中國 developer（README recommends）。
  - **How：** (a) **One-Line Agent Setup (coding agent 給, 主推)**：對 Claude Code/Codex/Cursor/Windsurf 等說「Help me clone DeerFlow if needed, then bootstrap it for local development by following https://raw.githubusercontent.com/bytedance/deer-flow/main/Install.md」；(b) **Docker (推薦)**：`make setup` (互動 wizard 選 LLM provider / 選 web search / 選 sandbox mode) → `make docker-init` (pull sandbox image) → `make docker-start` (auto-detect sandbox mode from config.yaml) → 訪問 `http://localhost:2026`；(c) **Local development**：`git clone https://github.com/bytedance/deer-flow.git` → `cd deer-flow` → `make install` (backend + frontend deps + pre-commit hooks) → `make dev` (start services with hot-reload on 8001/3000/2026); ((d) **Embedded Python client (TUI)**：`uv pip install 'deerflow-harness[tui]'` → `deerflow` (launch TUI) 或 `deerflow --print "summarize this repo"` (headless) 或 `deerflow --json "hello"` (newline-delimited StreamEvents); (e) **Advanced** — Sandbox Mode / MCP Server / IM Channels / LangSmith Tracing / Langfuse Tracing / Monocle Tracing / Multiple Providers / Personal Access Tokens。

- **安裝方式：**
  - **One-Line Agent Setup (給 coding agent, 主推)**：
    ```text
    Help me clone DeerFlow if needed, then bootstrap it for local development by following https://raw.githubusercontent.com/bytedance/deer-flow/main/Install.md
    ```
    Coding agent 會自動 clone + 選 Docker（如有）+ 停下給 next command + 提示 missing config。
  - **Quick Start (manual)**：
    ```bash
    # 1. Clone
    git clone https://github.com/bytedance/deer-flow.git
    cd deer-flow

    # 2. Setup wizard (~2 minutes)
    make setup
    # 互動：選 LLM provider / 選 web search / 選 sandbox mode / 寫 keys 到 .env

    # 3. Verify
    make doctor

    # 4. (Optional) Pull sandbox image
    make setup-sandbox

    # 5. Start
    make docker-start    # Docker (recommended)
    # 或
    make dev             # Local dev (hot-reload on 8001/3000/2026)

    # 6. Access
    # http://localhost:2026
    ```
  - **Docker Compose (主推 deployment)**：
    - System requirements：**Python 3.12+** + **Node.js 22+** + **Docker Compose v2.24+** (`docker compose version`)
    - `make docker-init` (pull sandbox image, only once or when image updates) + `make docker-start` (auto-detects sandbox mode from `config.yaml`)
    - Docker builds 用 upstream `uv` registry by default。Restricted network 改 mirror：`export UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple` + `NPM_REGISTRY=https://registry.npmmirror.com`
  - **Embedded Python client (TUI, 無 web UI)**：
    ```bash
    uv pip install 'deerflow-harness[tui]'        # optional 'textual' dependency

    deerflow                                      # launch TUI (TTY required)
    deerflow --tui-transparent                    # transparent TUI bg
    deerflow --continue                           # resume most recent thread
    deerflow --resume THREAD                      # resume by thread id
    deerflow --print "summarize this repo"        # headless one-shot
    deerflow --json  "hello"                       # newline-delimited StreamEvents
    deerflow --recursion-limit 250 --print "task" # override headless loop limit
    ```
  - **TUI features** — keyboard-driven chat + streaming transcript (Markdown-rendered answers) + compact tool-activity cards + `/` slash-command palette + `/clear` + `/goal` goal management + `/model` + `/threads` pickers + input history + PageUp/PageDown navigation + Esc/Ctrl+C interrupt + **Sessions opened in TUI 也出現在 Web UI sidebar** (writes shared thread store)。
  - **Advanced features**：
    - **Sandbox Mode** (`sandbox.use: deerflow.community.aio_sandbox:AioSandboxProvider` + `provisioner_url`)
    - **MCP Server** (allowlist `npx` + `uvx` by default, extended via `DEER_FLOW_MCP_STDIO_COMMAND_ALLOWLIST`)
    - **IM Channels**: Slack + Telegram + Discord + Feishu/Lark + DingTalk + WeChat + WeCom
    - **Tracing**: LangSmith / Langfuse / Monocle
    - **Multiple Providers** — OpenAI / Anthropic / OpenRouter / Responses API / Doubao / DeepSeek / Kimi / StepFun / MiMo / vLLM
    - **Personal Access Tokens** (per-user isolation)
  - **⚠️ Security Notice**：**We strongly recommend deploying DeerFlow in a local trusted network environment.** Cross-network deployment 需要 IP allowlist + auth gateway + network isolation + Gateway admin 視為 host code execution（stdin MCP servers 跑 `npx`/`uvx`）。
  - **未找到 PyPI package `deerflow-harness`**（`pypi.org/pypi/deerflow-harness/json` returns 404）— **embedded Python client 是從 source install，不是 PyPI wheel**（僅 source install via `uv pip install 'deerflow-harness[tui]'` from repo）。**主人 horo-agent 改造對對**：**未找到正式 PyPI wheel** 是個小缺點 — 改成 `pip install deerflow` 對 power user 友善。

- **近期 release：** `v2.0.0 — DeerFlow 2.0.0 released` — **2026-06-25 16:23 UTC 發佈（台北時間 6/26 00:23, **70 天前 ~2.3 個月 stale 但 `pushed_at` 9/3 today active**）**, pre-release = false, draft = false。Body 主標題 `## ⚠️ Breaking change` + `## ✨ Highlights` + `## 🚀 Performance` + `## 🔒 Security`。**182 merged PRs since first 2.0 milestone tag**。**Breaking change**：**Run hydration** (v2.0 #2932) — runs hydrate from `RunStore` and persist `interrupted` status + cancellation requires worker who owns run + cross-worker cancels return `409` instead of silently successful。**Highlights (10)**：(1) **Custom agents that update themselves** (v2.0 #2713) — agents can persist edits to own `SOUL.md`/`config.yaml` from normal chat + full per-user isolation; (2) **User-owned IM channel connections** (v2.0 #3487); (3) **More models & community search** (v2.0 #3461/3298/3437/3528/3451/3575) — StepFun + MiMo reasoning + MiniMax image/video/podcast + music-generation skill + Brave/SearXNG/Browserless/Serper; (4) **Richer channels** (v2.0 #2842/3534) — Discord mention-only mode + thread routing + typing indicators + Telegram streams replies; (5) **Configurable loop detection** (v2.0 #2586/2711/2752); (6) **Subagent token usage** (v2.0 #2882/3611); (7) **Trace polish** (v2.0 #3101/2944/3104); (8) **Token-usage tracking on by default** (v2.0 #2841/2329/3658); (9) **Flexible memory & suggestions** (v2.0 #3465/3591); (10) [more in body]。**Performance (5)**：Push thread metadata filters into SQL (#2865) + Index runs by `thread_id` (#3499) + Index messages in MemoryRunEventStore (#3531) + Cache `Base.to_dict` (#3654) + Speed up `should_ignore_name` in sandbox walks (#3657)。**Security (2)**：Reject symlinked upload destinations on Linux + Windows (#2623/2794) + Mask sensitive info... + more。**對 horo-agent / air-gapped downstream 影響評估**：**MIT permissive + clean archetype**。**Functional parallels to horo-agent**：(a) sub-agents + persistent memory ≡ kanban delegation + cycle memory; (b) custom agents that update themselves (SOUL.md self-edit) ≡ **horo-agent memory-editor use case 完全對齊** — **主人 8260 memory-editor 直接 reference**; (c) user-owned IM channel (Slack/Telegram/Discord/Feishu/WeChat per-user bind) ≡ **horo-agent agent-teamworkflow-task 多 channel 路線**; (d) LangGraph + LangSmith + Langfuse + Monocle 4-tracing 支援 ≡ **horo-agent observability 階段參考**; (e) embedded Python client TUI + Web UI sidebar sync ≡ **horo-agent TUI 對齊**。**Spirit-adjacent** (不是直接 fork，**架構啟發 + feature reference 高度對齊**)。**Repo `pushed_at` 2026-09-03 01:24 UTC（台北時間 9/3 09:24, **今天 master 仍 commit active**）** — `pushed_at` 顯著比 release 新鮮得多（70 天落差），**main branch 仍 active 開發中**（v2.1.0 / v2.2.0 正在醞釀 + 後續 patch）。**Release-cadence**：v1.x (2025-05-07 ~ 2026-02) → v2.0.0 (2026-06-25) = **v1 ~ 10 個月 + v2 launch 後 ~2.3 個月** = **major version cadence ~12 個月 + minor 仍在 main branch 開發**。**Pre-relase cadence `pre-release_stale`** = **70 天 release tag + 0 天 master push today = `pushed_at` 救回 stale release 的極佳 example**。**Repo `created_at` 2025-05-07（**16 個月內從 0 衝到 81,286★**, viral-tail 持續中 — **Trending #1 on 2026-02-28**）+ 11,207 forks + 18 topics + **License = MIT** (case-A verified via LICENSE file) + **Homepage**: `https://deerflow.tech` + **Sister project**: `https://github.com/deer-flow/llm-space`。

### `farion1231/cc-switch`

- **Repo 摘要：** **CC Switch — The All-in-One Manager for Claude Code, Claude Desktop, Codex, Gemini CLI, Grok Build, OpenCode, OpenClaw & Hermes Agent** — **13 個月內從 0 衝到 130,796★** (`created_at` 2025-08-04), Rust + Tauri 2 desktop app + 20 topics (`ai-tools`/`claude-code`/`codex`/`desktop-app`/`grok`/`grokbuild`/`hermes`/`hermes-agent`/`mcp`/`open-source`/`openclaw`/`openclaw-ui`/`opencode`/`pi`/`provider-management`/`rust`/`skills`/`skills-management`/`tauri`/`wsl-support`)，**Cross-platform desktop All-in-One assistant for Claude Code, Codex, OpenCode, OpenClaw, Grok Build & Hermes Agent**。**核心差異化**：**(1) Directly references Hermes Agent** — repo description + topics + README 開頭明示 "All-in-One Manager for Claude Code, Claude Desktop, Codex, Gemini CLI, Grok Build, OpenCode, OpenClaw & **Hermes Agent**" — **top 5 search 命中主人 MEMORY "Hermes-Agent" 直接 hit**。**(2) Multi-platform desktop** — Windows + macOS + Linux (Ubuntu 22.04+ / Debian 11+ / Fedora 34+) + WSL, **Native desktop app via Tauri 2 (Rust + WebView frontend)**。**(3) Multi-CLI provider management** — Single switch across **7 AI CLI providers** (Claude Code + Claude Desktop + Codex + Gemini CLI + Grok Build + OpenCode + OpenClaw) + Hermes Agent。**(4) Install methods (multi-format)** — Windows MSI/Portable ZIP + macOS Homebrew Cask (`brew install --cask cc-switch`) + macOS DMG/ZIP + Arch Linux AUR (`paru -S cc-switch-bin`) + Linux Debian (.deb) + RPM (.rpm) + AppImage (universal)。**(5) v3.20.1 release (6 天前 fresh) — Codex 0.149 adaptation**：(a) **Codex CLI ≥ 0.149 第三方供應商切換 config-only** (no auth.json fallback) — 0.149 拒絕舊切換方式 (401 Missing API key); (b) **同一 Team workspace 多 ChatGPT 帳號共存** — 防止 account 互相覆蓋; (c) **保存成功靠譜** — 接管備份行不再讓活躍供應商只 update DB 而 config 不變; (d) **Codex 編輯框不再串染別張卡的密鑰**; (e) **WebDAV/S3 還原備份不再清空手寫的 prompt 文件** (`CLAUDE.md`/`AGENTS.md`/`GEMINI.md`/`SOUL.md`); (f) **用量頁新增「自動掃描會話記錄」開關** (秒級降到毫秒級); (g) **OpenCode Go 訂閱額度可見**; (h) **macOS 終端預設 Otty**; (i) **包含數據庫遷移 v17 → v18, 升級前自動備份**。**(6) Architecture** — React + TypeScript frontend + TanStack Query (cache/sync) + Rust backend (Tauri 2)。**(7) Provider management** — Add provider from preset list or custom config + Switch via main UI / system tray / per-CLI command + Backup & restore (WebDAV/S3/Local)。**(8) MCP / Prompts / Skills management** — Per-CLI MCP servers + custom prompts + Skills library。**(9) Multi-language docs** — English + 中文 + 日本語 + Deutsch + Changelog + User Manual (provider management, MCP/Prompts/Skills, proxy & failover)。**(10) Sponsors** — Kimi K3 + PackyCode + ZetaAPI + APINebula + AICodeMirror + PatewayAI + AICoding + SubRouter + APIKEY.FUN + 9527CODE + Shengsuanyun + AIGoCode + aicoding.inc — **multi-API relay partners**。

- **3W1H：**
  - **What：** Tauri 2 (Rust + WebView) 寫的 **cross-platform desktop All-in-One manager** for 7 AI CLI tools (Claude Code / Claude Desktop / Codex / Gemini CLI / Grok Build / OpenCode / OpenClaw) + Hermes Agent。Provider management + MCP/Prompts/Skills + proxy + failover + backup/restore (WebDAV/S3/Local)。System tray + main UI + 多 OS native installer。
  - **Why：** 解決「同時裝多個 AI CLI (Claude Code + Codex + Gemini CLI + OpenCode + OpenClaw + Hermes Agent) 但要一鍵切換供應商 / 想要備份 WebDAV/S3 + 多 OS 安裝 (.msi/.dmg/.deb/.rpm/.AppImage) / 想要 system tray + 自動切換 / 想要 Codex 0.149+ 安全切換 / 想要 MCP/Prompts/Skills per-CLI 管理 / 想要 Hermes Agent 整合」痛點。CC Switch 把「multi-CLI provider management」降到 1 個 native desktop app。
  - **Who：** 同時裝 Claude Code + Codex + Gemini CLI + OpenCode + OpenClaw + Hermes Agent 的 AI power user（**主人 horo-agent 多 CLI profile 對齊**）；要 WebDAV/S3 備份 config 的 sync user；用 Codex 0.149+ 的人（**v3.20.1 是首個支援 Codex 0.149 第三方切換的 version**）；想要 Tauri 2 native app 而非 Electron 的 developer；想要 system tray 一鍵切換的 efficiency user；對 multi-provider failover 有需求的 enterprise user。
  - **How：** (a) **Basic Usage**：Add Provider (preset or custom) → Switch Provider (main UI / system tray / per-CLI command) → Backup/Restore (WebDAV/S3/Local) → MCP/Prompts/Skills per-CLI mgmt; (b) **Windows**：Download `CC-Switch-v{version}-Windows.msi` 或 `-Windows-Portable.zip` from [Releases](../../releases); (c) **macOS (主推 Homebrew Cask)**：`brew install --cask cc-switch` (或 download `.dmg`); (d) **Arch Linux**：`paru -S cc-switch-bin`; (e) **Linux**：Download `.deb` (Debian/Ubuntu) + `.rpm` (Fedora/RHEL/openSUSE) + `.AppImage` (Universal); (f) **Tauri architecture** — React + TS frontend + TanStack Query + Rust backend; (g) **Documentation**：[User Manual](docs/user-manual/en/README.md) — provider management + MCP/Prompts/Skills + proxy & failover。

- **安裝方式：**
  - **macOS Homebrew Cask (主推)**：
    ```bash
    brew install --cask cc-switch
    # 更新
    brew upgrade --cask cc-switch
    ```
  - **macOS Manual**：
    - Download `CC-Switch-v{version}-macOS.dmg` (recommended) 或 `.zip` from [Releases](../../releases)
    - **CC Switch for macOS is code-signed and notarized by Apple** — install and open directly
  - **Windows**：
    - Download latest `CC-Switch-v{version}-Windows.msi` installer 或 `CC-Switch-v{version}-Windows-Portable.zip` portable version from [Releases](../../releases) page
  - **Arch Linux**：
    ```bash
    paru -S cc-switch-bin
    ```
  - **Linux**：
    - `CC-Switch-v{version}-Linux.deb` (Debian/Ubuntu)
    - `CC-Switch-v{version}-Linux.rpm` (Fedora/RHEL/openSUSE)
    - `CC-Switch-v{version}-Linux.AppImage` (Universal)
  - **System requirements**：
    - **Windows**: Windows 10 and above
    - **macOS**: macOS 12 (Monterey) and above
    - **Linux**: Ubuntu 22.04+ / Debian 11+ / Fedora 34+ and other mainstream distributions
    - **Wayland note** (Linux): If launch from desktop icon on tiling Wayland compositors (sway/Hyprland) where clicks don't register, add to `.desktop` `Exec=` line: `env CC_SWITCH_GDK_BACKEND=wayland /path/to/AppImage` or set `CC_SWITCH_GDK_BACKEND=x11` instead
  - **未找到 PyPI / npm package**：純 desktop app (Tauri 2)，**non-Python/non-Node ecosystem**，**no `pip install` / `npm install`**。
  - **Flatpak**：Not included in official releases. Build from `.deb` via [`flatpak/README.md`](flatpak/README.md)。
  - **Documentation**：[User Manual](docs/user-manual/en/README.md) — provider management + MCP/Prompts/Skills + proxy & failover + per-CLI mgmt。
  - **Official Website**：[ccswitch.io](https://ccswitch.io) (English | 中文 | 日本語 | Deutsch)。

- **近期 release：** `v3.20.1 — CC Switch v3.20.1` — **2026-08-28 00:43 UTC 發佈（台北時間 8/28 08:43, **6 天前 fresh**）**, pre-release = false, draft = false。Body 主標題 `## 重点内容：你现在可以` + 三語 release notes (中文主版 + English + 日本語)。**5 major fixes + 3 enhancements**：**(1) Codex CLI ≥ 0.149 第三方供應商切換 config-only** (#6744) — 0.149 拒絕舊切換方式 (401 Missing API key)，切換整體改為 config-only，密鑰寫進供應商 `[model_providers.*]` 表 (`experimental_bearer_token`, Codex 0.48 起支援)，`auth.json` 回歸純粹官方 ChatGPT 登录文件；**(2) 同一 Team workspace 多 ChatGPT 帳號安全共存** (#6780, 修復 #2245) — 此前帳號以 workspace ID 為主鍵，同一 Team 兩名成員會合併成一條記錄、後登錄者靜默覆蓋前者的令牌；**(3) 「保存成功」靠譜** (#6779) — 崩潰殘留的接管備份行曾讓活躍供應商的編輯只 update DB、真正的 config 紋絲不動；**(4) Codex 編輯框顯示自己的密鑰** (#6534, 修復 #6414) — 共享的 `auth.json` 沒有供應商身份，編輯活躍 Codex 供應商可能顯示——保存後固化——另一張卡遺留的 key；**(5) 放心恢復備份** (#6810, 修復 #6778) — 雲端快照沒有任何已啟用 prompt 時，WebDAV/S3 下載或備份導入不再把手寫的 `CLAUDE.md`/`AGENTS.md`/`GEMINI.md`/`SOUL.md` 清成空文件；**(6) 用量頁新增「自動掃描會話記錄」開關** (秒級降到毫秒級)；**(7) OpenCode Go 訂閱額度可見** — Token Plan 查詢識別 OpenCode Go, 5 小時/週/月三窗口的用量百分比與重置時間進入用量卡與托盤；**(8) macOS Otty 終端預設** (#6620) — 會話恢復、供應商終端與工具命令三處入口都可選 Otty；**(9) 數據庫遷移 v17 → v18, 升級前自動備份**。**對 horo-agent / air-gapped downstream 影響評估**：**MIT permissive + directly references Hermes Agent**。**Functional parallels to horo-agent**：(a) **CC Switch explicitly supports Hermes Agent** — repo description + topics + README 都明示 — **主人「horo-agent Hermes-Agent 整合」最高 priority reference**; (b) multi-CLI provider management ≡ **horo-agent 多 profile 編排** (`default` + executor + reviewer); (c) provider backup/restore (WebDAV/S3) ≡ **horo-agent Kanban persistent state 對齊**; (d) MCP / Prompts / Skills per-CLI mgmt ≡ **horo-agent memory-editor + skills 對齊**; (e) Codex 0.149+ adaptation ≡ **horo-agent 跟著 CLI vendor version bump 的能力**; (f) Tauri 2 native desktop app (vs Electron) ≡ **horo-webui 輕量化參考**。**Spirit-adjacent + directly-aligned** (description 直接 reference Hermes Agent, **不是 spirit-only, 是 explicit Hermes Agent mention**)。**對主人 MEMORY「主人常用 Hermes Agent 與 hermes-webui 做 enterprise-lite / air-gapped downstream」高度命中**。**Repo `pushed_at` 2026-09-02 04:57 UTC（台北時間 9/2 12:57, **1 天前 master 仍 commit active**）** — `pushed_at` 顯著比 release 新鮮（6 天落差），**main branch 仍 active 開發中**（v3.21.0 / v3.22.0 正在醞釀）。**Release-cadence**：6 sponsor partner APIs + 1 個月內 ~3-5 minor version (從 README 推估, 沒看完整 release list) = **rapid release cadence**。**Pre-relase cadence `pre-release_fresh`** = **6 天 release tag + 1 天 master push = 雙 fresh**。**Repo `created_at` 2025-08-04（**13 個月內從 0 衝到 130,796★**, viral-tail 持續中）+ 8,992 forks + 20 topics 含 `hermes-agent` + `openclaw` + `openclaw-ui` + `claude-code` + `codex` + `opencode` + `provider-management` + `skills-management` + `wsl-support` + `tauri` + `mcp` + `desktop-app` + **License = MIT** (case-A verified via LICENSE file) + **Homepage**: `https://ccswitch.io` + **Trendshift badge**: #15372 + **Star History**: star-history.com badge。

## 重點觀察

- **Tier-A top 3 = 3/3 全 FRESH (cross-day 0/3 repeat floor ×6 VERIFIED)**：今天 Tier-A 排除 9 個 repeat (`Gitlawb/openclaude` 9/2 + `Imbad0202/academic-research-skills` 9/2 + `THU-MAIC/OpenMAIC` 8/31+9/1+9/2 floor 3 天 + `iv-org/invidious` 9/2 + `jingyaogong/minimind` 9/1+9/2 + `3b1b/manim` 9/2 + `K-Dense-AI/scientific-agent-skills` 8/27/29/30/31+9/1+9/2 floor 6 天 + `handsomestWei/patent-disclosure-skill` 9/1 + `affaan-m/ECC` 9/1) 後，**3 個 pick 都是 fresh today**：`firecrawl/pdf-inspector` (pushed_at 9/3 today) + `browser-use/video-use` (pushed_at 8/30, 4 天內) + `unclecode/crawl4ai` (pushed_at 9/1, 2 天內)。**0/3 cross-day repeat floor 已達 ×6（08-20 + 08-21 + 08-31 + 09-01 + 09-02 + 09-03）** — 符合 08-20 codification「0/3 = natural floor = structurally optimal」標準連續 6 天。**Tier-A top 3 與 Web-search 2 (bytedance/deer-flow + farion1231/cc-switch) 都是 MIT permissive + `pushed_at` active** — `pdf-inspector` pushed_at 9/3 today + `video-use` 8/30 + `crawl4ai` 9/1 + `deer-flow` 9/3 today + `cc-switch` 9/2 = **5/5 today active**。**5 picks = Python ×3 (video-use + crawl4ai + deer-flow) + Rust ×1 (cc-switch) + Rust+4-bindings ×1 (pdf-inspector Rust core) = 5 picks 全 Python 或 Rust 生態**, **比 09-02「Python ×4 + TypeScript ×1」語言更集中**, **今天沒有 JavaScript/TypeScript pick** (排除 openclaude)。**注意**：`browser-use/video-use` 是 0 GitHub releases（releases tab empty），靠 `pushed_at` 4 天前 + Trending today #8 star-rate 472 撐 liveness；`firecrawl/pdf-inspector` release v1.15.0 17 天前 stale 但 `pushed_at` 9/3 today active, PyPI v1.17.0 (8/21) 比 GH release tag 更新。**5/5 = 100% `pushed_at` active LATER 或 AT LEAST 4 天內 fresh** 是本月 tier-A freshness **第 6 個 milestone**（08-12/13/14/15/17/20/21 + 08-31 + 09-01 + 09-02 + 09-03）。

- **License tier-split = 4 MIT + 1 Apache-2.0 (5/5 permissive, 100%)**：今天 pick 內 **`firecrawl/pdf-inspector`** = **MIT** (case-A verified via LICENSE file) — no commercial restriction; **`browser-use/video-use`** = **MIT** (inferred from "100% open source" + browser-use 既有 MIT 模式); **`unclecode/crawl4ai`** = **Apache-2.0** (case-A verified via file, 含 patent grant clause 完整) — slightly more protective than MIT 但仍 permissive; **`bytedance/deer-flow`** = **MIT** (case-A verified via LICENSE file) — ByteDance 對 open-source agent framework 採最 permissive license; **`farion1231/cc-switch`** = **MIT** (case-A verified via LICENSE file)。**5/5 = 100% pure permissive** — 比 09-02「60% permissive (3 MIT + 1 NOASSERTION + 1 CC BY-NC)」reverse 40pp, **今天是本月 14:00 系列第 11 個 100% permissive milestone**（08-12/13/14/15/17/20/21 + 08-31 + 09-01 + 09-02 + 09-03 = 11 個 milestone）。**對 horo-agent air-gapped downstream 影響**：**5 picks 可直接 fork 入庫**，**沒有任何 LICENSE 警訊**，**全部對 closed-source commercial fork safe**。

- **5 picks 5 種 install type 全 distinct (install type diversity is the new bar ×6 VERIFIED)**：今天命中：**Type-24 Multi-binding distribution (Rust core + Python + Node.js + WASM + CLI)** (**`firecrawl/pdf-inspector`** `pip install pdf-inspector` + `cargo add pdf-inspector` + `npm install @firecrawl/pdf-inspector` + `npm install @firecrawl/pdf-inspector-wasm` + `cargo install pdf-inspector` CLI — **第 1 次命中 cross-binding install type, 5 條 install paths 全 distinct**)+ **Type-25 Coding-agent skill install via symlink + uv sync + system ffmpeg** (**`browser-use/video-use`** setup prompt 給 coding agent + manual `git clone` + `ln -sfn ~/.claude/skills/video-use` + `uv sync` + `brew install ffmpeg` — **第 1 次命中「agent-driven install」+「skill symlink + system deps」type**)+ **Type-1 PyPI one-liner + Docker pull** (**`unclecode/crawl4ai`** `pip install crawl4ai==0.9.3` + `docker pull unclecode/crawl4ai:0.9.3` — **PyPI + Docker 雙 install path, 第 2 次命中** dual-mode install pattern after 09-02 OpenHarness)+ **Type-26 Embedded Python client from source + Docker Compose v2** (**`bytedance/deer-flow`** `uv pip install 'deerflow-harness[tui]'` from source (NOT PyPI wheel) + `make docker-init` + `make docker-start` Docker Compose — **第 1 次命中「embedded Python client from source」+「make-driven Docker Compose」type**)+ **Type-27 Native desktop app via Tauri 2 + multi-OS installer (brew cask + msi + dmg + deb + rpm + AppImage + AUR)** (**`farion1231/cc-switch`** `brew install --cask cc-switch` + `-Windows.msi` + `-Windows-Portable.zip` + `-macOS.dmg` + `paru -S cc-switch-bin` (Arch) + `-Linux.deb` + `-Linux.rpm` + `-Linux.AppImage` — **第 1 次命中「native desktop Tauri 2 + 7 OS installer formats」type, 最多元 installer type**)）。**5 picks = 5 distinct install paths** 達 **install type diversity ×6 VERIFIED（08-20 + 08-21 + 08-31 + 09-01 + 09-02 + 09-03）**。**新增 install types**: **Multi-binding (Rust + Python + Node + WASM)**, **coding-agent-driven install + skill symlink + system ffmpeg**, **embedded Python client from source + make-driven Docker Compose**, **native desktop Tauri 2 + 7 OS installer formats** — 都是過去 6 天未觀察到的新 pattern。**Install diversity evolution**：(Day 1 = PyPI + System deps + npm + Claude plugin + dual-mode curl/pip) → (Day 6 = Multi-binding + agent skill + embedded client + Tauri desktop + PyPI+Docker)。

- **Release 新鮮度 tier-split (5 picks)**：超 fresh (≤3 天) = **2** (`crawl4ai` v0.9.3 3 天前 + `cc-switch` v3.20.1 6 天前邊界) + fresh (≤14 天) = **1** (`pdf-inspector` v1.15.0 17 天前邊界) + mid-fresh (≤30 天) = **1** (`video-use` 4 天前 `pushed_at` but 0 GitHub release) + stale (>30 天 but `pushed_at` fresh) = **1** (`deer-flow` v2.0.0 70 天前 stale but `pushed_at` 9/3 today active)。**5/5 = 100% `pushed_at` ≤ 4 天內 fresh** 是本月 tier-A freshness **最強 milestone**。**2/5 = 40% fresh by release tag** (v0.9.3 + v3.20.1 ≤14 天) + 1/5 viral-tail (`video-use` no release) + 1/5 mid-fresh (`pdf-inspector` 17 天前) + 1/5 stale-release-active-push (`deer-flow` 70 天 vs 0 天 push)。**Operational reading**：今天 5 picks 中 **2 個 stale release (`pdf-inspector` 17 天 + `deer-flow` 70 天) 都是 `pushed_at` 救回的 stale release** — 證明 `pushed_at` 是比 release tag 更好的 liveness signal。**`browser-use/video-use` 0 GitHub releases + 4 天 `pushed_at`** 是 early-stage viral project 模式（跟 `nanobot` 早期類似 — **commit-driven visibility model**）。

- **Master MEMORY signal 命中 (3/5 picks = high-impact MEMORY-adjacent)**：今天 **3/5 picks fire Hermes-as-host-adjacent signal, 1/5 picks fire「視覺化 + 邊做邊看」signal, 1/5 picks fire「PDF/Markdown pipeline + RAG」signal**：(a) **`farion1231/cc-switch`** topic 包含 **`hermes`** + **`hermes-agent`** + **`openclaw`** + **`openclaw-ui`** + **`claude-code`** + **`codex`** + **`opencode`** + **`provider-management`** + **`skills-management`** + **`wsl-support`** + **`tauri`** + **`mcp`** + **`desktop-app`** — **直接 reference Hermes Agent 在 description 與 topics 與 README 內**, **Tauri 2 native desktop app** + 7 OS installer formats + Codex 0.149+ adaptation + MCP/Prompts/Skills per-CLI mgmt — **對主人「horo-agent Hermes-Agent 整合」+「enterprise-lite / air-gapped downstream」+「hermes-webui 輕量化」+「default profile Qwen + executor + reviewer 多 profile」+「Kanban persistent state 對齊」是直接 reference in production**; (b) **`browser-use/video-use`** mention Hermes in setup prompt: "Paste into Claude Code, Codex, Hermes, Openclaw, or any agent with shell access" — **直接 hit Hermes-Agent integration**, video editing via coding agent 跟主人「horo-agent 自動化 creative workflow」對齊 + 「視覺優先 + 邊做邊看」哲學對齊 (video = visual-first content); (c) **`unclecode/crawl4ai`** 提供 `result.markdown` LLM-ready output + **主人「公開情報蒐集」偏好**直接對齊 — **horo-agent pipeline 中「web → Markdown → LLM context」階段的 drop-in library**; (d) **`firecrawl/pdf-inspector`** 提供 Rust core + Python/Node/WASM bindings + 4 路 binding — **主人「工具組合」偏好 + 「local-first + air-gapped」偏好**對齊 — PDF parser 是 RAG pipeline 預處理核心; (e) **`bytedance/deer-flow`** sub-agents + persistent memory + custom agents that update themselves (SOUL.md self-edit) + LangGraph + LangSmith/Langfuse/Monocle 4-tracing + TUI + Web UI sidebar sync — **對齊主人「Kanban delegation + kanban_cycle_memory + memory-editor use case + multi-channel IM」**。**5/5 picks 全部高度對齊主人 MEMORY 偏好** — **本月 tier-A freshness **highest-MEMORY-adjacent-pick milestone** (08-21 was 3/5 + 09-02 was 3/5 + 09-03 is 5/5)**。**Master MEMORY 顯著對齊**：(i) **`cc-switch` 是 horo-agent 多 CLI profile 編排的原生 desktop reference** — Hermes-Agent 在 topics, Codex 0.149 適配, multi-OS installer — **對主人 horo-webui 整合最直接**; (ii) **`deer-flow` 是 horo-agent super agent harness 路線的 reference** — custom agents that self-edit SOUL.md ≡ `memory-editor` use case 直接命中; (iii) **`video-use` 是 horo-agent coding-agent-as-creative-tool 的 reference** — mention Hermes + agent-driven video editing; (iv) **`crawl4ai` + `pdf-inspector` 是 horo-agent pipeline 中「web → Markdown → LLM context」+「PDF → Markdown」階段的 drop-in libraries** — 對主人「公開情報蒐集」+「工具組合」+「local-first」三個偏好同時命中。**4-tick rolling avg ≈ 3/5** (08-30 = 2/5, 08-31 = 3/5, 09-01 = 4/5, 09-02 = 3/5 + 09-03 = 5/5, **4-tick avg = 3.75/5**), **09-03 是本月 tier-A freshness 上半段最強 MEMORY-adjacent pick day**。