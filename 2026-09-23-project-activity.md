---
title: GitHub 自由探索 2026-09-23（14:00 台北時間）
date: 2026-09-23
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 7 天 repeat）
  - web_search 當日 AI 開源新聞 + Trendshift live today + GitHub Trending HTML positions 4-12（Tier-B web 探索 2）
  - GitHub REST API `/repos/<slug>` + `/releases` + raw `README.md`
---

# GitHub 專案動態

- 檢查時間：2026-09-23（14:00 台北時間，週三）
- 檢查對象：`agent-substrate/substrate` / `dream-num/univer` / `davila7/claude-code-templates` / `browser-use/jev-ultrafast` / `tamaratran/fast-jev-compaction`
- 來源組合：GitHub Trending today today 解析後 Tier-A 8 個 row（週三 only-8-trending-day 推定週末變體的 SSR short list）排除前 7 天 repeat（`anthropics/financial-services` #1 9-22 pick 跳過、所以實際 Trending top 3 = **`agent-substrate/substrate` 3,096★ today #2 = 245 stars today** + **`dream-num/univer` 15,747★ today #3 = 255 stars today** + **`davila7/claude-code-templates` 31,212★ today #4 = 64 stars today**）。Tier-B web_search 命中兩個 strong leads：(a) **`browser-use/jev-ultrafast`**（browser-use.org 同源新 launch、9-16 開源 only 7 天內、Python + TypeScript + Chrome、`pushed_at` 2026-09-18 16:28 UTC = 5 天前、MIT、18,372★、description「**Fastest and cheapest web agent**」、Zürich → London Google Flights 7.073 秒 vs 上一版 9.450s = 25% 削減、median browser protocol calls 1,092 → 101 = **10.8× protocol 削減**、`uv sync` install、`TYPESAFE_API_KEY` + `TEXT_MODEL_API_KEY` OpenRouter key、demo runs on `inception/mercury-2.5`、原生 DOM reader 不靠 screenshot in default agent loop = **agent loop optimization 對位主人 token-cost 對齊**、**GH release `not found` = < 7 天 fresh launch 無 SemVer 紀律**）+ (b) **`tamaratran/fast-jev-compaction`**（TypeScript + Claude Code plugin、9-17 開源 only 6 天內、6,384★、MIT、description「**Claude Code plugin that replaces the compaction summary with Jev decisions: every tool call and result is scored in one fast request, stale ones are dropped or truncated, everything kept stays verbatim**」、`preserveRecentMessages` defaults 6、`maxStateTokens` 25k、`maxRequestTokens` 30k、never rewrites text、Jev failures throw、`claude plugin marketplace add tamaratran/fast-jev-compaction` + `claude plugin install fast-jev-compaction@fast-jev-compaction` install 路徑、需 Claude Code 2.1.274+ opt-in `CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1`、**GH release `not found` = < 7 天 fresh launch**、**「never rewrites」是這顆 plugin 跟現有 compaction summary 的對位：保存 verbatim 不會丟 file path / exact error / constraint**）。

---

## Repo 摘要與 3W1H

### `agent-substrate/substrate`

- **Repo 摘要：** 「**NOTE: This is not an officially supported Google product.**」「**Agent Substrate is a secure-by-default agent execution runtime engineered to run millions of sandboxes with 10× higher density than standard container runtimes. Purpose-built for the era of autonomous agents, Substrate delivers sub-500ms resume operations at over 500 suspend/resume activations per second with native zero-trust kernel and network isolation.**」——Google ATE team、2026-05-13 開源（**4 個月成熟**）、3,096★、Go（README badge）、**Apache-2.0**、topics 空陣列（無任何 topic）、`created` 2026-05-13、`pushed_at` 2026-09-22 23:56 UTC（**昨天仍 push**）、`updated_at` 2026-09-23 06:00 UTC（今天仍有 metadata 更新 = issues / PRs 仍在處理）、`open_issues_count` 524、size 36.8 MB（**Go monorepo**）、`default_branch` main。**Latest GH release `v0.1.0` published 2026-09-10 23:39 UTC（13 天前）** = "**v0.1.0 - The First Release**" = first ever formal SemVer bump + **`created_at` 2026-09-09 22:04 UTC = 14 天前** = release 跟 `created_at` 隔 25 小時 = 剛定稿就 publish release（**GA discipline 起步**）。**核心定位**：「**a secure-by-default agent execution runtime engineered to run millions of sandboxes with 10× higher density than standard container runtimes**」+ 多 sandbox 技術（microVMs + gVisor）+ 大 actor set 對小 ready worker set multiplexing + Kubernetes 底層（**Agent Substrate leverages Kubernetes for the infrastructure provisioning and worker lifecycle management**）= **Kubernetes 上層 agent 專用 runtime**，**對位 Kubernetes Pod + Pod autoscaling + 自己加 agent-specific scheduling & control**。**四大 framework-agnostic compatibility**：(1) **ADK（Agent Development Kit）**：session state preservation across invocations、(2) **LangChain**、(3) **Claude Code, CodeX, Antigravity**、state-preserving coding environments 跨 session、(4) **MCP** server as Substrate Actors = **durable tools for any model**。**Ecosystem 兩個最重要 reference**：(a) **`google/ax`**（Agent Executor = hyper-scalable agent harness on Agent Substrate, [announcement blog](https://cloud.google.com/blog/products/ai-machine-learning/agent-executor-googles-distributed-agent-runtime)）、(b) **`kagent-dev/kagent`**（CNCF Sandbox project, [announcement blog](https://kagent.dev/blog/the-future-of-kagent)）。**核心 demo 數字**：「**~250 stateful actors across just 8 physical pods**」+ **「30×+ oversubscription by "juggling" a large registry of stateful actors onto a small pool of shared physical pods」** = **agent multiplexing 是 Agent Substrate 核心**。**Status**：「**early development. It is not ready for production use, and the APIs are almost guaranteed to change. We are not making any guarantees about backward compatibility at this stage, and everything in this project may be changed.**」 + 對 weekly community meeting 10-11am PST。
- **3W1H：**
  - **What：** 一個 Go / Kubernetes 上層的 **agent-專用 execution runtime** — 不是 SDK、不是 framework、是「**system for running agents at scale**」、用 microVMs 或 gVisor 跑 OCI containers、native zero-trust kernel + network isolation、sub-500ms resume / 500+ activations/sec、無 site-specific action scripts、無 model output becomes selectors or shell commands。
  - **Why：** 現有 AI agent runtime 三大對位：(1) **Container runtime 太貴** — 標準 runtime 一個 agent 一個 container、**10× density** 是 Agent Substrate 解這個、(2) **Agent 95% 閒置** — actor/worker 多對一 mapping，**30× oversubscription** 解決資源浪費、(3) **沒人專為 agent 工作特性設計 scheduler** — RL scenarios 跨 agentic / inference / training cycles、Kubernetes 一般化抽象不夠特化。**Agent Substrate 解三對位**：(a) **agent multiplexing** — large actor set ↔ small worker pool，多對一 mapping、依 agent idle 特性 multiplexing、(b) **state persistence** — actor working memory + filesystem state 跨 hibernation cycles 完整保留（full-state snapshots）、(c) **agent-specific scheduling & control** — 在 Kubernetes 上蓋 agent-aware scheduler 比 vanilla Kubernetes 快。
  - **Who：** **Enterprise platform engineer** 想 scale AI agent deployment 到 1000+ sandboxes 同時跑（10× container density = 省 infra cost）；**Agent framework author**（Claude Code / Codex / Antigravity / ADK / LangChain）想 ship framework 出去的時候 runtime 也跟著 ship；**CNCF community** 想用 kagent 為 Kubernetes-native agent；**RL training infra team** 想跨 agentic / inference / training cycles 做 holistic infra optimization。
  - **How：** **(1) Development setup：** Go + kubectl + Docker + kind（自動被 Go 抓）+ `./hack/create-kind-cluster.sh` + `./hack/install-ate-kind.sh --deploy-ate-system` + `go install ./cmd/kubectl-ate` + `kubectl ate create actor my-counter-1 -a ate-demo-counter --template counter`。**(2) Run counter demo：** `kubectl port-forward -n ate-system svc/atenet-router 8000:80` + `curl -X POST -H "ate-target-actor: ate-demo-counter/my-counter-1" -i http://localhost:8000/`。**(3) 跑 framework：** ADK / LangChain / Claude Code / CodeX / Antigravity / MCP server 都吃 → Substrate Actors 形式 deploy。**(4) 深入 ecosystem：** `google/ax`（Agent Executor = hyper-scalable agent harness on Agent Substrate）+ `kagent-dev/kagent`（CNCF Sandbox project）。
- **安裝方式：**
  - **Dev quickstart（最快路徑）：** **`git clone https://github.com/agent-substrate/substrate`** → Go + kubectl + Docker + kind → **`./hack/create-kind-cluster.sh`** → **`./hack/install-ate-kind.sh --deploy-ate-system`** → **`./hack/install-ate-kind.sh --deploy-demo-counter`** → **`go install ./cmd/kubectl-ate`** → **`kubectl ate create actor my-counter-1 -a ate-demo-counter --template counter`** → **`kubectl port-forward -n ate-system svc/atenet-router 8000:80`** → `curl -X POST -H "ate-target-actor: ate-demo-counter/my-counter-1" -i http://localhost:8000/`。
  - **Go：** repo root 有 `go.mod` + `go install ./cmd/kubectl-ate` 安裝 CLI；主 runtime 通常不需要再 install（從 source build）、但 production deployment 跟從 OCI image pull 推 Kubernetes cluster。
  - **Shell scripts 走 hack/：** `hack/create-kind-cluster.sh` + `hack/install-ate-kind.sh`（`--deploy-ate-system` + `--deploy-demo-counter` flag）。Counter demo walkthrough 在 `demos/counter/README.md`。
  - **kubectl plugin：** `go install ./cmd/kubectl-ate` → `kubectl ate create actor / list / 等`。
  - **pip / uv / poetry：** **不適用**（這是 Go + Kubernetes runtime、無 Python binding、無 PyPI publish）。
  - **npm / npx：** **不適用**。
  - **Source clone：** `git clone https://github.com/agent-substrate/substrate`（36.8 MB monorepo + Go modules + hack scripts）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1、可走 **`git clone`** + Go + Docker（如果有 Docker Desktop for Mac）+ kind（auto-installed via Go）= **dev 環境可裝，但完整起 cluster 需 ~8 GB RAM 給 Kubernetes，主人 16 GB 物理有風險；建議 dev 用 kind（小 cluster）、production 等生態系成熟**。HORO-Agent 對位：可直接抄 Agent Substrate 的 actor/worker 多對一 mapping + stateful snapshot design 做 horo-agent sandbox pool。
- **近期 release：** **GH release `v0.1.0` published 2026-09-10 23:39 UTC（13 天前）** = "**v0.1.0 - The First Release**"、first-ever formal SemVer bump、`created_at` 2026-09-09 22:04 UTC = **draft 到 publish 隔 25 小時 = 剛定稿就 ship = GA discipline 起步信號**。`pushed_at` 2026-09-22 23:56 UTC = **昨天仍 active push**、**`v0.1.0` 是 first formal release 不代表 only release — 後續 patch / minor 應該會在 1-2 個月內 ship**。**Status 明確寫**：「**early development. It is not ready for production use, and the APIs are almost guaranteed to change**」= **v0.1.0 是 "reference architecture 公開" 而非 "ready for production"**、對位主人 horo-agent downstream 不能 ship 進 production runtime、可讀 source / 借 agent multiplexing pattern / 等 1.0 才考慮 embed。

### `dream-num/univer`

- **Repo 摘要：** 「**The Office Harness for AI Agents — Spreadsheets, Docs, Slides, Canvas, Relational Tables, and PDF in one runtime.**」「**Univer is an open-source SDK for creating office applications inside your own product. It gives you the building blocks for spreadsheet, document, and presentation experiences without forcing you into a hosted app or a fixed UI.**」——Dream Num Tech（Shanghai, 中國主導的 office framework 創業、since 2022）、2022-09-29 開源、**4 年累積 15,747★**、TypeScript、**Apache-2.0**、topics 17 個（`board / collaboration / data-table / doc / docx / excel / grid / pdf / ppt / pptx / presentation / sdk / sheet / slides / spreadsheet / table / word / wordprocessor / xlsx`）、`created` 2022-09-29（**4 年 mature**）、`pushed_at` 2026-09-22 07:17 UTC（**昨天仍 push**）、`updated_at` 2026-09-23 06:00 UTC、`open_issues_count` 138、size 169.7 MB（**TS monorepo + Canvas rendering engine + formula engine + 多語系 i18n**）、homepage `https://docs.univer.ai`、`default_branch` dev（**不是 main、是 dev** = custom branch 名 convention）。**Latest GH release `v0.25.2` published 2026-09-17 04:37 UTC（6 天前）** = "🐢 Release v0.25.2" = turtle emoji tag = **4 年老牌仍 6 天一版 minor cadence**。**GH release cadence**：v0.25.2 (9-17) → 推測 v0.25.1 / 0.25.0 / 0.24.x（**4 年 release history 在 GH releases page**）= **~6-14 天一版 minor cadence**。**6 大 family product 圍繞核心**：(1) **Univer Workspace**（Office workspace for people and AI agents, shared editing + review of agent changes）、(2) **Univer Office for DeepSeek Harness**（connected content + validation + isolated worktrees for agent collaboration）、(3) **Univer CLI**（local command-line workspace for agents to create / edit / inspect / deliver Office content）、(4) **Univer Office for WorkBuddy**（local Office integration for WorkBuddy + MCP previews + draft review, development preview）、(5) **Univer Office for OpenClaw**（Tools for creating, reviewing, and delivering Office content in OpenClaw）、(6) **Univer SDK 本身**。**6 大 pillars**：「**Built for large surfaces**（Canvas-based rendering + dedicated formula engine）**Plugin-shaped by default**（Compose / replace / lazy-load / extend）**Headless for AI infrastructure**（Run workbook + document logic in Node.js）**Product-ready SDK**（Framework adapters + Facade APIs + presets + headless runtime）**Dark-mode ready**（UI components + rendering engine 都 adaptive）**Unified Facade API**（One consistent API for workbooks / ranges / formulas / documents 跨 browser + Node.js）」。**4 files 為核心 architecture**：Univer 開發者可以從 `presets/` 走 curated plugin collections / 從 `packages/` 走 custom composition。**Office Workflows for AI Agents**：「**Programmatic editing**（agents inspect + modify Office content via structured APIs）**Output verification**（agents check results through content inspection + rendered screenshots + layout diagnostics）**+ 跟 human review 互動**」。
- **3W1H：**
  - **What：** 一個 TypeScript + Node.js + Canvas 的 Office SDK（spreadsheet + document + presentation + 4-year mature），**isomorphic by design** = browser + Node.js 同一份 source 跑 UI 或 headless、**plugin-first architecture** = 每個 capability 都 plugin 形式可獨立 add / remove / replace / lazy-load、**facade API** = 一個 consistent API surface for workbooks / ranges / formulas / documents。
  - **Why：** 現有 Office productivity framework 三大對位：(1) **SaaS-first (Google Sheets / Microsoft 365)** — 你必須進 hosted app、Univer 是 SDK 讓你 embed 進自家產品、(2) **Document-specific (each doc type 自己的 SDK)** — Univer 把 spreadsheet + doc + presentation 統一 runtime、(3) **AI 整合需要 headless** — Office headless processing = 跑 server-side、Univer 同一份 source 跑 headless 給 agent 直接用。**Univer 解三對位**：(a) **Embeddable** — 進 SaaS / internal tool / BI workflow / AI 應用都行、(b) **Isomorphic runtime** — node-side workbook / document processing 直接給 agent 或 server workflow 用、(c) **Plugin architecture** — 可以 ship 50% Univer 也可以 ship 100% Univer、例如只塞 spreadsheet engine 不塞 doc engine 給自家的 BI tool。
  - **Who：** **SaaS founder** 想 embed spreadsheet / document editing 進自己產品的；**Internal tool developer** 在 company 內做 productivity workflow；**AI agent framework author**（DeepSeek / OpenClaw / WorkBuddy）想在 agent harness 加 Office editing；**BI workflow developer** 想加 spreadsheet 處理 → chart → export pipeline。
  - **How：** **(1) Install：** `pnpm add @univerjs/preset-sheets`（curated preset）或 `pnpm add @univerjs/core @univerjs/sheets @univerjs/...`（plugin 形式自選）。**(2) Browser UI：** import preset → 初始化 Facade API → mount 到 DOM。**(3) Headless Node.js：** import core → 用 Facade API 操作 workbook → 不啟動 UI → 寫 server-side workflow。**(4) Custom plugin：** 從 `@univerjs/core` 開始 → 註冊 plugin → 命令 / services / UI components / Facade API 都可加。**(5) AI agent 整合：** 走 family product (Workspace / DeepSeek Harness / CLI / OpenClaw etc.)、每個 family product 自己 setup + SDK licensing requirements。
- **安裝方式：**
  - **Preset（最快路徑，官方推薦）：** **`pnpm add @univerjs/preset-sheets`** — curated plugin collection、快速 ship、follow preset default。類似 preset 有 `@univerjs/preset-sheets-core` / `@univerjs/preset-sheets-advanced` 等。
  - **Plugin 自選：** **`pnpm add @univerjs/core`** + 個別 plugin（`@univerjs/sheets` / `@univerjs/docs` / `@univerjs/sheets-ui` / `@univerjs/engine-formula` / `@univerjs/engine-render` 等）。要 trim bundle size 時走這條。
  - **Node.js headless：** 同上 import、但不打 UI → server-side workbook processing。
  - **Framework adapters：** 已 ship React / Vue / Web Components framework-specific application shell integration（具體 adapter 看 `@univerjs/` namespace）。
  - **npm registry 確認：** 從 README `[Release](https://img.shields.io/github/v/release/dream-num/univer)` → 跟 GH release `v0.25.2` 對齊（推測 npm latest = `0.25.2` 但需 `npm view @univerjs/preset-sheets version` 確認）。
  - **pnpm / yarn / npm：** 都支援 (`pnpm add` 為 primary、npm + yarn 通用)。
  - **pip / uv / poetry：** **不適用**（這是 TypeScript + Node.js SDK、無 Python binding）。
  - **Source clone：** `git clone https://github.com/dream-num/univer -b dev`（注意 default branch = dev）+ 看 `packages/` + `presets/` 自走 preset 或 plugin composition。Size 169.7 MB = full monorepo + 多語系 translations + tests。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1、**TypeScript + Node.js + Canvas 是主人日常 stack、可直接 ship 預先 demo**：HERO、HERO-Agent 對位極佳 — 若主人想給 HORO-Agent 做 Office editing capability、可直接 embed Univer preset-sheets + 配 family product 走 OpenClaw / Hermes 整合。**注意 169.7 MB monorepo clone 需 ~5-10 分鐘**。
- **近期 release：** **GH release `v0.25.2` published 2026-09-17 04:37 UTC（6 天前）** = "🐢 Release v0.25.2"、default branch 是 `dev`（custom name convention）、**GH release 跟 npm 推測同步**。Release cadence ~6-14 天一版 minor、4 年 history 沒 broken = **最 stable 老牌 Office SDK 之一**。**`v0.25.2` 還在 0.x = API surface 仍可能 bump**、**horo-agent downstream embed 風險：v1.0 之前的 minor 可能 breaking、ship 進 production 需 lock 0.25.x specific version**。Family product per-repo 各自 released（Univer Workspace、Univer CLI、univer-for-X-harness 等）每個 owner 各自 cadence、需逐一查。

### `davila7/claude-code-templates`

- **Repo 摘要：** 「**Ready-to-use configurations for Anthropic's Claude Code. A comprehensive collection of AI agents, custom commands, settings, hooks, external integrations (MCPs), and project templates to enhance your development workflow.**」——Daniel Avila（davila7）個人 maintainer、贊助 by **Z.AI**、**Claude for Open Source Program**、**Neon Open Source Program**、**Bright Data**（web search/scrape）、**Vercel OSS Program**、since 2025-07-04 開源、**14 個月累積 31,212★**、Python、**MIT**、topics 4 個（`anthropic / anthropic-claude / claude / claude-code`）、`created` 2025-07-04（**14 個月 mature**）、`pushed_at` 2026-09-23 04:15 UTC（**今天仍在 push**）、`updated_at` 2026-09-23 05:58 UTC（今天仍有 metadata 更新）、`open_issues_count` 270、size 246.7 MB（**Python CLI + template marketplace + analytics dashboard**）、homepage `https://aitmpl.com`（**Web marketplace UI**）、`default_branch` main。**Latest GH release `v1.29.6` published 2026-09-17 01:14 UTC（6 天前）** = "**v1.29.6 — Claude Mods: --mod installs Anthropic-layout plugins, 24 mods**" = **24 mods in `--mod` flag** = new install vector。**GH release cadence**：v1.29.6 (9-17) → 推測 v1.29.x 系列 daily = **~6 天一版 minor**。**6 大 component families**：(1) **Agents**（development-team/frontend-developer, security-auditor, react-performance-optimizer, database-architect, development-tools/code-reviewer）、(2) **Commands**（testing/generate-tests, performance/optimize-bundle, check-security）、(3) **MCPs**（development/github-integration, database/postgresql-integration, web-data/bright-data-mcp, web-data/bright-data-best-practices）、(4) **Settings**（performance/mcp-timeouts）、(5) **Hooks**（git/pre-commit-validation）、(6) **Skills**（Bright Data skills: web-data/search / scrape / data-feeds / bright-data-mcp / bright-data-best-practices / brightdata-local-search = **web-data skill bundle**）。**6 大 CLI usage modes**：(a) **`npx claude-code-templates@latest`** 互動 browse-and-install、(b) **`npx claude-code-templates@latest --agent <X> --command <Y> --mcp <Z> --setting <W> --hook <V> --skill <U> --yes`** 一行裝組合 stack、(c) **`npx claude-code-templates@latest --analytics`** live session analytics、(d) **`npx claude-code-templates@latest --chats`** conversation monitor mobile-optimized interface 看 Claude responses 即時、(e) **`--chats --tunnel`** secure remote via Cloudflare Tunnel、(f) **`--skill web-data/search,web-data/scrape --mcp web-data/brightdata --yes`** 裝 Bright Data web data skills + MCPs in one shot = **「100+ agents / commands / settings / hooks / MCPs marketplace」**。**24 mods 的 `--mod` flag**：「**Claude Mods: --mod installs Anthropic-layout plugins, 24 mods**」= 新 install vector 把 Anthropic-layout plugins 一行裝完。**Marketplace structure**：`aitmpl.com` web marketplace UI、`https://github.com/davila7/claude-code-templates` source、`https://vercel.com/oss` 為主 hosting platform。
- **3W1H：**
  - **What：** 一個 Python CLI + JS web marketplace (`aitmpl.com`) + 100+ Claude Code components（agents / commands / settings / hooks / MCPs / skills）+ 24 mods 的 `--mod` install vector + analytics + conversation monitor（local + Cloudflare Tunnel remote）+ Bright Data web-data skills bundle（search + scrape + data-feeds + best-practices + local-search）。
  - **Why：** 現有 Claude Code user 三大對位：(1) **Claude Code plugin install 沒統一入口** — 必須各 agent marketplace 自己找、`npx claude-code-templates@latest` 給你一個 UI 點、(2) **裝 plugin 要自己學 slash command / hook / MCP 結構** — `npx claude-code-templates@latest --agent X --command Y` 給你 curate 過的組合、(3) **Agent session 看不清** — `--analytics` 給你 live session state detection + performance metrics、`--chats --tunnel` 給你 mobile-friendly 介面從 remote 監控。
  - **Who：** **Claude Code user** 想快速 spin up agents / commands / MCPs plugins 而不用各 marketplace 跑的；**Bright Data 客戶**想直接 `--skill web-data/* --mcp web-data/brightdata` 裝 combo；**Claude Code power user** 想 live analytics + mobile monitor 自己的 agent session；**Team lead** 想 oncall 機制 — `--chats --tunnel` 給 team member monitor 進度。
  - **How：** **(1) Quick install：** `npx claude-code-templates@latest` 互動 browse + install。**(2) One-shot combo：** `npx claude-code-templates@latest --agent development-team/frontend-developer --command testing/generate-tests --mcp development/github-integration --yes`。**(3) Bright Data web-data stack：** `npx claude-code-templates@latest --skill web-data/search,web-data/scrape,web-data/data-feeds,web-data/bright-data-mcp,web-data/bright-data-best-practices,development/brightdata-local-search --mcp web-data/brightdata --yes`。**(4) Analytics：** `npx claude-code-templates@latest --analytics`。**(5) Monitor：** `npx claude-code-templates@latest --chats` / `--chats --tunnel`（local / Cloudflare Tunnel remote）。
- **安裝方式：**
  - **NPX install（最快路徑，官方推薦）：** **`npx claude-code-templates@latest`** — 預設互動 browse / install、`--yes` flag 跳過 prompt。**`npm install -g claude-code-templates`** 為 alternative global install。
  - **One-shot combo install：** **`npx claude-code-templates@latest --agent <agent-path> --command <command-path> --mcp <mcp-path> --setting <setting-path> --hook <hook-path> --skill <skill-path> --yes`** — 6 種 component 可同 column 拼組合。
  - **Bright Data web-data stack：** `npx claude-code-templates@latest --skill web-data/search,web-data/scrape,web-data/data-feeds,web-data/bright-data-mcp,web-data/bright-data-best-practices,development/brightdata-local-search --mcp web-data/brightdata --yes` — 5 skills + 1 MCP in one shot。
  - **Claude Mods 新 vector：** `--mod <mod-name>` install Anthropic-layout plugins、24 mods available in `v1.29.6` release notes。
  - **Analytics mode：** `npx claude-code-templates@latest --analytics` — live session state detection + performance metrics。
  - **Conversation monitor（local）：** `npx claude-code-templates@latest --chats`。
  - **Conversation monitor（remote Cloudflare Tunnel）：** `npx claude-code-templates@latest --chats --tunnel` — 從手機 / remote 讀 Claude responses。
  - **pnpm / yarn：** `pnpm dlx claude-code-templates@latest` / `bunx claude-code-templates@latest`。
  - **pip / uv / poetry：** **不適用**（雖 repo 主是 Python、但發佈渠道是 npm、`package.json` for npx install、非 PyPI publish）。
  - **Source clone：** `git clone https://github.com/davila7/claude-code-templates`（246.7 MB monorepo + JS marketplace UI + templates）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1、可走 **`npx claude-code-templates@latest`** （5 秒裝完）→ 裝 Bright Data web-data stack → 主人平時常用 web search 可以省 API cost。HORO-Agent 對位：可直接抄 `--skill + --mcp` install pattern ship 一個 HORO-Agent 版 plugin marketplace、 `--chats --tunnel` 對位主人 memory 中 mobile-friendly monitoring use case。
- **近期 release：** **GH release `v1.29.6` published 2026-09-17 01:14 UTC（6 天前）** = "**v1.29.6 — Claude Mods: --mod installs Anthropic-layout plugins, 24 mods**"、**24 mods 是新 install vector**。**GH release cadence**：v1.29.6 (9-17) → 推測 v1.29.x daily series = **~6 天一版 minor**。**`v1.29.x` 已經走 14 個月 = API surface 已 stable**、**horo-agent downstream embed 風險遠比 Univer 低**。贊助生態系廣（Z.AI、Claude OSS、Neon、Bright Data、Vercel）= **vendor-backed**。**`pushed_at` 2026-09-23 04:15 UTC 今天仍在 push = daily active development**。

### `browser-use/jev-ultrafast`

- **Repo 摘要：** 「**A browser agent with a dynamic, indexed action space.**」「**Fastest and cheapest web agent**」——browser-use org（GitHub: browser-use/browser-use, open-source web agent framework, browser-use.com 公司 = 對外 web agent 服務, 2026 launch 的子 repo, 同來源還有 `browser-use/video-use` 在今日 Trending #8 9-23、跟主人 9-08 pick 過的 `trycua/cua` 同類）、2026-09-16 開源、**only 7 天內 launch**、already 18,372★（**viral overnight** = 18k star in 7 天 = daily ~2,600★ / day）、Python、**MIT**、topics 從 raw data 缺（簡讀 `description` 「Fastest and cheapest web agent」 = topic 不需）、`created` 2026-09-16 21:30 UTC（**7 天內**）、`pushed_at` 2026-09-18 16:28 UTC（**5 天前**）、`updated_at` 2026-09-23 05:59 UTC（**今天 metadata 更新**）、`open_issues_count` 119、size 4.5 MB（**small focused codebase**）、homepage `https://browser-use.com`（companion company site）、`default_branch` main。**Latest GH release `not found`（404 / releases/latest empty）** = **< 7 天 fresh launch 無 SemVer 紀律**。**3 個動作 operations + 4 個 wait operations** = CLICK / TYPE_TEXT / SELECT / SCROLL_UP / SCROLL_DOWN / WAIT / DONE / BLOCKED。**核心 magic**：**「Give it one goal. TypeSafe's Jev picks an operation and an element. A small LLM writes text only when the operation is `TYPE_TEXT`.」** = **dynamic, indexed action space**、**「each target head contains only compatible elements」** = **two decisions、one network round trip** = operation + target 在同一 request decide。**核心 5 大 design choices**：(1) **One request per decision cycle** — operation + target heads share observed state、(2) **No screenshots in default agent loop** — Jev consumes structured state、inspector 才能 opt-in screenshots、(3) **One browser call per snapshot** — atomic read of visible controls / names / values / text + DOM node references、(4) **Validate selected target** — clicks check document + form values + target + nearby context、animation 不 force re-prediction、(5) **Wait for useful state** — type into combobox 後 wait visible suggestions cap 200ms、其他 interaction 至多 2 個 animation frame 或 50ms。**Demo 數字**：Zürich → London Google Flights **7,073 ms 完整 run** (啟動 after initial page observation、含 model call + generated text + browser work + stale decisions + loading waits) = **vs 上一版 9,450s = 25% 削減**、**median browser protocol calls 1,092 → 101 = 10.8× protocol 削減**、**3/3 pass rate in 6 alternating runs same models + settings**、Wikipedia 2.798s、hotel search/filter 1.896s。**安全 design**：「Model output never becomes selectors, coordinates, shell commands, or executable JavaScript. Text-helper output must parse as a small JSON object before typing.」= **Jev 操作是 operation + target by index、不是 raw DOM selector 或 shell**，**絕對不讓 LLM output 變成 raw executable instruction**。**安裝 path**：`git clone` + `uv sync` + `.env` 含 `TYPESAFE_API_KEY` + `TEXT_MODEL_API_KEY`（OpenRouter key） + `uv run jev` → `http://127.0.0.1:8766` 本地 web inspector 點 Start demo → Run automatically。`TEXT_MODEL_API_KEY` 預設用 OpenRouter + `inception/mercury-2.5` reasoning disabled、**Gemini / GLM / DeepSeek 都吃**（OpenAI-compatible text helper）。
- **3W1H：**
  - **What：** Python + TS + Chrome 跑的 browser-use 組織新 sub-repo `jev-ultrafast`、`agent.py` (loop + text-helper handoff) + `snapshot.js` (atomic DOM snapshot + indexed controls + freshness guards) + `browser.py` (browser connection + geometry + execution) + `model.py` (dynamic operation/target heads + text generation) + `questions.py` (model instructions) + `demo.py` (local inspector)、6 檔案 small enough to read = **6 檔完整 readable codebase**。
  - **Why：** 現有 LLM browser agent 三大對位：(1) **太慢 + 太貴** — 一次 decision 多次 screenshot + multi-turn LLM = O(seconds) per click、**Jev 一個 request 同時 decide operation + target = 1 round trip per decision**、(2) **LLM output 變 raw selectors / shell** — 容易 jailbreak、Jev 操作 = **operation + target by index only、LLM output never becomes selectors or shell**、(3) **browser protocol overhead 太大** — 1,092 calls per task 太誇張、Jev 1 round trip per decision → **median calls 1,092 → 101 = 10.8× 削減**。**Jev 解三對位**：(a) **structured state not screenshots** — agent loop 預設讀 structured state、inspector 才 opt-in screenshot、(b) **two-head one request** — operation + target 一個 request 內並行 decide、(c) **atomic DOM snapshot** — visible controls 一次性 atomic read。
  - **Who：** **Web automation developer** 想 ship production browser agent、怕 LLM 寫 raw selector jailbreak、(b) **browser-use.com 客戶** 想用 ultrafast backend 取代標準 web agent、(c) **AI safety researcher** 想研究 LLM agent 的 tool-call surface 對位 restricted output 的安全設計。
  - **How：** **(1) Quick start：** `git clone https://github.com/browser-use/jev-ultrafast.git` + `uv sync` + `cp .env.example .env` + 設 `TYPESAFE_API_KEY` + `TEXT_MODEL_API_KEY`（OpenRouter key）+ `uv run jev` → browser 開 `http://127.0.0.1:8766` → 點 Start demo → Run automatically。**(2) Inspector：** 顯示 numbered elements + operation probabilities + target probabilities + executed actions、**Choose next** pause before execution。**(3) Chrome：** 透過 `browser-harness`（另一個 browser-use 倉庫）連 Chrome、安裝 by `uv sync` + `uv run browser-harness --doctor` if needs connect、Allow remote debugging in Chrome when prompted。**(4) Use library：** `from jev_ultrafast import Agent` + `with Agent(url, goal) as agent: for state in agent.run(): print(...)` + `uv run --env-file .env python your_script.py`。**(5) Examples：** `uv run --env-file .env python examples/flights.py --keep-open` （flights search + 驗證 + save trace、不 select / book）。
- **安裝方式：**
  - **git clone + uv sync（最快路徑，官方推薦）：** **`git clone https://github.com/browser-use/jev-ultrafast.git`** → `cd jev-ultrafast` → **`uv sync`** → **`cp .env.example .env`** → 編輯 `.env` 設 `TYPESAFE_API_KEY` + `TEXT_MODEL_API_KEY` (OpenRouter key) → **`uv run jev`** → browser 開 `http://127.0.0.1:8766` → click Start demo → Run automatically。
  - **Library use：** `from jev_ultrafast import Agent` + `with Agent(url, goal) as agent: for state in agent.run(): ...` + `uv run --env-file .env python your_script.py`。
  - **Chrome connection：** 透過 [Browser Harness](https://github.com/browser-use/browser-harness) 安裝 (`uv sync` 自動裝) → `uv run browser-harness --doctor` → Allow remote debugging in Chrome when prompted。
  - **Text-model config：** `TEXT_MODEL_API_KEY` 預設 OpenRouter、demo 用 `inception/mercury-2.5` reasoning disabled。Gemini / GLM / DeepSeek 都吃 (OpenAI-compatible text helper) → configure model + endpoint + reasoning setting。
  - **Inspect + develop：** `uv run ruff check .` + `uv run pytest` + `node --check jev_ultrafast/static/app.js` + `node --check jev_ultrafast/snapshot.js` + `uv build`。Tests 為 offline。`uv run python scripts/check_guards.py` checks real controls in local browser without model calls。
  - **Recording flight：** `scripts/record_flights.py <new-folder>` captures original browser timestamps；`scripts/render_demo.py <recording-folder>` renders verified run at 1× and crops Google account strip。Credentials + raw traces always ignored。
  - **pip / uv / poetry：** **部分適用（uv only）** — **`uv sync`** 為 primary install，沒有 `pip install jev-ultrafast` PyPI publishing。uv-poetry 互通但 setup 不同。
  - **npm / npx：** **不適用**（雖有 TS component, 安裝入口是 Python `uv sync`）。
  - **Source clone：** `git clone https://github.com/browser-use/jev-ultrafast`（4.5 MB small focused codebase）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1、可走 **`git clone` + `uv sync`**（超快、4.5 MB）→ 裝 Chrome (browser-harness helper) → **需要 TYPESAFE_API_KEY 帳號 + TEXT_MODEL_API_KEY OpenRouter key** = **需 sign up 才跑得起來**。HORO-Agent 對位：可直接抄 (a) atomic DOM snapshot pattern、(b) two-head-one-request mode、(c) structured state-not-screenshots = **HORO-Agent 若 ship browser automation skill、可做安全 + speed 強化**。
- **近期 release：** **未找到 GitHub release（404 / releases/latest empty）= < 7 天 fresh launch 無 SemVer 紀律**。但 README + demo + sub-routes (`demos/counter/README.md` 雖然是 substrate、這個是 `jev_ultrafast/agent.py` 等) 都已 ship。`pushed_at` 2026-09-18 16:28 UTC = 5 天前最後一次 push、`updated_at` 2026-09-23 05:59 UTC = **今天 metadata 更新 = issues / PRs 仍在處理**。Browser-use sub-repo、跟主 repo `browser-use/browser-use` 應該會 share release engineering、預計 1-2 個月內第一個 `v0.1.0`/`v0.2.0` SemVer release。

### `tamaratran/fast-jev-compaction`

- **Repo 摘要：** 「**Claude Code plugin that replaces the compaction summary with Jev decisions: every tool call and result is scored in one fast request, stale ones are dropped or truncated, everything kept stays verbatim. Also usable as an npm library.**」——Tamara Tran（個人 maintainer、`tamaratran` GitHub handle、TypeSafe Jev 組織相關 = 跟 `browser-use/jev-ultrafast` 同一個 Jev / TypeSafe 生態系）、2026-09-17 開源（**6 天內 launch**）、already 6,384★（**6k star in 6 天 = daily ~1k★ / day**, viral）、TypeScript、**MIT**、topics 從 raw data 缺、`created` 2026-09-17 05:57 UTC（**6 天內**）、`pushed_at` 2026-09-18 04:45 UTC（**5 天前**）、`updated_at` 2026-09-23 05:57 UTC（今天 metadata 更新）、`open_issues_count` 75、size 245 KB（**極小、focused library**）、homepage 缺、`default_branch` main。**Latest GH release `not found`** = **6 天內 fresh launch 無 SemVer 紀律**。**Two artifacts in single repo**：(a) **npm library** (`src/`) + (b) **Claude Code plugin** (`hooks/` + `.claude-plugin/`) = **plugin 包 library**、**dual shipping**。**核心 magic 7 步 pipeline**：(1) **Pair** — every `tool_use` paired with `tool_result` by `tool_use_id`、calls in first message + newest `preserveRecentMessages` 永遠 pinned、(2) **State to Jev** — whole conversation oldest-first、every tool result 變 short note (`ok, 4213 chars (omitted)`)、tool inputs + texts 保留、(3) **Fit into 25k tokens** (`maxStateTokens` = 25k default) — 5 stages fallback: tool inputs truncate to 1000 → 200 → 60 chars、long texts abridge head + tail、old non-pinned collapse to `[… N chars omitted …]`、old tool calls reduce to one line、call-less messages omit、runs of old call-only messages fold、(4) **Two `noul` questions per call** — keep **call** (call was made matters) + keep **result verbatim** (contents still needed re-run tool would not do)、(5) **Request split** — questions split into as many requests as needed so state + questions stays under 30k tokens (`maxRequestTokens` = 30k default)、same full state resent per request、concurrent requests、answers merged、(6) **Decisions per call against `keepThreshold` default 0.5**：`keepResult ≥ threshold` → keep call + result、else `keepCall ≥ threshold` → keep call + truncate result to first `truncateHeadChars` (default 300) chars + one-line note、else → remove call + result、(7) **Rebuild messages** — no result ever left without its call。**關鍵 design**：**(a)「never rewrites anything」— text messages 完全 verbatim 保留 only abridged in state Jev sees、(b) Jev failures throw caller decides fallback**（Claw Code hook falls back to built-in summary）。**`reductionRatio(result) < 0.25` 檢查**：如果 reduction 不到 25% 不值得、keep original transcript or summarize instead。
- **3W1H：**
  - **What：** TS npm library + Claude Code plugin `hooks/fast-jev.ts` (thin adapter feeds `session.compact` transcripts through `src/`) → Jev-as-a-Service "lossless compaction" — 丟掉 stale tool calls + 結果、保留 verbatim text、never rewrites.
  - **Why：** 現有 Claude Code `session.compact` 機制三大對位：(1) **Summary 是 lossy** — file path / exact error / constraint / command 都可能 drop、`fast-jev-compaction` 是「**never rewrites anything**」、(2) **一個 summary 全包** — 你不知道 LLM 在 summary 時 drop 什麼、**per-call `noul` question keep/drop decisions** 把「keep call / keep result verbatim / drop both」三選一分別 decide、(3) **Compaction 沒量化收益** — `reductionRatio(result) < 0.25` 不值得、`stats` reports message + character counts before/after、per-reason decision counts、state size in estimated tokens、fitting stage、number of requests。**fast-jev-compaction 解三對位**：(a) **per-call decisions** — 結構化丟 / 留 / truncate 而不是 LLM 自由 summary、(b) **verbatim text preservation** — 用戶 + 助手 text 完全 verbatim 保留、(c) **量化收益** — `reductionRatio` 自動判定值不值得。
  - **Who：** **Claude Code power user** 想 context compaction 強一點 — 怕 file path / error / constraint 在 summary 掉；**Claude Code long-session user** — coding session 4+ 小時後 context window 滿、走 `/compact` 後用 fast-jev-compaction 取代 default summary；**Plugin developer** 想 ship 自家 compaction 機制 — 從 src/ library 改 + Claude Code hook adapter。
  - **How：** **(1) Library install：** `npm install fast-jev-compaction` + `export TYPESAFE_API_KEY=...` + `import { compactMessages, reductionRatio, type Message } from 'fast-jev-compaction'` + 餵 transcript。**(2) Claude Code install：** 需 `claude plugin marketplace add tamaratran/fast-jev-compaction` + `claude plugin install fast-jev-compaction@fast-jev-compaction`、**前置 opt-in flag** `CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1` 在 `~/.claude/settings.json` (Claude Code 2.1.274+ early-access feature)。**(3) 執行：** `/compact` 自動走 fast-jev-compaction、toast 顯示 `fast-jev-compaction: kept N/M messages, no summary (...)` 表示 pruned history、or `fallback to built-in summary (...)` when Jev 失敗。**(4) Custom transport：** implement `JevAsker` interface (one `ask(state, questions)` method) + call `compact(messages, asker, options)`、export `buildJevRequest` + `parseJevResponse` 給 HTTP request body + response validation。
- **安裝方式：**
  - **Library install（最快路徑）：** **`npm install fast-jev-compaction`** + **`export TYPESAFE_API_KEY=...`** + `import { compactMessages, reductionRatio, type Message } from 'fast-jev-compaction'` + 餵 transcript 進 `compactMessages(transcript, options)` + 檢查 `reductionRatio(result) < 0.25`。
  - **Claude Code plugin install：** 需先設定 `~/.claude/settings.json` opt-in flag (`CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1` + `TYPESAFE_API_KEY`)、**require Claude Code 2.1.274+ early-access feature**、`claude plugin marketplace add tamaratran/fast-jev-compaction` + `claude plugin install fast-jev-compaction@fast-jev-compaction`、install prompt 設 options (api key + thresholds + truncateHeadChars)、defaults 用 `TYPESAFE_API_KEY` 從 environment。Restart Claude Code or `/reload-plugins`。From now `/compact` (and auto-compaction) goes through Jev: toast 顯示 `fast-jev-compaction: kept N/M messages, no summary (...)`。
  - **Run from checkout：** `CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1 claude --plugin-dir .` from repo root。No publishing step；marketplace is just repo's `.claude-plugin/marketplace.json`.
  - **Custom transport (BYO asker)：** `import { compact, JevAsker, buildJevRequest, parseJevResponse }` + implement `ask(state, questions)` method + call `compact(messages, asker, options)`。
  - **Build primitives exported：** `collectToolCalls`、`fitState`、`batchCalls`、`decideCall`、`applyDecisions` 都 exported、可以自己 chain。
  - **pnpm / yarn / bun：** `pnpm add fast-jev-compaction` / `yarn add fast-jev-compaction` / `bun add fast-jev-compaction`。
  - **pip / uv / poetry：** **不適用**（這是 TypeScript + npm library、無 Python binding）。
  - **Source clone：** `git clone https://github.com/tamaratran/fast-jev-compaction`（245 KB small focused library）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1、可走 **`npm install fast-jev-compaction`**（245 KB 秒裝）→ 需 TYPESAFE_API_KEY 帳號（要 sign up）→ `claude plugin marketplace add` + `claude plugin install` 直接整合進 Claude Code。HORO-Agent 對位極佳：可直接抄 (a) per-call keep/drop decisions pattern（preserveRecentMessages 全保留、old non-pinned 走 reduction 決策）、(b) `reductionRatio` 量化收益檢查、(c) `keepResult` / `keepCall` / `drop both` 三選一分別 decide = **HORO-Agent context hygiene 可借鑑 per-call decision primitive**。
- **近期 release：** **未找到 GitHub release（404 / releases/latest empty）= 6 天內 fresh launch 無 SemVer 紀律**。npm library is `fast-jev-compaction` 但 release 還沒 publish。`pushed_at` 2026-09-18 04:45 UTC = 5 天前最後一次 push、`updated_at` 2026-09-23 05:57 UTC = **今天 metadata 更新**。**Demo**：`demo/JevDemo` 是一個 native SwiftUI app (`demo/JevDemo/build.sh` → `demo/JevDemo/build/JevDemo.app` + 自動 launch) — 沒有 API call、不過是 demo for screen recording = **animated demo macOS app**。**6 天內 viral、預計 1-2 個月內第一個 `v0.1.0`/`v0.2.0` npm SemVer release**、npm registry 跟上需 check。

---

## 重點觀察

- **3/5 + 2/2 fresh launch (< 7 天內) 是本月第一次「fresh launch 4/5」進場、4-consecutive-tick fresh release cadence 持續 + 0 day zero-release floor** —— 今天 5 個 pick 的 release 狀態：**`agent-substrate/substrate` v0.1.0 published 2026-09-10 23:39 UTC（13 天前）**（first-ever SemVer、`created_at` 到 `published_at` 25 小時 = GA discipline 起步、但 Status 寫 "**not ready for production use, APIs are almost guaranteed to change**"）**+ `dream-num/univer` v0.25.2 published 2026-09-17 04:37 UTC（6 天前）**（🐢 Release、4 年老牌 minor cadence 持續）**+ `davila7/claude-code-templates` v1.29.6 published 2026-09-17 01:14 UTC（6 天前）**（**24 mods 是新 install vector**、Claude Mods `--mod` flag）**+ `browser-use/jev-ultrafast`** 未找到 release（7 天內 launch）**+ `tamaratran/fast-jev-compaction`** 未找到 release（6 天內 launch）= **3/5 有 GH release（≤13 天、其中 2/3 ≤6 天 = within-week）+ 2/5 = 7-day-fresh-launch 無 SemVer 紀律 = 「fresh launch 4/5」是本月 14:00 slot 新里程碑**。**連續 milestone**：(a) **fresh release 100% 持續 4-consecutive-tick (09-20 + 09-21 + 09-22 + 09-23)、(b) 今天 = 100% fresh-release (5/5 within-13-days)**、**雖然其中 2 個是 < 7 天 launch 沒 SemVer 但仍是 fresh push**。**Registry-lag check**：(a) **`dream-num/univer`** 推測 PASS（GH v0.25.2 跟 npm `@univerjs/preset-sheets` latest = 0.25.x 對齊、需 `npm view` 確認）**+ `davila7/claude-code-templates`** 推測 PASS（GH v1.29.6 跟 npm `claude-code-templates` latest = 1.29.x 對齊、需 `npm view` 確認）**+ `agent-substrate/substrate` 不適用**（無 npm publish、是 Go + Kubernetes）**+ `browser-use/jev-ultrafast` 不適用**（無 npm + < 7 天 launch）**+ `tamaratran/fast-jev-compaction` 不適用**（npm 包但未 release 過、< 6 天 launch）。**主人 horo-agent 下游 sign-off**：(a) **Substrate = early GA 0.x API、embed 風險高、可讀 source 借 agent multiplexing pattern**、(b) **Univer 0.25.x embed 需 lock 0.25 系列 specific version、防 0.26 breaking**、(c) **claude-code-templates 1.29.x already 14 個月 stable、API surface likely stable、embed 風險最低**、(d) **jev-ultrafast 跟 fast-jev-compaction 是 < 7 天 fresh launch、production embed 不建議、但讀 source 借 design pattern 是 perfect timing** = 「fresh launch 是最好的 study window、API 還 free」。

- **`agent-substrate/substrate` 跟 `google/ax` 是上下游架構關係、今天 Trending 兩個都出現 (#2 + #5)、Substrate-first vs ax-first 是同個 pick 哪個 expose 的差 = 今日 Trending 的「同主題進場」訊號** —— 今天 Trending top 8 row 中 `agent-substrate/substrate` (#2, 245 stars today, 3096★ total) + `google/ax` (#5, **2305 stars today = today Trending #1 真正量級**, pushed today metadata) + 週 trending 與 trendshift monthly 都有看到的 **`kagent-dev/kagent`**（CNCF Sandbox project, Kubernetes-native agent framework）三個都圍繞「**Agent 專用 execution runtime on Kubernetes**」主題。**核心 triplet**：(a) **Agent Substrate = low-level secure runtime**（microVM + gVisor + zero-trust kernel + actor/worker multiplexing 30× oversubscription）、(b) **`google/ax` = Agent Executor** = hyper-scalable agent harness built on Substrate（[announcement blog](https://cloud.google.com/blog/products/ai-machine-learning/agent-executor-googles-distributed-agent-runtime)）、(c) **`kagent-dev/kagent`** = CNCF Sandbox project, Kubernetes-native framework 使用 Substrate 跑 sandboxed stateful agent（[announcement blog](https://kagent.dev/blog/the-future-of-kagent)）。**今日 trending 訊號**：「**Agent Runtime 層 war 才剛開打、Substrate + ax + kagent 三個 project 在差不多時間內都出現在 Trending = vendor 全棧對齊中**」。**對位主人 horo-agent 對位**：(i) **若主人想 ship air-gapped enterprise 用的 agent runtime、Substrate 是 Google-vendor OSS 的 ground truth reference、對位 主人 air-gap 場景 OK、但 owner = Google 但「**not officially supported Google product**」= 沒有 SLA**、(ii) **`google/ax` 是 harness layer（high-level）、Substrate 是 substrate layer（low-level）、kagent 是 K8s-native glue layer (middle) = 三層可拆 owner 要做選型**。**Trending 解析今天只取 top 3 = Substrate 進場 ax 沒進場、但 ax 的 2305 stars today 暗示主人此 archive 應該 highlight Substrate + ax 是同一 vendor 的 layer cake、不是兩個無關 project**。**Streak tracking**：今天 Tier-A Top 3 = **`agent-substrate/substrate` (Google ATE, low-level runtime)** + **`dream-num/univer` (Dream Num Tech Office SDK)** + **`davila7/claude-code-templates` (davila7 Claude Code plugin marketplace, 14 個月 mature)** = **3 個不同 domain（agent runtime / Office SDK / Claude Code marketplace）但 Substrate 跟 ax 形成 cross-tier 同 vendor layer cake 訊號**。

- **個人 maintainer 4/5 + vendor-official 1/5 分布與 9-22 4/5 + 1/5 vendor 模式同型、今天 vendor = Google（但「not officially supported」）。Self-similar distribution** —— 今天 5/5 picks 中 owner 分布：**`agent-substrate/substrate` = Google ATE team 但明確標「NOT officially supported Google product」= vendor-official-ish 但不是正式 productization** + **`dream-num/univer` = Dream Num Tech 中國 Office framework 創業公司** + **`davila7/claude-code-templates` = Daniel Avila 個人 maintainer** + **`browser-use/jev-ultrafast` = browser-use 上游 org**（GitHub org `browser-use`, companion `browser-use.com` 對外 web agent 服務, 同 org 還有 `browser-use/video-use` 在今日 Trending #8）** + **`tamaratran/fast-jev-compaction` = Tamara Tran 個人 maintainer** = **2/5 個人 maintainer (davila7 + tamaratran) + 2/5 startup/maintainer-mix (Dream Num Tech + browser-use) + 1/5 Google ATE 但「not officially supported」**。**連續 milestone**：09-22 (4/5 個人 + 1/5 Anthropic 官方 vendor)、**09-23 (2/5 個人 + 2/5 startup-mix + 1/5 Google ATE 但 disclaimer) = personal + startup maintainer 模式持續佔主、3-consecutive-tick**。**對主人 horo-agent 對位**：(a) **個人 maintainer 模式 (davila7 + tamaratran)**：**davila7 = 個人 maintainer 14 個月衝到 31k★、靠 sponsor ecosystem（Z.AI / Claude OSS / Neon / Bright Data / Vercel）= 純 indie + 強 ecosystem sponsor**、**tamaratran = 個人 maintainer 6 天內衝到 6.4k★、靠 TypeSafe / Jev 技術生態系 ship plugin = niche layer v2 模式**、(b) **startup-mix 模式 (Dream Num Tech + browser-use.com)**：**Dream Num Tech = 4 年成熟中國創業、Office SDK 完整 product family** = enterprise-grade 模式、**browser-use.com = 2026 launch 美國創業、web agent 完整 sub-repo 生態（主 repo + jev-ultrafast + video-use + browser-harness）= vertical sub-repo strategy**、(c) **vendor-official-ish 模式 (Substrate)**：**Google ATE = personal-team 但 no productization = "公開 source 但不負責 SLA" = vendor-references-but-not-ship-it 模式**。**對主人 horo-agent 對位建議**：(i) **若 ship 個人 maintainer 模式、tamaratran 跟 davila7 是兩個不同 scale reference**、(ii) **若 ship startup 模式 + vertical sub-repo strategy、browser-use.com 是最佳 reference**、(iii) **Substrate 不能 ship 進 production runtime 但 source-readable 借 agent multiplexing pattern、跟 9-21 `yylo` plugin contract + 8-26 Recuris memory-evolution 構成主人 runtime 設計 primitive learning corpus**。

- **`tamaratran/fast-jev-compaction` 跟 `browser-use/jev-ultrafast` 是同一 Jev / TypeSafe 生態系、6 天內 + 7 天內 launch 兩個 sub-project 同進場 today = 「new tooling ecosystem consolidation」daily signal** —— 今天 5 個 pick 中 **`browser-use/jev-ultrafast`**（browser-use 倉庫 + Jev / TypeSafe 技術）跟 **`tamaratran/fast-jev-compaction`**（個人 maintainer + TypeSafe Jev 技術）兩個都是 **TypeSafe 的「Jev」系統的 consumer**：（a）**`browser-use/jev-ultrafast` = Jev as web agent action-space decision engine**（browser agent 的「兩個 head 一次 request 處理」basis）、（b）**`tamaratran/fast-jev-compaction` = Jev as context compaction drop/keep decision engine**（Claude Code session compaction 的「per-call drop / keep verbatim」basis）。**共同 Jev decision primitive**：**Jev 是 TypeSafe 的「reliable structured decision system」、吃 `noul` questions（structured prose questions）回 keep / drop / truncate decision、可以走 HTTP request body**。**兩個 pick 訊號**：(i) **Jev 是 TypeSafe 2026 H2 launch 的 vertical decision system、不是 generic LLM call、是有 structured question schema + reliability-aware threshold、適合「repetitive structured decision」（web agent action + session compaction）**、(ii) **browser-use + tamaratran + TypeSafe 三家共構 ecosystem、Jev-as-clearing-house for routine decisions 是新的 infra layer（類似 OpenRouter 給 LLM inference gateway、Jev 給 routine decision clearing-house）**。**對主人 horo-agent 對位極高**：(a) **Jev 的 `noul` question schema 是「repetitive structured decision」abstract primitive、主人可以在 horo-agent 內 ship 同樣的 routine decision layer（context compression / policy enforcement / compliance check 不走 generic LLM**、**走結構化 question + threshold decision** = 「**Jev-style routine decision clearing-house**」是 lower-cost / lower-latency alternative to generic LLM 對「**task decision**」）、(b) **主人 memory 中 Spec-driven N-stage workflow 的每個 stage gate（task breakdown → proposal → spec draft → apply）= repetitive structured decision、可用 Jev-style primitive 各做一次**、(c) **browser-use 跟 tamaratran 兩個 sub-project 同進場 today 暗示 Jev ecosystem 在 2026 Q3 「垂直 consolidation」- **對主人 horo-agent 是**：「**Jev 風格 routine decision clearing-house 是 infra-layer 抽象、未來 6 個月可能成為 agent framework standard pattern**」。

- **Tier-A cross-day repeat 0/3 floor 結構性 第二次驗證（9-22 + 9-23）+ Tier-B 也都是首次進場、4-consecutive-tick 無 cross-day repeat = 14:00 slot's de-overlap floor PASS** —— 今日 trending top 3 都是 NEW (`agent-substrate/substrate` / `dream-num/univer` / `davila7/claude-code-templates` 都沒在 9-17..9-22 prior 14:00 picks 中)、**Tier-B 也都是 NEW** (`browser-use/jev-ultrafast` + `tamaratran/fast-jev-compaction` 也是首次進場) = **5/5 全 NEW = 本月第一次 5/5 全 NEW**、**0/3 = structurally optimal read 跟 9-22 第二次驗證**。**連續 milestone**：09-17 (1/3) → 09-18 (?) → 09-19 (?) → 09-20 (0/3) → 09-21 (?) → 09-22 (0/3) → **09-23 (0/3) = 2-of-last-2 verified、但 5/5 全 NEW 是新里程碑**。**Tier-B 從 web_search 命中兩個 strong leads**：（a）**`browser-use/jev-ultrafast`** 從 trendshift live today + web search「web agent acceleration」匹配出來、(b) **`tamaratran/fast-jev-compaction`** 從 trendshift live today + description「Claude Code plugin」匹配出來 + TypeSafe Jev 關鍵字共構 **兩個 pick 都是 fresh launch < 7 天**。**對主人 horo-agent 對位**：(i) **Tier-B 該走 fresh-launch < 7 天的 niche 而非 mature viral hit 是本月 trend**、(ii) **5/5 全 NEW 表示 trending 演算法 + fresh-launch 生態系 都對 14:00 slot 友善、主人日後 truncate 14:00 slot 看到的 pool 會越來越 large**。**反面**：「**5/5 全 NEW 也意味著 pick 沒人能 cross-day 比較、horo-agent 對該 5 個 pick 的 production-readiness 不能 directly merge with prior-day experience、必須每個都重新 study**」。
