---
title: GitHub 自由探索 2026-09-13（14:00 台北時間）
date: 2026-09-13
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 7 天 repeat）
  - web_search 當日 AI 開源新聞（Tier-B web 探索 2）
  - GitHub REST API `/repos/<slug>` + `/releases/latest` + raw `README.md` + `LICENSE` + `docs/install.md`
---

# GitHub 專案動態

- 檢查時間：2026-09-13（14:00 台北時間）
- 檢查對象：`multimodal-art-projection/YuE` / `max-sixty/worktrunk` / `SnailSploit/Claude-Red` / `microsoft/agent-framework` / `superset-sh/superset`
- 來源組合：GitHub Trending today 共解析 14 筆，**排除前 7 天已寫過的 repeat**（rank 1 `asgeirtj/system_prompts_leaks`（09-09）、rank 2 `nab138/iloader`（09-12）、rank 4 `jihe520/MathModelAgent`（09-08）、rank 5 `Sonarr/Sonarr`（09-12 經典跳過）、rank 6 `alsk1992/CloddsBot`（09-11）、rank 8 `Shubhamsaboo/awesome-llm-apps`（09-09）、rank 9 `p1neappleXpress/OpenFlux`（09-12）、rank 10 `armory3d/armorpaint`（09-12 跳過 niche）），**fresh top 3 = rank 11 `SnailSploit/Claude-Red` + rank 12 `multimodal-art-projection/YuE` + rank 13 `max-sixty/worktrunk`**（domain 完全不重疊：red-team skill library / frontier music generation / git worktree CLI for parallel AI agents）；rank 3 `Flowseal/zapret-discord-youtube`（俄羅斯 DPI 規避 niche）與 rank 7 `yuliskov/SmartTube`（Android TV 媒體瀏覽器）雖然 fresh 但與 AI agent / harness 路線無交集被跳過。Tier-B web_search 當週 AI 開源新聞取到 **`microsoft/agent-framework`**（dotnet-1.21.0 / python-1.18.0 release 於 09-11 / 09-10 兩週一版、雙軌 cadence、Microsoft Foundry ↔ Azure OpenAI ↔ OpenAI ↔ GitHub Copilot SDK provider-flexible）+ **`superset-sh/superset`**（desktop-v1.28.0 release 於 09-09、Elastic License 2.0、13k★、運算 100+ coding agents parallel in isolated git worktrees、每週 release、YC-backed），兩者都已用 `curl https://api.github.com/repos/<slug>` 驗證 HTTP 200 + metadata 非空才寫入。

## Repo 摘要與 3W1H

### `multimodal-art-projection/YuE`

- **Repo 摘要：** YuE2 frontier music generation framework——把 lyrics + style 變成一份**可編輯的樂譜（melody + chord plan）**後再生成 48 kHz stereo full song，並支援 zero-shot cover（從旋律 transcription 翻唱）與 agentic editing（透過 chat 把 score / arrangement / lyrics 改到好）。7,392★、Python、28 MB、Apache-2.0、`created` 2025-01-23、`pushed_at` 2026-09-11 17:37 UTC。底層是 **AR–NAR Mixture-of-Transformers** backbone（先 AR 生成 score + semantic tokens，再用 flow-matching NAR 生成 acoustic latents，最後 VAE 解碼為 stereo），加上 MERT-v2-FullSong audio representation 與 SheetSage2 lyric-aware transcription。**YuE2 v0.1.6 release 已是 4 天內 push**——發布節奏接近 weekly。
- **3W1H：**
  - **What：** Python library + CLI + pre-trained checkpoint（從 Hugging Face `m-a-p/YuE2-3B` 自動下載，3B 參數），並在 README 明列 `🤖 Agent skill` 段落——把 API `plan() → generate_semantic() → synthesize() → decode()` 拆成對 agent 可呼叫的 staged pipeline。**注意** main 分支已是 YuE2，原始 v1 YuE 程式碼保留在 `YuE-v1` branch。
  - **Why：** 把 Suno / Udio 等「黑盒 prompt → audio」的現狀打破——你可以讀懂、改寫、覆寫樂譜，而且 best-of-8 在 WildSongBench 上 **6.9632 SongBench Avg 是所有評測過的 setting 裡最高**（competitive with Suno v5/v6）。對音樂人 / agent builder 來說差異化在「**白盒 + 可編輯 + 同一個 checkpoint 支援三種創作業（create / cover / edit）**」。
  - **Who：** 音樂製作人、agent builder、想做 cover 翻唱的人、研究 audio LLM 的人、需要可控 / 可代理生成音樂的內容團隊。**不適用於**：沒 NVIDIA GPU + 24 GB VRAM + BF16 的人（硬需求）、只想做「prompt 一句就完事」的人（黑盒 path 完全交給 Suno 比較快）。
  - **How：** `git clone` → `python3.12 -m venv .venv` → `python -m pip install .` → `python examples/generate.py --output outputs/first-song`（拿到 `audio.flac` + score + semantic tokens + acoustic latents + generation settings）。Pyton interface：`from yue2 import YuE2Pipeline` → `YuE2Pipeline.from_pretrained("m-a-p/YuE2-3B", device="cuda")` → 餵 JSON request。三種 cot 模式：`cot="full"`（可編輯 melody-and-chord plan，default new songs）/ `cot="melody"`（用於 cover）/ `cot="off"`（從 lyrics + style 直生）。Agent integration 可走 README 的 staged API。
- **安裝方式：**
  - **pip（官方主推，PyPI `yue2_infer` 已 publish）：** `git clone https://github.com/multimodal-art-projection/YuE.git` → `cd YuE` → `python3.12 -m venv .venv && source .venv/bin/activate` → `python -m pip install --upgrade pip && python -m pip install .`。也可直接裝 release wheel：<https://github.com/multimodal-art-projection/YuE/releases/download/yue2-v0.1.6/yue2_infer-0.1.6-py3-none-any.whl>。
  - **硬體要求（**硬性**）：Linux、Python 3.12、NVIDIA GPU with BF16 + 24 GB VRAM——這是部署的真正 bottleneck，不在 pip 套件本身。Model files 從 Hugging Face `m-a-p/YuE2-3B` 首次執行時下載；AR + NAR + VAE 三個 checkpoint 需要足夠 VRAM 容納。
  - **Hugging Face：** `m-a-p/YuE2-3B`（主模型）、`m-a-p/MERT-v2-FullSong`、`m-a-p/SheetSage2`、`m-a-p/WildSongBench`（評測），對 agent builder 而言 huggingface_hub pipeline 已足夠，不必 clone source。
  - **License：Apache-2.0（標準 SPDX）**，含專利授權條款——對主人 horo-agent / horo-webui air-gapped downstream 是 permissive + 專利友好。
- **近期 release：** **yue2-v0.1.6 — "YuE2 · frontier music generation with symbolic planning"，published 2026-09-09 19:10 UTC（4 天前）**。release note 強調 two-axis 設計——frontier song quality（與 Suno v5/v6 可比）+ editable composition（white-box symbolic planning），並已包含 staged Python API + zero-shot cover + agentic editing 三種創作業。前 5 個 release cadence（從 GitHub API）：yue2-v0.1.6（9/9）/ yue2-v0.1.5（8 月底，推測）/ yue2-v0.1.4 / yue2-v0.1.3 / yue2-v0.1.2——**幾乎每週一版**，是 5 個 repo 裡 release cadence 對音樂類來說最快的（音樂類 open-source release 普遍偏慢）。

### `max-sixty/worktrunk`

- **Repo 摘要：** Rust 寫的 Git worktree CLI，把 git 原生 `worktree add -b feat ../repo.feat && cd ../repo.feat && claude` 的「三次輸入」壓成 `wt switch -c -x claude feat` 一行。7,314★、Rust、57 MB、**MIT **OR** Apache-2.0（dual-license，自己選）**、`created` 2025-10-17、`pushed_at` 2026-09-13 05:26 UTC（今天仍在 push）。README 直接寫出「**Worktrunk is a CLI for git worktree management, designed for running AI agents in parallel**」——slogan 把 AI agents + git worktree 綁在一起，是 2026 下半年 agent parallelization 路線的 single-purpose 工具。
- **3W1H：**
  - **What：** 單一 binary（`wt`，從 `cargo install worktrunk` / `brew install worktrunk` / `winget install max-sixty.worktrunk` / `apt / nix / conda`），加 shell integration（`wt config shell install`）讓 `wt switch feat` 真的能 cd 過去。**核心指令**：`wt switch` / `wt switch -c feat`（建新 worktree）/ `wt switch -x claude feat`（建 worktree 後 spawn `claude`）/ `wt remove`（清掉 branch + worktree）/ `wt list`（列表含狀態）。hooks（post-start）可自動 install deps / start dev server。
  - **Why：** AI agent 已經從「一個 task」變成「5–10 個 parallel」，每個 agent 各佔一個 working directory 才不會互踩——git worktree 是唯一原生答案，但原生 UX 笨重（每個 task 要打 branch name 三次）。Worktrunk 把這層 UX 修好，並把 shell integration 做好（`cd` 真的換目錄）。**差異化**：同類 worktree manager 多，但**「Worktrunk is designed for running AI agents in parallel」是 README 第一句**——其 hooks / handoff / status 列都圍繞 agent loop 而不是 developer sync。
  - **Who：** 同時跑 N 個 Claude Code / Codex / Cursor Agent 的開發者、單人「agent 編排」、研究 git-worktree 為 AI agent 隔離環境的 team。**不適用於**：還在跑「單一 sequential agent」的人（git worktree 開銷大於價值）、講究 GUI 的使用者（這是 CLI，沒有 desktop app）。
  - **How：** 安裝後 `wt config shell install` 註冊 shell integration → `wt switch -c -x claude feat` 開新 worktree 並把 `claude` 直接 attach 進去（在 `~/.claude` 之外的工作目錄）。多 agent 並行：`wt switch -c -x claude feat-1` 與 `wt switch -c -x claude feat-2` 各拿獨立 worktree → 完成時 `wt list` 看狀態 → `wt remove` 收掉。Hooks 設定可放 `.config/wt.toml` 或 repo-local（auto-detect）。
- **安裝方式：**
  - **Homebrew（macOS / Linux 主推）：** `brew install worktrunk && wt config shell install`——shell integration 是關鍵，沒設的話 `wt switch feat` 不會 cd。
  - **Cargo：** `cargo install worktrunk && wt config shell install`（Rust toolchain 已裝）。
  - **Winget（Windows）：** `winget install max-sixty.worktrunk` 後用 `git-wt`（Winget 安裝會用 `git-wt` 避免與 Windows Terminal 的 `wt` 衝突；想直接用 `wt` 要去 Settings → Apps → Advanced app settings → App execution aliases 關掉 Windows Terminal 的 alias）。
  - **Conda / Pixi（社群 feedstock）：** `conda install -c conda-forge worktrunk && wt config shell install`，或 `pixi global install worktrunk`。
  - **Arch Linux：** `sudo pacman -S worktrunk && wt config shell install`（community package）。
  - **從 source：** `cargo test` 跑單元測試；`cargo test --test integration --features shell-integration-tests` 跑 shell integration（需 bash / zsh / fish / nushell / pwsh + `jq`）。
  - **License：MIT **OR** Apache-2.0（dual，自己選 case-A1 或 case-A2，LICENSE 檔案頭明確標示 "This project is dual-licensed under MIT and Apache-2.0. You may choose either license at your option."）**——**5 個 repo 裡最 permissive 的雙重條款**，比純 MIT 更靈活（含專利授權），比純 Apache 更熟悉。對主人 horo-agent / horo-webui air-gapped downstream 是 **structurally clean**（任一子條款都 OK）。
- **近期 release：** **v0.77.0 — "0.77.0"，published 2026-09-08 22:12:09 UTC（5 天前）**。release note 一條：「`wt list` sizes and aligns its columns to the terminal width」——一個長 branch name 不再撐開所有 row 的 Branch column，cap 在 32 chars。前 7 個 release cadence（從 GitHub API）：v0.77.0（09-08）/ v0.76.0（09-01）/ v0.75.0（08-27）/ v0.74.0（08-14）/ v0.73.0（08-12）/ v0.72.0（08-07）/ v0.71.x（略）——**5 個 repo 裡 release cadence 對 CLI 工具來說最快**（約每 5–14 天一版，符合 Cargo semver pre-1.0 慣例）。`max_sixty` 在 README 同時開「September 2026: ... Please let me know any frictions at all」的 maintenance call，**展現強烈的 single-maintainer 訊號**——bus factor 1 是 worktrunk 的長期風險。

### `SnailSploit/Claude-Red`

- **Repo 摘要：** 一個 curated library of `SKILL.md` files，把 Claude 變成「context-aware red team operator」——drop 一個 skill 進 `~/.claude/skills/claude-red` 後，Claude 讀到 conversation trigger（SQL injection / WPA3 / EDR evasion / ROP chain…）就會 load 對應 skill。3,713★、Python、2.3 MB、MIT、`created` 2026-03-04、`pushed_at` 2026-08-30 11:02 UTC。**v0.3.0 release 已是 14 天前 stale** 但 `updated_at` 2026-09-13 05:59——README 與 main branch 仍 active，但 release tag 沒追上 master。
- **3W1H：**
  - **What：** 78 個 `SKILL.md` 檔案跨 23 個 categories（Web Application 16 / Wireless 14 / Infrastructure 7 / Exploit Development 6 / Fuzzing 4 / Post-Exploitation 3 等），每個 `SKILL.md` 是純 markdown + 結構化「expert methodology for specific attack surface」——不是 exploit code library，是「方法論 + tooling reference + escalation paths + edge cases」給 LLM context window 讀。
  - **Why：**「把 Claude 變成 red team operator」這個需求在 CTF / 內部 security review / bug bounty triage 上是真的痛點——手工把 sqli / SSRF / XXE / SSTI methodology 寫進 system prompt 既冗長又不可組合。Claude-Red 的設計是「skills load on demand based on conversational trigger」——你不需要為 unused skill 付 context 成本，符合 Claude Skills system 的 progressive disclosure 模型。
  - **Who：** 經授權做 red team engagement 的 security consultant、bug bounty triage、security research、CTF 練習、operator training。**不適用於**：沒拿到「authorized」書面同意的人（即使技術上能跑，semantic 與 legal 上不正當使用就是違規——README 把「Use cases」段第一條就明示「authorized red team engagements」）。
  - **How：** `git clone https://github.com/SnailSploit/claude-red ~/.claude/skills/claude-red` 是 entry point；若只要部分 category 可 `git clone --filter=blob:none --sparse https://github.com/SnailSploit/claude-red && cd claude-red && git sparse-checkout set Skills/web Skills/active-directory`。Claude Code 內也可 `cat Skills/web/offensive-sqli/SKILL.md | claude --system-file -` 加 single skill。Claude.ai 上是 manual paste SKILL.md 進專案 system prompt。`./install.sh --target ~/.claude/skills --category web` 是正式 install script。
- **安裝方式：**
  - **git clone（官方主推，Claude Skills system）：** `git clone https://github.com/SnailSploit/claude-red ~/.claude/skills/claude-red`。Claude auto-loads matching skill based on conversational trigger。
  - **sparse checkout（只取需要的 category）：** `git clone --filter=blob:none --sparse https://github.com/SnailSploit/claude-red && cd claude-red && git sparse-checkout set Skills/web Skills/active-directory`——避免 clone 全部 78 個 skill 浪費頻寬。
  - **Claude Code（手動 attach）：** `cat Skills/web/offensive-sqli/SKILL.md | claude --system-file -`（或 `Skills/active-directory/**/SKILL.md` 多個）。這是把 skill 當「過去式 system prompt」載入。
  - **Claude.ai（手動）：** paste `SKILL.md` 全文到 Project 的 system prompt / 對話開頭——無法享受 trigger 自動載入的 progressive disclosure。
  - **Install script：** `./install.sh`（互動式）/ `./install.sh --target ~/.claude/skills`（明確指定）/ `./install.sh --category web`（單 category）。
  - **未找到明確 pip/npm 套件安裝方式**（README 與 install.sh 都把「Claude Skills directory」當唯一 deployment target，無 PyPI 發行）。
  - **License：MIT（標準 SPDX）**——對主人 horo-agent / horo-webui downstream 是 case-A1。
- **近期 release：** **v0.3.0 — "v0.3.0 — 78 Skills, 23 Categories, Full Wireless Suite"，published 2026-08-30 11:02:25 UTC（14 天前）**。release note 強調「major expansion」：從 37 skills in flat directories 升級到 **78 skills across 23 organized categories**，並新增 full wireless suite（802.11、WPA2/3、EAP、WPS、evil-twin、BLE、Zigbee、Z-Wave、LoRa、sub-GHz 全套涵蓋）。**14 天 stale 但 `pushed_at` 與 `updated_at` 仍活**——release tag 沒追上 main branch，是這類 SKILL.md library 專案的典型 pattern（每新增一條 skill 就更新 README badge，但 release 通常在「結構性變更」時才升 tag）。

### `microsoft/agent-framework`

- **Repo 摘要：** Microsoft 官方 multi-language agent framework（Python + .NET + Go SDK），把 multi-agent orchestration（graph-based workflow：sequential / concurrent / handoff / group collaboration）、middleware、checkpointing、streaming、human-in-the-loop、OpenTelemetry observability、Foundry hosted agents 一次打包。13,498★、Python、144 MB、MIT、`created` 2025-04-28、`pushed_at` 2026-09-11 23:54 UTC（昨天仍在 push）。Go SDK 在獨立 repo `microsoft/agent-framework-go`。**dotnet-1.21.0 / python-1.18.0 release cadence 各約 2 週一版**——雙軌且穩定。
- **3W1H：**
  - **What：** 一個 monorepo（`python/packages/` + `dotnet/src/` + `declarative-agents/` + `docs/decisions/`）實作 Microsoft Agent Framework（MAF）的兩種 language binding，以及 `python/packages/lab/`（benchmarking、RL、research），還有 DevUI（interactive dev environment for agent testing）。不是 library-only，是「framework + hosting + observability + declarative YAML agents」一整套。
  - **Why：** 2026 下半年 agent ecosystem 缺一個「**production-grade orchestrator**」，主流框架（LangGraph / CrewAI / AutoGen）的 production readiness 各有缺口（durability、restartability、governance、human-in-the-loop、observability）。MAF 從 Microsoft Foundry ↔ Azure OpenAI ↔ OpenAI ↔ GitHub Copilot SDK 多 provider 互通切入，搭配 declarative agent YAML、AF Labs（RL/benchmark）、Foundry hosted agents（再加 2 行 code 部署）等於把「從 prototype 到 production」的 friction 一次收。**值得關注**：`addEventListener('blogpost', microsoft-agent-framework-version-1-0)`——MAF v1.0 雖 RC 但 release cadence 兩週一版，stable 1.0 訊號接近。
  - **Who：** 想把 agent 從 prototype 推到 production 的 .NET / Python / Go 團隊、需要 checkpointing + observability + multi-agent orchestration 的人、用 Microsoft Foundry / Azure OpenAI 的人、想搭 GitHub Copilot SDK agent 的 ISV。**不適用於**：只想寫「一次性的對話 UI」的人（直接 Open WebUI / Dify 比較快）、完全不碰 Microsoft stack 的純 OSS 主義者（雖然 MIT license 沒限制，但 Foundry 整合是賣點）。
  - **How：** 安裝後 `from agent_framework import Agent` 起手（Python），`agent_framework.foundry.FoundryChatClient(project_endpoint=..., model="gpt-5.3", credential=AzureCliCredential())` 為 LLM client。`Agent(client=..., name=..., instructions=...)` 為 single agent，`agent.run("...")` 為單次呼叫；多 agent workflow 走 `python/samples/03-workflows/`。.NET 端從 `Microsoft.Agents.AI` NuGet 起手；Go SDK 從 `microsoft/agent-framework-go` 拉。
- **安裝方式：**
  - **pip（Python 官方主推）：** `pip install agent-framework`（badge 顯示 PyPI，package name 即 `agent-framework`）；要 Foundry 額外 `pip install agent-framework[foundry]`，要用 Copilot SDK `pip install agent-framework[copilot]`。版本跟 `python-1.18.0` 同步。
  - **NuGet（.NET）：** `Microsoft.Agents.AI`、`Microsoft.Agents.AI.Workflows`、`Microsoft.Agents.AI.Foundry` 等多個 NuGet package，版本跟 `dotnet-1.21.0` 同步（從 <https://www.nuget.org/profiles/MicrosoftAgentFramework/> 看）。
  - **Go：** `go get github.com/microsoft/agent-framework-go/sdk`，從獨立 repo `microsoft/agent-framework-go` 取（非本 repo subtree）。
  - **Azure CLI：** `az login` 是 Foundry 認證前置，credential 走 `AzureCliCredential`（README 給的 exact pattern）。
  - **Source（開發者）：** `git clone` → monorepo 用 pnpm workspaces / uv workspaces / dotnet sln；`python/samples/` 提供 100+ 範例，包含 `02-agents/providers/`、`03-workflows/`、`04-hosting/foundry-hosted-agents/`。
  - **License：MIT（標準 SPDX）**——對主人 horo-agent / horo-webui downstream 是 case-A1，**Air-Gapped warning**：「Foundry hosted agents」一行需要 Microsoft Foundry 連線；air-gapped 環境下要用 MAF 本地/自架 LLM 部分（OpenAI / Anthropic / Gemini / Ollama / vLLM provider 都支援）。
- **近期 release：** **dotnet-1.21.0 — "dotnet-1.21.0"，published 2026-09-11 17:31:35 UTC（約 1 天前）**。release note 包含：`Replace deprecated AWSSDK.Extensions.Bedrock.MEAI with AWS.Bedrock.MEAI`、`Remove Azure.AI.Operations...` 等 .NET 端 AWS Bedrock / Azure 依賴替換。**python-1.18.0 — "python-1.18.0"，published 2026-09-10 09:23:52 UTC（3 天前）**。release list 從 08 月底到 09 月初共 8 個 release，**dotnet/python 雙軌 ~2 週一版**——dotnet-1.21.0 / python-1.18.0（09-11/09-10）/ dotnet-1.20.0 / python-1.17.0（08-31/09-03）/ dotnet-1.19.0 / python-1.16.0（08-22/08-28）/ dotnet-1.18.0 / python-1.15.0（08-18/08-21）——**5 個 repo 裡 release cadence 最密且雙語言同步**，是 9 月當週最大的 AI agent framework news。

### `superset-sh/superset`

- **Repo 摘要：** 一個 agentic IDE，讓你**同時跑 100+ CLI-based coding agents**（Claude Code / Codex / Cursor Agent / Copilot / OpenCode…）平行，每個 agent 在自己的 isolated git worktree / branch / terminal / environment，跑完可直接從 IDE 內 diff / merge / open in editor。14,131★、TypeScript、508 MB、**Elastic License 2.0（**source-available, NOT OSI open-source**）**、`created` 2025-10-21、`pushed_at` 2026-09-13 05:53 UTC（今天仍在 push）、**YC-backed**。`README.md` 一進場寫「Claude Code, Codex, or any CLI agent, each in its own isolated worktree. Spend your time shipping, not waiting.」——slogan 直接命中 2026 agent parallelization 主旋律。
- **3W1H：**
  - **What：** 一個 desktop app（macOS / Linux binary，目前不在 Windows）——把「multi-agent terminal multiplexing」做成 IDE 體驗：sidebar 顯示每個 agent 的狀態（working / done / needs attention / chime）、built-in diff viewer、one-click handoff 到 editor 或 terminal、`wt`-style isolated worktree 一鍵建出。底層走 git worktree + terminal multiplexer + React/TypeScript UI。CLI / SDK / MCP / remote host 都 expose，**不鎖死在 GUI**。
  - **Why：** 同時跑 N 個 Claude Code / Codex 是 2026 dev workflow 的「singleton + babysit」瓶頸——你開 5 個 terminal，每個手動 `cd` 切換、merge 順序各自不同、status 散在不同 buffer、收掉要自己刪 branch。Superset 把這堆 SOP 做成 product，且對每個 agent 用**你自己的 subscription**（不收 platform fee，**不綁 SaaS**——這是 OpenCode Anthropic OAuth ban 後的重要生態訊號）。
  - **Who：** 單人 dev 但同時跑 5–10+ coding agent 的人、小型 team 想編排 multi-agent workflow 的人、研究「git-worktree-as-agent-sandbox」模型的人。**不適用於**：在 Windows 上（目前無 Windows binary，雖然 release 有提到 cross-platform plan）、不想在你的 repo 開 100 個 branch 的人（bus factor × N）、完全單 agent sequential workflow 的人（用 terminal 直接跑 `claude` 已夠）。
  - **How：** 下載 macOS / Linux binary → 安裝 → `git init` 或打開既有 repo → Superset 自動 detect → 開新 workspace（= 開新 worktree + branch）→ 選 agent（Claude Code / Codex / OpenCode / Cursor Agent…）→ agent 跑在獨立 terminal → 完事看 sidebar status → diff viewer review → merge winner 或 discard。CLI / SDK / MCP 給非 GUI 工作流（CI / remote dev box / script 化 parallel runs）。
- **安裝方式：**
  - **桌面 app（macOS / Linux，目前無 Windows）：** 從 <https://github.com/superset-sh/superset/releases/latest> 下載對應平台 DMG / deb / rpm / AppImage。`desktop-v1.28.0` 是最新。Windows 使用者要等（雖然 codebase 是 TypeScript + Electron-class runtime，理論上可 port，release note 未公開 ETA）。
  - **CLI：** Superset 同時 expose CLI（"The Terminal for Parallel Coding Agents"），從 desktop app 安裝後可 `superset` 指令直接呼。**SDK / MCP / Remote host** 是 09 月 README 新增的 access surface（"Reach your workspaces from anywhere via remote hosts, the CLI, the SDK, or MCP"），適合把 parallel agent run 串到 CI 或 remote dev box。
  - **從 source（開發者）：** `git clone` → monorepo（pnpm workspaces）→ `pnpm install` → `pnpm dev`。`apps/marketing/` 是 marketing site + 多語系 README（19 種語系，含繁體中文 `readme/README.zh-TW.md`）。
  - **License：Elastic License 2.0（**source-available, 非 OSI 開源**）**——LICENSE badge 在 README 第一屏、`LICENSE.md` 而非 LICENSE 為「值**強**許可」（source-available, not copyleft）。**對主人 `horo-agent` / `horo-webui` air-gapped downstream 是 case-SV warning**：可下載、可閱讀、可 fork、不可作為「managed service / SaaS」提供（Elastic License v2 明令禁止 hosting-as-service 反向工程），但**自架 + air-gapped + 單機使用**在許可範圍內。要把 Superset 部分程式碼（不是用整個）併入下游須通盤評估。
- **近期 release：** **desktop-v1.28.0 — "Superset Desktop desktop-v1.28.0"，published 2026-09-09 18:48:21 UTC（4 天前）**。release note 列：`fix(desktop): relaunch agents on the host when the default account switches`、`chore(desktop): bump version to 1.27.0`、`feat(desktop): open the PR su...`（truncated）。**release cadence 每週一版**（從 Repository Radar：weekly releases、102 contributors、issues closed 86% ~26d、commits / week 119、+61% vs prior month）——**5 個 repo 裡 product maturity 最高**（YC-backed + 13k★ + binary downloads 75k + 持續 5+ 月 weekly cadence）。

---

## 重點觀察

- **Tier-A 主題三足鼎立：agent parallelization × 2 + 領域垂直（音樂/安全）× 2，唯一「純 AI 開發者工具」缺席。** 今天 rank 11–13 三個 fresh pick 在 domain 分佈上完美錯開——`Claude-Red`（red-team SKILL.md library）/`YuE`（music generation）/`worktrunk`（git worktree CLI）——而且 `worktrunk` 與 tier-B `superset-sh/superset` **共同點出「git-worktree-as-agent-sandbox」是 2026 Q3 主流 orchestration pattern**。`YuE` 走的是 LLM 生成高 baseline 領域裡的「frontier 但 white-box + agentic editing」分岔，與 tier-A 兩工具構成三層敘事：tooling 層（worktrunk）/ orchestration 層（superset）/ domain-layer 層（YuE）。**剩下一個非 agent 領域（Claude-Red）**是主人 MEMORY red-team 興趣的命中機會，但對 horo-agent / horo-webui 主線無直接幫助。
- **Tier-B 同主題雙聚焦（dotnet+python framework + TypeScript orchestration IDE），對主人 SOUL 的 downstream 模型最直接可借鑑。** `microsoft/agent-framework` 是 production-grade orchestrator 的「all-in-one」實作（graph-based workflow、checkpointing、streaming、OpenTelemetry、Foundry hosted agents），release cadence 兩週一版雙軌同步（dotnet-1.21.0 + python-1.18.0）——這對主人 `horo-agent` 是「第三方 but compatible」訊號，不是「我要取代你」。`superset-sh/superset` 是「agent parallelization + git worktree + IDE」三者整合的產品化代表，對 `horo-agent` 與 `hermes-webui` 而言**借鑑點在「如何把 multi-agent run 做成可見、可 diff、可 merge 的 product」，不必複刻整套 app**。**兩個 tier-B 加起來對今日的 SOP「用模型挑、今天可能要換」剛好命中——與 09-12 的「use-agent-os + book-to-skill」雙 tier-B 路線（agent runtime + skill ecosystem）形成 mirror**。
- **License spectrum 今天拉到了 3 段：strict-permissive（5/5 都是 MIT / MIT+Apache / MIT）→ source-available（Elastic 2.0, 1/5）。** 5 個 repo 細分：`worktrunk` 是 **MIT OR Apache-2.0 dual**（5/5 裡最 permissive + 專利友善，可挑條款）+ `YuE` / `Claude-Red` / `agent-framework` 是 MIT（case-A1，`YuE` 雖沒寫但 SPDX 顯示 Apache-2.0 = case-A2，因含專利）+ `superset` 是 **Elastic License 2.0（case-SV, source-available, NOT OSI）**——**主人 `horo-agent` / `horo-webui` air-gapped downstream 必須把 `superset` 視為「可參考架構、不可直接嵌入商業 closed-source fork 的程式碼」**，但**自架 + air-gapped + 單機使用**仍許可。**今日 case-H 缺席**（昨天 `OpenFlux` 是 GPL-3.0，今日 cross-day repeat 後 GPL 重現機率歸零，今天沒有 copyleft 觸礁）。
- **5 個 repo 語言光譜從 Rust（worktrunk）→ TypeScript（superset）→ Python×2（YuE / agent-framework / Claude-Red 共 3）—— Python / TS 主流，Rust 一個特例。** 與 09-12 的 TS 2 + Python 2 + Go 1 對比，今天 **TS 1（superset）+ Python 3（YuE / Claude-Red / agent-framework 含 Python 的）+ Rust 1（worktrunk）—— Rust 出現**是 2026 Q3 CLI 工具的「system-level reliability needs」訊號：Rust 已不只是 OS / 編譯器標準，也是單人 maintainer 想給「可長期用 / 不會 std::sync::Mutex deadlock」的 CLI 的選擇。**多語言生态 4 種**（Rust / TS / Python / C#/.NET 雖未在 5 個 repo 中，是 agent-framework 的雙軌），比 09-12 的 3 種多一種。
- **對主人的實用度排序 + 跨 repo 觀察：** `microsoft/agent-framework` 是**主軸可直接研究**（dotnet+python 兩線對應主人的 Python Hermes + 未來可能的 .NET Bridge；Foundry 整合與 AI Gateway 路由設計對 8000 ASMR Foley Lab / 8100 lmstudio-council 的 LLM-stack 抽象也有借鑑），其 release cadence 兩週一版雙軌的 engineer cadence 比 worktrunk / superset 都更對齊 enterprise downstream；`max-sixty/worktrunk` 是主人 Kanban `worktree` kind（`HERMES_KANBAN_BRANCH` 設定）的 obvious shell tool，如果未來要在 Kanban 那層把「每張卡自動開一個 worktree」做掉，`wt switch -c -x claude` 是 RAW pattern；`superset-sh/superset` 短期無法進主人 stack（Elastic License 2.0 + Electron-class runtime + 必須 macOS/Linux binary），但**它的「sidebar status / diff viewer / merge winner」對 hermes-webui 的 Kanban 卡片 merge conflict 視覺化設計是 golden reference**，尤其其 single-binary GUI 對「multi-Kanban-card parallel」的可視化學習值得抄；`SnailSploit/Claude-Red` 對主人 SOUL 的 red-team 興趣是 secondary MEMORY hit（read 路徑的 SKILL.md 方法論 + tooling reference 對主人寫 LLM-as-attacker-simulation 的 skill 有 engine 借鑑價值，但 skill library 本體受限於 Claude Skills host 與 authorized context）；`multimodal-art-projection/YuE` 對主人無直接 call，但**其「白盒 + editable + 三種創作業同 checkpoint」是 agentic-tool design 的 general pattern**，對將來主人寫「skill → tool-call → editable artifact」這類 skill 設計有 pattern-level 借鑑。
