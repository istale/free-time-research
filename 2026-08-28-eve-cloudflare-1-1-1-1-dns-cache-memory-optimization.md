# How we saved 100 terabytes of memory by optimizing 1.1.1.1's DNS cache

- 原始連結: https://blog.cloudflare.com/dns-cache-memory-optimization-1111/
- HN 討論: https://news.ycombinator.com/item?id=49468083 （HN #1, 752 分 / 221 則留言, 2026-08-28 17:30 Taipei 抓取 — cross-validation: Cloudflare engineering post + HN top 是 community 雙重確認訊號）
- 前作脈絡: https://blog.cloudflare.com/big-pineapple-intro/ （Big Pineapple / Rust + Wasm 平台介紹）
- 發表: 2026-08-27 17:02 UTC（**today-published** — 距 EVE cron fire 17:30 Taipei 09:30 UTC 不到 24 小時）
- 來源: Cloudflare Blog — Big Pineapple 系列 engineering deep dive
- 閱讀時間: 2026-08-28（晚間）

## 摘要

**核心故事：Cloudflare 的 1.1.1.1 DNS resolver（Big Pineapple 平台）靠五個連續的小記憶體 layout 改動，把 per-entry footprint 從 953 bytes 砍到 420 bytes（-56%）、fleet-wide 釋放約 100 TB RAM**——相當於 130 台 Gen 13 server 的全部 RAM。同時 cache insert throughput +43%、lookup latency -19%——**沒有用 speed 換 space，反而兩個一起贏**。這是 Cloudflare 自家 engineering post 把 production memory layout 故事講得最完整的一次：5 個改動、benchmark 量測方法、production rollout 時間軸（5/18 啟動、7/6 完成）、p90/p98/p99 量測全部攤開。

**第 1 改動（最容易拿到的 15 TB）：把 cache entry 的 8 個 `Vec`/`String` field 換成 `Box<[T]>`/`Box<str>`**。Cache 寫入後不會再 append，`Vec` 的 capacity field（8 bytes）跟 over-allocated heap space 對 frozen entry 完全無用。`Box<[T]>` 一旦建好就不長，省 capacity + 省 heap 預留空間。每個 entry 省 64 bytes，250B entries 算下來 15 TB+。

**第 2 改動（再省 28 bytes/entry）：把 answer / authority / additional 三個獨立 list 壓成一個 list + 兩個 u16 offset**。原本每個 list 是 8-byte pointer + 8-byte length（16 bytes overhead）；換成 u16 offset 只要 2 bytes，總共省 28 bytes/entry。順手把好幾個 bool field pack 成 bitflag，連帶消除 struct padding——Rust alignment 把小欄位移掉常會順手讓整個 struct 縮 8 bytes，這是 Rust-specific 的 free win。

**第 3 改動（most records 省 owner field）：用 `Option<Box<Name>>` 取代 always-allocated `Name`**。多數 cache record 的 owner 就是查詢的 qname，乾脆存 `None`、lookup 時從 cache key 反推；只有 CNAME 鏈裡 owner 跟 qname 不一樣的少數 record 才存 `Some`。這個 trade-off 對 hot path lookup 友善（不用解 name compression pointer），但對 cold path 的 record 讀出加一點複雜度。對 80%+ 的 A/AAAA record 來說是純賺。

**第 4 改動（enum sizing + boxing）：`pub enum RecordData { A(Ipv4Addr), Aaaa(Ipv6Addr), Txt(Txt), Naptr(Naptr), ... }` 永遠是最大 variant 的大小，144 bytes total。但 A/AAAA（佔 80%+ 流量）只需要 4/16 bytes**。解法：把 `Txt` / `Naptr` / `Svcb` 等大 variant box 到 heap，enum 改成存 8-byte pointer。這一步省了 120 bytes/record for A/AAAA——但付了兩個成本：(a) jemalloc size class 對齊的 allocator overhead（MX 40 bytes 進 48-byte bin），(b) boxed data 散在 heap 不同位置，破壞 CPU cache locality。這兩筆 cost 是接下來第 5 改動要解決的。

**第 5 改動（終極一擊）：直接把 record data 用 wire format 存成單一 `Box<[u8]>`**——每筆 record 是 2-byte length prefix + raw bytes，沒有 enum tag、沒有 boxed variant pointer、沒有 padding。A/AAAA/TXT/所有 DNSSEC record type 可以直接 memcpy 進 outgoing message（不用 parse-then-serialize），CNAME/NS/MX/SOA 因為要 DNS name compression 才仍需 parse。這個改動一次解決了上一步的兩個 cost：消除 per-variant enum overhead + 修好 CPU cache locality。Lookup latency 額外降 5%、insert throughput +13%。Reusable scratchspace buffer 跨 cache insert 共用，避免每次 insert 都 allocate——`Vec` shrink 不一定能 reclaim tail，`Box<[u8]>` + scratchspace 繞過這個 jemalloc 細節。

**production rollout 從 5/18 啟動、7/6 全部完成**：每個 release 帶一個或多個上述改動，所以 memory usage 是階梯式下降而非一次掉。p99 從 9.3 GB → 5.3 GB（-43%），p90 從 6.5 GB → 3.8 GB（-42%）。Cloudflare 明確說接下來要把這 100 TB 重新投入 cache capacity（hit rate↑、upstream query volume↓），而不是還回去當 idle memory——這條「投資 vs 退還」的後續規劃很值得做 enterprise infra 的人留意。

## 3W1H 分析

- **What（做了什麼/主題）**: Cloudflare 的 Big Pineapple DNS platform（撐 1.1.1.1 / Gateway DNS / DNS Firewall / AS112）對 `CacheKey` + `CacheEntry` 兩個 Rust struct 做了五個連續的記憶體 layout 優化：(1) `Vec`/`String` → `Box<[T]>`/`Box<str>` 拿掉 capacity field + over-allocated heap；(2) 三個獨立 section list → 單一 list + 兩個 u16 offset；(3) `Name` owner field → `Option<Box<Name>>`（多數 record owner 等於 qname 直接 None）；(5) enum variant 大小 boxing → wire format `Box<[u8]>` 連 length prefix。Production rollout 5/18 啟動、7/6 完成，p99 RAM 9.3→5.3 GB、cache insert +43%、lookup latency -19%、per-entry footprint 953→420 bytes（-56%）、fleet-wide 釋放 ~100 TB。配套 benchmark 工具：custom `GlobalAlloc` wrapper 量 allocations/sizes + 56% A / 25% AAAA / 19% TXT 的 traffic-mix 合成資料。

- **Why（為什麼重要）**: 對主人這條 substrate-arc 軸線，這篇有兩個直接意義。**第一，「substrate memory layout 作為公開教材」的判例**——Cloudflare 沒有用 zizmor 那種工具式 linter、沒有用 ML 推論模型，而是用最樸素的 struct-level 改動拿掉 100 TB RAM，這恰恰證明了「多數 production 記憶體浪費不是演算法問題、是 type-system 預設問題」。對主人 `hermes-agent-lite` / `horo-agent` 的 SQLite-based session store + Kanban DB + memory editor 的 substrate，相同的 layout 思維（`Box<[T]>` vs `Vec<T>`、enum boxing vs raw bytes）可以直接套用到 Rust side 的 future module 上。**第二，「speed 跟 space 不是 trade-off」的 production evidence**——insert throughput +43% 跟 lookup latency -19% 同時達成，這個結果挑戰了「省 RAM 一定要犧牲 latency」這個常見誤解；對做 enterprise-lite / air-gap downstream 的人來說，這條 insight 改變了 baseline 假設：layout-aware refactor 不是 cost、而是乘數。`r.jina.ai` 8 個被驗證 vendor 來源裡，這篇是 **第一篇 Rust-specific 的 low-level engineering deep dive**——前面 8 篇（Cloudflare MCP / Spectre / EmDash / agents-stack / Anthropic multiagent / Snowflake CDC / Google Radar Researcher / 等等）都偏 protocol / application / infra 層，這篇第一次把 substrate 拉到 **language-level memory layout**。今天 (08-28) 的 AM (cost curve) + PM (persona/execution audit) 都偏 governance / market 層，這篇剛好補上 substrate 軸線在「Rust memory layout」這個 niche 的位置——是 8 月 substrate arc 在語言層級的延伸。

- **How（如何運作/實作）**: 五個改動每一個都有獨立的測量跟 benchmark recipe。(1) `Box<[T]>` 替換：對 frozen data 永久省 capacity field——Rust 的 `Vec` capacity 是用於 amortized growth 的，但 cache 一旦寫就不變，capacity 純粹是 dead weight。(2) Section list collapse：利用 DNS 規範（section count 永遠 < 65535）讓 u16 足以表達 offset，把三個 list metadata 從 48 bytes 壓成 4 bytes；額外用 bitflag 把 bool field 打包，連帶消除 Rust alignment padding。(3) `Option<Box<Name>>`：用 cache key 反推 owner 是 free 的（key 已經在 hot path 上），所以只有 CNAME chain 那種 owner 真的不等的 record 才付 `Box` cost。(4) Enum boxing：對 80%+ 流量是 A/AAAA 來說，把大 variant 移到 box 等於免費省 120 bytes/record；用 jemalloc size class alignment 解釋為什麼 MX (40→48 bytes) 有 8 bytes waste。(5) Wire format store：直接把 DNS response bytes 寫進 `Box<[u8]>` 配 2-byte length prefix，省 enum tag + 省 boxing + 修 cache locality；A/AAAA/TXT/DNSSEC record 可以直接 memcpy 進 outgoing message（不用 parse-then-serialize），只有含 domain name 的 record 仍走 parse path。配套的 reusable scratchspace buffer 跨 insertion 共用，避免 `Vec::shrink_to_fit` 不 reclaim tail 的問題。Benchmarking 用 custom `GlobalAlloc` 計數 allocations/sizes，配 56/25/19% A/AAAA/TXT 的 traffic-mix 合成測試，最後 production rollout 採漸進 release，每個 release 帶一個或多個改動。

- **Insight（赫蘿心得）**: 這篇讓赫蘿最在意的不是「省 100 TB」這個數字——而是 Cloudflare 把「**substrate 的 memory layout 是一等工程材料**」這件事完整地公開出來：五個改動的 trade-off 圖、benchmark 方法、production rollout 時間軸、per-percentile RAM 數字、最後一句「we'll reinvest this into cache capacity」全部都寫。對主人的直接可移植點有三個層次。**Layer 1 — language-level 套用到 hermes-agent-lite / horo-agent 的 future Rust module**：主人目前 `kanban.db` 跟 session store 都是 SQLite（在 Python side），但 Hermes downstream public-release (`horo-agent`) 已經在計劃加 Rust-side 高頻模組（subprocess streaming、token-bucket 等）；這篇證明 `Vec<T>` → `Box<[T]>` + section collapse + wire-format 存儲這三招可以直接拿來當 first-pass layout audit checklist，不必每次重新發明。**Layer 2 — benchmark methodology 直接借**：custom `GlobalAlloc` 量 allocations/sizes + 配 traffic-mix 合成 + production 對比，這套「先在 synth 量、後在 production 量」的雙軌法是 Cloudflare 標準做法；主人現有的 `horo doctor` 沒有 RAM/alloc 維度，加一個 `horo doctor --alloc-audit`（量 module-level allocations-per-task、cache-misses-per-request）就是直接 borrow。**Layer 3 — 「speed 跟 space 不是 trade-off」的 baseline 翻轉**：主人 8 月 AM (small models cost curve) + 這篇 EVE (memory layout) 形成一個互補 pair——AM 講 inference cost、這篇講 RAM cost，兩者都用「之前的 baseline 是錯的」這個 insight 結構。Calvin 那篇講「$1 → $0.10/run」、這篇講「9.3 → 5.3 GB p99」，同樣是「看似要犧牲一個維度才能贏另一個，但新方法兩個一起贏」的敘事。這條 pattern 對主人做 enterprise-lite positioning 很有用：**horo-agent 不是「為了下游 air-gap 犧牲效能」而是「layout-aware refactor 讓 air-gap 跟效能同時贏」**——把這句話寫進下一輪 horo-agent 對外文件，比再多寫一頁「我們能做 air-gap」更 memorable。**cross-tick continuity anchor**：今天 08-28 AM 的 Calvin French-Owen 那篇（小模型抵達 Pareto frontier）和 08-28 晚這篇（memory layout 不需要 trade-off）是 **同一個「之前 baseline 是錯的」敘事 pattern**——AM 從 inference cost 切入、晚從 RAM cost 切入，兩個不同 substrate 都得出「trade-off 假設被新 evidence 推翻」。對主人 LMStudio-council 跟 horo-agent 對外故事的 framing 而言，這條連續性值得在下一輪 cron 摘要的 Insight 上加一個「**今日雙 pick 的 pattern 觀察**」維度。**anti-pattern warning**：不要把這篇當成「Cloudflare 用 Rust 所以才省 RAM」的 Rust-only story——`Box<[T]>` vs `Vec<T>`、enum boxing、wire-format 存儲這些技巧在 C/C++/Zig/Go 都有對應物（sized array + capacity flag、union 與 tag overhead、mmap-based store）；語言層的 layout 思維是 substrate-agnostic 的，主人不必為了拿這套而引入 Rust。**別複製「語言」、複製「思維」**。