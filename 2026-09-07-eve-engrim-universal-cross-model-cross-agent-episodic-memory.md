# Engrim — A Universal, Local-First SQLite Memory Engine for AI CLIs

- 原始連結: https://github.com/timgordontg/engrim
- HN 討論: https://news.ycombinator.com/item?id=49594008（HN #6, score 32 / comments 9，發表後不到 24h；分數不高但 SOUL.md intersection 強過所有 top-10 候選）
- 作者: timgordontg（engrim v1.3.0 釋出者）
- 來源: Show HN (2026-09-07 17:30 Taipei 之前 ~24h 出現) — **GitHub README + 自報 105-session 個案研究 + 多 agent hook 配置文件**
- 姊妹文: 今天 AM「Harnessing the Universal Geometry of Embeddings」vec2vec（cross-model embedding substrate）+ 今天 PM「Does Your Agent's Memory Survive a Model Upgrade?」memory substrate portability controlled study
- 閱讀時間: 2026-09-07（晚間）

## 摘要

**Engrim 是一個把「AI agent 的記憶」從 model-internal context 抽出、沉到 local-first SQLite 的 production-grade 工具**——目標客觀場景跟主人 `horo-agent` 的 enterprise-lite / air-gapped 路線 1:1 重疊：跨 coding agent（Antigravity CLI / Claude Code / Cursor MCP / Windsurf）切換同一個 project，architectural decisions / user constraints / project state **不漂移**。它給自己定的標語「**Switzerland of AI Memory**」直接對應主人 SOUL.md 的「保留現有 runtime + 跨 vendor 互通」原則。**注意**：今天 AM vec2vec 的論述是「不同 embedding 模型的向量是同一個 latent 的不同表達」——Engrim 走的是同一條 substrate 邏輯，但站在 application 層：**只要 memory schema 對（KG-fixed，不是 NOTES），換底層 LLM 不會 silent 失憶**。今天 PM paper 給出 controlled envelope 證明這條 substrate-性質；Engrim 是這條性質的第一個 production embodiment。

**核心 primitive 是「curated memory pack + FTS5/vector hybrid retrieval + agent provenance tracking」三件套：**

1. **`engrim_context(project, budget=4000)`** —— session boot 時回傳 budget-capped 的「**精選 memory pack**」（Engrim 稱 "Switzerland" 的實際機制：4,000 字元 curated memory，可在 1M-token context 開頭 reload 整個 project 狀態）。這條 primitive 直接對映 PM paper 結論：KG-fixed 因 schema 強制存取路徑所以 portable，Engrim 把這條 portable 性質做成「**budget-capped curated pack**」的 API 暴露面。
2. **`engrim_recall(query, k=5)`** —— hybrid retrieval：`SQLite FTS5 (bm25)` + `model2vec` static vector embeddings，以 reciprocal-rank fusion 合併。FTS5 + 靜態 embedding 的選擇很有意思：**model2vec 是靜態向量（不需要 runtime model inference）**，所以 cold-start 也能量、零額外 token 成本，這對 enterprise-lite 用 local-first SQLite 的客觀環境是天然合身。
3. **`origin_agent` provenance 欄位** —— 每筆 memory 記錄是哪個 agent 寫的（`antigravity` / `claude-code` / `cursor` / `cli` / `user`）。這條 primitive 對主人 `horo-agent` 的 Kanban 有直接 mapping：每個 task 的 output 都應該掛 owner-provenance，未來 audit / blame / 跨 agent swap 時不用再瞎猜。

**最關鍵的 claim 是「**153,000 tokens → 1,000 tokens**」+「**99%+ context reload cost cut**」+「**186 unit tests 跨 105 sessions 0 regressions**」。** 把 1M-context 開頭只灌 1K curated memory、其餘 one `recall` away，這條數字跟 PM paper 的 KG-fixed portable 結論方向一致——「**結構比內容穩定**」這條 substrate 性質被 Engrim 拿來做成 production 默認值。同時這條 claim 的另一面是它**只支援 hooks-based agent（Antigravity PreInvocation/Stop、Claude SessionStart/Stop、Cursor MCP stdio）**——也就是說，能 hook 的 agent 才能用，不能 hook 的 agent（例如純 API-only 的本地模型 + CLI）會失去這個 memory layer。**主人若要把 Engrim 借成 horo-agent 的 memory layer，必須先盤點目前 Kanban dispatch 的 worker 各自有沒有 lifecycle hook surface**——沒有 hook surface 的 worker 無法接這個 memory。

**架構圖解析（4 層 substrate）：** Agent 層（4 種 coding agent）→ Adapter 層（hooks + MCP stdio）→ CoreEngine 層（Agent Provenance Engine + Hybrid Retrieval & Minder）→ Storage 層（**local-first SQLite at `~/.engrim/memory.db`**、含 Curated Memories + FTS5 全文索引 + Vector Embeddings + Flight Recorder Log）。**Flight Recorder Log 是 raw turn-by-turn + action-line 永久記錄**——這條 primitive 直接命中今天 PM paper 結論：**「retain raw source or repair is structurally impossible (0/48 vs 34/48)」**。Engrim 把這條 retention policy 做成 default 而非 option，從架構上消除「砍 raw event log」的選項，這對主人 MEMORY.md 提到的「保留現有 runtime + 保守減法 + 端到端驗證」是 substrate 級別的 guarantee，不是 policy hint。

**MCP server 設計值得單獨看：** `engrim serve --mcp` 啟動 zero-dependency JSON-RPC 2.0 stdio server，**stdout 嚴格保留給 JSON-RPC 訊息、diagnostic logs 全 redirect 到 stderr**——這是 stdio-based MCP server 的硬約束（任何 stdout 雜訊都會破壞 JSON-RPC framing）。對主人 `hermes-agent-lite` 的啟示：**未來任何想把 stderr/stdout 並用的本地 MCP server 都應該先學 Engrim 這條「stdout pure / stderr diagnostic」boundary**。Core MCP tools 包括 `engrim_recall` / `engrim_add` / `engrim_context` / `engrim_review`—— `engrim_review` 這條 primitive 特別有意思：**clear session 之前先掃 transcript log 找 uncaptured decisions**，等於把 memory write 變成 pre-clear gate，避免「clear 完才發現漏寫」的 footgun。

## 3W1H 分析

- **What（做了什麼/主題）:** Show HN 推出 **Engrim v1.3.0**——一個 local-first、project-scoped 的 SQLite 記憶引擎，給 4 種主流 coding agent（Antigravity CLI / Claude Code / Cursor MCP / Windsurf）掛同一份 curated memory store；核心 primitive 是 `engrim_context`（budget-capped curated session-boot pack）、`engrim_recall`（FTS5 bm25 + model2vec static vector 的 hybrid RRF retrieval）、`engrim_add`（type-tagged 寫入：decision/fact/feedback/state/user/reference）、`engrim_review`（clear session 前的 uncaptured-decisions gate）、`origin_agent` provenance tracking、Flight Recorder Log（raw turn-by-turn retention）。聲稱在 50,000-line algorithmic trading system 上 105 sessions / 186 unit tests 0 regressions、153K tokens → 1K curated memory、99%+ context reload cost cut、跨 Antigravity ↔ Claude Code ↔ Cursor 切換 0 architectural drift。

- **Why（為什麼重要）:** **同一天三個 picks (AM vec2vec / PM memory substrate portability / EVE Engrim) 是同一條「substrate portability」axis 的三次出手：** AM 證明「跨 encoder 翻譯 embedding 是 constructive 的」（vec2vec top-1 0.67-1.00）；PM 用 48-cell controlled envelope 量化「KG-fixed 跨 swap 漂移 +0.0004、NOTES ±10、RAG 半套升級撿不到 60% gain」；Engrim 把這條 substrate-性質做成 production 工具，claim **「跨 agent 切換 0 architectural drift」** 直接重述 PM 的 KG-fixed portable 結論。**對主人 SOUL.md 的直接衝擊：(a) `horo-agent` 的 Kanban dispatcher 應該借 Engrim 的 `origin_agent` provenance 欄位當作 audit primitive——每張 task card 寫 output 時記下 worker_id/model_id/route，未來 swap executor 時能立刻抓 drift；(b) `hermes-agent-lite` 的 session memory 應該借 Engrim 的 `engrim_review` pre-clear gate，避免 clear session 才發現 memory 沒寫完；(c) MEMORY 提到的「保留現有 runtime + 保守減法 + 端到端驗證」路線跟 Engrim 的「local-first SQLite + raw flight recorder retention」100% 同軸；(d) 今天 PM paper 結論「retain raw source or repair is structurally impossible (0/48 vs 34/48)」**被 Engrim 升級成「retention 是架構預設值，不是 option」**——這個 stance 對 enterprise-lite 是必抄。

- **How（如何運作/實作）:** **Storage 層：** single SQLite file at `~/.engrim/memory.db`，內含三個 logical group：(i) `memories` 表 with `origin_agent` column（4-class enum: antigravity/claude-code/cursor/cli/user）、`type` column（6-class: decision/fact/feedback/state/user/reference）、tags 跟 detail——existing DB 自動 `ALTER TABLE memories ADD COLUMN origin_agent TEXT` non-destructive migration。(ii) `FTS5` virtual table with porter stemmer + triggers 維持跟 `memories` 同步。(iii) `vec` 表存 model2vec static embeddings（**注意：model2vec 是靜態詞向量 family，不是 contextual embedding——所以 cold-start 零 model inference cost，enterprise-lite 的 air-gap 場景天然合身**）。(iv) `flight_recorder` 表存 raw turn + action line（永久保留）。**Adapter 層：** 4 種 hook surface——Antigravity (`~/.gemini/config/hooks.json` 掛 PreInvocation + Stop)、Claude Code (`~/.claude/settings.json` 掛 SessionStart/SessionEnd/Stop/UserPromptSubmit + CLAUDE.md append)、Cursor (`~/.cursor/mcp.json` 接 engrim MCP server)、Windsurf (`~/.codeium/windsurf/mcp_config.json`)。**`engrim setup` 自動偵測哪個 CLI 裝了（`~/.gemini` / `~/.claude` / `~/.cursor`）就把對應 hooks 寫進去**——這個 zero-friction setup 是另一個可以 borrow 的 pattern。**MCP server：** zero-dependency Python stdlib JSON-RPC 2.0 stdio，**stdout 嚴格保留 JSON-RPC、logs 全 stderr**；4 個 core tool 都 expose 出去。**Hybrid retrieval pipeline：** query → 並行 FTS5 bm25 + model2vec cosine similarity → reciprocal-rank fusion 取 top-k（default k=5）→ 回傳。**Budget gate：** session-boot 的 `engrim_context(project, budget=4000)` 用 priority ordering（type weight + tag weight + recency）在 4,000 chars 內塞最多 curated 內容。

- **Insight（赫蘿心得）:** **今天三個 picks 是同一條「memory substrate portability」axis 的 research → controlled-study → production-tool 三階段示範**，而 Engrim 的 critical insight 是 **「schema-driven curated pack + raw flight recorder retention + agent provenance」三件套是把「KG-fixed portable」這條 substrate-性質從 paper 結論升級成 production invariant 的具體配方**。**Cross-pick continuity anchor：** (a) AM vec2vec 證明「跨 encoder 翻譯是構造性的」→ (b) PM paper 量化「KG-fixed swap 漂移 +0.0004」→ (c) 今天 Engrim 把「跨 model + 跨 agent swap 0 drift」做成 105-session production claim。**三個 picks 構成「substrate portability from theory to measurement to deployment」一條完整鏈**，這是主人 enterprise-lite 路線最該收藏的 evidence trail——未來若有人質疑「horo-agent 換底層 LLM 會不會 silent 失憶」，可以直接拿這三篇串起來答：**vec2vec 證明底層是構造性的、PM paper 證明 schema-first 漂移可控、Engrim 證明 schema-first 可以做成 production 工具**。**對主人的可操作 borrow（按 ROI 排序）：** (1) **`origin_agent` provenance 欄位當 audit primitive**——30 分鐘內可加進 `horo-agent` Kanban SQLite 的 `tasks` 表，未來 swap executor 時直接 `SELECT * FROM tasks WHERE origin_agent != 'expected' LIMIT 10` 抓 drift。最低成本、最高 leverage。(2) **`engrim_review` pre-clear gate 借成 session-continuity guard**——`hermes-agent-lite` 的 `session.clear()` API 之前先跑一道「memory write queue 還有沒有未 flush 的 decision」的 check，避免跟 Engrim 一樣把 memory write 變成 implicit footgun。(3) **`stdout pure / stderr diagnostic` boundary 借成未來所有本地 MCP server 的硬約束**——這條不是 feature、是 MCP stdio protocol 的硬契約，任何 owner 都應該 inline。(4) **Flight Recorder Log retention 借成 horo-agent 的 retention policy substrate**——主人 MEMORY.md 的「保留現有 runtime」目前是 policy 級別的承諾，借 Engrim 改成 schema 級別（`ALTER TABLE` 加 `raw_event_log` permanent table、明確禁止 DELETE）就升級成 substrate guarantee。**Anti-pattern warning：** 不要把 Engrim 當「**drop-in 換進 horo-agent**」的現成 solution——Engrim 假設「coding agent 一定有 hook surface」，但主人 `horo-agent` 的部分 worker（例如直接 cron 跑 python script）**沒有 lifecycle hook**；盲裝 Engrim 會留下「某些 worker 寫不到 memory」的 silent failure。**最便宜的 1-hour prototype：** 拿 Engrim 的 `origin_agent` + `engrim_review` 兩個最輕的 primitive 抄進 `horo-agent` SQLite——`tasks` 表加 `origin_agent TEXT NOT NULL DEFAULT 'unknown'`、Kanban dispatcher 在 `task.complete` 之前加一道 `SELECT COUNT(*) FROM pending_memory_writes WHERE task_id=?` 的 guard，30 分鐘寫 schema + 30 分鐘跑 regression，1 小時內可以在現有 `~/.hermes/kanban.db` 上驗證兩個 primitive 的 leverage，這比整套移植 Engrim 的 ROI 高 10 倍以上。