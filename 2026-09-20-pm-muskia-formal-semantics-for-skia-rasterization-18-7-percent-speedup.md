# Semantics for 2D Rasterization: A Formal μSkia Compiler-Style Optimizer for Skia Drawing
- 原始連結: https://arxiv.org/abs/2603.23696
- HN 討論: https://news.ycombinator.com/item?id=49770256
- 閱讀時間: 2026-09-20（午間）
- 來源: Hacker News 熱門前 10 第 7 名（83 分 / 20 則留言，讀取時 2026-09-20 11:31 Asia/Taipei）
- 作者: John Toman, Tahina Ramananandro, Zachary Tatlock, Conor McBride（U. of Washington / Inria / Galois, Inc. / OCI）

## 摘要

**主人，這一篇跟今早 HN#15 Cloudflare "Saving 100TB of RAM with math" 是同日同軸的姊妹作——但分攻 substrate 的不同層：Cloudflare 把 *ring 上的 virtual node 數量* 還原成「其實是 sorted slice + binary search」這個被教科書遺忘的等價形式（*server-routing layer* 的 substrate 瘦身）；μSkia 把 *Skia draw call sequence* 還原成「其實有 4 種 fixed pattern 可以形式化驗證 + 自動 rewrite」這個被 Chrome 工程師手調經驗遺忘的等價形式（*GPU-rendering layer* 的 substrate 瘦身）。** 兩篇合在一起剛好形成「**network-edge substrate 隱形 cost ↔ browser-edge substrate 隱形 cost**」的雙覆蓋，主線都是「substrate-aware 演算法比 naive 工程經驗更有 leverage」——主人今早 Insight 段提的「**ticket-driven engineering + math re-parametrization**」這個 portable pattern，今日 μSkia 給了對應的 **「formal semantics + Lean 證明 + closed-loop verified optimizer**」實作。

**核心命題：Chrome 連 top 100 website 都還在 emit 低效 Skia sequence，原因是 Skia 語意太複雜、執行模型太不透明** 作者群直接用 Skia 寫 benchmark 跑 99 個從 top 100 網站蒐集的 Skia programs，發現 Chrome 真的會產生低效 instruction sequence。**這個「連 co-developer 都還沒榨乾的隱形 cost」正是主人今早 Cloudflare 文章要對付的 substrate**：Cloudflare 的「N virtual node × server count」是 routing table 看不見的 silent tax，μSkia 的「Chrome 不會自動化的 draw call pattern」是 Skia call sequence 看不見的 silent tax。**μSkia 解法分四階段**：**(1) 形式化語意**——寫出 `μSkia`：一份 Skia 2D graphics library 的 formal semantics，在 Lean theorem prover 裡 mechanize 整套語意，覆蓋 canvas state / layer stack / blending / color filter 三個 strata；**(2) 識別 pattern**——從 Chrome 蒐集的 99 個 programs 中找出 4 個 sub-optimal pattern（這 4 個 pattern 是「Skia 工程師手調經驗的 crystallized knowledge」）；**(3) 形式化重写 + 驗證**——為每個 pattern 寫一個 replacement，**用 μSkia 證明 replacement 對原 program 是 refactoring-equal 的**（這裡抓到一堆 tricky side conditions，例如 layer state 是否被 intermediate draw call 修改）；**(4) 自動 optimizer**——寫一個 high-performance Skia optimizer 套用這 4 個 pattern，**最佳化 trace 可以被 load 回 μSkia semantics、translation 在 Lean 裡驗證**——end-to-end verified optimization。

**為什麼主人會想讀——這是 hermes-agent-lite + horo-agent 下游 + air-gapped runtime 的「把工程師經驗換成形式化證明 + 自動 optimizer」範式樣板** 三個 transferable primitive 直接對應主人 air-gap 下游偏好（保守減法、不重寫 core runtime、端到端驗證）：
1. **「Skia 工程師手調經驗」是 silent engineering tax**——Chrome / Figma / Material Design 這些高度最佳化產品都還在 emit 低效 Skia sequence，證明 **empirical optimization 有上限**：人腦對 4 個以上 variable 的 interaction 已經疲於應付，而 Skia draw call 有 15+ 個變因（layer / blend mode / filter / mask / matrix）。主人的 hermes-agent-lite tool-call dispatch layer 一樣有同型問題：今天主人靠人工讀 source 來找低效 tool-call path，下一步可以寫 **形式化 semantics for tool dispatch + Lean-style 證明 + 自動 optimizer**。
2. **μSkia 的「三 strata 設計」是 substrate-aware 抽象的範式樣板**——文章把 μSkia 切成 canvas state / layer stack / blending 三層，讓每層可以獨立 extend；對應主人 hermes-agent-lite / horo-agent 的「tool wrapper 是哪幾層？」「skill router 是哪幾層？」「profile isolation 是哪幾層？」——**形式化語意 + 分層抽象 = 可以 verify 的可組合性**。
3. **「closed-loop verified optimizer」是 horo-agent 下游評測床需要的 pattern**——μSkia 不只是寫一份 semantics、寫一個 optimizer，而是 **讓 optimizer 吐出 trace、trace load 回 semantics、translation 在 Lean 證明**。這是 hermes-agent-lite 那種「agent loop 跑出來是不是真的有效？」的 closed-loop verification——**沒有形式化驗證就只能看 sample 是否過；有了形式化驗證，sample 過 = 對所有相同 shape 的 input 過**。

**跟今早 Cloudflare 的 substrate-arc 對齊** 兩篇共同驗證「**substrate-aware 演算法比 naive engineering 更有 leverage**」這個 2026 下半年的 design philosophy：今早 Cloudflare 把 ring 上的 200× virtual node 換成 sorted slice + binary search（**演算法換 substrate，不是 fleet 換 RAM**）；今日 μSkia 把 Chrome 工程師的「手調 4 種 draw call pattern」換成 **形式化語意 + Lean 證明 + 自動 optimizer**（**形式化換 substrate，不是 Chrome 換工程師**）。三者共同點：**先用 math / formal semantics 證明「naive / empirical 做法在做 redundant work」或「naive 做法有 closed-form equivalence」、然後把 redundant work 砍掉或把 equivalence 自動套上，substrate 開銷自然回收**。對應到主人 air-gap 下游偏好——「保留已驗證 runtime 與真實行為，保守減法、端到端驗證」——μSkia 沒砍掉 Skia 任何外部 observable behavior（rendered pixel 完全不變、GPU backend 切換也不影響 speedup），只是把「假裝要工程師手調的 pattern」和「假裝高效的 hand-tuned sequence」換成形式化自動生成。

## 3W1H 分析

**What（做了什麼/主題）:** μSkia 是 USENIX/OSDI 級 paper，論文中四件事：(a) **寫出 Skia 2D graphics library 的 formal semantics μSkia**，把整個 Skia API 行為拆成 canvas state / layer stack / blending / color filter 三個 stratum，每個 stratum 在 Lean 4 theorem prover 裡 mechanize 成可執行的形式化語意；(b) **從 Google Chrome 蒐集 99 個真實 Skia programs**（取自 top 100 網站），**識別出 4 個 sub-optimal draw call pattern**——Chrome 工程師手調經驗的 crystallized knowledge、但因為 Chrome source code 太複雜沒自動套用；(c) **為每個 pattern 寫一份 replacement code，用 μSkia 證明 replacement 與原 program 的 rasterization 結果完全等價**（這裡抓到若干 tricky side conditions，例如某個 intermediate draw call 是否 modify layer state）；(d) **寫一個 high-performance Skia optimizer**，套用這 4 個 verified pattern，**optimizer 吐出的 translation trace 可以被 load 回 μSkia semantics、end-to-end translation 在 Lean 裡 verify**——不只是「我們相信這個 optimizer 沒 bug」，而是「**我們證明這個 optimizer 在所有 inputs 上都正確**」。

**Why（為什麼重要）:** 三個 transferable insight 直接掛上主人 stack——(a) **empirical engineering 有上限，formal semantics 是 escape hatch**——Chrome 是 Skia 的 co-developer，連他們都還沒榨乾 hidden cost；對應主人 hermes-agent-lite tool dispatch：「今天主人靠讀 source 找低效 tool-call 路徑」是 empirical，「明天主人寫一份 formal semantics for tool dispatch + Lean-style 證明 + 自動 optimizer」是 escape hatch。μSkia 證明這個 escape hatch **真的可以套在 production-grade library 上**：Lean mechanization + 18.7% speedup + 32 μs overhead + cross-GPU 通用。(b) **「verify the replacement」是 substrate-aware 優化的關鍵**——常見的 optimizer 寫完之後只能靠 sample 測試，**μSkia 把「optimizer 對所有輸入都正確」這件事降成 Lean theorem**；對應主人 hermes kanban「skill-effect / arena-bound function」設計——今天主人驗 tool-call 正確性靠 sample + smoke test，下一步可以考慮 **寫一份 tool dispatch 的 formal semantics + theorem-prove 自動 rewrite**。(c) **三 stratum 設計 = substrate-aware 抽象**——μSkia 不寫一個 monolithic semantics，而是分層：canvas state（global drawing context）/ layer stack（offscreen render target stack）/ blending（per-pixel compositing）。**每層可以獨立 extend、可以獨立 verify**。對應主人 hermes-agent-lite / horo-agent 的「tool wrapper / skill router / profile isolation 三層分離」設計——**形式化語意 + 分層抽象 = 可以 verify 的可組合性**，這比 monolithic design 更適合 air-gapped downstream（每層可以單獨 audit / 單獨 replace）。

**How（如何運作/實作）:** 四階段 pipeline：(1) **μSkia semantics authoring**——把 Skia 的 C++ API 行為 formalize 成 Lean 4 code，覆蓋 4 個 feature stratum：canvas state（save/restore/transform/clip）、layer stack（push/pop、composite）、blending（per-pixel alpha compositing）、color filter（matrix-based recoloring）。Lean mechanization 讓 semantics 可以被執行而不只是被描述。(2) **Pattern identification**——把 Chrome 真實 Skia programs 跑 Skia profiler / flame graph，找 hot draw call patterns，從 99 個 programs 中 crystallize 出 **4 個 sub-optimal pattern**：例如「redundant save/restore 對」「blend mode 切換後未還原」「layer save 後做 full-canvas composite」「color filter 在 layer 上被套用兩次」。(3) **Replacement + verification**——為每個 pattern 寫一份 replacement Skia source code（移除冗餘的 save/restore、blend reset、composite、filter）；**用 μSkia 證明 replacement 與原 program 在所有 canvas state 下 rasterize 結果完全等價**——這個證明抓到若干 tricky side conditions，例如某個 intermediate draw call 是否 modify layer state、某個 blend mode 是否被 inheritance 影響。(4) **Optimizer + closed-loop verification**——寫一個 Rust/Lean-based Skia optimizer，input 是一段 Skia source、output 是 replacement Skia source；optimizer 吐出 translation trace（每一步用了哪個 pattern、replaced 哪段 code），**trace 被 load 回 μSkia semantics、translation 在 Lean 裡 verify**——end-to-end closed-loop verification。**三個 substrate-aware 細節**：(a) **三 stratum 設計**讓每個 pattern 可以獨立 proof，不需要 global invariants；(b) **32 μs overhead**——optimizer 在 99 個 programs 上平均 32 μs 跑完，可以 inline 進 build pipeline；(c) **cross-GPU 通用**——speedup 在 Skia GPU backend 與 CPU backend、不同 GPU vendor、不同 driver 都成立。

**Insight（個人心得）:** 主人，這一篇在 substrate-identity 上是 9/20 早 HN#15 Cloudflare「Saving 100TB of RAM with math」+ **9/20（今日）μSkia「Semantics for 2D Rasterization」**「**substrate-aware engineering 把 empirical hidden cost 還原成 formal equivalence**」軸線的**第二個 instance**。文章最強的 portable primitive 是 **「optimizer 吐出 translation trace + trace load 回 semantics + Lean 裡 verify translation」的 closed-loop pattern**——它不只是「寫一個 optimizer」或「寫一份 semantics」，而是 **「讓 optimizer 與 semantics 之間的關係本身成為可驗證的 theorem」**。對應到 hermes-agent-lite / horo-agent 下游：今天主人驗 tool-call 正確性時常寫 sample test + smoke test，這是 empirical；下一步可以考慮 **寫一份 tool dispatch 的 formal semantics（Lean / Coq / TLA+） + theorem-prove 自動 rewrite + 把 rewrite trace 送回 semantics verify**——這個 closed-loop pattern 是 hermes-stack 從 empirical testing 升級到 formal verification 的 escape hatch。**1 小時 prototype 路徑**：在 `~/.hermes/profiles/default/skills/` 下新增 `tool-dispatch-semantics` skill（不要真的寫 Lean semantics，做的是 portable primitive：**把「empirically-validated 4 個 tool dispatch rewrite pattern」做成 Lean-style specification、host 成 CI gate**），提供四個 pattern 的 Lean-style type signature + 一個 verifier 接受 pattern name + before/after tool dispatch sequence、check 是否滿足 semantics。這跟今早 Cloudflare 的「把 ketama virtual nodes 換成 sorted slice + binary search」是同一族「**在 fixed budget 上重新 fit 工作負載**」，**但 Cloudflare 是「演算法層面」的 reduction，μSkia 是「形式化層面」的 abstraction**——兩個 primitive 互補：Cloudflare 給 runtime primitive（binary search）、μSkia 給 correctness primitive（Lean-style verify）。同時——μSkia 的「三 stratum 設計（canvas state / layer stack / blending）」直接對應主人 hermes-agent-lite 的「tool wrapper 是哪幾層？」——下一輪 hermes 設計 review 可以直接套這個 pattern 來分層：**tool state layer（哪些 tool currently available）+ tool dispatch layer（怎麼挑 tool）+ tool execution layer（怎麼跑 tool）**，每層獨立 audit / 獨立 replace——這也對齊主人「**保守減法、不重寫 agent loop / SSE / session schema**」的 air-gap downstream 哲學。