# A Preview of DuckDB v2.0 ("Cyanoptera")
- 原始連結：https://duckdb.org/2026/08/17/duckdb-20-highlights
- HN 討論：https://news.ycombinator.com/item?id=49330781
- 作者：Mark Raasveldt 與 Hannes Mühleisen（DuckDB 共同創辦人），DuckDB 官方部落格
- 閱讀時間：2026-08-18（早間）
- 來源：Hacker News 熱門前 10 第 3 名（496 分 / 86 則留言，讀取時 2026-08-18 07:01 Asia/Taipei）

## 摘要

**主人，這一篇是 8/13 Tailscale SQLite WAL-Reset 那篇同一條 SQLite/persistence substrate axis 的延伸，但把鏡頭從「SQLite forensic post-mortem」轉到「in-process analytics database 的 substrate-identity 全面換版」——本月 SQLite/persistence 子軸線連續 5 天都沒有被點到（8/13、8/14 Litt、8/15 Opus 5、8/16 AI vs mathematicians、8/17 QuoteBench），今天是回來的本尊。** DuckDB 是 SQLite 的「OLAP 對偶」，同樣是 single-file embedded engine、同樣是 C API、同樣在主人 16GB VM 跑得起來；v2.0 把這條線推到新臨界：10,000 個 commits、breaking changes、default storage format 換版、整個 SQL parser 重寫。HN 496 分、86 則留言，sample-test 6 則子留言全部機制討論（Quack server 部署經驗、materialized view 缺口、AI-assisted commit 比例、portability 讚嘆），無 merch / sentiment。

**DuckDB v2.0 的十個 headline features，分三層——應用層、查詢層、儲存層** **應用層：** Quack server mode（in-process engine 終於接受 client/server）+ `CONNECT` SQL 語句把遠端 DuckDB / PostgreSQL / MySQL 當成本地 ATTACH，搭配新的 remote pushdown optimizer 把 SQL 直接丟到 PostgreSQL/MySQL 執行；自建 extension repository（sign with RSA public key, pin 到 `duckdb_extension_repositories()` table function）讓第三方可以 host 自己的 .so。**查詢層：** VARIANT type 從 v1.5 的「JSON on steroids」升級到 end-to-end shredded execution（pushdown extraction、shredded read/write for Parquet、`variant_*` 函式家族），triggers 完整支援（BEFORE/AFTER、ROW/STATEMENT、REFERENCING OLD/NEW TABLE、`RETURNING`），新的 SQL 方言加 `NEAREST` joins（向量搜尋變成 join 子句）、CTE 內 DML、nested schemas、`$variable` 語法、JSON mutation 函式家族、recursive CTE `USING KEY` aggregation。**儲存層：** Storage Format v2.0（buffer-managed ART indexes 改成可 page-in，`DICT_FSST` 字串壓縮預設啟用，column metadata lazy load），全新的 PEG-based SQL parser（脫離 PostgreSQL parser 血統，extension 可以 hook 進 grammar 本身，支援 `SET dialect_compatibility_mode = 'spark'`），ICU library 完全移除（timezone data 從 IANA 內建 + FSST 壓縮到 45 kB，v1.5 ICU 全套可以丟掉）。

**為什麼這是主人會想讀的——SQLite/persistence substrate-identity 換版，加上 multiple concrete numbers** 主人 MEMORY 寫「主人目前大量使用 NousResearch/hermes-agent 與 nesquena/hermes-webui」並且 anti-pattern 列了 SQLite/cloudflare/agent；本月 8/13 Tailscale 那篇是「SQLite forensic post-mortem」、8/24 how-sqlite-is-tested 是「SQLite testing methodology」，今天是 SQLite 的姐妹 engine 的 substrate-identity 換版——儲存格式、SQL parser、C API、字串壓縮、ICU 移除——每個都是 substrate 層級的 breaking change。文章列出 4 個 headline numbers：recursive CTE 1M edges 從 v1.5.4 的 4.90s 降到 v2.0 preview 的 0.12s（**40× 加速**）、`ts AT TIME ZONE 'Europe/Paris'` 25M rows 從 0.24s 降到 0.11s（2.2× 加速）、German collation 5M rows 從 0.15s 降到 0.06s（2.6× 加速）、ICU library 完全移除（45 kB IANA timezone data vs 整套 ICU ~5 MB）。**40× 是本月所有 SQLite/persistence 子軸線的 pick 裡最強的單一數字**——比 8/13 Tailscale 的「單一 SQL 語句造成 silent data loss」、8/24 SQLite testing 的「覆蓋率 100% MC/DC」都更具備 owner-actionability 的形狀。

**主人 hermes-agent-lite 的三條可移植 primitive（從文章 → 主人既有 stack）** 第一，**「VARIANT + shredded execution」是 16GB VM 上做 log/session ingest 的默認答案**——主人 session cache 或 transcript 持久化目前用 SQLite 的話，要嘛塞 JSON text（沒辦法 query）、要嘛拆 schema（破壞 evolution）；VARIANT type 的 shredded execution 是「不宣告 schema 也能 pushdown extraction + 壓縮好 + 查詢快」的現成解，預計 v2.0 之後 JSON type 也會被 VARIANT 取代。第二，**「stable C API + 自建 extension repository」是 horo-agent 跨版本 ABI 的模板**——DuckDB 把 C API 改成 declarative YAML spec（`api_spec/` 目錄）、CI 驗證 header 對齊 spec、unified symbol versioning、custom allocation handlers、static linking；horo-agent 跨 hermes-agent-lite 版本的 FFI / Python C extension 也應該走同一條路：declarative spec + versioned header + symbol versioning，而不是每次大改就 break。第三，**「CONNECT + remote pushdown optimizer」對主人的 n8n / Postgres / Cloudflare D1 棧是直接 win**——DuckDB 的 `CONNECT 'postgres://...'` 不需要 ETL、不需要 materialized view，query 直接推到 PostgreSQL 跑；主人若有 local-first analytics workflow，這個比 pandas.read_sql + 整批拉回記憶體乾淨得多。

**主人本月 SQLite/persistence 子軸線的最新一塊** 8/13 Tailscale SQLite WAL-Reset（forensic post-mortem）、8/24 how-sqlite-is-tested（testing methodology）、今天 8/18 DuckDB v2.0（substrate-identity 換版 + 40× 加速）——本週三篇覆蓋 SQLite family 的三個視角：事故 / 測試 / substrate evolution。DuckDB 是 SQLite 在 OLAP 端的 sister engine，主人若考慮把 hermes-agent-lite session log / agent trace 的 OLAP 查詢層從 SQLite 升級到 DuckDB，今天的文章是最權威的時機點（v2.0 fall release，preview build 已可下載）。

## 3W1H 分析

**What（做了什麼/主題）:**
DuckDB 共同創辦人 Mark Raasveldt 與 Hannes Mühleisen 發布 v2.0 "Cyanoptera" preview post，列出 10 個 headline features：Quack server mode + `CONNECT` SQL、end-to-end shredded VARIANT、完整 triggers、新 SQL 方言（NEAREST joins、CTE 內 DML、nested schemas、$variable、JSON mutation、recursive CTE USING KEY）、asynchronous I/O（Parquet/CSV/file format）、40× recursive CTE 加速、Storage Format v2.0（buffer-managed ART indexes、DICT_FSST 預設、lazy column metadata）、全新 PEG-based SQL parser（脫離 PostgreSQL parser 血統，extension 可 hook grammar，dialect_compatibility_mode）、ICU 移除（45 kB IANA timezone data，2.2×/2.6× 加速）、stable C API + declarative YAML spec + 自建 extension repository（RSA 簽章 + pinned public keys）。Release 命名 "Cyanoptera"（cinnamon teal 鴨），預計 fall 2026 正式 release，preview build 已可從 duckdb.org/install/preview 下載。

**Why（為什麼重要）:**
主人本月 SQLite/persistence substrate axis 連續 5 天被 agent-harness 軸線蓋過（8/14 Litt、8/15 Opus 5、8/16 AI vs mathematicians、8/17 QuoteBench），今天是回到本尊的一天。DuckDB v2.0 的三層架構（應用層 Quack、查詢層 VARIANT + triggers + new dialect、儲存層 Storage Format v2.0 + new SQL parser + ICU 移除）每一層都是 breaking change，與 8/13 Tailscale SQLite WAL-Reset bug 是同樣的 substrate-identity family：**in-process single-file embedded engine 的大版本 bump，每一個 primitive 都是「下一個版本預設行為換掉」的型態**。主人 MEMORY 寫「主人 anti-pattern: ... 給選項前先主動搜尋是否有現成 self-host 平台」——DuckDB v2.0 正好是這個 anti-pattern 的反面：它本身就是一個「self-host 平台」（Quack server mode + 自建 extension repository 讓主人可以 host 自己的 DuckDB / 自己的 extensions），主人若有 local-first analytics workflow 或 hermes-agent-lite 想要 build 一個 substrate-aware session OLAP 層，v2.0 是 timing point。對主人 16GB VM 來說：DuckDB 是「在 SQLite 跑得起的硬體上跑得起的 in-process analytics engine」——這跟 frontier model release 是相反的規模需求，主人 VM 友善。

**How（如何運作/實作）:**
鴨三個 headline 的技術機制：(1) **Quack server mode + `CONNECT`**：在 server 端 `CALL quack_serve(token='...')` 啟動 native protocol，client 端 `ATTACH 'quack:server.example.com' AS qk (TOKEN '...'); CONNECT qk; SELECT count(*) FROM events;` 把 session 指向遠端 DuckDB；搭配新 remote pushdown optimizer（PR #22914）對 PostgreSQL / MySQL 也 work，`CONNECT 'postgres://localhost/mydb'` 直接把 SQL 推到 PostgreSQL 執行，不拉整批表回 client。(2) **VARIANT shredded execution**：`'{"user":{"id":42}}'::JSON::VARIANT` 自動偵測 common structure，shred 成 columnar storage；v2.0 把這條 pipeline 從 ingest 延伸到 query（shredded execution 直從 storage, PR #20912）+ scan pushdown (PR #22478) + Parquet read/write shredded，搭配 `variant_type` / `variant_keys` / `variant_contains` 函式家族；JSON mutation functions `json_set/insert/replace/remove` (PR #23786) 終於能 in-place 改 JSON。(3) **Storage Format v2.0**：buffer-managed ART indexes（PR #21458, #23605）把 indexes 從 pinned-in-memory 改成 pageable，wide tables 開啟更快；column metadata lazy load (PR #22333)、`DICT_FSST` 預設 (PR #23733)、deletes 緊湊存 (PR #24336)、更強的 read-time corruption validation。對主人來說可移植的實作點有三：(a) **horo-agent session log OLAP 層**：先用 `pip install duckdb` 把 DuckDB v2.0 preview build 跑起來（`uv pip install --pre duckdb==2.0.0.dev*`），用 `CREATE TABLE transcripts (id INTEGER, payload VARIANT)` 試 ingest 主人現有 session log，量測 `SELECT count(*), variant_type(payload) FROM transcripts GROUP BY 2` 的 throughput——若比主人目前 SQLite + JSON-text query 快 5× 以上，就是換的訊號；(b) **horo-agent 跨版本 C extension 模板**：照 DuckDB 的 `api_spec/` declarative YAML 寫一份 horo-agent 的 `api_spec.yaml`，把 Python C extension 的 header 從 C code 抽出來由 YAML 生成，加 CI 驗證 header 對齊 spec；(c) **stable repository pinning**：自建 horo-agent extension repository（`https://extensions.horo.example.com`），用 RSA public key sign 每個 .so，session 啟動時 `CREATE EXTENSION REPOSITORY horo_repo FROM 'https://extensions.horo.example.com' USING PUBLIC KEY '-----BEGIN PUBLIC KEY----- ...'` pin 進去，token rotation 透過 trust multiple keys。

**Insight（個人心得）:**
主人可以把今天的「VARIANT + shredded execution」具體化成 hermes-agent-lite session log schema 的一條規則——**「agent 寫出的 session content 一律存成 VARIANT，不宣告 schema；query 時才用 `variant_*` 函式家族抽出來」**。這個 primitive 比 8/04 KV-cache 8-byte tag 更直接命中主人自己的 working style：主人 cron 每天跑的 transcript / log 都還沒決定最終 schema，先用 VARIANT 落地、未來 query 不需要 migrate，整個家族只需要一個 SQL 語法 (`SELECT variant_keys(payload) FROM session_log LIMIT 1`) 就能 inspect 結構。**第二層（horo-agent extension ABI）**可以在 hermes-agent-lite 的 C extension 入口加一個 `api_spec.yaml`：先列出現有 Python C extension 用的所有 entry points，每個 entry point 加 `version_added`、`deprecated_in`、`replaced_by` 三個欄位，CI 跑 `python scripts/verify_abi.py` 驗證 header 跟 spec 一致——這個 primitive 跟 8/13 SQLite version pin 3.51.3、8/04 KV-cache 8-byte tag 是同一個 family：**「把看不見的 substrate 行為攔截下來變成 visible signal / artifact」**——只是今天攔截的是 extension ABI 的演化路徑，不是寫入或儲存競態。命名建議：把 horo-agent 的 extension repository 叫 `horo-registry`，對齊 DuckDB 的 `duckdb_extension_repositories()` table function；主人今天下載 DuckDB v2.0 preview build、把現有 session log ingest 進去、`SELECT count(*), variant_type(payload)` 看一輪，就是最便宜的驗證——**這個 30 分鐘 prototype 比寫 100 行 C extension 抽象層更直接告訴主人 v2.0 是不是真的 owner-actionable**。**這篇是本月第一篇把 SQLite family 從「事故 / 測試」拉到「substrate-identity evolution」視角的 article——比 8/13 Tailscale 的 forensic post-mortem 更具備 forward-looking actionability（preview build 已經能跑了），比 8/24 SQLite testing 更具備 substrate-mapping 強度（DuckDB 是 SQLite 的 OLAP sister engine，主人想升級 OLAP 層時這篇就是 spec）。**