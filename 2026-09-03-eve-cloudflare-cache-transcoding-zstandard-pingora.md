# How we could save petabytes of cache storage with Zstandard and Pingora

- 原始連結: https://blog.cloudflare.com/how-we-could-save-petabytes-of-cache-storage-with-zstandard-and-pingora/
- 來源: Cloudflare Blog — Cache Storage + Pingora engineering（**09-01 12:59 UTC 發表** = 09-01 20:59 台北，距 EVE cron fire 約 45 小時；雖未嚴格觸發 today-published override、但屬於**兩日內**且 still-valid pick）
- 前作脈絡: https://blog.cloudflare.com/how-we-built-pingora-the-proxy-that-connects-cloudflare-to-the-internet/ （Pingora 2022 年 Rust HTTP proxy 介紹）+ https://blog.cloudflare.com/dns-cache-memory-optimization-1111/ （09-02 EVE 揀的 Big Pineapple DNS 快取記憶體 layout 改進）
- 姊妹文: https://blog.cloudflare.com/tiered-cache-smart-topology/ （Tiered Cache 拓樸）
- 閱讀時間: 2026-09-03（晚間）

## 摘要

**本文核心：** 一位 Cloudflare 1.1.1.1 Intern Program 實習生在 Pingora proxy 內側加了 **Cache Transcoding** prototype —— 把 eligible HTTP response 在寫進磁碟前用 **Zstandard level 3** 編碼，cross-tier 傳輸保留壓縮形式，只在 client-facing hop 解碼一次回原始 identity。實測 1M+ request 跨 10 台 cache server，compressible text 達 **2.834× 壓縮比、單 entry decode 只要 1.56 ns/byte (~641 MB/s)、encode 4.31 ns/byte (~232 MB/s)**；作者估算全 fleet 等於多出 petabytes 的有效 cache capacity。

**為何 storage 層才做壓縮而非 origin 端：** Cloudflare 過去遵循 origin 端的 `Content-Encoding` —— origin 沒壓就 raw bytes 寫 disk、再 raw bytes 傳給下個 tier。Cache Transcoding 把壓縮這個動作**搬進 cache 自己**：以可觀察的 `200 OK` + `Content-Encoding` 未設 + `Content-Type` 是 compressible text + `Content-Length ≥ 4 KiB` 為 eligibility gates，每個 gate 各擋掉一類不該編碼的回應（image/video/font 已壓過的 21.4% request / 63.3% bytes 整批跳過；range request / chunked encoding / pre-compressed upstream 也跳過）。這個**「在 cache 邊界做 transcoding、不碰 origin 的 wire format」**的設計選擇，是本文最具體可借鏡的 primitive。

**為何 encode 一次、decode 每 serve 的 trade 是划算的：** 評估模型不是只看壓縮比，而是 amortized over reuse count —— 一個 entry 從「fill 一次」變成「serve 幾萬次」時，encode cost 4.31 ns/byte × 一次 vs decode cost 1.56 ns/byte × 幾萬次，整體 CPU 漲幅幾個 percent。**storage encoding marker** 是 cross-tier 的關鍵機制：上層 tier 已經存成 zstd 時，下層 cache 收到會看到 marker、保留 zstd 形式而不重編，避免二次壓縮 + 額外 CPU 浪費。這個 marker 是 stateless 訊號（只是 metadata 的一個 bit 概念），不需要 origin-side 改動。

**為何是 Zstandard level 3 + 4 KiB threshold：** Zstandard 2016 由 Yann Collet 在 Facebook 開源，設計目標是 speed / ratio balance —— 比 Brotli 快 42%、ratio 接近；比 gzip 快 ~11%、小一點。本文先前 Cloudflare 自己測的 browser compression 也走 zstd 為主。Level 3 是 speed-vs-CPU 的保守選擇，把 encode 推到 cache fill 不會變成 CPU bottleneck。**4 KiB threshold** 過濾掉一大堆 tiny request 但只放棄 ~1% 的 eligible bytes —— 因為 Zstandard 的 per-object overhead（frame header、dictionary 結構）在小物件上占比過高，threshold 化是「犧牲少量 storage 換大量 CPU」的 Pareto sweet spot。

**實驗方法論的兩條 production takeaway：** (a) **controlled test zone + traffic-mix corpus** —— 不在 production 全開，而是用一個可控 zone、用 Prometheus metrics 與 Jaeger traces 對齊每個 request 看 encode/decode 發生在哪個 hop；(b) **million-request 跨 10 servers 的兩組 campaign** —— 一組 Tiered Cache 關閉、一組打開，分別量 local 行為與 tier 間傳輸 —— 直接證明「Tiered Cache 開著時，壓縮形式是 cross-tier 傳輸、只 decode 在 client-facing hop」這條 topology claim。這套雙 campaign 設計比一般 single-claim benchmark 更值得 borrow。

**為何這是 intern project：** 這是作者在 Cloudflare 1.1.1.1 Intern Program 的實習成果。文章結尾直接招募 internship 與 early talent jobs —— 這條「internship → production prototype → published engineering blog → recruiting hook」是 Cloudflare 公開 engineering 系列裡少見的「internship-as-product-launch」案例，跟08-28 BotBase for Operators 的 operator UX 系列是兩個不同方向的工程培養展示。

## 3W1H 分析

- **What（做了什麼/主題）:** Cloudflare 在 Pingora proxy 內側新增 **Cache Transcoding** pipeline：對 `200 OK` + `Content-Encoding` 未設 + compressible text `Content-Type` + `Content-Length ≥ 4 KiB` 的 response，在 cache fill 時用 **Zstandard level 3** 編碼寫入磁碟，cross-tier 保留 zstd 形式不重編，client-facing hop 才 decode 回原始 bytes。實驗覆蓋 1M+ request、10 台 cache server、Tiered Cache 開/關雙 campaign。實測 compressible text 2.834× 壓縮比、encode 4.31 ns/byte (232 MB/s)、decode 1.56 ns/byte (641 MB/s)、total fleet 等於多 petabytes 有效 cache capacity + cross-DC backbone 用量降低。Storage encoding marker 確保 entry 不被多次壓縮。Internship-as-product-launch 案例，作者是 1.1.1.1 Intern Program 參與者。

- **Why（為什麼重要）:** 這是主人目前 data-plane/storage substrate arc 上**最直接的 cache pipeline 公開教材**。與 09-02 EVE 的「Big Pineapple DNS in-memory layout」是同一條軸的兩個不同座標 —— 09-02 處理**進 process 的 RAM 佈局**，本文處理**跨 process / 跨 tier 的 disk + on-wire 佈局**，兩篇合在一起就是一個完整 cache system 的兩端。**對主人 `hermes-agent-lite` / `horo-agent` 的直接可移植點**有三層。**Layer 1 — Cache Transcoding 的 eligibility gates 設計直接可抄**：主人 kanban DB / session store / memory editor 的 backing store 都用 SQLite，要不要對 metadata JSON column 做 zstd encoding（in-memory encode + on-disk compressed + decode on read）是同等 trade-off —— 主人若把 SQLite 的 `journal_mode=WAL` + `pragma mmap_size` 配上 zstd 對 metadata blob 編碼，可以省 ~60% disk I/O 但付幾 percent CPU，這跟本文 trade-off 同形。**Layer 2 — 雙 campaign benchmark methodology 直接 borrow**：controlled zone + Prometheus + Jaeger traces + Tiered-on/Tiered-off 兩組，主人現在沒有 production observability 標準，這套方法學比一般 single-claim benchmark 更接近 enterprise infra 的驗證 bar。**Layer 3 — 「internship-as-product-launch」是 Cloudflare public 內容的 niche**：Cloudflare 是少數把 intern project 直接以 production engineering post 公開的 vendor，這對主人 `horo-agent` 的「保留現有 runtime、保守減法、端到端驗證」路線是正面示範 —— 小改動、可量測、production rollout，每一步都是公開標準。

- **How（如何運作/實作）:** Cache Transcoding pipeline 細節：**(1) eligibility gates**：`200 OK` + `Content-Encoding` 未設 + `Content-Type` 屬 compressible text (HTML/JSON/CSS/JS) + `Content-Length` 已知 + ≥ 4 KiB；不符合任一條件都跳過（image/video/font、range request、chunked encoding、pre-compressed origin response、binary content 都直接 bypass）。**(2) cache miss path**：Pingora proxy 收到 origin 的 identity bytes → 用 zstd level 3 encode 一次 → 寫 disk 同時把 zstd 形式送往 lower tier；lower tier 收 zstd 寫 disk、client-facing hop 才 decode 回 identity。**(3) cache hit path**：lower tier 直接讀 zstd bytes from disk → decode → 送出 identity，不需 network transfer、也不需 encode。**(4) Tiered Cache 跨層 hit**：upper tier 已存 zstd 時，lower tier 不必重新 encode，物件直接從 upper tier 傳 zstd 到 lower tier、decode 在 client-facing hop 完成。**(5) double-encode prevention**：storage encoding marker 是 metadata 的一個 bit 概念 —— cache layer 收到物件時會看 marker、若已是 zstd 就保留 zstd 形式，不重編。**(6) 4 KiB threshold 的 Pareto sweet spot**：tiny request 過濾掉大量 Zstandard per-object frame overhead，只放棄 ~1% eligible bytes，是 CPU-vs-storage 的最優轉折。**(7) 監控與驗證**：Prometheus metrics（cache 命中率、CPU 使用率、cross-DC bandwidth）+ Jaeger traces 對齊每個 request 看 encode/decode 發生在哪個 hop；production 漸進式 rollout，每個 release 帶一個或多個改動。**(8) Benchmark 設計**：controlled test zone 配 56% A / 25% AAAA / 19% TXT-style compressible text traffic-mix corpus；million-request 跨 10 cache servers 跑 Tiered-on / Tiered-off 兩組 campaign。

- **Insight（赫蘿心得）:** 本文最具體可借鏡的不是「我們也用 zstd 壓 cache」這個表面結論，而是 **eligibility gates + storage encoding marker 兩條 primitive 對「在 storage 邊界做 transcoding 怎麼不破壞現有 wire format」的回答**。**失敗模式對映**：主人 `hermes-agent-lite` 目前 SQLite-based session store + Kanban DB 都是 raw JSON text column，沒有 metadata-level transcoding；當 session 成長到幾萬筆時，每個 entry 幾 KB 會放大成 GB 級 I/O。**對映 primitive**：把 Cloudflare 的四個 gate（status 200 / Content-Encoding unset / compressible content-type / min size）借成 SQLite 的 column-level gate（table 為 kanban_tasks / sessions / memory_notes / `is_metadata_blob=1` / blob size ≥ N byte），再用 Python `zstandard` library 在 session write path 加 encode hook、read path 加 decode hook，整個改動**不動 SQLite schema、不動 wire protocol、不動 application code**，只是一層 column-level middleware。**Storage encoding marker** 在 SQLite 對應是 `pragma application_id` 或一個 reserved column 名（例如 `_zstd_marker INTEGER`），讓下游 reader 看到 marker 就直接讀 zstd bytes、不重複 decode。**Cross-tick continuity anchor**：09-02 EVE（Big Pineapple DNS in-memory layout）+ 09-03 EVE（Cache Transcoding cross-tier storage）= 同一條 data-plane/cache substrate 軸線的**進 process 端 + 跨 process 端**。兩篇合起來給主人的 substrate blueprint 是「**先進 process 的 memory layout、再做 storage 邊界的 compression pipeline**」，這對應主人 MEMORY 裡 `horo-agent` 「保守減法、端到端驗證」的整體方針 —— 不是「一次大改 cache system」，而是分兩個 stage、每個 stage 都可量測、每個 stage 都用 production benchmark 驗證。**Anti-pattern warning**：不要把本文當「Cloudflare 用 zstd 所以我們也要用 zstd」的工具崇拜 —— zstd 對 cache transcoding 的價值在於 **speed/ratio balance + streaming-friendly API + encoder/decoder 的非對稱成本（4.31 vs 1.56 ns/byte）**，如果主人的 use case 偏「希望 decoder 比 encoder 更便宜」、以及「需要 streaming write 不需整個 payload 在 memory」、zstd 是對的；如果 use case 是「壓一次存很久、讀很多次」且資料本身已部分壓縮，gzip 或 LZ4 也許更省事。**Anti-pattern warning 2**：不要把 Cache Transcoding 的 eligibility gates 抄成「壓縮率越高越好」的 single-metric 優化 —— 本文明確說 tiny request 對 Zstandard overhead 不划算（4 KiB threshold 過濾掉大量 Zstd frame header cost），這對應到主人 SQLite metadata column 上：若 column 是幾 KB 的 single-task row，壓縮 overhead 可能吃掉省下的 I/O；column 至少要到 8-16 KB 才有 Pareto sweet spot。**Operational borrow**：本文的 **storage encoding marker** 這個 stateless bit-of-metadata pattern 可以直接 borrow 到主人 `horo-agent` 的任何「cross-tier storage」場景 —— SQLite blob / S3 object / Kafka message —— 只要加上 marker 就保證不會被中間層重複 encode。