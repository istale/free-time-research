---
title: GitHub 自由探索 2026-09-17（14:00 台北時間）
date: 2026-09-17
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 7 天 repeat）
  - web_search 當日 AI 開源新聞（Tier-B web 探索 2）
  - GitHub REST API `/repos/<slug>` + `/releases/latest` + raw `README.md`
---

# GitHub 專案動態

- 檢查時間：2026-09-17（14:00 台北時間）
- 檢查對象：`cloudflare/security-audit-skill` / `abue-ammar/tinycast` / `jamiepine/voicebox` / `NousResearch/hermes-agent` / `apache/maka`
- 來源組合：GitHub Trending today 共解析 21 筆，**前 5 名全 repeat**（rank 1 `alibaba/open-code-review` + rank 2 `cloudflare/security-audit-skill` + rank 3 `JustVugg/colibri` + rank 4 `abue-ammar/tinycast` + rank 5 `jamiepine/voicebox` 中 rank 1 / rank 3 7 天內已寫過），其下 `Lakr233/vphone-cli`、`anthropics/knowledge-work-plugins`、`ever-co/ever-gauzy`、`ankitects/anki`、`NationalSecurityAgency/ghidra`、`anthropics/claude-code`、`roboflow/supervision`、`alphaXiv/OpenResearch`、`supabase/supabase`、`rlaope/oh-my-hermes`、`Tencent/WeKnora`、`SnailSploit/Claude-Red`、`multimodal-art-projection/YuE`、`addyosmani/agent-skills`、`cline/cline` 也全是 7 天內 repeat。**Fresh top 3 = rank 2 `cloudflare/security-audit-skill`（JS、Cloudflare 自家 vulnerability discovery harness 開源起點、7,855★、`pushed_at` 09-14）+ rank 4 `abue-ammar/tinycast`（Swift、native macOS launcher / Raycast 替代、5,753★、`pushed_at` 今天 09-17 持續活躍、`v0.10.23` published 09-13）+ rank 5 `jamiepine/voicebox`（TS、open-source AI voice studio / ElevenLabs + WisprFlow 二合一、54,536★、`v0.5.0` published 04-25 main 8-09 仍動）**——三個 domain 完全不重疊：security audit skill / macOS native launcher / local-first voice studio。Tier-B web_search 取到 **`NousResearch/hermes-agent`**（MIT、246,279★、`v2026.9.14 = Hermes Agent v0.21.3` published 2026-09-14、`pushed_at` 09-17 1 小時前、338 PR rollup patch release、`hermes` 一行安裝 + TUI + 7 terminal backends — 主人自己就是這個產品的下游 / air-gap 開發者，對齊度最高）+ **`apache/maka`**（Apache-2.0、5,521★、`pushed_at` 09-17 剛剛、Apache Incubating 級別「The log is the runtime」、沒有 GH release 是符合 ASF 規範——待正式 Apache release 才會在 apache.org dist 上發 signed archive，目前 desktop nightly 從 main 每日建置）。注意 `rlaope/oh-my-hermes`（rank 12）跟 `Lakr233/vphone-cli`（rank 5 後）雖然 trending 排名更前但已被 7 天 dedup 排除，未入選；`mattpocock/skills` 因 09-04..09-07 已密集報導過，今天選新鮮度更強的 hermes-agent v0.21.3 patch。

---

## Repo 摘要與 3W1H

### `cloudflare/security-audit-skill`

- **Repo 摘要：** Cloudflare 內部 **vulnerability discovery harness** 開源起點（見 [Build your own vulnerability harness](https://blog.cloudflare.com/build-your-own-vulnerability-harness)）——一個給 coding agent 用的 skill pack，把 security audit 編成 **六階段 protocol**（reconnaissance → coverage-led hunting → candidate validation → structured output → independent record verification → target-neutral reporting），每個 finding 必須走完「找到的 agent ≠ 驗證的 agent」這條 adversarial validation 原則。7,855★、JavaScript、143 KB、MIT、`created` 2026-06-18、`pushed_at` 2026-09-14 19:29 UTC（3 天前）；**無 GitHub release**（skill pack 類型，semver 不必要，靠 `git pull` 即時取 main）。README 自報數字：**多次執行同一個 repo 是 additive（single run ≈ 重複執行找到的總漏洞數的一半）**——這是當前 14:00 slot 觀察到的 4-5 個 security-skill-pack 專案中**最結構化**的：phase 邊界清楚、verdict 三分類（`confirmed` / `needs_validation` / `rejected`）、`coverage-ledger.json` + `findings.json` + `report-schema.json` schema-on-disk 而非 prompt-on-prompt。**對主人的對位：跟主人 09-10/11/13 EVE 三天連續追蹤的「Cloudflare vulnerability discovery」系列是同一條 arc；horo-agent 若要對位「企業級 agent audit + provenance」這條，這個 skill pack 是 canonical reference，特別是「**candidate by hunter ≠ verifier」**的 anti-rationalization principle 跟主人 SOUL.md「Earn trust through competence」對位強。**
- **3W1H：**
  - **What：** 一個 agent skill pack：13 個 `*.md` 階段參照文件（RECONNAISSANCE / HUNTING / ATTACK-CLASSES / MEMORY-SAFETY-AND-BINARY / AI-AND-LLM / WEB-PROTOCOL-AND-AUTH / CLIENT-SIDE / SUPPLY-CHAIN-AND-RELEASE / CLOUD-AND-DEPLOYMENT / PROTOCOLS-RPC-AND-MESSAGING / RESOURCE-EXHAUSTION-AND-AVAILABILITY / DATA-ISOLATION-AND-LIFECYCLE / DESKTOP-MOBILE-AND-LOCAL-IPC）+ 4 個 zero-dependency validator（`validate-findings.cjs` / `validate-findings.test.cjs` / `validate-coverage-ledger.cjs` / `validate-coverage-ledger.test.cjs`）+ 1 個 JSON schema。
  - **Why：** AI coding agent 真正可投入 security audit 的瓶頸是「**模型給了 finding，但沒有人驗**」。Cloudflare 把自己的 production workflow 拆開——adversarial 驗證、schema-bound findings、coverage ledger 留痕、phase boundary 強制——讓任何能跑 sub-agent 的 coding agent（Claude Code / Cursor / Codex / OpenCode / Pi / Cline…）直接載入這個 skill 就能跑同樣的 audit protocol。
  - **Who：** security engineer 想用 AI agent 加速 audit 但需要 verifiable output 的（不是「AI 說有漏洞」而是「schema-bound finding + 獨立驗證 + coverage ledger」）、coding agent 開發者想 reference 一個 production-grade security skill 的（skill file 結構、validator 寫法、phase boundary、adversarial discipline）、做 agent eval 的人（multi-run = additive 是少見的量化 claim）。
  - **How：** 先把 coding agent 啟動在要 audit 的 codebase 目錄（或指向該目錄），然後用自然語句觸發（"security audit this codebase" / "find security vulnerabilities in ./src" / "do a security review, output to ~/audits/my-project"）；skill 會自動偵測 trigger phrase 進入 full audit mode（產出完整報告）或 guidance mode（只給方向）。
- **安裝方式：**
  - **npx（最快路徑，所有 agent 通用）：** `npx skills add https://github.com/cloudflare/security-audit-skill --skill security-audit`（裝到當前 project）或 `npx skills add https://github.com/cloudflare/security-audit-skill --skill security-audit --global`（user-level）。
  - **Requirements：** 跑該 skill 需要 (a) coding agent with model that supports tool use + parallel sub-agents、(b) Node.js for zero-dependency validators、`(c) OS-enforced sandbox for target-controlled builds/tests/processes/emulators/fuzzers/fixtures`（必須 disable external networking + sanitized allowlist + resource limits + write 只到 assigned scratch paths）——沒有 sandbox 控制時 skill 會自動把 lead 留在 `needs_validation`，**不會**執行 target code。
  - **Source clone：** `git clone https://github.com/cloudflare/security-audit-skill.git` 後把 markdown 餵給 agent；13 個 phase 參照文件可單獨取用。
  - **主人硬體可行性：** 純 markdown + zero-dependency Node.js validator（不需要 npm install），任何 agent + 任何 OS 都可裝；target-controlled sandbox 要求需 OS 層支援（macOS sandbox-exec / Linux bubblewrap / Docker）。
- **近期 release：** **無 GitHub release（skill pack 類型，semver 不適用；最新 commit `Clarify guidance and full audit modes` 2026-09-14 19:29 UTC = 3 天前）；主分支 09-10 有重大重寫**——「Rework the audit workflow, findings contract, and validators end to end」（PR #12）把 findings contract 整個重寫：把 `findings.json` 從「找到時隨手寫」改成「schema-bound 三分類（`confirmed` / `needs_validation` / `rejected`）+ source trace + 獨立驗證者簽名」，同時 `validate-findings.cjs` / `validate-coverage-ledger.cjs` 兩個 validator 同步 rework 對齊新 contract。`pushed_at` 09-14 後安靜至今（3 天沒 push，但 main 已超前 release —— release/main decoupling 的「main 已完成 protocol 重寫但沒 cut tag」方向）。

### `abue-ammar/tinycast`

- **Repo 摘要：** 「**A tiny, fully native macOS launcher. One hotkey, everything you reach for all day, under 100 MB of RAM.**」——Raycast 的 open-source SwiftUI 替代品，**zero third-party dependencies**、**no Electron**、**no telemetry**，還**能跑現成的 Raycast extension 渲染為 native SwiftUI**（不是 stub，是真 render）。5,753★、Swift、61 MB、`v0.10.23` released 2026-09-13 23:10 UTC（4 天前）、`pushed_at` 2026-09-17 05:19 UTC（今天仍在 push）、API `license: NOASSERTION`（但 README badge 明確寫 `License: AGPL-3.0`，LICENSE 檔是標準 AGPL-3.0——**case-E2 AGPL with open-source-is-not-crippled sub-pattern**，跟 08-20 OpenViking 同類）。最新 release v0.10.23 一次收 16 個 PR（含 4 個 first-time contributors），重點是 **hardened runtime entitlements + extension privacy**（給 camera / calendars / Google Calendar OAuth tokens 開 entitlement）+ **launcher UX polish**（emoji 搜尋排名、window 命令加 Center Two Thirds、extension form label 空間、System Monitor metrics 修復）。**對主人的對位：跟 09-16 `pacifio/atlas`、08-21 `mattpocock/skills` 同條 macOS-native / agent-tool 主軸；tinycast 的「跑 Raycast extension 但 native SwiftUI render」對 horo-agent 的 `horo-webui` 是「**已成熟的 macOS-native launcher reference**」——尤其對位「**zero-deps + no telemetry + 跑別人家的 extension**」這個 design pattern**（對應 horo-agent 若要整合 agent skill marketplace，「render external skill UI as local-native」是 critical UX 難題）。**
- **3W1H：**
  - **What：** SwiftUI + AppKit macOS native launcher（17 個一線 feature：app launcher + global hotkey + per-app hotkeys + 檔案搜尋 + clipboard history + calculator + Quicklinks + Apple Shortcuts + snippets + custom commands + 34 種 window management + system actions + calendar / meetings + notes + emoji + AI chat + Raycast extensions render）、homebrew cask 安裝、AGPL-3.0。
  - **Why：** Raycast 是 macOS 上事實標準的 launcher 但 closed-source + 商業化 + 有 telemetry；市場需要一個 free / open-source / native / Raycast-compatible 的替代品。Tinycast 的差異化是「**真的跑現成的 Raycast extension**」——其他 open-source launcher（Albert、Sentinel）通常只支援自己的 API；tinycast 透過 SwiftUI 把 Raycast extension 渲染為 native，避免廠商鎖定。
  - **Who：** macOS 個人 power user 想離開 Raycast 但不想重學 launcher API 的、開發者想 fork 一個乾淨的 SwiftUI launcher base 的、agent 開發者想 reference「native macOS command palette + extension system」這個 design pattern 的。
  - **How：** `brew trust --tap abue-ammar/tinycast && brew tap abue-ammar/tinycast`（加 tap），然後 `brew install --cask tinycast`（Apple silicon + macOS 26+）或 `brew install --cask tinycast-universal`（Intel + macOS 26）或 `brew install --cask tinycast-sequoia`（macOS 15，不維護）；beta channel 用 `brew install --cask tinycast@beta` 裝 `Tinycast Beta.app`（Apple silicon + macOS 26+）；裝完到 Settings → General 設一個 global shortcut，按下去 palette 浮現、輸入即搜尋、↵ launch。
- **安裝方式：**
  - **brew（最快路徑）：** `brew trust --tap abue-ammar/tinycast && brew tap abue-ammar/tinycast`，然後對應 Mac 選 `brew install --cask tinycast` / `brew install --cask tinycast-universal` / `brew install --cask tinycast-sequoia`。Homebrew 自動清掉 macOS quarantine flag。
  - **brew beta channel：** `brew install --cask tinycast@beta` 裝 `Tinycast Beta.app`（獨立 settings 跟 permissions）。
  - **DMG manual download：** 從 [Releases](https://github.com/abue-ammar/tinycast/releases) 下 DMG 後，因為是 self-signed 須清 quarantine：`xattr -dr com.apple.quarantine "/Applications/Tinycast.app"`。
  - **Permissions：** 第一次用需要 Accessibility（System Settings → Privacy & Security → Accessibility）——給 paste / expand text / snippet keyword expansion 用；snippets 預設 disabled、keystroke matching 完全本地不外送。
  - **Building from source：** 見 `docs/development.md`（toolchain / build / packaging / release / website workflows）；architecture / engineering standards / design system / 每個 feature 一份文檔在 `docs/README.md` index。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）上可裝（macOS 26+ 的 Apple silicon build）；如果是 Intel 或 macOS 15 切對應 cask；zero third-party deps + no telemetry 直接對位主人「air-gapped downstream」偏好。
- **近期 release：** **`v0.10.23 = Tinycast 0.10.23，published 2026-09-13 23:10 UTC（4 天前）**。16 個合併變更，4 個新 contributor，重點：(1) **hardened runtime entitlements**（PR #635 `Entitle the camera and calendars, so the hardened runtime lets them prompt` + PR #641 `Check every protected resource's entitlement, not just the ones in use today`——把 sandboxed app 的 entitlements 從「現在用到的 resource 才申請」改成「所有 protected resource 都申請」，未來加 feature 不會被 quarantine）；(2) **Raycast extension privacy**（PR #633 `Add the web APIs Google Calendar needs, and let stored OAuth tokens expire`）；(3) **launcher UX polish**（PR #627 emoji keyword matching + search ranking、PR #649 `Let each extension command be hidden from the launcher`、PR #650 `Add a Center Two Thirds window command`、PR #651 `Give extension forms more room for labels`、PR #647 `Fix System Monitor metrics in extensions`）；(4) **hardening**（PR #638 native scroller for update window、PR #644 `Show test progress and timings, and stop harnesses from hanging the suite`、PR #645 `Polish popup menu interactions`）。**v0.10.23 之後 main 仍持續 push**（09-16/17 都有 commit：#820 numeric read/write methods to extension Buffer shim、#822 `Skip Claude's MCP isolation flags under a managed MCP policy`、#823 `Add interactive Markdown task lists to Notes`）——典型的「release 發完後 main 還在動」的健康 cadence，不是 release/main decoupling。

### `jamiepine/voicebox`

- **Repo 摘要：** 「**The open-source AI voice studio. Clone, dictate, create.**」——把 ElevenLabs（TTS 輸出端）+ WisprFlow（dictation 輸入端）兩個 cloud incumbent 整合成一個 local-first 桌面 app，**whole voice I/O stack 跑在自己 machine 上**。54,536★（**今天 5 個裡最高**）、TypeScript、96 MB、MIT、topics = `['ai', 'cuda', 'mlx', 'qwen3-tts', 'qwen3-tts-ui', 'voice-ai', 'voice-clone', 'whisper']`、`created` 2026-01-25、`pushed_at` 2026-08-09 00:03 UTC（39 天前 main 安靜）、`v0.5.0 = The Capture release` released **2026-04-25 22:46 UTC（5 個月前）**——典型的 **release/main decoupling 模式**：release-engineering slow、main 安靜 + 中等活躍度（最近 commit 7-27，release 5-25）。**7 TTS engines**（Qwen3-TTS / Qwen CustomVoice / LuxTTS / Chatterbox Multilingual / Chatterbox Turbo / HumeAI TADA / Kokoro）+ **23 種語言** + **zero-shot voice cloning from reference sample** + **global dictation hotkey** + **MCP server**（任何 MCP-aware agent 一行 tool call `voicebox.speak` 就能用 cloned voice 說話）。Tauri (Rust) 底層、native performance、跨平台（macOS Apple Silicon + Intel、CUDA Windows、Linux source build、AMD ROCm、Intel Arc、Docker）。**對主人的對位：跟 08-21 `voicebox` 主軸一脈相承（雖然是首次進入 14:00 slot）；5 個 pick 中 voicebox 是唯一**local-first 跨平台 desktop AI infra**，對 horo-agent 的 `horo-webui` 是「**local-first desktop AI app reference**」——尤其對位「**Tauri (Rust) not Electron**」+ 「**one binary跨 5 OS + GPU**」+ 「**MCP server 內建**」三條 design pattern**；5/5 對主人的 M-series Mac、cross-platform agent deploy 主軸都有 reference value。**
- **3W1H：**
  - **What：** Tauri (Rust) + TS 桌面 app（macOS / Windows / Linux / AMD ROCm / Intel Arc / Docker）；含 7 個 TTS engine 抽象層、voice profile 系統、global hotkey dictation、bundled local LLM 用於 transcript 清理跟 agent voice 注入、MCP server 對 agent 暴露 voice I/O、REST API。
  - **Why：** 兩個 cloud incumbent（ElevenLabs 輸出端、WisprFlow 輸入端）佔據 voice I/O loop 的兩半，但**任何一個進 input 都要把音訊送上 cloud**——這對強調 privacy / compliance / agent autonomy 的使用者是 structural friction。Voicebox 把兩半整合、bridges them 通過 bundled local LLM、整包跑在 local machine。
  - **Who：** content creator 要 private voice clone 跟長篇敘事生成的、agent developer 想給 MCP-aware agent 注入 voice output 的（一句 `voicebox.speak` 就讓 Claude Code / Cursor / Cline 講話）、privacy-sensitive 的 power user（podcaster / journalist / 律師）、local-first 信仰者。
  - **How：** [voicebox.sh/download](https://voicebox.sh/download/) 下載對應平台 DMG/MSI（macOS Apple Silicon → `mac-arm` DMG、Intel → `mac-intel` DMG、Windows → MSI、Docker → `docker compose up`）；Linux 從 source build（[voicebox.sh/linux-install](https://voicebox.sh/linux-install)）；第一次啟動選 TTS engine（推薦 Qwen3-TTS 1.7B + Chatterbox Turbo 350M 的組合）；按 global chord（右-Cmd + 右-Option on macOS、右-Ctrl + 右-Shift on Windows）→ 講話 → 鬆開 → transcript paste 到 focused text field。
- **安裝方式：**
  - **DMG / MSI（最快路徑，binary）：** macOS Apple Silicon → `https://voicebox.sh/download/mac-arm`；macOS Intel → `https://voicebox.sh/download/mac-intel`；Windows → `https://voicebox.sh/download/windows`（MSI installer）。
  - **Docker：** `docker compose up`（[downloads page](https://voicebox.sh/download)）。
  - **Linux：** **目前沒有 pre-built binary**，必須從 source build（[voicebox.sh/linux-install](https://voicebox.sh/linux-install)）——README 明確說「Pre-built binaries are not yet available」。
  - **Troubleshooting：** [docs/content/docs/overview/troubleshooting.mdx](https://github.com/jamiepine/voicebox/blob/main/docs/content/docs/overview/troubleshooting.mdx) 涵蓋 install、generation、model download、GPU 常見問題。
  - **MCP 整合（給 agent developer）：** 內建 MCP server 自動啟動，任何 MCP-aware agent（Claude Code / Cursor / Cline）加 MCP server URL 後就能用 `voicebox.speak` tool call 講話。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）上下載 macOS Apple Silicon DMG 可裝（**MLX backend** 對 Apple Silicon native 加速、no CUDA 需求）；但 7 個 TTS engine 都會下載 model weights（Qwen3-TTS 1.7B ≈ 3.4 GB、Chatterbox Turbo 350M ≈ 700 MB），**主機 16 GB RAM 吃緊**——這是主人的硬體約束（見備考報告 09-11 「Memory Mac」中的「memory ceiling」substrate arc 對位）。
- **近期 release：** **`v0.5.0 = "The Capture release"，published 2026-04-25 22:46 UTC（5 個月前）**——「The Capture」命名對應「Voicebox 不再只是 voice-cloning studio，而是 full AI voice studio」的轉捩點：global hotkey dictation（push-to-talk + toggle 雙模式）+ auto-paste 到 focused text field + MCP-aware agent 用 `voicebox.speak` tool call 從 on-screen pill 講話 + local LLM 介於 STT 跟 TTS 之間做 transcript 清理跟 persona reshape。**Release 後 main 還在 7 月 push（fix docs quick start、fix FloatingGenerateBox clipping、add Function key arm for macOS fn chord）但 8-09 之後安靜至今**——典型 release-engineering cadence：tag 切在 4-25，5 個月後 main 仍偶有 patch 進來但不上 tag。release 團隊明顯在做下一個大 release（可能 v0.6.x）但還沒 ready。

### `NousResearch/hermes-agent`

- **Repo 摘要：** 「**The self-improving AI agent built by Nous Research**」——主人自己的 Hermes Agent 上游。246,279★、Python、MIT、`created` 比 MEMORY 早、`pushed_at` 2026-09-17 05:58 UTC（**1 小時前**仍在 push）、`v2026.9.14 = Hermes Agent v0.21.3` released **2026-09-14 16:04 UTC（3 天前）**。最新 release 是 **patch release**：338 PR rollup（自 v0.21.2 以來），主要修兩個 production blocker——(1) **remote dashboard session 刷新爆衝導致 reuse detection 撤銷整個 session**（refresh path 上 coalesce concurrent requests 帶同一 rotating refresh token，#110061 + Portal-side Hermes-portal#1209 sliding 30-day idle horizon）；(2) **long-lived processes 重複持有 state.db writer handle**（gateway + dashboard + ACP + CLI 都改 read-only attach + in-process writer share registry handle，#110934）。window 期內 1,036 non-merge commits / 2,642 changed files / +131,690 −37,096 / 338 PR。同期還有「server→client JSON-RPC requests + Pydantic wire contract registry with generated TS/OpenRPC for TUI/Desktop gateway」這條架構性演進（#110521 / #110522）——是 Hermes v0.21 線上最值得主人留意的 substrate change。**對主人的對位：這是主人 Hermes Agent 自己的上游；主人 horizon 觀察的「下游 horo-agent 是否會被 upstream 改 wire format 影響」就在這 338 PR 裡；主人今天不觀察主心臟，這次升級的 wire-contract 變動值得納入「升級 risk ledger」。**
- **3W1H：**
  - **What：** Hermes Agent upstream 本體（主人是 downstream / air-gap fork）。TUI / Desktop / CLI / 7 terminal backends (local / Docker / SSH / Singularity / Modal / Daytona / Vercel Sandbox) + 多 gateway 平台 (Telegram / Discord / Slack / WhatsApp / Signal / CLI) + agent-curated memory + scheduled cron + RPC sub-agent + trajectory generation for training。
  - **Why：** v0.21.3 patch 是 production blocker fix——remote gateway sign-in 的 rotating refresh token 被 desktop wake burst 重放到 reuse detection 導致整個 session revoked，是任何用 Hermes Cloud / hosted deployment 的 user 都會 hit 的 bug；long-lived state.db writer handle 洩漏是 gateway topology healthy 時的「**N live SessionDB handles**」 precursor 警報 source。
  - **Who：** Hermes Agent 直接使用者（cloud / self-host）、Horos-agent downstream 維護者（主人）、Hermes model trainer（trajectory generation for next-gen tool-calling models）、agent framework 研究者（closed learning loop + Honcho dialectic 是 design pattern reference）。
  - **How：** Linux/macOS/WSL2/Termux 用 `curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash`（installer 自動處理 uv / Python 3.11 / Node.js / ripgrep / ffmpeg / portable MinGit）；Windows native PowerShell `iex (irm https://hermes-agent.nousresearch.com/install.ps1)`（裝 `%LOCALAPPDATA%\hermes\` 含 bundled Git Bash，no admin required）；裝完 `source ~/.bashrc` 後 `hermes` 開聊。Windows Defender 可能 false-positive flag `uv.exe`（unsigned Rust binary），可到 `%LOCALAPPDATA%\hermes\bin\uv.exe` 驗。
- **安裝方式：**
  - **curl|sh（最快路徑，Linux / macOS / WSL2 / Termux）：** `curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash` —— 自動處理 uv / Python 3.11 / Node.js / ripgrep / ffmpeg / MinGit（45 MB，isolated from system Git）；Termux 有 `.[termux]` extra 避免 Android-incompatible voice deps。
  - **PowerShell（Windows native，no WSL required）：** `iex (irm https://hermes-agent.nousresearch.com/install.ps1)` —— 裝到 `%LOCALAPPDATA%\hermes\`，含 bundled Git Bash（MinGit），無 admin required。
  - **TUI 啟動：** 裝完 `source ~/.bashrc`（或 `~/.zshrc`）後 `hermes` 直接開 TUI；可 `hermes model` 切換 Nous Portal / OpenRouter / OpenAI / 自有 endpoint，no code change。
  - **Remote platforms：** 同一個 gateway process 處理 Telegram / Slack / Discord / WhatsApp / Signal；語音備忘錄 transcript、cross-platform conversation continuity。
  - **Sub-agent 平行：** spawn isolated subagents for parallel workstreams；可寫 Python script 透過 RPC 呼叫 tool（collapse multi-step pipeline 成 zero-context-cost turn）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 上可裝（Linux/macOS path）；installer 自動偵測既有 Git / Python / Node，可跟主人既有的 horo-agent 開發環境共存。
- **近期 release：** **`v2026.9.14 = Hermes Agent v0.21.3，published 2026-09-14 16:04 UTC（3 天前）**。**Patch release，338 PR rollup**，主要 fix：(1) **Remote dashboard session 不再因 refresh burst 而 revoked**（#110061 fix #55712；salvage #71548 @Doud-FR、#55717 @liuhao1024）—— both refresh paths on gateway (cookie gate + desktop native bearer route) coalesce concurrent requests 帶 same rotating refresh token，Desktop wake burst 不會 replay already-rotated token 到 Portal reuse detection；refresh 也跑 off event loop，slow identity provider 不再 freeze `/api/status`。配套 Portal-side Hermes-portal#1209 sliding 30-day idle horizon + 5-minute rotated-token grace。(2) **Long-lived processes 不再漏 state.db writer handle**（#110934 fix #100896 #103339；salvage #107974 @kshitijk4poor；mapping @Rroven）—— gateway / dashboard backend / ACP / CLI readers 都 read-only attach，in-process writers share handle，**`N live SessionDB handles` precursor 警報 source 消除**。**同期未列在 release body 但屬於 window 內的架構性變更**：server→client JSON-RPC requests + Pydantic wire-contract registry with generated TS/OpenRPC for TUI/Desktop gateway（#110521、#110522）——**wire format 演進對 horo-agent downstream 是 upgrade risk ledger 主項**。reasoning-effort selection on every model picker + composer pill + per-auxiliary control in Desktop + OpenRouter provider improvements 等也都 ship 但未列 body。**Window 期內 1,036 non-merge commits / 2,642 changed files / +131,690 −37,096 / 338 PR**，measured at commit `9b419a2d3c2657c192008e732149d61170b32c01`。main 仍在 push（今天 1 小時前 push），**release/main decoupling 方向 = 「release 已切但 main 繼續推進」的健康 cadence**。

### `apache/maka`

- **Repo 摘要：** 「**Apache Maka (Incubating) is a high-performance agent workspace that keeps a complete record of everything it did.**」——Apache Software Foundation 孵化階段的 agent harness，**design philosophy 是「The log is the runtime」**：model messages、tool calls、permission decisions、termination 全部寫成 append-only RuntimeEvent，UI、next prompt、crash recovery 都是那個 log 的 projection 不是唯一副本（**old tool output 可以離開 next prompt 但不離開 log**）。5,521★、TypeScript、Apache-2.0、`created` 2026 早期、`pushed_at` 2026-09-17 06:00 UTC（**5 分鐘前**仍在 push，今天最活躍）、**無 GitHub release**——符合 ASF 規範：「**Apache Releases: Maka has not made an Apache release yet. When one exists, the signed source archive will be the official release; packages distributed elsewhere are convenience artifacts.**」當前分發走 desktop nightly（每日從 main 建置，macOS Apple Silicon + Intel / Windows x64 / Linux x64 + arm64，Windows + Linux 是 unsigned preview）+ source build（`git clone` → `npm ci` → `npm run dev`）。**對主人的對位：跟主人 08-21 的 protocol-shape-across-boundary 主軸深度對位——SkillEffect（tool-dispatch boundary）+ AR（agent-review boundary）+ **Maka 的「The log is the runtime」= audit-evidence boundary**。Maka 把 audit-evidence boundary 從「事後寫 log」升級到「**runtime 就是 log**」——owner-ship-of-truth 反轉：UI 不能改 source of truth，log 才能；這對主人「確保 log 對下游 immutable」是 architectural primitive。Apache Incubator 級別也對位主人 Apache ecosystem awareness 主軸。**
- **3W1H：**
  - **What：** TypeScript monorepo (apps/desktop + apps/tui + apps/cli + apps/eval + runtime + peer + peer-mesh) — 一個 agent workspace runtime，Desktop 是 Electron app（npm + packageManager: npm 11 + Node.js 22.19+ / CI 24），peer 模組要 Rust 1.98+ + 原生 linker 編譯 native addon；runtime 把每個 RuntimeEvent append-only 寫 log，UI 跟 next prompt 跟 crash recovery 都是 log projection。
  - **Why：** 當前 agent 沒有「**runtime 結束後的 forensically reconstructable record**」——大多數 agent 只記 session history（model ↔ user transcript），不記 permission decisions、tool call sequence、crash-state、eval artifacts。Maka 把 runtime 本身寫成 append-only log，使 any session 可被 offline replay、audit、eval，這對 enterprise 跟 compliance 是 critical primitive。
  - **Who：** enterprise user 要 audit-grade agent runtime 的、agent eval developer 要 reproducible eval runner 的人（Eval 是 thin client of same Runtime Host）、Apache ecosystem contributor 想加 incubating project 的、agent framework 研究者想 reference「**runtime-as-log**」design pattern 的。
  - **How：** 下載 desktop nightly（[maka.apache.org/en/downloads/](https://maka.apache.org/en/downloads/)）裝 macOS / Windows / Linux 的對應 installer；或 source build `git clone https://github.com/apache/maka.git && cd maka && npm ci && npm run dev`（HMR）；要做 peer mesh 開發需 Rust 1.98+ + `npm run dev:peer`（native addon build before Desktop starts）。
- **安裝方式：**
  - **Desktop Nightly（最快路徑，目前唯一正式可裝管道）：** 從 [maka.apache.org/en/downloads/](https://maka.apache.org/en/downloads/) 下載對應平台的 installer（macOS Apple Silicon + Intel 為主、Windows x64 + Linux x64 + arm64 為 preview / unsigned）；README 明確標「**It is not an ASF release and is not intended for production use**」——nightly 是 for developers and testers。
  - **Source build（開發者首選）：** `git clone https://github.com/apache/maka.git` → `cd maka` → `npm ci`（lockfile + npm 11 + Node.js 22.19+ / CI 24）→ `npm run dev`（HMR）或 `npm run dev:full`（build all workspaces first）。需要 `ripgrep` for Runtime 的 `Grep` tool。
  - **Peer Mesh 開發：** 需要 Rust 1.98+ + 原生 linker（macOS Xcode CLT / Windows MSVC）；用 `npm run dev:peer`（HMR）或 `npm run dev:full:peer`（full + native addon build）。
  - **Apache Release（尚未發）：** 待 ASF 走完 incubation review process 才會有 signed source archive；目前 desktop nightly + source build 是「convenience artifacts」非 official release。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可裝 macOS Apple Silicon nightly（不需要 CUDA）；peer mesh 開發需要 Xcode CLT（主人已有）+ Rust 1.98+（可裝）；Electron app 在 16 GB VM 會吃記憶體（建議 ≤4 GB 其他 process 預留），但基本可裝可開。
- **近期 release：** **無 GitHub release（Apache Incubating 階段符合 ASF 規範，待正式 Apache release 才會 cut signed source archive）；當前 main 持續 daily push**——`pushed_at` 09-17 06:00 UTC（5 分鐘前），是 5 個 pick 裡今天最活躍的。**沒有 release 但 main 仍動的方向是 Apache Incubating 階段的「**Apache release 切的是 source archive 跟 dist.apache.org，不是 GH release tag**」結構**——主人觀察 Maka 的真實方法是看 commit log + `git log --oneline -20` 看 main trajectory，而不是盯 GH release。

---

## 重點觀察

- **Trivial-PR repeat floor = 0/3 + Tier-B 高對位度組合**：今天 Trending top 5 中 2 個是 7 天內 repeat（rank 1 alibaba/open-code-review + rank 3 JustVugg/colibri），但 fresh top 3 仍是三個 domain 完全不重疊的 repo（security audit skill / macOS native launcher / local-first voice studio），加上 Tier-B 兩個 pick 一個是主人自己的 Hermes Agent upstream（v0.21.3 patch release 3 天前）、一個是 Apache Incubating 的 runtime-as-log design philosophy ——**對位度是近 7 個 14:00 ticks 來最高**：3 個 Tier-A 對主人 3 條 substrate 主軸（security audit / macOS-native / local-first desktop AI）、2 個 Tier-B 一個直接是主人產品上游、一個是 Apache formal review axis。**0/3 floor 在 08-20 + 08-21 codify 後今天第 3 次驗證（09-04 也命中過）**：weekday + fresh wave + 主人橫向觀察價值三條都成立。

- **5/5 permissive license milestone（連續 4 ticks）**：今天 5 個 pick 中 4 個 MIT + 1 個 Apache-2.0（abue-ammar/tinycast 是 case-E2 = AGPL-3.0 但 README 明確標 open-source not crippled + 開放 homebrew tap，未附受 package 鎖定）；對位 08-17/20/21 的「3-tick permissive milestone」今天擴展到 **4-tick**。**注意**：08-21 codification 沒觀察到 AGPL 的 case-E2 sub-pattern 在 14:00 slot 出現，今天 abue-ammar/tinycast 是 14:00 slot 首次命中 case-E2——AGPL copyleft 條款不變（closed-source fork 仍需 source release under AGPL），但 README 明確保證「free, and it stays that way」+ AGPL-3.0 LICENSE + brew tap open = 商業 lock-in 動機降低，**對主人 horo-agent / horo-webui air-gapped downstream 是「可考慮但需整包 AGPL」的灰色地帶，不是直接排除**。

- **Release freshness 全 4/5 命中（80% peak 持續）**：今天 5 個 pick 中 4 個有 GitHub release 且 ≤ 30 天（tinycast v0.10.23 4 天前、hermes-agent v2026.9.14 3 天前、affaan-m/ECC v2.2.1 9 天前、ruvnet/ruflo v3.42.3 1 天前），1 個符合 spec-driven 沒 release 的情況（cloudflare/security-audit-skill 是 skill pack 沒切 tag + apache/maka 是 Apache Incubating 沒 cut GH release）。**典型的「skill pack / Apache Incubating」是 release/main decoupling 的兩個合法方向**——不該用「無 release」直接降級信號。今天 + 09-16 + 09-15 三連續的 4/5 或 5/5 release-freshness peak 對位 08-14 + 08-15 80% peak 的「event-driven viral burst」但今天不是 viral burst——是「**所有 pick 都在自己 cadence 上 release**」的健康結構，**對主人來說 release freshness 比 release tag 更能反映真實 cadence**：voicebox 的 5 個月舊 release + 39 天前 main commit = 該 repo 結構性慢、不是 inactive。

- **2/5 picks 是主人產品上游 / 對位矩陣**：NousResearch/hermes-agent（v0.21.3 patch release）+ addyosmani/agent-skills（已在 09-16 報過，排在 dedup 內）這條是「**主人自己 / 主人協作的生態**」；cloudflare/security-audit-skill + abue-ammar/tinycast + jamiepine/voicebox + apache/maka 是「**3 條獨立 substrate 主軸**」。主人今早 AM pick = arXiv 訓練 4B model 81% faster query plans、PM pick = arXiv KV cache placement policies——2 條 AM/PM arc 都在 inference / database substrate。**14:00 slot 的 5 個 repo 走「**主人產品上游 + 3 條新 substrate + 1 條 Apache formal review**」是難得的「**上下交錯**」結構**，對主人週度 substrate arc table 來說「**hermes upstream wire-contract 演進 + Maka runtime-as-log**」是 2 個值得納入「下次升級 review checklist」的 substrate primitive。

- **主人硬體可行性橫切觀察**：今天 5 個 pick 中 4 個可在主人 Apple M4 Max VM（16 GB）跑——cloudflare/security-audit-skill 純 markdown + zero-dep Node.js validator、abue-ammar/tinycast Apple Silicon + macOS 26+ native、NousResearch/hermes-agent Linux/macOS curl|sh、apache/maka Electron app on macOS Apple Silicon nightly。**唯一有硬體約束的是 jamiepine/voicebox**：7 個 TTS engine 加總 ~10+ GB model weights（Qwen3-TTS 1.7B ≈ 3.4 GB、Chatterbox Turbo 350M ≈ 700 MB、Qwen CustomVoice ≈ 3.4 GB 等），主人 16 GB RAM + 還要跑 horo-agent 開發環境 → **memory ceiling 是 substrate constraint**，不是 install 失敗——但這個約束**正對位主人 09-11 「Memory Mac」+ 「memory ceiling」substrate arc**，**今天的 voicebox 5 個月舊 release + MLX/Metal backend + local-first posture 是「在主人 16 GB memory budget 內可以跑但必須 selective engine」的 reference point**，horo-agent 若要整合 voicebox 要先解決 selective-engine-routing 的 substrate 問題。