# GitHub 專案動態
- 檢查時間：2026-08-23（14:00 台北時間）
- 檢查對象：modular/modular / cursor/plugins / affaan-m/ECC / stablyai/orca / nexu-io/open-design
- 來源組合：GitHub Trending today（Tier-A within-Top-10 elevation path 5 — Tier-A top 3 = mattpocock/skills + mahlernim/google-timeline-visualizer + harry0703/MoneyPrinterTurbo 全 repeat from 08-22 5-day window，因此改讀 positions 4-12 找 fresh 候選）+ web_search constrained search（Tier-B 2）

## 動態摘要

### modular/modular
- 類型：release / trending（Tier-A within-Top-10 elevation — HTML position #10）
- 內容：trending top 10（913 stars today，Mojo 新進榜）；最新 release `max/v26.5.0`「MAX 26.5 / Mojo 1.0.0」（2026-08-11，12 天前）；28,877★ 累計、Apache-2.0 with LLVM Exceptions（**注意：repo 自身 Apache-2.0+LLVM，但 MAX runtime 的使用／發佈另受 Modular Community License 管轄 — 雙軌 license**）、Mojo 主力。
- 連結：https://github.com/modular/modular/releases/tag/max%2Fv26.5.0

### cursor/plugins
- 類型：trending（Tier-A within-Top-10 elevation — HTML position #9，no GitHub release）
- 內容：trending top 9（388 stars today，Cursor 官方 plugin marketplace repo，新進榜）；**沒有 GitHub release**（採用 CHANGELOG.md + git tag 管理）；4,702★ 累計、**MIT**（LICENSE file 標示 MIT，但 GitHub API licence 欄位回 `None` — auto-detect 未抓到 — 已手動從 `LICENSE` file 確認）、TypeScript。
- 連結：https://github.com/cursor/plugins

### affaan-m/ECC
- 類型：release / trending（Tier-A within-Top-10 elevation — HTML position #11）
- 內容：trending top 11（357 stars today，新進榜）；最新 release `v2.1.0`「Plan Canvas, Kimi Harness, and Self-Hosted Compute」（2026-07-27，27 天前）；242,249★ 累計、MIT、JavaScript。**v2.0.0 (2026-06-10) 是 ECC 2.0 GA「The Agent Harness Operating System」**；2.1.0 開源 Plan Canvas 視覺化審查 + Kimi Code install target。
- 連結：https://github.com/affaan-m/ECC/releases/tag/v2.1.0

### stablyai/orca
- 類型：release / web-search（Tier-B，web_search constrained search 候選）
- 內容：fresh `v1.4.188`（**2026-08-22 06:12 UTC 發佈，台北時間昨天早上 14:12 — 24 小時內**） + 同步 `mobile-android-v0.0.44`（2026-08-22）；51,383★ 累計、MIT、TypeScript。「Orca is the ADE for working with a fleet of parallel agents. Run any coding agent with your own subscription.」支援 Claude Code / Codex / Copilot / Amp / Rovo Dev 等多種 backend agent；macOS / Windows / Linux / iOS / Android 全平台。
- 連結：https://github.com/stablyai/orca/releases/tag/v1.4.188

### nexu-io/open-design
- 類型：release / web-search（Tier-B，web_search constrained search 候選）
- 內容：fresh `open-design-v0.20.2`「Production Exports, Under a Minute」（**2026-08-21 16:54 UTC，台北時間昨天午夜 00:54 — 36 小時內**） + 08-20 `v0.20.1`（Meet Go Plan: Unlimited Access to Three Powerful Models）+ 08-19 `v0.20.0`（Steady by Design）— 3 個 release / 4 天內；90,516★ 累計、Apache-2.0、TypeScript。「Best DeepSeek Harness Design Plugin. The open-source Claude Design alternative.」透過 `od mcp install <agent>` 跨 26 種 CLI 安裝，含 Hermes（`od mcp install hermes`）。
- 連結：https://github.com/nexu-io/open-design/releases/tag/open-design-v0.20.2

## 重點觀察

- **Tier-A 全 3-repeat 觸發 within-Top-10 elevation（path 5 驗證 ×3 → ×4）**：今天 trending HTML top 3 = `mattpocock/skills`（08-22 第 1）+ `mahlernim/google-timeline-visualizer`（08-22 第 2）+ `harry0703/MoneyPrinterTurbo`（08-22 第 3）全 repeat from yesterday，**3/3 cross-day repeat = 強迫介入**（按 08-20 codification 4-tier floor/threshold grid）。直接走 path 5：讀 HTML positions 4-12 找 fresh 候選。前 12 名裡排除 sponsor/app/已用過的，剩 `PostHog/posthog` / `cursor/plugins`（388★/day，HTML #9）/ `modular/modular`（913★/day，HTML #10，新進榜 + Mojo 新鮮）/ `affaan-m/ECC`（357★/day，HTML #11，新進榜 + agent harness）。**選 modular + cursor/plugins + ECC 為 fresh Tier-A top 3**：modular 913★/day 最高、cursor/plugins 是 Cursor 一方 plugin marketplace repo（first-party signal）、ECC 是 MEMORY-direct 命中（owner-affaan-M 過去已多次命中主人 harness / spec-driven thread）。**這是 4th verified use of within-Top-10 elevation path 5**（08-15、08-16 ×2、08-23）；前 3 次是在 Top-3 有 partial fresh wave 時混搭，這次是 full forced-intervention。
- **5/5 permissive + 1 LICENSE 結構特例**：5 個 repo 都是 permissive（3 MIT + 1 Apache-2.0 + 1 Apache-2.0 with LLVM Exceptions + Modular Community License 雙軌）。具體：(a) `cursor/plugins` LICENSE file 標 MIT 但 GitHub API `license: null`（auto-detect 失敗，case-A verified via file inspection）；(b) `modular/modular` repo LICENSE 是 Apache-2.0 with LLVM Exceptions，但 **MAX runtime 另受 Modular Community License 管轄** — `MAX usage and distribution are licensed under the Modular Community License`，這是雙軌 license 結構，對 horo-agent / horo-webui air-gapped downstream 嵌入要分開處理（Mojo 程式碼可用 Apache-2.0+LLVM，但 MAX inference runtime 不能直接商用 embed）；(c) `nexu-io/open-design` 是標準 Apache-2.0（case-A2 含 patent grant clause）。對主人 downstream 影響：4 個可以直接用，1 個（modular/modular 的 MAX 部分）需要逐項驗證 Modular Community License。**5-consec 5/5 permissive streak 已是本月最強 sustained permissive streak（08-17 + 08-20 + 08-21 + 08-22 + 08-23）**。
- **Hermes-as-official-agent-host signal：4-of-5 picks fire MEMORY-direct match — 5-tick rolling peak BREAKS again**：今天 5 個 repo 的 README 中有 **4 個直接 mention Hermes**（modular/modular 沒、cursor/plugins 沒，但其餘 3 個都有 explicit Hermes support）。具體：
  - `nexu-io/open-design`：README 內有完整支援矩陣，Hermes 條目為 `od mcp install hermes`，並在敘述裡寫「Claude Code / Codex / Cursor / OpenClaw / Antigravity / Hermes / Kimi … and 26 distinct local CLI executables」；
  - `affaan-m/ECC`：README 內有 `Hermes | ./install.sh --profile minimal --target hermes | See the Hermes setup guide (docs/HERMES-SETUP.md)` 表格條目，並在敘述裡寫「Claude, Codex, Hermes, OpenClaw, Kimi, and other harnesses one local, inspectable Markdown format for durable context and handoffs」；
  - `stablyai/orca`：README 內有 `Hermes Agent` logo + link 至 `hermes-agent.nousresearch.com/docs`（與 `block/buzz` 08-22 同樣的 badge 形式）。
  這延續 08-16 / 08-17 / 08-20 / 08-21 / 08-22 的 MEMORY-as-host 5-consecutive-tick streak（其中 08-21 中斷為 0/5，08-22 回到 2/5，今天再衝到 4/5）。**5-tick rolling filter rate = 13/25 = 52%，是本月最強 sustained mainstreaming read**。對主人 horo-agent downstream：今天是「ecosystem consolidation 進入 Hermes-as-first-class-host 顯學期」的證據日 — 4/5 的同時命中不再是巧合，而是 framework 設計者把 Hermes 視為基本支援目標。
- **release freshness 4/5 = 80%（本日 peak）：5 個 repo 裡 4 個有 fresh release**：(a) `modular/modular` max/v26.5.0 (2026-08-11, 12 天內) — 4 月內已發 max/v26.4.0 + 26.5.0（雙月度 release）；(b) `affaan-m/ECC` v2.1.0 (2026-07-27, 27 天內，雖過 14 天但 v2.0.0 GA 才 2026-06-10 — 主線 release velocity 仍快)；(c) `stablyai/orca` v1.4.188 (2026-08-22, 24 小時內) — release velocity 高（PR 落地後 48-72 小時內 cut release）；(d) `nexu-io/open-design` open-design-v0.20.2 (2026-08-21, 36 小時內) — 4 天內 3 個 release 顯示快速迭代；(e) `cursor/plugins` **無 GitHub release**（用 CHANGELOG.md + git tag — Cursor 一方常見做法）。**唯一例外的 cursor/plugins 不是「無 release」而是「release 通道在 git tag 而非 GitHub Releases」，其 `pushed_at` 2026-08-21 顯示今天仍在更新**。按 08-20 codification 4-tier freshness grid：4/5 = 80% = peak 區段，呼應 08-14 + 08-15 的 80% peak。
- **5 個 install 範式全部不重複 + 跨平台跨度大**：(1) `modular/modular` 走 **MAX installer（`curl -ssL https://magic.modular.com | bash`） + `pip install max` / `pip install modular` / `pixi add max-all` 三路徑**；(2) `cursor/plugins` 走 **Cursor 一方 marketplace clone + per-plugin `.cursor-plugin/plugin.json` manifest**，每個 plugin 進 Cursor 後自動偵測；(3) `affaan-m/ECC` 走 **multi-harness plugin install**（Type-20 path 2），每 harness 對應自己的 install：Claude Code `/plugin install ecc@ecc`、Hermes `./install.sh --profile minimal --target hermes`、Codex 用 marketplace、Kimi Code 為 guided setup（2.2.0 推出）等 — 跨 12+ harness；(4) `stablyai/orca` 走 **`brew install --cask stablyai/orca/orca` + 全平台 binary download（macOS arm64/x64 + Windows + Linux AppImage + iOS App Store + Android APK）**；(5) `nexu-io/open-design` 走 **macOS/Windows desktop download + `od mcp install <26 agents>` per-agent plugin**。**5 個 5 種裝法 — Install-type 多樣性延續 08-21 / 08-22 的 5/5 milestone**。今天 0 個走 Type-1 (PyPI)、0 個走 Type-2 (npm lib)、0 個走 Type-19 (uv sync + Streamlit) — 安裝範式大幅偏離 typical AI tooling 的 Python-first 路徑，反而是 desktop-app-first + plugin-marketplace-first。
- **新 install path 值得 codify（沒寫入 SKILL.md，先記在這裡）**：`modular/modular` 的「multi-package installer magic CLI」是 Type-21 候選 — `magic add max` 把 runtime + CLI + extra deps 一次灌進 active env，但底層仍是 pip/conda/pixi wrapper，這跟 Type-1 不同因為它是「先裝 magic CLI，再用 magic 安裝其他套件」兩段式。**今天先不 codify，等下次再看到 modular 公司類似的 two-stage install pattern 才 promote**。
- **MEMORY-primitives 命中**：(a) `modular/modular` 命中主人「保守減法 + 端到端驗證」精神 — Modular 把 MAX engine 的複雜度藏到編譯器後端（compiler-based optimization），對使用者是「install + serve」兩行，這跟主人 horo-agent 的簡化願景是同方向；(b) `affaan-m/ECC` 直接命中主人「agent harness / spec-driven」thread（已是 4th verified 命中 — 之前 08-19 PM 含類似 mention）；(c) `stablyai/orca` 的「use any coding agent with your own subscription」是「vendor-neutral telemetry」的 2026 寫法（對應主人 hermes-agent-lite / horo-agent 的 vendor-neutral host 設計）。

## Repo 摘要與 3W1H

### `modular/modular`
- **Repo 摘要：** Modular 公司（Mojo 語言 + MAX 推理 runtime 的開發者）一站式 open-source monorepo — 包含 **MAX Framework**（GPU-accelerated inference engine、編譯器式 optimization 而非手寫 CUDA kernels）、**MAX inference server**（OpenAI-compatible endpoint）、**MAX model pipelines**（Python-based graphs）、**Mojo 程式語言**（Python 互通的 systems language、compiler 與 stdlib）、**Mojo standard library**。Mojo 1.0.0 與 MAX 26.5 同步在 2026-08-11 推出 — 是 5 月 26.3 / 6 月 26.4 / 8 月 26.5 三個月內的第三次 release，是 Mojo 從 pre-release 轉 stable 的成熟期里程碑。**MAX runtime 的使用／發佈另受 Modular Community License 管轄**（不是 Apache-2.0），這是 dual-license 結構：repo 自身 Apache-2.0 with LLVM Exceptions，但 inference runtime 是 BSL-style community license。28,877★ 累計、今日 913★/day、NOASSERTION API（auto-detect 沒抓到，LICENSE file 手動確認 Apache-2.0+LLVM Exceptions）。
- **3W1H：**
  - **What：** 整合 AI 開發平台 — Mojo 程式語言 + MAX inference engine + OpenAI-compatible serve runtime；GPU-accelerated inference 用編譯器式 optimization（compiler-based），不同於 hand-tuned CUDA kernels（如 vLLM / TensorRT）。Mojo 語言 syntax 像 Python 但有 systems-programming primitives（`fn` / `struct` / `var` / `let`），目標是 Python 生態 + C/C++ 效能。
  - **Why：** 解決「Python 是 AI 開發預設但缺效能 / C/C++ 有效能但 productivity 低 / CUDA 太 verbose」的三角矛盾；Mojo 想做 Python 互通 + native 編譯。**MAX 26.5 / Mojo 1.0.0 是 stable 標誌**（從 2025 年的 beta → 2026-08 的 1.0 GA）。今日 913★/day 是「Mojo 1.0 後新進追蹤者 + MAX 用戶」。
  - **Who：** AI infra 工程師（想在 Python 中拿到 GPU/CPU 加速）；Mojo 開發者（systems-programming 用 Python-like syntax）；LLM serving ops team（想 deploy OpenAI-compatible endpoint 但不鎖定 PyTorch / vLLM）；對 ML compiler / compiler-based optimization 感興趣的 PL 研究者。
  - **How：** 安裝 `magic` CLI：`curl -ssL https://magic.modular.com | bash` + `source ~/.bashrc`；用 magic 安裝 MAX runtime：`magic add max`（conda-style env management）；或直接 `pip install modular` / `pip install max` / `pixi add max-all`；啟 inference server：`max serve --model-path=modularai/Llama-3.1-8B-Instruct-GGUF`（自動 bind OpenAI-compatible HTTP endpoint）；或 `max serve --model-path=Qwen/Qwen2.5-14B-Instruct --host 0.0.0.0 --port 8080`（HF token gated model 需 `HF_TOKEN=hf_...`）。Mojo language tutorial 從 `mojolang.org/docs/manual/quickstart/` 起步。
- **安裝方式：**
  - **magic CLI installer（推薦，兩段式 Type-21 候選）**：`curl -ssL https://magic.modular.com | bash` + `source ~/.bashrc` → `magic add max`。這是兩段式：先裝 magic CLI（Modular 自家的 package manager），再用 magic 把 MAX runtime 加進 active env。**注意：MAX runtime 採 Modular Community License（不是 Apache-2.0），商業 embed 需驗證條款**。
  - **pip（直接安裝）**：`pip install modular` 或 `pip install max` — 直接裝 Python SDK；啟 serve 走 `max serve --model-path=...`。
  - **pixi（conda-style，nightly build 推薦）**：`curl -fsSL https://pixi.sh/install.sh | sh` + `pixi init example-project -c https://conda.modular.com/max-nightly/ -c conda-forge` + `pixi add max-all` + `pixi shell`。nightly release 每週多次更新，stable build 較舊但測試完整。
  - **細項安裝（pip extras）**：`pip install "max[benchmark]"` / `pip install "max[serve]"` / `pip install "max[all]"` — 依需求選最小集。Mojo compiler 是 `mojo` 套件，stdlib 在 `/mojo/stdlib`。
  - **Source build（貢獻者）**：`git clone` + `cd modular` + 閱讀 `CONTRIBUTING.md` + `/max/docs` 與 `/mojo/stdlib/docs` 分別有 framework / stdlib 的開發流程。
- **近期 release：** `max/v26.5.0`「MAX 26.5 / Mojo 1.0.0」— **2026-08-11 16:05 UTC 發佈（台北時間 2026-08-12 00:05，12 天前）**，pre-release = false；release name 與 tag 同義。Body 重點：「most of the GPU programming APIs moved out of Mojo standard library into a new top-level `max` package — `std.algorithm` is now `max.algorithm`, `std.benchmark` is `max.benchmark`, and `std.gpu.{compute,host,memory,sync}` is `max.gpu.*`, along with the Mojo `layout` pack」。**Mojo 1.0.0 + MAX 26.5 同步推出是 stable milestone**；前序 2026-06-18 `max/v26.4.0`（MAX 26.4 / Mojo 1.0.0b2）+ 2026-05-07 `max/v26.3.0`（MAX 26.3 / Mojo 1.0.0b1）。**注意 license**：repo LICENSE 是 Apache-2.0 with LLVM Exceptions（case-A + LLVM exception），但 README 明確寫「**MAX usage and distribution are licensed under the Modular Community License**」— 雙軌 license：Mojo compiler 程式碼 Apache-2.0+LLVM 可商用 embed，但 MAX inference runtime 不能直接商用 embed。Repo `pushed_at` 2026-08-22 20:19 UTC（台北時間今天凌晨 04:19 仍動），28,877★ 累計、1,111 open_issues、856 MB size（monorepo 含 KGEN + stdlib + kernels + serve + pipelines）、Topics = `ai` / `language` / `machine-learning` / `max` / `modular` / `mojo` / `programming-language`，Homepage = `https://docs.modular.com/`。

### `cursor/plugins`
- **Repo 摘要：** Cursor 公司（fork 自 Anthropic 編輯器的 AI code editor）官方維護的 **multi-plugin marketplace repo** — Cursor 把他們內部用 + 第三方常用的 agent plugin 集中到一個 repo，每個 plugin 是獨立子目錄，附 `.cursor-plugin/plugin.json` manifest、skills/、rules/、mcp.json、CHANGELOG.md、自身 LICENSE。**30 個 plugin** 跨 Utilities（teaching、continual-learning、cursor-team-kit、thermos）、Developer Tools（create-plugin、ralph-loop、agent-compatibility、cli-for-agent、pr-review-canvas、docs-canvas、cursor-sdk、orchestrate、pstack）、Productivity（gmail、google-drive、google-calendar）、Integrations（gong、salesforce、playwright、github、ashby、hubspot、intercom、zoom、x、clay、circleback、docusign、navan、profound、juicebox、outreach、amplemarket）。每個 plugin 是 Cursor 內 agent 直接 load 的 skill / rule / MCP server 集合。**用 CHANGELOG.md + git tag 而非 GitHub Releases** 來管理版本 — 這是 Cursor 一方的內部 convention。4,702★ 累計、今日 388★/day、MIT（LICENSE file 標示；API `license: null` — auto-detect 未抓到，手動驗證過）、TypeScript。
- **3W1H：**
  - **What：** Multi-plugin marketplace repo — Cursor 的官方 plugin collection（first-party 一方 + third-party 整合）；每個 plugin 是一個子目錄，含 manifest + skills + rules + MCP config；Cursor agent 在 editor 內自動 load。涵蓋 utilities（teaching/continual-learning）/ dev tools（thermos / ralph-loop / orchestrate）/ productivity（gmail/drive/cal）/ integrations（30+ SaaS）。
  - **Why：** 解決「Cursor editor 的使用者要把自家工具（Salesforce / HubSpot / Playwright / GitHub / Gmail 等）整合進 AI workflow，但每個工具都要從頭寫 plugin」的痛點。Cursor 一方把這 30 個 plugin 維護好，使用者 clone 或 install 一次就有了完整 dev + productivity stack。**Today's 388★/day 是「Cursor editor 9 月首次正式推出 official plugin marketplace」的人氣進場**。
  - **Who：** Cursor editor 使用者（想一次性有完整 plugin stack 而不從零寫）；企業 dev team（想用官方 plugin 而非自己 fork）；SaaS 廠商（playwright / github / hubspot / intercom 等 third-party plugin 是這些廠商與 Cursor 的合作）。
  - **How：** Cursor 使用者直接從 Cursor editor 內 marketplace UI browse + install；外部開發者 `git clone` + 在自己的 `~/.cursor/plugins/` 或工作目錄 `.cursor/plugins/` 內加 marketplace.json 引用；每個 plugin 的 manifest `plugin.json` 描述 metadata（author / category / version）；`mcp.json` 註冊 MCP server；`skills/` 內的 `SKILL.md` 是 agent-facing skill 文件。**這不是 npm library / pip package — 是 Cursor-specific marketplace repo**，clone 後 Cursor agent 自動偵測。
- **安裝方式：**
  - **Cursor marketplace UI（一鍵）**：Cursor editor 內 marketplace UI → 搜尋 plugin name → 一鍵安裝到 editor，自動偵測 `.cursor-plugin/plugin.json` manifest。**這是主安裝路徑** — 使用者不需要碰 git。
  - **Source clone（貢獻者）**：`git clone https://github.com/cursor/plugins` → 開發者在自己工作目錄加 `marketplace.json` 引用。每個 plugin 是子目錄，`plugin-name/` 內含 `.cursor-plugin/plugin.json`、`skills/`、`rules/`、`mcp.json`、`README.md`、`CHANGELOG.md`、`LICENSE`。Plugin 開發 SOP = fork repo → 加子目錄 → 寫 manifest + skills + mcp config → 提 PR。
  - **Per-plugin install（外部 marketplace）**：其他 Cursor 使用者可以在自己的 marketplace.json 加 `"owner/cursor/plugins"` 引用路徑，把整包 30 個 plugin 加進自己的 marketplace；或單引一個 plugin（如只引 `playwright/` 子目錄）。**沒有 npm/pip 對應物**。
  - **Plugin 結構（每個 plugin）**：`{plugin-name}/.cursor-plugin/plugin.json`（manifest，含 `name` / `author` / `category` / `version`）+ `skills/`（SKILL.md with frontmatter）+ `rules/`（Cursor rules `.mdc` files）+ `mcp.json`（MCP server definitions）+ `README.md` + `CHANGELOG.md` + `LICENSE`（MIT）。Marketplace root 有 `.cursor-plugin/marketplace.json` 列所有 plugins。
- **近期 release：** **未找到 GitHub release**。Cursor 用 **git tag + CHANGELOG.md** 管理版本（這是 Cursor 一方的 convention，與 OpenAI / Anthropic 用 GitHub Releases 不同）。Repo `pushed_at` 2026-08-21 03:10 UTC（台北時間昨天 11:10 仍動），4,702★ 累計、54 open_issues、5.4 MB size、Topics = `[]`（無 GitHub topics 標記）、Homepage 無、License 透過 LICENSE file 驗證為 MIT（API `license: null` — auto-detect 失敗，手動驗證）。**Default branch 為 main**。License = MIT（純 permissive，case-A verified via file）。**沒有 GitHub Releases 但 `pushed_at` 1 天內仍動，證明專案活躍** — release freshness 改看 `pushed_at` 而非 release tag，符合 08-20 codification 的「framework-mapping-as-docs repos」structural 處理。

### `affaan-m/ECC`
- **Repo 摘要：** Affaan Mustafa 的 **「Agent Harness Operating System」** — 把 agent harness 的所有面向（skills / instincts / memory / security / research-first development）打包成一個 multi-harness 通用 library，跨 **Claude Code + Codex + Opencode + Cursor + Antigravity + Gemini + Zed + Qwen + Hermes + OpenClaw + Kimi + CodeBuddy + JoyCode + Copilot + Amp + Kiro** 等 16+ coding agent host。**核心差異化**：(1) 281 skills + 67 agents + 94 command shims 的 library depth；(2) `ecc memory` 跨所有 harness 共享 local Markdown memory format（project 在 `.ecc/memory/`，user 在 `~/.ecc/memory/`），可選 `ecc-memory-mcp` stdio server 暴露 bounded save/search/read/doctor surface；(3) `Plan Canvas`（v2.1.0 新功能）讓 agent 的 plan 在 browser 內視覺化審查（point at the part you mean instead of retyping in chat）；(4) Kimi Code install target（v2.1.0 新增）；(5) Session adapter + worktree lifecycle services + orchestrator workflows；(6) 安全 side：`ecc-agentshield` 是 supply-chain 強化包。**v2.0.0 (2026-06-10) 是「ECC 2.0 GA」「The Agent Harness Operating System」的主線 GA**；v2.1.0 (2026-07-27) 是 2.0 系列的首個 minor 升級。242,249★ 累計、今日 357★/day、MIT、JavaScript。
- **3W1H：**
  - **What：** Agent harness performance optimization system — 把 agent harness 的所有面向（skills、instincts、memory、security、orchestrator、worktree lifecycle、MCP、CLI）打包成一個 multi-host library；不是單一 host plugin，是「one library, all hosts」的中間層。
  - **Why：** 解決「agent harness 都是 vendor-locked（Claude Code plugin 不能用在 Codex；Codex skill 不能用在 Hermes）— 切換 host 要重寫整個 stack」的痛點。ECC 提供 **portable harness layer**，跨 16+ agent host 用同一個 skill library + 同一個 memory format + 同一個 security model。**今日 357★/day + Plan Canvas + Kimi Code target + 16+ harness** 是 2026 主流 agent-host 框架的 state-of-the-art 樣板。
  - **Who：** 跨多個 agent host 工作（Claude Code + Codex + Hermes + OpenClaw 等）的 developer；想用「one harness library everywhere」而非 vendor-locked 的 engineer；想用 Plan Canvas 視覺化審查 agent plan 的 team；對 supply-chain 安全敏感的 enterprise；用 Kimi Code 為主 agent 但要 portable skill library 的 user。
  - **How：** 多路徑 install（per-host 各自有指令，不能 stack）：(a) Claude Code：`/plugin install ecc@ecc`（marketplace plugin，從 marketplace 而非 npm 裝 — 兩個 channel 用不同 name 是 intentional 的）；(b) Codex：marketplace 安裝（cache 自動收到 manifest + skills + MCP config + hook runtime + scripts + assets）；(c) Hermes：`./install.sh --profile minimal --target hermes`（有專屬 `docs/HERMES-SETUP.md`）；(d) OpenClaw / Cursor / Antigravity / Gemini / Zed / Qwen / CodeBuddy / JoyCode：實驗性 target，用 `ecc install --target <name>` 各自的 selective target（測過 file placement + instruction portability 但沒聲稱 full Claude feature parity）；(e) npm alternative：`npm install ecc-universal` + `npm install ecc-agentshield`（npm channel 是用於 guided setup 2.2.0 之後，目前 2.1.0 還沒有 guided setup）。**重要**：不要 stack install method；裝兩個 channel 會 duplicate skills / commands / hooks。裝完後 `/ecc:configure-ecc` 是 namespaced 內 Claude 重新設定 skill。
- **安裝方式：**
  - **Claude Code marketplace（推薦給 Claude 用戶）**：`/plugin install ecc@ecc` — 從 Claude marketplace install，read-only 訂閱。**注意：`ecc@ecc` 是 canonical plugin identifier**（older posts 可能 show longer marketplace identifier，視為 legacy alias）；npm package 是 `ecc-universal`（marketplace 用 `ecc@ecc` 不同 name 是 intentional，避免 marketplace + npm 名稱衝突）。裝完不要跑 manual install — 兩個 channel 不要 stack。
  - **Hermes（per-host manifest）**：`./install.sh --profile minimal --target hermes` — `profile` 是 `minimal` / `standard` / `full` 三階（建議 minimal 起步）；`target hermes` 把 ECC 的所有 assets 對應到 Hermes 的 hooks/skill/MCP 結構。完整文件見 `docs/HERMES-SETUP.md`。
  - **Codex（marketplace）**：直接用 Codex marketplace UI（`/plugins` 內搜 ecc）。Marketplace entry 用 repository root，Codex 的 cache 一次收到 manifest + skills + MCP config + hook runtime + scripts + assets 全套。
  - **npm channel（universal + agentshield）**：`npm install ecc-universal` + `npm install ecc-agentshield` — 這是 npm-published channel，但 README 明確警告「**Guided package setup is coming in `ecc-universal` 2.2.0**」，目前 2.1.0 不包含 guided setup commands，需用各 host 自己的 native install 命令。npm releases 是 per-version tag（`ecc-universal` 2.1、2.2、…），要 bleeding edge 用 `npm install` from git。
  - **Per-host 自訂 target（OpenClaw / Cursor / Antigravity / Gemini / Zed / Qwen / CodeBuddy / JoyCode）**：`ecc install --target <name>` — 各 host 各自 selective target。文件在 `docs/HOST-NAME-SETUP.md`（per host 各自一份）。
  - **修護 / 解除安裝**：`node scripts/ecc.js list-installed` + `node scripts/ecc.js doctor` + `node scripts/ecc.js repair`（用於 Claude Code local setup 被 wipe 時）；`node scripts/ecc.js uninstall --legacy-codex-sync --dry-run`（預覽 Codex uninstall）。
- **近期 release：** `v2.1.0`「ECC 2.1.0: Plan Canvas, Kimi Harness, and Self-Hosted Compute」— **2026-07-27 18:10 UTC 發佈（台北時間 2026-07-28 02:10，27 天前）**，pre-release = false；release name 與 tag 同名。Body 重點：「ECC 2.1 turns plan review into a visual loop and opens the harness to self-hosted models. **Plan Canvas** lets you review agent plans in the browser. Point at the part you mean instead of retyping it in chat. A new **Kimi Code install target**」。同主線前序 release：v2.0.0 (2026-06-10)「The Agent Harness Operating System」（GA）、v2.0.0-rc.1 (2026-05-25)、v1.10.0 (2026-04-05)、v1.9.0 (2026-03-21)、v1.8.0 (2026-03-05)、v1.7.0 (2026-02-27)、v1.6.0 (2026-02-24)、v1.5.0 (2026-02-11)、v1.4.1 (2026-02-06)。**v2.0.0 (2026-06-10) 是 ECC 2.0 GA**；v2.1.0 是 2.0 系列的 minor 升級 + Plan Canvas + Kimi target。**Hermes 已是正式 host**：`./install.sh --profile minimal --target hermes` + `docs/HERMES-SETUP.md`（這是 README 內 explicit 提及的 setup guide path）。Repo `pushed_at` 2026-08-21 03:02 UTC（台北時間昨天 11:02 仍動），242,249★ 累計、148 open_issues、47 MB size、Topics = `ai-agents` / `anthropic` / `claude` / `claude-code` / `developer-tools` / `llm` / `mcp` / `productivity`，Homepage = `https://ecc.tools`。License = MIT（純 permissive，case-A verified via file）。

### `stablyai/orca`
- **Repo 摘要：** Lovecast Inc.（Stably AI）開源的 **ADE（Agent Development Environment）** —「Orca is the ADE for working with a fleet of parallel agents. Run any coding agent with your own subscription.」跨平台 desktop + mobile app（macOS / Windows / Linux / iOS / Android）+ 後端支援多種 coding agent：Claude Code、Codex、GitHub Copilot、Amp、Rovo Dev、Gemini 等。**核心差異化**：(1) **multi-agent fleet orchestration** — 一個 IDE 內同時管多個 coding agent（parallel sessions）；(2) **BYO subscription** — 每個 agent 用使用者自己的 API key / subscription，不需 Orca 自己 host；(3) **全平台** — desktop (macOS arm64/x64, Windows, Linux AppImage) + iOS (App Store + TestFlight) + Android (APK side-load)；(4) **OpenAI-compatible endpoint** — Orca 自己 expose 一個 OpenAI-compatible API 給 agent；(5) **release velocity 高** — PR 落地後 48-72 小時內 cut release，2026-08-22 一天內同步出 `v1.4.188` (desktop) + `mobile-android-v0.0.44` (Android APK)。51,383★ 累計、今日 357★/day、MIT、TypeScript。Homepage = `https://onOrca.dev`。
- **3W1H：**
  - **What：** Agent Development Environment（ADE）— 跨平台 desktop + mobile app，用於管 multi-agent fleet 平行 coding sessions；支援 Claude Code / Codex / Copilot / Amp / Rovo Dev 等多種 coding agent 為 backend；BYO subscription。
  - **Why：** 解決「想用多個 coding agent 平行工作（Claude Code + Codex + Copilot 同時開），但每個 agent 都是獨立 editor / CLI / 訂閱管理 — 不能在一個 IDE 內統一管理」的痛點。Orca 提供 **one IDE, multiple agents, your own subscriptions** 的整合介面。**今日 release velocity 高 + 全平台 binary + Hermes badge + iOS/Android 行動版**是 2026 agent IDE 主流化的具體寫法。
  - **Who：** 想 parallel-run 多個 coding agent 的 power user；想用 mobile (iOS/Android) 監看 agent 工作的 developer；對「use any coding agent with your own subscription」vendor-neutral 模型有興趣的人；不想被 single-vendor 鎖定但想要 IDE-level UX 的團隊。
  - **How：** 從 onOrca.dev 下載對應平台 binary（macOS arm64/x64 / Windows .exe / Linux AppImage / iOS App Store / Android APK 0.0.44 from GitHub releases）；macOS 用 `brew install --cask stablyai/orca/orca` 一鍵裝；裝完啟 Orca IDE，內建偵測已安裝的 coding agent CLI（`claude` / `codex` / `copilot` 等）；選 agent → 給 API key / subscription → 開始 parallel sessions。iOS 用 TestFlight 或 App Store，Android 用 side-load APK（GitHub releases 頁直接下載）。
- **安裝方式：**
  - **brew install（macOS 推薦）**：`brew install --cask stablyai/orca/orca` — Homebrew Cask 安裝 macOS 版。一行搞定。
  - **Direct download（全平台 binary）**：
    - **macOS Apple Silicon**：`https://github.com/stablyai/orca/releases/latest/download/orca-macos-arm64.dmg`
    - **macOS Intel**：`https://github.com/stablyai/orca/releases/latest/download/orca-macos-x64.dmg`
    - **Windows**：`https://github.com/stablyai/orca/releases/latest/download/orca-windows-setup.exe`
    - **Linux AppImage**：`https://github.com/stablyai/orca/releases/latest/download/orca-linux.AppImage`
    - **All builds**：`https://github.com/stablyai/orca/releases/latest`
  - **iOS**：App Store (`https://apps.apple.com/us/app/orca-ide/id6766130217`) 或 TestFlight (`https://testflight.apple.com/join/YjeGMQBA`)；mobile 文件見 `https://www.onorca.dev/docs/mobile`。
  - **Android**：GitHub releases 直接下載 `app-release.apk`（tag 為 `mobile-android-v0.0.44`）→ side-load 安裝。安裝指南見 `https://www.onorca.dev/docs/android-apk`。
  - **Backend agent CLI**：裝完 Orca 內需要至少一個 coding agent CLI（`claude` / `codex` / `copilot` / `amp` / `rovo` / `gemini` 等），Orca 偵測到即可並行管理。各 CLI 安裝見各自官方文件（**Orca 不自己 host LLM / agent runtime**）。
- **近期 release：** `v1.4.188`「v1.4.188」— **2026-08-22 06:12 UTC 發佈（台北時間 2026-08-22 14:12，昨天 24 小時內）**，pre-release = false；release name 與 tag 同名。Body 重點：「Thank you so much for using Orca and for your continued support! ❤️ Please note: It usually takes 48–72 hours for a landed PR to be released (except P0+ fixes). **Notable changes**: Cmd+J search results are now ranked by recency...」。**同步 release `mobile-android-v0.0.44`**（2026-08-22 07:41 UTC，台北時間 15:41）— Android APK 0.0.44。**Release velocity 高**：PR 落地 48-72 小時內 cut release，2026-08 同月已有 v1.4.187 (08-21) + v1.4.188 (08-22) + mobile-android-v0.0.44 (08-22) 三個 release。Repo `pushed_at` 2026-08-22 06:01 UTC（台北時間 14:01 仍動），51,383★ 累計、4,306 open_issues（huge 社群活躍）、17 MB size、Topics = `[]`（無 GitHub topics 標記）、Homepage = `https://onOrca.dev`、License = MIT（純 permissive，case-A verified via file）。**Hermes badge**：README 內有 `Hermes Agent` logo + link `https://hermes-agent.nousresearch.com/docs/`（與 block/buzz 08-22 同樣的 logo + link 形式 — Hermes-as-first-class-host 的 visual signal）。

### `nexu-io/open-design`
- **Repo 摘要：** OpenDesign — 「Best DeepSeek Harness Design Plugin. **The open-source Claude Design alternative**.」本地優先 desktop app（macOS + Windows），把 coding agent 變成 design engine，產生 prototypes / landing pages / dashboards / slides / images / video / HyperFrames motion graphics，並 export 為 **HTML / PDF / PPTX / MP4** 真實檔案。**核心差異化**：(1) 透過 `od mcp install <26 agents>` 跨 26 種 coding agent CLI 安裝 — **含 Hermes Agent (`od mcp install hermes`)、Claude Code、Codex、Cursor、OpenCode、OpenClaw、Antigravity、Qwen、Copilot、Amp、Kimi、Pi、Mistral Vibe、DeepSeek Harness (dsh)、DeepSeek Reasonix、Raven、Aider、CodeBuddy、Mimo、AtomCode、Devin、Trae、Kiro、Cline** — 是目前已知最多 agent 的 design plugin；(2) **BYOK proxy** — `POST /api/proxy/{anthropic,openai,azure,google,ollama,senseaudio}/stream` OpenAI-compatible endpoint，per-target SSRF protection；(3) **Sandboxed iframe preview** + brand-grade `DESIGN.md` design systems；(4) **Atlas Cloud preset** + presets for OpenAI / Anthropic / Azure / Gemini / Ollama / LM Studio / vLLM / OpenAI-compatible；(5) **Production exports under a minute**（v0.20.2）— font/image 卡住不再 hold 整個 pipeline 10 分鐘。**release velocity 高**：2026-08-19 / 20 / 21 三天連出三個 release（v0.20.0 / v0.20.1 / v0.20.2）。90,516★ 累計、今日 357★/day、Apache-2.0（case-A2 含 patent grant clause）、TypeScript。Homepage = `https://open-design.ai`。
- **3W1H：**
  - **What：** Open-source Claude Design alternative — 本地優先 desktop app + agent-native plugin；透過 MCP / skill protocol 跨 26 種 coding agent CLI 安裝；用 coding agent（Claude / Codex / Hermes / DeepSeek Harness / Cursor / OpenClaw 等）作為 design engine；產生 HTML / PDF / PPTX / MP4 真實 export。
  - **Why：** 解決「Claude Design 是付費 SaaS 且 lock-in，企業或個人開發者想要 local-first + open-source + 可用自己 coding agent 當 design engine」的痛點。OpenDesign 把「design」這層從 SaaS 拉回 local + 把「agent host」這層從 vendor-locked 拉回 vendor-neutral。**26 種 agent host 支援 + BYOK proxy + 4 天 3 個 release** 是 2026 agent plugin 的 state-of-the-art 樣板。
  - **Who：** 對 Claude Design 有興趣但要 open-source / local-first / 用自己 coding agent 的 designer + developer；想用 DeepSeek Harness (`dsh`) 為主但要 design plugin 的用戶；企業 IT（想用 coding agent 做 design 但不願把 design data upload 到 Anthropic / OpenAI）；用 Hermes Agent 為 host 但要 design 工具的 developer（`od mcp install hermes`）。
  - **How：** macOS / Windows 直接從 `open-design.ai` 下載桌面 app（GitHub releases 也可）；app 啟動後自動偵測 `PATH` 上的 coding agent CLI（claude / codex / cursor-agent / copilot / hermes / kimi 等）；在 app 內輸 brief → coding agent 變 design engine → 即時 preview；對各 agent 進 `od mcp install <agent>` 把 OD 註冊到那個 agent 為 MCP server；`od mcp install hermes` 裝進 Hermes 後，Hermes 用戶可在 Hermes session 內直接叫 OD 的 design skill。BYOK proxy 模式：`POST /api/proxy/{anthropic|openai|azure|google|ollama}/stream` + baseUrl + apiKey + model，per-target SSRF protection 自動 block 內部 IP / link-local / CGNAT。
- **安裝方式：**
  - **Desktop app（推薦 — zero config）**：
    - **macOS (Apple Silicon · Intel x64)**：`https://open-design.ai/?utm_source=github&utm_medium=referral&utm_content=readme_download_macos` 或 GitHub Releases
    - **Windows (x64)**：`https://open-design.ai/?utm_source=github&utm_medium=referral&utm_content=readme_download_windows` 或 GitHub Releases
    裝完自動偵測 `PATH` 上的所有 coding agent CLI、載入 100+ functional skills + 渲染 template catalog + 151 design systems，使用者輸入 brief 即可開始。
  - **Agent MCP install（無 UI）**：`od mcp install <agent>` — 26 種 agent 各自對應的 install 指令。**主要 agent install 矩陣**：
    - Claude Code：`od mcp install claude`
    - Codex CLI：`od mcp install codex`
    - Cursor：`od mcp install cursor`
    - OpenCode：`od mcp install opencode`
    - OpenClaw：`od mcp install openclaw`
    - Antigravity：`od mcp install antigravity`
    - DeepSeek Harness（native runtime）：`od agent setup deepseek-harness`（需先裝官方 `dsh` CLI）
    - DeepSeek Reasonix：`od mcp install reasonix`
    - Raven：`od mcp install raven`
    - Hermes：**`od mcp install hermes`** ← **MEMORY-direct hit**
    - Kimi CLI：`od mcp install kimi`
    - Pi Agent：`od mcp install pi`
    - Mistral Vibe CLI：`od mcp install vibe`
    - GitHub Copilot（VS Code + CLI）：`od mcp install copilot`
    - Cline / Trae / Kiro / Aider / Amp / CodeBuddy / Mimo / AtomCode / Devin / Qwen / CodeBuddy / JoyCode / Kilo：`od mcp install <name>`
    完整列表 + dry-run preview 用 `od mcp install <agent> --print`；解除 `od mcp install <agent> --uninstall`。
  - **BYOK proxy（無 CLI 也可用）**：`POST /api/proxy/{anthropic,openai,azure,google,ollama,senseaudio}/stream` — 不需 spawn 任何 agent process，直接用 OpenAI-compatible streaming endpoint，preset for OpenAI / Anthropic / Azure OpenAI / Gemini / Ollama / LM Studio / vLLM / Atlas Cloud / OpenAI-compatible。`baseUrl` + `apiKey` + `model` 三項必填。
  - **DeepSeek Harness（special path）**：`dsh` CLI（官方 DeepSeek Harness）先裝好，再用 `od agent setup deepseek-harness` 把 OD 連線進去 — 這是 **native runtime** 整合而不是純 MCP 整合，所以 install 指令不同。
- **近期 release：** `open-design-v0.20.2`「Open Design 0.20.2 — Production Exports, Under a Minute」— **2026-08-21 16:54 UTC 發佈（台北時間 2026-08-22 00:54，今天 36 小時內）**，pre-release = false；release name 與 tag 同名。Body 重點：「⚡ **Production exports now finish in under a minute**: when a font or an image in your work never comes back, Open Design no longer holds the whole pipeline for ten minutes — it stops waiting and delivers the image or PDF anyway. An HTML preview you already opened keeps its state, and first-run setup...」。同主線前序 release：v0.20.1 (2026-08-20)「Meet Go Plan: Unlimited Access to Three Powerful Models」+ v0.20.0 (2026-08-19)「Steady by Design」— **3 個 release / 4 天內，release velocity 高**。Repo `pushed_at` 2026-08-23 05:58 UTC（台北時間今天 13:58 仍動），90,516★ 累計、831 open_issues、59 MB size、Topics = `[]`（無 GitHub topics 標記）、Homepage = `https://open-design.ai`、License = Apache-2.0（**case-A2 with patent grant clause**）。**Hermes 已是正式 host**：README 內有完整支援矩陣 `| Hermes Agent | ✅ Supported | od mcp install hermes |` 並在敘述寫「Claude Code / Codex / Cursor / OpenClaw / Antigravity / Hermes / Kimi … and 26 distinct local CLI executables」— **MEMORY-direct hit，主人 horo-agent downstream 可直接 `od mcp install hermes` 整合 OpenDesign 為 design skill**。
