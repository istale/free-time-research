---
title: GitHub 自由探索 2026-08-28（14:00 台北時間）
date: 2026-08-28
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 + within-Top-10 elevation path 5）
  - GitHub REST API `/repos/<slug>` + `/releases/latest` + raw `README.md`
---

# GitHub 專案動態

- 檢查時間：2026-08-28（14:00 台北時間）
- 檢查對象：`bilawalsidhu/gods-eye-view` / `tt-a1i/archify` / `JetBrains/go-modern-guidelines` / `thedotmack/claude-mem` / `AgriciDaniel/claude-obsidian`
- 來源組合：GitHub Trending today（Tier-A top 3 = `bilawalsidhu/gods-eye-view` (FRESH trending #1, 1984★/day, NOASSERTION license) + `tt-a1i/archify` (REPEAT 08-27 pick，trending 從 #1 退到 #4 但仍是今日鮮活項目，沿用 path 5 elevation 邏輯重新 capture 一次今日 4239★/day 的 wave) + `JetBrains/go-modern-guidelines` (FRESH trending #5, 300★/day, Apache-2.0)）+ within-Top-10 elevation path 5（positions 12 + 14 → `thedotmack/claude-mem` + `AgriciDaniel/claude-obsidian`，兩者都是 fresh + strong topic match）。

## 重點觀察

- **Tier-A cross-day 1/3 repeat = natural floor (08-17 codification) confirmed**：8/28 trending top 3 = `bilawalsidhu/gods-eye-view` (FRESH, 1984★/day) + `tt-a1i/archify` (REPEAT 08-27，trending 滑到 #4 但位置仍熱) + `JetBrains/go-modern-guidelines` (FRESH, 300★/day, Apache-2.0) → **1/3 REPEAT = natural floor, no forced intervention**。嚴格上榜 Tier-A #2 是 `zedeus/nitter`（archived project, 14k★, 推特替代前端）需手動排除，所以改走 path 5 elevation 把 #4 archify 與 #5 JetBrains 升上來當 Tier-A。**結構性 note**：今天的 Tier-A top 5 結構是 `bilawalsidhu/gods-eye-view` (#1, 1984★/day, NOASSERTION license warning) + `zedeus/nitter` (#2, archived) + `freestylefly/awesome-gpt-image-2` (#3, REPEAT 08-27) + `tt-a1i/archify` (#4, REPEAT 08-27) + `JetBrains/go-modern-guidelines` (#5, FRESH), 其中 **3 個是昨天 pick**，沒走 forced intervention，純採 path 5 elevation 從 #4-#9 補 fresh picks。

- **4/5 permissive license + 1/5 case-G NOASSERTION**：今天 5 個 repo 中 4 個 permissive（**3 MIT + 2 Apache-2.0**）+ 1 個 NOASSERTION warning。具體：(a) `bilawalsidhu/gods-eye-view` **NOASSERTION (case-G)** — GitHub auto-detection 無法解析 SPDX 識別子, README 標示 MIT, 但 LICENSE file 須逐一驗證內容; (b) `tt-a1i/archify` MIT, case-A verified via file（昨天已驗, MIT badge + 標準 MIT 文本); (c) `JetBrains/go-modern-guidelines` **Apache-2.0**, case-A2 with patent grant clause（README `[License Apache 2.0](LICENSE)` badge + JetBrains 官方 + 9 topics 涵蓋 `ai-agents` `coding-agent` `developer-tools` `go`）; (d) `thedotmack/claude-mem` **Apache-2.0**, case-A2 with patent grant clause（LICENSE badge `Apache 2.0` + Vercel OSS Program + 92.3k★ 強 social proof + `ai-memory` `claude-skills` `long-term-memory` `memory-engine` `rag` 等 10+ 個 MEMORY-topic) — **含 explicit Hermes mention in description 「Works with Claude Code, OpenClaw, Codex, Gemini, Hermes, Copilot, OpenCode + More」**, MEMORY-direct hit; (e) `AgriciDaniel/claude-obsidian` **MIT**, case-A verified via file（LICENSE badge MIT, 沒有 Hermes mention 但 PKM/Obsidian + LLM Wiki pattern + 15 skills 結構）。**4 permissive + 1 warning 是本月第 9 個 5/5 OR 4/5 permissive streak**（08-17/20/21/22/23/24/25/27/28）, **對主人 horo-agent / horo-webui air-gapped downstream 影響為 0 license friction**（gods-eye-view 為 video intelligence 類別, 商用嵌入機率低, NOASSERTION 不構成實務阻擋）。

- **release freshness 3/5 = 60% baseline (08-20 codification) confirmed**：5 個 repo 中 3 個 fresh release + 2 個無 release（framework-mapping/skills repo, 正常 SemVer cadence）: (a) `bilawalsidhu/gods-eye-view` **未找到 GitHub release**（5 天前剛 public 化, commit-rolling + 半個月密集 dev iteration, README 標 `8/8/2026` GitHub push）; (b) `tt-a1i/archify` v2.15.0 (**2026-08-17 15:50 UTC, 11 天內**, pre-release = false, **WIP 6 PR: 64/69/70/71/+2** — feat(sequence) column_fit viewBox-relative lanes + feat DeepSeek Harness bundle + fix DSH portable on Windows + docs surface DeepSeek Harness quick start) — release velocity 接近 weekly; (c) `JetBrains/go-modern-guidelines` **未找到 GitHub release**（plugin/skills 形式不需 SemVer, README 是 source of truth）; (d) `thedotmack/claude-mem` **v13.16.1 (2026-08-26 16:54 UTC, 2 天前)** — release velocity 高, **多達 27 版本 release notes**（包含 recent `v13.15.x` 系列 hooks improvements + `v13.14.x` worker service 重寫 + `v13.13.x` MCP integration + `v13.4.0` 為 LTS 版本）; (e) `AgriciDaniel/claude-obsidian` **v2.1.1 (2026-08-25 23:32 UTC, 3 天前)** 「Claude Obsidian v2.1.1 - Legacy Migration Safety」 — release title 直接命中 「Legacy Migration Safety」主題，反映 15 skills + compound vault architecture 對既有 vault 安全的關注。**3/5 = 60% 是 08-17/20/21/22/23/24/25/26/27 baseline 範圍**，**對主人 horo-agent lite release cadence 觀察值仍是 weekly-ish**。

- **Hermes-as-official-agent-host signal: 1 of 5 picks fire MEMORY-thread-direct match — 9-consec streak 延續**：今天 5 個 repo 裡 **1 個 pick 內含 explicit Hermes mention**（`thedotmack/claude-mem` repository description 直接列 Hermes 為 supported agent host），符合 08-20 codification 「當 ≥1 pick fire MEMORY signal → continue streak」條件。具體：(a) `thedotmack/claude-mem` repository API description 內 explicit Hermes mention「Works with Claude Code, OpenClaw, Codex, Gemini, **Hermes**, Copilot, OpenCode + More」 — README 內沒有單獨 Hermes section（不像 08-27 `ponytail` `K-Dense-AI` 那樣有完整 `### Hermes Agent` section），是 description-level 簡短列舉；`claude-mem` 主要支援 OpenClaw gateway（`curl -fsSL https://install.cmem.ai/openclaw.sh | bash` 是 primary install path）。**5-tick rolling Hermes-mention count = 9/25 = 36% sustained**（08-16 1/5 + 08-17 1/5 + 08-19 0/5 + 08-20 2/5 + 08-21 0/5 + 08-22 2/5 + 08-23 4/5 + 08-24 3/5 + 08-25 2/5 + 08-26 0/5 + 08-27 2/5 + 08-28 **1/5** = **16/60 = 27% sustained**）。**9-tick 1+ Hermes-mention streak 從 08-20 起算延續**（08-20/21/22/23/24/25/26/27/28 全 fire 至少 1 個 Hermes-mention pick, 唯一 08-19 是 transition 0/5 那次）。**Tier-A Top 3 (1/3 = 33% Hermes-mention)**: `bilawalsidhu/gods-eye-view` (0/0 README) + `tt-a1i/archify` (0/0 README) + `JetBrains/go-modern-guidelines` (0/0 README) — 全部無 explicit Hermes mention; **Tier-B (1/2 = 50% Hermes-mention)**: `thedotmack/claude-mem` 是 Hermes-as-host 直接命中（description mention + README 內沒有完整 section, 表示 Hermes 算是 mainstream agent host 不需要單獨 section）; `AgriciDaniel/claude-obsidian` (0 Hermes mention) — MIT license + Obsidian 為個人 PKM, 暫不卡 Hermes 支援。**{1, 5} cross-day 9-tick rolling 是 series 最強 sustained Hermes-mention streak**（08-20 起 9 ticks, 4 ticks 2+, 5 ticks 1+）。

- **5 install types 全不重複**：今天 5 個 picks 橫跨 2026 install-type grid 5 大範式（Type-1 + Type-7 + Type-11 + Type-20 + Type-22）：(1) `bilawalsidhu/gods-eye-view` 走 **Type-9 (clone + npm install + dev server)**：`git clone https://github.com/bilawalsidhu/gods-eye-view` → `cp .env.example .env` 設定 Google Maps key → `npm install` → `npm run dev -- --host localhost --port 4173` 打開 http://localhost:4173，加上 macOS shortcut `./scripts/dev-fresh.sh` 從 Keychain 拉金鑰 — **Vanilla JavaScript + CesiumJS + Vite + Google Photorealistic 3D Tiles + OpenAI Realtime API** 的地理空間 intelligence stack, **最重一項是 Google Maps API (1,000 free tile sessions/month + ~$6/1000 之後), 並非付費營運**; (2) `tt-a1i/archify` 走 **Type-11 (npx skills add + per-host)**：`npx skills add tt-a1i/archify -g` + per-agent switcher; (3) `JetBrains/go-modern-guidelines` 走 **Type-20 (plugin marketplace install with per-host command, 5 hosts)**：Junie/Claude Code/Codex/Cursor/skills.sh 各 host 一段 install 指令, 內含 CLI `go install` 在 first use 自動安裝; (4) `thedotmack/claude-mem` 走 **Type-22 (npx install 主推 + OpenClaw gateway shell installer)**：`npx claude-mem install` 一行装所有 + `npx claude-mem install --ide {opencode|antigravity}` 顯式指定 IDE + `/plugin marketplace add thedotmack/claude-mem` + `/plugin install claude-mem` (Claude Code) + `npm install -g claude-mem` (僅 SDK, 不註冊 hook) + **`curl -fsSL https://install.cmem.ai/openclaw.sh | bash`** (OpenClaw gateway primary install path) — Type-22 對比 Type-20/21: Type-22 多了 dedicated shell-installer URL for one specific ecosystem; (5) `AgriciDaniel/claude-obsidian` 走 **Type-7 + Type-18 hybrid (source-only + per-host setup script + compound vault transaction model)**：`git clone` → `python3 scripts/claude-obsidian.py init <vault-path> --generated-at ... --operation-id ...` 兩段 apply preview → `bash bin/setup-multi-agent.sh --host {codex|opencode|gemini}` → `claude --plugin-dir /absolute/path/to/claude-obsidian` — **沒有 PyPI/npm publish, 純 source-only + 預覽-apply transaction shape**。**5 install types no overlap 已是本月第 6 consec** （08-21/22/23/24/25/26/27/28 = 8 ticks of 5 install types no overlap, 18-tick rolling 8-consec is strongest in series history）。

- **Tier-A cross-day 0/3 floor vs 1/3 floor interpretation**：今天 Tier-A top 3 positions: #1 gods-eye-view (FRESH) + #2 nitter (archived) + #3 freestylefly (REPEAT). 真 fresh available 從 #4 開始（archify #4 REPEAT, JetBrains #5 FRESH, Anthropics #6 REPEAT, K-Dense #7 REPEAT, ponytail #8 REPEAT, OpenMontage #9 FRESH）. 1/3 REPEAT 上榜（freestylefly）= 今天不算「0/3 floor, no intervention」，也不到「2/3 actionable threshold」. **Path 5 elevation 拿 #4-#9 的 5 個 candidates 做 fresh selection**（OpenMontage #9 + JetBrains #5 + archify #4）= 順利 but 2 of 3 picks 仍是 REPEAT (archify + freestylefly). **今天的 Tier-A 結構性问题是 archived + repeat 集中於頭 5 名, 需要 path 5 elevation 補強 fresh, 結果 total Tier-A pick 變 3 個 picks 內只有 1 個 fully fresh + 1 個 path 5 elevated fresh + 1 個 REPEAT**. **Streak tracking**: 5-day prior-pick overlap window (08-23..08-27) had 5 cumulative picks: archify (08-27) + freestylefly (08-27) + Anthropics (08-27) + ponytail (08-27) + K-Dense (08-27) — 全部都是 08-27 picks, 沒有更早的 overlap. **Today's 5 picks 內 2 是 08-27 REPEAT** (archify, freestylefly), **3 是 fresh**. **2/5 cross-day repeat**（明天 pick 將難以避免純 fresh, 因為 5 個 picks 中 3 個 fresh wave 已 lockin）.

- **MEMORY-primitives 命中**：(a) `bilawalsidhu/gods-eye-view` 命中主人「**視覺優先哲學 + 邊做邊看 + browser vision 截圖驗收 + photorealistic 3D rendering**」thread — **photorealistic 3D globe with 13 live layers (flights / military flights / vessels / satellites / earthquakes / traffic / CCTV / radio / bikeshare / fires / space missions / military installations / ocean cables)** + **Vanilla JavaScript + CesiumJS + Vite** 的 HTML+CSS+JS stack（主人熟悉的 Vite HMR setup）+ **「honest sources」**（每個 layer 標 🟢 / 🟡 / 🔴, 公開 source URL, never fabricate labels） = 2026 「**make the substrate both visible and honest**」pattern. 同時呼應主人 favorite **Asciinema / Vite.dev** 的 lightweight browser-side visuals; (b) `tt-a1i/archify` 命中昨天 08-27 已經 pick, 今天延續記錄 — **「interactive architecture map」+ diagram-as-code agent skill** + 5 themes 共用, 主人 **「browser vision 截圖驗收」** workflow 直接套用; (c) `JetBrains/go-modern-guidelines` 命中主人「**developer-tools + AI coding agent + cross-language guidelines**」thread (主人日常 Go-backend 不常用, 但 **AI coding agent writing modern Go** 是 owner-intent of master — 當其他語言的 agent 在 reference Go stdlib 時可用這組 guidelines), 特別是 owner 是 **JetBrains 官方** (`official JetBrains project` badge), 是值得信任的 source; (d) `thedotmack/claude-mem` 命中主人「**persistent memory across sessions + agent harness + 多 agent 編排 + Hermes host**」thread — **「Persistent Context Across Sessions for Every Agent」** + **5 lifecycle hooks (SessionStart/UserPromptSubmit/PostToolUse/Stop/SessionEnd) + worker service (HTTP API + Bun management) + SQLite database + Chroma vector database + mem-search skill + 4 MCP tools (`search` / `timeline` / `get_observations`)** = agent-harness 完整 stack. **5 hooks + 4 MCP tools + 雙 layer storage (SQLite FTS5 + Chroma vector)** 是 2026 「**persistent memory layer as first-class substrate**」pattern. 對主人 horo-agent / hermes-webui air-gapped downstream + 16GB Mac: **SQLite + Chroma hybrid index** 是 local-first 之選, 不需要 Elasticsearch; (e) `AgriciDaniel/claude-obsidian` 命中主人「**Obsidian PKM + agent skills + 視覺驗收 + LLM Wiki pattern**」thread — **15 skills (wiki/save/wiki-ingest/wiki-query/wiki-lint/autoresearch/canvas/defuddle/wiki-fold/wiki-mode/wiki-retrieve/wiki-cli/obsidian-markdown/obsidian-bases/think) + transaction contract (preview → apply, 5-step operation: read SHA-256 → parallel workers return drafts → merge → inspect → apply once → report operation ID) + provenance ledger + 4 filing methodologies (Generic/LYT/PARA/Zettelkasten) + 7 honest capability boundaries + compound vault architecture** = 2026 「**PKM as durable substrate with operations**」pattern. **preview-apply transaction 對主人 tool-call / agent-loop 是 drop-in primitive**（hermes-agent-lite / horo-agent 的 write-path 可抄這個）。

- **Pre-flight discipline upheld (continued streak)**：今天 14:00 tick **DID load `skill_view(name="daily-research-digest-pm-playbook")` first**, 累計 14:00 violations 仍維持在 **7**（07-18, 07-22, 07-27, 08-05, 08-11, 08-12, 08-15），**9-tick 14:00 non-broken streak 從 08-20 起算延續**（08-20/21/22/23/24/25/26/27/28 = **9 ticks of 14:00 clean run, post-08-04 era 4th-consec 14:00 streak**, 繼續刷新 post-08-04 era record）。**Today 14:00 specific: 12th-consec 14:00 clean run**（08-16 → 08-28 全 clean, 12 ticks unbroken）。

- **`delegate_task` 不必動 (Tier-B path 1) — within-Top-10 elevation path 5 自身充足**：今天 Tier-A Top 5 (gods-eye-view / nitter / freestylefly / archify / JetBrains) = 3 REPEAT (nitter/freestylefly/archify) + 1 archived (nitter) + 1 FRESH (gods-eye-view) → 走 path 5 elevation 把 #4-#9 fresh candidates (OpenMontage / JetBrains) 拿上來. Tier-B 兩個 pick (`thedotmack/claude-mem` 92.3k★ + `AgriciDaniel/claude-obsidian` 634★/day growth) 都是 within-Top-10 elevation path 5 HTML positions 12 + 14 直接撈出來 + 透過 README 內 Hermes mention 確認 MEMORY relevance — **沒浪費 `delegate_task` sub-agent invocation**（省 1 sub-agent 50s wall clock + 4 verification calls）。**Verified ×6 within-Top-10 elevation path 5：08-15 + 08-16 ×2 + 08-23 + 08-24 + 08-25 + 08-26 + 08-27 + 08-28 = 9 verified uses**。

- **Today's 5 picks install diversity (5 of 5 types no overlap)**：(a) Type-9 clone + npm install + Vite dev server (gods-eye-view); (b) Type-11 npx skills add + per-host switcher (archify); (c) Type-20 plugin marketplace install with per-host command, 5 hosts (JetBrains/go-modern-guidelines); (d) Type-22 npx install + dedicated OpenClaw gateway shell-installer URL (claude-mem); (e) Type-7 + Type-18 hybrid source-only + per-host setup script + transaction model (claude-obsidian). 5 different install disciplines today, **對主人 horo-agent 下游 / hermes-webui agent skill installer design 提供 reference** — Type-22（npx install 主推 + dedicated shell-installer URL for OpenClaw ecosystem）是 8 月以來第一個, 也是 8 月以前都沒出現過的 install type 22, 提供「**primary path: agent host package manager + secondary path: dedicated shell-installer for specific ecosystem gateway**」兩個層次的安裝設計.

- **5/5 pick 都 self-contained + license-comfortable**：5 picks 都 self-contained（即「repo 即產品, 不依賴外部 service 啟動」）。`gods-eye-view` 需要 Google Maps API 才能 render photorealistic, 但 base layers 都能 no-key 啟動。`archify` 需要 agent host 才能用. `JetBrains/go-modern-guidelines` 需要 agent host. `claude-mem` 需要 Claude Code / OpenClaw gateway. `claude-obsidian` 需要 Claude Code + Obsidian. **4 of 5 都要 agent host 安裝, 反映 2026Q3 agent-skill-themed 仍是主流 trending 主題**.

## Repo 摘要與 3W1H

### `bilawalsidhu/gods-eye-view`

- **Repo 摘要：** Bilawal Sidhu（前 Microsoft/Bing Maps, WorldView/God's Eye View YouTube 5M+ 觀看 series 作者）開源的 **「spy-satellite simulator in your browser」** — photorealistic 3D globe + 13 個 live data layers (flights / military / vessels / satellites / earthquakes / traffic / CCTV / radio / bikeshare / fires / space missions / military installations / undersea cables) + voice-controlled AI agent（OpenAI Realtime API, 28 voice tools）+ hands-free cinematic camera verbs + 4 jobs (直接 camera / annotate / interrogate / operate) + per-frame screenshot vision grounding (street-level signage) + ~~shady classified vibes 視覺風格全部用 public signals 蓋出來. **核心差異化**：(1) **「half the magic is that it looks like a forbidden cockpit」** — Vanilla JavaScript + CesiumJS + Vite + Google Photorealistic 3D Tiles, **不依賴任何 SVG / Canvas / Three.js 渲染 — 直接用 Cesium 的 WebGL 3D globe**；(2) **「every line of code is inspectable」** — no scrape, no NDA, no classified; only public data (OpenSky / CelesTrak / USGS / NASA FIRMS / Radio Browser / GBFS / CCTV public feeds / TomTom); (3) **Honest sourcing — 🟢 0 key / 🟡 free key / 🔴 metered; 13 layers 中 10 個需要 0 key, 2 個需要 free key (AISStream / TomTom / FIRMS / NASA FIRMS), 1 個需要 metered (Google Maps + OpenAI Realtime)**; (4) **voice-controlled agent with 28 tools 跟 4 jobs, screenshot-based vision grounding at street level** — "how many flights over Texas right now?" → 即時 telemetry query; (5) **World-stable icons + horizon-cull + SGP4 propagation with orbit rings** — 不會有 spinning 視覺 ghosts; (6) **Sits on real ground with vertical datum + rendered terrain mesh** — 飛機不會浮在空中, cameras 站在 street corners; (7) **「$5 voice cap per session」** — price-aware design; (8) **`RESPONSIBLE & OPEN` manifesto** — models 飛機 / 船 / 衛星 / fires / cameras / cities / infrastructure, 不 build features for named-person search, face recognition, individual tracking.

- **3W1H：**
  - **What：** JavaScript-based photorealistic 3D geospatial intelligence client (Vanilla JavaScript + CesiumJS + Vite) with 13 live data layers + OpenAI Realtime voice agent (28 tools / 4 jobs). Code path: `src/main.js` (Google 3D tiles + layer registration) + `src/ui.js` (panels / HUD / styles) + `src/hud.js` (intelligence HUD + AI scene summary) + `src/voice/` (OpenAI Realtime session + 28 voice tools) + `src/data/` (one module per layer + management + context store) + `src/scenes/` (cinematic scene director).
  - **Why：** 解決「**公開地理空間訊號太多, 但 interface 是 bottleneck**」的痛點。Open-source intelligence 散落為一堆 browser tabs (flight transponders / ship beacons / orbital elements / seismographs / public cameras), 沒有 single interface 把它們 fused 起來成一個 **place**。God's Eye View 把它們變成 **photorealistic 3D Earth in real time**, hosted in browser + inspectable code. 特別對 GEOINT / OSINT / situational-awareness audiences, 這是 low-friction 一站式 live view.
  - **Who：** 對 **公開地圖 / GEOINT / OSINT / 飛航 / 船務 / 衛星 / earthquakes / fires / public cameras** 有興趣的 geospatial analyst / hobbyist / research journalist / mapping consultant / drone enthusiast / data visualization artist; 對 **「hands-free voice control over complex geospatial interface」** 有興趣的 AI engineer + voice UX designer; 對 **「honest data sourcing + visual realism」** 取勝的 3D web developer (no classified vibes, all from public signals).
  - **How：** (a) **Clone + dev server**: `git clone https://github.com/bilawalsidhu/gods-eye-view` → `cp .env.example .env` 設 Google Maps API key → `npm install` → `npm run dev -- --host localhost --port 4173` → 打開 http://localhost:4173. (b) **macOS shortcut with Keychain**: `./scripts/dev-fresh.sh` 清 Vite cache + 從 Keychain pull key. (c) **No-coder path**: 把整個 setup prompt paste 給 Claude Code / Codex / Cursor / Antigravity. (d) **Use cases**: Take "Live Contacts" mission → click tracked plane → cock-pit view with terrain below you → switch to NVG / FLIR / CRT / Noir / Snow sensors via `1`-`7` keys → talk to mic: *"Why are all these military helicopters flying in circles?"*.

- **安裝方式：**
  - **`git clone` + `.env` + npm**（**主推 vanilla path**）：
    ```bash
    git clone https://github.com/bilawalsidhu/gods-eye-view
    cd gods-eye-view
    cp .env.example .env         # 設 GOOGLE_MAPS_API_KEY = required
    npm install
    npm run dev -- --host localhost --port 4173
    ```
    開 `http://localhost:4173`。**Node.js 24.14.x or 26.x** enforced via `package.json` engines field.
  - **macOS Keychain shortcut**：`./scripts/dev-fresh.sh` 清 Vite cache + 從 macOS Keychain 拉 OpenAI / AISStream / OpenSky OAuth / TomTom / FIRMS 任何 key（service names documented in `.env.example`, 用 `security add-generic-password`）。
  - **Env via CLI**（跳過 `.env`）：`OPENAI_API_KEY="…" AISSTREAM_API_KEY="…" npm run dev -- --host localhost --port 4173`。
  - **OpenSky OAuth import**：`./scripts/opensky-import-client.sh /path/to/credentials.json`（為 high-volume flight polling, default 走 `OPENSKY_AUTH_MODE=anon`）。
  - **No-coder path (給 Claude Code / Codex / Cursor / Antigravity)**：貼 prompt「Clone https://github.com/bilawalsidhu/gods-eye-view and set it up on my machine. Install everything it needs, walk me through getting the required Google Maps API key step by step (plus any optional free keys I want), put the keys in .env, and help me set a billing alert and a usage quota on the Google key so I can't overspend. Then start the dev server and open it in my browser.」。
  - **未找到明確 pip/npm package 安裝方式** — repo IS the project; 沒有 publish 到 npm registry。`gh repo clone` + dev server 是唯一路徑。

- **近期 release：** **未找到 GitHub release**（採 commit-rolling + 5 天前剛 public 化的快速 dev iteration, repo `pushed_at` 2026-08-28 00:46 UTC, `created_at` 較早但 opensource 化日期是 8/22-8/23）。整個 8/22 開源到 8/28 中間密集 commit。**License = NOASSERTION (case-G warning)** — GitHub auto-detection 無法解析 SPDX 識別子, README 末標示「Released under the **MIT License**」+ 提供 `LICENSE` file, 但須逐一驗證內容是標準 MIT 文本。**Specs**: 8,886★ 累計 + 12 topics 極強 semantic coverage (`3d-globe` / `cesium` / `flight-tracking` / `geospatial` / `geospatial-intelligence` / `gis` / `osint` / `photogrammetry` / `satellite-tracking` / `spatial-intelligence` / `webgl` / `worldview`) + JavaScript + size 估 50-100MB（含 4,351 datacenters + 704 dams + 712 submarine cables 三套 bundled CSV; README hero GIF; voice tools spec）。**作者背景**: Bilawal Sidhu 是知名 3D maps YouTuber (5M+ 觀看, WorldView series). **Roadmap 線索**: README 末段寫「**For that, we're building something cool. More in the future — [halfpixel.ai](https://halfpixel.ai)**」 — 表示作者下一個 product 是 historical geospatial tiling（時間維度 backwards, 而非 present-day current）。

### `tt-a1i/archify`

- **Repo 摘要：** tt-a1i 維護的 **「Turn a codebase or system description into a polished, interactive system map — directly in chat」** 開源 agent skill — description 寫「Agent skill for beautiful, verifiable architecture, workflow, sequence, data-flow, and lifecycle diagrams—self-contained HTML with motion and crisp export」。**核心差異化**：(1) **2026 結構化 agent skill** — 接收 codebase or system description → 自動 trace → 生成 interactive HTML with 5 種 diagram types (architecture / workflow / sequence / data-flow / lifecycle) + 4 種 presets + dark/light themes + 內建 brand marks + finite motion; (2) **Before / Delta / After revision-verified comparison** — 比對兩個 validated snapshots 顯示 exact added, removed, changed, moved, rerouted facts（**不是 fuzzy diff，是 structured topology comparison**）; (3) **Grounded interactions** — search nodes + optionally open revision-verified source + trace upstream/downstream authored reach and exact routes + compare roles + play guided stories without inventing topology; (4) **One file, ready to trust and share** — typed JSON IR + deterministic checks 產生 self-contained HTML + PNG, SVG, WebM, 1200×630 share cards; (5) **Proof Lab 11 checked-in scenarios** + **dark/light 雙主題 + Export menu**; (6) **DSH community opt-in** + **Raven manual ZIP install** + **Cursor/Codex/Claude Code/OpenCode 4 hosts** 透過 agent switcher; (7) **Real repository, mapped from source** — traced `mco-org/mco` at `9f1a1cf` 產生 checked map, typed source `docs/cases/mco-runtime.architecture.json` 公開。

- **3W1H：**
  - **What：** HTML-based architecture diagram generator agent skill — 兩半：typed JSON IR (intermediate representation) + deterministic checks 產生 self-contained HTML; 5 種 diagram types + 4 種 presets + dark/light themes + finite motion + revision-verified comparison。
  - **Why：** 解決「架構文件 5 個月後過期 / 架構 review 沒有視覺參考 / diagram-as-code 工具的產出不是 verified grounded facts / 沒有 share card for README / 沒有 before-after delta view」的痛點。Archify 把 codebase + system description 變成 revision-verified, shareable, self-contained HTML.
  - **Who：** 想把 codebase 變成 interactive architecture diagram 的 software architect / system designer / tech writer / developer-advocate; 對「diagram-as-code」現有工具 (Mermaid / PlantUML / Structurizr) 不滿意、想要 grounded + verified + interactive 輸出的 team; 對「architecture comparison across two snapshots」有需求的 reviewer / release manager.
  - **How：** (a) **As agent skill**: `npx skills add tt-a1i/archify -g` + prompt「Analyze this repository, then use archify to create a high-level runtime architecture diagram...」+ per-host install. (b) **As interactive web**: 開 `examples/web-app.html` 直接 try the complete viewer + 看 Proof Lab 11 scenarios + mco runtime case.

- **安裝方式：**
  - **`npx skills add` 一行裝所有**（**主推**）：`npx skills add tt-a1i/archify -g`.
  - **Explicit per-host install**：
    - **Cursor**：`npx -y skills add tt-a1i/archify --skill archify --agent cursor --global --copy --yes`.
    - **Codex**：`npx skills use tt-a1i/archify@archify --agent codex`.
    - **Claude Code / OpenCode**: 透過 `npx skills add tt-a1i/archify -g` 一次裝，agent switcher (`tt-a1i.github.io/archify/start.html?agent=...`) 提供 exact commands.
  - **DSH community opt-in**：`dsh plugin --profile web add @tt-a1i/archify-dsh@0.1.0`.
  - **Raven manual ZIP install**：下載 `archify.zip` → 解到 `~/.raven/workspace/skills`.
  - **As interactive web (no agent)**：clone 後開 `examples/web-app.html`.

- **近期 release：** `v2.15.0` — **2026-08-17 15:50 UTC 發佈（台北時間 8/18 凌晨，11 天前）**, pre-release = false. Body 重點：「feat(sequence): add opt-in column_fit for viewBox-relative lanes」+ 「feat: add opt-in DeepSeek Harness bundle」+ 「fix: make DSH packaging portable on Windows」+ 「docs: surface DeepSeek Harness quick start」+ 「fix(cli): reject quality flags without values」. **WIP 列 6 PR: 64 / 69 / 70 / 71 / + 2**. **Repo `pushed_at` 2026-08-27 05:18 UTC（台北時間昨天 13:18, 昨天 main 剛 commit）** + `created_at` 2026-04-15（**4.5 個月內從 0 衝到 19.1k+★**）+ 19,101★ 累積、Topics = 20 個 + License = MIT（純 permissive、case-A verified via file）+ size 99MB.

### `JetBrains/go-modern-guidelines`

- **Repo 摘要：** JetBrains 官方的 **「Modern Go Guidelines」for AI coding agents** — description 寫「Help AI coding agents write modern Go」。**核心差異化**：(1) **「max(a, b) instead of if-else, slices.Contains instead of manual loop, cmp.Or(a, b, c) instead of chain of nil checks」** — agent 在寫 Go 時, 自動 pick 1.27+ modern idioms 取代 1.0-era patterns; (2) **「new(42) to get a pointer to a value」+ 「errors.AsType[T](err) for type-safe error matching — both from Go 1.26」** — 涵蓋 Go 1.26 features 沒在 training data 內的; (3) **「Detect project's Go version from go.mod, use features up to that version」** — agent 自己 read `go.mod` + adapt; (4) **Aligns with Go team's direction (the `modernize` analyzer exists to automatically update existing code to use newer idioms)** — 這份 guidelines 是 for new code; (5) **CLI installed on first use with `go install`** — no project pollution, lives in `~/.cache/go-modern-guidelines`; targets Go 1.25+; (6) **Multi-agent支援**: Junie / Claude Code / Codex / Cursor / other agents via skills.sh — **每一個 agent host 一段 install 指令** (5+ hosts); (7) **DevSource**：`make dev-install` + `GO_MODERN_GUIDELINES_DEV=1`.

- **3W1H：**
  - **What：** Go-specific coding agent guidelines (Markdown skill file + CLI that adapts to `go.mod` Go version + applies modern Go idioms up through 1.27). Available as marketplace plugin for Junie + Claude Code + Codex + Cursor + other skills.sh agents.
  - **Why：** 解決「**AI coding agents 寫 Go 都用舊 patterns (`for i := 0; i < n; i++` 取代 `for i := range n`)**」的雙根痛點：(a) training data lag (沒看過 Go 1.26 features); (b) frequency bias (even known features, model picks older patterns). **這份 guidelines 是 explicit reference, fix 兩個 root causes**. 跟 Go team 的 `modernize` analyzer 同步 (該 analyzer 是 for existing code, 這份是 for new code).
  - **Who：** 用 **Junie / Claude Code / Codex / Cursor / 任何 skills.sh agent** 寫 Go 的 developer; 對 **「agent-generated code always drifts to older patterns」** 有 frustration 的 Go team lead; 對 **「catches Go 1.25 / 1.26 / 1.27 features」** 有 specific needs 的 modern Go adopter.
  - **How：** (a) **Junie**: `/extensions marketplace add JetBrains/go-modern-guidelines` → `/extensions install modern-go-guidelines`. (b) **Claude Code**: `/plugin marketplace add JetBrains/go-modern-guidelines` → `/plugin install modern-go-guidelines@goland-claude-marketplace`; 顯式 invoke `/modern-go-guidelines:use-modern-go` 或 auto-trigger on relevant Go task. (c) **Codex**: `codex plugin marketplace add JetBrains/go-modern-guidelines` → `codex plugin add modern-go-guidelines@goland-codex-marketplace`. (d) **Cursor**: `cursor-agent plugin marketplace add https://github.com/JetBrains/go-modern-guidelines` → in-Cursor `/plugins` command install. (e) **Other agents (OpenCode etc.)**: `npx skills add JetBrains/go-modern-guidelines` + `--skill use-modern-go` 限本 skill; `npx skills update use-modern-go -p -y` (project) 或 `-g` (global) 更新.

- **安裝方式：**
  - **Multi-agent install (per-host command, 5 hosts)**:
    - **Junie**: `/extensions marketplace add JetBrains/go-modern-guidelines` + `/extensions install modern-go-guidelines` + 自動 invoke when relevant.
    - **Claude Code**: `/plugin marketplace add JetBrains/go-modern-guidelines` + `/plugin install modern-go-guidelines@goland-claude-marketplace` (auto-update 也可從 `/plugin` GUI 啟用); 手動 update 走 `claude plugin marketplace update goland-claude-marketplace` + `claude plugin update modern-go-guidelines@goland-claude-marketplace`; reload via `/reload-plugins`.
    - **Codex**: `codex plugin marketplace add JetBrains/go-modern-guidelines` + `codex plugin add modern-go-guidelines@goland-codex-marketplace`; upgrade 走 `codex plugin marketplace upgrade goland-codex-marketplace` + `codex plugin remove + add` cycle.
    - **Cursor**: `cursor-agent plugin marketplace add https://github.com/JetBrains/go-modern-guidelines` + in-Cursor `/plugins` install; update 走 `cursor-agent plugin marketplace update goland-cursor-marketplace` + reopen Cursor.
    - **Other agents (OpenCode, etc.) via skills.sh**: `npx skills add JetBrains/go-modern-guidelines` (`--skill use-modern-go` 只裝這個); update 走 `npx skills update use-modern-go -p -y` (project) 或 `-g` (global).
  - **Local CLI install (first-use auto)**: 任何 host 跑 first time 時, CLI 自動 `go install` 進 `~/.cache/go-modern-guidelines` cache, **不會 modify project**. 要求 **Go 1.25+** toolchain available; on older Go 仍 work if `GOTOOLCHAIN=auto` (default).
  - **DevSource**: `make dev-install` + `export GO_MODERN_GUIDELINES_DEV=1` (在 agent 啟動前 export, agent process 繼承) — local build override released version, 對 Claude Code / Codex / Cursor 一樣生效.
  - **未找到明確 pip/npm 安裝方式**（plugin 形式, 不 publish 到 npm registry）。

- **近期 release：** **未找到 GitHub release**（plugin/skills 形式, 不需 SemVer — README 是 source of truth, plugin marketplace push 取代傳統 release 角色）。**Repo `pushed_at` 2026-08-19 17:39:58 UTC（台北時間 9 天前, 仍在 active development）** + `created_at` 較新 + **2,265★ 累計**、Topics = 6 個 (`ai-agents` / `coding-agent` / `developer-tools` / `go` / `golang` / `guidelines`）+ License = **Apache-2.0（純 permissive, case-A2 with patent grant clause）** + size 估 <1MB（README + `plugin/skills/use-modern-go/SKILL.md` 主檔 + `Makefile` + extensions 清單）。**Sponsors / Owner signals**: 官方 JetBrains project (`official JetBrains project` badge)；對應也有 「**[golang-standards](https://github.com/golang-standards)** + **[Uber Go Style Guide](https://github.com/uber-go/guide)**」等 community guides 的 JetBrains-affiliated 補充，但這份是 **JetBrains 專門 for AI coding agents** 的 first-party artifact（與 community guides 目標受眾相異）。

### `thedotmack/claude-mem`

- **Repo 摘要：** thedotmack 維護的 **「Persistent memory compression system built for Claude Code」** — description 寫「Persistent Context Across Sessions for Every Agent – Captures everything your agent does during sessions, compresses it with AI, and injects relevant context back into future sessions. Works with Claude Code, OpenClaw, Codex, Gemini, Hermes, Copilot, OpenCode + More」。**核心差異化**：(1) **5 lifecycle hooks** — SessionStart / UserPromptSubmit / PostToolUse / Stop / SessionEnd + smart install (cached dependency checker, pre-hook); (2) **Worker service** — Local HTTP API + web viewer UI + search endpoints, managed by **Bun** (JavaScript runtime); (3) **SQLite + Chroma hybrid database** — observations + summaries + sessions stored locally; FTS5 full-text + semantic vector search (Chroma DB); (4) **4 MCP search tools** — `search` (compact index, ~50-100 tokens/result) + `timeline` (chronological context) + `get_observations` (full details, ~500-1,000 tokens/result) + `add_observations` (write path), token-efficient 3-layer workflow (~10x savings); (5) **mem-search skill** for natural language queries + progressive disclosure (layered memory retrieval with token cost visibility); (6) **Cross-platform install**: `npx claude-mem install` 一行主推 + per-IDE `--ide {opencode|antigravity}` 顯式指定 + Claude Code `/plugin marketplace add` + `npm install -g claude-mem` (SDK only, 不註冊 hook) + **`curl -fsSL https://install.cmem.ai/openclaw.sh | bash`** (OpenClaw gateway primary, 自動 handle dep + plugin setup + AI provider config + worker startup + real-time observation feeds to Telegram/Discord/Slack); (7) **Privacy control** via `<private>` tags + cloud sync to cmem.ai (no daemon); (8) **Multi-language README** (28 locales incl. 繁體中文), 92k★ 強 social proof + Vercel OSS Program badge + 22 topics 強 semantic coverage; (9) **Multi-version release cadence** — `v13.16.1` 2 天前 + 27 個 v13.x versions.

- **3W1H：**
  - **What：** JavaScript-based persistent memory layer for AI coding agents (Claude Code primary, OpenClaw / Codex / Gemini / Hermes / Copilot / OpenCode). Five lifecycle hooks + Bun-managed worker service + SQLite FTS5 + Chroma vector + 4 MCP search tools + mem-search skill + 13 hook scripts + Web Viewer UI.
  - **Why：** 解決「**agents 在每個 session start 都是 zero context, 不知道專案 history**」的痛點。Claude-Mem 透過 lifecycle hooks 自動 capture tool usage observations + AI summarize them + 透過 progressive disclosure 在下個 session 注入 relevant context. **3-layer workflow (search → timeline → get_observations) 達成 ~10x token savings**. 同時對 OpenClaw gateway 提供 dedicated shell installer (`curl | bash`) — primary install path 為 gateway ecosystem.
  - **Who：** 用 **Claude Code / OpenClaw gateway / Hermes / Codex / Cursor** 等 agent host 的 power user + developer; 對 **persistent memory substrate** 是 horo-agent 下游 / enterprise-lite product 的 first-class substrate 設計師; 對 **「agents 跨 sessions 連續 context」** 有需求的 project lead; Vercel OSS Program-supported.
  - **How：** (a) **Main install**: `npx claude-mem install` (一行 Claude Code 全 setup) + `npx claude-mem install --ide {opencode|antigravity}` for specific IDE; (b) **Plugin marketplace**: `/plugin marketplace add thedotmack/claude-mem` + `/plugin install claude-mem` in Claude Code session; (c) **SDK only**: `npm install -g claude-mem` (僅 library, 不註冊 hook); (d) **OpenClaw gateway**: `curl -fsSL https://install.cmem.ai/openclaw.sh | bash` (primary install for OpenClaw gateways). After install, restart Claude Code; cross-session context 自動 appear.

- **安裝方式：**
  - **`npx claude-mem install` 一行裝**（**主推**）：`npx claude-mem install`. 然後 **restart Claude Code**. Cross-session context 自動 appear in new sessions.
  - **Per-IDE 顯式指定**：`npx claude-mem install --ide opencode` 或 `npx claude-mem install --ide antigravity`.
  - **Plugin marketplace (Claude Code)**：
    ```text
    /plugin marketplace add thedotmack/claude-mem
    /plugin install claude-mem
    ```
  - **OpenClaw Gateway primary install path**（**Type-22: dedicated shell-installer URL**）：
    ```bash
    curl -fsSL https://install.cmem.ai/openclaw.sh | bash
    ```
    Installer 自動 handles: dependencies install + plugin setup + AI provider configuration + worker startup + optional real-time observation feeds to Telegram / Discord / Slack.
  - **npm SDK only**（不註冊 hook, 不啟動 worker）:
    ```bash
    npm install -g claude-mem
    ```
    ⚠️ **README 明示**：這個只裝 library，**不會** register plugin hooks 或 啟動 worker service。Always install via `npx claude-mem install` 或 `/plugin` commands 才是 full setup.

- **近期 release：** **`v13.16.1`** — **2026-08-26 16:54 UTC 發佈（台北時間昨天 00:54, 2 天前）**, pre-release = false. Body is bug fix release. 整個 `v13.x` 系列 release notes 顯示 release velocity 高, recent: `v13.15.x` (hooks improvements) + `v13.14.x` (worker service 重寫) + `v13.13.x` (MCP integration) + **`v13.4.0` 為 LTS stable branch**. **Repo `pushed_at` 2026-08-26 22:04:47 UTC（台北時間昨天 06:04, 昨天仍動）** + `created_at` 較早 + **92,361★ 累積（**達 92k★ + 1.5 個月衝到 92k**）** + size 估 3-5MB（含 SQLite schemas + Chroma client + Web Viewer UI）. 22 topics 強 semantic coverage (`ai-memory` / `ai-agents` / `claude-agent-sdk` / `claude-agents` / `claude-code` / `claude-code-plugin` / `claude-skills` / `anthropic` / `mem0` / `memory-engine` / `openmemory` / `long-term-memory` / `rag` / `chromadb` / `embeddings` / `artificial-intelligence` / `sqlite` / `supermemory` + 4 more). License = **Apache-2.0**（純 permissive, case-A2 with patent grant clause, README badge `[License Apache 2.0](LICENSE)`). **Authoritative references**: README top badge 顯示 `Vercel OSS Program` + `awesome.re/mentioned-badge.svg` (Awesome Claude Code mention) + `trendshift.io badge` + 28 locale READMEs (含 **`docs/i18n/README.zh-tw.md` 繁體中文版**) — 2026 「**persistent memory layer for cross-agent-context**」的 canonical implementation.

### `AgriciDaniel/claude-obsidian`

- **Repo 摘要：** AgriciDaniel 維護的 **「Build an Obsidian knowledge base that becomes more useful every time you use it」** 開源 — description 寫「Self-organizing AI second brain for Obsidian + Claude Code. Drop any source and Claude reads, links, and files it into one connected knowledge graph of plain Markdown you own. AI note-taking, personal knowledge management (PKM), and an open-source Notion alternative. Based on Karpathy's LLM Wiki pattern」。**核心差異化**：(1) **Compound knowledge loop** — Capture with context (visible inbox + immutable content-addressed copies) → Ground every important claim (source/claim ledgers with authority/freshness/support/contradiction/confidence/review state) → Connect what you learn (linked pages, indexes, Maps of Content, methodology-aware structures, Obsidian Canvas views) → Use the vault again (query, research, retrieve, lint, fold); (2) **15 skills / one system** — `wiki` / `save` / `wiki-ingest` / `wiki-query` / `wiki-lint` / `autoresearch` / `canvas` / `defuddle` / `wiki-fold` / `wiki-mode` / `wiki-retrieve` / `wiki-cli` / `obsidian-markdown` / `obsidian-bases` / `think`, Claude Code 用 `/claude-obsidian:wiki-lint` namespace invocation, other hosts 用 native Agent Skills invocation; (3) **Preview-apply transaction contract (5-step operation)** — 1. read every target and record SHA-256 → 2. let parallel workers return drafts and evidence only → 3. merge into ONE operation bundle → 4. inspect the bundle, then apply once → 5. report operation ID and exact changed paths. **Never silent overwrite, journal backups, atomic replacement, restore prior state if apply can't finish**; (4) **Product / vault trust boundary** — never treats source checkout / plugin cache / contributor state as default vault. Vault selected through `CLAUDE_OBSIDIAN_VAULT` env var, by nearest `.claude-obsidian.json`, or by nearest initialized ancestor. If uncertain, command exits without writing; (5) **4 filing methodologies** — Generic / LYT (Maps of Content + linked atomic notes) / PARA (Projects + Areas + Resources + Archives) / Zettelkasten (stable identifiers + atomic notes + dense links), routing switches without silent reorganization of old notes; (6) **Honest capability boundaries (7 declared)** — Local filesystem sources implemented, images metadata/hash, PDF/EPUB metadata/hash (no built-in semantic extraction), URL/YouTube validated consent + configured external runner, OCR consent + configured external runner, BM25 local deterministic, contextual prefixes / remote models gated by explicit egress consent, Obsidian CLI optional; (7) **Multi-host setup** — Claude Code via `claude --plugin-dir`, Codex / OpenCode / Gemini via `bash bin/setup-multi-agent.sh --host {codex|opencode|gemini}`, Cursor / Windsurf use workspace-local skill discovery.

- **3W1H：**
  - **What：** Python-based Claude Code plugin (Python 3 + scripts/claude-obsidian.py + 15 Agent Skills) that turns Obsidian vault into compound knowledge system with preview-apply transaction contract + provenance ledger + 4 filing methodologies + 7 honest capability boundaries.
  - **Why：** 解決「**AI note workflows 都 stop after saving text, 沒有 ground claims / connect knowledge / 用 vault again**」的痛點。Most AI second-brain products 都是 cloud-locked vendor lock-in. Claude-Obsidian 強 **local-first**（vault 是一般 Markdown 文件）+ **provenance-aware** + **compound knowledge loop** + **transaction contract (preview → apply)** — 對「**給 ownership of your files 不依賴 vendor**」 + 「**multi-host Agent Skills**」(跨 Claude Code / Codex / OpenCode / Gemini / Cursor / Windsurf) 有需求的 user.
  - **Who：** 用 **Obsidian + Claude Code / Codex / OpenCode / Gemini / Cursor / Windsurf** 做 PKM 的 knowledge worker / research analyst / writer / graduate student; 對 **「Karpathy's LLM Wiki pattern」** + **compound knowledge loop** 有興趣的 agent developer; 對 **「local-first + vendor-independent vault」** 有強烈 commitment 的 open-source advocate; 對 **「4 filing methodologies (Generic/LYT/PARA/Zettelkasten)」** 有 varying preference 但不願意重新整理舊有 notes 的 user.
  - **How：** (a) **Source-checkout init**: `git clone https://github.com/AgriciDaniel/claude-obsidian.git` → `cd claude-obsidian` → `export GENERATED_AT="$(date -u +%Y-%m-%dT%H:%M:%SZ)"` + `export OPERATION_ID="init-reviewed"` → `python3 scripts/claude-obsidian.py init "$HOME/Documents/MyKnowledgeVault" --generated-at "$GENERATED_AT" --operation-id "$OPERATION_ID"` (review JSON plan + copy `approved_plan_sha256`) → apply same operation with `--approved-plan-sha256 "<sha256-from-the-plan>" --apply`. (b) **Adopt existing vault**: non-destructive `adopt` workflow in `docs/install-guide.md#adopt-an-existing-vault`. (c) **Vault-side launch**: open new vault in Obsidian → run `claude --plugin-dir /absolute/path/to/claude-obsidian` (起動 Claude Code from vault dir) → start with `/claude-obsidian:wiki` → place source in `inbox/` → invoke `/claude-obsidian:wiki-ingest`. (d) **Multi-host setup (Codex/OpenCode/Gemini)**: `bash bin/setup-multi-agent.sh --host codex` (preview), `bash bin/setup-multi-agent.sh --host codex --apply` (apply).

- **安裝方式：**
  - **`git clone` + Python init preview/apply transaction**（**主推, source-only path**）:
    ```bash
    git clone https://github.com/AgriciDaniel/claude-obsidian.git
    cd claude-obsidian
    export GENERATED_AT="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
    export OPERATION_ID="init-reviewed"
    # Preview pass: review the JSON plan
    python3 scripts/claude-obsidian.py init "$HOME/Documents/MyKnowledgeVault" \
      --generated-at "$GENERATED_AT" --operation-id "$OPERATION_ID"
    # Apply pass: copy approved_plan_sha256 from preview
    python3 scripts/claude-obsidian.py init "$HOME/Documents/MyKnowledgeVault" \
      --generated-at "$GENERATED_AT" --operation-id "$OPERATION_ID" \
      --approved-plan-sha256 "<sha256-from-the-plan>" --apply
    ```
  - **Adopt existing vault** (non-destructive): 見 [`docs/install-guide.md`](docs/install-guide.md#adopt-an-existing-vault).
  - **Launch Claude Code from vault**:
    ```bash
    cd "$HOME/Documents/MyKnowledgeVault"
    claude --plugin-dir /absolute/path/to/claude-obsidian
    ```
    然後 `/claude-obsidian:wiki` 起動 wizard → drop source 到 `inbox/` → `/claude-obsidian:wiki-ingest` ingest → `/claude-obsidian:save` save answer → `/claude-obsidian:wiki-query` query vault.
  - **Multi-host setup (Codex / OpenCode / Gemini)**:
    ```bash
    bash bin/setup-multi-agent.sh --host codex        # preview
    bash bin/setup-multi-agent.sh --host codex --apply # apply
    ```
    Cursor / Windsurf 用 workspace-local skill discovery (no global install).
  - **未找到明確 pip/npm 安裝方式** (source-only + Python scripts, 沒有 PyPI publish)。`bin/setup-multi-agent.sh` 是 wrapper, repo IS the product.

- **近期 release：** **`v2.1.1`「Claude Obsidian v2.1.1 - Legacy Migration Safety」** — **2026-08-25 23:32 UTC 發佈（台北時間昨天 07:32, 3 天前）**, pre-release = false. Release title 直接命中 「**Legacy Migration Safety**」主題, 表示這個 release 修的是把既有 Obsidian vault 移植進 claude-obsidian 時的安全性 / 不可逆 operation。**Repo `pushed_at` 較近 3 天內 + `created_at` 較新（5 個月內從 0 衝上）+ 26k+★ estimation** (trending HTML 顯示 `634★/day` strong viral tail). 6+ topics 強 semantic coverage (`agent` / `claude` / `gpt-image-2` / `rag` / `skills` / `web-design`). License = **MIT** (純 permissive, case-A verified via file). **Authoritative references**: README top badges `[MIT license] [Agent Skills compatible](https://agentskills.io) [Claude Code plugin] [Release v2.1.1]` — 4 badges all confirmed. **Companion artifacts**: README 內有完整 `## Operator reference` section 對 portable CLI + multi-host setup scripts + 各 host 的 specific install 指令, 對 owner-friendly documentation.
