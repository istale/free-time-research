---
title: GitHub 自由探索 2026-09-22（14:00 台北時間）
date: 2026-09-22
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 7 天 repeat）
  - web_search 當日 AI 開源新聞（Tier-B web 探索 2）
  - GitHub REST API `/repos/<slug>` + `/releases` + raw `README.md` + npm registry
---

# GitHub 專案動態

- 檢查時間：2026-09-22（14:00 台北時間，週二）
- 檢查對象：`anthropics/skills` / `JuliusBrussee/caveman` / `blader/humanizer` / `magnitudedev/magnitude` / `Fission-AI/OpenSpec`
- 來源組合：GitHub Trending today 解析後 Tier-A top 3 排除前 7 天 repeat（`fmtlib/fmt` #1 9-19 pick 跳過、`mattpocock/skills` #2 9-19 + 9-20 pick 跳過、`NousResearch/hermes-agent` #3 9-17 pick 跳過、`DietrichGebert/ponytail` #4 9-19 pick 跳過、`affaan-m/ECC` #6 9-21 pick 跳過、`addyosmani/agent-skills` #12 9-16 + 9-20 pick 跳過、`debpalash/VoiceStudio` #17 9-15 pick 跳過、所以實際 Trending top 3 = **`anthropics/skills` 281★ today #5** + **`JuliusBrussee/caveman` 543★ today #7** + **`blader/humanizer` 1208★ today #8`**）。Tier-B web_search 命中兩個 strong leads：**`magnitudedev/magnitude`**（open source inference engine optimized for consumer hardware、Apple Silicon / NVIDIA / AMD / CPU、profilers + recommenders + speculative-decoding tuner、Apache-2.0、TypeScript、4,782★、`pushed_at` 2026-09-22 04:58 UTC **今天仍在 push**、topics 空但 description 顯式列 **Pi / OpenCode / Hermes / OpenClaw / Codex / Claude Code / Oh My Pi / Cline** = **8-harness explicit Hermes signal**、GH release `@magnitudedev/cli@0.1.3` published 2026-09-18、npm `@magnitudedev/cli` latest = `0.0.15` = **registry-lag 反例：GH release 用 scoped `@magnitudedev/cli@0.1.3` 但 npm latest 是 unscoped `0.0.15`、GH 領先 npm 3 天**）+ **`Fission-AI/OpenSpec`**（Spec-driven development for AI coding assistants、TypeScript、MIT、69,791★、topics 10 個含 `sdd / spec-driven-development / sdlc / prd / context-engineering`、GH release `v1.13.1` published 2026-09-17、npm `@fission-ai/openspec` latest = `1.13.1` **完全同步**、dist-tags = `next:0.3.0` + `beta:1.6.0-beta.1` + `latest:1.13.1` = **3 軌 release engineering**、30+ AI assistants via slash commands、`/opsx:explore` + `/opsx:propose` + `/opsx:apply` + `/opsx:archive` 是 default profile workflow、brownfield + greenfield 都吃、`openspec init` 一行 init）。

---

## Repo 摘要與 3W1H

### `anthropics/skills`

- **Repo 摘要：** 「**Skills are folders of instructions, scripts, and resources that Claude loads dynamically to improve performance on specialized tasks.**」——Anthropic 官方 2025-09-22 開源、滿一年的「**Claude Agent Skills 標準 reference implementation**」、**spec 跟 sample 雙軌**：repo 同時持有 (1) `./skills/` 含 Creative & Design / Development & Technical / Enterprise & Communication / Document Skills 四類 sample skills + (2) `./spec/` 是 **Agent Skills specification**（agentskills.io 標準）+ `./template/` 是 skill template。177,527★、Python（README + YAML + Markdown mix、API 顯示 Python）、**license None（README 寫「Many skills in this repo are open source (Apache 2.0)」、但 root 沒有 LICENSE 檔、個別 skills 子資料夾各自帶 LICENSE）**、topics 1 個（`agent-skills`）、`created` 2025-09-22（**恰好 1 歲 = 一年 anniversary**）、`pushed_at` 2026-09-10 19:44 UTC（**12 天前 = 進入穩定期**）、`updated_at` 2026-09-22 05:56 UTC、`open_issues_count` 1,263、size 4.7 MB。**未找到 GitHub release**（`/releases/latest` 回空）—— **「ship via main + 不切 formal release」型態**、跟 09-17 `cloudflare/security-audit-skill` 9-19 `markfulton/ai-employees` 09-21 同類「**vendor-official skill bundle + no semver**」。**Sample skills 結構**：每個 skill 是一個資料夾、含 `SKILL.md`（YAML frontmatter + Markdown body）、YAML 只需兩個必填欄位 `name` + `description`。**核心定位**：「**Equipping agents for the real world with Agent Skills**」（Anthropic engineering blog 文）、把 Skills 定位成「**Claude 動態載入的能力模組**」而不是 prompt 片段、**Skills = folders of instructions + scripts + resources** = 比 prompt 更結構化、比 MCP 更貼 Claude runtime。**4 大 sample skill 類別**：(1) **Creative & Design**（art / music / design）、(2) **Development & Technical**（test web apps / generate MCP server / brand application review）、(3) **Enterprise & Communication**（communications / branding）、(4) **Document Skills**（`skills/docx` `skills/pdf` `skills/pptx` `skills/xlsx` = Claude 內建文件能力 source-available reference、**不是 open source**、是 Anthropic 用來 ship Claude 文件能力的 source code）。**對主人的對位**：跟 09-16 `addyosmani/agent-skills` + 09-17 `cloudflare/security-audit-skill` + 09-18 `Tencent/BrowserSkill` + 09-19 `mattpocock/skills` + 09-21 `affaan-m/ECC` + 09-21 `markfulton/ai-employees` + 09-21 `yylo-dev/yylo` 同類「agent skill bundle / framework」、但這個獨佔「**Anthropic 官方 spec authority**」（repo `./spec/` 是 agentskills.io 標準）+ 「**1 year mature + 177k stars**」+ 「**Document Skills 是 Claude production 能力的 source code reference**」 niche。
- **3W1H：**
  - **What：** 一個 GitHub repo 雙軌 = (1) `skills/` 4 大類 sample skills（Creative / Development / Enterprise / Document）+ (2) `spec/` Agent Skills specification（YAML frontmatter schema + Markdown body rules + how Claude loads skills dynamically）+ (3) `template/` skill template starter + 4 個 document skills（`docx/pdf/pptx/xlsx`）是 **Claude production 用的 source-available reference code**。
  - **Why：** 現有 AI agent skill ecosystem 三大對位：(1) **skill 沒有標準**——每個 agent framework 各自定義 skill 格式（affaan-m/ECC 有自己的 skills、addyosmani/agent-skills 有自己的、mattpocock/skills 有自己的、Tencent/BrowserSkill 有自己的）= **skill 在不同框架不可移植**、(2) **skill 只有 prompt 片段**——不是 production-grade module、(3) **企業用戶不知道從哪裡 start**。**anthropics/skills 解三對位**：(a) **`spec/` 是 agentskills.io 標準** = 任何框架可以 adopt（README 寫「For information about the Agent Skills standard, see agentskills.io」）、(b) **`SKILL.md` 結構 = YAML frontmatter + Markdown** = 簡單、可人類讀、可 grep、(c) **4 大類 + Document Skills sub-tree** = starter pack 給個人 + 企業 + creative 三種 user。**為什麼是 vendor-official 而非 community-driven**：Anthropic 同時是 Claude model 作者 + Claude.ai / Claude Code / Claude API 平台作者 = 「**Skills 標準 = platform 定義者定**」= de facto 標準。
  - **Who：** **AI agent skill author** 想 ship skill 跨平台（Claude Code + Claude.ai + Claude API）給 user 用的；**enterprise developer** 想用 Anthropic pre-built document skills（`docx/pdf/pptx/xlsx`）整合進自家產品的；**Claude API developer** 想透過 Skills API 在 production 用 Anthropic pre-built + custom skill 的；**research lab** 想 study agent skill spec evolution 的。
  - **How：** **(1) Claude Code 用**：`/plugin marketplace add anthropics/skills` + `/plugin install document-skills@anthropic-agent-skills` 或 `/plugin install example-skills@anthropic-agent-skills` —— 然後在 Claude Code session 內直接 prompt「Use the PDF skill to extract the form fields from `path/to/some-file.pdf`」。**(2) Claude.ai 用**：所有 sample skills 都 already available 在 paid plans、可 upload custom skills 透過 Claude.ai UI（[support 教學](https://support.claude.com/en/articles/12512180-using-skills-in-claude)）。**(3) Claude API 用**：`POST /v1/skills` + Anthropic pre-built skill ID（[Skills API Quickstart](https://docs.claude.com/en/api/skills-guide#creating-a-skill)）。**(4) 創建 custom skill**：從 `template-skill/` 拷貝 → 改 `SKILL.md` 的 YAML frontmatter（只需 `name` + `description`）→ 填 instructions body。
- **安裝方式：**
  - **Claude Code 安裝（最快路徑，官方推薦）：** **`/plugin marketplace add anthropics/skills`** + **`/plugin install document-skills@anthropic-agent-skills`** 或 **`/plugin install example-skills@anthropic-agent-skills`**。Claude Code 用 browse-and-install UI 也可。或 `npx skills add anthropics/skills -g`（透過 skills.sh CLI）。
  - **Claude.ai：** 直接在 Claude.ai UI 透過 Settings → Features → Skills 上傳 custom skill 或啟用 pre-built skills。Paid plans 預設 available。
  - **Claude API：** 透過 Skills API endpoints（`POST /v1/skills` create custom skill + `GET /v1/skills/{id}` list），需先在 console 建立 skill。
  - **直接 clone reference source：** **`git clone https://github.com/anthropics/skills`** + 看 `skills/` sub-folders 學 sample 結構 + 看 `spec/` 學 agent skill 標準 + 看 `template/` 開始 custom skill。
  - **pip / uv / poetry：** **不適用**（repo 是 skills bundle + Markdown + YAML、無 Python package；個別 Document Skills sub-dir 是 reference source code 不是 publishable package）。
  - **npm / npx：** **`npx skills add anthropics/skills -g`**（透過 [skills.sh](https://skills.sh/anthropics/skills) CLI 統一安裝 protocol、跨 30+ agents 通用）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可走 **`/plugin marketplace add anthropics/skills`** 在 Claude Code 內裝、或 **`npx skills add anthropics/skills -g`** 透過 skills.sh CLI 裝。HORO-Agent 對位：可直接 adopt `spec/SKILL.md` schema（YAML frontmatter `name` + `description` + Markdown body）作為 HORO-Agent 自家 skill format、或 ship 同樣 structure 給 Hermes Agent。
- **近期 release：** **未找到 GitHub release**（`/releases/latest` 回空陣列、`/releases` 全空）—— 但 README + spec 持續在 main 更新（**`pushed_at` 2026-09-10 19:44 UTC = 12 天前最後一次 push**、**`updated_at` 2026-09-22 05:56 UTC = 今天仍有 metadata 更新 = issues / PRs 仍在處理**）、**ship via main + 無 semver =「reference implementation」型態**。**1 year mature（`created` 2025-09-22）= 進入穩定期**、`pushed_at` 12 天前 = 「**不是 daily push、是有 event 才 push**」= 對位 09-21 `affaan-m/ECC`「13 天前 release + 每天 push」、`anthropics/skills` 是「**沒 release + selective event-driven push**」是 vendor-official reference repo 的典型 cadence。**對位 `cloudflare/security-audit-skill`（09-17）「無 release」+ `markfulton/ai-employees`（09-21）「無 release」+ 09-17 `apache/maka` 「無 release」同類「vendor / community-maintained skill bundle 不切 formal semver」**。

### `JuliusBrussee/caveman`

- **Repo 摘要：** 「**why use many token when few do trick. Viral skill + proxy for coding agents that cuts 65% of tokens by talking like a caveman.**」——Julius Brussee（個人 maintainer、**#1 Hacker News 904 points 366 comments** + Product Hunt **#8 Product of the Day** + **JetBrains lab test** + **Adobe Research CAVEWOMAN paper** + **ThePrimeagen reaction video** + **GitHub Trending #1 July 2026**）2026-04-04 開源、6 個月衝到 107,220★ 的「**token-cost 削減 viral hit**」。**3 層產品線**：(1) **Skill**（rule file、30+ agents 通用、MIT free forever）、(2) **Proxy**（本地 machine 跑、介於 agent 跟 AI provider 之間、壓縮 agent 讀進來的 token、`@caveman-ai/cli` + `caveman setup --install` + `caveman claude/codex/gemini/aider/kilo/qwen/opencode/hermes/openclaw/pi`）、(3) **Middleware**（給 developer 整合進自家 LangChain / Vercel AI SDK / OpenAI / Anthropic code、TypeScript + Python 3.13+）。107,220★、Go、**license NOASSERTION（API 顯示）但實際是 MIT + BSL-1.1 dual**：README badge 寫「MIT + BSL」、LICENSE 檔第一行寫「this MIT license covers this repository except Engine-linked directories listed in LICENSING.md (engine/, proxy/, rewriter/, browse/, mcp/, shrink/, cavemem Go core, shared/platform/), which are licensed [under BSL]」、**目錄分層式 license**：client-side skill/CLI 是 MIT 可自由用、engine/proxy/middleware 是 BSL-1.1 防止競爭對手 fork 成 SaaS。topics 10 個（`ai / anthropic / caveman / claude / claude-code / llm / meme / prompt-engineering / skill / tokens`）、`created` 2026-04-04（**6 個月**）、`pushed_at` 2026-09-22 02:10 UTC（**今天仍在 push**）、`updated_at` 2026-09-22 05:57 UTC、`open_issues_count` 127、size 14 MB、homepage `https://docs.caveman.so/docs/quickstart`。**Latest GH release `v2.7.0` published 2026-09-15 04:01 UTC（7 天前）** + **`bin-v1.1.7` 2026-09-15 01:20 UTC（7 天前）**、**npm `@caveman-ai/cli` latest = `1.3.4`（npm view 2026-09-22）+ `@caveman-ai/middleware` alpha = `0.1.0-alpha.2`** = **GH release 跟 npm CLI 對齊（npm latest `1.3.4` 在 9-15 後 publish）、但 middleware npm 仍 alpha = 雙軌產品線 release engineering**。**30+ agents 支援**：Claude Code / Codex / Gemini / Cursor / Windsurf / Cline / Copilot / Aider / Kilo / Qwen / OpenCode / **Hermes** / OpenClaw / Pi = **11+ agents explicit mention + 30+ total**。**對主人的對位**：跟 09-15 `666ghj/MiroFish`（swarm-intelligence prediction）+ 09-15 `caura-ai/caura` + 09-15 `zylos-ai/zylos-core` + 09-19 `mattpocock/skills`（skill bundle）+ 09-21 `markfulton/ai-employees`（small-business employee）同類「**個人 maintainer viral hit + AI agent 工具**」、但這個獨佔「**token-cost 削減 + 3 層產品（skill / proxy / middleware）+ MIT + BSL dual license + HN #1 + Product Hunt #8 + JetBrains + Adobe Research paper citation**」 niche + 「**11+ agents explicit Hermes mention**」= 主人若關心 cost optimization 跟 token economy 對位極高。
- **3W1H：**
  - **What：** 3 層產品線 = (1) **Skill layer**（rule file、`SKILL.md` format、30+ agents 通用、MIT、free forever）、(2) **Proxy layer**（`@caveman-ai/cli` Go binary、`caveman setup --install` 自動偵測 agent、介於 agent 跟 AI provider 之間、壓縮 agent 讀進來的 token、BSL-1.1）、(3) **Middleware layer**（`@caveman-ai/middleware` + `caveman-middleware`、TypeScript / Python 3.13+、給 developer 在自家 code 用、6 行 code wrap 既有 LangChain / Vercel AI SDK / OpenAI / Anthropic call）。
  - **Why：** 現有 AI agent token cost 三大對位：(1) **Agent 寫得多**（prompt throat-clearing、「I'd recommend using useMemo」之類 69 tokens 的 prose = 19 tokens 的 caveman）= **billed 兩端（write + read）**、(2) **Agent 讀得更多**（logs / test output / JSON / diffs / search results = firehose）、(3) **沒人壓縮 input token**。**Caveman 解三對位**：(a) **Skill layer 壓縮 write**：agent 改成 caveman voice（"New object ref each render. Inline object prop = new ref = re-render. Wrap in `useMemo`."）= 保留 technical accuracy 但去掉 prose padding、**code / commands / file paths / error messages 不被 cavemanned**、security warnings 跟「are you sure?」confirmations 保持 full sentence、(b) **Proxy layer 壓縮 read**：介於 agent 跟 provider 之間、agent 收到的 logs / test output / JSON / diffs 都被 shrink、**shrunk byte 仍有 backup 讓 agent 隨時 pull 原始**、(c) **Middleware layer 在 user code 內**：LangChain / Vercel AI SDK / OpenAI / Anthropic call 包 6 行 = tool results 進 model 前先 shrink、原始留在 history、model 隨時 fetch back。**近期受關注原因**：(a) **HN #1 904 points** + **(b) Adobe Research CAVEWOMAN paper cite caveman = 1.4-2.4× cost reduction up to 3×** + **(c) JetBrains lab test 86 tasks = "costs you nothing measurable in quality"** + **(d) ThePrimeagen reaction** + **(e) Trending #1 July 2026** + **(f) Product Hunt #8 Product of the Day**。
  - **Who：** **AI agent user** 想省 token bill 的（Claude Code / Codex / Gemini / Cursor / Windsurf / Cline / Copilot / Aider / Kilo / Qwen / OpenCode / **Hermes** / OpenClaw / Pi = 14+ agents 顯式支援）；**AI agent platform developer** 想在自家 LangChain / Vercel AI SDK / OpenAI / Anthropic code 內 token-optimize 的（Python 3.13+ 或 TypeScript）；**enterprise CTO** 想降 agent 雲端 API 成本的（Caveman 廣告 claim「**1.4-2.4× 削減**」）；**個人 maintainer / startup founder** 想從 joke project → viral hit → Adobe Research paper citation path 學習的。
  - **How：** **(1) Small rock（skill layer，最快路徑）：** `npx skills add JuliusBrussee/caveman -g` —— 一行裝好、agent 自動 wake up、若沒 wake 在 prompt 內打 `/caveman`。**(2) Big rock（proxy layer）：** `npm install -g @caveman-ai/cli && caveman setup --install` 自動偵測所有 installed agent + `caveman claude`（或 `codex / gemini / aider / kilo / qwen / opencode / hermes / openclaw / pi`）。**(3) Your own app（middleware layer）：** TypeScript：`npm install @caveman-ai/middleware @caveman-ai/sdk` + wrap LangChain / Vercel AI SDK / OpenAI / Anthropic call；Python 3.13+：`pip install 'caveman-middleware[langchain]' caveman-sdk`（swap extra 給 OpenAI / Anthropic）。**(4) Full installer（wires up Claude Code hooks + statusline badge）：** `curl -fsSL https://raw.githubusercontent.com/JuliusBrussee/caveman/v2.7.0/install.sh | bash`（需 Node.js 22.13+）；Windows PowerShell 5.1+ 有對應 `irm` 版本。**(5) Stacking：** skill + proxy + middleware 可疊加（多數人從 small rock 開始、再升 big rock、最後在自己 code 內用 middleware）。
- **安裝方式：**
  - **Small rock — skill layer（最快路徑，30+ agents 通用）：** **`npx skills add JuliusBrussee/caveman -g`** —— 透過 skills.sh CLI 統一 install、agent 自動 wake、若沒 wake 打 `/caveman`。**npm fallback**：`npm install -g @caveman-ai/cli && caveman skill install`。
  - **Big rock — proxy layer（推薦多 agent 用法）：** **`npm install -g @caveman-ai/cli`** + **`caveman setup --install`**（自動偵測所有 installed agent）+ **`caveman claude`**（或 `codex / gemini / aider / kilo / qwen / opencode / hermes / openclaw / pi`）。
  - **Your own app — middleware layer TypeScript：** **`npm install @caveman-ai/middleware @caveman-ai/sdk`** + 你的 framework（`ai / openai / anthropic-ai`）+ 6 行 wrap。
  - **Your own app — middleware layer Python 3.13+：** **`pip install 'caveman-middleware[langchain]' caveman-sdk`**（swap extra：`[openai]`、`[anthropic]`、`[vercel-ai]`）。
  - **Full installer（hooks + statusline badge）：** **`curl -fsSL https://raw.githubusercontent.com/JuliusBrussee/caveman/v2.7.0/install.sh | bash`** —— Node.js 22.13+。Windows PowerShell 5.1+：`irm https://raw.githubusercontent.com/JuliusBrussee/caveman/v2.../install.ps1 | iex`（truncated、see README）。
  - **pnpm / yarn / bun：** 對應 `pnpm dlx skills add JuliusBrussee/caveman -g` / `bunx skills add JuliusBrussee/caveman -g`。
  - **pip / uv / poetry：** **部分適用** —— **`pip install 'caveman-middleware[langchain]' caveman-sdk`**（Python 3.13+、需選對 framework extra）。uv 等同 `uv pip install 'caveman-middleware[langchain]' caveman-sdk`。**Poetry**：`poetry add caveman-middleware caveman-sdk` + 加 framework extra。
  - **Source clone：** **`git clone https://github.com/JuliusBrussee/caveman`** + 看 `skill/`（MIT）+ `engine/` `proxy/` `rewriter/` `browse/` `mcp/` `shrink/` `cavemem/` `shared/platform/`（BSL-1.1）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可走 **`npx skills add JuliusBrussee/caveman -g`**（最快、5 秒裝完）+ 後續升級 **`npm install -g @caveman-ai/cli && caveman setup --install`**（proxy 層、10 agents 自動偵測含 **Hermes**）+ Python middleware 用 **`pip install 'caveman-middleware[langchain]' caveman-sdk`**（Python 3.13+ 但主人 VM 可能 Python < 3.13、需 check）。
- **近期 release：** **GH release `v2.7.0` published 2026-09-15 04:01 UTC（7 天前）** = latest stable GH release。**npm `@caveman-ai/cli` latest = `1.3.4`**（從 release 歷史推測 9-15 後 publish、npm view 2026-09-22 確認 latest=1.3.4）= GH release 跟 npm CLI 對齊。**npm `@caveman-ai/middleware` latest = `0.1.0-alpha.2`**（dist-tag `alpha`、`latest` 相同）= middleware 仍 alpha 雙軌產品線。**GH release tag 還有 `bin-v1.1.7` published 2026-09-15 01:20 UTC（7 天前、跟 `v2.7.0` 同日）** —— 推測 bin 系列是 Go binary release、`v2.x` 系列是 source / npm release = **dual tag release engineering**（Go binary + npm JS package 各有 version cadence）。**Main 仍 active push**（`pushed_at` 2026-09-22 02:10 UTC **今天仍在 push**）。**Release cadence 推測**：GH release ~7-11 天一版（從 9-04 v2.6.0 → 9-15 v2.7.0 + bin-v1.1.7 = 11 天），**npm CLI daily publish**（從 README claim + npm view）。對位 09-21 `yylo-dev/yylo`「GH release 23 天前 + npm daily」、Caveman 是「**GH release weekly + npm daily + Go binary 雙軌**」是 personal-maintainer-viral-hit 的 release engineering 典型樣態。**License 雙軌**：README badge `MIT + BSL`、LICENSING.md 列 `engine/proxy/rewriter/browse/mcp/shrink/cavemem/shared/platform` 為 BSL-1.1、其餘（含 skill）為 MIT = **目錄分層式 license** = 對主人 horo-agent downstream 若要 fork 成 SaaS 會撞 BSL-1.1 限制（commercial restriction 4 years）、但用 skill / CLI 則完全 MIT free。

### `blader/humanizer`

- **Repo 摘要：** 「**Agent skill that removes signs of AI-generated writing from text**」——blader（個人 maintainer、2026-01-18 開源、9 個月衝到 51,157★ 的「**AI writing detector 對位工具**」）的「**rewrite AI-sounding text so it reads like a person wrote it, without changing what it says**」skill。**25 個 numbered patterns** 按 strength 排序、最強 5 個 pattern single sighting 就 justify edit、`*weak alone*` pattern 需多個同時出現。**3 種 install path**：(1) **`npx skills add blader/humanizer --global`**（skills.sh CLI 跨 30+ agents 通用）、(2) **`/plugin marketplace add blader/humanizer` + `/plugin install humanizer@humanizer`**（Claude Code 2.1.142+ native plugin）、(3) **Claude Desktop：download ZIP → upload as skill** 或 manual 拷貝 `SKILL.md`。51,157★、Python、**MIT**、topics 7 個（`agent-skills / ai-writing / claude-code / codex / cursor / prompt-engineering / writing-tools`）、`created` 2026-01-18（**9 個月**）、`pushed_at` 2026-09-06 20:26 UTC（**16 天前**）、`updated_at` 2026-09-22 05:58 UTC、`open_issues_count` 24、size 205 KB（**極小 = pure skill bundle、不是 monorepo**）、homepage `https://skills.sh/blader/humanizer`。**Latest GH release `v3.0.0` published 2026-09-06 20:26 UTC（16 天前）**（"Humanizer v3.0.0" 標題、**major bump** 因為加了 voice-matching 跟 25 patterns 結構化）。**GH release cadence**：v3.0.0 (9-06) → v2.11.1 (8-18) → v2.11.0 (8-17) = **~20 天一個 minor、9-06 是 major v3**。**3 大功能**：(1) **直接 rewrite**：`/humanizer` + paste text → show its work（first rewrite + critique + final version）、(2) **Voice matching**：paste 2-3 paragraphs 自己的 writing → Humanizer follow sample's rhythm / word choice / punctuation / deliberate quirks（含 dashes）、(3) **File rewrite**：`Humanize the prose in docs/launch-post.md` → 改 prose 但不改 code / data / frontmatter / link targets。**核心 philosophy**：「**A language model writes whatever is most likely to come next, so by default it makes the choice that fits the widest range of readers and subjects. A person chooses for one reader and one subject.**」—— Wikipedia「Signs of AI writing」是 Humanizer 的判讀 reference。**對主人的對位**：跟 09-21 `markfulton/ai-employees`（SEO/AEO Employee routine 寫 blog post）+ 09-21 `yylo-dev/yylo`（workflow）同類「**個人 maintainer AI writing 工具**」、但這個獨佔「**AI writing detection 反向 + 25 numbered patterns + voice matching**」 niche。
- **3W1H：**
  - **What：** 一個極小 skill repo（205 KB = pure Markdown + YAML）+ `SKILL.md` 為核心 + 25 個 numbered patterns（按 strength + frequency 排序、5 個最強 pattern single-sighting justify edit、`*weak alone*` pattern 需多個同現）+ 3 個 invocation mode（`/humanizer` slash、plain language prompt、file path rewrite）。
  - **Why：** 現有 AI-generated text 三大病灶：(1) **AI 寫得像 AI**（"delve into"、"tapestry"、"in conclusion"、em dash 過度使用、rule-based formatting、「pivotal moment」式 dressing-up）、(2) **讀者疲勞 + 信任流失**（Wikipedia 累積的「Signs of AI writing」是文化現象）、(3) **沒有 systematic rewriting 工具**（Grammarly 不針對 AI tells、ChatGPT 自己 rewrite 仍是 AI tells）。**Humanizer 解三病灶**：(a) **25 numbered patterns** = systematic detection（最強 5 pattern single sighting 就該改、weak pattern 需多個同現才修 = 避免 over-edit）、(b) **rewrite 不動 source structure**（rewrite the draft without treating original structure as fixed 但保留 facts）、(c) **show its work**（first rewrite + critique + final version = transparent、可 audit）。
  - **Who：** **Content writer / blogger / ghostwriter** 用 AI 加速初稿但要 hide AI tells 的；**Marketing team** 想讓 AI-assisted copy 通過 AI detector 的（SEO / Ad copy / Product description）；**Academic writer** 想讓 AI-assisted draft 通過 journal plagiarism + AI detector 的；**Solo developer 寫 README / changelog / blog post** 想要 AI 幫寫但 voice 一致的；**主人 horo-agent** 若 ship 文書生成 skill 可借鑑 25 patterns 結構。
  - **How：** **(1) 直接 invoke slash：** `/humanizer` + paste text。**(2) Plain language：** `Please humanize this text: [your text]`。**(3) File rewrite：** `Humanize the prose in docs/launch-post.md`（保留 code / data / frontmatter / link targets）。**(4) Voice matching：** paste 2-3 paragraphs 自己的 writing + 然後 humanize AI text → Humanizer follow sample's rhythm + word choice + punctuation + dashes。**(5) Show its work：** 自動 output first rewrite + short critique + final version。
- **安裝方式：**
  - **Skills CLI（最快路徑，30+ agents 通用）：** **`npx skills add blader/humanizer --global`** —— 可省略 `--global` 只裝 current project、可加 `--agent <name>` 或 `--agent '*'` 選擇 agent。skill name 是 `/humanizer`。
  - **Claude Code 2.1.142+ native plugin：** **`/plugin marketplace add blader/humanizer`** + **`/plugin install humanizer@humanizer`**。plugin name 是 `/humanizer:humanizer`。
  - **Claude Desktop（manual）：** Download ZIP → 上傳為 skill。或 manual 拷貝 `SKILL.md` 到 agent skill folder。
  - **Manual install：** 拷貝 `SKILL.md` 到 agent 的 skill folder。
  - **pnpm / yarn / bun：** 對應 `pnpm dlx skills add blader/humanizer --global` / `bunx skills add blader/humanizer --global`。
  - **pip / uv / poetry：** **不適用**（repo 是 pure skill bundle + Markdown + YAML、205 KB、無 Python package）。
  - **Source clone：** **`git clone https://github.com/blader/humanizer`**（205 KB、秒 clone）+ 看 `SKILL.md`。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可走 **`npx skills add blader/humanizer --global`**（5 秒裝完、跨 30+ agents 通用）或 Claude Code plugin path。HORO-Agent 對位可直接借鑑 25 patterns 結構 ship 自家 writing 工具。
- **近期 release：** **GH release `v3.0.0` published 2026-09-06 20:26 UTC（16 天前）** = latest GH release、**major bump**（從 v2 → v3 因為加 voice-matching + 25 patterns 結構化）。**前一個 release `v2.11.1` 2026-08-18、往前 `v2.11.0` 2026-08-17 = 20 天一版 minor cadence**。**Main 仍 active push**（`pushed_at` 2026-09-06 20:26 UTC = 跟 v3.0.0 release 同期 push、`updated_at` 2026-09-22 05:58 UTC = issues / PRs 仍在處理）。**Registry-lag check**：Humanizer 沒 publish npm（skill-only、無 npm package）、所以 npm 同步議題不適用、GH release 是 source of truth。**Release cadence 推測**：~20 天一個 minor、v3.0.0 是 9 個月內第一個 major = **「minor bump for patterns addition + major for architecture change」是 personal-skill cadence**。對位 09-15 `caura-ai/caura`「minor weekly」、09-19 `mattpocock/skills`「無 release」、Humanizer 是「**minor bi-weekly + major quarterly**」是 mature personal skill 的 release engineering。

### `magnitudedev/magnitude`

- **Repo 摘要：** 「**Open source inference engine optimized for consumer hardware. Profiles your machine, recommends the best models for it, then downloads, tunes, and runs them. Works on Apple Silicon, NVIDIA, AMD, or nothing but a CPU.**」——magnitudedev（個人 maintainer、`magnitude.dev`、Discord + Twitter `@usemagnitude`）2026-06-12 開源、3 個月衝到 4,782★ 的「**local LLM inference 一鍵安裝 desktop app**」、**與 Ollama / LM Studio 對位** = **它不只 run model、還會先 profile + recommend**。**5 大設計 choice**：(1) **Knows your machine**：profile hardware、estimate tok/s 在 download 之前、(2) **Recommends best models**：ranked by speed + accuracy + intelligence + memory、(3) **Tuned end to end**：speculative decoding 跟更多都 set 給你硬體、(4) **Works with your agent**：one-click connect Pi / OpenCode / Hermes / OpenClaw / Codex / Claude Code / Oh My Pi / Cline = **8-harness integration list**、(5) **Fully private offline**：models + prompts + files 都 on your machine、download 後 no internet needed。4,782★、TypeScript、**Apache-2.0**、**topics 空陣列（無任何 topic）**、`created` 2026-06-12（**3 個月**）、`pushed_at` 2026-09-22 04:58 UTC（**今天仍在 push**）、`updated_at` 2026-09-22 05:56 UTC、`open_issues_count` 20、size 123 MB（**desktop app = 含 native binary 跟 model catalog**）、homepage `https://magnitude.dev`。**Latest GH release `@magnitudedev/cli@0.1.3` published 2026-09-18 18:23 UTC（4 天前）** + `0.1.2` (9-18 03:42) + `0.1.1` (9-18 01:47) = **同一天 ship 3 個 patch**。**npm `@magnitudedev/cli` latest = `0.0.15`**（從 npm view 2026-09-22 確認）、dist-tags `{alpha: 0.0.15-alpha.1, latest: 0.0.15}` = **Registry-lag 反例：GH release 領先 npm 3 天 + GH release 用 `@magnitudedev/cli@0.1.3` scoped、npm latest 用 unscoped `0.0.15` = 雙重反例（version drift + scope 命名錯位）**。**對 Ollama / LM Studio 的明確對位**：「**Magnitude helps you pick. They run whatever model you pick.**」+ 「**Estimates how every model and quant will perform on your machine before you download**」+ 「**Tunes the one you choose for your exact hardware, from context size to speculative decoding**」。**對主人的對位**：跟 09-19 `deepseek-ai/deepseek-harness`（agent execution harness）+ 09-15 `huggingface/funes`（HF inference）+ 09-16 `MemTensor/MemOS`（memory OS）同類「**AI infrastructure 開源 layer**」、但這個獨佔「**consumer hardware inference + profile + recommend + tune + 8-harness integration**」 niche + 「**顯式 Hermes integration**」= 主人若 ship local LLM 對 Hermes Agent 對位極高。
- **3W1H：**
  - **What：** TypeScript monorepo（123 MB = 含 desktop app source + native binaries + model catalog + CLI）+ `@magnitudedev/cli` CLI（雖然 npm unscoped 是 `magnitudedev/cli` 0.0.15、但 GH release 用 scoped `@magnitudedev/cli@0.1.3`）+ desktop app（macOS / Linux / Windows native）+ **8-harness integration adapter**（Pi / OpenCode / Hermes / OpenClaw / Codex / Claude Code / Oh My Pi / Cline）+ model catalog（Apple Silicon / NVIDIA / AMD / CPU 都吃、含 DGX Spark / Strix Halo unified-memory boxes）。
  - **Why：** 現有 local LLM inference 三大對位：(1) **「裝了 Ollama / LM Studio 但不知道裝哪個 model」** —— 你選 model、它 run、你不知道 tok/s until downloaded、(2) **「model 在我的硬體跑得慢」** —— 沒有 tune 給你 hardware、(3) **「model 跟 agent 接不起來」** —— 要手動配 OpenAI-compatible endpoint + API key。**Magnitude 解三對位**：(a) **Profile + recommend** = Magnitude 先 profile 你的 hardware、estimate tok/s、rank by speed + accuracy + intelligence + memory、(b) **Tune end to end** = speculative decoding + context size + quant 都 set 給你 hardware、(c) **One-click connect** = 8-harness integration list 點一下就連。**為什麼是 8 harness 而不是 30+**：Magnitude 鎖 coding-agent 場景、不做 general-purpose local inference（Ollama / LM Studio 是 general）。
  - **Who：** **AI agent user** 想 local LLM 給 coding agent 用、不想付 OpenAI / Anthropic API 的（Pi / OpenCode / Hermes / OpenClaw / Codex / Claude Code / Oh My Pi / Cline = 8 agents）；**Mac mini / Mac Studio user**（Apple Silicon unified memory）想跑大 model 的；**DGX Spark / Strix Halo owner**（unified-memory boxes）想跑 70B+ model 的；**Privacy-conscious developer** 想 model + prompts + files 都 on-machine 的。
  - **How：** **(1) Download desktop app（最快路徑）：** [magnitude.dev/download](https://magnitude.dev/download) → 裝 macOS / Windows / Linux native → 開 app。**(2) 選 model：** Discover tab → 看推薦 → download（profile 完才 rank）。**(3) Connect agent：** Connections tab → 選 Pi / OpenCode / **Hermes** / OpenClaw / Codex / Claude Code / Oh My Pi / Cline → one-click。**(4) CLI：** desktop app includes `magnitude` CLI、no separate install needed。
- **安裝方式：**
  - **Desktop app（最快路徑，官方推薦）：** **[magnitude.dev/download](https://magnitude.dev/download)** → 裝 macOS / Windows / Linux native。App 內含 `magnitude` CLI。
  - **CLI（透過 desktop app bundled）：** desktop app 安裝後 `magnitude` 直接可用。
  - **GH release scoped CLI（manual）：** **`npm install -g @magnitudedev/cli@0.1.3`**（**注意：GH release 用 scoped、但 npm registry 公開 unscoped 版本是 `0.0.15`、這是 registry-lag 反例、install 前先 `npm view @magnitudedev/cli` check**）。
  - **pnpm / yarn / bun：** 對應 `pnpm install -g @magnitudedev/cli@0.1.3` / `yarn global add @magnitudedev/cli@0.1.3` / `bun install -g @magnitudedev/cli@0.1.3`。
  - **Source clone：** **`git clone https://github.com/magnitudedev/magnitude`**（123 MB monorepo、含 native binary build artifacts、clone 時間偏長）。
  - **pip / uv / poetry：** **不適用**（這是 TypeScript + Electron / Tauri 桌面 app + CLI、無 Python package）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可走 **[magnitude.dev/download](https://magnitude.dev/download)**（但 **virtualized Apple Silicon = performance 差、跑 model 會受限**，**Magnitude 在 bare-metal Apple Silicon 才是 sweet spot**）。HORO-Agent 對位可直接吃 Magnitude one-click connect Hermes 的 integration、ship 同樣 8-harness adapter pattern。**注意：M4 Max VM 是 virtual、跑 70B+ model 會 OOM、但 7B-13B quant 應可跑**。
- **近期 release：** **GH release `@magnitudedev/cli@0.1.3` published 2026-09-18 18:23 UTC（4 天前）** = latest stable GH release。**同一天 ship 3 個 patch**（`0.1.1` 01:47 → `0.1.2` 03:42 → `0.1.3` 18:23 UTC、間隔 17 小時內 3 版 = **active hot-fix day**）。**npm `@magnitudedev/cli` latest = `0.0.15`** + **dist-tags `{alpha: 0.0.15-alpha.1, latest: 0.0.15}`** = **Registry-lag 反例雙重**：(a) **Version drift**：GH release 0.1.3 vs npm latest 0.0.15 = GH 領先 npm 3 版、(b) **Scope naming 錯位**：GH release tag 是 `@magnitudedev/cli` scoped、npm registry unscoped latest `magnitudedev/cli` 0.0.15 = **兩個不同的 package identifier**。**推測原因**：(i) GH release 是內部 staging tag、npm latest 是另一個 distribution channel、或 (ii) maintainer 改過 package naming 但 GH release tag 沒跟上、或 (iii) npm `0.0.15` 是早期 release 沒 bump、`@magnitudedev/cli@0.1.x` 是新 pipeline = **建議 install 前 `npm view @magnitudedev/cli` check 實際 available 版本**。**Main 仍 active push**（`pushed_at` 2026-09-22 04:58 UTC **今天仍在 push**）。**Release cadence 推測**：3 個月內 0.1.x → 0.0.15 = early-stage rapid iteration + release tag / npm version 雙軌不對齊 = **「early-stage startup」release engineering**。對位 09-21 `yylo-dev/yylo`「GH release 23 天前 + npm daily」、Magnitude 是「**GH release active hot-fix + npm 落後 3 版 + scope 命名錯位**」是比 yylo 更年輕 startup 的更混亂 release engineering。**Registry-lag 反例提醒**：**README install command 若用 `npm install -g @magnitudedev/cli@latest` 會拿到 `0.0.15`、若要 `0.1.3` 需 `npm install -g @magnitudedev/cli@0.1.3` exact pin**。

### `Fission-AI/OpenSpec`

- **Repo 摘要：** 「**Spec-driven development (SDD) for AI coding assistants.**」——Fission-AI（個人 maintainer、@0xTab on X、Discord server、since 2025-08-05 開源、**13 個月衝到 69,791★**）的「**lightweight spec layer**」、讓 AI coding assistant 在 code 前先 agree on what to build。**5 大哲學**：「**fluid not rigid → iterative not waterfall → easy not complex → built for brownfield not just greenfield → scalable from personal projects to enterprises**」。69,791★、TypeScript、**MIT**、topics 10 個（`ai / context-engineering / engineering / planning / prd / sdd / sdlc / spec / spec-driven-development / specification`）、`created` 2025-08-05（**13 個月**）、`pushed_at` 2026-09-21 19:50 UTC（**昨天仍 push**）、`updated_at` 2026-09-22 05:53 UTC、`open_issues_count` 263、size 9.5 MB、homepage `https://openspec.dev/`。**Latest GH release `v1.13.1` published 2026-09-17 01:11 UTC（5 天前）** = "v1.13.1 - Hardened CLI, safer archives"。**GH release cadence**：v1.13.1 (9-17) → v1.13.0 (9-09) → v1.12.0 (9-03) = **~7 天一個 minor cadence**。**npm `@fission-ai/openspec` latest = `1.13.1`** + dist-tags `{next: 0.3.0, beta: 1.6.0-beta.1, latest: 1.13.1}` = **Registry-lag check PASS**（GH release 跟 npm latest 完全同步）+ **3 軌 release engineering**：`latest` = stable、`beta` = `1.6.0-beta.1`（較高 minor 號但 beta 標記）、`next` = `0.3.0`（較低號但 prerelease）。**5 大 workflow command**（default profile）：`/opsx:explore`（no-stakes thinking partner、讀 code + weigh options + shape plan）+ `/opsx:propose <what-you-want-to-build>`（建 openspec/changes/<name>/ 含 proposal.md + specs/ + design.md + tasks.md）+ `/opsx:apply`（implement tasks.md 逐項）+ `/opsx:archive`（archived to openspec/changes/archive/YYYY-MM-DD-<name>/ + specs updated）。**Expanded profile**（選用）：`/opsx:new`、`/opsx:continue`、`/opsx:ff`（fast-forward）、`/opsx:verify`、`/opsx:bulk-archive`、`/opsx:onboard`。**核心定位**：每個 change 是一個獨立資料夾含 proposal.md（為什麼做）+ specs/（requirements + scenarios）+ design.md（technical approach）+ tasks.md（implementation checklist） = **plan-first before code**。**Cross-repo feature**：**Stores**（beta、planning 在獨立 repo、shared 給整個 team + 多個 coding agent read）= 一個 change 在 platform team 寫 spec、product teams read-only reference、`git push` shared 跨多 repo。**對 Spec Kit / Kiro / nothing 的 3 個對位**：「vs Spec Kit」=「Thorough but heavyweight. Rigid phase gates, lots of Markdown, Python setup. OpenSpec is lighter and lets you iterate freely.」+ 「vs Kiro」=「Powerful but you're locked into their IDE and limited to Claude models. OpenSpec works with the tools you already use.」+ 「vs nothing」=「AI coding without specs means vague prompts and unpredictable results.」。**對主人的對位**：跟 09-21 `yylo-dev/yylo`（typed task + validation + merge + release-readiness 4-stage gate）同類「**AI coding workflow 結構化工具**」、但這個獨佔「**spec-first 而非 validation-after**」+ 「**30+ AI assistants via slash commands**」+ 「**Stores beta for cross-repo**」 niche。**對主人 horo-agent Kanban + N-stage workflow 對位極高**（Spec-Driven N-stage + 每階段 commit 是主人 MEMORY 既有偏好）。
- **3W1H：**
  - **What：** TypeScript monorepo（9.5 MB）+ npm CLI `@fission-ai/openspec` + `openspec/` folder structure（`openspec/specs/` + `openspec/changes/` + `openspec/changes/archive/`）+ 30+ AI assistant slash command integrations（`/opsx:explore` `/opsx:propose` `/opsx:apply` `/opsx:archive` + expanded profile 的 `/opsx:new` `/opsx:continue` `/opsx:ff` `/opsx:verify` `/opsx:bulk-archive` `/opsx:onboard`）+ Stores（beta、cross-repo planning）。
  - **Why：** 現有 AI coding assistant workflow 三大對位：(1) **AI 是 unpredictable 的 without specs**（requirements 在 chat history 內 drift）、(2) **團隊多人多 repo** 不知道哪裡是 single source of truth（wiki drift、requirements 不在 agent 能讀的地方）、(3) **Spec Kit / Kiro 是 heavyweight 或 vendor lock-in**。**OpenSpec 解三對位**：(a) **Spec-first before code** = `/opsx:explore` + `/opsx:propose` 階段先 write proposal.md + specs/ + design.md + tasks.md、agent 跟人都 review、`/opsx:apply` 才 implement、(b) **Stores beta** = planning 在獨立 repo、cross-repo team + multiple coding agent 都能 read、`git push` shared、(c) **30+ AI assistants via slash commands** = Claude Code / Cursor / GitHub Copilot / Codex / Amazon Q / Oh My Pi + more。**5 大哲學直接寫在 README**：「fluid not rigid → iterative not waterfall → easy not complex → built for brownfield not just greenfield → scalable from personal projects to enterprises」。
  - **Who：** **Solo developer** 想 AI-assisted coding 但要 spec-first 對齊的；**Team lead / engineering manager** 想 AI-assisted 但要 plan 在 shared repo 跨 multiple code repo 的；**Brownfield project maintainer**（已有 legacy code）想 adopt SDD 不需 rewrite 的；**AI coding tool author** 想 ship 同樣 slash command framework 的；**主人 horo-agent** 想 ship Spec-Driven N-stage workflow 給主人的（主人 MEMORY「Spec-driven N-stage + 每階段 commit workflow」）。
  - **How：** **(1) Install：** `npm install -g @fission-ai/openspec@latest`（需 Node.js 20.19.0+、pnpm / yarn / bun / nix 都支援）。**(2) Init：** `cd your-project && openspec init`。**(3) Workflow：** `/opsx:explore`（想什麼、不確定）→ `/opsx:propose add-dark-mode`（建 openspec/changes/add-dark-mode/ 含 proposal + specs + design + tasks）→ `/opsx:apply`（implement tasks）→ `/opsx:archive`（archive + specs updated）。**(4) 進階 expanded profile：** `openspec config profile` 選 expanded → `openspec update` apply → 解鎖 `/opsx:new` `/opsx:continue` `/opsx:ff` `/opsx:verify` `/opsx:bulk-archive` `/opsx:onboard`。**(5) AI install：** 把 [setup prompt](docs/installation.md#install-with-your-ai-assistant) paste 進 coding assistant → 它自己 install CLI + 跑 `openspec init` + verify。**(6) Tool-specific slash prefix：** `/opsx:propose`（default）/ `/opsx-propose`（Cursor / GitHub Copilot）/ `@opsx-propose`（Amazon Q）/ `$openspec-propose`（Codex）、`openspec init` 會 print 你 tool 的 right form。
- **安裝方式：**
  - **npm install global CLI（最快路徑，官方推薦）：** **`npm install -g @fission-ai/openspec@latest`** —— 需 Node.js 20.19.0+。**npm view 2026-09-22 確認 latest = `1.13.1` 跟 GH release 同步**。
  - **Init project：** `cd your-project && openspec init`（自動 detect 已安裝的 AI assistant + 寫 `openspec/` folder + register slash commands）。
  - **Prerelease beta：** **`npm install -g @fission-ai/openspec@beta`** = `1.6.0-beta.1`（若想搶鮮）。
  - **Prerelease next：** **`npm install -g @fission-ai/openspec@next`** = `0.3.0`（**注意：`next` 號碼比 `latest` 低、是 OpenSpec 特殊 convention**）。
  - **Exact pin：** `npm install -g @fission-ai/openspec@1.13.1`（CI 場景）。
  - **AI-driven install：** Paste [setup prompt](docs/installation.md#install-with-your-ai-assistant) 進 coding assistant → 它 install CLI + 跑 `openspec init` + verify。
  - **Update：** **`npm install -g @fission-ai/openspec@latest`** + **`openspec update`**（在每個 project 內 refresh agent instructions + slash commands）。
  - **pnpm / yarn / bun / nix：** 都支援、`pnpm install -g @fission-ai/openspec@latest` / `yarn global add @fission-ai/openspec@latest` / `bun install -g @fission-ai/openspec@latest` / `nix profile install npm-@fission-ai/openspec`。
  - **Source clone（for contributor）：** **`git clone https://github.com/Fission-AI/OpenSpec`** + TypeScript source + 看 `src/`。
  - **pip / uv / poetry：** **不適用**（這是 TypeScript + npm distribution、無 Python binding）。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可走 **`npm install -g @fission-ai/openspec@latest`**（需 Node.js 20.19.0+）+ **`openspec init`** 開始 ship spec-driven workflow。HORO-Agent 對位極佳：主人 MEMORY「Spec-driven N-stage + 每階段 commit workflow」可直接 adopt OpenSpec 的 `/opsx:explore` → `/opsx:propose` → `/opsx:apply` → `/opsx:archive` 4-stage、ship 成 HORO Kanban 子 workflow。**注意 OpenSpec 預設 recommend Codex 5.5 / Opus 4.7 做 planning + implementation**、主人用 Qwen 協調 + GPT 審查 model stack 可能 quality 比 Opus 4.7 略低、需自己 verify。
- **近期 release：** **GH release `v1.13.1` published 2026-09-17 01:11 UTC（5 天前）** = "v1.13.1 - Hardened CLI, safer archives" = latest stable GH release。**npm `@fission-ai/openspec` latest = `1.13.1`** = **Registry-lag check PASS**（GH release 跟 npm 完全同步）。**3 軌 release engineering**：dist-tags `{next: 0.3.0, beta: 1.6.0-beta.1, latest: 1.13.1}` = **`next` 號碼比 `latest` 低、是 OpenSpec 特殊 prerelease convention**（通常 next 應比 latest 高、但 OpenSpec 用 next 當 experimental 短期 prerelease channel、beta 才是 long-running prerelease）。**GH release cadence**：v1.13.1 (9-17) → v1.13.0 (9-09) → v1.12.0 (9-03) = **~7 天一版 minor cadence**（v1.13 系列 9-09 → 9-17 = 8 天、含 v1.13.0 + v1.13.1 兩版）。**Main 仍 active push**（`pushed_at` 2026-09-21 19:50 UTC = 昨天仍 push）。**對位 09-21 `vercel-labs/json-render`「3-7 天一版」**、OpenSpec 是「**~7 天一版 minor、daily push**」是 mature active maintainer 的 release engineering。對位 09-21 `yylo-dev/yylo`「registry-lag 反例 21 天」、OpenSpec 是「**registry-lag check PASS + 3 軌 release engineering**」是 mature release engineering 反例對照組。

---

## 重點觀察

- **5/5 permissive license 3-consecutive-tick 持續、今天 4/5 MIT + 1 Apache-2.0 + 1 NOASSERTION 混合 = 5/5 acceptable 但多樣性提高**：今天 license 分布：**`anthropics/skills` None（README 寫 many skills Apache 2.0、個別 skills sub-folder 自帶 LICENSE、根目錄無 LICENSE = 「mixed: Apache 2.0 + source-available」）** + **`JuliusBrussee/caveman` NOASSERTION（API 顯示）+ 實際 README badge 寫「MIT + BSL-1.1 dual」、目錄分層式 license（skill/CLI MIT、engine/proxy/middleware BSL-1.1）** + **`blader/humanizer` MIT** + **`magnitudedev/magnitude` Apache-2.0** + **`Fission-AI/OpenSpec` MIT** = **3 個明確 permissive（2 MIT + 1 Apache-2.0）+ 1 雙軌（MIT skill + BSL-1.1 engine）+ 1 mixed（Apache 2.0 + source-available）**。**連續 milestone**：09-19 (1/5 NOASSERTION) → 09-20 (4/5 permissive) → 09-21 (5/5 permissive) → **09-22 (3 explicit + 2 mixed-but-permissive = 5/5 acceptable 但 diversity 提高)**。**對主人 horo-agent downstream 影響**：(a) **`anthropics/skills` 的 Document Skills（docx/pdf/pptx/xlsx）是 source-available 不可 ship 進 horo-agent binary、但 sample skills 是 Apache 2.0 可 ship**、(b) **`caveman` 的 skill/CLI 是 MIT free forever、但 engine/proxy/middleware 是 BSL-1.1 = fork 成 SaaS 4 年內禁止**、(c) **`magnitudedev/magnitude` Apache-2.0 對主人 horo-agent downstream 有 patent grant bonus**。**報告時應明確分「**3 個明確 permissive** + 2 個 mixed-but-permissive」而非只標 permissive tick**，因為 dual license 跟 source-available 對 horo-agent enterprise downstream 的 fork-to-SaaS 路徑有實質影響。

- **Hermes-as-official-agent-host signal 連續第 3 個 14:00 tick（09-20 2/5 → 09-21 2/5 → 09-22 2/5）+ 兩個 pick 都把 Hermes 列為 first-class integration target**：今天 2/5 picks 顯式 mention Hermes Agent：(a) **`JuliusBrussee/caveman`** README Quick Start `caveman hermes` 列在 proxy 支援清單（`claude / codex / gemini / aider / kilo / qwen / opencode / hermes / openclaw / pi` = 11 agents explicit mention）+ skill layer 透過 skills.sh CLI 跨 30+ agents 通用、(b) **`magnitudedev/magnitude`** README FAQ「**Which harnesses work with it? Pi, OpenCode, Hermes, OpenClaw, Codex, Claude Code, Oh My Pi, and Cline.**」= 8-harness explicit integration list 含 Hermes + Why Magnitude bullet「**Works with your agent: one click to connect Pi, OpenCode, Hermes, and more**」。**對主人 horo-agent / horo-webui 直接利多**：(i) **caveman 的 11-agent explicit mention list 是個人 maintainer-viral-hit 對 Hermes 的 first-class 認可**、(ii) **Magnitude 的 8-harness integration 給主人 horo-agent 一個 ready-to-integrate local LLM backend** = 主人 horo-agent 可直接 ship 「Hermes Agent + Magnitude local inference」 combo 不需自建 inference backend。**MEMORY-direct-match 6-of-9-tick count（08-16 + 08-17 + 08-19 + 08-20 + 09-21 + 09-22）+ 連續 6 tick 中有 Hermes 提及的比例是 6/9 = 67%** = **Hermes-as-host signal 結構性 consolidation 持續第 6 個 tick**。**今天兩個 pick 是不同 niche（token-cost proxy vs local LLM inference engine）獨立 fire Hermes-as-host signal = cross-domain signal** 比同類 niche fire 更有結構意義。

- **Release freshness 5/5 全在過去 16 天內 release、registry-lag check 3/5 PASS + 1/5 反例（version drift）+ 1/5 不適用（無 npm）= 「fresh release 100%」是本月第三次**：今天 5 個 pick 全部都在 9-04 ~ 9-17 範圍內 release：**`anthropics/skills` 無 release（`pushed_at` 9-10 = 12 天前最後 push、`updated_at` 9-22 = today metadata 更新）** + **`JuliusBrussee/caveman` v2.7.0 Sep 15（7 天前）+ bin-v1.1.7 Sep 15 + npm cli 1.3.4 Sep 15 之後** + **`blader/humanizer` v3.0.0 Sep 06（16 天前）** + **`magnitudedev/magnitude` @magnitudedev/cli@0.1.3 Sep 18（4 天前）** + **`Fission-AI/OpenSpec` v1.13.1 Sep 17（5 天前）** = **4/5 有 GH release、1/5 無 release = 20% zero-release floor (verify ×3)、其餘 4/5 ≤ 16 天 release、且 3/4 ≤ 7 天 = 75% within-7-day = 「100% fresh + 75% within-week」** = fresh-release rate 100% 連續第 3 個 tick（09-20 + 09-21 + 09-22）。**Registry-lag check**：(a) **`JuliusBrussee/caveman` PASS**（GH v2.7.0 Sep 15 跟 npm cli 1.3.4 對齊）+ (b) **`Fission-AI/OpenSpec` PASS**（GH v1.13.1 Sep 17 = npm latest 1.13.1 完全同步）+ (c) **`magnitudedev/magnitude` 反例**（GH @magnitudedev/cli@0.1.3 Sep 18 vs npm latest 0.0.15 = GH 領先 npm 3 版 + scope naming 錯位）、(d) **`blader/humanizer` 不適用**（無 npm package）、(e) **`anthropics/skills` 不適用**（無 npm + 無 release）= **3/5 PASS + 1/5 反例 + 2/5 不適用 = 75% PASS rate (3/4 applicable)**。**今天 Magnitude 的 registry-lag 反例比 09-21 yylo 反例更嚴重**：(i) yylo 是 GH release 落後 npm 21 天、(ii) **Magnitude 是 GH release 領先 npm 3 版 + scope 命名錯位（GH tag `@magnitudedev/cli@0.1.3` vs npm unscoped `0.0.15`）= dual-failure 反例**、**建議主人 horo-agent install Magnitude 用 desktop app（[magnitude.dev/download](https://magnitude.dev/download)）不用 npm CLI、避開 registry-lag 反例**。

- **Personal-maintainer 4/5 + vendor-official 1/5 分布 = 4-consecutive-tick personal-maintainer 80% 以上、今天 = 80%**：今天 5/5 picks 中 owner 分布：**`anthropics/skills` = Anthropic 官方 vendor（1/5）** + **`JuliusBrussee/caveman` = Julius Brussee 個人** + **`blader/humanizer` = blader 個人** + **`magnitudedev/magnitude` = magnitudedev 個人** + **`Fission-AI/OpenSpec` = Fission-AI 個人 + @0xTab 個人 maintainer** = **4/5 個人 + 1/5 vendor-official = 80% personal-maintainer**。**連續 milestone**：09-15 (5/5 personal + vendor mix)、09-16 (5/5 personal)、09-17 (5/5 personal mix)、09-18 (5/5 corporate mix)、09-19 (5/5 personal-vendor mix)、09-20 (4/5 personal + 1 corporate)、09-21 (3/5 personal + 2 vendor-Labs)、**09-22 (4/5 personal + 1 vendor-official) = 本週 7 tick 中 personal-maintainer 6 tick ≥ 60%**。**對主人 horo-agent 對位**：「**個人 maintainer 模式 + 1 viral-hit + 3 utility skill** = 主人在 horo-agent downstream 若 ship 個人 maintainer 模式產品、4 個 reference 模式可挑：(a) caveman = viral-joke-to-research-paper (b) humanizer = niche-detector-rewrite (c) magnitude = desktop-app-hard-fork (d) OpenSpec = spec-driven-workflow-as-product」**。**4 個 personal-maintainer 商業 model 對位**：(i) caveman = **MIT skill free + BSL-1.1 engine/proxy 防止 SaaS fork + HN #1 + Product Hunt #8 + Adobe Research cite** = viral-hit + freemium dual-license、(ii) humanizer = **pure skill MIT、no commercial extension** = 純 community signal、(iii) magnitude = **Apache-2.0 + desktop app + Discord community + Twitter** = 早期 startup（13 個月 4.8k★ 仍在 build-out）、(iv) OpenSpec = **MIT CLI + 30+ AI tool integrations + Stores beta for team + Discord** = mature solo-maintainer product（13 個月 70k★ 已 scale）。**對主人 horo-agent 對位建議**：「**主人若 ship horo-agent 為 viral-joke-to-product 路線、caveman 是最佳 reference；若 ship 為 mature solo-maintainer 路線、OpenSpec 是最佳 reference**」。

- **Token-cost / Context-engineering 是 09-22 五個 pick 共同主題 = 「**token economy is the new abstraction layer**」訊號**：今天 5 個 pick 各自有 token/context 對位：(a) **`anthropics/skills`** 是 **Anthropic 官方 context engineering skill framework**（skills = dynamic context load、不是 prompt fragment、是 Claude runtime context module）、(b) **`JuliusBrussee/caveman`** 是 **token-cost 削減 viral hit**（skill layer 壓縮 write、proxy 壓縮 read、middleware 在 user code 內壓縮、Adobe Research cite 1.4-2.4× cost reduction up to 3×、JetBrains lab test 86 tasks = "costs you nothing measurable in quality"）、(c) **`blader/humanizer`** 是 **AI writing detection 反向 + 25 patterns**（解決 LLM default to widest-audience choice 的 token-output 問題）、(d) **`magnitudedev/magnitude`** 是 **local LLM inference = zero token cost after download**（no API key、no rate limit、private offline、模型 download 後 token 成本歸零）、(e) **`Fission-AI/OpenSpec`** 是 **spec-driven development = context hygiene**（README 直接寫「Context hygiene: OpenSpec benefits from a clean context window. Clear your context before starting implementation and maintain good context hygiene throughout your session.」）。**共同 abstraction**：「**token 是新 oil、context 是新 memory、context engineering 是新 discipline**」—— (i) Anthropic 官方把 Skills 定位成「**dynamic context loader**」、(ii) caveman 把 token cost 當 viral hit topic、(iii) humanizer 把 AI writing pattern 當 context pollution、(iv) Magnitude 把 inference 從雲端 token API 拉回本地 = token 成本歸零、(v) OpenSpec 把 spec-first 當 context hygiene 防 drift。**對主人 horo-agent 對位極高**：「**主人若 ship HORO-Agent 通用 framework、context engineering / token economy / context hygiene 應該是 1st-class 抽象層、不只是「prompt 寫好」**」+ 「**主人若 ship HORO 通用 framework、可以借鑑 OpenSpec 的 spec-first workflow 把 context hygiene 做成 ship-time invariant（不是 prompt-time best-effort）**」+ 「**主人若 ship HORO-Agent local-first 模式、可直接用 Magnitude 作 inference backend、不需自建**」。
