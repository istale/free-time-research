# GitHub 專案動態
- 檢查時間：2026-08-07
- 檢查對象：TencentCloud/TencentDB-Agent-Memory、addyosmani/agent-skills、cloudflare/computer、CherryHQ/cherry-studio、modu-ai/moai-adk
- 來源：Trending Daily Top 3 + 自由探索 GitHub Search（雙查詢：created:>2026-07-22 高星 + topic:ai-agent 高星 active，並以 MEMORY pinned topics 做交叉篩選）

## Repo 摘要與 3W1H

### TencentCloud/TencentDB-Agent-Memory
- **Repo 摘要：** TencentDB Agent Memory 走「symbolic short-term + layered long-term」雙路徑：把 heavy tool log 抽成 Mermaid 符號（短期），把對話抽成 L0 conversation / L1 atom / L2 scenario / L3 persona 的 semantic pyramid（長期），刻意拒絕「平舖向量」+「暴力歷史堆疊」。README 開宗明義標示與 OpenClaw / Hermes Gateway 同一條生態線，並用 WideSearch 51.52% 相對 pass-rate gain / 61.38% token 節省、PersonaMem 48% → 76%、SWE-bench +9.93% 等具體 benchmark 結果。Repo 為 TypeScript，MIT（LICENSE 檔明載），16,715 stars（+1,400 / 24h），最後更新 2026-08-06。
- **3W1H：**
  - **What：** Memory hub 服務（Memory Core / Memory Hub / Proxy）+ 四類資產（Chat Memory / Skill / LLM-Wiki / CodeGraph）+ OpenClaw 與 Hermes Gateway 雙路徑 plugin。
  - **Why：** 傳統 agent 每次從零重學 SOP / 專案背景 / 工具約定；TencentDB 把記憶做成 hierarchical + heterogeneous storage（底層 DB 留證據、上層 Markdown 留結構），AI 在 context 內只看 L3 persona 與 Mermaid canvas，drill down 走 `node_id`。
  - **Who：** 想避免「換框架就重來」、要跨 OpenClaw / Hermes / Claude Code 共用記憶的 agent 團隊；以及評估 memory layering / 符號化 / token-saving 設計模式的架構師。
  - **How：** `git clone https://github.com/Tencent/TencentDB-Agent-Memory.git` → `cd TencentDB-Agent-Memory/deploy/global-images` → `cp .env.example .env` → `$EDITOR .env` → `./start-all.sh`（啟三服務，預設 `http://localhost:8125` 為 Memory Hub 面板）；v1.x/v0.x → v2.0.0 升級前跑 `MemoryCore/scripts/migrate-v2-to-v3`。
- **安裝方式：** **npm：** `@tencentdb-agent-memory/memory-tencentdb`（latest 1.0.1，2026-07-14 上傳，OpenClaw plugin）；**GitHub source deploy：** clone → 進 `deploy/global-images` → `.env` 填兩組 LLM 參數 → `./start-all.sh`；**Hermes Gateway 整合：** 設 `MEMORY_TENCENTDB_GATEWAY_CMD="sh -c 'cd ~/.memory-tencentdb/tdai-memory-openclaw-plugin && exec npx tsx src/gateway/server.ts'"` 或放 `~/.memory-tencentdb/memory-tdai/tdai-gateway.json`；**推薦 auto-discovery**：把 plugin 放在 `~/.memory-tencentdb/tdai-memory-openclaw-plugin/`，Gateway 第一次對話時自動 `Popen()` 啟動。完整流程見 `INSTALL.md` / `INSTALL_CN.md`。
- **Repo metadata：** Description `TencentDB Agent Memory is a team-level memory hub for AI Agents — turning conversations, docs, and code into four reusable memory assets (Chat Memory, Skill, LLM-Wiki, Code-Graph) that are governed, s`；Stars 16,715；Language TypeScript；Topics `agent`, `ai-agent`, `embedding`, `llm`, `local-first`, `long-term-memory`, `memory`, `openclaw-plugin`, `vector-search`；License NOASSERTION（API 字段）；LICENSE 檔實為 MIT（Copyright Tencent 2026）；Updated at 2026-08-06。
- **近期 release：** **2026-08-03**；`v2.0.0`；**v2.0.0**（v2.0.0 不是預發行；連續三日出現在 Trending Top 10 — 08-05、08-06、08-07 結構性 source property 見 ## 重點觀察）。

### addyosmani/agent-skills
- **Repo 摘要：** Addy Osmani（Google Chrome 團隊）做的「production-grade engineering skills for AI coding agents」：把 senior engineer 的 workflow（spec / plan / build / test / review / ship + webperf + code-simplify 八個 slash command）和 24 個可單獨裝載的 skill（code-review-and-quality、test-driven-development、interview-me 等）打包成一個 plugin，目標是讓 Claude Code / Cursor / Codex / Copilot / Cline / Antigravity 在不同 phase 都吃到對應 skill。`/build auto` 模式可單次批准 plan 後整批自主跑完，但每個 task 仍 test-driven + 個別 commit + 失敗 / 高風險步驟時暫停。Repo 為 JavaScript / Markdown，MIT，83,145 stars（+1,800 / 24h），最後更新 2026-08-06。
- **3W1H：**
  - **What：** 8 個 slash command（`/spec` `/plan` `/build` `/test` `/review` `/webperf` `/code-simplify` `/ship`）+ 24 個 skill + 自帶 skills CLI 的 multi-agent plugin（Claude Code / Cursor / Codex / Copilot / Cline / Antigravity 等 70+ agents 通用）。
  - **Why：** 個人 developer 把好習慣留在 chat history 裡就散掉；OS-level skill plugin 把 spec-before-code、tests-are-proof、measure-before-optimize、clarity-over-cleverness 變成跨 session 跨工具的一致契約，並把「自動跑 vs 暫停」的決策權交還 owner。
  - **Who：** 想在 Claude Code / Cursor / Codex 強制 spec-driven + TDD + code-review 流程而不自己寫 hook 的工程師；做 code review / web performance / API design / interview / simplify 任一工作流被 AI 亂寫搞過、想用「skill-as-code」強約束的團隊 lead。
  - **How：** Fastest：`npx skills add addyosmani/agent-skills`（用 vercel-labs/skills CLI 一次裝全部 24 skill）；或逐個：`npx skills add addyosmani/agent-skills --skill <name>`；或 native Claude Code：`/plugin marketplace add addyosmani/agent-skills` → `/plugin install agent-skills@addy-agent-skills`；或 local dev：`git clone https://github.com/addyosmani/agent-skills.git` → `claude --plugin-dir /path/to/agent-skills`。
- **安裝方式：** **npm/pnpm/yarn/npx：** `npx skills add addyosmani/agent-skills`（一次裝全部 24 skill）或 `npx skills add addyosmani/agent-skills --skill <name>`（單 skill）；**Claude Code plugin：** `/plugin marketplace add addyosmani/agent-skills` + `/plugin install agent-skills@addy-agent-skills`（SSH 失敗時先 `git config --global url."https://github.com/".insteadOf git@github.com:`）；**Antigravity CLI：** `agy plugin install https://github.com/addyosmani/agent-skills.git`；**Local dev：** `git clone https://github.com/addyosmani/agent-skills.git` + `claude --plugin-dir /path/to/agent-skills`。注意：單 skill install 不含 repo-level `references/` 共享 checklist，需整包裝或手動補 `references/`（#361 tracked）。
- **Repo metadata：** Description `Production-grade engineering skills for AI coding agents. Skills encode the workflows, quality gates, and best practices that senior engineers use when building software. These ones are packaged so AI agents follow them consistently across every phase of development.`；Stars 83,145；Language JavaScript；Topics `agent-skills`, `antigravity`, `claude-code`, `codex`, `cursor`, `skills`；License MIT（LICENSE 檔明確，Copyright 2025 Addy Osmani）；Updated at 2026-08-06。
- **近期 release：** **2026-08-04**；`0.6.6`；**Agent Skills 0.6.6**（0.6.6 不是預發行；主要是 fix release：恢復 Claude Code plugin 上 4 個 persona（#449）+ 收尾 ecosystem-neutral pass + 性能優化 workflow）。

### cloudflare/computer
- **Repo 摘要：** Cloudflare Computer 把「Agent 的執行環境」做成 SQLite-backed �擬檔案系統 + pluggable runtime 服務，state 永遠在 Durable Object 內、執行面可換 container（computerd + FUSE mount）、just-bash、Workers 內的 ECMAScript module 三種 backend，稱為「給 agent 一台電腦」。Preview 階段，README 標示 NOT production ready，但 sandbox-sdk `npm install` 對比與 `fs-bench` 已揭露 FUSE 在 metadata-heavy 工作勝過實體磁碟；對想在 Cloudflare 上架 long-running agent sandbox、又不想自己接 LiteFS / DO sqlite 同步的 builder，這是第一個官方「可上車」介面。Repo 為 TypeScript，MIT，4,991 stars（+1,380 / 24h），最後更新 2026-08-06。
- **3W1H：**
  - **What：** Cloudflare Workers SDK 內的 SQLite 虛擬檔案系統 + 三種 pluggable execution backend（Container with FUSE / just-bash / Workers JavaScript）。
  - **Why：** 過去在 Workers 上跑 agent sandbox 要自接 LiteFS、自管 DO state、自接 bash；這個包把 state-of-truth 收進 DO、把執行面做成可換 backend 的 pluggable surface，並把 `fs` 與 `runtime.exec(source, { backend })` 收成單一 entrypoint。
  - **Who：** 想在 Cloudflare 架 long-running agent sandbox、要把 bash / node 互操作放在同一個 state 上的 builder；以及評估 E2B / Modal / Fly machines 替代品的 Workers-native 架構師。
  - **How：** `npm install @cloudflare/computer` 後照 `packages/computer/README.md` 建 Workspace、掛想要的 backend；example 提供 `examples/think`（chat agent 用 workspace 當 working dir）、`examples/think-compare-runtimes`（同任務並列跑 container 與 worker 兩種 runtime）等。
- **安裝方式：** **npm/pnpm/yarn/npx：** `npm install @cloudflare/computer`（npm 上 latest = 0.1.1，2026-08-03 上傳；與 `v0.1.1` git tag 同步釋出）。**Docker：** 預建 `computerd` 透過 `packages/computer-computerd-linux-x64` 私有 image 釋出，並非 npm 套件。完整 reproduce 流程見 `COLLABORATORS.md`（contributor-only），一般使用者直接 install npm 套件 + 走 `examples/*`。
- **Repo metadata：** Description `Give your agent a computer 👾`；Stars 4,991；Language TypeScript；Topics **空**（repo owner 未設 GitHub topic tags）；License MIT（LICENSE 檔明確，Copyright 2026 Cloudflare, Inc.）；Updated at 2026-08-06。
- **近期 release：** **未找到 GitHub release**（`/releases?per_page=5` 回空 list；最新 tag `v0.1.1` commit 為 2026-08-03 `release: stop bundling computerd binary in npm tarball; default publish to unreleased tag`，「Zero-GH-release + npm 是事實上的 release 通道」模式；`@cloudflare/computer` 0.1.1 對應同個 commit）。

### CherryHQ/cherry-studio
- **Repo 摘要：** Cherry Studio 是 multi-LLM-provider 桌面 client（Windows / macOS / Linux + HarmonyOS / Android / iOS roadmap）：支援 OpenAI / Gemini / Anthropic / Claude / Perplexity / Poe / Ollama / LM Studio 等多家 LLM，300+ 預設 assistant、自訂 assistant、multi-model 同時對話、文件 / 圖片 / Office / PDF 處理、WebDAV、Mermaid、code syntax highlighting、全域搜尋、topic 管理、AI 翻譯、MCP server、Light/Dark 主題。v2.0.1 同時推出 Enterprise Edition（私有部署、unified model mgmt、企業級 KB、細粒度 ACL、admin backend），社群版採 AGPL-3.0 授權。Repo 為 TypeScript / Electron，**AGPL-3.0**（LICENSE 檔明確，OSI-approved 但 copyleft；商用 production 部署 / fork / 二次發佈前需確認 AGPL 條款，或洽 bd@cherry-ai.com 取得商業授權），49,943 stars（+1,200 / 24h），最後更新 2026-08-07。
- **3W1H：**
  - **What：** 跨平台（Win/macOS/Linux + HarmonyOS / Android / iOS roadmap）multi-LLM desktop client；社群版（AGPL-3.0）+ Enterprise Edition（私有部署 + admin backend + 中央 KB）。
  - **Why：** 多 LLM provider 在 client 端分散切換成本高、各家 desktop app UI 不一致；Cherry Studio 把 OpenAI / Anthropic / Gemini / Claude / Ollama / LM Studio 收成同一個 client，配 MCP server、自訂 assistant、topic 管理、WebDAV、企業級 KB，走「社群版開源 + 企業版買斷 / 訂閱」雙軌制。
  - **Who：** 想在桌面用 multi-model 對話 + 統一 assistant + 文件處理 + MCP 生態的個人 / 小團隊；以及評估私有 LLM + 集中管理 + 合規部署的企業 IT。
  - **How：** 一般使用者直接從 `https://github.com/CherryHQ/cherry-studio/releases/latest` 抓對應平台 installer（macOS Apple Silicon `Cherry-Studio-2.0.1-arm64.dmg`、macOS Intel `Cherry-Studio-2.0.1-x64.dmg`、Windows x64 `Cherry-Studio-2.0.1-x64-setup.exe`、Linux `Cherry-Studio-2.0.1-amd64.deb` / `Cherry-Studio-2.0.1-x86_64.AppImage` / `Cherry-Studio-2.0.1-aarch64.rpm`）；enterprise 用 bd@cherry-ai.com 取得 demo；contributor 走標準 fork → branch → PR 流程（CONTRIBUTING.md）。
- **安裝方式：** **未列（無 npm/pip 套件；純 desktop client）：** 從 `https://github.com/CherryHQ/cherry-studio/releases/latest` 下載對應 OS installer（macOS Apple Silicon 抓 `Cherry-Studio-2.0.1-arm64.dmg` 336MB；macOS Intel 抓 `Cherry-Studio-2.0.1-x64.dmg` 366MB；Windows x64 抓 `Cherry-Studio-2.0.1-x64-setup.exe` 286MB；Linux Debian/Ubuntu 抓 `Cherry-Studio-2.0.1-amd64.deb` 288MB；Linux AppImage `Cherry-Studio-2.0.1-x86_64.AppImage` 378MB；Fedora/RHEL 抓 `Cherry-Studio-2.0.1-aarch64.rpm`）。**License 注意：** AGPL-3.0 為 OSI-approved copyleft — 商用部署 / 修改後對外提供 service 時需開源衍生作品，或洽 bd@cherry-ai.com 取得商業授權（README 與 LICENSE 末尾明載「Should you require a commercial license that provides an exemption from the AGPL-3.0 requirements, please contact us at bd@cherry-ai.com」）。
- **Repo metadata：** Description `Cherry Studio is a desktop client that supports multiple LLM providers, available on Windows, Mac and Linux.`；Stars 49,943；Language TypeScript；Topics `agent-skills`, `ai-agent`, `awesome-skills`, `claude-code`, `codex`, `deepseek`, `hermes-agent`, `openclaw`；License **AGPL-3.0**（LICENSE 檔明確，Copyright Free Software Foundation 2007；OSI-approved copyleft）；Updated at 2026-08-07。
- **近期 release：** **2026-08-07**；`v2.0.1`；**v2.0.1**（v2.0.1 不是預發行；同日 02:45:33Z 釋出 15 個 platform installer，包含 macOS arm64/x64 dmg + Windows arm64/x64 setup/portable exe + Linux amd64/arm64 deb + x86_64/aarch64 AppImage + aarch64 rpm）。

### modu-ai/moai-adk
- **Repo 摘要：** MoAI-ADK 是一個 Go 寫的 single-binary Claude Code agentic harness，沿三條 axis 包 Claude Code：**Cost（Tokenomics）**、**Self-improvement（Agentic loop engineering）**、**Quality control（Agentic harness，SPEC lifecycle + TRUST 5 gates）**。核心主張：「model 是 stochastic worker，harness 必須從外面 enforce budget、quality bar、continuity」 — 開宗明義對應「the harness gets better as it runs, turning observation into rules」。README 引述「Token prices fell 98% over three years, yet enterprise AI spend rose 320%」與 Uber 5,000 工程師 4 個月燒光一整年 coding budget 等真實產業數據，把 cost / quality / learning 三軸互鎖成不再「runaway quota」。Repo 為 Go，Apache-2.0，1,161 stars（+120 / 24h），最後更新 2026-08-07。
- **3W1H：**
  - **What：** Go 寫的 single-binary Claude Code harness — `plan` / `run` / `sync` 為 SPEC pipeline backbone、`fix` / `loop` / `gate` / `review` / `clean` / `codemaps` / `e2e` / `feedback` / `goal` 為 utility、TRUST 5 quality gates（Tested 85%+ / Readable / Unified / Secured / Trackable）+ 12-Agent Catalog（11 個 MoAI custom + built-in Explore），分開 planning 與 auditing。
  - **Why：** Claude Code 預設在「成本天花板、測試閘門、session continuity、learning loop」全留給 user — 開了三個 turn 後預算爆、測試失敗默默繼續、session 結束就失憶；MoAI-ADK 把這四件事收成 harness-level contract，並用 SPEC lifecycle（plan → run → sync）讓每個 task 都有可驗收的 acceptance criteria。
  - **Who：** 已經在用 Claude Code、想加上「cost ceiling + TRUST 5 gate + cross-session learning」而不自己寫 hook 的工程團隊；以及評估「spec-driven + multi-agent + harness contract」架構的 builder（書 `Practical Agentic Coding with Claude Code` 同捆）。
  - **How：** 安裝：`curl -fsSL https://raw.githubusercontent.com/modu-ai/moai-adk/main/install.sh | bash`（macOS / Linux / WSL）；Windows 用 `irm https://raw.githubusercontent.com/modu-ai/moai-adk/main/install.ps1 | iex`；Go 1.26+ 可從 source build。啟動：第一次跑 `moai init` 走 init wizard（語言 / 使用者名稱 / advanced-bridge 設定）→ `moai plan`（SPEC-MODEL-PROFILE-MATRIX）→ `moai run` → `moai sync`；status / profile / doctor / update / web / goal / handoff / harness / worktree 等子命令做運維。
- **安裝方式：** **未列（無 PyPI/npm 套件；走 single-binary installer，Type-4「curl|sh + zero-deps 單檔 CLI」）：** **curl | sh（macOS / Linux / WSL）：** `curl -fsSL https://raw.githubusercontent.com/modu-ai/moai-adk/main/install.sh | bash`；**PowerShell（Windows 7.x+）：** `irm https://raw.githubusercontent.com/modu-ai/moai-adk/main/install.ps1 | iex`；**Source build（Go 1.26+）：** `git clone https://github.com/modu-ai/moai-adk` → `go build` → binary 放 `$PATH`。**無額外依賴**，macOS / Linux / Windows 三平台同 binary 行為。
- **Repo metadata：** Description `MoAI-ADK enables Claude Code to produce code — and then makes that code reliable at predictable cost, on a path that keeps improving. A harness wraps the model from the outside. The model is a stochastic worker moving token by token: it remembers neither budget, nor quality bar, nor where the last session broke off.`；Stars 1,161；Language Go；Topics `agentic-ai`, `agentic-coding`, `agentic-workflow`, `ai-agent`, `ai-coding`, `anthropic`, `claude`, `claude-code`；License Apache-2.0（LICENSE 檔明載 Copyright 2026 MoAI 作者）；Updated at 2026-08-07。
- **近期 release：** **2026-07-24**；`v3.0.1`；**v3.0.1**（v3.0.1 不是預發行；features 含 init wizard 加 language / user name / advanced-bridge steps + SPEC-MODEL-PROFILE-MATRIX-002 plan-phase artifacts；bugfix 含 moai agent lint errors LR-12 effort drift + LR-01/LR-03 harness）。注意：v3.0.2 已在 README badge 顯示（`Release-v3.0.2-blue.svg`），但 `/releases/latest` 仍回 v3.0.1，v3.0.2 為 git tag-only 釋出（08-01 tag-without-release 模式）。

## 重點觀察
- **release 新鮮度**：今日五個 repo 全部有當週 release / 顯著 push activity — TencentDB Agent Memory `v2.0.0`（8-3，跨三日 Trending Top 10 結構性重複）、agent-skills `0.6.6`（8-4）、cloudflare/computer 無 GH release（npm `0.1.1` / 8-3，Zero-GH-release 模式）、Cherry-studio `v2.0.1`（8-7 02:45:33Z，當日釋出 15 個 platform installer）、MoAI-ADK `v3.0.1`（7-24 但有 git tag-only `v3.0.2` 待 release 同步）。release cadence 整體偏熱，今日 Tier-A + Tier-B 5/5 全屬「最近一週有 release / push」軸。
- **Tier-A 跨日重複觀察（第二度驗證）**：Trending Top 3 中 `TencentCloud/TencentDB-Agent-Memory`（08-05 + 08-06 + 今日 連三日）、`cloudflare/computer`（08-06 + 今日 連二日）皆跨日重複 — 按 cron prompt「取前 3 個」字面 + 08-06 codified rule 仍將其納入並標示結構性 source property。`addyosmani/agent-skills` 是今日 Tier-A 新面孔；Tier-B 兩個 (`CherryHQ/cherry-studio`、`modu-ai/moai-adk`) 為首次進入本日清單，未與最近 4 天 Tier-A / Tier-B 重�。
- **MEMORY pinned topic 命中**（08-06 codified rule 首次�發）：五個 repo 中四個 API topics 直接命中 MEMORY 主人 pinned terms。`CherryHQ/cherry-studio` 同時命中 `claude-code` + `codex` + `hermes-agent` + `openclaw`（4 個直接命中，是本次 Tier-B 候選人中 MEMORY 命中密度最高者），但同時採 **AGPL-3.0**（copyleft）授權 — 主人若要 fork / 二次發佈需特別評估商用條款或洽商業授權，這與昨日 `EKKOLearnAI/hermes-studio` 的 BUSL-1.1（非 OSI）走不同 license 軸：`AGPL-3.0` 為 OSI-approved copyleft，`BUSL-1.1` 為 source-available 非 OSI。
- **MEMORY 「Stage gate ≠ 真驗證」對位**：今日 Tier-B `modu-ai/moai-adk` 的 **TRUST 5 Quality Gates**（Tested 85%+ / Readable / Unified / Secured / Trackable）是 MEMORY 主人 2026-08-01 spec-driven N-stage 框架裡「stage gate ≠ 真驗證」MEMORY rule 的直接外部對位 — 主人若要借「harness-level contract + verification gate」觀念，MoAI-ADK 的 5-axis taxonomy 是可參考的最小驗收 schema（每軸獨立可驗、不互相掩蓋）。
- **「Agent-化既有 app axis」持續**：今日 5/5 仍屬「agent-化既有介面」（08-04 codification 第四度驗證）— agent-skills 把 senior engineer workflow 變成「AI 跨 session 強制遵守的 contract」、cloudflare/computer 把 Cloudflare Workers 變成「agent 的電腦」、Cherry-studio 把 multi-LLM desktop client 變成「AI 助手市集 + 企業 KB」、MoAI-ADK 把 Claude Code 變成「harness-level 包覆」、TencentDB Agent Memory 把 agent memory 變成「跨框架可共享的 hub」。Layer taxonomy（今日更新）：spec/contract layer / compute substrate / desktop client / harness wrapper / memory hub。
- **上手門檻分層**：(1) `agent-skills` + `MoAI-ADK` + `cloudflare/computer` 三條一行指令（`npx skills add …` / `curl | bash` / `npm install @cloudflare/computer`）即可起；(2) Cherry-studio 走預編譯 DMG / setup.exe / deb / AppImage / rpm 對應 OS，但貢獻者要 fork + build；(3) TencentDB Agent Memory clone + `.env` + `./start-all.sh` 起三服務，部署成本仍是五個中最高。
- **語言 / 套件化生態**：TypeScript / Electron 仍是 agent desktop + dashboard 的事實標準（Cherry-studio、cloudflare/computer、TencentDB Agent Memory 3/5）；JavaScript / Markdown（agent-skills，純 plugin metadata + skill files）；Go（MoAI-ADK，single-binary 三平台無依賴）。**License 觀察**：今日五個中出現 AGPL-3.0（Cherry-studio，OSI-approved copyleft）、Apache-2.0（MoAI-ADK，permissive）、MIT（agent-skills、cloudflare/computer、TencentDB Agent Memory LICENSE 檔實質），加上昨日的 BUSL-1.1（hermes-studio，非 OSI）— 四種 license 軸在最近兩日內都被觀察到，主人 air-gap / 下游精簡專案要 fork 時需逐案確認條款。
- **對主人 air-gap / 下游精簡專案特別相關**：(1) `agent-skills` 的 8 個 slash command (`/spec` `/plan` `/build` `/test` `/review` `/webperf` `/code-simplify` `/ship`) 與主人 spec-driven N-stage workflow 一對一對位 — 主人若要把 N-stage 拆成 slash-command surface 讓 Claude Code 強制遵守，agent-skills 是可直接借鏡的 plugin 結構（特別是「`/build auto` 單次批准 plan 後自主跑、每 task 仍 test-driven + 個別 commit + 失敗時暫停」這個安全閥機制）；(2) `MoAI-ADK` 的 **12-Agent Catalog + 分開 planning 與 auditing** 是主人「不要 agent 自己評自己工作」的最小外部樣本，主人若要把 verifier 從 author role 拆出來獨立，MoAI-ADK 的 `Explore` built-in + `MoAI custom 11` 分離是乾淨的 reference；(3) `cloudflare/computer` 的 **pluggable runtime backend**（container + just-bash + workers js）是主人「同樣的 state-of-truth、不同的執行面」設計哲學的雲端對位 — 主人若要做 hermes-webui-lite 的「同樣 SQLite-backed session、不同前端 surface」結構，cloudflare/computer 的 DO-as-truth + pluggable runtime 模型值得參考。
