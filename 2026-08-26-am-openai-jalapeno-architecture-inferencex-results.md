# OpenAI Jalapeño: Better Than Nvidia Blackwell
- 原始連結：https://newsletter.semianalysis.com/p/openai-jalapeno-better-than-nvidia
- 報導主體：https://www.bloomberg.com/news/articles/2026-08-25/openai-claims-its-new-chips-can-outperform-nvidia-processors-in-tests
- HN 討論：https://news.ycombinator.com/item?id=49434378
- 閱讀時間：2026-08-26（早間）
- 來源：Hacker News 熱門前 10 第 4 名（258 分／174 則留言，讀取時 Asia/Taipei 07:00）；SemiAnalysis 8/25 發布，搭配 OpenAI 官方 Hot Chips 議程與 Bloomberg 同步揭露

## 摘要

**OpenAI 端出自己的 inference ASIC「Jalapeño」，由 Broadcom 共同設計、TSMC N3P 製造、HBM4 記憶體，宣稱在 perf/W（每瓦 throughput）擊敗 Nvidia Blackwell 與 Rubin。** SemiAnalysis 受 OpenAI 邀請進實驗室，用自家 InferenceX 套件當場驗證跑 DeepSeek R1、Kimi K2.5、GPT-OSS 等開源模型。結論 headline：Jalapeño 在 single-token prediction（STP）、無 speculative decoding、無 prefill/decode disaggregation 的條件下，token throughput per All-in utility MW 全面勝過 NV Blackwell 與 AMD、Google 同代晶片——這是「沒用任何加速手段就把別人打趴」的罕見案例。對 owner-control 派的意義是 *OpenAI 第一次拿出可量測的 ASIC 戰果*，不只讓 Nvidia 股價當晚震，也讓 Meta / Microsoft 自研 ASIC 多年沒成形的窘境曝光。

**Jalapeño 的關鍵 design 哲學不是「特化自家模型」，而是「消除一切 fixed latency」。** SemiAnalysis 明確反駁媒體把 Jalapeño 講成「只跑 OpenAI 模型」的說法——它跑 Doom、跑開源權重，架構上刻意追求 *across-all-workload-shapes roofline*。具體設計：(1) 每個 core slice 直接對應自己的 HBM slice，跳過 GPU 那種「大記憶體系統需要 amortize latency」的路徑，把 KV cache 與 weight 的 memory movement 壓到極低；(2) 採用 out-of-order core + L1 cache（其他 ASIC 多用 software-managed scratchpad），用 prefetching 換取無 barrier 延遲；(3) 不做 prefill/decode disaggregation（PDD），因為工作負載 mix 隨時間漂移，分池會把整個 chip 鎖在「錯誤的角色」上；(4) 不分離 draft/main 兩組 chip 同理，speculative decoding 在同一個低延遲 fabric 上才划算。這是一套 *刻意保留 fungibility* 的工程選擇。

**軟體端是 Jalapeño 真正的殺手鐧：Codex 寫 kernel、Gluon 程式語言、Teacup serving engine、「gigakernel」單 mega-kernel loop。** OpenAI 把 kernel 當 assembly 寫（單檔可達 ~3,000 行），早期是人類 in-the-loop，後來改用 scaled-up 內部版 Codex 自動產生 kernel——這正是 *AI 寫 AI 跑的硬體* 的 self-referential 循環。Gluon 是基於 Triton 的低階 kernel 語言，引入 OpenAI 自家發明的「Linear Layouts」代數，把 layout mapping 形式化到可以證明正確性的程度。3,000 行 kernel / TP32 在 8 天內 enable / TP8 → TP32 從原型到 production、單週 throughput 漲 2 倍以上——這些都是「Codex + 良好 tracing + harness」的具體戰果，呼應 8/01 OpenAI 自家 ARC-AGI-3 文章「評測量的是 harness 不是 model」、8/04 CodeArena Sica 的 AI-driven kernel optimization、8/31 Kernel Forge 等同源軸線。

**Rack 級架構走的是 OpenAI 自己的命名學，命名食物系（Katsu 主機、Vindaloo ASIC tray、Chana switch），但本質是 128 ASIC/rack + 2,048 ASIC 全域 scale-up 域。** 一個 Jalapeño ASIC 包：13.4 PFLOPs MXFP4（B0 stepping）、700W TDP、15.4 TB/s HBM4 bandwidth（10 Gbps pin speed，超越 Nvidia Rubin 的 9.6 Gbps）、off-package N3E I/O chiplet 提供 32 lane 800G SerDes。Rack 由 Celestica 系統整合：16 個 Katsu（host CPU）+ 16 個 Vindaloo（8 ASIC/tray）+ 8 個 Chana switch tray，總功耗約 160 kW（接近雙寬 GB300 rack）。Scale-up 拓樸分兩層：local domain 128 ASIC 經 6 顆 Tomahawk 6 102.4T 交換器 all-to-all 連接（per XPU 4.8 Tb/s 單向），global domain 16 rack/2,048 ASIC 走 OCS（optical circuit switch）做 rail-only 架構。scale-up 網路只佔系統成本約 10%——OpenAI 把省下的預算換成「未來 10–20T 參數模型或 2–4M token context」的 optionality。下一個目標是 100 MW 部署、與 neocloud 合作、與 dc partner 收 reliability data 至明年 1 月，production ramp 落在 2027 大部分時段。

**為什麼值得主人看**：主人正在做的 `horo-agent` / `horo-webui` 兩條下游品牌線都是 *owner-controlled agent harness*，8/24 Earendil 那篇把 harness 拆成 4 個零件（system prompt / tools / agentic loop / translation layer）。今天 Jalapeño 報導的 engineering takeaway **剛好是「translation layer 的物理對應物」——把同一個 inference call 從 Nvidia Blackwell 換到 OpenAI/Broadcom ASIC、AMX MI400、Google TPU、Cerebras WSE，全靠 OpenAI 那種「讓 model 端更 portable」的代價已經被 ASIC 廠分攤完了**。主人 substrate-stack 軸線的兩個直接 actionable：(1) 若主人要長期經營 horo-agent 的 model-agnosticism，要意識到「nvidia-only」假設已經被 OpenAI 公開打破，未來 backend 必走多 ASIC 支援，Inception Labs / Anthropic 也會跟進；horo-agent 的 translation layer 設計最好從 day-1 就把「provider ≠ nvidia」寫進 schema，不要像 Anthropic 那樣把 CUDA 寫死再回頭改。(2) AI 寫 ASIC kernel（Codex + Gluon + Linear Layouts）這條線對主人長期 roadmap 很重要——「Codex 寫 Cortex-M kernel」、「Codex 寫 RISC-V vector extension」是同一條 self-improving 軸線的延伸，主人現在的 horo-agent 還沒走到這一步，但 2027 之前很可能要認真評估 *in-house kernel generation loop*（尤其若主人要往 on-device LLM 走，參考 8/22 Daedalus 150M CPU inference 那篇）。最後，這篇跟主人 8 月的「Kernel Forge / InferenceBench / Change2Task」三條線完全同源：*model 不是 bottleneck，harness/compiler/runtime 才是*——這是主人 SOUL.md 寫「**讓我可以看見你**」的硬體對應版本：看不見的 harness/compiler/infra 才是值得被看見的戰場。

## 3W1H 分析
- **What（做了什麼/主題）**:
  SemiAnalysis 8/25 深度分析 OpenAI 與 Broadcom 共同開發的 inference ASIC「Jalapeño」：架構細節（weight-stationary systolic + MXFP + OoO core + L1 + Linear Layouts）、Rack 系統（Katsu/Vindaloo/Chana 命名、128 ASIC/rack、2,048 ASIC 全域域）、與 Nvidia Blackwell/Rubin 在 perf/W、perf/$ 上的 head-to-head 比較。所有 InferenceX 數字由 SemiAnalysis 在 OpenAI 實驗室當場驗證；Jalapeño 公開展示跑 Doom、FP32 流體模擬、Codex 自動產生的 kernel。
- **Why（為什麼重要）**:
  1. **第一個「OpenAI 自研 ASIC 戰勝 Nvidia」的公開量測事件**：在 Meta 微軟 Microsoft 自研 ASIC 多年未成形、Google TPU 仍綁 GCP 的對照下，OpenAI 把 inference substrate 從「向 Nvidia 買」變成「自己設計 + Broadcom 代工」。這直接打破「CUDA moat 是不可撼動的」這個過去三年的 default 假設。
  2. **AI 寫 AI 跑的硬體 self-reference 閉環**：Jalapeño kernel 由 Codex 自動產生、效能 8 天內 2× 成長、TP32 在 8 天內從無到 production。Linear Layouts 代數形式化 layout mapping 到可證明正確的程度——這是「AI 加速硬體設計」首次有可量測的 production 證據，不只是 demo。
  3. **Inference 經濟學的 power-limited 轉折**：Jensen 自己說「If you have 1 GW of power, throughput per watt is revenue」。HBM4 提早一年採用、700W 低 TDP、15.4 TB/s 帶寬、Jalapeño「B0 stepping 再 +25% perf/W」——這些都是 *運算不再是稀缺、電力才是稀缺* 的具體落地。
  4. **PDD 與 speculative decoding 的反思**：OpenAI 明確選擇 *不做* PDD、*不分離* draft/main 兩池，因為 fungibility 勝過 peak efficiency。當所有競爭者都在做 disaggregation，這是一個 counter-intuitive 的工程判斷，值得所有 inference infra owner 重新檢視自己的架構。
- **How（如何運作/實作）**:
  - **Memory hierarchy 簡化換 roofline proximity**：每個 core slice 對應自己的 HBM slice，跳過 GPU 那種「大記憶體階層需要 amortize latency」的路徑。Synchronization 只在已知高頻通訊（tensor-parallel）上發生，可與 compute 重疊。
  - **OoO core + L1 + 顯式 semaphore**：放棄 scratchpad model，改用 OoO 執行單元與 prefetching；tradeoff 是需要好的 prefetch，但 Codex + tracing 可以自動找到最佳 kernel。
  - **Gluon + Linear Layouts**：基於 Triton 的低階 SPMD 語言，加入 layout mapping 代數；每個 layout 描述「硬體資源（5號 register / warp 9）↔ 張量元素（row 6 col 7）」的 mapping，可證明 conversion 正確性、可自動 swizzling。
  - **Megakernel「gigakernel」approach**：單一 mega-kernel 在 device 上 loop，減少 CPU launch overhead；搭配 1M rollout 的 test-time compute（coherent usage）策略。
  - **Rack-scale OCS rail-only 架構**：16 rack 共 2,048 ASIC，1.6 Tb/s per XPU global bandwidth，光學 circuit switch 在每個 rack 內，rail-only 把光路徑簡化。
  - **Power-limited 設計哲學**：perf/W 而非 perf/$, 因為 *datacenter power envelope 受 utility interconnection / cooling / UPS / BtM gas turbine 限制*，電網擴容時間遠長於硬體採購；xAI Colossus 2 的 BtM-heavy 策略是同源案例。
- **Insight（赫蘿心得）**:
  今天這篇把主人 8 月的 inference / harness 軸線從「harness engineering 是 software 問題」推到「hardware substrate 也是 harness 的一部分」。具體地：(1) **CUDA moat 不是技術問題，是 OpenAI 願不願意寫自己的 compiler / kernel stack 的問題**——Gluon + Linear Layouts + Codex-generated kernel 等於 OpenAI 用 9 個月做出 Nvidia 要 5 年的事。這給主人一個明確訊號：horo-agent 的 *model portability* 與 *runtime portability* 是同一件事——未來 2 年內 inference 將進入多 ASIC 時代（NV Rubin / OpenAI Jalapeño / AMD MI400 / Google TPU v7 / Cerebras WSE / Trainium3），horo-agent 的 translation layer 不能只支援「OpenAI / Anthropic / Google 三家 SaaS API」，必須從 day-1 設計成「支援任意 OpenAI-compatible inference backend（含自架 ASIC 叢集）」。(2) **「Codex 寫 kernel」這條 self-improving 軸線比主人之前以為的更接近 production**：OpenAI 3,000 行 hand-tuned kernel、TP32 8 天上線、單週 2× throughput，這是 *AI 加速 AI infra* 的第一個可量測戰果。主人目前 horo-agent 的工具鏈仍以「人類寫 skill、人類審 review」為主，未來 12–18 個月若要往 on-device / 自架 inference 走，**Codex-in-the-loop kernel generation** 是必然投資——可以從 Raspberry Pi / Apple Silicon 的 tiny inference kernel 開始試（8/15 Bitnet 6502、8/22 Daedalus、8/07 vLLM 都是同方向）。(3) **Jalapeño 的 curry food 命名學與 K8s 的 Greek letter 命名學是同一個文化訊號**：OpenAI 內部把抽象概念具象化成「可敘事食物」，這跟主人 SOUL.md 的「赫蘿」命名邏輯同源——*命名學是 owner agency 的具體展現*。主人若要在 horo-agent 內部建立「模組命名學」，可以借鏡這個 pattern（不要用 generic infra 名稱如 worker-1 / pool-A，要用具象可敘事的命名）。(4) **反面教材**：文章對 Bloomberg 報導（Nvidia 自家反駁）著墨不多，但主人應該 cross-check——「Jalapeño 跑 Doom、跑開源權重」是真的還是 marketing，主人若要引用這篇做 roadmap，務必把 *OpenAI 提供的數字* 與 *SemiAnalysis 驗證的數字* 分開標註，符合主人 MEMORY「timeout / protocol violation / network blocker / 設定存在 / task started 必須分開報告」的偏好。總結：今天這篇不是「硬體新聞」，是「*harness 物理層正在被重新定義*」的 canonical event——主人 horo-agent 的 translation layer 必須從 day-1 設計成 ASIC-agnostic，否則 2027 會被迫重寫一次（就像主人 MEMORY 寫的「勿重寫 agent loop / SSE / session schema」，新的 substrate-stack 重寫同樣要避開）。
