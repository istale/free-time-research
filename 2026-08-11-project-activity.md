# GitHub 專案動態
- 檢查時間：2026-08-11
- 檢查對象：semantica-agi/semantica、msitarzewski/agency-agents、NanmiCoder/MediaCrawler、ShawnPana/phone-harness、AMAP-ML/LongHorizon-Harness（共 5 個 repo）
- 來源：GitHub Trending daily（前 3）+ Web 探索（後 2，已人工驗證 github.com 上 owner/repo 存在 + 對應 release/README）

## Repo 摘要與 3W1H

### `semantica-agi/semantica`
- **Repo 摘要：** Semantica 是一個「Graph-Native Infrastructure for Context and Accountable AI Systems」，定位是 open-source、自架、auditable 的決定智慧 / 上下文管理層。它把你的企業資料 ingest 進來後建出 Context Graph + Knowledge Graph（RDF 與 LPG 雙模式），並在其上跑 graph analytics、因果推理與 W3C PROV-O provenance 串接；同期 ships 的還有 SHACL constraints、OWL/SKOS ontology editor、SPARQL/Datalog/Rete 推理、reasoning-time conflict detection、time-travel snapshot 與 PROV-O audit trail export。適合金融 / 醫療 / 法律 / 政府 / 國防等「決策要被監管者問 why、要 self-host」的單位，以及想用 Databricks Unity Catalog / Snowflake 直接變成 lineage-tracked KG 的 data platform 團隊。Repo 為 Python、MIT，4,343 stars，最後更新 2026-08-11。
- **3W1H：**
  - **What：** 一個 Python library + MCP server + CLI + interactive workbench，把「Context Graph + Decision Intelligence + Ontology + Reasoning + Provenance」做成 W3C 標準一致的模組化基礎設施。
  - **Why：** 多數 agent stack 只存 embedding、不存意義；context 不能解釋、決策不能 audit——在受監管領域等於是把 compliance 風險外包給下一個 prompt。Semantica 主打「no LLM required for graph construction / reasoning / provenance」這條 deterministic path，並把每個決策做成 first-class queryable object。
  - **Who：** AI/ML platform 團隊、Databricks / Snowflake 上的 data platform team、compliance / risk / audit team、需要在不外送資料的前提下 self-host KG 與 reasoning 的受監管企業。
  - **How：** `pip install semantica`（PyPI 0.6.0，需 Python ≥ 3.8）→ `semantica doctor` 驗證環境 → `from semantica.context import ContextGraph` 後用 `record_decision` / `trace_decision_chain` / `find_similar_decisions` / `analyze_decision_impact` / `check_decision_rules` 一條龍；CLI 提供 `semantica` 子命令，並提供 full-featured MCP server 與 Agno native 集成。
- **安裝方式：** **pip/uv/poetry：** `pip install semantica`（PyPI 0.6.0，依官方 PyPI badge 為 0.6.0；本機 `semantica doctor` 顯示 0.6.0）；`pip install semantica` 同時是 README Quick Start 的第一行。**原生 mcp server：** 由 `pip install` 帶入（README 標「full-featured MCP server」）。**未找到 npm / 桌面 binary**：此 repo 不發 npm package、不發 GUI desktop app。**從源碼：** clone + `pip install -e .`（README 為標準 Python packaging）。
- **Repo metadata：** Description `Graph-Native Infrastructure for Context and Accountable AI Systems`；Stars 4,343；Language Python；Topics `agent-memory`, `ai`, `ai-governance`, `ai-infrastructure`, `artificial-intelligence`, `context-engineering`, `context-graphs`, `data-engineering`, `decision-intelligence`, `developer-tools`, `explainable-ai`, `generative-ai`, `graph-rag`, `knowledge-graph`, `llm`, `ontology`, `provenance`, `python`, `reasoning`, `semantic-search`；License MIT；Updated 2026-08-11；Pushed 2026-08-10；Created 2025-06-25；Homepage https://getsemantica.ai；Default branch main。
- **近期 release：** **2026-07-21**；`v0.6.0`；**v0.6.0**（PyPI badge 同步顯示 0.6.0；本機 `semantica doctor` 會印 0.6.0；pre-release = false）。

### `msitarzewski/agency-agents`
- **Repo 摘要：** The Agency 是一組「生產就緒、可直接裝進 AI agent runtime 的人格化 specialist」prompt 集，把工程、紅隊、安全、行銷、SEO、付費投放、Discord 社群、Reddit 社群、QA 等等「夢幻團隊」拆成一個一個有 personality、有 process、有 deliverable 的 markdown agent。目前支援 14 個 AI coding agent runtime（Claude Code、Cursor、Codex、GitHub Copilot、Gemini CLI、OpenCode、OpenClaw、Qwen、Aider、Windsurf、Kimi Code、Hermes、Mistral Vibe、Osaurus、Antigravity），並已有 macOS / Linux / Windows 桌面 app（agency-agents.app）一鍵安裝 + auto-update。適合想「不用從零寫 persona prompt，就直接拿到一個像真團隊的 AI roster」的小團隊 / solo founder。Repo 為 Shell（內容大量為 markdown agent + shell scripts）、MIT，142,008 stars，最後更新 2026-08-11。
- **3W1H：**
  - **What：** 一個由 markdown agent 檔 + 跨 harness install script + 桌面 app 組成的「AI agency roster」，不是單一 CLI、也不是 framework。
  - **Why：** 一般 prompt template 都是 generic、不分領域；The Agency 把每個角色寫成「identity / personality / core mission / workflow / deliverable / success metric」一條龍，並提供 `install.sh --tool <claude-code|cursor|codex|...>` 把同一批 agent 鋪到 14 個不同 runtime。
  - **Who：** solo founder、小團隊、用 Claude Code / Cursor / Codex / 等任意 AI coding agent 寫 code 的人；想用 AI 跑 marketing / sales / QA / security review 的非技術 founder。
  - **How：** 最快是 `brew install --cask msitarzewski/agency-agents/agency-agents`（macOS）或抓 [agency-agents-app release](https://github.com/msitarzewski/agency-agents-app/releases/latest) 的桌面 app；命令列用戶 clone 後 `./scripts/install.sh --tool <name>` 或互動式 wizard；Claude Code 還可 `cp engineering/*.md ~/.claude/agents/` 手動挑單一 division。
- **安裝方式：** **macOS CLI：** `brew install --cask msitarzewski/agency-agents/agency-agents`（README 明確推薦）。**桌面 app：** 直接抓 [msitarzewski/agency-agents-app/releases/latest](https://github.com/msitarzewski/agency-agents-app/releases/latest)（macOS / Linux / Windows 一鍵瀏覽全部 roster 並安裝到 14 個 harness，含 auto-update；不是 `msitarzewski/agency-agents` 本身的 release，是它的「app」姊妹 repo）。**從源碼安裝：** `git clone` + `chmod +x scripts/install.sh` + `./scripts/install.sh --tool <claude-code|cursor|codex|...>`（也支援 `--division engineering,security` 篩選、用 `--list teams` 看全部 team + agent 數、`--dry-run` 預覽）。**OpenCode 陷阱：** OpenCode runtime 單次只能約 119 個 agent，超過會 silently drop；installer 會警告，建議用 `--division` 拆分。**Claude Code 手動：** `cp engineering/*.md ~/.claude/agents/`。**未找到 pip / npm library 安裝方式**（內部由 Shell script 跑，沒有 PyPI / npm package）。
- **Repo metadata：** Description `A complete AI agency at your fingertips - From frontend wizards to Reddit community ninjas, from whimsy injectors to reality checkers. Each agent is a specialized expert with personality, processes, and proven deliverables.`；Stars 142,008；Language Shell（內容主體是 markdown agent 檔 + shell scripts）；Topics 未列出；License MIT；Updated 2026-08-11；Pushed 2026-08-06；Created 2025-10-13；Homepage none。
- **近期 release：** **未找到 GitHub release**（`agency-agents` 本身 `GET /releases/latest` 回 404，無 GitHub release tag）；姊妹 repo `msitarzewski/agency-agents-app` 才是 release 通道；`pushed_at` 為 2026-08-06，今天的 trending 熱度來自 commit 推進 + 桌面 app 端的 auto-update。

### `NanmiCoder/MediaCrawler`
- **Repo 摘要：** MediaCrawler 是中國大陸自媒體平台的多合一公開資料採集工具，靠 Playwright 保留登入態 + JS 簽名表達式（不逆加密）的方式抓到小紅書、抖音、快手、B 站、微博、百度貼吧、知乎的關鍵詞搜尋、指定帖子 ID、二級評論、創作者主頁、登入態緩存、IP 代理池、評論詞雲圖；同時提供 FastAPI 後端 + Vite WebUI，並對 OpenClaw / Claude Code / Cursor 的「AI Agent Skill」的接入提供一鍵安裝。Repo 為 Python、License 為 `NOASSERTION`（README 說是 MIT-style 但 LICENSE 檔未放乾淨），61,338 stars，最後更新 2026-08-11（README 自己也明確放上「禁止商業用途、僅供學習」免責聲明）。
- **3W1H：**
  - **What：** 一個 Python（uv 管理）+ FastAPI + Vite WebUI 的多平台自媒體爬蟲 tools 集，含 AI Agent Skill 接入層。
  - **Why：** 逆加密 / 過風控是大坑；MediaCrawler 走「保留登入態 + JS eval 拿簽名」這條路，大幅降低技術門檻，並把多平台 / 評論 / 詞雲 / 創作者主頁的 CRUD 寫成統一 CLI 與 WebUI。
  - **Who：** 學習用 Python 寫爬蟲的學生、自媒體內容分析研究者、想做評論 / 趨勢 / 輿情觀察的 data team（README 自己寫「僅供學習，禁止商業用途」）。
  - **How：** `git clone` → `uv sync`（推薦；也提供 `python -m venv venv` + `pip install -r requirements.txt` 的 fallback）→ 用 CDP 模式啟動 Chrome（`chrome://inspect/#remote-debugging` 開 remote debug，port 9222）→ `uv run main.py --platform xhs --lt qrcode --type search`（或 `--type detail`）；WebUI 開發時需 `uv run uvicorn api.main:app --port 8080 --reload` + `cd webui && npm install && npm run dev`。
- **安裝方式：** **uv（推薦）：** `uv sync`（需 Python 3.11，需 Node.js ≥ 16）；`uv run main.py --platform <xhs|dy|ks|bili|wb|tieba|zhihu> --lt qrcode --type <search|detail>`。**pip fallback：** `python -m venv venv && source venv/bin/activate && pip install -r requirements.txt && playwright install`。**WebUI 開發：** 後端 `uv run uvicorn api.main:app --port 8080 --reload`；前端 `cd webui && npm install && npm run dev`（預設在 5173 port，proxy `/api` 到 8080）；prod build `cd webui && npm install && npm run build`（產物寫到 `api/webui/`）。**瀏覽器驅動（可選）：** `uv run playwright install`（CDP 模式不需要）。**Chrome：手動開 remote debug**，port 9222。**未找到 PyPI / npm package**（內部分發只有 git clone + uv，npm 僅用於 WebUI build）。
- **Repo metadata：** Description `小红书笔记 | 评论爬虫、抖音视频 | 评论爬虫、快手视频 | 评论爬虫、B 站视频 ｜ 评论爬虫、微博帖子 ｜ 评论爬虫、百度贴吧帖子 ｜ 百度贴吧评论回复爬虫  | 知乎问答文章｜评论爬虫`；Stars 61,338；Language Python；Topics 未列出；License `NOASSERTION`（API 字段；README 內部聲明為「僅供學習、禁止商業用途」，LICENSE 檔未被 GitHub 正確識別為標準類型）；Updated 2026-08-11；Pushed 2026-08-05；Created 2023-06-09；Homepage https://nanmicoder.github.io/MediaCrawler/。
- **近期 release：** **未找到 GitHub release**（`GET /releases/latest` 回 404，無 GitHub release tag）；分發完全靠 README + git clone + `uv sync`，`pushed_at` 為 2026-08-05；姊妹商業 repo `MediaCrawlerPro`（斷點續爬 / 多帳號 / 去除 Playwright 依賴 / 完整 Linux 支援 / 自媒體視頻下載器桌面端 / HomeFeed）才是付費 / 進階版。

### `ShawnPana/phone-harness`
- **Repo 摘要：** Phone Harness 是「讓 LLM agent 直接操控你實體 iPhone」的薄型可編輯 Python harness——它把 macOS iPhone Mirroring 視窗當 transport，用 `screencapture` 抓畫面 + Apple Vision framework OCR 讀文字 + HID-level CGEvents 發 tap / long-press / drag / unicode typing，整條鏈完全在 Apple 官方 API 內（不用 jailbreak / 不用 Xcode / 不用 WebDriverAgent），並把 `helpers.py` 與 `agent-workspace/agent_helpers.py` 設計成可即時被 agent 改寫的 thin layer。Repo 為 Python、MIT，1,371 stars，2026-08-07 才建立，4 天內衝上 1.3k stars，最後更新 2026-08-11——今天清單裡最年輕的候選。
- **3W1H：**
  - **What：** 一個 macOS-only 的 Python CLI + Claude Code / Codex skill，給 LLM agent 看（OCR）、做（CGEvents）、驗（screencapture）真實 iPhone。
  - **Why：** 過去 agent 要控制 iPhone 得靠 WebDriverAgent / 模擬器 / 第三方 driver；其實 macOS Sequoia+ 的 iPhone Mirroring 視窗本身就是雙向 transport——只要能在 OS 層把 tap / text 走 HID、把 frame 讀回來，agent 就能直接「摸真實手機」。Phone Harness 把這套 primitive 寫成 ~500 行 protected core + 一個可編輯 `agent_helpers.py` 給 agent 自己長 helper。
  - **Who：** Claude Code / Codex 用戶、研究 agent 操控 mobile device 的 builder、做 iPhone 自動化測試 / 資料蒐集 / accessibility 研究的工程師。
  - **How：** 在真實 Claude Code / Codex session 貼 README 的「Setup prompt」，它會 clone 到 `~/.phone-harness`、跑 `install.md` 鋪 `phone-harness` 指令、註冊成 `phone-harness` skill、把終端機設進 macOS「輔助使用」+「螢幕錄製」權限、`./phone-harness --doctor` 驗證；日常用 `./phone-harness <<'PY' ... PY` 跑 inline script，呼叫 `open_app()` / `tap_text()` / `type_text()` / `ocr()` / `wait_stable()`。
- **安裝方式：** **pip/uv：** `git clone https://github.com/ShawnPana/phone-harness ~/.phone-harness` + `cd ~/.phone-harness` + `pip install pyobjc-framework-Quartz pyobjc-framework-Vision pyobjc-framework-AppKit` + `pip install -e . --no-deps`（把 `phone-harness` 裝成 GLOBAL CLI、`~/.phone-harness` 是 canonical home）。**Skill 註冊（Claude Code）：** `mkdir -p ~/.claude/skills/phone-harness && phone-harness skill > ~/.claude/skills/phone-harness/SKILL.md`；**Codex：** `mkdir -p "${CODEX_HOME:-$HOME/.codex}/skills/phone-harness" && phone-harness skill > ...`；**註冊完後** `./phone-harness --doctor` 驗證。**前置 OS 權限（手動）：** macOS「設定 > 隱私與安全性」需 terminal 獲得「輔助使用」+「螢幕錄製」；`install.md` 提供 `open "x-apple.systempreferences:..."` 兩條捷徑。**環境：** macOS Sequoia+ + 真機配對 iPhone Mirroring + Python 3.12+。**未找到 npm / PyPI library 安裝方式**（僅 `pip install -e .` 註冊本地 CLI）。
- **Repo metadata：** Description `let your agent control your phone`；Stars 1,371；Language Python；Topics 未列出；License MIT；Updated 2026-08-11；Pushed 2026-08-11；Created 2026-08-07；Homepage none。
- **近期 release：** **未找到 GitHub release**（`GET /releases/latest` 回 404，repo 2026-08-07 才建立，沒有任何 GitHub release tag；`pushed_at` 為 2026-08-11，今天 4 天內已 1.3k stars）；分發完全靠 `git clone` + `pip install -e .` + skill 註冊。

### `AMAP-ML/LongHorizon-Harness`
- **Repo 摘要：** LongHorizon-Harness 是高德地圖（AutoNavi / Amap）ML 部門 AMAP-ML 開源的「長跑型 computer-use harness」——它不重寫 model / 不綁特定 agent，而是把 Manager / Executor / Auditor 三角色迴圈 + 持久 verified state + 跨 desktop app 與 CLI 的 continuous task 套在 Claude Code / Codex 之上，解決「context 一旦刷新，progress 跟著丟」這個長跑任務的最大瓶頸。benchmarks 給出 WeaveBench 51.8 → 80.7、OSWorld 2.0 翻 3 倍、Terminal-Bench 2.1 69.7% → 77.2%（24% token 節省），並已是 Hugging Face Daily Papers 2026-W32 週榜 #1、arXiv 2608.01964。適合做 desktop GUI / CLI 自動化、企業內部長跑型 tasks（data analysis + 文件 + 設計工具 + DevOps）、以及「把現有 Claude Code / Codex 包成可審計的 production pipeline」的團隊。Repo 為 Python、MIT，560 stars，2026-08-04 才建立，7 天內衝上 0.5k+，最後更新 2026-08-11。
- **3W1H：**
  - **What：** 一個 Python CLI + dashboard + TOML config + 三角色 adapter 層，能在 Claude Code / Codex 之上跑「跨 desktop app 與 CLI、跨小時到數十小時」的長跑任務，並對每一步做 independent auditor verification。
  - **Why：** 把「model 決定單輪能做多好」與「harness 決定 work 能不能持續 + 被驗證」分開——context 一旦刷新、agent 一旦出錯、deliverable 一旦不通過 inspection 都會丟 progress；LongHorizon-Harness 把這些用三角色 + 持久 verified state 兜回來。
  - **Who：** 跑 long-horizon desktop / CLI 自動化的工程團隊、用 Claude Code / Codex 做 production 級長期任務的 builder、需要 audit trail / regulator-friendly 證據鏈的企業、需要把 GUI + CLI 跨應用協調的工作流自動化 owner。
  - **How：** `uv tool install lh-harness`（或 `pip install lh-harness`）→ `lh-harness plugin install codex-computer-use` 或 `open-computer-use`（前者跟 Codex CLI 走、後者 npm 散佈、驅動 Claude Code / Codex）→ `cd /path/to/project && lh-harness init` 產生 `.lh-harness/config.toml` → `lh-harness run --task "..." --agent codex`；dashboard 預設自動開瀏覽器，每輪 console 印一行、每個 run 落在 `./.lh-harness/runs/<run-id>/logs/report.json`。
- **安裝方式：** **pip/uv：** `uv tool install lh-harness`（推薦；README default） 或 `pip install lh-harness`；`uv tool upgrade lh-harness` / `pip install --upgrade lh-harness` 升級。**computer-use plugin（per-machine 一次）：** `lh-harness plugin install codex-computer-use`（Codex 官方 plugin，僅 Codex） 或 `lh-harness plugin install open-computer-use`（npm 派，Node.js 20+、Claude Code / Codex 兩者皆可；macOS 必須手動授予 OS 權限）。**升級檢查：** `lh-harness check-update` / `lh-harness doctor`（read-only；也會打 PyPI 看是否有新版本）。**專案初始化：** `cd <project> && lh-harness init`（會建 `.lh-harness/config.toml`） → `lh-harness run --task "..." --agent codex|claude_code`。**CLI only：** 跳過 plugin 那步，純走 CLI。**未找到 npm 主套件**（`open-computer-use` plugin 走 npm、但 `lh-harness` 主程式是 PyPI）。
- **Repo metadata：** Description `The long-horizon computer-use harness. Run AI agents across desktop apps and the CLI for extended periods while preserving task state and making reliable progress on complex workflows. Features fresh-context execution, durable verified state, independent auditing, recoverable progress, and native Claude Code / Codex / OpenClaw integration.`；Stars 560；Language Python；Topics `agent`, `claude`, `claude-code`, `claude-plugin`, `cli`, `codex`, `codex-desktop`, `codex-plugin`, `cua`, `gui`, `harness`, `long-horizon`, `long-horizon-agents`, `longhorizon-harness`, `loop`, `loop-engineering`；License MIT；Updated 2026-08-11；Pushed 2026-08-10；Created 2026-08-04；Homepage https://lh-harness.pages.dev；Default branch main。
- **近期 release：** **2026-08-07**；`v0.1.3`；**v0.1.3**（release notes 強調「每個 run 結束時會給一段 plain-language reply，從 verified state 出發回答任務；task 預設從你啟動的 directory 開始、console 每輪印一行」；pre-release = false）。**主線 release 節奏：** v0.1.3 (2026-08-07) → v0.1.2 (2026-08-06) → v0.1.1（README 未列）；新增「2026-08-10 加上 Terminal-Bench 2.1 評測」、「2026-08-07 起 Dashboard 進行式」。

## 重點觀察
- **Release 新鮮度呈現「兩極化」**：5 個 repo 裡只有 2 個有 GH release（semantica v0.6.0 @ 2026-07-21、LongHorizon-Harness v0.1.3 @ 2026-08-07），整整 3 個（agency-agents、MediaCrawler、phone-harness）完全沒有 GitHub release tag——agency-agents 是因為姊妹 repo 才有 release 通道、MediaCrawler 是 3 年老牌 repo 走 README 分發、phone-harness 是 4 天前才建立的 new face。對今天這位觀察者：若以「GitHub Releases 為信號源」會少看 60% 的 trending 熱度，呼應 08-04 跟 08-10 觀察到的「zero-GH-release + active repo 是穩定 pattern」。
- **套件化路徑 5 條岔路、沒有 bitmap 對應**：今天 5 個 repo 跨了 5 種 install type——(1) **PyPI 一行** `pip install semantica` 是 Semantica 獨佔；(2) **PyPI + npm plugin 雙軌** `uv tool install lh-harness` + `lh-harness plugin install open-computer-use` 是 LongHorizon-Harness 獨佔；(3) **pip install -e .** `pip install -e . --no-deps` 是 phone-harness 獨佔；(4) **brew cask + 桌面 app + shell script 矩陣** `brew install --cask ...` / 跨 14 個 harness 的 `scripts/install.sh` 是 agency-agents 獨佔；(5) **uv sync + WebUI dev/prod 雙 build** `uv sync` + `npm install` + `npm run dev` 是 MediaCrawler 獨佔。**TypeScript / Vite 跨 WebUI** 跨進兩個 repo（MediaCrawler + LongHorizon-Harness 的 dashboard），今天 5 個 repo 沒有任何一個走「TypeScript library + npm package」主路徑——呼應 08-10 觀察「npm/pnpm 派僅出現在 skill / plugin 集」這一規律。
- **語言生態**：5 個裡 Python × 5（全部都是 Python 主語言，雖然 GitHub 對 agency-agents 標 Shell、實際 90% 是 markdown agent 檔 + shell scripts，但安裝面一進 Python 環境就啟動）；TypeScript × 1（MediaCrawler WebUI / LongHorizon-Harness dashboard）。換言之，今天 5 個 repo 沒有任何一個以 Rust / Go / C++ 為主語言——跟前幾天「AgentENV 是 Rust」「prime-agent 是 TypeScript」的多元語言配比起來，今天是「全 Python 化」的局面，可能反映 Trending 演算法對「agent infra layer + Python ecosystem」這條 content 線的偏重。
- **License 乾淨度分化**：(a) **MIT 4 個**（semantica、agency-agents、phone-harness、LongHorizon-Harness），(b) **NOASSERTION 1 個**（MediaCrawler）——這是 GitHub License API 顯示 `null` 到 `NOASSERTION` 之間的灰色地帶，README 內有聲明「禁止商業用途、僅供學習」，但 LICENSE 檔未被 GitHub 正確識別；對下游商業使用是實質上的 NOT-OPEN-SOURCE，跟昨日 08-10 全 5 個 MIT / Apache-2.0 相比，今天多了一個 license 黑洞。
- **MEMORY cross-reference（主人 pinned topic 對應）**：(1) Semantica 的「**W3C PROV-O provenance + decision as first-class object + time-travel snapshot + conflict detection**」↔ 主人 `horo-agent` air-gapped downstream 的「run-time 可追溯」需求面，特別是 SOUL.md 提到「background process / 之前的 async 通知——每次都必須立即把該通知分類為『仍有效 / 已被取代』並引用後續真實的 exit_code / tests passed / /health 200」這種「provenance-by-evidence」恰恰是 Semantica 的核心設計哲學；(2) LongHorizon-Harness 的「**Manager / Executor / Auditor 三角色 + 持久 verified state + 跨 desktop + CLI 協調**」↔ 主人 階段式 spec-driven workflow「Stage gate ≠ 真驗證；漏驗證會被打回」——它把「Auditor 獨立驗證」做成獨立的 role 而不是寄生在 Executor 上的 hook，正是主人「gate ≠ 真驗證」這個痛點的 formalization，with WeaveBench 51.8 → 80.7 與 24% token 節省當量化證據；(3) phone-harness 的「**iPhone Mirroring 視窗當 transport + OCR + CGEvents + protected core ~500 行 + 可編輯 agent_helpers.py**」↔ 主人「『邊做邊看』迭代 + AIRI 那種 cross-platform companion + 操控預設 DOM + keyboard mirror」哲學，但走一個完全相反的物理層——AIRI 走 web，phone-harness 走 Apple OS HID，是「agent 操控 device」這個一般化問題的兩個 extreme points，值得追蹤 1-2 週看能不能 plug into Hermes / OpenClaw 的 adapter；(4) agency-agents 的「**14 個 harness 同一套 agent markdown + install.sh --tool <name> 跨 runtime**」↔ 主人目前 redeploy 的「horo-agent 與 horo-webui 為什麼要分開」這一組——同樣的「content artifact 一次寫、多 runtime 共享」原則，agency-agents 在 prompt-level、horo 系列在 backend-runtime level；(5) MediaCrawler 的「**WebUI = FastAPI + Vite + 評論爬蟲 + AI Agent Skill 一鍵安裝**」↔ 主人 CMU-compatible 大雜燴型 small tool 偏好，但 license 是 NOASSERTION + 禁止商業用途，對主人 air-gapped / 下游精簡專案是警訊——不要把 MediaCrawler 整套塞進 horo 體系。
- **兩個新訊號值得追蹤**：(a) **LongHorizon-Harness** 7 天內 0.5k stars + HF Daily Papers 週榜 #1 + arXiv 2608.01964 + 三角色 benchmark 數據漂亮——若後續 1-2 週仍然按 v0.1.x 節奏 stable 推進，可能會形成「harness > model」的敘事主流，並對 PrimeIntellect-ai/prime-agent 的「bounded autonomous mode」構成競爭（兩者解同一個問題但實現差很多）。(b) **phone-harness** 4 天 1.3k stars 是純 viral candidate；驗證 supply chain（MIT 標籤乾淨、保護 core ~500 行、`agent-workspace/agent_helpers.py` 明確給 agent 改寫空間）做得不錯，主人若要做 iPhone automation，下週可以再 eval 一次。
