# GitHub 專案動態
- 檢查時間：2026-08-01
- 檢查對象：zhaoxuya520/reverse-skill、different-ai/openwork、mvanhorn/last30days-skill、yc-software/qm、VictorTaelin/OptMem
- 來源：Trending Daily Top 3（github.com/trending?since=daily）+ 自由探索 Web Search（GitHub Search API by created:2026-07-25..2026-08-01, stars>200, sort=stars desc）

## Repo 摘要與 3W1H

### zhaoxuya520/reverse-skill
- **Repo 摘要：** `reverse-skill` 是給 AI coding client（Claude Code / Codex / Cursor / Kiro / Cline）用的「網安/逆向/滲透/CTF/EDR bypass 場景路由器」skill pack：當模型遇到 APK、二進位、前端 JS 加密、CTF 題目或滲透目標時，依照 `RULES.md` → `MASTER-ROUTING` → `case-init` → 對應場景 skill → tools/MCP/scripts 的鏈條決定走 `jadx`/`apktool`/`Frida`/`IDA`/`BurpSuite` 哪一支，並自動檢查本地工具鏈、產出 timeline + Evidence→Finding→Path + report + field-journal。Skill 矩陣覆蓋 APK/iOS、二進位、.NET、前端 JS、OLLVM、PCAP、malware/YARA、pentest、攻擊鏈、CTF、firmware、patch-diff、pwn、EDR bypass、API/GraphQL、supply chain、LLM security 等 20+ 場景，並提供 Kali/Ubuntu/macOS 平台指引；README 明確指定 `README_AI.md` 給 AI agent 用 `MASTER-ROUTING.md` 當 fast ladder，整體是「讓 AI 別亂猜逆向命令」的工程化框架。Repo 為 PowerShell（混合 Python/Node/Bash/Java）、MIT、10,988 stars，最後 push 2026-07-31、今天仍在動；**v1.0.0 為唯一 GitHub release（2026-07-17），v2.0.0 為 git tag 但未開對應 release entry**。
- **3W1H：**
  - **What：** 給 AI coding agent 用的「Cybersecurity Skills Router」skill pack（PowerShell 主控 + 多語言工具鏈）。
  - **Why：** AI 接到逆向/滲透任務時常卡在「該用 jadx 還是 apktool？該跑 IDA 還是 radare2？」的選擇題；同一個錯誤在不同 session 重犯、工具 MCP 散在不同機器上、案例經驗沒沉澱。reverse-skill 把 20+ 場景編成 routing matrix + per-case scope gate，強制走「先初始化 scope、再 ACT」的鏈條，把經驗寫進 `field-journal` 並自動進化。
  - **Who：** 用 Claude Code / Cursor / Codex / Kiro / Cline 做逆向工程、滲透測試、CTF 訓練、惡意程式分析、firmware / IoT 安全、LLM/AI 安全、EDR bypass 研究的工程師與 researcher，以及需要在 Kali/Ubuntu/macOS 跨平台維持一致 methodology 的紅隊 / 藍隊團隊。
  - **How：** `git clone https://github.com/zhaoxuya520/reverse-skill.git` → 跑 `bash skills/scripts/refresh-tool-index.sh`（或 Windows 的 `.ps1`、Kali 的 `kali/scripts/refresh-tool-index.sh`）重建本地 tool index → 進 `skills/MASTER-ROUTING.md` 看 fast ladder → 對應場景打開 `skills/<scenario>/SKILL.md`；scope gate 在 `skills/ops/`，evidence chain 與 field-journal 都已結構化。
- **安裝方式：** **pip/uv/poetry：** 未找到 PyPI 套件（PowerShell 為主、非 Python library）。**npm/pnpm/yarn/npx：** 未列。**From source（必要路徑）：** `git clone https://github.com/zhaoxuya520/reverse-skill.git`。**前置：** Java/JDK（jadx + apktool）、Node.js 22.12+（JS toolchain + MCP servers）、Python 3.x（Frida + 輔助腳本）、一個 AI coding client（Claude Code / Codex CLI / Cursor 等）。**Refresh tool index：** `bash skills/scripts/refresh-tool-index.sh`（Linux/macOS）/ `powershell -File skills/scripts/refresh-tool-index.ps1`（Windows）/ `bash kali/scripts/refresh-tool-index.sh`（Kali）。**Platform docs：** Kali → `kali/README-kali.md`、Ubuntu/Debian → `docs/platforms/linux.md`、macOS → `docs/platforms/macos.md`。
- **Repo metadata：** Description `Reverse Engineering / Authorized Penetration Testing / Security Research Skill Router Pack AI-powered routing + On-demand toolchain bootstrapping + Self-evolving knowledge base  Supports Claude Code, Kiro, Cursor, Cline, and other AI coding clients 逆向/渗透/安全技能路由包 - AI 自动路由 + 按需自举工具链 + 自动进化经验库 | 支持 Claude Code / Kiro / Cursor / Cline 等代码 AI 客户端`；Stars 10,988；Forks 1,679；Language **PowerShell**（內含 Python/Node/Bash/Java 等多語言腳本，README icon row：py/nodejs/powershell/bash/java/docker/git）；Topics 空；License MIT；Created 2026-05-13；Updated 2026-08-01；Pushed 2026-07-31；Size 7.6 MB。
- **近期 release：** GitHub Release：**2026-07-17**；`v1.0.0`；**v1.0.0 — First formal release**。補充：另有 `v2.0.0` git tag 但未開 GitHub release entry（releases API 仍只回 v1.0.0）；版本節奏以 `CHANGELOG.md` + commit + tag 為主，README release badge 仍指向 v1.0.0。

### different-ai/openwork
- **Repo 摘要：** OpenWork 自稱「Claude Cowork / Codex 的開源替代」：跨 macOS / Windows / Linux 的桌面 app（Electron + Fastify + Lit + Vite + Postgres），可與 Codex / Claude Code / Cursor / OpenCode 等 agent 透過 OpenWork MCP（`https://api.openworklabs.com/mcp/agent`）互通 skills、MCPs、連線服務，並提供企業控制面 **OpenWork Den**（provision inference、邀請成員、發佈 skill marketplace、設定 desktop policy、import Anthropic-compatible plugins）。整個 stack 把「個人 AI workflow 與團隊共享」綁在同一個 identity + configuration 之下：同一組 skills / plugins / MCPs / Google Workspace / Microsoft 365 連線可在 Slack 頻道、桌面 app、agent client 之間共用，並透過 `search_capabilities` / `execute_capability` 兩個 MCP tool 暴露。Repo 為 TypeScript（pnpm workspace，內含 `@openwork/desktop` / `@openwork/app` / `@openwork-ee/den-api` / `@openwork-ee/inference` 等多 package），License `NOASSERTION`（未放 LICENSE 檔），19,646 stars，最後 push 2026-07-31；**stable release v0.18.12（2026-07-30），另有 macOS alpha prerelease 流水線**。
- **3W1H：**
  - **What：** 跨平台桌面 app + MCP server + 企業控制面的「AI workflow 共享」平台（Electron + Fastify + Lit + Postgres）。
  - **Why：** Claude Cowork / Codex 把「多 agent 共享同一組 skills、MCPs、雲端服務」鎖在雲端帳號內；OpenWork 把這件事做成可在自己機器上跑的開源 desktop app，並把組織層的 inference provisioning、policy、marketplace 全部拉出來變成 **Den** 控制面，搭配 OpenWork MCP 讓任何 agent client 都能複用同一個 workspace。
  - **Who：** 想把 AI workflow 從個人延伸到小團隊 / 早期 startup 的 builder、用 Claude Code / Cursor / Codex 做日常 agent 工作的開發者、需要管控「誰能用哪個 model / 哪個 MCP / 哪個本地 model」的安全團隊，以及想把 Anthropic-compatible plugin import 到自家 workspace 的企業 IT。
  - **How：** 個人路徑：在 [openworklabs.com/download](https://openworklabs.com/download) 抓桌面安裝檔（DMG / MSI / AppImage），或在 Claude Code / Codex / Cursor 貼 `https://api.openworklabs.com/start.md?v=hero` 這段 prompt 讓 agent 自己 install + 建立 workspace；接 MCP：`codex mcp add openwork --url https://api.openworklabs.com/mcp/agent` / `claude mcp add --transport http openwork https://api.openworklabs.com/mcp/agent` / OpenCode 寫 `opencode.json`。**Local dev：** `pnpm dev`（單 checkout 共用 dev profile）或 `pnpm dev:worktree`（多 worktree 隔離），banner 會印 `[openwork] dev profile=... cdp=http://127.0.0.1:9223`；Mac 上 mock keychain 預設 `OPENWORK_ELECTRON_USE_MOCK_KEYCHAIN=1`。
- **安裝方式：** **pip/uv/poetry：** 未列。**npm/pnpm/yarn/npx：** 未列（package name 是 private `@different-ai/openwork-workspace`，要從 source clone）。**Desktop 二進位（推薦）：** `https://openworklabs.com/download` 抓 macOS / Windows / Linux 安裝檔（README 截圖顯示 `.dmg` / `.msi` / `.AppImage`）。**Agent 自動 install：** 貼 prompt `Install OpenWork on my computer, set up my first workspace, and open it ready to use. Follow the steps in https://openworklabs.com/start.md?v=hero` 進 Claude Code / Cursor / Codex。**MCP 連線：** `codex mcp add openwork --url https://api.openworklabs.com/mcp/agent` / `claude mcp add --transport http openwork https://api.openworklabs.com/mcp/agent` / OpenCode `opencode.json` 加 remote MCP block；任何相容 MCP client 直接吃 URL `https://api.openworklabs.com/mcp/agent`。**Local dev：** `git clone` + `pnpm install` + `pnpm dev`（單 checkout）或 `pnpm dev:worktree`（多 worktree）。
- **Repo metadata：** Description `The open-source alternative to Claude Cowork (powered by opencode)`；Stars 19,646；Forks 2,015；Language TypeScript（pnpm workspace：含 `@openwork/desktop`、`@openwork/app`、`@openwork-ee/den-api`、`@openwork-ee/inference`、`@openwork-ee/den-web` 等）；Topics 空；License **NOASSERTION**（repo 內未放 `LICENSE` 檔，README 僅以「free, open-source」描述使用條款，引用 / 二次發佈前需 owner 確認授權）；Created 2026-01-14；Updated 2026-08-01；Pushed 2026-07-31；Size 280 MB；Homepage `https://openworklabs.com`。
- **近期 release：** GitHub Release：**2026-07-30**；`v0.18.12`；**OpenWork v0.18.12**。補充：另有流水線式的 `alpha-macos-v0.18.13-alpha.<build>+<sha>` prereleases（每日多個 commit-suffixed 版本，例如 `alpha-macos-v0.18.13-alpha.2082+0e84c10` / `2081+915c43d` …，2026-07-31 一連發 7 個），是 macOS alpha channel 的迭代節奏；亦有 `knoppers-N-experimental` 實驗性 release tags（2026-07-30，`prerelease: True`）。**stable 渠道就是 v0.18.12；alpha / experimental 走 prerelease flag。**

### mvanhorn/last30days-skill
- **Repo 摘要：** `/last30days` 是一款「AI agent-led search engine scored by upvotes, likes, real money — not editors」：Claude Code / Codex / Cursor / Copilot / Gemini CLI / Grok Build / OpenClaw 等 50+ agent skill host 共用的 skill pack，把 Reddit、X、YouTube、TikTok、Instagram Reels、Hacker News、Polymarket、GitHub、Digg、arXiv、Techmeme、LinkedIn、StockTwits、Threads、Pinterest、小紅書、Bluesky、Perplexity、Web 等 19 條來源以「真實 engagement」加權排序，由 agent judge 合成 brief。Repo 為 Python、MIT、56,342 stars（已是今天清單最大），最後 push 2026-08-01（active）；README 把每條來源的能力矩陣、典型用例（會前、招聘訊號、AI 主題發現、話題爆點預測、tool 比較、世界事件觀察、旅行、學習 prompt）都舉例寫死，並在 v3.11.1 列出 175 merged PRs（其中 52 位 community contributors、122 個 community-merged），社群治理（OpenSSF Scorecard、build provenance、Semgrep、OSV-Scanner、84% test coverage floor）做得極重。**v3.18.4（2026-07-28）為最新 stable release。**
- **3W1H：**
  - **What：** 跨 19 條社群/金流/學術來源的「AI agent-led search engine」skill pack + 社群健康治理。
  - **Why：** Google 聚合的是 editor、各家 AI 拿到的是 walled garden（ChatGPT 有 Reddit deal 但沒 X、Claude 全沒有、Gemini 有 YouTube 但沒 Reddit），沒有任何單一搜尋引擎能同時橫跨 Reddit / X / YouTube / TikTok / Polymarket / arXiv；last30days 把 19 條平台以 engagement（upvotes、likes、views、real-money odds）取代 SEO relevancy 排序，再交給 AI judge 合成 brief——這是「dumb Google 補不起來」的差異化。
  - **Who：** 開會前要先快速掌握對方 30 天動向的 builder / VC / BD；想找 AI 主題爆發前的 velocity 訊號的 researcher / podcaster；做 tool comparison（`/last30days OpenClaw vs Hermes vs Paperclip`）、讀 hiring signals（`--hiring-signals`）、做 podcast/文章角度挖掘的內容創作者；任何用 Claude Code / Cursor / Codex 等 agent 又不想從 12 個平台手動湊情報的開發者。
  - **How：** Claude Code：`/plugin marketplace add mvanhorn/last30days-skill` + `/plugin install last30days`（marketplace 自動更新）；其他 50+ Agent Skills host：`npx skills add mvanhorn/last30days-skill -g`；Codex 走 native plugin；Grok Build：`grok plugin marketplace add mvanhorn/last30days-skill` + `grok plugin install last30days`；Claude.ai web：到 `releases/latest/download/last30days.skill` 抓 `.skill` 上傳；Claude Desktop：抓 `.mcpb`；OpenClaw：`clawhub install last30days-official`。使用時：`/last30days Peter Steinberger`、`/last30days Listen Labs --hiring-signals`、`/last30days what's exploding in AI agents?`、`/last30days OpenClaw vs Hermes vs Paperclip`、`/last30days Iran vs USA`、`/last30days Nano Banana Pro prompting`。
- **安裝方式：** **pip/uv/poetry：** 未列（skill pack，沒有 PyPI entry）。**npm/pnpm/yarn/npx：** **Claude Code（推薦）：** `/plugin marketplace add mvanhorn/last30days-skill` + `/plugin install last30days`（marketplace 自動更新版本；或 `claude plugin update last30days@last30days-skill` 強制更新）。**Codex / Cursor / Copilot / Gemini CLI / 50+ Agent Skills host：** `npx skills add mvanhorn/last30days-skill -g`（`-g` 安裝全域、不加則 scope 到當前 project；更新用 `npx skills update last30days -g`）。**Grok (xAI Build CLI)：** `grok plugin marketplace add mvanhorn/last30days-skill` + `grok plugin install last30days`（加 `--trust` 跳過確認；更新 `grok plugin update last30days`）。**claude.ai web：** 從 `releases/latest/download/last30days.skill` 下載 `.skill`，於 claude.ai > Customize > Skills > + > Create skill > Upload 上傳。**Claude Desktop：** 從 `releases/latest` 抓對應平台 `.mcpb`，拖進 Settings > Extensions。**OpenClaw：** `clawhub install last30days-official` / `clawhub update last30days-official`。**Claude Code 雙安裝注意：** marketplace plugin 與 `npx skills` 兩種會共存且不 dedupe，`/last30days` 會出現兩條 entry——一台機器請選一個方法。
- **Repo metadata：** Description `AI agent skill that researches any topic across Reddit, X, YouTube, HN, Polymarket, and the web - then synthesizes a grounded summary`；Stars 56,342；Forks 4,903；Language Python；Topics 20 個，含 `ai-prompts`, `ai-skill`, `bluesky`, `claude`, `claude-code`, `clawhub`, `deep-research`, `hackernews`, `instagram`, `openclaw`, `polymarket`, `recency`, `reddit`, `research`, `social-media`, `tiktok`, `trends`, `twitter`, `web-search`, `youtube`；License MIT；Created 2026-01-23；Updated 2026-08-01；Pushed 2026-08-01；Size 50 MB。
- **近期 release：** GitHub Release：**2026-07-28**；`v3.18.4`；**v3.18.4**。補充：tags 流水線亦維持 `v3.18.4` / `v3.18.3` / `v3.18.2` 連發（自 2026-07-28 起每版都對應 release entry）；README 標示 v3.11.1（2026-07）後已 15 個 releases、社群 175 merged PRs 累計。

### yc-software/qm
- **Repo 摘要：** `qm` 是 YC（ycombinator.com 那個）出品、3 天前（2026-07-29）剛建立的「multiplayer agent harness for work」：headless core（Node 24+ + Fastify + TypeScript + Postgres）做為「共享組織的 agent」，每個人 / 每個 room（Slack channel / 群組 / project）拿到「隔離的 workspace + scoped memory + scoped files + keychain view + permissions + crons + web apps + durable sandbox」，並可換 Pi / OpenCode / Codex / Claude Code 等不同 harness 與模型而不被單一 vendor 鎖死；Slack 用 Bolt、Web UI 用 Vite + Lit、admin 介面走 Fastify HTTP，整體部署靠 `qm init` 生成 deployment repo（Fly 或 AWS target）後 `npm install` 部署。Repo 為 TypeScript、MIT、2,687 stars，最後 push 2026-08-01，仍在極早期高頻迭代；**v0.1.4（2026-07-31）為最新 stable release，版本節奏已從 v0.1.2 連發到 v0.1.4**。
- **3W1H：**
  - **What：** 給 startup 用的「multiplayer agent harness」headless core（Node + Fastify + Postgres + Vite + Lit），可接 Slack / Web UI / admin panel / portal。
  - **Why：** 個人型 agent harness（Claude Code / Codex / OpenCode）要擴到整間公司，權限、audit、secret rotation、skill sharing、background work、Slack 整合每件事都要重做；qm 把「per-scope sandbox + scoped memory + scope keychain + scope files」做成 first-class，每個 scope 一個「持久的小電腦」，共用 agent 但身份與權限隔開，組織層可設 strict / auto / dangerous posture 並由 classifier screen 外部資料。
  - **Who：** 想把 AI agent 從個人推到整個 startup / 團隊的 CTO / engineering manager；用 Slack 為主溝通、需要 agent 在 channel 裡協作的 ops / sales / support team；想自建內部 AI infra、不想被單一 vendor 鎖死、要 audit、要 governance 的 security-conscious 組織；以及想比較 Pi / OpenCode / Codex / Claude Code 哪個 harness 適合自架的開發者。
  - **How：** 組織 deploy 路徑：`npm exec --yes --package=@yc-software/qm@latest -- qm init . --org <slug> --target <fly-or-aws>` 然後 `npm install`，依 `deployment.md` 走完 infrastructure / web sign-in / connector credentials / Slack access / deploy / live verification；私人 fork 路徑：`gh repo create <org>/qm-private --private` 後 `git clone --bare git@github.com:yc-software/qm qm-seed.git` + `git -C qm-seed.git push --mirror git@github.com:<org>/qm-private` + `git remote add upstream git@github.com:yc-software/qm`，客製化集中在 `deploy/layers/<org>/`；Local dev：`pnpm dev`（共用 dev profile）或 `pnpm dev:worktree`（自動派生 profile + 自動釋放 CDP/Vite port）。
- **安裝方式：** **pip/uv/poetry：** 未列（Node 為主）。**npm/pnpm/yarn/npx：** **`qm init`（組織 deploy 入口）：** `npm exec --yes --package=@yc-software/qm@latest -- qm init . --org <slug> --target <fly-or-aws>` + `npm install`（README 原文命令）。**Private fork：** `gh repo create <org>/qm-private --private` + `git clone --bare git@github.com:yc-software/qm qm-seed.git && git -C qm-seed.git push --mirror git@github.com:<org>/qm-private && rm -rf qm-seed.git && git clone git@github.com:<org>/qm-private && git -C qm-private remote add upstream git@github.com:yc-software/qm`（README 特別警告：用 plain clone，**不要**用 GitHub 的 Fork button，因為 public fork 不能變 private、且共用 object network 讓 commit SHA 仍從 public side 取得）。**Local dev（pnpm）：** `pnpm dev`（單 checkout，共用 dev profile）；`pnpm dev:worktree`（多 worktree，`OPENWORK_DEV_PROFILE=auto` 自動派生）；命名 profile 例：`OPENWORK_DEV_PROFILE=my-feature OPENWORK_ELECTRON_REMOTE_DEBUG_PORT=0 PORT=0 pnpm dev`。**前置：** Node ≥ 24.15.0、npm ≥ 11.10.0（`engines` 欄位硬要求）；Postgres（per-deployment image）；可選 Slack Bolt credentials。**Customization：** `deploy/layers/<org>/` 放 config / sandbox / skill / plugin image / infra；與 upstream qm 共存靠 `update-qm`（merge upstream） + `upstream-pr`（送 fix 回 upstream）兩個 skill 維持界線。
- **Repo metadata：** Description `Multiplayer agent harness for work`；Stars 2,687；Forks 243；Language TypeScript（核心 `qm` package `name: qm`，`version: 0.1.0`，`type: module`）；Topics 空；License MIT；Created 2026-07-29；Updated 2026-08-01；Pushed 2026-08-01；Size 5.6 MB；Homepage `https://qm.ycombinator.com`。
- **近期 release：** GitHub Release：**2026-07-31**；`v0.1.4`；**v0.1.4**。補充：tags 流水線 `v0.1.4` / `v0.1.3` / `v0.1.2` 三天連發（`v0.1.2` 與 `v0.1.3` 皆開對應 release entry），節奏明顯是「3 天 3 版」的極早期 high-velocity shipper。

### VictorTaelin/OptMem
- **Repo 摘要：** `OptMem` 是 Victor Taelin（曾做 Ramer / skize 等）給 AI agent 的「426-token prompt 永久記憶」外掛：極簡到一個 `install.sh`（`curl -fsSL ... | sh`）把單檔 Python 工具（`memo`，~31 KB，零依賴）裝到 `~/.optmem/memo`，印出一段 `## Memory` block 讓你貼進 `AGENTS.md` / `CLAUDE.md`，agent 之後每個 session 都跑 `memo wake` 開頭讀記憶、邊做邊 `memo note "<1 行，max 280 bytes>"` 寫下學到的事。底層用 fixed-width record + append-only LOG + 二元樹狀 summary cache（rebuildable from log alone），到一百萬筆（608 MB）`wake` 仍 0.03 秒；`memo recall <regex>` 做全文搜尋、`memo zoom <lo>-<hi>` 開節點、`memo nap` 處理 merge、`memo forget <lo>-<hi>` 丟壞 summary。Repo 為 Python、License **None**（repo 內未放 `LICENSE` 檔，但 `memo` 是單檔可審閱）、1,044 stars，2026-07-25 建立、今天仍持續 push（最後 push 2026-07-31）；**未找到 GitHub release / git tag**，版本以 commit 推進。
- **3W1H：**
  - **What：** 單檔 Python CLI（`memo`）+ 一段 markdown prompt 的「AI agent 永久記憶層」，採用 fixed-width record + 二元樹 summary。
  - **Why：** 多數 agent memory 方案是「裝個向量 DB + embedding + retrieval pipeline」，對「agent 跨 session、跨 model、跨 vendor 該記得什麼」這件事過重、且依賴外部服務；OptMem 把 prompt + 工具壓到 426 token，安裝 1 行、零依賴、固定寬度 record 讓「position is identity」、每個 lookup 一次 seek，且強制 subagent 不寫 memo 以避免重複污染。
  - **Who：** 想要「跨 session 持久記憶」但不想引入 vector DB 的單人/小團隊 agent 開發者；用 Claude Code / Codex / Cursor + 多 session 多 project、需要 agent 記住使用者偏好 / 決策 / 嘗試的 builder；對極簡依賴哲學（單檔 Python + append-only log + 二元樹）有興趣的研究者；想為自己 `AGENTS.md` 加一段記憶守則的 power user。
  - **How：** `curl -fsSL https://raw.githubusercontent.com/VictorTaelin/OptMem/main/install.sh | sh` → 把印出的 `## Memory` block 貼到 agent 的 `AGENTS.md` 或 `CLAUDE.md` 頂端 → 之後每次 session 第一件事 `~/.optmem/memo wake`，過程中 `~/.optmem/memo note "..."`（max 280 bytes / 1 行）累積記憶；想搜尋用 `memo recall <regex>`、想打開 tree 節點用 `memo zoom <lo>-<hi>`、遇到 merge 用 `memo nap`、丟壞 summary 用 `memo forget <lo>-<hi>`；想換位置設 `$MEMORY_DIR`；想調整 wake 印幾行 `memo config WAKE_LINES=300`（reading budget，不影響存儲）。
- **安裝方式：** **pip/uv/poetry：** 未列（單檔 Python CLI，刻意不上 PyPI；`memo` 為 zero-dependency 單檔）。**npm/pnpm/yarn/npx：** 未列。**官方 install（推薦）：** `curl -fsSL https://raw.githubusercontent.com/VictorTaelin/OptMem/main/install.sh | sh`（把 `~/.optmem/memo` 裝進 `~/.optmem/`、印出 `## Memory` prompt block）。**手動 install：** `git clone https://github.com/VictorTaelin/OptMem.git` + 複製 `memo` 至 `~/.optmem/memo` + chmod +x；或從 `WINDOWS.md` 走 Windows 路徑。**PATH：** 把 `~/.optmem` 加到 `PATH` 即可直接打 `memo`（install 完預設會提示）。**設定：** `memo config` 看設定、`memo config WAKE_LINES=300` 改 wake 印幾行（96 ≈ 8k tokens；可隨時改、且只影響 reading budget、不重算存儲）；`$MEMORY_DIR` 自訂 memory 路徑（可指到 synced folder 或 git repo）。**Subagent 守則（prompt 內建）：** subagent 不准跑 `memo`、spawn 時需寫 `You are a subagent. Don't run memo.`。
- **Repo metadata：** Description `Permanent memory for AI agents. A 426-token prompt, a script, plug and play.`；Stars 1,044；Forks 60；Language Python（單檔 `memo`，~31 KB、zero-dependency）；Topics 空；License **None**（repo 內未放 `LICENSE` 檔；`memo` 為單檔可讀源碼評估，引用 / 二次發佈前請 owner 確認授權）；Created 2026-07-25；Updated 2026-08-01；Pushed 2026-07-31；Size 11 MB（含 `anim/` 內 GIF 與 `test.py`，不含記憶本體——記憶在 `~/.optmem/memory/`）。
- **近期 release：** **未找到 GitHub release**（無 `/releases/latest` 也無 git tag；版本節奏以 commit 為主，最後 push 2026-07-31）。

## 重點觀察

- **release 通道四種光譜，全部再現一次**：今天 5 個 repo 把昨日的 release 光譜再演一遍。`mvanhorn/last30days-skill`（v3.18.4 / 2026-07-28）和 `different-ai/openwork`（stable v0.18.12 / 2026-07-30 + 每日多發 `alpha-macos-v0.18.13-alpha.N+SHA` prerelease）走「健康 GitHub Release + 高頻 prerelease 流水線」型；`yc-software/qm`（v0.1.4 / 2026-07-31，3 天 3 版連發）和 `zhaoxuya520/reverse-skill`（v1.0.0 / 2026-07-17，v2.0.0 為 git tag 但未開 release）走「每個 tag 都開 release」或「部分 tag 不開 release」型；`VictorTaelin/OptMem` 完全 release-less，僅靠 commit 推進。值得注意的是 reverse-skill 與昨日 `gavamedia/deltafin` 出現完全同樣的「git tag 比 GitHub release 多一格」現象——也就是說 release 文化在「研究／實驗型」repo 的界線真的正在鬆動，今天開 release 的權力比昨日更集中在「有真實 binary deliverable」的 3 個 repo（qm、last30days、openwork）。
- **套件化路線四型**：今天沒有任何一個走「一行 `pip install` 就能用」的路線（Hugging Face speech-to-speech 的昨日姿態今天缺席）。**Type A：個人 agent skill pack，跨多 host 用 marketplace / skills CLI 安裝**——`mvanhorn/last30days-skill`（`/plugin marketplace add ...` / `npx skills add ... -g` / `clawhub install ...`）是這型的代表，README 把 6 種 host 安裝路徑都列出來，是今日唯一在「installation 跨多家 platform」做得最齊全的。**Type B：必須 clone、pnpm workspace 跑**——`yc-software/qm`（`pnpm dev` / `pnpm dev:worktree`，組織 deploy 走 `npm exec --package=@yc-software/qm@latest -- qm init`）與 `different-ai/openwork`（`pnpm dev`，binary 下載走 `openworklabs.com/download`）都屬這型，後者還補上一段「agent 自動 install」的 prompt 貼紙。**Type C：必須 clone、PowerShell/腳本做 refresh tool index**——`zhaoxuya520/reverse-skill`，且支援 Windows / Linux/macOS / Kali 三套 refresh script。**Type D：1 行 curl 安裝 + 單檔 zero-dependency Python**——`VictorTaelin/OptMem`，刻意不上 PyPI，連「subagent 不准跑 memo」都寫進 prompt。換言之，今天清單裡沒有人想要「最容易 pip install」這條路，反而都在搶「agent 怎麼用」的入口。
- **語言生態與「agent-host surface」**：今天 5 個 repo 落在 4 個主語言：Python（last30days-skill、OptMem）、TypeScript（openwork、qm，兩個都是 Node 24+ 當門檻）、PowerShell 為主混多語言（reverse-skill）、Python single-file（OptMem）。**AI coding client host surface 在今天是真正的「installation substrate」**：last30days-skill 把「Claude Code plugin marketplace / `npx skills add` / `grok plugin install` / `clawhub install` / `claude.ai` upload / `claude desktop` `.mcpb`」6 條路徑當成一等公民；reverse-skill 把「Claude Code / Codex CLI / Cursor / Kiro / Cline」當目標 client；qm 把「Pi / OpenCode / Codex / Claude Code」當可換 harness；openwork 把「Codex / Claude Code / OpenCode / ChatGPT」當 MCP consumer；OptMem 把「任何讀 `AGENTS.md` 的 agent」當消費者。今天的共同訊號是：**「能裝到哪個 agent」比「能被 pip install」更重要**，與昨日 `huggingface/speech-to-speech` 走「OpenAI Realtime API 相容」是同一個轉向——只是今天轉得更極端，installation 直接綁死在 agent skill marketplace。
- **上手門檻四級**：最低是 `OptMem` 的 1 行 `curl | sh` + 貼 prompt（前提是有裝 Python 3 與 `AGENTS.md` 的 agent）；中段是 `last30days-skill`（看 host 選 1 行命令，但每條來源 optional API key 要備齊——README 列了 14 個 `optionalEnv`）與 `reverse-skill`（clone + 跑 refresh-tool-index 確認本地有 jadx/Frida/IDA 等）；高的是 `openwork`（要看你是用 desktop binary 還是要 `pnpm dev` 接 Den）；最高是 `qm`（組織 deploy 要 Fly / AWS account、Postgres、Slack credentials、SCIM-like 管理流程；私人 fork 要 mirror + remote upstream + 兩條 boundary skill）。今天 5 個 repo 加總起來，剛好把「單人 agent skill → 團隊共享 → 組織 deploy」三層都蓋到。
- **值得追的兩個新訊號**：(1) **installation 正式從「library 註冊表」轉向「agent skill marketplace」**——last30days 一次列出 6 條 agent host 安裝路徑、openwork 把 MCP server URL 寫成「給任何 client 用」、qm 把「換 harness」當 first-class feature；對主人目前在做 Hermes / agent harness 路線的工作而言，這代表明天起「我要不要被 OpenAI Codex plugin marketplace / Claude Code marketplace / Grok Build marketplace / `clawhub` 列上」會變成「這個 skill 算不算上線」的標準門檻。(2) **「release-less 研究型交付」這個文化今天多了一位新案例**：昨日 `gavamedia/deltafin` 與 `xikhar/persona` 還帶個 prerelease tag，今天 `VictorTaelin/OptMem` 連 tag 都不放，純靠 commit + `WINDOWS.md` 與 `install.sh` 推送；這暗示 7 月底這波「極簡 agent 工具」圈對 semver 的敵意比之前更重，prompt 與安裝單檔即可 ship，「沒 release」已不再是品質警訊而是身份標記。下週值得再看的是 qm 是否會在 v0.1.x 收尾後切到 v0.2 主版（README 已暗示「organization-specific customization → deployment directory → org config, custom tools, sandbox image」這條路還會延伸），以及 openwork 是否會把 macOS alpha prerelease 收進 stable main branch 觸發 v0.19.0。