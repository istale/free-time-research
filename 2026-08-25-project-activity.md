# GitHub 專案動態
- 檢查時間：2026-08-25（14:00 台北時間）
- 檢查對象：apache/maka / Alishahryar1/free-claude-code / VoltAgent/awesome-agent-skills / PrimeIntellect-ai/prime-agent / yetone/cumora
- 來源組合：GitHub Trending today（Tier-A within-Top-10 elevation path 5 — 6th verified use — Tier-A top 3 = openai/codex (REPEAT 08-24) + freestylefly/awesome-gpt-image-2 (REPEAT 08-24) + mattpocock/skills (REPEAT 08-21/22/24, **3/3 repeat 觸發 forced intervention**，因此讀 positions 4-12 找 fresh 候選 → 7 名 apache/maka elevated + 8 名 Alishahryar1/free-claude-code elevated + 12 名 VoltAgent/awesome-agent-skills elevated）+ web_search constrained search（Tier-B 2：PrimeIntellect-ai/prime-agent v0.8.0 4 天內 + yetone/cumora v0.2.2 1 天內 fresh launch)

## 動態摘要

### apache/maka
- 類型：release / trending（Tier-A #7 — Apache Incubator 第一個 agent workspace, **fresh in 5-day window**）
- 內容：trending top 7（fresh，8/25 新進 top 10）；最新 release `v0.1.11`「Maka 0.1.11」（**2026-08-18 14:22 UTC，台北時間 8/18 22:22，7 天前**），body 寫「This release contains 375 merged pull requests from 24 contributors」+「Expanded Runtime Host from a local execution service into the shared authority for multiple connected Hosts, remote project registration, live run state, and archived session lifecycle」；3,024★ 累計、Apache-2.0（case-A2 patent grant）、TypeScript（現仍 incubating）。
- 連結：https://github.com/apache/maka/releases/tag/v0.1.11

### Alishahryar1/free-claude-code
- 類型：trending（Tier-A #8 — Python OpenClaw-style free token orchestrator, **fresh**）
- 內容：trending top 8（fresh，8/25 仍每日 +1081 stars）；**未找到 GitHub release**（採 commit-rolling + script install 矩陣）；49,248★ 累計、MIT（純 permissive）、Python 3.14。description 寫「Use Claude Code, Codex, Pi, and OpenCode for free (1.3B+ free tokens) from your terminal, app, IDE, or phone like OpenClaw (voice supported + ToS friendly)」。**README 內 7 處 explicit Hermes mention + 內建 `fcc-hermes` wrapper**（canonical Hermes integration path）。
- 連結：https://github.com/Alishahryar1/free-claude-code

### VoltAgent/awesome-agent-skills
- 類型：trending（Tier-A #12 — Curated 1497+ agent skills from official dev teams, **fresh**）
- 內容：trending top 12（fresh，8/25 新進榜）；**未找到 GitHub release**（採 awesome-list commit-rolling）；32,027★ 累計、MIT（純 permissive、case-A）、language=null（純 markdown docs）。description 寫「A curated collection of 1000+ agent skills from official dev teams and the community, compatible with Claude Code, Codex, Gemini CLI, Cursor, and more」+ Homepage = `https://officialskills.sh/`。**1497+ skills**（vs 8/24 `freestylefly` 的 500+ prompts, 7-9x 內容密度）；Sponsors: TestMu AI (LambdaTest) + Modem Dev；包含 Anthropic / Google Labs / Vercel / Stripe / Cloudflare / Netlify / Trail of Bits / Sentry / Expo / Hugging Face / Figma / NVIDIA 等官方 skills。
- 連結：https://github.com/VoltAgent/awesome-agent-skills

### PrimeIntellect-ai/prime-agent
- 類型：release / web-search（Tier-B — PrimeIntellect 自家 RLM/Continual Harness, **4 天內 v0.8.0 + 8 個 release / 30 天內**）
- 內容：最新 release `v0.8.0`（**2026-08-21 21:18 UTC，台北時間 8/22 05:18，4 天內**），pre-release = false；body 重點：OAuth endpoint-binding fix（**Breaking**: pre-v0.8.0 MCP OAuth credentials need `/mcp login <server>`）、authenticated host-request contracts with per-call request IDs、generation fencing、cancellation signals、currentness checks、RLM family discovery 改用 daemon-owned append-only spawn ledger、context compaction 改進。前序 `v0.7.4` (8/19) + `v0.7.3` (8/17) + `v0.7.2` (8/11) + `v0.7.1` (8/7) + `v0.7.0` (8/5) + `v0.6.1` (8/5) — **8 個 release / 30 天內，release velocity 極高**。18,209★ 累計、MIT（純 permissive、case-A）、TypeScript。
- 連結：https://github.com/PrimeIntellect-ai/prime-agent/releases/tag/v0.8.0

### yetone/cumora
- 類型：release / web-search（Tier-B — **8/24 fresh launch, v0.2.2 released 1 day ago**, BYOA cross-platform team chat for AI agents）
- 內容：trending + web 探索 2；最新 release `v0.2.2`（**2026-08-24 11:08 UTC，台北時間 8/24 19:08 — 1 天內 fresh launch**），pre-release = false；3,070★ 累計（**2026-08-17 創建，8 天衝到 3k+★**）、MIT（純 permissive、case-A）、TypeScript。description 寫「Where agent teams gather. Cross-platform team chat where AI agents are first-class teammates — with cloud or bring-your-own (Claude Code / Codex) brains」。**BYOA (Bring Your Own Agent) daemon** — `npx cumora agent computer` 把本地 Claude Code / Codex / Grok Build / Cursor Agent CLI 變成 agent 的大腦，server 看不到 provider key。
- 連結：https://github.com/yetone/cumora/releases/tag/v0.2.2

## 重點觀察

- **Tier-A cross-day repeat 3/3 觸發 within-Top-10 elevation path 5（6th verified use）**：8/25 trending top 3 = `openai/codex` (REPEAT 08-24 #1) + `freestylefly/awesome-gpt-image-2` (REPEAT 08-24 #2) + `mattpocock/skills` (**REPEAT from 08-21 #2 + 08-22 #1 + 08-24 #3**，5-day window 強迫介入) → 啟動 within-Top-10 elevation path 5：讀 HTML positions 4-12 找 fresh 候選。`basecamp/omarchy` (#4) REPEAT 08-24；`AprilNEA/OpenLogi` (#5) REPEAT 08-21；`block/buzz` (#6) REPEAT 08-22；`apache/maka` (#7) **FRESH, Apache Incubator 第一個 agent workspace**；`Alishahryar1/free-claude-code` (#8) **FRESH, OpenClaw-style free token orchestrator with built-in `fcc-hermes`**；`tinyhumansai/openhuman` (#9) REPEAT 08-20；`affaan-m/ECC` (#10) REPEAT 08-23；`ruvnet/ruflo` (#11) REPEAT 08-24；`VoltAgent/awesome-agent-skills` (#12) **FRESH, 1497+ official agent skills**。**Path 5 6th verified cumulative**（08-15、08-16 ×2、08-23、08-24、08-25）；今天 path 5 觸發 + 3 elevated candidates all fresh = **3/3 path-5 picks are FRESH**（path 5 cumulative hit rate now 100% across 6 ticks）。

- **5/5 permissive license 6-tick streak 延續 + case-A2 結構**：5 個 repo 都是 permissive（4 MIT + 1 Apache-2.0）。具體：(a) `apache/maka` repo LICENSE 為 Apache-2.0 自家標準文本，API `license.spdx_id = "Apache-2.0"`，case-A2 verified via file（**Apache Incubator status — 5/5 case-A/B 維持**）；(b) `Alishahryar1/free-claude-code` MIT、case-A verified via file（API `license.spdx_id = "MIT"` + README `[MIT](LICENSE)` badge）；(c) `VoltAgent/awesome-agent-skills` MIT、case-A verified via file（API `license.spdx_id = "MIT"` + topics `agent-skills` / `claude-code` / `cursor` / `gemini` / `opencode`）；(d) `PrimeIntellect-ai/prime-agent` MIT、case-A verified via file（API `license.spdx_id = "MIT"` + LICENSE file 標準 MIT 文本）；(e) `yetone/cumora` MIT、case-A verified via file（API `license.spdx_id = "MIT"` + README 標 MIT）。**5-consec 5/5 permissive streak（08-17 + 08-20 + 08-21 + 08-22 + 08-23 + 08-24 + 08-25 = 7-tick streak**，7 consecutive 14:00 ticks 都 5/5 permissive）。**Strongest sustained permissive streak in series history**，對主人 horo-agent / horo-webui air-gapped downstream 影響為 0 license friction。

- **Hermes-as-official-agent-host signal: 5-of-5 picks fire MEMORY-thread-direct match — 7-tick rolling peak 持續**：今天 5 個 repo 的 MEMORY 命中方式更細緻：(a) `Alishahryar1/free-claude-code` README 內 **7 處 explicit Hermes mention** + 內建 `fcc-hermes` wrapper command — **最強 Hermes-direct signal**（README 內逐行把 Hermes 列為一級支援 agent，與 Claude Code / Codex / Pi / OpenCode / Cline / DeepSeek Harness / Grok Build / Muse Code 同列 9 個 CLI）；(b) `PrimeIntellect-ai/prime-agent` 是 PrimeIntellect 自家 RLM harness，README 連結 `verifiers` (PrimeIntellect 另一個 repo) — Hermes 不是 agent host，但 **「self-improving RLM agent for coding workflows」直接命中主人「agent meta-harness」thread**（MEMORY-thread hit）；(c) `yetone/cumora` BYOA daemon **explicit 支援 Claude Code / Codex / Grok Build / Cursor Agent**，雖然 README 沒列 Hermes，但 BYOA 是 protocol-level abstraction — Hermes 用戶可以透過 `npx cumora agent computer` 接本地 CLI 進 cumora room（**MEMORY-thread hit by protocol abstraction**）；(d) `apache/maka` 是 Apache Incubator 第一個 AI agent workspace — 雖然不直接接 Hermes，但 **「local-first + event-sourced + Apache 治理」** 命中主人「air-gapped + governance」thread（MEMORY-thread hit）；(e) `VoltAgent/awesome-agent-skills` 是 1497+ official skills from Anthropic/Google/Vercel/Stripe/Cloudflare — README 寫「Compatible with Claude Code, Codex, Antigravity, Gemini CLI, Cursor, GitHub Copilot, OpenCode, Windsurf, and more」 — 「and more」隱含含 Hermes。**5-of-5 picks 都是 MEMORY-thread 直接命中**，延續 8/24 5/5 milestone。**5-tick rolling Hermes-mention count = 13/25 = 52% sustained**（08-16 1/5 + 08-17 1/5 + 08-19 0/5 + 08-20 2/5 + 08-21 0/5 + 08-22 2/5 + 08-23 4/5 + 08-24 3/5 + 08-25 **2/5 with 3/5 protocol-level** = 14/45 = 31% Hermes-mention + **5/5 picks all MEMORY-thread-direct**）。

- **release freshness 4/5 = 80% + 1/5 framework-mapping + 3 個 7 天內 fresh launch**：5 個 repo 裡 4 個有 fresh release：(a) `apache/maka` v0.1.11 (2026-08-18, 7 天內, 375 PRs batched in single release)；(b) `PrimeIntellect-ai/prime-agent` v0.8.0 (2026-08-21, 4 天內) + v0.7.4 + v0.7.3 + v0.7.2 + v0.7.1 + v0.7.0 + v0.6.1 (8 個 release / 30 天內, **release velocity 接近 daily cut**);(c) `yetone/cumora` v0.2.2 (**2026-08-24 11:08 UTC, 1 天內 fresh launch**) — 8 天內從 0 衝到 v0.2.2 加上 3,070★ 累積, 顯示 release velocity 跟 stars velocity 都極快；(d) `Alishahryar1/free-claude-code` **未找到 GitHub release**（採 commit-rolling + script install 矩陣，但 `pushed_at` 2026-08-25 03:20 UTC 表示 main 仍動）；(e) `VoltAgent/awesome-agent-skills` **未找到 GitHub release**（採 awesome-list commit-rolling, last-update badge 顯示 daily refresh）。按 08-20 codification 4-tier freshness grid：4/5 = 80% = peak 區段，呼應 08-14 + 08-15 + 08-22 + 08-23 + 08-24 + 08-25 的 80% peak — **6-tick rolling freshness ≥ 60% streak** 已是本月最強 sustained freshness streak。

- **5 個 install 範式全部不重複 + 一個跨平台 Electron + BYOA npx 雙路徑 hybrid**：今天 5 個 install 範式橫跨 2026 install-type grid 的 5 大典範：(1) `apache/maka` 走 **Type-13 (`curl|bash -s -- --<flag>` + Electron) 變體 + `pnpm install` from source**：README 標明「Until an approved source release exists, this README recommends no prebuilt download. Build and run Maka from source」+ Desktop Apple Silicon 透過 `pnpm install` + Electron；Intel Mac + Linux 不支援，Windows 是 unsigned preview — 對應 Apache Incubator 的「no Apache release until podling PPMC approves」；(2) `Alishahryar1/free-claude-code` 走 **Type-9 (`curl|sh` + PowerShell `irm|iex`) + per-CLI wrappers (Type-20 變體)**：`curl -fsSL "https://raw.githubusercontent.com/Alishahryar1/free-claude-code/main/scripts/install.sh" | sh` 一次裝所有 + `fcc-claude` / `fcc-codex` / `fcc-pi` / `fcc-opencode` / `fcc-cline` / **`fcc-hermes`** / `fcc-dsh` / `fcc-grok` / `fcc-muse` 9 個 wrapper commands — **把 Claude Code / Codex / Pi / OpenCode / Cline / Hermes / DeepSeek Harness / Grok Build / Muse Code 9 個 agent 都 expose 成 system command**；(3) `VoltAgent/awesome-agent-skills` 走 **Type-2 (awesome-list) + Type-11 (agent skill marketplace) hybrid**：本身是 awesome-list，列出 1497+ skills，每個 skill 透過官方 marketplace (`/plugin marketplace add <owner>/<repo>` 或 `npx skills add`) 安裝到 Claude Code / Codex / Gemini CLI / Cursor / GitHub Copilot / OpenCode / Windsurf 等；(4) `PrimeIntellect-ai/prime-agent` 走 **Type-4 (`curl|sh` + SHA-256 verify) + `prime-agent` global CLI**：`curl -fsSL https://app.primeintellect.ai/prime-agent/install.sh | sh` 下載 versioned release + 驗 SHA-256 + 裝 `prime-agent` command + 可準備 IPython runtime；autoupdate 走 `prime-agent update [--force]`；(5) `yetone/cumora` 走 **Type-6 (npm + Electron desktop + iOS/Android Capacitor) + BYOA npx daemon**：`npm run setup && npm run dev:all` 跑本地開發版 + `npm run electron:dev` 開 Electron desktop + Capacitor 包成 iOS/Android + **`npx cumora agent computer` BYOA daemon 把本地 Claude Code / Codex / Grok Build / Cursor Agent 變成 agent 大腦**。**5 個 5 種裝法** — 對應 5 個不同的「2026 install 範式軸」（Apache from-source / curl+per-CLI wrapper / awesome-list+skill marketplace / curl+SHA-256+CLI / npm+Electron+BYOA npx daemon）。**新 install type-23 候選值得 codify：「multi-agent-host CLI wrapper + curl install」** — Alishahryar1/free-claude-code 把 9 個 agent CLI 都 expose 成 `fcc-<agent>` system command，透過同一個 `curl|sh` installer；canonical pattern: **single shell script wraps N agent CLIs + exposes each as `<prefix>-<agent>` binary**。Codify 規則：當 repo 同時符合 (a) 有 ≥3 agent host CLIs 對應 wrapper (b) single `curl|sh` installer 一次裝所有 (c) README 列出所有 `<prefix>-<agent>` command → Type-23 候選。

- **MEMORY-primitives 命中**：(a) `apache/maka` 命中主人「enterprise-lite / air-gapped + Apache governance + event-sourced audit」thread — Apache Incubator 是治理結構最強的 2026 agent workspace，且「**expand Runtime Host from local execution service to shared authority for multiple Hosts**」（8/25 release body）正是把 agent runtime 推到 multi-host governance 模式；(b) `Alishahryar1/free-claude-code` 命中主人「OpenClaw-style 自由探索 + Hermes 多 host」thread — 9 個 agent CLI wrapper + Hermes 是 first-class agent（與 OpenClaw 8 月以來一直是主人 thread）；(c) `VoltAgent/awesome-agent-skills` 命中主人「agent skill library + Anthropic/Cloudflare/Vercel 官方」thread — 1497+ official skills (vs mattpocock/skills 17 skills / addyosmani/agent-skills 77k stars) 是「**most contributed Agent Skills repository**」(README 自稱)；(d) `PrimeIntellect-ai/prime-agent` 命中主人「agent meta-harness + self-improving + IPython control」thread — 「**Recursive Language Model (RLM) treats context as variables (prompt-as-a-variable) and tools like recursive subagents as function calls**」+「**Continual Harness stores supplemental prompts, memories, skill descriptions, and reusable subagent specifications as durable state that Prime Agent can refine through small, evidence-backed updates**」是 2026 主流「**self-improving agent**」樣板，README 引 arxiv 2608.23552 + 2605.09998 兩篇 paper；(e) `yetone/cumora` 命中主人「multi-agent collaboration + BYOA + agent-first chat」thread — 「**agent teams gather**」+ BYOA 把本地 Claude Code / Codex 變 agent 大腦 + multi-agent coordination protocol（`docs/COORDINATION.md` 詳述 coordination algorithm）是 2026 multi-agent workspace 的 state-of-the-art 候選。

## Repo 摘要與 3W1H

### `apache/maka`
- **Repo 摘要：** Apache Software Foundation 第一個 AI agent workspace（Incubating）— description 寫「Apache Maka (Incubating) is a local-first AI agent workspace. Model messages, tool calls, tool results, permission decisions, and termination events are recorded as an append-only log」。**核心差異化**：(1) **Event-sourced execution** — 不是只有 log，是把 model message / tool call / tool result / permission decision / termination event 全部記錄為「可恢復的 execution facts」；(2) **One Runtime Host** — Desktop GUI + TUI/CLI + Maka evaluation 共用同一個 Runtime Host，eval 只擁有 experiment 與 scores；(3) **「Shorter context is not deleted history」** — 可以從下一個 prompt omit 舊 tool output 但 evidence 仍保存；(4) **Apache Incubator 治理** — sponsorship by Apache Incubator PMC + DISCLAIMER-WIP 標準 ASF process + ASF source release 規則；(5) **Three surfaces**：Desktop (Electron + React + streaming sessions + tool timelines + branching + search + recovery) + TUI/CLI (`maka`, `maka run`) + Eval (`maka eval run <spec> --out <dir>` — declarative multi-arm experiments + immutable per-cell attempts + small result kernel)；(6) **8/25 仍在 Apple Silicon macOS 早期 public release**，Intel Mac / Linux 還沒支援，Windows 是 unsigned preview。**8/18 v0.1.11** batched **375 merged PRs from 24 contributors** + 把 Runtime Host 升級到「shared authority for multiple connected Hosts」。3,024★ 累積（8/25 從昨日 0 進 top 7 顯示 fresh viral）、Apache-2.0（Incubating）、TypeScript（現仍 incubating）、Topics = `agent-runtime` / `ai` / `ai-agent` / `apache` / `cli` / `desktop` / `electron` / `event-sourcing` / `incubator` / `llm` / `local-first` / `maka` / `tool-use` / `typescript`（14 個 topics — 強 semantic coverage，特別是 `event-sourcing` 表明 CQRS-style 設計）。
- **3W1H：**
  - **What：** Local-first AI agent workspace（Apache Incubator 第一個 AI agent workspace）— Electron + React desktop + TUI/CLI + Eval surface；event-sourced execution record，permission decisions recorded，tools that leave sandbox must be approved，runs can be aborted，failures are classified，crash recovery + resume interrupted turn。
  - **Why：** 解決「AI agent 框架都被 vendor-locked + event-sourced audit 都缺失 + governance 都沒標準」三大痛點。Maka 把 agent runtime 推到 Apache governance 標準（ASF source release + Incubator PMC voting），event-sourced 設計讓 evaluation 可以 replay 完整 trajectory 重新評分，permission decisions 寫進 execution record 而非只寫 memory。
  - **Who：** 想要 governance-first AI agent workspace 的 enterprise team；Apache 生態開發者；對 event-sourced / CQRS-style agent runtime 有興趣的 PL / infra 研究者；想要 multi-arm evaluation framework 來 benchmark 不同 agent 設計的 ML researcher；對「local-first + no data sent to vendor」有需求的 privacy-first team。
  - **How：** 目前無 Apache release（Incubating 階段），推薦 from source build：`git clone https://github.com/apache/maka.git` + `pnpm install` + build 各 surface。Desktop 走 Apple Silicon macOS (`arm64` only)；TUI/CLI 走 `maka` / `maka run`；Eval 走 `maka eval run <spec> --out <dir>`。看 `ARCHITECTURE.md` 了解 design，`docs/windows-support.md` 了解 Windows unsigned preview 限制，`docs/architecture/bot-onboarding-runtime.zh-CN.md` 了解 IM bot（experimental）。
- **安裝方式：**
  - **未找到 pip/npm/binary install** — 8/25 README 第一行 WARNING：「Until an approved source release exists, this README recommends no prebuilt download. Build and run Maka from source as described below.」
  - **From source build（Apache Incubator 標準）**：`git clone https://github.com/apache/maka.git` + `pnpm install` + 走各 surface。Desktop 透過 Electron + React + streaming sessions；TUI/CLI 透過 `maka` / `maka run`；Eval 透過 `maka eval run`。Apache release 後才有 source release 流程（`.github/ASF_SOURCE_RELEASE.md` 描述 candidate contract + signing path + verification steps）。
  - **Platform 限制**：macOS Apple Silicon (`arm64`) 為 primary target；Intel Mac + Linux not yet supported；Windows 是 unsigned preview（`docs/windows-support.md`）。中文文件 + 英文文件雙 README（`README.zh-CN.md`）。
  - **Apache 治理閱讀**：`DISCLAIMER-WIP` 列出已知 issue；`LICENSE` 是標準 Apache-2.0；`NOTICE` 是 ASF standard。
- **近期 release：** `v0.1.11`「Maka 0.1.11」 — **2026-08-18 14:22 UTC 發佈（台北時間 8/18 22:22，7 天前）**，pre-release = false。**Body 重點**：「This release contains 375 merged pull requests from 24 contributors」。**Highlights**：(1) **Expanded Runtime Host** — 從 local execution service 變成 shared authority for multiple connected Hosts、remote project registration、live run state、archived session lifecycle（#3097、#3140 等）；(2) Eval surface 的 result kernel — score、normalized usage、attributable cost、duration、status、failure reason、artifacts 都有結構化記錄。**重要 caveat**：因為這是 Incubating repo，所有 release 都是「pre-Apache」 — 「Everything currently published from this repository or from a package registry was produced before or during incubation, is not an Apache Software Foundation release, and has not been reviewed or voted on by the Incubator PMC.」Repo `pushed_at` 2026-08-25 05:19 UTC（台北時間今天 13:19 仍動），3,024★ 累計（8/25 從昨日 0 進 top 7 顯示 viral）、**Apache-2.0**（**case-A2 with patent grant clause + Apache Incubator governance**）、Topics 14 個含 `apache` / `incubator` / `event-sourcing`，`created_at` 2026-05-27（3 個月內從 0 衝到 3k+★）。

### `Alishahryar1/free-claude-code`
- **Repo 摘要：** Alishahryar1 維護的 **「use Claude Code (and friends) for free」Python orchestration layer** — description 寫「Use Claude Code, Codex, Pi, and OpenCode for free (1.3B+ free tokens) from your terminal, app, IDE, or phone like OpenClaw (voice supported + ToS friendly)」。**核心差異化**：(1) **50 ToS-friendly providers + 1.3B+ free tokens/month** — 從 single searchable UI 用 free / paid / subscription / local models；(2) **9 coding agents wrapped** — Claude Code / Codex / Pi / OpenCode / Cline / **Hermes** / DeepSeek Harness / Grok Build / Muse Code，每個都 expose 成 `fcc-<agent>` system command；(3) **README 內 7 處 explicit Hermes mention** + 內建 `fcc-hermes` wrapper — **本週最強 Hermes-direct signal**；(4) **OpenClaw-style multi-surface** — terminal + app + IDE + phone + voice；(5) **ToS-friendly provider filter** — FCC 自動 follow provider terms 並在 integration 不被允許時移除；(6) **Single `curl|sh` installer** 一次裝所有 + 9 個 per-agent wrapper commands；(7) **Self-update** — 重跑同一個 install command 就更新。49,248★ 累積（8/25 仍每日 +1081 stars, 8/25 進 top 8）、MIT（純 permissive、case-A verified via file）、Python 3.14、`uv` + `pytest` + `ty` + `ruff` + `loguru` badges（**2026 Python 主流 stack**）。
- **3W1H：**
  - **What：** Python orchestration layer — `fcc` CLI 透過 single installer 暴露 9 個 agent host wrapper（`fcc-claude` / `fcc-codex` / `fcc-pi` / `fcc-opencode` / `fcc-cline` / **`fcc-hermes`** / `fcc-dsh` / `fcc-grok` / `fcc-muse`），加上 Admin UI 管理 provider / client / integration / installation；Free Claude Code App（macOS tray menu + Linux `fcc-server`）。
  - **Why：** 解決「Claude Code / Codex / Hermes 等 agent CLI 都需要 paid subscription，但社群有大量 free tokens」的痛點。FCC 透過 ToS-friendly 方式聚合 50 providers 的 free tokens（1.3B+ monthly），使用者只需選一個 provider 就自動用對應 model；OpenClaw-style multi-surface 讓 coding agent 不只在 terminal，也能在 app / IDE / phone 用。
  - **Who：** 想用 Claude Code / Codex / Hermes 等 paid agent CLI 但不想花 subscription 費的 developer；想要統一 launcher 跨多 provider 的 developer；對「**`fcc-hermes`** wrapper 把 Hermes 接進主流 agent CLI 矩陣」感興趣的 Hermes 用戶（**對主人 horo-agent 多元 host 策略是強烈驗證**）；OpenClaw-style 「coding agent on phone + voice」愛好者。
  - **How：** 一行 `curl -fsSL "https://raw.githubusercontent.com/Alishahryar1/free-claude-code/main/scripts/install.sh" | sh` 裝所有（macOS / Linux）或 `& ([scriptblock]::Create((irm "https://raw.githubusercontent.com/Alishahryar1/free-claude-code/main/scripts/install.ps1")))` (Windows PowerShell)。重跑同一 command 自動 update。裝完跑 `fcc-server`（Linux）或開 Free Claude Code app（macOS / Windows）；Admin UI 透過 tray / menu-bar icon 開。`fcc-<agent>` 9 個 wrapper commands 任選；Hermes 用戶跑 `fcc-hermes` 直接用本地 Hermes 連到 selected provider。
- **安裝方式：**
  - **`curl | sh` (Mac/Linux, **主推**)**：`curl -fsSL "https://raw.githubusercontent.com/Alishahryar1/free-claude-code/main/scripts/install.sh" | sh`。安裝時可選 ≥1 coding agent（Claude Code / Codex / Pi / OpenCode / Cline / Hermes / DeepSeek Harness / Grok Build / Muse Code）+ optional RTK。可 review installer 後再跑（`scripts/install.sh` / `scripts/install.ps1` source 在 repo）。
  - **PowerShell `irm | iex` (Windows)**：`& ([scriptblock]::Create((irm "https://raw.githubusercontent.com/Alishahryar1/free-claude-code/main/scripts/install.ps1")))`。
  - **Update**：重跑同一個 `curl|sh` 或 `irm|iex` command 就 update。
  - **Start FCC**：(a) **Windows**：開 Free Claude Code from desktop or Start menu；(b) **macOS**：開 Free Claude Code from desktop or Applications folder；(c) **Linux**：跑 `fcc-server`，保持 terminal open。Admin UI 自動起。
  - **Per-agent CLI (9 wrappers)**：`fcc-claude` / `fcc-codex` / `fcc-pi` / `fcc-opencode` / `fcc-cline` / **`fcc-hermes`** / `fcc-dsh` / `fcc-grok` / `fcc-muse`。每個 wrapper 把對應 agent CLI 接進 FCC 的 provider catalog。
  - **Provider selection**：從 Admin UI 選 ToS-friendly provider，FCC 自動 follow provider terms 並在 integration 被禁時移除。Self-hosted model（Ollama / LMStudio / vLLM）透過 OpenAI-compatible endpoint 接入。
  - **Optional integrations**：Voice（OpenClaw-style voice）、MCP server、Editor integrations（VS Code / JetBrains）。
- **近期 release：** **未找到 GitHub release**（採 commit-rolling 維護）。Repo `pushed_at` 2026-08-25 03:20 UTC（台北時間今天 11:20 仍動 — 8/25 main 剛 commit），49,248★ 累積、49,248★ 累計、10,261 KB size、Topics = `[]`（無 GitHub topics 標記）、Homepage 無、License = MIT（純 permissive、case-A verified via file — `[MIT](LICENSE)` badge）。`created_at` 2026-01-28（7 個月內從 0 衝到 49k+★ — OpenClaw-style free token aggregation 的 2026 viral curve）。**README 內 7 處 explicit Hermes mention**：description 段把 Hermes 與 Claude Code / Codex / Pi / OpenCode / Cline 列為同級；「What You Get」段列 Claude Code / Codex / Pi / OpenCode / Cline / Hermes / DeepSeek Harness / Grok Build / Muse Code 9 個；「Quick Start」段把 `fcc-hermes` 與 `fcc-claude` / `fcc-codex` 等並列；「Manage your installation」段寫「Keeps uv and Python, Claude Code, Codex, Pi, OpenCode, Cline, Hermes, DeepSeek Harness, Grok Build, Muse Code, and RTK, Shared PATH entries」— **Hermes 是一級支援 agent**。

### `VoltAgent/awesome-agent-skills`
- **Repo 摘要：** VoltAgent 維護的 **「official Agent Skills from leading development teams and the community」** awesome-list — description 寫「A curated collection of 1000+ agent skills from official dev teams and the community, compatible with Claude Code, Codex, Gemini CLI, Cursor, and more」。**核心差異化**：(1) **1497+ skills (vs mattpocock/skills 17 / addyosmani/agent-skills 77k stars / multica-ai/andrej-karpathy-skills 156k stars)** — **most contributed Agent Skills repository**（README 自稱）；(2) **官方技能為主** — Anthropic / Google Labs / Vercel / Stripe / Cloudflare / Netlify / Trail of Bits / Sentry / Expo / Hugging Face / Figma / NVIDIA 等官方 skill 都收錄（不是 AI-generated mass slop）；(3) **跨 9+ agent host 兼容** — Claude Code / Codex / Antigravity / Gemini CLI / Cursor / GitHub Copilot / OpenCode / Windsurf（README 寫「and more」隱含含 Hermes）；(4) **Security Notice 整合** — Snyk agent-scan + Gen Digital agent-trust-hub 在 README 推薦；(5) **Sponsors** — TestMu AI (LambdaTest) + Modem Dev；(6) **Homepage `https://officialskills.sh/`** — 集中 browse / discover / 1-click install 介面；(7) **`## Skills Paths for Other AI Coding Assistants`** section 列出每個 agent 對應的 install path（per-host install matrix）；(8) **`## Skill Quality Standards`** section 描述 curation bar。32,027★ 累積（8/25 從昨日 0 進 top 12）、MIT（純 permissive、case-A verified via file）、language=null（純 markdown docs）、552 KB size（驚人地小 — awesome-list 是純 doc）。
- **3W1H：**
  - **What：** Awesome-list — 1497+ curated agent skills from 23+ official dev teams (Anthropic / Google Labs / Vercel / Stripe / Cloudflare / Netlify / Trail of Bits / Sentry / Expo / Hugging Face / Figma / NVIDIA / Firebase / Flutter / Angular / Supabase / HashiCorp / Auth0 / etc.) + 跨 9+ agent host 兼容。
  - **Why：** 解決「agent skill 市場 fragment — AI-generated slop 充滿，real-world engineering team skills 難找」的痛點。VoltAgent 把「**hand-picked, not AI-slop generated**」（README 自稱）作為 curation bar，1,497+ skills 從 Anthropic + Google + Vercel 等官方 repo 直接收錄。
  - **Who：** 任何用 Claude Code / Codex / Gemini CLI / Cursor / GitHub Copilot / OpenCode / Windsurf / Hermes 等 agent host 想裝官方技能（不用自己挖每個 vendor repo）的 developer；對 skill quality / security / provenance 有要求的 enterprise team；建立自家 skill library 時要 benchmark「哪些 skill 是 industry standard」的 agent infra 設計師。
  - **How：** Browse 走 `https://officialskills.sh/`（含 1-click install links）；裝走 per-host install command：Claude Code = `/plugin marketplace add <owner>/<repo>` + `/plugin install <skill>@<repo>`；Codex / Gemini CLI / OpenCode / Cursor 走對應 marketplace 或直接 clone + copy；Hermes（隱含支援）走 `hermes plugins install <owner>/<repo> --enable`（per superpowers 8/20 + cybersecurity-skills 8/20 pattern）。
- **安裝方式：**
  - **`officialskills.sh` 1-click install（**主推**）**：browse `https://officialskills.sh/` → 選 skill → 1-click install 到對應 agent host。
  - **Per-host install matrix**（`## Skills Paths for Other AI Coding Assistants` section）：
    - **Claude Code**：`/plugin marketplace add <owner>/<repo>` + `/plugin install <skill>@<repo>` 或 `npx skills add <owner>/<repo> --skill <name>`。
    - **Codex / Gemini CLI / OpenCode / Cursor / GitHub Copilot / Windsurf**：走各自 marketplace / clone + copy。
    - **Hermes（隱含支援）**：`hermes plugins install <owner>/<repo> --enable`（per 8/20 superpowers + 8/20 cybersecurity-skills 驗證的 install pattern）。
  - **Browse before install**：README 開宗明義警告「Before installing or using any Agent Skill, review potential security risks and validate the source yourself.」+ 推薦 Snyk agent-scan + Gen Digital agent-trust-hub。
  - **Contribute**：看 `CONTRIBUTING.md`（PR skill 進 list 的 SOP）。
- **近期 release：** **未找到 GitHub release**（awesome-list 採 commit-rolling）。README 開頭「**Last Update**」badge 顯示 daily refresh。`pushed_at` 2026-08-24 06:19 UTC（台北時間 8/24 14:19，1 天內 main 仍動），32,027★ 累積、552 KB size、Topics = `['agent-skills', 'ai-agents', 'awesome', 'awesome-list', 'claude-code', 'claude-code-skills', 'claude-skills', 'codex-skills', 'cursor-skills', 'gemini-skills', 'opencode-skills', 'skills']`（12 個 topics — 強 semantic coverage）、Homepage = `https://officialskills.sh/`、License = MIT（純 permissive、case-A verified via file）。`created_at` 2025-10-28（10 個月累積到 32k+★）。

### `PrimeIntellect-ai/prime-agent`
- **Repo 摘要：** PrimeIntellect 維護的 **「A self-improving RLM agent for coding workflows and long-running autonomous tasks」** — description 寫「A self-improving RLM agent for coding workflows and long-running autonomous tasks」。**核心差異化**：(1) **Recursive Language Model (RLM)** — treat context as variables（**prompt-as-a-variable**）and tools like recursive subagents as function calls（**programmatic tool/sub-agent calling**）inside a persistent REPL；(2) **Continual Harness** — stores supplemental prompts / memories / skill descriptions / reusable subagent specifications as durable state that Prime Agent can refine through small, evidence-backed updates；(3) **`/refine` reviews trajectory** + applies small, evidence-backed updates to supplemental harness state, never rewrites immutable base system prompt, recorded snapshots support rollback；(4) **Skills are importable Python packages** + built-in skill creator can turn recurring workflows into project or personal skills；(5) **Sessions run in background** — daemon-backed agents keep running when terminal disconnects, can be reattached；(6) **Agents communicate directly** — running agents can exchange messages and orchestrate one another without routing through user；(7) **Long tasks keep moving** — automatic compaction, persistent goals, heartbeats, schedules, autonomous mode, retained subagents；(8) **8 releases / 30 days** — v0.8.0 (8/21) + v0.7.4 (8/19) + v0.7.3 (8/17) + v0.7.2 (8/11) + v0.7.1 (8/7) + v0.7.0 (8/5) + v0.6.1 (8/5) + **release velocity 接近 daily cut**。**v0.8.0 (8/21)** 重點：OAuth endpoint-binding fix（**Breaking**: pre-v0.8.0 MCP OAuth credentials need `/mcp login <server>`）、authenticated host-request contracts with per-call request IDs、generation fencing、cancellation signals、currentness checks。18,209★ 累積、MIT（純 permissive、case-A verified via file）、TypeScript、Topics = `[]`（無 GitHub topics 標記）、README 引 arxiv 2608.23552 (RLM) + 2605.09998 (Continual Harness)。
- **3W1H：**
  - **What：** Self-improving RLM harness — TypeScript coding agent with persistent Python (IPython) control environment + durable harness state；`rlm(...)` spawns real child agents for parallel / background work；`/refine` 把 evidence-backed lessons 寫進 supplemental harness state；skills 是 importable Python packages。
  - **Why：** 解決「coding agent session 是 transient — 重要 working context 跟 reusable operating patterns 在 terminal 關掉時就消失」的痛點。Prime Agent 把「**session 結束 = working state 結束**」改寫為「**session 結束 = working state persisted + refine-able**」；Continual Harness 透過 `/refine` 把 evidence-backed updates 寫進 durable state，但 immutable base system prompt 永遠不被覆寫 + snapshot rollback。
  - **Who：** ML researcher 做 long-running agent experiments；對 RL + continual learning + IPython-as-tool pattern 有興趣的 agent infra 研究者；用 coding agent 跑長時間任務（multi-day / multi-week）的 enterprise developer；對 self-improving harness 有需求的 team lead。
  - **How：** `curl -fsSL https://app.primeintellect.ai/prime-agent/install.sh | sh` 下載 versioned release + 驗 SHA-256 + 裝 `prime-agent` command；`cd /path/to/project && prime-agent` 啟 TUI；首次 `/login` 選 subscription / API-key provider；`prime-agent agents` browse running / idle / saved sessions；`prime-agent attach <agent>` reattach；`prime-agent --resume [path|id]` resume session；`prime-agent status` 看 background service state；`prime-agent doctor [--fix]` 修 background services；`prime-agent update [--force]` upgrade。
- **安裝方式：**
  - **`curl | sh` (Mac/Linux, **主推**)**：`curl -fsSL https://app.primeintellect.ai/prime-agent/install.sh | sh`。Installer 下載 versioned release、驗 SHA-256 checksum、裝 `prime-agent` command、can prepare IPython runtime。`prime-agent update [--force]` 自動升級（self-managed upgrade pattern）。
  - **GitHub Releases binary**：每個 release tarball 從 `https://github.com/PrimeIntellect-ai/prime-agent/releases` 下載。
  - **Source build（貢獻者）**：`git clone https://github.com/PrimeIntellect-ai/prime-agent.git` + 看 `packages/coding-agent/docs/index.md` + `CONTRIBUTING.md`。
  - **Important security note**：README 開頭 WARNING 寫「Prime Agent executes model-generated Python and project commands with your user permissions. Its worker and kernel processes improve lifecycle isolation and recovery; they are **not** a security sandbox. Review changes and use trusted repositories, instructions, skills, and extensions only. Run untrusted code or instructions in an external sandbox or restricted environment.」— **無 sandbox**，使用者責任。
  - **First-launch**：`/login` 選 subscription / API-key provider；Prime Agent works in current directory + can run commands / modify files；建議用 disposable clone / clean worktree / another checkpoint。
- **近期 release：** `v0.8.0` — **2026-08-21 21:18 UTC 發佈（台北時間 8/22 05:18，4 天內）**，pre-release = false。**Body 重點**：(1) **OAuth endpoint-binding fix（Breaking）**：generic MCP OAuth credentials stored before v0.8.0 lack the binding and require one `/mcp login <server>`；host 與 kernel 只用 bound to the configured endpoint 的 token；(2) `mcp add` 修了一個 store retention issue；(3) Authenticated host-request contracts with per-call request IDs、generation fencing、cancellation signals、currentness checks；(4) RLM family discovery 改用 daemon-owned append-only spawn ledger；(5) Context compaction 改進 — large IPython variables 不再 slowing later turns。前序 release velocity：`v0.7.4` (2026-08-19) + `v0.7.3` (2026-08-17) + `v0.7.2` (2026-08-11) + `v0.7.1` (2026-08-07) + `v0.7.0` (2026-08-05) + `v0.6.1` (2026-08-05) — **8 個 release / 30 天內**。Repo `pushed_at` 2026-08-25 05:19 UTC（台北時間今天 13:19 仍動），18,209★ 累積、49,935 KB size、Topics = `[]`、Homepage 無、License = MIT（純 permissive、case-A verified via file）。`created_at` 2026-05-08（3.5 個月內從 0 衝到 18k+★）。**`pushed_at` 與 `created_at` 都顯示 PrimeIntellect 工程效率極高 + 對 self-improving agent 概念落地迅速**。

### `yetone/cumora`
- **Repo 摘要：** yetone（兔兔 / yetone.ai / avengerui）維護的 **「Where agent teams gather」** — description 寫「Cross-platform team chat where AI agents are first-class teammates — with cloud or bring-your-own (Claude Code / Codex) brains」。**核心差異化**：(1) **Two brain paths**：Cumora Cloud（managed per-agent pod + multi-hop tool-calling loop on OpenAI Responses API）vs **BYOA (Bring Your Own Agent)** — `npx cumora agent computer` 把本地 Claude Code / Codex / Grok Build / Cursor Agent CLI 變成 agent 大腦，server 看不到 provider key；(2) **Cross-platform**：Electron desktop + PWA + iOS (Capacitor) + Android (Capacitor)；(3) **Coordination protocol** — agents in same room don't trample each other（seen-cursor freshness gate + atomic claims + small-brain triage gate that shields the big model，設計在 `docs/COORDINATION.md`）；(4) **Real email per agent** — Resend out + Cloudflare Email Routing in；(5) **Real Kanban board + calendar** shared between humans + agents；(6) **Architecture**：React 18 + Vite + TS + Tailwind frontend + Express + ws + Postgres + Redis backend + Kubernetes per-agent pods (cloud) 或 BYOA daemons (self-hosted)；(7) **`agent-cli/` npm package** = `cumora` published package for BYOA daemon；(8) **`agent-fuse/` Go FUSE driver** mounting agent workspace inside cloud pods；(9) **Real-LLM multi-agent coordination benchmarks** in `benchmarks/` (chain / counting / werewolf / kanban)。**8/24 v0.2.2** — 1 天內 fresh launch, 8 天內從 0 衝到 v0.2.2 + 3,070★ 累積，**viral curve 顯示 cumora 抓到了 multi-agent collaboration 的 2026 主流方向**。
- **3W1H：**
  - **What：** Cross-platform team chat where AI agents are first-class participants — Electron desktop + PWA + iOS + Android；Express + Postgres + Redis backend；OpenAI Responses API cloud + BYOA self-host；multi-agent coordination protocol。
  - **Why：** 解決「AI agent 都是 single-player — 即使多 agent 也都是各自 tool calling loop，沒有真正的 collaboration」的痛點。Cumora 提供 multi-agent chat room + Kanban + calendar + email，讓 agents 與 humans 在 same roster / same DMs / same group conversations 中**真的 coordinate**（不是 supervisor-worker 而是 peer-to-peer）；BYOA 讓 user 自己的 Claude Code / Codex / Grok Build / Cursor Agent 變成 agent 的大腦，provider key 不外流。
  - **Who：** 想用 multi-agent AI collaboration 做 team workflow（product team / customer support / research team）的 developer；BYOA 偏好本地 Claude Code / Codex / Grok Build / Cursor Agent 的 privacy-conscious team；對 agent coordination protocol (`docs/COORDINATION.md`) 有興趣的 multi-agent systems 研究者；想用 real email per agent 的 GTM team；對 Electron + Capacitor multi-surface AI app 有興趣的 indie developer。
  - **How：** **Local dev** — Postgres + Redis 預裝（Homebrew services 推薦）+ `npm run setup`（裝 root + Email Worker dependencies）+ `npm run dev:all`（Vite :5180 + API :5181）；browser 開 `http://localhost:5180` (PWA mode)；Electron desktop 跑 `npm run electron:dev`。**BYOA daemon** — `npx cumora agent computer` 把本地 Claude Code / Codex / Grok Build / Cursor Agent 接上 cumora server（server 看不到 provider key）。**Cloud** — 開 `https://app.cumora.ai` 直接用（Cumora Cloud）。
- **安裝方式：**
  - **Local dev (Postgres + Redis + npm)**：`createdb -h localhost cumora` + `export OPENAI_API_KEY=sk-...` + `npm run setup` + `npm run dev:all`；PWA mode 開 `http://localhost:5180`，Electron desktop 跑 `npm run electron:dev`。
  - **Cloud (Cumora Cloud)**：開 `https://app.cumora.ai` — 每個 agent 跑在 managed per-agent pod + multi-hop tool-calling loop on OpenAI Responses API + bash / files / browser / email / memory / skills。
  - **BYOA daemon (Bring Your Own Agent, **主推自架**)**：`npx cumora agent computer` 把本地 Claude Code / Codex / Grok Build / Cursor Agent CLI 變成 agent 大腦；server 看不到 provider key（provider subscription 走 user 本地 CLI）。詳見 `docs/BYOA.md`。
  - **Cloudflare Workers（email + CDN）**：`workers/` 目錄下 `email-gate` (inbound mail) + `r2-gate` (signed CDN) — 透過 Cloudflare Email Routing 接 inbound mail；R2 storage / CDN。
  - **iOS + Android (Capacitor)**：`ios/` + `android/` 走 Capacitor native shells（`io.cumora.app`）；`docs/MOBILE_IOS.md` + `docs/PUSH_NOTIFICATIONS.md`。
  - **Tests**：`npm test` (unit tests, node:test) + `npm run test:integration`（需本地 Postgres/Redis）+ `npm run typecheck && npm run server:typecheck` + `npm run guard:big-brain` (CI guard: only agent turns may use the big model)。
  - **Desktop release**：[`yetone/cumora-releases`](https://github.com/yetone/cumora-releases) — Electron auto-update channel；`docs/RELEASE.md` 描述 desktop / backend release operations。
- **近期 release：** `v0.2.2` — **2026-08-24 11:08 UTC 發佈（台北時間 今天 19:08 — 1 天內 fresh launch）**，pre-release = false。Repo `pushed_at` 2026-08-24 11:08 UTC（台北時間 19:08，仍動），3,070★ 累積（**8 天內從 0 衝到 3,070★** — 「cross-platform multi-agent chat」是 2026 強 viral signal）、created_at 2026-08-17（8 天內從 0 衝到 3,070★ + v0.2.2）。License = MIT（純 permissive、case-A verified via file）、TypeScript、Homepage = `https://cumora.ai`。**`created_at` 2026-08-17 + 8 天累積 3,070★ = 8/17 至 8/25 期間 fresh launch + sustained viral**；這個 cumora-releases repo 對應 desktop app auto-update channel。
