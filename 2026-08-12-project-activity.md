# GitHub 專案動態
- 檢查時間：2026-08-12
- 檢查對象：msitarzewski/agency-agents、semantica-agi/semantica、nvm-sh/nvm、generalaction/emdash、inkeep/open-knowledge（共 5 個 repo）
- 來源：GitHub Trending daily（前 3）+ Web 探索（後 2，已人工驗證 github.com 上 owner/repo 存在 + 對應 release/README）

## Repo 摘要與 3W1H

### `msitarzewski/agency-agents`
- **Repo 摘要：** The Agency 是一組「生產就緒、可直接裝進 AI agent runtime 的人格化 specialist」prompt 集，把工程、紅隊、安全、行銷、SEO、付費投放、Discord 社群、Reddit 社群、QA 等「夢幻團隊」拆成一個一個有 personality、有 process、有 deliverable 的 markdown agent；目前支援 14 個 AI coding agent runtime（Claude Code、Cursor、Codex、GitHub Copilot、Gemini CLI、OpenCode、OpenClaw、Qwen、Aider、Windsurf、Kimi Code、Hermes、Mistral Vibe、Osaurus、Antigravity），並已有 macOS / Linux / Windows 桌面 app（agency-agents.app）一鍵安裝 + auto-update。適合想「不用從零寫 persona prompt，就直接拿到一個像真團隊的 AI roster」的小團隊 / solo founder。Repo 為 Shell（內容大量為 markdown agent + shell scripts）、MIT，143,699 stars（昨日 142,008 → 今日 143,699，24h 增量 +1,691），最後更新 2026-08-12。
- **3W1H：**
  - **What：** 一個由 markdown agent 檔 + 跨 harness install script + 桌面 app 組成的「AI agency roster」，不是單一 CLI、也不是 framework。
  - **Why：** 一般 prompt template 都是 generic、不分領域；The Agency 把每個角色寫成「identity / personality / core mission / workflow / deliverable / success metric」一條龍，並提供 `./scripts/install.sh --tool <claude-code|cursor|codex|...>` 把同一批 agent 鋪到 14 個不同 runtime。
  - **Who：** solo founder、小團隊、用 Claude Code / Cursor / Codex / 等任意 AI coding agent 寫 code 的人；想用 AI 跑 marketing / sales / QA / security review 的非技術 founder。
  - **How：** 最快是 `brew install --cask msitarzewski/agency-agents/agency-agents`（macOS）或抓 [agency-agents-app release](https://github.com/msitarzewski/agency-agents-app/releases/latest) 的桌面 app；命令列用戶 clone 後 `./scripts/install.sh --tool <name>` 或互動式 wizard；Claude Code 還可 `cp engineering/*.md ~/.claude/agents/` 手動挑單一 division。
- **安裝方式：** **macOS CLI：** `brew install --cask msitarzewski/agency-agents/agency-agents`（README 明確推薦）。**桌面 app：** 直接抓 [msitarzewski/agency-agents-app/releases/latest](https://github.com/msitarzewski/agency-agents-app/releases/latest)（macOS / Linux / Windows 一鍵瀏覽全部 roster 並安裝到 14 個 harness，含 auto-update；不是 `msitarzewski/agency-agents` 本身的 release，是它的「app」姊妹 repo）。**從源碼安裝：** `git clone` + `chmod +x scripts/install.sh` + `./scripts/install.sh --tool <claude-code|cursor|codex|...>`（也支援 `--division engineering,security` 篩選、用 `--list teams` 看全部 team + agent 數、`--dry-run` 預覽）。**OpenCode 陷阱：** OpenCode runtime 單次只能約 119 個 agent，超過會 silently drop；installer 會警告，建議用 `--division` 拆分。**Claude Code 手動：** `cp engineering/*.md ~/.claude/agents/`。**未找到 pip / npm library 安裝方式**（內部由 Shell script 跑，沒有 PyPI / npm package）。
- **Repo metadata：** Description `A complete AI agency at your fingertips - From frontend wizards to Reddit community ninjas, from whimsy injectors to reality checkers. Each agent is a specialized expert with personality, processes, and proven deliverables.`；Stars 143,699（24h +1,691）；Language Shell（內容主體是 markdown agent 檔 + shell scripts）；Topics 未列出（API 為空 list）；License MIT；Updated 2026-08-12；Pushed 2026-08-06；Created 2025-10-13；Homepage none；Default branch main。
- **近期 release：** **未找到 GitHub release**（`agency-agents` 本身 `GET /releases/latest` 回 404，無 GitHub release tag）；姊妹 repo `msitarzewski/agency-agents-app` 才是 release 通道；`pushed_at` 為 2026-08-06，今天的 trending 熱度來自 commit 推進 + 桌面 app 端的 auto-update + 24h star velocity。

### `semantica-agi/semantica`
- **Repo 摘要：** Semantica 是「Graph-Native Infrastructure for Context and Accountable AI Systems」，定位是 open-source、自架、auditable 的決定智慧 / 上下文管理層。它把你的企業資料 ingest 進來後建出 Context Graph + Knowledge Graph（RDF 與 LPG 雙模式），並在其上跑 graph analytics、因果推理與 W3C PROV-O provenance 串接；同期 ships 的還有 SHACL constraints、OWL/SKOS ontology editor、SPARQL/Datalog/Rete 推理、reasoning-time conflict detection、time-travel snapshot 與 PROV-O audit trail export。適合金融 / 醫療 / 法律 / 政府 / 國防等「決策要被監管者問 why、要 self-host」的單位，以及想用 Databricks Unity Catalog / Snowflake 直接變成 lineage-tracked KG 的 data platform 團隊。Repo 為 Python、MIT，5,108 stars（昨日 4,343 → 今日 5,108，24h 增量 +765），最後更新 2026-08-12。
- **3W1H：**
  - **What：** 一個 Python library + MCP server + CLI + interactive workbench，把「Context Graph + Decision Intelligence + Ontology + Reasoning + Provenance」做成 W3C 標準一致的模組化基礎設施。
  - **Why：** 多數 agent stack 只存 embedding、不存意義；context 不能解釋、決策不能 audit——在受監管領域等於是把 compliance 風險外包給下一個 prompt。Semantica 主打「no LLM required for graph construction / reasoning / provenance」這條 deterministic path，並把每個決策做成 first-class queryable object。
  - **Who：** AI/ML platform 團隊、Databricks / Snowflake 上的 data platform team、compliance / risk / audit team、需要在不外送資料的前提下 self-host KG 與 reasoning 的受監管企業。
  - **How：** `pip install semantica`（PyPI 0.6.5，需 Python ≥ 3.8）→ `semantica doctor` 驗證環境 → `from semantica.context import ContextGraph` 後用 `record_decision` / `trace_decision_chain` / `find_similar_decisions` / `analyze_decision_impact` / `check_decision_rules` 一條龍；CLI 提供 `semantica` 子命令，並提供 full-featured MCP server 與 Agno native 集成。
- **安裝方式：** **pip/uv/poetry：** `pip install semantica`（PyPI 0.6.5，依官方 PyPI badge 為 0.6.5；本機 `semantica doctor` 顯示 0.6.5）；`pip install semantica` 同時是 README Quick Start 的第一行。**原生 mcp server：** 由 `pip install` 帶入（README 標「full-featured MCP server」）。**未找到 npm / 桌面 binary**：此 repo 不發 npm package、不發 GUI desktop app。**從源碼：** clone + `pip install -e .`（README 為標準 Python packaging）。
- **Repo metadata：** Description `Graph-Native Infrastructure for Context and Accountable AI Systems`；Stars 5,108（24h +765）；Language Python；Topics `agent-memory`, `ai`, `ai-governance`, `ai-infrastructure`, `artificial-intelligence`, `context-engineering`, `context-graphs`, `data-engineering`, `decision-intelligence`, `developer-tools`, `explainable-ai`, `generative-ai`, `graph-rag`, `knowledge-graph`, `llm`, `ontology`, `provenance`, `python`, `reasoning`, `semantic-search`；License MIT；Updated 2026-08-12；Pushed 2026-08-11；Created 2025-06-25；Homepage https://getsemantica.ai；Default branch main。
- **近期 release：** **2026-08-11**；`v0.6.5`；**v0.6.5**（PyPI badge 同步顯示 0.6.5；`requires_python: >=3.8`；pre-release = false）。**主線 release 節奏：** v0.6.5 (2026-08-11) → v0.6.0 (2026-07-21 見昨日 08-11 報告) → 中間小修；對比昨日報告的 0.6.0，今日 0.6.5 是 21 天內推進 5 個小版，stable 推進中。

### `nvm-sh/nvm`
- **Repo 摘要：** nvm（Node Version Manager）是 POSIX-compliant 的 bash script，給「單機同時存多個 active node.js」用的 per-user、per-shell 切換工具；2010 年由 creation 起 16 年歷史，是 Node.js 生態最權威的版本管理器。它支援 `nvm install 24` / `nvm use 22` / `nvm alias default 20` / `nvm exec 18 npm test` / `.nvmrc` 自動切換 / `nvm exec` / `nvm migrate` 把 global packages 帶著走，也提供 zsh / bash / fish completion、Docker image 一鍵安裝、Ansible role、Alpine Linux 完整支援、cURL 與 wget 兩條 install path；trending 連兩天是因為 v0.40.6 釋出帶動一波 auto-update 流量。適合所有 Node.js 開發者（特別是要同時維護 Node 18 LTS / 20 LTS / 22 / 24 多個 major / minor 的 polyglot backend team）以及任何「在 CI / Docker 內需要 locked Node version」的人。Repo 為 Shell、MIT，94,519 stars，最後更新 2026-08-12。
- **3W1H：**
  - **What：** 一個 POSIX bash script 寫成的 Node.js 版本管理器，本質是 sh / bash / dash / ksh / zsh script + 一些 symlink / alias 工具，安裝到 `~/.nvm` 後 source 進 shell rc。
  - **Why：** 不同專案要鎖不同 Node major，system-wide 的 node 切換成本太高；nvm 用 per-shell、per-user 的安裝位置 + per-directory `.nvmrc` + `nvm use` 自動切換 + `nvm exec` 把 node 帶進 subprocess 解決「同一台機跑多個 Node 生態」的痛點；對 CI 還提供「`RUN curl -o- https://...install.sh | bash && nvm install $NODE_VERSION`」單行 pattern。
  - **Who：** 任何 Node.js / Deno 旁路但仍需 node 工具鏈的工程師；需要把多個 Node LTS 共存於同一開發機的 full-stack 與 polyglot backend team；CI / Docker / GitHub Actions 想 lock Node version 又不想每次下載完整 node tarball 的 SRE。
  - **How：** `curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.6/install.sh | bash` 一行 → `export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"` 載入 → `nvm install 24` 拿 node 24、`nvm use 22` 切換到 22、`nvm alias default 20` 設預設；在 repo 根目錄放 `.nvmrc` 寫 `22.22.1` 後 `nvm use` 自動切；CI 用 `nvm exec 18 npm test` 把 node 帶進 subcommand。
- **安裝方式：** **curl | bash（一行）：** `curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.6/install.sh | bash`（README default；script 會 clone 到 `~/.nvm` 並自動 append `~/.bashrc` / `~/.zshrc` / `~/.profile`）。**wget fallback：** `wget -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.6/install.sh | bash`。**Custom install path：** `curl ... | NVM_DIR="path/to/nvm" bash`（避免 trailing slash）。**Docker：** `FROM ubuntu:latest` → `apt install curl` → `RUN curl -o- ...install.sh | bash` → `ENV NVM_DIR=/root/.nvm` → `RUN bash -c "source $NVM_DIR/nvm.sh && nvm install $NODE_VERSION"`（README 給完整 Dockerfile）。**CI：** 同上但加 `BASH_ENV` 變數讓 non-interactive bash source 進 nvm.sh。**Ansible：** 社群有 geerlingguy/ansible-role-nvm 等 role。**manual install：** clone → source `nvm.sh` 直接用。**未找到 pip / npm / brew**：nvm 是 Node.js 上游的版本管理器，刻意不走 PyPI（無關 Python 也不上 npm 因為要管理 npm 自己的 host 環境）；macOS 雖然有 `brew install nvm` 但官方 README 推薦 `curl|sh` 路徑。
- **Repo metadata：** Description `Node Version Manager - POSIX-compliant bash script to manage multiple active node.js versions`；Stars 94,519（24h ~+1.2k）；Language Shell；Topics `bash`, `install`, `lts`, `node`, `node-js`, `nodejs`, `nvm`, `nvmrc`, `posix`, `posix-compliant`, `shell`, `version-manager`, `zsh`；License MIT；Updated 2026-08-12；Pushed 2026-07-24（v0.40.6 釋出後約 19 天未推 master）；Created 2010-04-15（16 年老牌 repo）；Default branch master。
- **近期 release：** **2026-07-15**；`v0.40.6`；**v0.40.6**（release notes 主軸為「`nvm set-colors`、`nvm migrate` 的 global package migration 改善」與 node 24 LTS 預設 alias 對齊；pre-release = false）。**主線 release 節奏：** v0.40.6 (2026-07-15) → v0.40.5 / v0.40.4（README 推導回推 2-3 月一個 minor）；今天 trending 衝量來自「`v0.40.6` 釋出帶動 7 月底到 8 月中的升級潮 + node 24 LTS 普遍在企業 team 啟用」的雙引擎，不是 8-12 當天新 release。

### `generalaction/emdash`
- **Repo 摘要：** Emdash 是 YC W26 的「Open-Source Agentic Development Environment」—— 一個 Electron 桌面 app 讓你「同時跑多個 AI coding agent」且每個 task 跑在自己獨立的 Git worktree（不是 workspace、不是 branch、不是 fork，而是真正 `git worktree add` 的實體隔離），你可以同時開 5 個 agent 探索 5 個不同 fix / feature 方向，再從單一視窗 review diffs、建 PR、看 CI、merge。Agent 端採 provider-agnostic（自動偵測已安裝的 Claude Code / Codex / Cursor / OpenCode / Amp / Devin / Qwen Code / Droid / GitHub Copilot 等 CLI），並透過 lifecycle hooks 寫進 agent user-level config 讓 status / notification / resumable session 跨 app 邊界都能跟到；Linear / GitHub / Jira / GitLab / Asana / Featurebase / Monday.com / Forgejo / Plain 的 issue 與 ticket 可一鍵送進 agent；remote project 透過 SSH / SFTP 連自家機器同套 workflow。Repo 為 TypeScript、Apache-2.0，5,389 stars，2025-08-28 建立、1 年內衝上 5k stars，最後更新 2026-08-12。
- **3W1H：**
  - **What：** 一個 Electron desktop app（macOS / Windows / Linux 全平台）+ provider-agnostic adapter，把「跑多個 coding agent」這件事從「多開幾個 terminal tab」提升成「每個 task 各自有 worktree、PR、CI、review 視角的 production-grade 環境」。
  - **Why：** 多 agent 平行作業過去都是「自己多開 tmux pane / 多 IDE window」，缺的是：(1) task 邊界不清楚（哪個 pane 跑哪個 fix？）(2) diff review 散落各處 (3) agent crash / 中斷後 resumable state 沒人管 (4) ticket-to-PR 沒打通；Emdash 用 Git worktree + Electron UI + lifecycle hook + 內建 PR review 把這四個痛點都收進來。
  - **Who：** 需要「同時跑 3-10 個 agent 探索多條 fix 路徑」的 senior engineer 與 team lead；用 Linear / Jira 跑 ticket flow 的 platform team；需要在 remote box（dev server、CI runner、雲端 workstation）跑同套 workflow 的 SRE / 平台工程師；以及「想從單一窗口監看多個 coding agent 進度」而不開一堆 terminal 的 solo founder。
  - **How：** `brew install --cask emdash`（macOS）或從 [release page](https://github.com/generalaction/emdash/releases/latest) 抓對應 OS installer（macOS arm64 / x64 DMG、Windows x64 MSI / portable exe、Linux x86_64 AppImage / amd64 deb）→ 開 app、選 local project 或加 SSH remote → 在 UI 內「Add task」並挑 CLI agent（Emdash 自動偵測已安裝 CLI）→ agent 跑時寫進 `~/.claude/`（或對應 agent config dir）的 lifecycle hook 讓 status / resumable session 跟到 Emdash → 完成後從 Emdash 內 review diff、建 PR、看 CI、merge。
- **安裝方式：** **macOS：** `brew install --cask emdash`（README 推薦；對應 [emdash-arm64.dmg](https://github.com/generalaction/emdash/releases/latest/download/emdash-arm64.dmg) Apple Silicon 與 [emdash-x64.dmg](https://github.com/generalaction/emdash/releases/latest/download/emdash-x64.dmg) Intel 兩條）。**Windows：** [Installer](https://github.com/generalaction/emdash/releases/latest/download/emdash-x64.msi)（per-user、不需 admin）+ [Portable](https://github.com/generalaction/emdash/releases/latest/download/emdash-x64.exe)。**Linux：** [AppImage](https://github.com/generalaction/emdash/releases/latest/download/emdash-x86_64.AppImage)（x86_64）+ [Debian package](https://github.com/generalaction/emdash/releases/latest/download/emdash-amd64.deb)。**Source build：** README 未列；main 分支是 Electron + TypeScript monorepo，可 `pnpm install && pnpm run dev`（由 `package.json` scripts 推導）。**未找到 pip / npm library 安裝方式**（Electron 桌面 app 不發 npm package，因為「npx emdash」無法承載 Electron runtime；CLI 派走 brew / OS installer）。
- **Repo metadata：** Description `Emdash is the Open-Source Agentic Development Environment (🧡 YC W26). Run multiple coding agents in parallel. Use any provider.`；Stars 5,389；Language TypeScript；Topics `agenticdevelopment`, `agenticdevelopmentenvironment`, `ai`, `claude-code`, `cli`, `coding-agents`, `codingagents`, `containerization`, `docker`, `gitworktrees`, `jira`, `linear`, `llm`, `openai`, `opensource`, `orchestration`, `parallel-agents`, `parallelexecution`, `terminal`；License Apache-2.0（LICENSE.md 明載 Copyright 2026 Generalaction）；Updated 2026-08-12；Pushed 2026-08-11；Created 2025-08-28；Homepage https://emdash.com；Default branch main。
- **近期 release：** **2026-07-17**；`v1.1.40`；**v1.1.40**（pre-release = false；release notes 主軸為「更多 agent provider 整合 + Linux deb / AppImage installer」）。**主線 release 節奏：** v1.1.40 (2026-07-17) → v1.1.x（推算月節奏 5-8 個 patch release）；今天 trending 來自 8-11 push 與 5k+ stars 衝量 + YC W26 標籤帶動。

### `inkeep/open-knowledge`
- **Repo 摘要：** OpenKnowledge 是「Notion meets VS Code meets LLM Wiki」的 markdown IDE / knowledge base 工具——它把「WYSIWYG markdown 編輯 + 整合 Claude / Codex / OpenCode / Pi 等 coding agent + out-of-the-box MCP / skills / agentic search + 視覺化 graph wiki link + Git/GitHub auto-sync + embeddable HTML」整合進單一 desktop / web app，並內建 LLM Wiki / second brain / knowledge graph starter pack；與主人先前 08-08 看過的 Karpathy `llm-wiki` 同一條設計哲學，但 OpenKnowledge 把整套「知識 ↔ agent」做成可裝可裝的 production app 而非 demo。Repo 為 TypeScript、GPL-3.0-or-later（OSI 開源但 copyleft 強），3,407 stars，2026-06-03 建立、70 天衝上 3.4k stars，最後更新 2026-08-12。
- **3W1H：**
  - **What：** 一個跨 macOS / Windows / Linux desktop app + Node.js 24+ CLI `@inkeep/open-knowledge` + web UI 的 markdown 編輯 + 知識管理 + LLM Wiki + agent integration 平台；前端 WYSIWYG、底層 MCP / skills 給 agent 用。
  - **Why：** 「Obsidian 太 geek、Notion 不能本地、AI markdown 編輯器都是 demo」三個 pain point 同時解：Obsidian 的本地 markdown vault 體驗 + Notion 的 WYSIWYG + Cursor 的 side-by-side AI editing + 第二腦 / LLM Wiki 的 graph link 視覺化，並用 Git/GitHub 同步取代 Notion 的 SaaS 鎖定。
  - **Who：** 寫 markdown spec / docs / knowledge base 的工程師；想做「second brain / PKM / LLM Wiki」的研究者與 analyst；想在自己 machine 跑 Notion-like experience + 整合 Claude Code / Codex 的 builder；以及想用 MCP / skills 把 knowledge base 暴露給 agent 的 RAG / 知識圖譜 builder。
  - **How：** **桌面 app：** 從 [openknowledge.ai/download](https://openknowledge.ai/download) 抓對應 OS（macOS Apple Silicon DMG、Windows 10+ x64 / arm64 Setup installer、Linux x64 / arm64 deb / rpm）。**Web app / CLI：** `npm install -g @inkeep/open-knowledge`（需 Node.js 24+、git）→ `cd your-project` → `ok init`（scaffold + wire up Claude Code / Claude Desktop / Cursor / Codex / OpenCode / OpenClaw）→ `ok start --open`（serve web editor + 開瀏覽器）。**MCP / skills 安裝：** `ok init` 自動偵測 host 的 agent harness 並安裝對應 MCP server + skills（用於 enriched search + authoring）。
- **安裝方式：** **npm（CLI / Web app）：** `npm install -g @inkeep/open-knowledge`（npm latest = 0.51.5，2026-08-11 21:49:07Z 上傳，與 `v0.51.5` git tag 同步釋出；engines.node = `>=24`；bin = `ok` 與 `open-knowledge`，兩個別名都指向 `dist/cli.mjs`）；LICENSE 為 GPL-3.0-or-later。**macOS desktop：** 從 [openknowledge.ai/download](https://openknowledge.ai/download) 抓 Apple Silicon DMG 拖進 Applications。**Windows desktop：** 從 release 抓 Setup installer（per-user、不需 admin）。**Linux desktop：** deb（Debian/Ubuntu）或 rpm（Fedora/RHEL）。**專案初始化：** `ok init`（自動偵測已安裝的 AI editor 並 wire up；支援 Claude Code、Claude Desktop、Cursor、Codex、OpenCode、OpenClaw）。**啟動：** `ok start --open`（serve web editor + 開瀏覽器）。**Git 同步：** 可選，README 寫「Git/GitHub based sync and sharing can optionally be enabled」；不啟用時完全本地 SQLite + filesystem。**未找到 pip / Homebrew**：此 repo 不發 PyPI package（TypeScript + Node.js stack），macOS 也不上 Homebrew Cask（官方只給 DMG + web app）。
- **Repo metadata：** Description `Beautiful, AI-native markdown IDE and LLM wiki`；Stars 3,407；Language TypeScript；Topics `2nd-brain`, `agent-skills`, `claude`, `codex`, `company-brain`, `docs`, `knowledge-`, `knowledge-base`, `knowledge-management`, `llm`, `llm-wiki`, `llm-wiki-karpathy`, `markdown`, `markdown-editor`, `md`, `mdx`, `notes`, `pkm`, `second-brain`, `skills`；License GPL-3.0-or-later（README 與 LICENSE 檔雙重明載；OSI 開源但 copyleft 強，closed-source fork / 商用嵌入需注意 copyleft 條款）；Updated 2026-08-12；Pushed 2026-08-12；Created 2026-06-03；Homepage https://openknowledge.ai；Default branch main。
- **近期 release：** **2026-08-11**；`v0.51.5`；**v0.51.5**（release notes 主軸為「agent harness 整合清單擴大 + web UI / desktop 同步推進」；pre-release = false；npm `@inkeep/open-knowledge` 0.51.5 為同日 21:49:07Z 上傳，與 git tag `v0.51.5` 同步釋出 — 雙通道 release pipeline 已穩定）。**主線 release 節奏：** 推算 v0.51.x 月節奏 8-15 個 patch release（70 天 3.4k stars + 從 v0.x 衝到 v0.51.x 暗示極高 release cadence）。

## 重點觀察
- **Tier-A 跨日重複觀察（第三度驗證）**：今天 Trending Top 3 之中 `#1 msitarzewski/agency-agents` 與 `#2 semantica-agi/semantica` 與昨日 2026-08-11 報告（`2026-08-11-project-activity.md`）的 5 個 repo 之中前 2 名完全重疊；按 cron prompt「取前 3 個」字面 + 2026-07-17 prompt-body-wins 先例 + 2026-08-06 第二次 codification，今日仍將其納入並標示結構性 source property —— 不是 bug，是 Trending 演算法把爆款 repo 推到第二日仍會停在 Top 10。今天多了一個差異：`#3 nvm-sh/nvm` 是 16 年老牌 repo 因 `v0.40.6` (2026-07-15) 的升級潮 + node 24 LTS 採用而擠進 Top 3（過去 2 週 trending Top 3 從未出現非 AI repo，今天破例）；對主人的意義是：trending 已從「AI infra / agent 框架」這個單調賽道擴張到「AI infra + 經典 devtools 因 AI 採用而被再次拱起」的雙引擎。
- **新興觀察：parallel-agent 桌面 app 開戰**：今日 5 個 repo 之中 `generalaction/emdash` 是 5,389 stars 的「YC W26 背書 + Git worktree 隔離 + 多 agent 平行 + Linear/Jira 整合」 desktop app，與昨日 trending 邊緣常客 `AMAP-ML/LongHorizon-Harness`（`v0.1.3`、560 stars、跨 desktop + CLI）形成同一條「multi-agent orchestration layer」 axis；差異是 LongHorizon-Harness 走「三角色 + 持久 verified state + 跨 desktop/CLI」的「harness-as-control-plane」 派，emdash 走「Git worktree + UI 視覺 + provider-agnostic CLI adapter」的「UI-for-agents」派。對主人的意義：若要做 `horo-agent` 的 air-gapped downstream，「agent 怎麼被可視化、agent 怎麼被平行調度」是 2026 下半年最值得追蹤的兩條設計哲學分支，今天這兩個 repo 分別給出兩種答案。
- **安裝 / 上手門檻 5 條岔路、TypeScript desktop 派佔大宗**：今天 5 個 repo 跨了 5 種 install type —— (1) **npm 一行 CLI + 多平台 desktop app** `npm install -g @inkeep/open-knowledge` + macOS DMG / Windows Setup / Linux deb/rpm 是 inkeep/open-knowledge 獨佔；(2) **Homebrew Cask + 桌面 app + 跨 runtime shell script 矩陣** `brew install --cask emdash` 是 emdash 獨佔；(3) **curl | sh + Docker + Ansible** `curl -o- https://...install.sh | bash` 是 nvm 獨佔；(4) **PyPI 一行 + 原生 MCP server** `pip install semantica` 是 Semantica 獨佔；(5) **brew cask + 桌面 app + shell script 矩陣** `brew install --cask ...` / 跨 14 個 harness 的 `scripts/install.sh` 是 agency-agents 獨佔。**TypeScript desktop 派佔 2 個**（emdash + open-knowledge），呼應 08-06 觀察「TypeScript 是 2026 團隊級 agent / dashboard / office 的事實標準」、並把 desktop 與 Web UI 重新整隊為同一條 TypeScript 派系。
- **語言生態**：5 個裡 Python × 1（semantica）、TypeScript × 2（emdash、open-knowledge）、Shell × 2（agency-agents、nvm；agency-agents 標 Shell 但 90% 是 markdown agent 檔 + shell scripts）。今天 5 個 repo 沒有任何一個以 Rust / Go / C++ 為主語言 —— 對比 08-04 `agentic-agi/AgentENV`（Rust）跟 08-05 一些 multi-lang 樣本，今天趨於「Shell 經典 + TypeScript 桌面 + Python infra」的三分；呼應 08-10 觀察「npm/pnpm 派僅出現在 skill / plugin / desktop 集」。
- **License 乾淨度分化（本日新增 GPL-3.0 例外）**：(a) **MIT 3 個**（semantica、agency-agents、nvm），(b) **Apache-2.0 1 個**（emdash），(c) **GPL-3.0-or-later 1 個**（open-knowledge）—— GPL-3.0 是 OSI 開源但 copyleft 強，closed-source fork / 商用嵌入需注意條款；對主人 air-gapped / 下游精簡專案是「能用但要保留 source + 同一 license」的設計約束，呼應 08-04 的「license 黑洞對下游的實質封鎖」、並升級昨日 08-11「license 黑洞 = NOASSERTION」的分類（今天多了一條「GPL-3.0 是真實黑洞、不是 NOASSERTION 灰色地帶」）。
- **MEMORY cross-reference（主人 pinned topic 對應）**：(1) **`semantica-agi/semantica` 的「W3C PROV-O provenance + decision as first-class object + time-travel snapshot + conflict detection」** ↔ 主人 `horo-agent` air-gapped downstream 的「run-time 可追溯」需求面，特別是 SOUL.md 提到「background process / 之前的 async 通知——每次都必須立即把該通知分類為『仍有效 / 已被取代』並引用後續真實的 exit_code / tests passed / /health 200」這種「provenance-by-evidence」恰恰是 Semantica 的核心設計哲學；今日本來 `v0.6.0 → v0.6.5`（21 天推進 5 個小版）也印證穩定節奏。(2) **`generalaction/emdash` 的「Git worktree 隔離 + provider-agnostic + Linear/Jira 整合 + lifecycle hook」** ↔ 主人 spec-driven N-stage workflow「一次 in_progress 一項、結束前 self-verify」 —— emdash 把「同一 repo 平行開多條 fix 路徑」當 first-class，比主人的「一次一項」更激進，但 lifecycle hook 讓 status / resumable session 可被外部讀回，正是主人「每階段可驗證」哲學的工程化擴展。(3) **`inkeep/open-knowledge` 的「WYSIWYG markdown + MCP / skills / agentic search + LLM Wiki + Git sync」** ↔ 主人 08-08 觀察過的 Karpathy `llm-wiki` + 主人 CMU-compatible 大雜燴型 small tool 偏好；open-knowledge 把 Karpathy 那一個 demo 做成 production app，並 out-of-the-box wire 進 Claude Code / OpenCode / OpenClaw；對主人「horo-agent + horo-webui 為什麼要分開」這組 downstream 設計是「markdown IDE + agent runtime 兩層分離」的同構案例。(4) **`nvm-sh/nvm` 的「16 年穩定維護 + POSIX-compliant + Docker / CI / Ansible 全路徑覆蓋」** ↔ 主人「下游精簡」硬規則的對立面 —— nvm 是「上游經典該怎麼做」的教科書：zero-deps shell script、release cadence 慢但向後相容 100%、multi-platform installer 一行；對主人做 `horo-agent` lite 下游時的啟示：「上遊可上 16 年不壞不是因為寫得多、是因為每一個 PR 都 backward-compatible + 不擴 scope」。(5) **`msitarzewski/agency-agents` 的「14 個 harness 同一套 agent markdown + install.sh --tool <name> 跨 runtime」** ↔ 主人目前 redeploy 的「horo-agent 與 horo-webui 為什麼要分開」這一組 —— 同樣的「content artifact 一次寫、多 runtime 共享」原則，agency-agents 在 prompt-level、horo 系列在 backend-runtime level；今天 agency-agents 24h +1,691 stars、semantica 24h +765 stars，兩個昨日上榜的 repo 今日仍在 Top 3，呼應 08-06 codification「Tier-A 跨日重複是結構性 source property」。
- **兩個新訊號值得追蹤**：(a) **`generalaction/emdash` 是 5,389 stars 的 YC W26 背書 + Git worktree 隔離 + multi-agent 平行 desktop app** —— 跟昨日 trending 邊緣的 `AMAP-ML/LongHorizon-Harness`（560 stars、v0.1.3）形成「multi-agent orchestration」 axis 的兩個對照樣本：emdash 走「UI / Git worktree 派」、LongHorizon-Harness 走「三角色 / control-plane 派」；主人若要做 horo-agent 的多任務調度 UI，後續 1-2 週應同時追蹤這兩個的 release cadence。(b) **`inkeep/open-knowledge` 70 天 3.4k stars + `v0.51.5` 一天一版 + `llm-wiki-karpathy` 直接列入 GitHub topic** —— 把 Karpathy 的 LLM Wiki 概念直接做成 production app，是「research idea → shippable product」最乾淨的樣本；release cadence 推算月節奏 8-15 個 patch release 是極高頻，但 license 走 GPL-3.0-or-later 對商業 fork 是警訊，主人若參考其架構做 `horo-` 系列下游，license 選擇應避免 copyleft。
