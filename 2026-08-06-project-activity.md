# GitHub 專案動態
- 檢查時間：2026-08-06
- 檢查對象：cloudflare/computer、huangruiteng/loopx、TencentCloud/TencentDB-Agent-Memory、genspark-ai/genoffice、EKKOLearnAI/hermes-studio
- 來源：Trending Daily Top 3 + 自由探索 GitHub Search（雙查詢：created:>2026-07-22 高星 + topic:ai-agent 高星 active）

## Repo 摘要與 3W1H

### cloudflare/computer
- **Repo 摘要：** Cloudflare Computer 把「Agent 的執行環境」做成 SQLite-backed 虛擬檔案系統 + pluggable runtime 服務，state 永遠在 Durable Object 內、執行面可換 container（computerd + FUSE mount）、just-bash、Workers 內的 ECMAScript module 三種 backend，稱為「給 agent 一台電腦」。Preview 階段，README 標示 NOT production ready，但 sandbox-sdk `npm install` 對比與 `fs-bench` 已揭露 FUSE 在 metadata-heavy 工作勝過實體磁碟；對想在 Cloudflare 上架 long-running agent sandbox、又不想自己接 LiteFS / DO sqlite 同步的 builder，這是第一個官方「可上車」介面。Repo 為 TypeScript，MIT，3,610 stars，最後更新 2026-08-05。
- **3W1H：**
  - **What：** Cloudflare Workers SDK 內的 SQLite 虛擬檔案系統 + 三種 pluggable execution backend（Container with FUSE / just-bash / Workers JavaScript）。
  - **Why：** 過去在 Workers 上跑 agent sandbox 要自接 LiteFS、自管 DO state、自接 bash；這個包把 state-of-truth 收進 DO、把執行面做成可換 backend 的 pluggable surface，並把 `fs` 與 `runtime.exec(source, { backend })` 收成單一 entrypoint。
  - **Who：** 想在 Cloudflare 架 long-running agent sandbox、要把 bash / node 互操作放在同一個 state 上的 builder；以及評估 E2B / Modal / Fly machines 替代品的 Workers-native 架構師。
  - **How：** `npm install @cloudflare/computer` 後照 `packages/computer/README.md` 建 Workspace、掛想要的 backend；example 提供 `examples/think`（chat agent 用 workspace 當 working dir）、`examples/think-compare-runtimes`（同任務並列跑 container 與 worker 兩種 runtime）等。
- **安裝方式：** **npm/pnpm/yarn/npx：** `npm install @cloudflare/computer`（npm 上 latest = 0.1.1，2026-08-03 上傳；與 `v0.1.1` git tag 同步釋出）。**Docker：** 預建 `computerd` 透過 `packages/computer-computerd-linux-x64` 私有 image 釋出，並非 npm 套件。完整 reproduce 流程見 `COLLABORATORS.md`（contributor-only），一般使用者直接 install npm 套件 + 走 `examples/*`。
- **Repo metadata：** Description `Give your agent a computer 👾`；Stars 3,610；Language TypeScript；Topics **空**（repo owner 未設 GitHub topic tags）；License MIT（LICENSE 檔明確，Copyright 2026 Cloudflare, Inc.）；Updated at 2026-08-05。
- **近期 release：** **未找到 GitHub release**（`/releases?per_page=5` 回空 list；最新 tag `v0.1.1` commit 為 2026-08-03 `release: stop bundling computerd binary in npm tarball; default publish to unreleased tag`，這是「Zero-GH-release + npm 是事實上的 release 通道」模式；`@cloudflare/computer` 0.1.1 對應同個 commit）。

### huangruiteng/loopx
- **Repo 摘要：** LoopX 是「給 long-running AI agent 用的 local 控制面 state kernel」：把 objective / gates / todos / scope / evidence / quota 收成一個 compact 層，給 Codex / Claude Code / Cursor / shell agent 當 control-plane 共享，但 **不取代** agent runtime；它把所有 multi-day / multi-turn agent loop 的「owner 還沒答、這 turn 該不該花 quota、handoff 寫回哪」顯式化，避免聊天記憶 + 計時器治不好的長任務。對應在「LoopX 為 agent-native Kanban for long-running work」的心智模型下，cards 帶 identity / authority / evidence，moves 是 validated operators（claim / gate / monitor / writeback）。Repo 為 Python，MIT，2,364 stars，最後更新 2026-08-06。
- **3W1H：**
  - **What：** Agent-agnostic local state kernel + 控制面 CLI（`loopx doctor` / `connect` / `status` / `quota should-run` / `todo claim` / `refresh-state` / `spend-slot`）+ Codex / Claude Code adapter。
  - **Why：** Multi-day / multi-turn 任務的痛點是「目標改、owner 決策出現、evidence 過期、agent 互交、scheduler 沒用也繼續燒 quota」；chat memory + timer 治不好，把這些收成顯式的 state kernel 才會 reviewable / restartable / hand-offable。
  - **Who：** 跑 multi-day 工程 / 研究 / 實驗 / benchmark / issue-fix loop、需要 owner / safety / 私資料 gate、需要 peer-agent lease / handoff 的人；以及評估「control plane 與 agent runtime 分層」架構的 builder。
  - **How：** 安裝採 `curl -fsSL https://raw.githubusercontent.com/huangruiteng/loopx/main/scripts/install-from-github.sh | bash` → `export PATH="$HOME/.local/bin:$PATH"` → 在專案根目錄 `loopx connect` / `loopx status`；agent 端從 Codex App / Codex CLI / Claude Code / OpenCode / Cursor 任一 host 走 `/loopx <task>` 啟動。
- **安裝方式：** **未列（刻意不上 PyPI；`loopx` 為 zero-deps 單檔 CLI 配合本地 state 目錄）**：**curl | sh**：`curl -fsSL https://raw.githubusercontent.com/huangruiteng/loopx/main/scripts/install-from-github.sh | bash`（安裝到 `~/.local/bin/loopx`）；**Contributors**：`git clone https://github.com/huangruiteng/loopx ~/loopx` → `~/loopx/scripts/install-local.sh`。Python 3.11+、POSIX shell、curl/tar 為唯一依賴；git 僅 contributor 流程需要。
- **Repo metadata：** Description `Lightweight loop engineering state kernel for long-running AI agent teams. Agent-loop agnostic across Codex, Claude Code, and other coding agents, with durable goals, quota-aware auto-wake, executable`；Stars 2,364；Language Python；Topics `agent-control-plane`, `agent-ops`, `ai-agents`, `codex`, `long-running-agents`, `loop-engineering`, `loopx`, `workflow-automation`；License MIT（LICENSE 檔 Copyright 2026 LoopX contributors）；Updated at 2026-08-06。
- **近期 release：** **2026-08-04**；`v0.4.1`；**LoopX v0.4.1**（v0.4.1 不是預發行）。

### TencentCloud/TencentDB-Agent-Memory
- **Repo 摘要：** TencentDB Agent Memory 走「symbolic short-term + layered long-term」雙路徑：把 heavy tool log 抽成 Mermaid 符號（短期），把對話抽成 L0 conversation / L1 atom / L2 scenario / L3 persona 的 semantic pyramid（長期），刻意拒絕「平舖向量」+「暴力歷史堆疊」。README 開宗明義標示與 OpenClaw / Hermes Gateway 同一條生態線，並用 WideSearch 51.52% 相對 pass-rate gain / 61.38% token 節省、PersonaMem 48% → 76%、SWE-bench +9.93% 等具體 benchmark 結果。Repo 為 TypeScript，MIT（LICENSE 檔明載），15,315 stars（+1,212 / 24h），最後更新 2026-08-05。
- **3W1H：**
  - **What：** Memory hub 服務（Memory Core / Memory Hub / Proxy）+ 四類資產（Chat Memory / Skill / LLM-Wiki / CodeGraph）+ OpenClaw 與 Hermes Gateway 雙路徑 plugin。
  - **Why：** 傳統 agent 每次從零重學 SOP / 專案背景 / 工具約定；TencentDB 把記憶做成 hierarchical + heterogeneous storage（底層 DB 留證據、上層 Markdown 留結構），AI 在 context 內只看 L3 persona 與 Mermaid canvas，drill down 走 `node_id`。
  - **Who：** 想避免「換框架就重來」、要跨 OpenClaw / Hermes / Claude Code 共用記憶的 agent 團隊；以及評估 memory layering / 符號化 / token-saving 設計模式的架構師。
  - **How：** `git clone https://github.com/Tencent/TencentDB-Agent-Memory.git` → `cd TencentDB-Agent-Memory/deploy/global-images` → `cp .env.example .env` → `$EDITOR .env` → `./start-all.sh`（啟三服務，預設 `http://localhost:8125` 為 Memory Hub 面板）；v1.x/v0.x → v2.0.0 升級前跑 `MemoryCore/scripts/migrate-v2-to-v3`。
- **安裝方式：** **npm：** `@tencentdb-agent-memory/memory-tencentdb`（latest 1.0.1，2026-07-14 上傳，OpenClaw plugin）；**GitHub source deploy：** clone → 進 `deploy/global-images` → `.env` 填兩組 LLM 參數 → `./start-all.sh`；**Hermes Gateway 整合：** 設 `MEMORY_TENCENTDB_GATEWAY_CMD="sh -c 'cd ~/.memory-tencentdb/tdai-memory-openclaw-plugin && exec npx tsx src/gateway/server.ts'"` 或放 `~/.memory-tencentdb/memory-tdai/tdai-gateway.json`；**推薦 auto-discovery**：把 plugin 放在 `~/.memory-tencentdb/tdai-memory-openclaw-plugin/`，Gateway 第一次對話時自動 `Popen()` 啟動。完整流程見 `INSTALL.md` / `INSTALL_CN.md`。
- **Repo metadata：** Description `TencentDB Agent Memory is a team-level memory hub for AI Agents — turning conversations, docs, and code into four reusable memory assets (Chat Memory, Skill, LLM-Wiki, Code-Graph) that are governed, s`；Stars 15,315；Language TypeScript；Topics `agent`, `ai-agent`, `embedding`, `llm`, `local-first`, `long-term-memory`, `memory`, `openclaw-plugin`, `vector-search`；License NOASSERTION（API 字段）；LICENSE 檔實為 MIT（Copyright Tencent 2026）；Updated at 2026-08-05。
- **近期 release：** **2026-08-03**；`v2.0.0`；**v2.0.0**（v2.0.0 不是預發行；同時為昨日 2026-08-05 報告的同 release，跨日重複出現於今日 Trending — 結構性 source property 見 ## 重點觀察）。

### genspark-ai/genoffice
- **Repo 摘要：** GenOffice 是「五個 Electron app 共用一個 engine layer」的 AI-native office suite：Docs（`.docx` byte-preserving 來回，dirty paragraph 才重生）、Sheets（`.xlsx` 走 Univer 核心 + in-house Rust sidecar + Konva 圖表 + pivot / slicer / conditional formatting）、Slides（`.pptx` 自家 parse/render，含 masters / charts / cropping / ink / HarfBuzz text shaping）、PDF（pdf.js + pdf-lib），加上 shell app 做 hosting + auto-update。每個 app 共用同一個 AI panel：block-granular editing + diff 快照，或在 Sheets/Slides/PDF 上做 tool-calling agent。Repo 為 TypeScript + Electron，Apache-2.0，1,862 stars（最新版本 v0.5.83 在 2026-08-05 釋出），最後更新 2026-08-05。
- **3W1H：**
  - **What：** 5 個 Electron apps（Docs / Sheets / Slides / PDF / shell）+ 7 個 pure-TS engine packages（docx-engine / pptx-engine / file-parse / agent-core / ai-provider / ai-search / i18n / ui / project-store / electron-utils）。
  - **Why：** 多數 AI office 工具把 AI 視為「聊天視窗貼在旁」；GenOffice 反過來把「AI 編輯」當 first-class workflow：block 級別的 dirty tracking、version snapshot、diff，並且 byte-preserving round trip — 開了再存不會破 Word layout。
  - **Who：** 想在自家設備（macOS Apple Silicon / Windows x64）跑一套 self-host-able AI office 的個人與小團隊；研究 block-granular AI editing / byte-preserving docx round trip / 五 app 共用 engine 架構的 builder。
  - **How：** 一般使用者下載 `GenOffice-0.5.83-arm64.dmg` 或 `GenOfficeSetup-v0.5.79.exe`（簽章後 installer，從 release 直接抓）；開發者 `npm install` → `npm run fixtures` → `npm test` → `npm run dev`；Sheets 還需 `cargo` toolchain 編譯 xlsx sidecar。
- **安裝方式：** **預編譯 installer（推薦）：** `GenOffice-0.5.83-arm64.dmg`（macOS Apple Silicon，2026-08-05 從 `v0.5.83` release 釋出）、`GenOfficeSetup-v0.5.79.exe`（Windows x64，nsis installer）。**Source build：** `npm install` → `npm run dev` 啟五 app 對應 Vite dev server；`npm run dist:mac` / `npm run dist:win` 重新打包；Sheets app 額外需 Rust toolchain 編 xlsx sidecar（`npm run build -w @genoffice/sheets` 自動）。**注意：** `ee/` 目錄為未來 enterprise modules 保留，採「GenOffice Enterprise License」例外（其餘 Apache-2.0）。
- **Repo metadata：** Description `An AI-native office suite for macOS and Windows: word processor, spreadsheet, presentations, and PDF.`；Stars 1,862；Language TypeScript；Topics `ai`, `docx`, `electron`, `office-suite`, `pdf`, `pptx`, `xlsx`；License Apache-2.0（LICENSE 檔明載，另有 `ee/LICENSE` 例外）；Updated at 2026-08-05。
- **近期 release：** **2026-08-05**；`v0.5.83`；**GenOffice v0.5.83**（v0.5.83 不是預發行）。

### EKKOLearnAI/hermes-studio
- **Repo 摘要：** Hermes Studio 把自己定位為「給 Hermes Agent 用的 desktop app + local runtime + web console」：Agent chat 走 Socket.IO `/chat-run`（流式 + tool traces + generated-file preview + 持久 local session），另開「Local control plane」管理 profile / provider / model / credentials / memory / skills / plugins / logs，加上 10 個 platform channel（Telegram / Discord / Slack / WhatsApp / Matrix / Feishu / DingTalk / QQBot / WeChat / WeCom）的統一設定頁、Kanban、視覺 workflow（Vue Flow canvas）、usage analytics、scheduled jobs、model management。分發三條路：Windows / macOS / Linux desktop app + npm CLI 套件 `hermes-web-ui` + Docker image。Repo 為 TypeScript / Vue 3，**Business Source License 1.1**（LICENSE 檔實為 BUSL-1.1，非 OSI 開源；商用 production 部署前需對 Owner 確認細則），9,794 stars，最後更新 2026-08-06。
- **3W1H：**
  - **What：** Hermes Agent 的 desktop / local runtime / web console，內含 agent chat、profile-aware 控制面、視覺 workflow（Vue Flow）、scheduled jobs、Kanban、10 個 platform channel 設定、usage analytics、model management。
  - **Why：** Hermes Agent 命令列 + state.db 的單機體驗很好，但多人 / 多 channel / 視覺化 workflow 這層還缺 UI；Hermes Studio 把這層補上，並用「自建 SQLite session DB + 讀 Hermes state.db」雙軌制，讓 Web UI 不取代 Hermes state 而是它的 projection。
  - **Who：** 已經在用 Hermes Agent 但想要 Web UI / 排程面板 / 視覺 workflow / 多 platform channel 統一管理的人；以及評估「自建 session DB 與上游 state DB 共存」架構（WebUI-lite / air-gapped downstream）的 builder。
  - **How：** 一般使用者 `npm install -g hermes-web-ui && hermes-web-ui start`；或下載 desktop app（Windows / macOS / Linux 三平台）；或拉 Docker image。Web UI 啟動後自動探索 `~/.hermes/auth.json` 取得已登入的 profile / provider / model 列表。
- **安裝方式：** **npm/pnpm/yarn/npx：** `npm install -g hermes-web-ui`（npm latest = 0.6.39，2026-08-06 05:44:26Z 上傳；與 `v0.6.39` git tag 同步釋出；repository.url 指向 `git+https://github.com/EKKOLearnAI/hermes-studio.git`，**這是同名的另一個 `hermes-web-ui`，與上游 `nesquena/hermes-webui` 的 `hermes-webui` 套件不衝突**）；**Desktop app：** `https://github.com/EKKOLearnAI/hermes-studio/releases/latest` 抓對應 OS 安裝檔；**Docker：** README 提及有提供 image。**License 注意：** BUSL-1.1 並非 OSI 開源；商用 production 部署 / 二次發佈 / fork 需對 Owner 確認細則。
- **Repo metadata：** Description `Web dashboard for Hermes Agent — multi-platform AI chat, session management, scheduled jobs, usage analytics`；Stars 9,794；Language TypeScript；Topics `agent`, `ai-agent`, `chat-ui`, `dashboard`, `hermes`, `hermes-agent`, `hermes-web-ui`, `llm`, `multi-model`, `multi-platform`, `self-hosted`, `typescript`, `vue3`, `web-ui`；License NOASSERTION（API 字段）；LICENSE 檔實為 **Business Source License 1.1**（Copyright 2026 EKKOLearnAI）；Updated at 2026-08-06。
- **近期 release：** **2026-08-06**；`v0.6.39`；**v0.6.39**（v0.6.39 不是預發行；npm `hermes-web-ui` 0.6.39 為同日 05:44:26Z 上傳，與 git tag `v0.6.39` 同步釋出 — 雙通道 release pipeline 已穩定）。

## 重點觀察
- release 新鮮度今日整體偏熱：LoopX `v0.4.1` 為 8-4、TencentDB Agent Memory `v2.0.0` 為 8-3（連兩天出現在 Trending Top 10，跨日重複出現屬結構性 source property 而非 dedup bug — 見下一條）、GenOffice `v0.5.83` 與 hermes-studio `v0.6.39` 同日釋出（8-5 / 8-6）；只有 `cloudflare/computer` 走 Zero-GH-release + npm-only（`@cloudflare/computer` 0.1.1 對應 tag `v0.1.1`，8-3 釋出）。五個 repo 中四個有當週 release，release cadence 在 2026-08 已接近每日。
- Tier-A 跨日重複觀察：今天 Trending Top 3 之中 `TencentCloud/TencentDB-Agent-Memory` 與昨日 2026-08-05 報告（`f154630`）的 5 個 repo 之一重疊；按 cron prompt「取前 3 個」字面 + 2026-07-17 prompt-body-wins 先例，今日仍將其納入並標示結構性 source property（**不是 bug，是 Trending 本身把爆款推到第二日仍會出現在 Top 10**），下一份報告若再出現就會觸發真正的 dedup。
- 上手門檻明顯分三層：(1) hermes-studio + LoopX + cloudflare/computer 一行指令（`npm install -g hermes-web-ui` / `curl | bash` / `npm install @cloudflare/computer`）即可起； (2) GenOffice 走預編譯 DMG / nsis 安裝，但源碼 build 需要 Rust toolchain + Playwright + Electron； (3) TencentDB Agent Memory 要 clone + `.env` + `./start-all.sh` 起三服務，部署成本是五個中最高。
- 語言生態：TypeScript 仍是 2026 團隊級 agent / dashboard / office 的事實標準（cloudflare/computer、TencentDB Agent Memory、GenOffice、Hermes Studio 4/5）；Python（LoopX）走 control-plane state kernel 的單檔 CLI 路徑。Hermes Studio 是唯一 Vue 3 / Electron 雙棧，其餘都用 React / Worker 體系。
- 套件化觀察：五個 repo 的交付路徑差異極大 — hermes-studio + cloudflare/computer 都用「npm 是事實上的 release 通道」（gh release 空、tags + npm 同步）；LoopX 走 Type-4「curl|sh + zero-deps 單檔 CLI」（08-01 第四類）；TencentDB Agent Memory 是少數「npm scoped + GitHub source deploy」雙路徑；GenOffice 走「預編譯 DMG / nsis installer + npm source dev」。**套件化類型比上次（08-05 三型分布）再多出「BUSL-1.1 + 商用前需授權」這條 Hermes Studio 例外**。
- 跨 5 repo 觀察，「agent 化既有 app axis」今日再延續：cloudflare/computer 把 Cloudflare Workers 變成「agent 的電腦」、GenOffice 把 Office 變成「AI-first editing」、Hermes Studio 把 Hermes Agent 變成「Web UI 可達的桌面 app」、LoopX 把 long-running agent loop 變成「可管理的控制面」、TencentDB Agent Memory 把 agent 變成「跨框架可共享的記憶體」 — 5/5 全部屬「agent 化既有介面」這條結構性 axis（08-04 codification 第三度驗證，從 08-05 的 4/5 升到 5/5）。對主人的意義：觀察重點已從「哪個 repo 值得試」轉到「這條 axis 還能開出什麼新介面」 — 比如把 LoopX 的 control-plane 觀念套到主人的 `daily-research-digest-pm-playbook` 的 spec-driven N-stage workflow。
- 對主人 air-gap / 下游精簡專案特別相關的兩個觀察：(1) Hermes Studio 走「自建 SQLite session DB + 讀 Hermes state.db」雙軌制，是「保守減法 + 端到端驗證」原則在 dashboard-side 的實踐樣本 — 不重寫 state 結構、Web UI 仍能做自己的事（08-04 reference 的 Lite-agent 模式）；(2) LoopX 顯式標示「不取代 agent runtime，只在 control plane 投影」 — 跟主人 `kanban-orchestrator` 的「don't do the work yourself」同樣的拆分哲學；主人若要把 LoopX 的 loop engineering 觀念借進來用，可以從 `loopx quota should-run` 與 `loopx todo claim` 兩個最小介面開始借。
