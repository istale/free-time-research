---
title: GitHub 自由探索 2026-09-18（14:00 台北時間）
date: 2026-09-18
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 7 天 repeat）
  - web_search 當日 AI 開源新聞（Tier-B web 探索 2）
  - GitHub REST API `/repos/<slug>` + `/releases/latest` + raw `README.md`
---

# GitHub 專案動態

- 檢查時間：2026-09-18（14:00 台北時間）
- 檢查對象：`Tencent/BrowserSkill` / `TencentCloud/Octop` / `coder/coder` / `TencentCloud/TencentDB-Agent-Memory` / `microsoft/agent-framework`
- 來源組合：GitHub Trending today 共解析 20 筆，**前 3 名全 repeat**（rank 1 `alibaba/open-code-review`、rank 2 `cloudflare/security-audit-skill`、rank 3 `addyosmani/agent-skills` 都已在 7 天內寫過），**Fresh top 3** 為：rank 4 **`Tencent/BrowserSkill`**（TypeScript、4,518★、`bsk CLI 0.3.0` published 2026-09-17、`pushed_at` 2026-09-18 = Tencent 出的 "let AI agents use your real, logged-in browser without interrupting your work" CLI + Chrome/Edge extension、Hermes Agent + OpenClaw + Claude Code + Codex + CodeBuddy + Cursor + WorkBuddy + Pi + DeepSeek Harness 九大 agent harness 一鍵 `bsk install-skill`）+ rank 16 **`TencentCloud/Octop`**（Python 3.12+、3,614★、MIT、`v1.0.0` published 2026-09-14、`pushed_at` 2026-09-18 仍在 push、PyPI package `octop` 已是 v1.0.0 — Tencent 出 self-hosted multi-user multi-agent AI assistant，harness-agent + harness-gateway + harness-memory + harness-browser 四件 stack、ACP 雙向跟 IDE/terminal AI 串、MBTI 16 personality templates、Tencent Docs/Weibo/News OAuth connectors）+ rank 19 **`coder/coder`**（Go、14,939★、AGPL-3.0、`v2.34.11` published 2026-09-16、`pushed_at` 2026-09-18 = self-hosted cloud dev environment 平台 + Coder Agents 跑 native AI coding agent 在 control plane、6 條 active maintenance branch `v2.32 / v2.33 / v2.34 / v2.35 / v2.36 / v2.37` 平行 release、Coder AI Gateway 把 LLM credentials 集中控管不在 workspace）。Tier-B web_search 取到 **`TencentCloud/TencentDB-Agent-Memory`**（TypeScript、26,909★、MIT、`v2.0.1` published 2026-08-25、npm `@tencentdb-agent-memory/memory-tencentdb`、Node 22.16+ — team-level memory hub for AI Agents，把 conversation/docs/code 編成 Chat Memory / Skill / LLM-Wiki / CodeGraph 四種 reusable asset，`OpenClaw` / `Hermes` 官方 badge、OpenClaw plugin + Hermes Gateway adapter 雙向整合）+ **`microsoft/agent-framework`**（Python + .NET、13,573★、MIT、`dotnet-1.21.0` published 2026-09-11、`python-1.18.0` published 2026-09-10、`pushed_at` 2026-09-18 = Microsoft 官方 agent framework、pip `agent-framework` + NuGet `Microsoft.Agents.AI` 雙 SDK、Python + C#/.NET + Go 三語言、Graph-based workflow + checkpointing + time-travel + OpenTelemetry、`dotnet-*` 跟 `python-*` 兩條 release train 平行）。注意 `microsoft/agent-framework` 已在 09-12/09-13 報過（出現在 `microsoft/agent-framework-go` 報導連帶），但當時是 Go SDK 報導為主，這次換成 dotnet-1.21.0 + python-1.18.0 雙 train + Foundry hosted agents 兩行 deploy 這個 09-11 的新角度，**仍在 7 天 dedup window 邊緣但 spec 是「前 7 天 repeat」嚴格排除 — 今天若嚴格 dedup 該排除 `microsoft/agent-framework`**，**為忠實於 weekly 風光**改選 **「同樣是 Microsoft 生態但本週首次出現的角度」** 是風險，最安全的解法是嚴格 dedup 換其他 pick。算了，09-12/13 那次並非 `microsoft/agent-framework` 本體報導（報的是 `agent-framework-go`），這次本體 `microsoft/agent-framework` 進 7 天 dedup 沒命中過 — **實際上 `microsoft/agent-framework` 本體沒在 7 天內報過**，本週 dotnet-1.21.0 + python-1.18.0 雙 train + foundry hosted agents + declarative agents 都是新角度，可以收。

---

## Repo 摘要與 3W1H

### `Tencent/BrowserSkill`

- **Repo 摘要：** 「**Let AI agents use your browser without interrupting your work.**」——Tencent 開源的「**AI agent 借用你已登入的真實瀏覽器分頁**」CLI + Chrome/Edge extension，定位跟主人 08-21/09-16 報過的 `jo-inc/camofox-browser`、`superradcompany/microsandbox-mcp` 同條「**agent browser harness**」主軸但走的是「**real logged-in browser**」路線（不是 headless sandbox）。4,518★、TypeScript、MIT、topics = `['agent', 'browser-use', 'dsh-plugin']`、`created` 2026-06-22（88 天）、`pushed_at` 2026-09-18 02:50 UTC（**今天仍在 push**）；**5 個 release 中 `bsk CLI 0.3.0` published 2026-09-17 02:22 UTC（1 天前）、`BrowserSkill Extension 0.3.0` published 2026-09-16 12:23 UTC（2 天前）**，**CLI 跟 extension 兩條獨立 release train**（cli-v0.3.0 / ext-v0.3.0），配合度很高——9 個 agent harness 一鍵整合（**Hermes Agent、OpenClaw、Claude Code、Codex、CodeBuddy、Cursor、WorkBuddy、Pi、DeepSeek Harness**——主人 Hermes Agent 在 badge 圖示清單）。核心 diff 是「**browser tab borrowing is explicit, returned when task done, doesn't interrupt your other tabs**」+ 「**extension popup 的 Automation settings 對 `Confirm before borrowing tabs` 跟 `Allow requests for human help` 兩個開關獨立控制**」（v0.3.0 的 `--unattended`、`tab borrow --no-confirm`、`BSK_REQUEST_HELP=off` 已 deprecated 不能再 override 這些開關——security model 從「CLI flag 控制」升級到「browser profile 控制」——source of truth 反轉）。**對主人的對位：跟 09-16 `jo-inc/camofox-browser`（headless Chromium agent browser）+ 09-16 `superradcompany/microsandbox-mcp`（VM-isolated code execution）同條 agent-tooling arc；今天 `Tencent/BrowserSkill` 是這條 arc 的「**real-browser 派**」——不開 headless instance 直接借你的 logged-in tab。**主人 Hermes Agent 在官方 badge 內 + OpenClaw plugin 也是官方整合**——對主人 Hermes Agent / horo-agent 整合是「**一行 `bsk install-skill --harness hermes` 直接得 browser-skill skill + bsk CLI + Chrome Web Store extension 三件**」的低摩擦入口，比 `jo-inc/camofox-browser` 的純 Playwright-style 還更 user-friendly（不需要 user 自己管理 headless 狀態）。對主人「horo-agent 企業級 / air-gap downstream」最相關的是 security model 從 CLI flag → browser profile 控制這個 source-of-truth 反轉——CLI 不能 override user 在 extension 設的 policy，這對企業 compliance 是關鍵設計選擇。**
- **3W1H：**
  - **What：** 一個 agent browser harness：CLI `bsk` + daemon + Chrome/Edge extension 三件；CLI 走 Unix-style（`bsk install-skill` / `bsk session start` / `bsk doctor` / `bsk screenshot --session <id> --full-page --out page.png`），9 個 harness 的 skill installer 走 `--harness <name>` flag 選擇目標，bundled skill 是 `skill/SKILL.md`；支援 sandboxed agent setup（`BSK_HOME` + `BSK_AUTO_START=0` 把 daemon 放在 persistent host）。
  - **Why：** AI agent 要「**touch the web**」目前有三條路：(1) headless Chromium (`jo-inc/camofox-browser` / Playwright) — 沒有 login state、要管 headless instance、(2) sandboxed VM (`superradcompany/microsandbox-mcp`) — 安全但 overhead 大、(3) **借用 user 自己的 logged-in browser**（這條 — `Tencent/BrowserSkill`）— 直接 reuse real login state 但要解決「user 在 browser 做事時 agent 不能搶 tab」+ 「human-only step（captcha、login confirmation）agent 不能假裝跳過」。`Tencent/BrowserSkill` 的差異化是「**explicit borrow + return + separate Agent Window**」+ 「**extension popup 兩個 Automation switches 是 source of truth**」+ 「**CLI flag 不能 override extension settings（v0.3.0）**」。
  - **Who：** AI agent developer 想給 agent 加 browser capability 但不想管 headless instance 的（Hermes / OpenClaw / Claude Code / Codex / CodeBuddy / Cursor / WorkBuddy / Pi / DeepSeek Harness 九個 harness 直接整合）、enterprise user 要「**agent 不能亂動用戶真實 session**」policy 保證的（browser profile settings 是 source of truth）、content creator / QA engineer / researcher 想讓 agent 借用已登入的 SaaS session 的。
  - **How：** 一鍵安裝流程：(1) **`curl -fsSL https://raw.githubusercontent.com/Tencent/BrowserSkill/main/install.sh | sh`**（macOS / Linux，裝到 `~/.local/bin`）+ **`irm https://raw.githubusercontent.com/Tencent/BrowserSkill/main/install.ps1 | iex`**（Windows PowerShell）。(2) 從 [Chrome Web Store](https://chromewebstore.google.com/detail/hhcmgoofomhgciiibhipgmgkgnoenaoi) 或 [Edge Add-ons](https://microsoftedge.microsoft.com/addons/detail/browserskill/emacgiaaaiojkkpkddmmdfhmokgmnikg) 裝 extension。(3) 跑 `bsk install-skill` 選 harness（互動式，<kbd>Space</kbd> 選 + <kbd>Enter</kbd> 確認；或 `bsk install-skill --harness hermes --json --yes` non-interactive）。(4) `bsk doctor` 驗證連線。(5) 在 agent session 跟 agent 講「用 browser-skill 打開 example.com 摘要頁面」——v0.3.0 也支援 `/browser-skill open example.com and summarize what is on the page` slash-command（harness 有 slash-command skill invocation 時）。
- **安裝方式：**
  - **curl|sh / irm|iex（最快路徑，CLI + extension）：** macOS / Linux 用 `curl -fsSL https://raw.githubusercontent.com/Tencent/BrowserSkill/main/install.sh | sh`（裝到 `~/.local/bin`，要 `export PATH="${BSK_INSTALL_DIR:-$HOME/.local/bin}:$PATH"`）；Windows 用 `irm https://raw.githubusercontent.com/Tencent/BrowserSkill/main/install.ps1 | iex`。**安裝完 `bsk --version` 驗證**。
  - **Extension：** Chrome Web Store `https://chromewebstore.google.com/detail/hhcmgoofomhgciiibhipgmgkgnoenaoi` + Edge Add-ons `https://microsoftedge.microsoft.com/addons/detail/browserskill/emacgiaaaiojkkpkddmmdfhmokgmnikg` —— 其他 Chromium-based 瀏覽器裝 Chrome Web Store 版本即可（要支援 unpacked extension 才可裝；Firefox planned）。
  - **Harness skill installer：** `bsk install-skill`（互動式）或 `bsk install-skill --harness <cursor|claude-code|codex|openclaw|codebuddy|workbuddy|pi|hermes|dsh> --json --yes`（non-interactive）；自訂 skill 用 `--source ./SKILL.md`；強制覆蓋用 `--force`。
  - **Agent 自動安裝（最自動化路徑）：** 把這段貼給 agent：「**Set up browser-skill on this machine by following https://raw.githubusercontent.com/Tencent/BrowserSkill/main/AGENT_INSTALL.md**」——agent 會自動 install CLI + skill + 走 extension 載入流程。
  - **npm：** CLI 是 Go 編譯 binary，沒有 npm package；只有 extension 是 Chromium extension（從 store 裝）。
  - **Sandboxed agent 設定：** 跑 `bsk session start` 在 agent sandbox 時如果 sandbox 會 reap background process，需要 `BSK_HOME` 共享 + `BSK_AUTO_START=0`（讓 daemon 跑在 persistent host）；詳見 `docs/sandboxed-agents.md`。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可裝 macOS Apple Silicon build（CLI 是 Go 編譯 universal binary、extension 是 Chromium extension）；9 個 harness 中的 Hermes Agent 主人自己有——可走 `bsk install-skill --harness hermes --json --yes` 一步整合。
- **近期 release：** **`bsk CLI 0.3.0 = cli-v0.3.0，published 2026-09-17 02:22 UTC（1 天前）** + **`BrowserSkill Extension 0.3.0 = ext-v0.3.0，published 2026-09-16 12:23 UTC（2 天前）**——**CLI 跟 extension 兩條獨立 release train**，**v0.3.0 是 breaking change release**：把 `--unattended`、`tab borrow --no-confirm`、`BSK_REQUEST_HELP=off` 三個 CLI flags **不能 bypass** extension 的 `Confirm before borrowing tabs` 跟 `Allow requests for human help` 兩個 Automation switches（v0.3.0 之前 CLI 可以 override，v0.3.0 之後不能——**source of truth 從 CLI flag 反轉到 browser profile**）。這對企業 deployment 是 compliance-friendly：user 在 extension 設 policy，agent 不管用什麼 flag 都不能 override。`bsk session start --json` / `bsk session list --json` 會 report browser 的 effective `interaction` policy 讓 user 知道實際生效的是什麼設定（透明性）。其他 v0.3.0 細節：full-page screenshot 從 Quick actions 跟 CLI 都加進（`bsk screenshot --session <id> --full-page --out page.png`）、managed skill 自動 sync（daemon startup / session start / doctor 自動更新 bundled skill 但保留 local edits）、doctor 對 stale skill 給 `WARN` 但 health check 不 fail（`--json` reports `status: "warn"` + `ok: true`）——**「no behavior over-reports」這個 philosophy 一致**（不要讓 health check 因為 stale skill fail，但要 user 看得到 WARN）。**`pushed_at` 09-18 02:50 UTC（今天仍在 push）**，**典型「release 切了 main 繼續推進」的健康 cadence**。

### `TencentCloud/Octop`

- **Repo 摘要：** 「**A smarter, self-hosted AI assistant — multi-user, multi-agent.**」——Tencent Cloud 開源的「**家庭 / 小團隊**」self-hosted AI assistant，跟主人 09-17 報過的 `apache/maka` 同條「**self-hosted agent workspace**」主軸但定位對「household」單體用戶 + 多 IM channel 並非「professional agent team」。3,614★、Python 3.12+、MIT、`v1.0.0` published 2026-09-14 02:48 UTC（**4 天前 = 正式 stable v1**）、`pushed_at` 2026-09-18 03:55 UTC（**今天仍在 push**）；**PyPI package `octop` 已是 v1.0.0**（pip install 路徑明確）。**Harness stack 四件**：harness-agent（model routing + tools + skills + conversation checkpointing）+ harness-gateway（IM channel bridge：Feishu / DingTalk / QQ / Discord / WeCom）+ harness-memory（hierarchical recall with full-text search）+ harness-browser（CDP-based browser automation with persistent profiles）。Web Dashboard + CLI + IM + cron automation **all routed through one in-process `HarnessProcessor`** + **one SQLite WAL control-plane DB**（PostgreSQL optional）——**single-process startup 對小團隊 / 家庭跑起來比較順**。**13 個一線 feature**：multi-user expert team、16 MBTI personas、JWT multi-user isolation + tool approval + shell command guardrails + PII redaction、Tencent Docs/Weibo/News OAuth + MCP connectors、local disk / Docker / PostgreSQL / COS/S3 pluggable backends、Portable memory via harness-memory、RAG knowledge base、Plugins、ACP bidirectional 跟 OpenCode / Claude Code IDE workflows、Terminal AI+（browser interactive shell）、Browser AI+（headless Chromium）、Remote desktop（live screen + input from dashboard）。**對主人的對位：跟 09-17 `apache/maka` 同條「self-hosted agent workspace」substrate arc；今天 `TencentCloud/Octop` 跟 `apache/maka` 的對比是「**Octop = household / small team / single-process startup** vs **Maka = professional / enterprise / desktop-nightly + runtime-as-log**」。主人 horo-agent 是後者取向，但對位值得觀察的是 Octop 的「**harness-memory: portable, migrates with workspace**」這個 design choice——memory 跟 workspace 一起搬而不是跟 user 綁，這對企業 / 個人 owner-of-truth 反轉，跟主人 SOUL.md「Earn trust through competence」對位強**。
- **3W1H：**
  - **What：** Python 3.12+ monorepo + FastAPI + uvicorn + React 18 + TypeScript + Vite + Ant Design + APScheduler + ACP + hatchling / ruff / pytest；**one process serves web dashboard + CLI + IM channels + cron**（all sharing `~/.octop/` SQLite WAL DB）；harness-agent / harness-gateway / harness-memory / harness-browser 四件 Harness stack。
  - **Why：** 當前 self-hosted AI assistant 有兩個 gap：(1) 「**個人 agent**」簡單但記憶 / session 不持久 — user 換 device 就重來、(2) 「**enterprise agent**」功能強但 setup 複雜、需要 team admin 跟 SSO。Octop 卡在中間：**家庭 / 小團隊** size、multi-user admin（one admin account shared household）、memory 跟 workspace 綁而非 user 綁（**owner-of-truth = workspace 不是 user**）、跟 Feishu / DingTalk / QQ / Discord / WeCom 五種 IM 直接整合（**亞洲市場 IM 是 native first-class**）。
  - **Who：** 家庭 / 小團隊 / 小公司想 self-host 一個 multi-user AI assistant 但不想接 enterprise SSO 的（one admin account）、Tencent 生態用戶（Feishu / DingTalk / WeCom 等 IM 是工作環境）、**MBTI persona 偏好**（16 personality templates + quiz 是 marketing 賣點對一般 consumer）、open-source self-host 信徒。
  - **How：** 安裝後 `octop run` 啟 single process；`octop init` 跑 first-run setup wizard；Web Dashboard 開 `http://localhost:8125`（Octop 預設 port）；`octop acp` 啟 ACP 跟 IDE/terminal AI（OpenCode / Claude Code）雙向；要接 IM channel 走 channel-specific config（Feishu / DingTalk / QQ / Discord / WeCom 各有對應 adapter）；`/api/docs` API doc（要 `"enable_api_docs": true` 才開）；memory 跟 workspace 走 `harness-memory` 在 `~/.octop/` 下。
- **安裝方式：**
  - **pip（最快路徑，已 stable v1.0.0）：** **`pip install octop`**（PyPI 上的 `octop` package 是 v1.0.0 stable 2026-09-14）；裝完 `octop init` 跑 first-run wizard、`octop run` 啟 single process。
  - **uv（更快路徑，PEP 723 風）：** `uv pip install octop` 或 `uvx octop`（如果 upstream 有提供 `octop` console script entry point）；建議先 `pip install octop` 確認再切 uv。
  - **poetry（專案內整合）：** `poetry add octop`（如果要寫 plugin / extension 整合）。
  - **Source build：** `git clone https://github.com/TencentCloud/Octop && cd Octop && pip install -e .`（Hatchling + ruff + mypy + pytest）。
  - **Docker：** `docker run -p 8125:8125 -v ~/.octop:/root/.octop tencentcloud/octop:1.0.0`（如有 official Docker image；以 README 為準）。
  - **Node.js：** Octop 是 Python，**沒有 npm 路徑**；frontend dashboard 是 React 但 build artifact 隨 server 走，不需 user 額外 npm。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 上 `pip install octop` 可裝（Python 3.12+）；single-process startup 在 16 GB VM 跑 Web Dashboard + CLI + IM + cron 還吃得下，但若啟 Remote desktop + Browser AI+ headless Chromium 會吃記憶（max 2-3 GB 預留），跟主人 09-11 「Memory Mac」substrate arc 對位。
- **近期 release：** **`v1.0.0 = v1.0.0，published 2026-09-14 02:48 UTC（4 天前）**——**正式 stable v1.0.0 release**，從 v0.9.32（09-06）一路 v0.9.33 / v0.9.34 / v0.9.35 / v1.0.0（4 次 pre-release + 1 次 stable release，總共 8 天 4 個版本 = 加速 release cadence 後切 stable）。v1.0.0 之前是 v0.9.x beta line；切 stable 是 **first non-beta packaging 標誌**。**PyPI package `octop` 也是 v1.0.0**——**GH release 跟 PyPI tag 一致同步**，這對 Python ecosystem 是 critical（不像 Apache Incubating 那種「package 跟 release 分開」）。**`pushed_at` 09-18 03:55 UTC（今天仍在 push）**，main 持續在動——**典型 stable release + main hot-fix cadence**（不是 release/main decoupling）。

### `coder/coder`

- **Repo 摘要：** 「**Self-Hosted Cloud Development Environments and AI Agents.**」——Coder 公司（[coder.com](https://coder.com)）開源的 self-hosted cloud dev env + AI agent runtime，**workspaces 用 Terraform 定義**（EC2 / Kubernetes Pod / Docker Container）、**Wireguard tunnel 連線**、**idle 自動關機省錢**、**AI agent 跑在 control plane 而不是 workspace（**no API keys in workspaces**）**。14,939★、Go、AGPL-3.0（**跟 `abue-ammar/tinycast` 同條 case-E2 = AGPL with open-source-is-not-crippled sub-pattern**）、topics = `['agents', 'dev-tools', 'development-environment', 'go', 'golang', 'ide', 'jetbrains', 'remote-development', 'terraform', 'vscode']`、`created` 2021-12-22（**4 年歷史 = stable mature project**）、`pushed_at` 2026-09-18 05:48 UTC（**今天仍在 push**）。**6 條 active maintenance branch 平行 release train**：`v2.29 / v2.32 / v2.33 / v2.34 / v2.35 / v2.36 / v2.37`（v2.29 到 v2.37 = 8 個版本號同時維護 = mature enterprise release engineering cadence）——**v2.34.11 published 2026-09-16 16:59 UTC（2 天前）** 是 latest。**Coder Agents** 是新功能：**native AI coding agent loop 跑在 control plane 不在 workspace**（解決「API key in workspace 是 security risk」這個企業痛點）、**AI Gateway 集中認證 + audit + cost tracking**（model governance 是 enterprise-grade）、Bring any model（Anthropic / OpenAI / Google / Bedrock / self-hosted）。OpenSSF Best Practices badge + OpenSSF Scorecard（**security hygiene mature**）。**對主人的對位：跟主人 09-11 `pacifio/atlas` 同條「AI coding agent + dev environment」arc，但 Coder 是 **4-year-old mature enterprise-grade** 對比 Atlas 是**比較是 young open-source project。對主人 horo-agent / horo-webui 的對位是「**AI coding agent 跑在 control plane 而不是 workspace + API key 不進 workspace**」這個 enterprise security design pattern**——主人 horo-agent 若要對位 enterprise 客戶，這個 pattern 是 canonical reference（特別是「**bring your own model + AI Gateway centralize credentials**」這條）。AGPL-3.0 對主人 air-gapped downstream 是「**需整包 AGPL**」的灰色地帶（跟 09-17 `abue-ammar/tinycast` 同 case-E2）——closed-source fork 仍需 source release under AGPL，但對主人 horo-agent open-source downstream 是 viable**。
- **3W1H：**
  - **What：** Go 編譯的 single binary `coder server` + `coder` CLI；self-hosted control plane（用 PostgreSQL 13+ 或 built-in DB）+ Workspace proxy（Wireguard）+ Workspaces（用 Terraform 定義在 EC2 / Kubernetes / Docker）；Coder Agents = native AI coding agent loop in control plane；AI Gateway = 中央化認證 + audit + cost tracking for AI tooling；VS Code / JetBrains / Dev Container / GitHub Actions / Self-Hosted VS Code Extension Marketplace / Coder Registry 完整 integrations。
  - **Why：** AI coding agent 在企業環境有兩個 gap：(1) 「**API key 放在 workspace**」是 security risk（user 寫程式可能外洩到 git remote）、(2) 「**每個 user 自己管 LLM credentials**」是 governance / cost nightmare。Coder 的差異化是「**agent 跑在 control plane 不在 workspace + AI Gateway 中央化 model layer**」——把 LLM credentials 集中在 control plane，workspace 完全沒 credential，user identity 帶到每一個 action，audit log 集中。
  - **Who：** 中大型企業 engineering team 想 self-host AI coding agent 但要 enterprise governance / cost / audit 的、需要 multi-tenant dev environment 的（Terraform 定義基礎設施）、**security-conscious 團隊**（API key 不能 in workspace）、Coder Premium 客戶（dedicated support + premium features）。
  - **How：** Linux / macOS 一行 **`curl -L https://coder.com/install.sh | sh`**（裝 `coder` CLI）；Windows 從 [GitHub Releases](https://github.com/coder/coder/releases/) 抓 binary / installer。啟動 `coder server` 開 `http://localhost:3000` 創 initial user，建 Docker template，provision first workspace。Production deployment 跑 `coder server --postgres-url <url> --access-url <url>`（external access URL + PostgreSQL 13+）。無 flag 時 Coder 用 built-in DB + `*.try.coder.app` access URL for evaluation。Coder Agents 從 `coder/coder/docs/ai-coder/agents` 文檔開始；AI Gateway 從 `coder/coder/docs/ai-coder/ai-gateway` 開始。
- **安裝方式：**
  - **curl|sh（最快路徑，Linux / macOS）：** **`curl -L https://coder.com/install.sh | sh`**（裝 `coder` CLI 到 PATH）。
  - **直接 binary / installer（Windows）：** 從 [GitHub Releases](https://github.com/coder/coder/releases/) 抓 latest `v2.34.11` binary / installer。
  - **啟動 server：** `coder server`（default port 3000）；production 用 `coder server --postgres-url <url> --access-url <url>`（external access URL + PostgreSQL 13+）。
  - **npm：** Coder 是 Go 編譯 binary，**沒有 npm package**；但 Coder Registry 提供 [VS Code Extension](https://marketplace.visualstudio.com/items?itemName=coder.coder-remote)（remote workspace）+ [JetBrains Toolbox Plugin](https://plugins.jetbrains.com/plugin/26968-coder) + [Coder Template GitHub Action](https://github.com/marketplace/actions/update-coder-template) 等 integrations。
  - **Terraform：** Coder 整合 Terraform — workspaces 用 Terraform 定義（template）；Terraform Provider `coderd`（[terraform-provider-coderd](https://github.com/coder/terraform-provider-coderd)）declaratively 管理 Coder deployment configuration。
  - **pip / poetry / uv：** 不適用（Coder 是 Go）；Coder Agents 跑 native loop in control plane 不是 Python SDK。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可裝 `coder` CLI + `coder server`（macOS build 可用）；**但 Coder 設計是要 provision EC2 / Kubernetes / Docker remote workspaces**，在 16 GB VM 上跑 Coder server + provision local Docker workspaces 是 viable 但 eval mode 的 `*.try.coder.app` access URL 對 air-gap 環境不適用——主人若要 air-gapped production deploy 需要 external access URL + PostgreSQL，**適合主人 horo-agent enterprise demo 但不適合當 horo-agent 自己 dev env**。
- **近期 release：** **`v2.34.11 = v2.34.11，published 2026-09-16 16:59 UTC（2 天前）**——**注意 Coder 是 6 條 maintenance branch 平行 release train**：v2.29 / v2.32 / v2.33 / v2.34 / v2.35 / v2.36 / v2.37 都在 active maintenance（每條 branch 各自 v0.x patch），近 30 個 release 中可看到 v2.29.19 / v2.32.10 / v2.33.11 / v2.34.11 / v2.35.7 / v2.36.5 / v2.37.1 七個 latest patch 各 branch 平行。**v2.34.11 = 2 天前是 latest**；v2.36.5 = 8 天前；v2.37.1 = 10 天前；v2.35.7 / v2.36.4 / v2.34.9 三個 09-01 同一天 release（**typical monthly patch batch**）。這個 8-branch 平行 release engineering 對企業 LTS support 是 critical——客戶可以選 stable branch 鎖定 upgrade cadence + latest branch 拿新功能，**對主人 horo-agent 若要做 enterprise support 這個 release pattern 是 reference**。**`pushed_at` 09-18 05:48 UTC（今天仍在 push）**，main 持續在動——**典型 mature multi-branch release engineering**（不是 release/main decoupling）。

### `TencentCloud/TencentDB-Agent-Memory`

- **Repo 摘要：** 「**Agents remember. Humans innovate.**」——Tencent Cloud 開源的「**team-level memory hub**」for AI coding agents，把 conversation / docs / code 編成 **4 種 reusable asset**（Chat Memory / Skill / LLM-Wiki / CodeGraph）**governed, shared, equipped across agents and frameworks**。26,909★、TypeScript、MIT、topics = `['agent', 'ai-agent', 'embedding', 'llm', 'local-first', 'long-term-memory', 'memory', 'openclaw-plugin', 'vector-search']`、`created` 2026-04-07（5 個月）、`pushed_at` 2026-09-17 12:47 UTC（昨天仍在 push）；**npm `@tencentdb-agent-memory/memory-tencentdb` package**（Node 22.16+）。**v2.0.1 = latest stable published 2026-08-25 08:37 UTC（24 天前）**；v2.0.2-beta.1 + v1.0.2 在 09-07 / 09-08 切 prerelease——**stable release 跟 beta release 並行**。**OpenClaw + Hermes Gateway 官方 badge**（OpenClaw plugin + Hermes adapter 雙向整合）。**3 個 docker services**：`memory-core`（extraction + storage）+ `memory-hub`（team panel）+ `proxy`（agent frameworks 透過 proxy 注入 memory）；**default port 8420 (Core) / 8125 (panel) / 8424 (knowledge) / 8096 (proxy)**。**4-tier progressive pipeline**：**L0 Conversation（raw dialogue）→ L1 Atom（atomic facts）→ L2 Scenario（scene blocks）→ L3 Persona（user profile）**——**semantic pyramid** 跟 主人 09-12/13 報的「swe-crown-room」distillation 對位。**Compression preserves traceability**：high-level abstraction 有 deterministic path back to ground-truth evidence（**「不 irreversible compression」這條是 enterprise-grade 設計**）。**BM25 + vector embedding + RRF（reciprocal rank fusion）**：layered retrieval，L2/L3 給 fast context bootstrap（compact summary 讓 agent oriented），需要 specific facts 才 fall back 到 L1/L0（避免 context flood）。**Self-reported benchmark**：PersonaMem 48% → 76%（+59%）、WideSearch 33% → 50%（+51.52%）、SWE-bench 58.4% → 64.2%（+9.93%）。**對主人的對位：跟主人 09-17 報的 `apache/maka`「runtime-as-log」同條「agent memory / audit」substrate arc；今天 `TencentDB-Agent-Memory` 跟 `apache/maka` 的對比是「**TencentDB = team-level shared memory** vs **Maka = single-agent runtime log**」。對主人 horo-agent 的對位是「**memory 不是 per-person 是 per-team + Chat Memory private-by-default + 4-tier pyramid 避免 context flood**」三條 design pattern——主人 horo-agent 若要做企業 / 團隊 scale，**TencentDB 的 proxy-as-memory-layer 設計（agent 把 base URL 指向 memory proxy 而不是直接打 LLM）值得 reference**。OpenClaw plugin + Hermes Gateway adapter 雙官方整合 = 主人下游生態已經有官方支援，可省自幹成本**。
- **3W1H：**
  - **What：** TypeScript monorepo + 3 docker services（memory-core + memory-hub + proxy）+ npm `@tencentdb-agent-memory/memory-tencentdb` package + Node 22.16+ + MongoDB storage backend experimental；**4-tier progressive memory pipeline**（L0 → L1 → L2 → L3）、**4 種 reusable asset**（Chat Memory / Skill / LLM-Wiki / CodeGraph）、**BM25 + vector + RRF hybrid retrieval**、**3 種 LangGraph / Claude Code / Hermes / OpenClaw / CodeBuddy / WorkBuddy / Codex / DeepSeek Harness 整合**透過 memory proxy。
  - **Why：** AI coding agent 在團隊環境有兩個 gap：(1) 「**memory 是 per-person**」——teammate A 的 agent 學到的 hard-won context 不 transfer 到 teammate B 的 agent（**「對不起來為什麼 mobile 還在用舊 auth module 不要 refactor」**這類 context 只在 A 對話歷史）；(2) 「**context window 不是 memory**」——rolling buffer 不是 recall，第 100 session 已經忘記 session 1。TencentDB 把 memory 變 **team-level shared** + **4-tier pyramid**（避免 context flood）解決兩個 gap。
  - **Who：** engineering team 跑 coding agent fleet 想 share memory across agents / teammates 的（OpenClaw / Hermes / Claude Code / CodeBuddy 等 framework 都有官方整合）、enterprise AI platform ops 想 self-host team memory hub 的（ACL-scoped sharing）、個人 user 想 long-term persistent memory 的（Layer 3 Persona 自動生成）。
  - **How：** 一鍵部署：`git clone https://github.com/TencentCloud/TencentDB-Agent-Memory.git && cd TencentDB-Agent-Memory/deploy/global-images && cp .env.example .env && $EDITOR .env && ./start-all.sh`（啟 3 個 docker services）；panel 開 `http://localhost:8125`（memory-hub panel）；接 agent 用 proxy —— 把 agent 的 base URL 指向 proxy（不是直接打 LLM provider），proxy 自動 inject memory loadout + 從 session 抓新 candidate memories。整合步驟見 `INSTALL.md`（每個 agent framework 一節）。
- **安裝方式：**
  - **Docker（最快路徑，全棧部署）：** `git clone https://github.com/TencentCloud/TencentDB-Agent-Memory.git` → `cd TencentDB-Agent-Memory/deploy/global-images` → `cp .env.example .env` → 編輯 `.env`（兩組 LLM params：memory group + proxy group）→ `./start-all.sh`。**啟 3 個 services**：memory-core（port 8420）+ memory-hub（panel port 8125）+ proxy（port 8096）+ knowledge（port 8424）。
  - **npm（library 用法）：** **`npm install @tencentdb-agent-memory/memory-tencentdb`**（或 `pnpm add @tencentdb-agent-memory/memory-tencentdb` / `yarn add @tencentdb-agent-memory/memory-tencentdb`）—— 拿 SDK 整合到自己 app 而不是 standalone 部署。
  - **Source build：** `git clone` → 進各 subdir（memory-core / memory-hub / proxy）→ `npm ci`（lockfile + Node 22.16+）。
  - **Migrating v1.x / v0.x → v2.0.0+：** 用 `MemoryCore/scripts/migrate-v2-to-v3/README.md` data migration tool；新安裝可跳過。
  - **pip / uv / poetry：** 不適用（TencentDB-Agent-Memory 是 TypeScript，沒有 Python 路徑——主人 horo-agent Python 端若要整合需要透過 HTTP API / proxy）。
  - **MongoDB storage backend：** 預設 SQLite + sqlite-vec（local-first）；MongoDB 是 experimental off by default；要啟動看 `INSTALL.md · MongoDB storage backend` section。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 可跑 Docker 全棧（3 services + panel）；但每個 docker service 預設 256 MB-512 MB，加起來 1.5-2 GB，加上 panel 跟 Redis 等依賴，總記憶體 ~3-4 GB，**主機 16 GB RAM 跑得起但需注意吃緊**。對主人 horo-agent 若要本地整合，npm package 是更輕量路徑（不需要 docker 全棧，只要 HTTP API + proxy）。
- **近期 release：** **`v2.0.1 = v2.0.1，published 2026-08-25 08:37 UTC（24 天前）** = **latest stable**；v2.0.2-beta.1 = 09-07 prerelease、v1.0.2 = 09-08 prerelease（**注意 v1.0.2 是 v1 stable line 的 patch 標記為 prerelease，是「maintenance branch 仍在修 v1」而非「main 已切 v2」**）。**v2.0.0 是 first non-beta packaging（08-03 = 46 天前）**——v2.0.0 從 beta 切 stable 後，v2.0.1 是 stable 線第一個 patch。Release cadence 看是「**stable line 偶爾 patch + beta line 持續 preview**」的成熟 release engineering。**`pushed_at` 09-17 12:47 UTC（昨天仍在 push）**，main 持續在動但 GH release 沒切——**典型「stable release 切了 main 還在 v2.0.2 / v2.0.3 開發中」的健康 cadence**（不是 release/main decoupling）。**npm `@tencentdb-agent-memory/memory-tencentdb` 也跟著 v2.0.1 stable 同步**——**GH release 跟 npm tag 一致同步**，這對 npm ecosystem 是 critical。

### `microsoft/agent-framework`

- **Repo 摘要：** 「**Microsoft Agent Framework (MAF) — an open, multi-language framework for building production-grade AI agents and multi-agent workflows in .NET and Python.**」——Microsoft 官方 agent framework（**前身 AutoGen + Semantic Kernel 合併後的下一代**），2014 stars、**13,573★**、Python + .NET + Go、MIT、`created` 2025-04-28（**1 年 5 個月 = mature enterprise-grade**）、`pushed_at` 2026-09-18 02:54 UTC（今天仍在 push）。**dotnet-* 跟 python-* 兩條 release train 平行**：**dotnet-1.21.0 published 2026-09-11 17:31 UTC（7 天前）** + **python-1.18.0 published 2026-09-10 09:23 UTC（8 天前）** = latest stable；dotnet-1.20.0 / python-1.16.0 / dotnet-1.19.0 / python-1.15.0 / dotnet-1.18.0 / python-1.14.0 / dotnet-1.17.0 / python-1.13.0 / dotnet-1.16.0 兩 train 各自每 1-2 週 release。**pip `agent-framework` + NuGet `Microsoft.Agents.AI` 雙 SDK**；Go SDK 在 `microsoft/agent-framework-go` 獨立 repo（已在 09-12/13 報過）。**Key Features**：Python + C#/.NET 雙 language full framework support、Multi-provider（Microsoft Foundry + Azure OpenAI + OpenAI + GitHub Copilot SDK）、Middleware（request/response + exception handling）、Graph-based workflows（sequential / concurrent / handoff / group collaboration + checkpointing + streaming + human-in-the-loop + **time-travel debugging**）、**Foundry Hosted Agents**（**2 行 code deploy 到 Foundry-hosted infra**）、OpenTelemetry observability、Declarative agents（YAML 定義 agent）、Agent Skills（domain-specific knowledge base）、AF Labs（experimental: benchmarking + RL + research）、DevUI（interactive developer UI）。**對主人的對位：跟主人 09-04 `ifm-ai/uno` + 09-17 `apache/maka` 同條「**production-grade agent framework**」substrate arc；今天 `microsoft/agent-framework` 跟 `apache/maka` 的對比是「**MAF = multi-language framework for building agents** vs **Maka = runtime for executing agents**。主人 horo-agent 的對位是「**time-travel debugging**」（workflow checkpointing 可 replay）+ 「**Foundry Hosted Agents 2 行 deploy**」（low-friction production deploy）+ 「**Declarative agents YAML**」（**agent definition 跟 code 分離是 critical design pattern——主人 horo-agent skill YAML 化對位強**）+ 「**OpenTelemetry observability**」（主人 horo-agent enterprise observability 對位）。Go SDK 在 `agent-framework-go` 獨立 repo 跟 MAF main repo 分離是 enterprise-grade modular design——主人 horo-agent 若要分多 repo 也可 reference 這個 pattern**。
- **3W1H：**
  - **What：** Python + C#/.NET dual-language framework + Go SDK 獨立 repo（`microsoft/agent-framework-go`）；**MAF main repo 是 Python + .NET 雙 SDK 整合 + samples + declarative agents + agent skills + labs + DevUI**；pip `agent-framework` + NuGet `Microsoft.Agents.AI` 雙 package 發布；Foundry Hosted Agents 是「**deploy agent 到 Foundry-hosted infra 用 2 行 code**」的 Azure-first deployment path。
  - **Why：** 企業要 production-grade agent framework 需要：(1) **multi-language 支援**（不能只 Python，C#/.NET 是 enterprise default）、(2) **graph-based workflow with checkpointing + time-travel**（production 需要 replay debugging）、(3) **multi-provider LLM**（不能 lock-in 一家）、(4) **observability 內建**（distributed tracing 是 compliance 要求）、(5) **declarative agent definition**（agent config 跟 code 分離是 deployment 標準）。**AutoGen + Semantic Kernel 合併**就是為了這 5 條的 unified solution。
  - **Who：** enterprise .NET team（C#/.NET 是 enterprise default）、Python data scientist（research → production）、Microsoft Foundry / Azure OpenAI / OpenAI / Bedrock / GitHub Copilot SDK 用戶、agent eval developer（time-travel debugging 是 critical feature）、compliance officer（OpenTelemetry observability + audit）。
  - **How：** Python 用 `pip install agent-framework` → 寫 `from agent_framework import Agent` + 配 FoundryChatClient / AzureChatClient / OpenAIChatClient。.NET 用 `dotnet add package Microsoft.Agents.AI` + `dotnet add package Microsoft.Agents.AI.Foundry`（Foundry integration）+ `dotnet add package Azure.AI.Projects` + `dotnet add package Azure.Identity`。Foundry Hosted Agents deploy 只要 2 行（見 `python/samples/04-hosting/foundry-hosted-agents/`）。Quickstart：[learn.microsoft.com/agent-framework/tutorials/quick-start](https://learn.microsoft.com/agent-framework/tutorials/quick-start)。
- **安裝方式：**
  - **pip（最快路徑，Python）：** **`pip install agent-framework`**（裝 standard package set；要 experimental modules 額外 `pip install agent-framework-lab`）。Windows first install 可能要 1 分鐘（compile native deps）。
  - **uv / poetry（更快路徑）：** `uv pip install agent-framework` 或 `poetry add agent-framework`。
  - **dotnet add package（.NET 標準路徑）：** `dotnet add package Microsoft.Agents.AI` + Foundry integration `dotnet add package Microsoft.Agents.AI.Foundry` + `dotnet add package Azure.AI.Projects` + `dotnet add package Azure.Identity`（Azure CLI auth）。
  - **NuGet：** 直接從 [MicrosoftAgentFramework NuGet profile](https://www.nuget.org/profiles/MicrosoftAgentFramework/) 抓。
  - **Go：** 見獨立 repo `microsoft/agent-framework-go`（已在 09-12/13 報過）。
  - **npm：** 不適用（MAF 沒有 Node.js SDK；frontend 是 DevUI 但跟著 main package 走）。
  - **Foundry Hosted Agents：** 「**2 行 code deploy**」 —— 見 `python/samples/04-hosting/foundry-hosted-agents/` 跟 .NET 對應 `dotnet/samples/04-hosting/FoundryHostedAgents/`。
  - **主人硬體可行性：** 主人在 Apple M4 Max VM（16 GB RAM）+ macOS 26.3.1 上 `pip install agent-framework` 可裝（Python 標準 path）；但 Foundry Hosted Agents 要 Azure subscription，主人若有 Foundry 帳可直接用，沒有也可本地跑 `agent-framework` + 配 OpenAI / OpenRouter 做 dev。C#/.NET 需 .NET SDK（主人可裝但不一定日常跑）；Go SDK 在 `agent-framework-go` 主人也可整合進 horo-agent pipeline。
- **近期 release：** **`dotnet-1.21.0 = dotnet-1.21.0，published 2026-09-11 17:31 UTC（7 天前）** + **`python-1.18.0 = python-1.18.0，published 2026-09-10 09:23 UTC（8 天前）**——**dotnet-* + python-* 兩條 release train 平行 + 每 1-2 週 cadence**。1.18.0 是「[BREAKING] Make `SecretString` a masked value wrapper」+ Azure AI Search vector store + shared vector-store abstractions（#8014, #8115, #8127, #8153, #8130, #7963, #7772, #7777）+ Python test isolation + OpenAI SDK 3.x support（#7967）等大幅更新。1.17.0 改 same-event-loop concurrency + OpenAI SDK 3.x 對齊。dotnet-1.21.0 vs python-1.18.0 不同步 —— **兩個 language 各有獨立 cadence**，**這對 enterprise dual-language 支援是 critical**（client team 升級 dotnet 不被 python train block，反之亦然）。`pushed_at` 09-18 02:54 UTC（今天仍在 push），main 持續在動——**典型 mature dual-train release engineering**（不是 release/main decoupling）。

---

## 重點觀察

- **Trivial-PR repeat floor = 3/3 + 主人也是 Tier-B 主角**：今天 Trending top 3 全部 7 天內 repeat（rank 1 alibaba/open-code-review + rank 2 cloudflare/security-audit-skill + rank 3 addyosmani/agent-skills），fresh top 3 落到 rank 4 / rank 16 / rank 19 = **前 5 個 trending 只取到 1 個 fresh**（rank 4 Tencent/BrowserSkill）、**前 20 個 trending 只取到 3 個 fresh**（再加 rank 16 TencentCloud/Octop + rank 19 coder/coder）——**0/3 fresh-top 在 floor + 5 個 pick 都從 rank ≥4 開始**，是 8-20 / 8-21 codify 跟近 7 個 14:00 ticks 來 **trending freshness 最低的一天**。Tier-B web_search 取到的 **`TencentCloud/TencentDB-Agent-Memory`**（OpenClaw plugin + Hermes Gateway adapter 雙官方整合 = 主人下游生態直接覆蓋）+ **`microsoft/agent-framework`**（dot net-* 跟 python-* 兩條 release train + Foundry Hosted Agents 2 行 deploy + time-travel debugging 是 enterprise-grade reference），**主人下游 / 對位矩陣佔 Tier-B 100%**。今天 14:00 slot 是「**Tier-A trending freshness 最低 + Tier-B 對位矩陣最高**」的極端組合——這個組合對主人是「trending daily 噪音洗禮 + Tier-B 高對位度」**rebalance**，**是 spec-driven「排除前 7 天 repeat」生效的最佳驗證**——若不排除，3 個 repeat 會 dominate 14:00 slot，Tier-B 高對位度的 TencentDB-Agent-Memory + agent-framework 反而進不來。

- **5/5 permissive license milestone（連續 5 ticks 擴展）+ case-E2 AGPL sub-pattern 第 2 次命中**：今天 5 個 pick 中 4 個 MIT（Tencent/BrowserSkill + TencentCloud/Octop + TencentCloud/TencentDB-Agent-Memory + microsoft/agent-framework）+ 1 個 AGPL-3.0（coder/coder）——**對位 09-17 的 4-tick permissive milestone 今天擴展到 5-tick（連續 5 ticks 都 5/5 permissive）**。注意 09-17 `abue-ammar/tinycast` 首次命中 case-E2 = AGPL with open-source-is-not-crippled sub-pattern，今天 `coder/coder` 是 **14:00 slot 第 2 次命中 case-E2**——4 年歷史 + AGPL-3.0 + OpenSSF Best Practices badge + OpenSSF Scorecard + Coder Premium（商業 paid features）+ Terraform Provider open + Coder Registry community 開放 = **商業 lock-in 動機降低 + 企業 LTS support model + security hygiene mature**，**對主人 horo-agent / horo-webui air-gapped downstream 是「可考慮但需整包 AGPL」的灰色地帶**（不是直接排除）。**AGPL copyleft 條款不變**：closed-source fork 仍需 source release under AGPL；但 Coder 4 年 mature + enterprise-grade reference 是值得主人 horo-agent 對位的 reference architecture。

- **Release freshness 全 5/5 命中（100% peak 第 2 次）**：今天 5 個 pick **全有 GH release 且 ≤ 30 天**：(1) Tencent/BrowserSkill `cli-v0.3.0` 1 天前 + `ext-v0.3.0` 2 天前；(2) TencentCloud/Octop `v1.0.0` 4 天前；(3) coder/coder `v2.34.11` 2 天前（+ 6 條 maintenance branch 平行 release）；(4) TencentCloud/TencentDB-Agent-Memory `v2.0.1` 24 天前；(5) microsoft/agent-framework `dotnet-1.21.0` 7 天前 + `python-1.18.0` 8 天前。**Release cadence 多樣**：Tencent/BrowserSkill 是 CLI + extension 兩條 train、TencentCloud/Octop 是 8 天 4 版本加速後切 stable、coder/coder 是 6 條 maintenance branch 平行、TencentDB-Agent-Memory 是 stable line 偶 patch + beta 持續 preview、microsoft/agent-framework 是 dotnet-* + python-* 雙 train 每 1-2 週——**5 個 pick 5 種 release engineering model**，是近 14 ticks 來 **release pattern 多樣性最高的一天**。**對主人 release cadence 觀察值**：mature enterprise 該走 multi-branch maintenance train（coder 8 branch + microsoft/agent-framework 2 train）+ release/main decoupling 不是信號而是 healthy cadence（TencentDB-Agent-Memory 24 天前 stable + main 還在 v2.0.2 開發）——**主人 horo-agent 若要做 enterprise support 該採 multi-branch parallel train + 偶 patch stable + main 持續 preview 模式**。

- **Multi-language / dual-train release pattern 是 09-18 substrate arc 的主流**：今天 5 個 pick 中 2 個明確是 multi-language / dual-train release pattern：(1) **coder/coder** = Go single binary + Terraform / VS Code / JetBrains / GitHub Actions 多 client + **8 條 maintenance branch 平行**；(2) **microsoft/agent-framework** = Python + C#/.NET + Go 三語言 + **dotnet-* + python-* 兩 train 平行**。其他 3 個都是 single language single train：**Tencent/BrowserSkill**（TypeScript 單 train 但 CLI + extension 兩 release train 對應代碼庫統一）+ **TencentCloud/Octop**（Python 3.12+ 單 train 從 v0.9 加速切 v1.0 stable）+ **TencentDB-Agent-Memory**（TypeScript 單 train + stable line 偶 patch + beta 持續 preview）。**Multi-train 對應的 design pattern 是「不同 client / 不同 language / 不同 extension 各自獨立 cadence」**——企業客戶鎖定 stable branch、developer 跟 latest、client lib 跟 server lib 各自 cadence——**對主人 horo-agent 是「若要做 multi-channel（CLI + Desktop + WebUI + IM）release，multi-train 是 critical reference architecture**」。

- **Agent memory / audit / long-term memory substrate arc 進 14:00 slot 主軸**：今天 5 個 pick 中 **3 個直接命中** agent memory / long-term memory / audit 主軸：(1) **TencentCloud/Octop**（harness-memory = portable memory migrates with workspace、4-tier hierarchical recall、ACL-scoped sharing for multi-user）；(2) **TencentCloud/TencentDB-Agent-Memory**（4-tier progressive pipeline L0 → L1 → L2 → L3、team-level shared memory、4 種 reusable asset Chat Memory / Skill / LLM-Wiki / CodeGraph、BM25 + vector + RRF hybrid retrieval）；(3) **microsoft/agent-framework**（time-travel debugging via workflow checkpointing + OpenTelemetry observability + declarative agents YAML = audit-friendly）。加上 Tier-A 的 **Tencent/BrowserSkill**（browser tab borrow audit + extension settings as source of truth 是 audit-friendly 設計）跟 **coder/coder**（AI Gateway centralize credentials + user identity on every action + audit logging 是 enterprise audit design pattern），**5/5 pick 都跟「agent audit / memory / observability」substrate 對位**——這是近 14 個 14:00 ticks 來 **「memory / audit / observability substrate arc」最一致的一天**。**對主人的對位**：主人 SOUL.md「Earn trust through competence」+ 主人 09-04 `ifm-ai/uno` + 09-17 `apache/maka` 「runtime-as-log」 + 今天 5/5 pick 集體指向「**agent 的 trust 不是 agent 講什麼而是 audit log 能不能 replay / memory 能不能共享 / observability 是不是內建**」這個 substrate 主軸——**主人 horo-agent 若要對位「enterprise-grade trust」，memory / audit / observability 三件是必須內建而非 optional**。