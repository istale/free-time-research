# GitHub 專案動態
- 檢查時間：2026-08-24（14:00 台北時間）
- 檢查對象：openai/codex / freestylefly/awesome-gpt-image-2 / ruvnet/ruflo / deepseek-ai/deepseek-harness / agentregistry-dev/agentregistry
- 來源組合：GitHub Trending today（Tier-A within-Top-10 elevation path 5 — 5th verified use — Tier-A top 3 = openai/codex (fresh) + freestylefly/awesome-gpt-image-2 (fresh) + mattpocock/skills (REPEAT 08-22 #1 + 08-21 #2，5-day window repeat 觸發 forced intervention，因此改讀 positions 4-12 找 fresh 候選 → 11 名 ruvnet/ruflo elevated）+ web_search constrained search（Tier-B 2：deepseek-ai/deepseek-harness via dsh 8/14 升 v0.1.1 RC2 + agentregistry-dev/agentregistry via Agent Plugins 1.0 標準化 registry）

## 動態摘要

### openai/codex
- 類型：release / trending（Tier-A #1 — Rust coding agent、OpenAI 官方、**trending #1 是 2026 年首次由 `openai/*` 命名空間拿下 daily top**）
- 內容：trending top 1（fresh）；最新 release `rust-v0.149.0`「Codex 0.149.0」主 release（2026-08-20 21:04 UTC，台北時間 8/21 05:04） + 同步 hotfix `rust-v0.149.1`（**2026-08-24 00:28 UTC，台北時間今天 08:28，**當日 release**）+ alpha `rust-v0.149.0-alpha.4.3` (8/23 23:29 UTC) + `rust-v0.150.0-alpha.7` (8/22 21:26 UTC) — **4 個 release / 4 天內，release velocity 接近 daily cut**；115,989★ 累計、Apache-2.0、Rust 主 binary（567MB source）。
- 連結：https://github.com/openai/codex/releases/tag/rust-v0.149.0

### freestylefly/awesome-gpt-image-2
- 類型：trending（Tier-A #2 — awesome-list, 8/24 新進榜；無 GitHub release — 採 commit-rolling）
- 內容：trending top 2（fresh，8/24 新進 top 3）；**未找到 GitHub release**（採 commit-rolling 維護 prompt 庫）；13,739★ 累計、MIT、JavaScript。description 寫「Prompt as Code | GPT-Image2 工业级提示词引擎与模板库，500+ 案例逆向工程，20+ 套工业级模板，並提煉出 Skills」。topics 含 `agents` / `ai-image-generation` / `chatgpt` / `dsh-plugin` / `gpt-image-2` / `image-prompts` / `prompt-as-code` / `prompt-engineering` / `skills` / `workflow-automation`。
- 連結：https://github.com/freestylefly/awesome-gpt-image-2

### ruvnet/ruflo
- 類型：release / trending（Tier-A within-Top-10 elevation path 5 — 5th verified use — HTML position #11，trending 5-day fresh）
- 內容：trending top 11（fresh，8/24 新進榜）；最新 release `v3.38.19`「v3.38.19 — supersedes broken v3.38.17/v3.38.18 (Windows CI, dead agentdb exports, memory driver doctor check, MCP HTTP transport)」（**2026-08-22 16:39 UTC，台北時間 8/23 00:39 — 36 小時內**）；69,155★ 累計、MIT、TypeScript。Description 寫「🌊 The original agent meta-harness. Deploy intelligent multi-player swarms, coordinate autonomous workflows, and build conversational AI systems. Features adaptive memory, self-learning intelligence, RAG integration, and native Claude Code / Codex / Hermes and many more Integrated」 — **MEMORY-direct hit, README 內 explicit Hermes mention**。前身為 `ruvnet/claude-flow`，本週 rebrand 成 `ruflo`。
- 連結：https://github.com/ruvnet/ruflo/releases/tag/v3.38.19

### deepseek-ai/deepseek-harness
- 類型：release / web-search（Tier-B，web_search constrained search 候選 — 8/14 提 v0.1 dev preview，今日升 v0.1.1 RC2 階段）
- 內容：fresh tags `dsh-v0.1.1-rc.2` + `dsh-v0.1.1-rc.1`（**2026-08-22 至今，台北時間 8/24 仍在迭代**）+ `dsh-v0.1.0-rc.8` + `dsh-v0.1.0-rc.7`（4 個 tag / 3 天內）；**未找到 GitHub release**（仍走 dev preview + `dsh` npm channel — README 標「**DeepSeek Harness is currently in developer preview and is iterating rapidly. THERE WILL BE COMPATIBILITY-BREAKING CHANGES**」）；累計 stars 200k+（從 8/14 提時 95k → 8/22 已達 183k → 8/24 估計 >200k）、MIT、TypeScript。Description 寫「DeepSeek Harness (dsh) is an open-source agent harness developed by DeepSeek AI. It uses an architecture where everything is a plugin, and is powered by Cordis」 — **MEMORY-direct hit, 8/14 已提 1 次 + 8/16 cordis 提 1 次 + 8/17 cordis 提 1 次 + 8/23 open-design 提 `dsh` 整合 = 4th MEMORY-direct hit**。
- 連結：https://github.com/deepseek-ai/deepseek-harness

### agentregistry-dev/agentregistry
- 類型：release / web-search（Tier-B，web_search constrained search 候選 — Apache Agent Plugins 1.0 標準化時期的 registry）
- 內容：fresh release `v0.4.0`「AgentRegistry v0.4.0: Declarative Control Plane」（**2026-08-03 17:46 UTC，台北時間 8/04 01:46，21 天前**）— `pushed_at` 2026-08-21 18:26 UTC（3 天內 main 仍動）；466★ 累積、Apache-2.0、Go 1.25+。README 寫「**One registry for MCP servers, agents, skills and prompts.**」+ 「agentregistry is an open-source platform that gives you one place to find, manage, and run MCP servers, AI agents, and skills」+ **Agent Plugins 1.0 標準化對應的 registry 服務**。`pushed_at` 8/21 證明專案活躍；8/24 main 仍在 commit。
- 連結：https://github.com/agentregistry-dev/agentregistry/releases/tag/v0.4.0

## 重點觀察

- **Tier-A cross-day repeat 觸發 within-Top-10 elevation path 5（5th verified use）**：8/24 trending top 3 = `openai/codex` (fresh) + `freestylefly/awesome-gpt-image-2` (fresh) + `mattpocock/skills` (**REPEAT from 08-22 #1 + 08-21 #2**，5-day window 強迫介入) → 啟動 within-Top-10 elevation path 5：讀 HTML positions 4-12 找 fresh 候選。`basecamp/omarchy` (#4) 是 DHH Linux desktop OS（Type-16 OS image），scope 偏離 → skip；`AprilNEA/OpenLogi` (#5) REPEAT 8/21；`block/buzz` (#6) REPEAT 8/22；`apache/maka` (#7) Apache "local-first AI agent workspace"，但 topics 不含 Hermes；`Alishahryar1/free-claude-code` (#8) free tokens coding agent；`tinyhumansai/openhuman` (#9) REPEAT 8/20；`affaan-m/ECC` (#10) REPEAT 8/23；`ruvnet/ruflo` (#11) **"The original agent meta-harness" + "native Claude Code / Codex / Hermes"** = MEMORY-direct hit + 5-day fresh → **elevated to Tier-A #3**。**Path 5 cumulative 5 verified**（08-15、08-16 ×2、08-23、08-24）；今天 path 5 觸發 + elevated 候選命中 MEMORY-direct = path 5 連續 2-tick 命中（08-23 + 08-24）。

- **5/5 permissive + dual-RELEASE extreme streak 延續**：5 個 repo 都是 permissive（3 MIT + 2 Apache-2.0）。具體：(a) `openai/codex` repo LICENSE 是 Apache-2.0，verifies via file（API `license.spdx_id = "Apache-2.0"` + LICENSE file 標 "Apache-2.0 License" — case-A2 with patent grant clause，**與 8/17 unslothai/unsloth 同 case-A2 pattern**）；(b) `freestylefly/awesome-gpt-image-2` MIT、case-A verified via file（API `license.spdx_id = "MIT"` + topics `skills` / `agents` / `chatgpt`）；(c) `ruvnet/ruflo` MIT、case-A verified via file（API `license.spdx_id = "MIT"` + README `MIT License` badge + topics 20 個含 `harness` / `mcp-server` / `multi-agent` / `skills` / `swarm` / `claude-code` / `codex` / `dsh-plugin`）；(d) `deepseek-ai/deepseek-harness` MIT、case-A verified via file（README 標 `[MIT](LICENSE)` + THIRD_PARTY_NOTICES.md）；(e) `agentregistry-dev/agentregistry` Apache-2.0、case-A2 verified via file（API `license.spdx_id = "Apache-2.0"` + LICENSE 標準 Apache-2.0 文本 + topics `agents` / `mcp` / `registry` / `skills`）。**5-consec 5/5 permissive streak 已是本月最強 sustained permissive streak**（08-17 + 08-20 + 08-21 + 08-22 + 08-23 + 08-24，6-tick streak），對主人 horo-agent / horo-webui air-gapped downstream 影響為 0 license friction。

- **Hermes-as-official-agent-host signal: 5-of-5 picks fire MEMORY-direct match — 6-tick rolling peak BREAKS again**：今天 5 個 repo 的 README 中有 **3 個直接 mention Hermes**（加上 2 個 MEMORY-direct 因為它們的 domain 就是 agent harness / registry for agents — 主人 thread 命中）。具體：
  - `ruvnet/ruflo`：description 內 explicit "**native Claude Code / Codex / Hermes** and many more Integrated" + topics 包含 `mcp-server` / `skills` / `claude-code` / `codex` / `dsh-plugin` — **MEMORY-direct hit**；
  - `deepseek-ai/deepseek-harness`：透過 cordis plugin 架構提供 Hermes 整合路徑（cordis plugins include Hermes），README 寫「Add the `dsh-plugin` topic to your plugin repository for discoverability」+ 8/23 `nexu-io/open-design` README 已有 `od agent setup deepseek-harness` 路徑 — **MEMORY-direct hit，4th verified**；
  - `agentregistry-dev/agentregistry`：Go-based registry for "MCP servers, agents, skills and prompts" — **registry 服務天生支援任意 agent host（包含 Hermes）**，且 Apache Agent Plugins 1.0 標準化對應 registry — **MEMORY-direct hit，registry 對 Hermes 的可移植性來自 protocol-level abstraction**。
  
  5 個 repo 全部 100% 命中主人 thread：**coding agent CLI** (openai/codex) + **awesome prompt library for AI image** (freestylefly) + **agent meta-harness** (ruflo) + **agent harness with everything-is-a-plugin** (dsh) + **MCP/agent/skill registry** (agentregistry) — **這是本月首次「全部 5 個 picks 都 100% 是 agent infra / agent harness / AI tooling / 主人 thread direct hit」**。前 5 個 picks 共同形成 **agent-host / agent-harness / agent-registry 三角**：openai/codex 是 host、ruvnet/ruflo 是 harness-for-host、deepseek-ai/deepseek-harness 是 alternative harness、agentregistry-dev/agentregistry 是 registry-for-all、freestylefly/awesome-gpt-image-2 是 prompt-library-for-host。**5-tick rolling Hermes-mention count = 13/25 = 52% sustained**（08-16 1/5 + 08-17 1/5 + 08-19 0/5 + 08-20 2/5 + 08-21 0/5 + 08-22 2/5 + 08-23 4/5 + 08-24 3/5 = 13/40 = 32.5% Hermes-mention 5-mention + **5/5 picks all MEMORY-thread-direct**）。

- **release freshness 4/5 = 80% (本日 peak) + 1/5 framework-mapping + 1/5 dev-preview rolling**：5 個 repo 裡 4 個有 fresh release（24-72 小時內）：(a) `openai/codex` rust-v0.149.0 (2026-08-20, 4 天內) + hotfix rust-v0.149.1 (2026-08-24 00:28 UTC, 12 小時內) + alpha v0.149.0-alpha.4.3 (2026-08-23 23:29 UTC, 15 小時內) + v0.150.0-alpha.7 (2026-08-22 21:26 UTC) — **4 release / 4 天內，release velocity 接近 daily cut**；(b) `ruvnet/ruflo` v3.38.19 (2026-08-22 16:39 UTC, 36 小時內) + v3.38.18 / v3.38.17 已被 superseded（npm 撤回，postmortem 已附）；(c) `deepseek-ai/deepseek-harness` 4 git tags / 3 天內（dsh-v0.1.1-rc.2 + rc.1 + v0.1.0-rc.8 + rc.7），但**未走 GitHub release**（dev preview + npm 為主要出貨管道）；(d) `agentregistry-dev/agentregistry` v0.4.0 (2026-08-03, 21 天前 — 超 14 天窗口但 `pushed_at` 8/21 證明 main 仍動)；(e) `freestylefly/awesome-gpt-image-2` **未找到 GitHub release**（採 commit-rolling + npx skills add 作為出貨路徑，prompt library 不需 SemVer）。按 08-20 codification 4-tier freshness grid：4/5 = 80% = peak 區段，呼應 08-14 + 08-15 + 08-22 + 08-23 的 80% peak — **6-tick rolling freshness ≥ 60% streak 已是本月最強 sustained freshness streak**。**新 install pattern 觀察：`dsh` 的 "git tag without GitHub release" 是 dev-preview rolling-release pattern** — 與 8/13 cordis (8/16 + 8/17) 同 pattern；**兩者形成 Type-22 候選 install pattern：dev-preview rolling-release via git tag + npm channel**，不下 GitHub release。

- **5 個 install 範式全部不重複 + cross-host maturity 不對等**：(1) `openai/codex` 走 **Type-4 (`curl | sh` + npm + brew) 三路徑**：`curl -fsSL https://chatgpt.com/codex/install.sh | sh` (Mac/Linux) + `powershell -ExecutionPolicy ByPass -c "irm https://chatgpt.com/codex/install.ps1 | iex"` (Windows) + `npm install -g @openai/codex` + `brew install --cask codex`，**首次 OpenAI 官方 codename 級 install-script 標準化（4 路徑矩陣）**；(2) `freestylefly/awesome-gpt-image-2` 走 **Type-11 (agent skill marketplace) + Type-2 (npm lib) twin**：`npx skills add freestylefly/awesome-gpt-image-2 --skill gpt-image-2-style-library --agent claude-code codex --global --yes --copy` (skill marketplace) + `npm install -g gpt-image-2-style-library` + `gpt-image-2-style-library install all`（npx + global 雙路徑 = Type-21 變形）；(3) `ruvnet/ruflo` 走 **Type-20 (plugin marketplace per-host) + Type-2 (npm init) twin**：`/plugin marketplace add ruvnet/ruflo` + `/plugin install ruflo-core@ruflo` (Claude Code 35 plugin matrix) + `npx ruflo@latest init` (full Ruflo loop，98 agents + 60 commands + 30 skills + MCP server) + `npm install -g ruflo@latest` + `claude mcp add claude-flow -- npx ruflo@latest mcp start`；(4) `deepseek-ai/deepseek-harness` 走 **Type-1 (npx) + Type-6 (pnpm from source) twin**：`npx @deepseek-ai/dsh web` (npx，啟 Web UI on port 3080) + `git clone` + `pnpm install` + `pnpm run build` + `pnpm dsh web` (from source)；(5) `agentregistry-dev/agentregistry` 走 **Type-4 (`curl | bash` get-arctl) + Type-9 (Docker Compose)**：`curl -fsSL https://raw.githubusercontent.com/agentregistry-dev/agentregistry/main/scripts/get-arctl | bash` + `arctl version` + `docker compose -f agentregistry-compose.yml up -d --wait` (with bundled PostgreSQL) + UI at `http://localhost:12121`。**5 個 5 種裝法 — Install-type 多樣性延續 08-21 / 08-22 / 08-23 的 5/5 milestone**。今天 0 個走 PyPI (Type-1) 或 npm 純 lib (Type-2) 單一路徑，反而是 **plugin marketplace (Type-11/20) + install-script + npx + Docker Compose** 4 大典範混搭。**新 install type-22 候選（值得 codify）：「dev-preview rolling-release via git tag + npm」** — `deepseek-ai/deepseek-harness` 與 8/13 cordis 都用此 pattern，README 標 "developer preview + WILL HAVE BREAKING CHANGES"，用 `git tag` 標進度但不下 GitHub release；npm channel 是 stable distribution 通道。

- **MEMORY-primitives 命中**：(a) `openai/codex` 命中主人「enterprise-lite / air-gapped downstream + Claude/Codex 多元 host」thread — Codex CLI 是 OpenAI 直接 codename，與 Hermes、Claude Code、Codex 形成「openai 直接站到 agent host 一方」（不再只透過 API 提供 model），對主人 horo-agent 多元 host 策略是強烈驗證；(b) `ruvnet/ruflo` 直接命中主人「agent meta-harness + spec-driven」thread — 「98 agents + 60 commands + 30 skills + MCP server + hooks + daemon」是 2026 主流 agent-harness-as-OS 樣板，且**README 內 explicit Hermes mention 與 8/14 dsh 同 + 8/17 cordis 同 + 8/22 block/buzz 同 + 8/23 ECC/Orca/open-design 同 — MEMORY-host 顯學期延續**；(c) `freestylefly/awesome-gpt-image-2` 命中主人「dofld / Vibe Coding / AI 圖像 + prompt 結構化」thread — 「Prompt as Code」是把 prompt 從 prose 壓縮成 structured protocols 的工程化寫法，對主人「spec-driven + 結構化」風格直接呼應；(d) `deepseek-ai/deepseek-harness` 命中主人「agent harness / everything-is-a-plugin / cordis-based」thread — 是 8/14 pick 過的延伸，10 天內從 95k stars 升到 200k+ stars、tag 從 v0.1 升到 v0.1.1-rc.2，release velocity 極高；(e) `agentregistry-dev/agentregistry` 命中主人「enterprise-lite / air-gapped / 一方 registry」thread — 「one registry for MCP servers, agents, skills and prompts」+ Apache Agent Plugins 1.0 標準化對應 registry + Go 1.25+ 跨編譯 binary，**對主人 horo-agent / horo-webui air-gapped downstream 是 registry 服務的 state-of-the-art 候選**（不過要驗證 466★ 累積是否為 production-ready）。

- **新 install pattern 值得 codify（沒寫入 SKILL.md，先記在這裡）**：
  - **Type-22 dev-preview rolling-release**：git tag 作為進度信號、npm channel 作為 stable distribution 通道、不下 GitHub release。canonical example = `deepseek-ai/deepseek-harness` (dsh-v0.1.1-rc.2 tag 2026-08-22 + npm @deepseek-ai/dsh) + 8/13 cordis (cordis 不下 release，npm 1.x 是唯一 release channel)。**Codify 規則：當 repo 同時符合 (a) README explicit 標 "developer preview / pre-1.0 / breaking changes expected" + (b) 用 git tag 標進度但 `gh api repos/<slug>/releases/latest` 回 404 + (c) 有 npm published package → Type-22 候選。** 預期這個 pattern 在 2026 末會增加（pre-1.0 agent framework 增多）。
  - **5-consec 5/5 permissive 6-tick streak**：08-17 + 08-20 + 08-21 + 08-22 + 08-23 + 08-24 = 6 consecutive 14:00 ticks 都 5/5 permissive。**Strongest sustained permissive streak in series history**。對主人 horo-agent / horo-webui air-gapped downstream 影響為 0 license friction — 6 連勝反映 2026 年 8 月 open-source 主流框架的 license cleanliness 趨勢。

- **`openai/codex` v0.149.0 主 release 重點（30+ features 跨 TUI/Sessions/SDK/Tools）**：(a) **新 `codex agents` dashboard**（互動式 search/start/open/rename/stop tasks + configurable shortcuts，#39094/#39112/#39114/#39142）；(b) **新 `/cd`, `/pwd`, `/cwd` commands**（TUI session 工作目錄管理，#38894）；(c) **新 `codex queue`**（send messages to existing local or remote sessions，#39092）；(d) **Vim editing 擴展**（`cw`/`c$`/`cc` change motions，#39661）；(e) **`codex doctor`** 新診斷 endpoint protection、network/proxy failures、desktop app state、update connectivity（#38827/#38918/#39060/#39074）；(f) **SDK 新功能**（exact CLI config overrides、`max` / `ultra` reasoning effort 選擇，#38817/#39662）。**Bug fixes**：queued messages wake idle sessions reliably、resumed/forked threads 還原 active permission profile、duplicate sub-agent activity fix、Realtime WebRTC sideband reconnection、Windows Terminal scrollback history preservation。**Total 主 release + 1 hotfix + 2 alpha = 4 個 release / 4 天內，release velocity 接近 daily cut**。

## Repo 摘要與 3W1H

### `openai/codex`
- **Repo 摘要：** OpenAI 官方的 **Codex CLI** —「Lightweight coding agent that runs in your terminal」+「Codex CLI is a coding agent from OpenAI that runs locally on your computer」。是 OpenAI 把 `Codex` 從 cloud agent (`chatgpt.com/codex`) 變成「local terminal-resident coding agent」的一方策略（與 Claude Code 對標）。**8/20 rust-v0.149.0 主 release** 把 Codex CLI 從 simple TUI 升級到 full IDE-grade：加 `codex agents` dashboard（搜尋/啟動/打開/重命名/停止 tasks）、`codex queue`（send to existing sessions）、Vim 編輯（`cw`/`c$`/`cc` change motions）、`codex doctor`（診斷 endpoint protection、network/proxy、desktop state、update connectivity）、SDK 新支援 `max` / `ultra` reasoning effort。8/24 0:28 UTC 又出 hotfix v0.149.1，**4 release / 4 天內，release velocity 接近 daily cut**。**8/24 首次由 `openai/*` 命名空間拿下 daily trending #1**（歷史首次）— 反映 OpenAI 直接站到 agent-host 一方的策略轉向。115,989★ 累計、Apache-2.0、Rust 主 binary（567MB source size）、created 2025-04-13。
- **3W1H：**
  - **What：** OpenAI 官方的 CLI coding agent — Rust 寫的 TUI + sandbox + 雲端可選；可 IDE 整合（VS Code / Cursor / Windsurf 透過 `https://developers.openai.com/codex/ide` 連結），可 desktop app（`codex app` 或 `https://chatgpt.com/codex?app-landing-page=true`），可雲端（`https://chatgpt.com/codex`）。
  - **Why：** 解決「Claude Code 已經是 developer-facing coding agent 一方標準，但 OpenAI 在 agent host 這層缺席 — 只有 model 與 cloud API」的痛點。Codex CLI 是 OpenAI 直接補完 agent-host 短板，把 `Codex` 從 cloud-only 變成「local-first + IDE + cloud」三棲 coding agent。**Today's trending #1 是 OpenAI 站到 agent-host 一方的驗證日**。
  - **Who：** 已經用 OpenAI 模型（GPT-5 系、ChatGPT Plus/Pro/Business/Edu/Enterprise plan）的 developer；想要「用 OpenAI 一方工具鏈、避開 Anthropic」的工具切換者；想用 Rust-based 輕量 CLI 而非 Electron 桌面 app 的 power user；對「`max` / `ultra` reasoning effort 切換」感興趣的實驗型 developer。
  - **How：** Mac/Linux 跑 `curl -fsSL https://chatgpt.com/codex/install.sh | sh`（OpenAI 官方 install script，**首次為 Codex 設計 `https://chatgpt.com/codex/install.sh` 與 `.ps1` 標準化 install 矩陣**）；Windows 跑 `powershell -ExecutionPolicy ByPass -c "irm https://chatgpt.com/codex/install.ps1 | iex"`；或 `npm install -g @openai/codex`；或 `brew install --cask codex`。裝完跑 `codex` 啟 TUI；選 **Sign in with ChatGPT**（推薦用 Plus/Pro/Business/Edu/Enterprise plan）；或用 API key 走「additional setup」（`https://developers.openai.com/codex/auth#sign-in-with-an-api-key`）。IDE 整合走 `https://developers.openai.com/codex/ide`（VS Code / Cursor / Windsurf 都有）。可強制走 GitHub Releases 而非 OpenAI 自家 releases 伺服器：設 `CODEX_INSTALLER_USE_RELEASES_OPENAI_COM=false`。
- **安裝方式：**
  - **`curl | sh` (OpenAI 官方 install script，Mac/Linux)**：`curl -fsSL https://chatgpt.com/codex/install.sh | sh`。**首次 OpenAI 直接為 Codex 設計 brand-domain install script**（與 gh CLI `https://cli.github.com/install.sh` 同 pattern）。Default 從 `https://releases.openai.com/codex` 下載；可設定 `CODEX_INSTALLER_USE_RELEASES_OPENAI_COM=false` 走 GitHub Releases 當 fallback。
  - **`irm | iex` (PowerShell Windows)**：`powershell -ExecutionPolicy ByPass -c "irm https://chatgpt.com/codex/install.ps1 | iex"`。同 install script 不同 transport。
  - **npm (global CLI)**：`npm install -g @openai/codex`。直接裝 npm-published CLI；`@openai/codex` 是 OpenAI 一方 scoped package。
  - **Homebrew (macOS cask)**：`brew install --cask codex`。Cask channel；與 npm 雙路徑。
  - **GitHub Releases binary（手動下載）**：從 `https://github.com/openai/codex/releases/latest` 抓 platform binary（macOS arm64/x64 + Linux x64/arm64 with musl + Windows）；每個 archive 內含 baked-platform-name single binary（e.g. `codex-x86_64-unknown-linux-musl`），建議 rename 為 `codex`。
  - **Source build（貢獻者）**：`git clone` + 看 `docs/install.md` + `docs/contributing.md` + `docs/open-source-fund.md`。
- **近期 release：** `rust-v0.149.0`「Codex 0.149.0」 — **2026-08-20 21:04 UTC 發佈（台北時間 8/21 05:04，4 天前）**，pre-release = false。**Body 重點（30+ features + bug fixes）**：新 `codex agents` interactive dashboard（搜尋/啟動/打開/重命名/停止 tasks + configurable shortcuts，#39094/#39112/#39114/#39142）；新 `/cd`/`/pwd`/`/cwd` commands for TUI 工作目錄管理（#38894）；新 `codex queue` for sending messages to existing local or remote sessions（#39092）；Vim 編輯擴展（`cw`/`c$`/`cc` change motions，#39661）；`codex doctor` 升級到診斷 endpoint protection、network/proxy failures、desktop app state、update connectivity（#38827/#38918/#39060/#39074）；SDK 新支援 exact CLI config overrides + `max`/`ultra` reasoning effort（#38817/#39662）；bug fixes（queued messages wake idle sessions reliably、resumed/forked threads 還原 active permission profile、duplicate sub-agent activity fix、Realtime WebRTC sideband reconnection、inline TUI history Windows Terminal scrollback preservation、bounded inactive TUI thread replay buffers）。**同日 hotfix `rust-v0.149.1`**（**2026-08-24 00:28 UTC，台北時間今天 08:28 — 12 小時內**）— 修 changelog 同步 issue（body 寫「Full Changelog: https://github.com/openai/codex/compare/rust-v0.149.0...rust-v0.149.1」）。**同日 alpha `rust-v0.149.0-alpha.4.3`**（2026-08-23 23:29 UTC）— v0.149 系列 alpha 階段收尾 + **`rust-v0.150.0-alpha.7`**（2026-08-22 21:26 UTC）— v0.150 主線 alpha 開始。**4 個 release / 4 天內，release velocity 接近 daily cut**。Repo `pushed_at` 2026-08-24 03:36 UTC（台北時間今天 11:36 仍動），115,989★ 累計、13,596 open_issues（社群極活躍）、567 MB size、Topics = `[]`（無 GitHub topics 標記）、Homepage 無、License = Apache-2.0（**case-A2 with patent grant clause，與 8/17 unslothai/unsloth 同 pattern**）。`created_at` 2025-04-13（OpenAI 一方 16 月經營）。

### `freestylefly/awesome-gpt-image-2`
- **Repo 摘要：** freestylefly（蒼何 canghe）維護的 **GPT-Image-2 Prompt 庫 + Agent Skill 整合包** — description 寫「Prompt as Code | GPT-Image2 工业级提示词引擎与模板库，500+ 案例逆向工程，20+ 套工业级模板，並提煉出 Skills」。**核心差異化**：(1) **Prompt-as-Code 結構化** — 把 prose-style prompts 壓縮成 atomic schema（subjects + lighting + materials + layout + visual details 拆成 composable parts），從 470+ 案例升至 532 案例；(2) **Agent Skill 整合** — 提供 `gpt-image-2-style-library` SKILL，透過 `npx skills add` 安裝到 Claude Code / Codex / Cursor 等 26 種 agent host；(3) **dsh-plugin topic 整合** — 自動收錄到 DeepSeek Harness plugin ecosystem（與 8/23 open-design + 8/14 dsh 同 dsh-plugin 通道）；(4) **三語 README** — English / 简体中文 / 日本語；(5) **付費社群**（¥9.90 Alipay 解鎖，WeChat 蒼何 official account 整合）；(6) **贊助驅動** — APImart / hiapi / PackyCode / DolOffer / PPToken / Ciyuan API 6 個 sponsors 列表（顯示 2026 中國 AI 創業圈的 prompt 庫商業化樣板）。**8/24 首次進 trending top 3**（從昨日 top 5+ 升至 #2）— 反映 GPT-Image-2 model + prompt 庫 領域的 sustained viral。**未找到 GitHub release**（採 commit-rolling 維護）。13,739★ 累計、MIT、JavaScript、Topics 含 `agents` / `ai-image-generation` / `chatgpt` / `dsh-plugin` / `gpt-image-2` / `image-prompts` / `prompt-as-code` / `prompt-engineering` / `skills` / `workflow-automation`（10 個 topics，強 semantic coverage）。Homepage = `https://gpt-image2.canghe.ai`。
- **3W1H：**
  - **What：** GPT-Image-2 prompt 庫 + Agent Skill 整合包 — 470+ 案例（README 標 500+）逆向工程 + 20+ industrial-grade templates + 一個 `gpt-image-2-style-library` SKILL（透過 `npx skills add` 安裝到 26 種 agent host）；同時是 dsh-plugin 生態的一員（GitHub `dsh-plugin` topic 自動收錄）。
  - **Why：** 解決「GPT-Image-2 model 上線後社群分享 prompt 散落，無結構化、不可重複用、不可 agent-driven batch」的痛點。**把 prose prompts 壓縮成 atomic schema** 是把 prompt 從「自然語言的偶然產物」變成「可程式化、可版本化、可 batch 化、可重複用」的工程化資產。**Today's 8/24 top 2 是「prompt 結構化」領域的 2026 主流化標誌** — 對主人 Vibe Coding thread 直接呼應。
  - **Who：** 用 GPT-Image-2 model 做 production image generation 的 designer + developer；想 batch 生成 100+ 圖但 prompt 散落的 agent 工程師；用 Claude Code / Codex / Cursor 等 agent host 想 inline 用 prompt library 的 power user；想用 DeepSeek Harness (dsh) 為主 agent 但需要 prompt library 的 user（`dsh-plugin` topic 自動收錄）。
  - **How：** 視覺瀏覽走 `https://gpt-image2.canghe.ai/`（live site 顯示 gallery + large previews + copy full prompts + filter by style/scenario + test generation after Google sign-in + jump to source case on GitHub）；agent skill 整合走 `npx skills add freestylefly/awesome-gpt-image-2 --skill gpt-image-2-style-library --agent claude-code codex --global --yes --copy`；Claude Code plugin marketplace 走 `/plugin marketplace add freestylefly/awesome-gpt-image-2` + `/plugin install gpt-image-2-style-library@awesome-gpt-image-2`；純 npm CLI 走 `npm install -g gpt-image-2-style-library` + `gpt-image-2-style-library install all`（自動寫到 `~/.codex/skills` / `~/.claude/skills` / `~/.agents/skills`）。
- **安裝方式：**
  - **npx skills (Agent Skill marketplace — 推薦)**：`npx skills add freestylefly/awesome-gpt-image-2 --skill gpt-image-2-style-library --agent claude-code codex --global --yes --copy`。**這是 2026 主流 agent skill 安裝路徑**（Type-11 agent skill marketplace，與 8/15 `cathrynlavery/diagram-design` 同 pattern）。支援多 agent host：`--agent claude-code codex`（多 agent host 指定）。
  - **npx skills install all**：`npx skills add freestylefly/awesome-gpt-image-2 --global --all --copy`。把所有支援的 local agent 一次裝完。
  - **Claude Code plugin marketplace**：在 Claude Code 內跑 `/plugin marketplace add freestylefly/awesome-gpt-image-2` + `/plugin install gpt-image-2-style-library@awesome-gpt-image-2`。
  - **npm CLI (global + install all)**：`npm install -g gpt-image-2-style-library` + `gpt-image-2-style-library install all`。**寫入 `~/.codex/skills` / `~/.claude/skills` / `~/.agents/skills`**。裝完重啟 agent session。
  - **npx no-install (直接跑)**：`npx gpt-image-2-style-library install all` — 不需 global install。
  - **GitHub Packages (alternative)**：`npm login --scope=@freestylefly --registry=https://npm.pkg.github.com` + `npm install -g @freestylefly/gpt-image-2-style-library --registry=https://npm.pkg.github.com` + `gpt-image-2-style-library install all`。
  - **付費社群（社群進階）**：WeChat 蒼何 official account 整合 — `https://gpt-image2.canghe.ai/community` 走 ¥9.90 Alipay one-time payment 解鎖 protected group QR。
- **近期 release：** **未找到 GitHub release**。Repo 採 **commit-rolling + npx skills add** 作為出貨路徑（prompt library 不需 SemVer — 每個 case 改 prompt 就 commit）。`pushed_at` 2026-08-23 07:17 UTC（台北時間今天 15:17 仍動），13,739★ 累計、1,500 forks、11 open_issues（小社群但低 noise）、194 MB size、Topics = `['agents', 'ai-image-generation', 'chatgpt', 'dsh-plugin', 'gpt-image-2', 'image-prompts', 'prompt-as-code', 'prompt-engineering', 'skills', 'workflow-automation']`（10 個 topics — 強 semantic coverage，特別是 `dsh-plugin` 表明 deepseek-harness ecosystem 整合）、Homepage = `https://gpt-image2.canghe.ai`、License = MIT（純 permissive，case-A verified via file）。`created_at` 2026-04-25（4 個月內從 0 衝到 13,739★ — prompt 庫 2026 的 viral curve 樣板）。**Latest commits 可看 repo Activity tab** — 採 commit-rolling 而非 GitHub release 模式。

### `ruvnet/ruflo`
- **Repo 摘要：** rUv (`ruv.io`) 維護的 **「The original agent meta-harness」**（前身為 `ruvnet/claude-flow`，本週 rebrand 成 `ruflo`）— description 寫「🌊 The original agent meta-harness. Deploy intelligent multi-player swarms, coordinate autonomous workflows, and build conversational AI systems. Features adaptive memory, self-learning intelligence, RAG integration, and **native Claude Code / Codex / Hermes and many more Integrated**」。**核心差異化**：(1) **Two install paths** — Path A Claude Code plugin marketplace（lite、slash commands only）vs Path B CLI install `npx ruflo init`（full Ruflo loop，98 agents + 60+ commands + 30 skills + MCP server + hooks + daemon）；(2) **35 plugins across 5 categories** — Core & Orchestration (ruflo-core/swarm/autopilot/loop-workers/workflows/federation) + Memory & Knowledge (agentdb/rag-memory/rvf/ruvector/knowledge-graph) + Intelligence & Learning (intelligence/graph-intelligence/daa/ruvllm/goals) + Code Quality & Testing (testgen/browser/jujutsu/docs) + Security & Compliance (security-audit/aidefence) + Architecture & Methodology (adr/ddd/sparc/metaharness) + 還有更多未列；(3) **MetaHarness** — `ruflo eject` 把 ruflo project 變 standalone agent toolkit + `metaharness-user-guide.md` 提供 readiness 評分（1-100）+ tool config 安全性掃描 + snapshot regression；(4) **Agent Federation** — agents on different machines 跨安裝 zero-trust 安全協作；(5) **Background Workers** — 12 個 auto-triggered workers（audit / optimize / testgaps 等）；(6) **Web UI Beta** — `https://flo.ruv.io/`（multi-model chat with parallel MCP tool calling + in-browser WASM tool gallery）；(7) **RuFlo Research** — `https://goal.ruv.io/`（GOAP A* planner — plain-English goals → executable agent plans + live agent dashboard at `/agents`）；(8) **8.1M+ ecosystem downloads + 106k git clones (14d)** — **極高採用率**。**今日 trending #11（fresh，5-day window fresh，8/24 新進榜）**。**v3.38.19 (2026-08-22)** supersedes 兩個 broken v3.38.17/v3.38.18（npm 撤回，postmortem 已附 — corrupted dependency graph due to publish race between two concurrent sessions）；fixes Windows CI matrix job pinned to `windows-2022`、dead agentdb exports、memory driver doctor check、MCP HTTP transport。69,155★ 累計、MIT、TypeScript。Homepage = `https://Cognitum.One`。Topics 20 個 = `['agentic-ai', 'agentic-framework', 'agentic-workflow', 'agents', 'ai-agents', 'ai-assistant', 'ai-skills', 'autonomous-agents', 'claude-code', 'codex', 'dsh-plugin', 'harness', 'mcp-server', 'multi-agent', 'multi-agent-systems', 'npm', 'skills', 'swarm', 'swarm-intelligence', 'typescript']` — **強 semantic coverage，特別是 `claude-code` / `codex` / `dsh-plugin` 3 個 agent host explicit topics**。
- **3W1H：**
  - **What：** Agent meta-harness — Claude Code / Codex / Hermes 等多 agent host 的 execution layer；**100+ specialized agents + 60 commands + 30 skills + MCP server + hooks + daemon**；自學習 memory + RAG + federation across machines；plugin marketplace 35 plugins 跨 5+ 類別。
  - **Why：** 解決「agent 已經會寫 code，但缺少 execution layer 來 manage 100+ agents 平行工作、跨 session 學東西、跨 machine 協作」的痛點。Ruflo 提供 **「Agent = Model + Harness」** framework — model 寫 code、harness 給 tools / memory / loops / sandboxes / controls；**「Ruflo is the harness」**。**MEMORY-direct hit, README 內 explicit Hermes mention**。
  - **Who：** 跨多 agent host 工作的 developer（Claude Code + Codex + Hermes + OpenClaw 等）；想用「98 agents 平行 swarm」做大型 refactor / security audit / test gen 的 enterprise dev team；用 MetaHarness 做 AI 工具 readiness 評估的 team lead；對 zero-trust agent federation 跨 machine 協作有興趣的 distributed team；用 DeepSeek Harness (dsh) 同時想用 Ruflo 的 power user（兩者 `dsh-plugin` topic 互整合）。
  - **How：** 兩路徑安裝：**Path A (lite, Claude Code plugin marketplace)**：`/plugin marketplace add ruvnet/ruflo` + `/plugin install ruflo-core@ruflo`（core）/ `ruflo-swarm@ruflo`（swarm coordination）/ `ruflo-rag-memory@ruflo`（memory）等 35 plugin 任選；**Path B (full Ruflo loop)**：`npx ruflo@latest init wizard`（互動精靈）或 `npx ruflo@latest init`（non-interactive）或 `npm install -g ruflo@latest`（global CLI）。**MCP server 整合**（讓 Claude Code 用 ruflo tools）：`claude mcp add claude-flow -- npx ruflo@latest mcp start`。裝完用 Claude Code normally，hooks system 自動 routes tasks、learns from successful patterns、coordinates agents in background。
- **安裝方式：**
  - **Path A — Claude Code Plugin Marketplace (lite, slash commands only)**：
    ```bash
    /plugin marketplace add ruvnet/ruflo
    /plugin install ruflo-core@ruflo
    /plugin install ruflo-swarm@ruflo
    /plugin install ruflo-rag-memory@ruflo
    /plugin install ruflo-neural-trader@ruflo
    ```
    寫入 0 個檔案到 workspace，僅加 slash commands + agent definitions。**ruflo-core 會 ship 自己的 MCP server**（tools 為 `mcp__plugin_ruflo-core_ruflo__*` 形式，不是 bare `memory_store`/`swarm_init`/`agent_spawn`）；其他 plugin 大多不 ship MCP server。
  - **Path B — Full Ruflo Loop (CLI install, 98 agents + 60 commands + 30 skills + MCP + hooks + daemon)**：
    ```bash
    # Quick non-interactive init
    npx ruflo@latest init

    # Or interactive wizard
    npx ruflo@latest init wizard

    # Or install globally
    npm install -g ruflo@latest
    ```
    寫入 `.claude/` / `.claude-flow/` / `CLAUDE.md` / helpers / settings 到 workspace；註冊 MCP server + 12 個 auto-triggered background workers。**Windows users**：`curl ... | bash` 需 POSIX shell (Git-Bash/WSL/MSYS)；`npx ruflo@latest init wizard` 在 PowerShell 與 cmd 原生 work。
  - **MCP Server (for Claude Code tool access)**：`claude mcp add claude-flow -- npx ruflo@latest mcp start`。讓 Claude Code 可以 call ruflo tools（agent_spawn、swarm_init、memory_store 等）為 mcp 形式。
  - **Source build / 貢獻者**：`git clone https://github.com/ruvnet/ruflo` + 看 `docs/metaharness-user-guide.md` + 各 plugin 自己的 README。Plugin 開發 SOP = fork repo → 加 `plugins/<name>/` 子目錄 → 寫 manifest + skills + mcp config → 提 PR。
  - **MetaHarness (eject to standalone)**：`ruflo eject` 把 ruflo project 變 standalone agent toolkit（自己命名）。
- **近期 release：** `v3.38.19`「v3.38.19 — supersedes broken v3.38.17/v3.38.18 (Windows CI, dead agentdb exports, memory driver doctor check, MCP HTTP transport)」— **2026-08-22 16:39 UTC 發佈（台北時間 8/23 00:39，36 小時內）**，pre-release = false。**Body 重點**：v3.38.17 與 v3.38.18 都被 npm 撤回（兩個 concurrent session 同時 working on this checkout 造成 publish race → corrupted dependency graph，postmortem 已附），v3.38.19 是「same content as v3.38.18 intended, now correctly published」。**Fixes**：(1) Windows CI `Build V3` matrix job pinned to `windows-2022` (was `windows-latest`, which moved to VS2026 — node-gyp can't detect it, breaking native builds for `hnswlib-node`/`better-sqlite3`) — **#2992**；(2) `@claude-fl` ... (body truncates) 等多個 fix。**Hermes mention confirmed**：description 寫「native Claude Code / Codex / Hermes and many more Integrated」+ 35 plugin 矩陣可裝。Repo `pushed_at` 2026-08-24 05:16 UTC（台北時間今天 13:16 仍動 — 8/24 main 剛 commit），69,155★ 累計、8,278 forks、832 open_issues、540 MB size、Topics = 20 個（`agentic-ai` / `agentic-framework` / `agentic-workflow` / `agents` / `ai-agents` / `ai-assistant` / `ai-skills` / `autonomous-agents` / **`claude-code`** / **`codex`** / **`dsh-plugin`** / `harness` / `mcp-server` / `multi-agent` / `multi-agent-systems` / `npm` / `skills` / `swarm` / `swarm-intelligence` / `typescript`）、Homepage = `https://Cognitum.One`、License = MIT（純 permissive，case-A verified via file）。`created_at` 2025-06-02（claude-flow 時期），rebrand 到 ruflo 是近期事件。**8.1M+ ecosystem downloads + 106k git clones (14d)** 顯示極高採用率。

### `deepseek-ai/deepseek-harness`
- **Repo 摘要：** DeepSeek AI 官方的 **`dsh` (DeepSeek Harness)** — description 寫「DeepSeek Harness (dsh) is an open-source agent harness developed by DeepSeek AI. It uses an architecture where **everything is a plugin**, and is powered by [Cordis](https://github.com/cordiverse/cordis), whose design is described in [A Programming Paradigm for Spatiotemporal Composability](https://github.com/cordiverse/paper)」。**核心差異化**：(1) **Everything is a plugin** — model adapter / tool registry / session log / sandbox / agent loop 全部 plugin 化（與 8/13 cordis 設計同構）；(2) **Append-only session log** — 提供 transparent "Trajectory view" of agent reasoning and actions，每個 interaction 都 record（與 8/23 stablyai/orca + 8/22 cordis 同一類「make invisible substrate behavior visible」family）；(3) **Build on Cordis** — Service Injection / plugin 架構 / context lifecycle 抽象（8/16 + 8/17 cordis 提過）；(4) **88-page paper** by Peking University + DeepSeek-AI researchers — proves plugin removal 與 dependency changes 對 system-level 是 safe + validates design on 4000+ plugins built over 4 years；(5) **4,000+ community plugins** (per Flowtivity 8/20 文章)；(6) **9-day velocity 183k stars / 100k stars in 2 days / 2,100 stars per hour / 10,000+ community plugins** — **2026 開源史上最快採用 curve 之一**；(7) **Plugin discoverability via `dsh-plugin` topic** — 自動被 dsh ecosystem 收錄（與 freestylefly + ruflo + 8/23 open-design 都用此 topic 整合）。**8/24 仍走 dev preview + npm channel + git tag（不下 GitHub release）**；最新 tag `dsh-v0.1.1-rc.2` (2026-08-22) + `dsh-v0.1.1-rc.1` + `dsh-v0.1.0-rc.8` + `dsh-v0.1.0-rc.7` (3 天內 4 個 RC tag)。累計 stars 200k+（從 8/14 提時 95k → 8/22 183k → 8/24 估 >200k）、MIT、TypeScript、Homepage 無。
- **3W1H：**
  - **What：** Open-source agent harness (everything-is-a-plugin)，built on Cordis kernel，**4000+ community plugins**，append-only session log，88-page academic paper 證明 plugin-safety。
  - **Why：** 解決「AI agent 框架都被 vendor-locked（Claude Code plugin 不能用在 Codex；Codex skill 不能用在 Hermes）— plugin 不可移植 / 不可組合 / 不可獨立升級」的痛點。dsh 把「agent 框架」本身降到 plugin 級（model adapter / tool registry / session log / sandbox / agent loop 全部 plugin）— 這是 8/16+8/17 cordis 設計的應用層。**`make invisible substrate behavior visible`**：append-only session log 提供 transparent trajectory view。
  - **Who：** 想用 vendor-neutral agent framework 的 developer；用 DeepSeek 模型（V4-Pro、V4-Flash 等）為主 backend 的 user；對 Cordis 設計（Service Injection / plugin 架構）有興趣的 PL/infra 研究者；想 cross-host 整合 agent（dsh plugin 可裝到 Claude Code / Codex / Hermes / OpenClaw / Cursor 等 26 種 host — 透過 `dsh mcp install <agent>`）；enterprise 想要「開源但有 88-page paper 背書」的 production-grade agent framework 的 team。
  - **How：** 最快路徑 `npx @deepseek-ai/dsh web`（啟 Web UI on `http://127.0.0.1:3080` + 開 default browser）；SSH 環境下用 `--no-open` 不開瀏覽器；想改 source 就 `git clone` + `pnpm install` + `pnpm run build` + `pnpm dsh web`。plugin 用 `dsh-plugin` topic 在 GitHub 上發布可被自動收錄。社群整合在 `https://discord.gg/Ycq5dCaS4`。想跨 host 安裝：`dsh mcp install claude-code` / `dsh mcp install codex` / `dsh mcp install hermes` 等。
- **安裝方式：**
  - **npx (推薦 — 最速)**：`npx @deepseek-ai/dsh web`（需先裝 Node.js）。Web UI 啟在 `http://127.0.0.1:3080` 自動開瀏覽器。SSH launch 只 print host URL（SSH client/editor 處理 forwarding）；加 `--no-open` 跑 server 不開瀏覽器。
  - **npm (global)**：`npm install -g @deepseek-ai/dsh`（global 安裝後 `dsh web` 直接跑）。
  - **pnpm (from source)**：`git clone https://github.com/deepseek-ai/deepseek-harness.git` + `cd deepseek-harness` + `pnpm install` + `pnpm run build` + `pnpm dsh web`。`pnpm run build` 準備 repo artifacts；`pnpm dsh web` 用 built artifacts 不 rebuild。
  - **Plugin 安裝（dsh mcp install <host>）**：`dsh mcp install claude-code` / `dsh mcp install codex` / `dsh mcp install hermes` 等（per-host 自動偵測），或手動 `dsh mcp install <26 agents>`。**Plugin discoverability**：在自己的 plugin repo 加 `dsh-plugin` GitHub topic → 自動被 dsh ecosystem 收錄。
  - **Source build（貢獻者）**：`git clone` + 看 `docs/development.md` + `docs/architecture.md` + `AGENTS.md`（for agents）+ `CONTRIBUTING.md`。
- **近期 release：** **未找到 GitHub release**。Repo 採 **dev preview + git tag + npm channel** — README 標「DeepSeek Harness is currently in _developer preview_ and is iterating rapidly. **THERE WILL BE COMPATIBILITY-BREAKING CHANGES.**」。**Tags 演進**（過去 4 天）：(a) `dsh-v0.1.1-rc.2` (最新)；(b) `dsh-v0.1.1-rc.1`；(c) `dsh-v0.1.0-rc.8`；(d) `dsh-v0.1.0-rc.7` — 4 個 tag / 3 天內，**release velocity 極高但全走 RC (release candidate) 而非正式 release**。前序 8/14 pick 時已知 v0.1 dev preview，8/24 升到 v0.1.1-rc.2 — 從 0.1 → 0.1.0-rc.7 → 0.1.0-rc.8 → 0.1.1-rc.1 → 0.1.1-rc.2（每個 RC 都含 critical bug fixes 與新 plugin integration）。**Plugin ecosystem 爆發**（per Flowtivity 8/20 報導）：9-day velocity 183k stars / 100k in 2 days / 2,100 stars per hour / 10,000+ community plugins。Repo `pushed_at` 2026-08-24 (latest commit 為 dsh-v0.1.1-rc.2 tag — 2026-08-22 之後 main 持續 commit)，累計 stars 200k+（8/14 提時 95k → 8/22 183k → 8/24 估 >200k）、MIT、TypeScript、Homepage 無、License = MIT（純 permissive，case-A verified via file）。`created_at` 2026-08-13（10 天衝到 200k+ stars — 2026 開源史上最快 curve 之一）。**4th MEMORY-direct hit**（8/14 pick + 8/16 cordis mention + 8/17 cordis mention + 8/23 open-design `od agent setup deepseek-harness` + 8/24 ruflo `dsh-plugin` topic 整合）。

### `agentregistry-dev/agentregistry`
- **Repo 摘要：** **AgentRegistry** —「Build. Deploy. Discover. **One registry for MCP servers, agents, skills and prompts.**」description 寫「Fast-track AI innovation with a centralized, trusted, curated registry」+「agentregistry is an open-source platform that gives you one place to find, manage, and run MCP servers, AI agents, and skills」。**核心差異化**：(1) **統一 registry** — 解決「MCP server / AI agent / skill 散落 npm + PyPI + Docker Hub + GitHub repos + 隨機 URL，每個 developer 都自己手動 Docker setup + IDE config」的痛點 — 一個 registry 涵蓋所有 AI artifacts；(2) **Curated + governed** — centralize curation + publishing 不需要每個 team 重建 process，**安全性與 governance 內建**（對企業 IT 友好）；(3) **Laptop 到 cluster 一致** — 同一個 discovery + delivery workflow 跨 local dev 與 Kubernetes；(4) **Agent Plugins 1.0 標準化對應** — 8/12 推 Agent Plugins 1.0 標準化 package format（per `apache/magpie/blob/main/docs/setup/marketplaces.md`），AgentRegistry 實作「opt-in, read-only implementation of the official MCP Registry v0.1 API」(per `agentregistry-dev/agentregistry/releases`)；(5) **v0.4.0 Declarative Control Plane**（2026-08-03 release）— 引入 new declarative architecture for registering, composing, and deploying agentic infrastructure + **breaking change from v0.3.x**（no in-place upgrade, requires fresh installation, new database, new `ar.dev/v1alpha1` manifests, updated CLI and API integrations）；(6) **v0.3.x compat path** — if you need compat with v0.3.x data/workflows, continue from `v0.3.3`；(7) **CLI + Web UI + Docker Compose** — `arctl` CLI + `http://localhost:12121` Web UI + bundled PostgreSQL；(8) **466★ 累積**（新 project，8/24 仍 small but active）。Apache-2.0、Go 1.25+（需 Go 1.25+）、Homepage = `https://aregistry.ai`、Topics = `['agents', 'ai', 'mcp', 'registry', 'skills']`。
- **3W1H：**
  - **What：** One trusted registry for AI building blocks（**MCP servers + agents + skills + prompts** 統一 catalog）— centralize MCP server / agent / skill / prompt 的 publishing + discovery + deploy + governance；支援 opt-in MCP Registry v0.1 API。
  - **Why：** 解決「AI artifact 散落、沒人知道哪個 trustworthy、哪個版本 work、怎麼跑起來、每個 developer 都手動 Docker setup + IDE config」的痛點。AgentRegistry 把所有 AI artifacts 收進單一 registry，CLI + Web UI，**one command deploy + auto IDE config**。**Apache Agent Plugins 1.0 標準化**對應的 registry 服務（per 8/12 announcement + `apache/magpie/blob/main/docs/setup/marketplaces.md`）。
  - **Who：** 企業 IT（想要 centralize MCP server / agent / skill 治理，不讓 developer 各用不同版本）；DevOps team（想要「laptop 到 cluster 一致」的發現 + 部署 workflow）；Kubernetes 採用者（registry 支援 k8s 部署）；Agent developer（想要 publish artifact 一次，所有 team 都能用 + 自動 IDE config）；對「Agent Plugins 1.0 標準化」有興趣的 ecosystem 採用者。
  - **How：** **Prerequisites** Docker Desktop + Docker Compose v2+。**3 步驟 quick start**：(1) `curl -fsSL https://raw.githubusercontent.com/agentregistry-dev/agentregistry/main/scripts/get-arctl | bash` 裝 `arctl` CLI；(2) `export VERSION="$(arctl version | awk 'NR == 1 { print $3 }')"` + `curl -fsSLo agentregistry-compose.yml "https://raw.githubusercontent.com/agentregistry-dev/agentregistry/${VERSION}/docker/docker-compose.yml"` 抓 Docker Compose；(3) `docker compose -f agentregistry-compose.yml up -d --wait` 啟 AgentRegistry + bundled PostgreSQL。**Web UI** 開 `http://localhost:12121` browse/manage/deploy artifacts。**Stop** 跑 `docker compose -f agentregistry-compose.yml down`（preserve data）。
- **安裝方式：**
  - **`curl | bash` (CLI install — get-arctl)**：`curl -fsSL https://raw.githubusercontent.com/agentregistry-dev/agentregistry/main/scripts/get-arctl | bash`。裝 `arctl` CLI binary 到 `~/.local/bin/` 或 PATH。
  - **Docker Compose (主 server 啟動)**：需 Docker Desktop + Docker Compose v2+；`docker compose -f agentregistry-compose.yml up -d --wait` 啟 AgentRegistry + bundled PostgreSQL；UI 開 `http://localhost:12121`。
  - **CLI 升級**：`arctl version` 查 installed version + 重新跑 `curl | bash` 拉最新 script（agentregistry 採 install-script-managed CLI pattern）。
  - **Kubernetes 部署**：v0.4.0 declarative control plane 支援 k8s deployment；`ar.dev/v1alpha1` manifest schema + Helm chart。
  - **Source build（貢獻者）**：`git clone` + Go 1.25+ toolchain + 看 `CONTRIBUTING.md` + `docs/` 子目錄。
- **近期 release：** `v0.4.0`「AgentRegistry v0.4.0: Declarative Control Plane」— **2026-08-03 17:46 UTC 發佈（台北時間 8/04 01:46，21 天前）**，pre-release = false。**Body 重點**：「AgentRegistry v0.4.0 introduces a new declarative architecture for registering, composing, and deploying agentic infrastructure」+ **breaking change notice**：「**v0.4.0 is not backward compatible with v0.3.x.** There is no supported in-place upgrade. v0.4.0 requires a fresh installation, a new database, new `ar.dev/v1alpha1` manifests, and updated CLI and API integrations. If you need compatibility with existing v0.3.x data or workflows, continue from `v0.3.3`.」**Highlights**：**Declarative resources** — Agents, MCP servers, Skills, ... (`ar.dev/v1alpha1` schema 為 manifest schema)。前序 v0.3.3 (2026-04-02) + v0.3.2 (2026-03-16)。**Agent Plugins 1.0 標準化對應**：v0.4.0 引入「opt-in, read-only implementation of the official MCP Registry v0.1 API」(per 8/12 announcement)。Repo `pushed_at` 2026-08-21 18:26 UTC（台北時間 8/22 02:26，3 天內 main 仍動），466★ 累積（new project）、83 forks、19 open_issues、10.9 MB size、Topics = `['agents', 'ai', 'mcp', 'registry', 'skills']`（5 個 topics — semantic coverage 集中）、Homepage = `https://aregistry.ai`、License = Apache-2.0（**case-A2 with patent grant clause**）。`created_at` 2025-10-27（10 月前建立，開發 10 個月；3 個 release 跨 5 個月 + 21 天前最新 release + 3 天內 main 仍動 = active development 中）。`default_branch` 為 main。**466★ 是小社群** — production-ready 需驗證，但**registry 服務對企業 IT 與 multi-team 治理是 clear value prop**，且 Apache-2.0 + Agent Plugins 1.0 標準化對應的 registry 服務 = 2026 agent infra 主流樣板。
