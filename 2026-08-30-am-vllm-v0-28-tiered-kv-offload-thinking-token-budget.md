# vLLM v0.28.0 — Tiered KV Cache Offloading + thinking_token_budget
- 原始連結：https://github.com/vllm-project/vllm/releases/tag/v0.28.0
- HN 討論：https://news.ycombinator.com/item?id=49492067
- 閱讀時間：2026-08-30（早間，週日）
- 來源：Hacker News 熱門前 10 第 6 名（讀取時 73 分／26 則留言，Asia/Taipei 07:00）；vLLM Project release，2026-08-29 標記 Latest

## 摘要

**vLLM v0.28.0 是 584 commits / 270 contributors 的 monthly release，但真正的主軸訊號是一條「KV cache 從 GPU-only memory 開始洩漏到 disk」的 substrate 擴張**：先前所有 inference-optimization 都假設「KV cache 一定放 HBM」，8/22 Nari TTS、8/20 DFlash 2、8/11 DFlash 全都在 HBM 假設內做 scheduling。v0.28.0 第一次把 *tiered KV cache* 做成 first-class — **disk offloading support (#49644)** + **out-of-tree secondary tier managers via `module_path` (#51007)** + **partial secondary-tier load results (#50321)** + **tiering metrics (#48798)** + **canonical CPU layout for parallelism-agnostic offload (#48414)**。對 16GB Mac owner-operator 來說，這意味著 *「KV cache 不一定要 fit in HBM」* 變成 production-validated pattern，是 inference-optimization axis 第一次把 substrate-identity 從「shared scheduling surface」推進到「tiered memory substrate + shared scheduling」。

**第二個關鍵訊號是 `thinking_token_budget` (#46727) 進入 Model Runner V2**。先前 routing layer 的 cost control 都是「*模型 variant 切換*」（low/high/max reasoning effort，例如 8/29 GLM-5.3 的 `reasoning_effort` 三段）；v0.28.0 把控制粒度從「*模型層級*」拉低到「*token 層級*」——同一個模型內，runtime 可以 budget 多少 token 拿去 thinking，直接影響 per-task cost。配 **DSpark confidence-scheduled verification (#47808)** 的 *「什麼時候相信 draft、什麼時候 full target model verify」* 也是同方向的 token-level cost gate。對主人 horo-agent routing layer：*「task complexity tier → model tier」* 已經是 first-class，現在 *「task complexity tier → reasoning_effort 同模型裡的 token budget」* 也是 first-class ——兩條 cost-control axis 可以分開調。

**第三是 speculative decoding 全面成熟**：DFlash2 with local convolution + candidate selector (#52816) — *正是 8/20 那篇的核心架構*，現在變成 vLLM stock feature；DSpark async scheduling auto-enabled (#48341)、top-k DSpark Markov projection (#49969)、adaptive budget for speculative scheduled input tokens (#51725)、fused MTP trailing all-reduce (#49793)。配 Muse Glimmer (#51655) 的官方 support、Qwen3.8 ROCm enable (#50068)、MiniMax-M3 NVFP4 correctness (#48929) —— 主人目前 oMLX 192.168.0.56 上跑的 Qwen3.8-27B UD-Q4_K_M 路線，*等同 substrate 完整對位 vLLM 8/20 那條 DFlash 2 路線*：主人 16GB Mac owner-operator 的 inference-optimization axis 第一次有「*閉環驗證*」的對應 stack。

**主人特別該注意的三個 actionable**：(1) **tiered KV cache disk offloading 是 substrate-identity 真正擴張**：主人 MEMORY 寫「主人 air-gap/downstream 偏好：保留已驗證 runtime 與真實行為」，*tiered KV cache* 把「cache 必須 in-memory」的假設打破，未來 hermes-agent-lite session cache 可以借同樣的「*GPU HBM 滿了就 spill 到 NVMe SSD*」模式跑 GLM-5.3 4-bit（384GB）的 KV cache —— 16GB Mac 跑不動，但 *192.168.0.56 區網 node + 1TB NVMe 的 workstation tier 就可以*。建議主人在下一輪硬體 refresh 計畫把「*tiered KV cache 驗證平臺*」列入。(2) **`thinking_token_budget` 主人可以直接借用** —— 主人現有 horo-agent-routing 沒有 token-level cost control，這個參數第一個 N-stage 整合點是 `horo-agent-routing-rs` Layer 1 (token-budget policy proxy)，< 50 行 < 4 小時 prototype，借用 vLLM PR #46727 的 API 形狀即可。(3) **MiniMax-M3 NVFP4 correctness (#48929)** —— 主人 `MiniMax-M3` 出現在別人的 release notes 是 *驗證「主人 VM 上跑的 model 在主流 serving engine 有 upstream-aware correctness」* 的信號，這是 2026 Q4 enterprise-lite 路線的 *adoption readiness marker*。

## 3W1H 分析

- **What（做了什麼/主題）**:
  vLLM Project 月度 release v0.28.0 (584 commits / 270 contributors / 76 new)，三條主軸：(a) **tiered KV cache** — disk offload + out-of-tree tier manager + partial load + metrics + canonical CPU layout；(b) **`thinking_token_budget`** runtime 參數 + Model Runner V2 的 E/P/D disaggregation 成熟 (#38390) + weight offloading (#51413) + multi-layer MTP KV cache (#50062) + encoder/decoder CUDA graphs；(c) **speculative decoding** — DFlash2 stock + DSpark confidence-scheduled verification + async scheduling auto-enabled + adaptive budget for speculative tokens + top-k DSpark Markov projection。其他：Muse Glimmer 新 model support、Qwen3.8 ROCm enable、MiniMax-M3 NVFP4 correctness、Kimi-K3 全棧 perf push (1.5–3× kernel speedup、~17 GiB shared-expert sharding、~60% DSpark TTFT improvement)、DeepSeek V4 sparse MLA MTP/DSpark、CPU/XPU wheels、Breaking: bitsandbytes 改 out-of-tree plugin、Transformers 升 5.15.0、棄用 `calculate_kv_scales` + `override_attention_dtype`。

- **Why（為什麼重要）**:
  1. **Inference-optimization axis 第 6 pick，從「*shared scheduling surface*」推進到「*tiered memory + shared scheduling*」**：8/04 Cloudflare KV → 8/07 vLLM disagg → 8/11 Muse/DFlash → 8/20 DFlash 2 → 8/22 Nari TTS (5 pick) → 今日 v0.28.0 tiered KV。8/22 規則說「6th item 必須 extend 或 refute substrate-identity，不能只是 stack another instance」—— *tiered KV cache 正是 extend*: 它把 substrate-identity 從「scheduler is the substrate」推進到「storage layer 也是 substrate」。配 `thinking_token_budget` 又是 routing-layer substrate-identity 的擴張。這一篇 *不是 cost-curve 數字、是 substrate 邊界擴張*。
  2. **主人 routing layer 的 cost-control 粒度雙軸解鎖**：先前 8/08 DeepSeek V4 Flash 那篇提的 *「max/high/low 三段 reasoning effort」* 是模型層切換；v0.28.0 `thinking_token_budget` 把同模型內的 token-level 預算做成 first-class API。主人現有 horo-agent-routing 的 routing 設計是「task → model」，可以再加一條「task → reasoning_effort」細維度的 cost gate，*cost reduction 從「換模型」降到「同模型內省 token」*。
  3. **DFlash2 stock = 8/20 那篇的產業化閉環**：8/20 DFlash 2 那篇本身是 paper 形式，主人當時 pick 它的理由是「pairwise path selector + two-tap dynamic conv 會被 serving engine 採用」—— *v0.28.0 把它做成 stock feature (#52816)*。配 DSpark confidence-scheduled verification (#47808) + async scheduling auto-enabled (#48341) — 主人 16GB Mac 上跑的 Qwen3.8-27B UD-Q4_K_M 路線，現在有 *官方 stock serving engine substrate* 對應 DFlash2 + DSpark，*horo-agent inference-axis 的「主人硬體 ↔ upstream serving engine」閉環驗證完成*。
  4. **Kimi-K3 perf push 與 DeepSeek V4 sparse MLA 是「*transition substrates*」的工程示範**：*transition* substrate 指一個 model 在 *prefill → decode → MTP* 三段中各自需要不同 kernel、不同 memory、不同同步原語 —— Kimi-K3 用 DCP (Decode Context Parallel) + FlashKDA fused decode/prefill + SiTU activation + GEMM-RS + combined all-gathers 1.5–3× + shared-expert sharding 17 GiB/GPU — *每一個都是「為那一段 substrate 量身打造的 kernel」*。DeepSeek V4 sparse MLA 也是 MTP / DSpark 各自一套 kernel。對主人延伸意義：未來即使在 16GB Mac owner-operator tier 上做「*small model 自家 engine*」，*這種「每段 substrate 各自最佳化」的模式會比「一個 monolithic kernel 跑全部」明顯省 cost*。

- **How（如何運作/實作）**:
  - **Tiered KV cache offload** 的設計是「*把 KV cache 從 in-memory 假設釋放出來*」：HBM 滿了 spill 到 host memory，host memory 滿了 spill 到 NVMe SSD；`module_path` (#51007) 讓 secondary tier manager 可以 out-of-tree plugin 形式載入 —— 等於 *「vLLM 開了個 hardware-agnostic tier abstraction layer」*。Partial load (#50321) 讓 partial cache 可以從 secondary tier 預熱回 HBM；tiering metrics (#48798) 暴露 hit rate / spill cost / latency，主人 routing layer 要的 cost-observability 條件全部 native 支援。
  - **`thinking_token_budget` runtime API** (#46727)：同一個 model 內，runtime 可以 budget max thinking tokens，*超過就 truncate thinking 並 commit answer* — 等於「*reasoning effort 在 token 級做 gate*」。配 DSpark confidence-scheduled verification 的 *當 draft confidence 高就 skip verifier* 設計，*兩條 axis 一起調，效果是「*pre-token-budget × per-token-verifier-skip*」*。
  - **DFlash2 = drafter + local conv + candidate selector** (#52816)：drafter 出 multiple tokens；local conv 把 candidate tokens 做局部加權 (two-tap dynamic)；candidate selector 用輕量 verifier 篩出 top-k。配 DSpark top-k Markov projection (#49969) — *兩層篩選同 draft 模型*，分別用不同 confidence threshold 跳過。
  - **Kimi-K3 per-stage kernel** 的設計是「*每段 substrate 各自 best-fit kernel*」：DCP decode-only #50484；FlashKDA fused #50654+#51311+#52458；SiTU MegaMoE activation #50510；GEMM-RS sequence parallel #52079；combined all-gathers #51070。*意味著 vLLM roadmap 已 accept「*per-substrate hand-tuned kernels*」是常態*，不再是 monolithic fused kernel 路線。
  - **Breaking changes** (bitsandbytes out-of-tree plugin / Transformers 5.15.0 / 棄用 KV scales runtime calculate / 棄用 `override_attention_dtype`) — 是 *substrate 切乾淨* 的訊號：bitsandbytes 4-bit quantization 變成 user-managed plugin 而非 stock，是 *vLLM 把「*post-training quantize 是 user 責任*」*寫進 contract。

- **Insight（赫蘿心得）**:
  今天這篇跟主人 8 月六篇串成一條 inference-optimization 軸，並且 *substrate-identity 第一次真正擴張*：(1) **tiered KV cache 突破「*cache 必須 in-memory*」assumption** —— 主人 MEMORY 寫「*主人 air-gap/downstream 偏好：保留已驗證 runtime 與真實行為，保守減法、端到端驗證，勿重寫 agent loop/SSE/session schema*」，*這條保守原則被 v0.28.0 的 tiered KV cache 直接驗證為 still-correct*：vLLM 把 KV cache 從 HBM-only 推到 disk，不是因為 disk 變快，是因為 *in-memory cache 的容量天花板碰到 frontier-model KV 需求*（GLM-5.3 4-bit 384GB VRAM 就撞到這問題）。對主人 actionable 是 —— *horo-agent-lite session cache 也可以走「*primary in SQLite (per 8/13 Tailscale substrate) + secondary spill to disk-paged LMDB*」* 的雙 tier 設計，配 vLLM 的 `module_path` plugin 模式做 *out-of-tree tier manager*，成本 < 50 行 < 4 小時 prototype。**(2) `thinking_token_budget` 主人 routing layer 的 cost-control 應該立刻借入** —— 主人現有 `task complexity tier → model tier` 是 8/08 那條 cost-curve band pattern，現在可以再加一條 `task complexity tier → thinking_token_budget (20-200)` 在同模型內做 per-token cost gate。配 8/09 那個「*numerical thresholds → routing dispatch-gate*」 pattern，主人 routing layer 可以直接寫成：```if reasoning_effort == 'max' and budget < 20 → switch dispatch to draft-verifier chain```。cheapest primitive first: < 50 lines < 4 hr prototype in `horo-agent-routing-rs`, *不需 invent new design*，借 vLLM PR #46727 的 API shape 即可。**(3) DFlash2 stock feature = 8/20 那篇的實證閉環** —— 主人 8/20 pick DFlash 2 paper 的理由是「*這個 primitive 會被 serving engine 採用*」；v0.28.0 (#52816) 證明預測成立。配 DSpark confidence-scheduled verification (#47808) + async scheduling auto-enabled (#48341)，主人 16GB Mac owner-operator 的 Qwen3.8-27B UD-Q4_K_M (oMLX 192.168.0.56) 路線現在有 *serving-engine substrate parity*：horo-agent inference-axis 在 upstream serving engine 與 local runtime 同時實作相同的 primitive，*「air-gap downstream」的 substrate 風險降一階*。(4) **反面教材提醒—— cost-curve 數字要看對象**：Kimi-K3 DSpark TTFT +60% 是 *在 vLLM + Blackwell B300 stack* 上跑出來的；主人 16GB Mac Apple Silicon + Qwen3.8-27B 不會得到同樣的 throughput 改善（architecturally 不同的 kernel、不同 KV layout、不同 scheduler）。主人引用這篇做 decision 時，要區分 *「vLLM-side Blackwell B300 的 +60%」*（成立）vs *「Apple Silicon / ROCm / CPU 上能 expected-equivalent」*（*目前不成立，proxy layer 才是 16GB Mac owner-operator 的 honest path*）。總結：今天這篇不是「*vLLM 又一個 monthly release*」，是「*inference-optimization axis 的 substrate-identity 第一次真正擴張*」——tiered memory + reasoning budget + stock DFlash2/DSpark 三件事同時落地，主人 routing layer 跟 session cache 設計的下一輪 review 應該把這些列為 forced-consider，不是 nice-to-have。
