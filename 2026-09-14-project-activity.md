---
title: GitHub 自由探索 2026-09-14（14:00 台北時間）
date: 2026-09-14
tags:
  - github-activity-patrol
  - 14-00
  - free-exploration
sources:
  - GitHub Trending daily（Tier-A top 3 — 排除前 7 天 repeat）
  - web_search 當日 AI 開源新聞（Tier-B web 探索 2）
  - GitHub REST API `/repos/<slug>` + `/releases/latest` + raw `README.md` + `LICENSE`
---

# GitHub 專案動態

- 檢查時間：2026-09-14（14:00 台北時間）
- 檢查對象：`JustVugg/colibri` / `ever-co/ever-gauzy` / `calesthio/OpenMontage` / `langwatch/langwatch` / `modelcontextprotocol/typescript-sdk`
- 來源組合：GitHub Trending today 共解析 19 筆，**排除前 7 天已寫過的 repeat**（rank 3 `bilawalsidhu/gods-eye-view`（09-11）+ rank 4 `tech-leads-club/agent-skills`（今日 PM 已開）+ rank 5 `melgarafael/DeskcommCRM`（09-12）+ rank 7 `asgeirtj/system_prompts_leaks`（09-09）+ rank 9 `multimodal-art-projection/YuE`（09-13）+ rank 10 `yuliskov/SmartTube`（09-12）+ rank 13 `SnailSploit/Claude-Red`（09-13）+ rank 15 `jihe520/MathModelAgent`（09-08）），**fresh top 3 = rank 1 `JustVugg/colibri` + rank 2 `ever-co/ever-gauzy` + rank 6 `calesthio/OpenMontage`**（domain 幾乎不重疊：frontier MoE 推論引擎 / ERP-CRM-HRM 開源商業平台 / agentic video production；rank 8 `vxcontrol/pentagi`、rank 11 `alphaXiv/OpenResearch`、rank 12 `debpalash/VoiceStudio`、rank 14 `alibaba/open-code-review`、rank 16 `tonhowtf/omniget` 雖然 fresh 但都已落入 tier-A 3 名後，且與今日 tier-B 兩條路線（MCP SDK + LLM observability）互補性較弱，被跳過）。Tier-B web_search 當週 AI 開源新聞取到 **`langwatch/langwatch`**（typescript-sdk@v1.15.0 + agent-plugin@v1.2.0 同步 release 於 2026-09-13、Apache-2.0 + Enterprise open-core、4.8k★、`npx @langwatch/server` 一行 self-host、coding-agent 監控 + LLM observability + AI gateway virtual keys 四象限）+ **`modelcontextprotocol/typescript-sdk`**（canonical MCP TS SDK、v1.30.0 + `@modelcontextprotocol/server@2.0.0` 等九個 v2.0.0 packages 同步 release 於 2026-07-27、MCP 2026-07-28 stateless spec 對位、Standard Schema + Zod v4 / Valibot / ArkType），兩者都已用 `curl https://api.github.com/repos/<slug>` 驗證 HTTP 200 + metadata 非空才寫入。

## Repo 摘要與 3W1H

### `JustVugg/colibri`

- **Repo 摘要：** Colibrì — pure C 的 frontier MoE 推論引擎，主打「**AI memory multitiering**」：把 VRAM/RAM/NVMe 視為單一推論層級，experts 從 disk streaming 進來，目標是「讓 744B–2.8T MoE 模型跑在你自己已有的硬體上」。30,311★、**C**、14.8 MB（source，binary 很小）、**Apache-2.0**、`created` 2026-07-01、`pushed_at` 2026-09-14 00:59 UTC（今天仍在 push）、README 三語系（English / 簡中 / 繁中 / Italian）。目前支援 GLM-5.2/5.3、GLM-5.3-Flash、Inkling、Kimi K3、DeepSeek V4/V4.1 Flash、Qwen3.8-Flash-Next、Qwen3.6、OLMoE 共 9 個 MoE family。**v1.11.0 release 已是昨天**——**在 Trending rank 1 一天就 +868 stars 是罕見 momentum**。
- **3W1H：**
  - **What：** 兩個東西：(1) `colibri` / `colibri.exe` 純 C 推論 binary（依賴只有 gcc+OpenMP，**zero engine dependencies**），(2) `coli` Python launcher（CLI / API gateway / web dashboard / brain 視覺化）。三大 subcommand：`./coli chat` / `./coli serve` / `./coli web`（dashboard 有 Brain page — 19,456 個 experts 顯示 routing heat、Atlas page — measured expert atlas 3-D galaxy、live metrics、TTFT 1.6s 跑 744B 可達 4 tok/s）。
  - **Why：** 真正解 frontier MoE 的 **hardware dependency**——hyperscaler 級 GPU 不是人人有，但 372 GB int4 GLM-5.2 + 兩條 SSD + 一些 RAM/VRAM 就能跑。Colibrì 推**end-to-end correctness gates**而不是 microbenchmark，並明寫「no SLA on speed, hard guarantee on semantics」——這是從 v1.x 一路 quality 回歸（per-row int4 9pp worse、MTP head int4 0% acceptance）學到的教訓。**真正賣點**：「你可以讀懂、改寫、量測自己的 inference engine」—— 一個 C 檔對應一個 model family，aggressive optimization 不需要重新編譯整個 PyTorch stack。
  - **Who：** 想要 **「frontier model on local hardware」** 的研究者/獨立開發者、需要 audit model semantics 的 alignment team、想 understand expert routing 行為的人、有大 SSD 但少 VRAM 的人（multitier 是答案）。**不適用於**：想要 production SLA（README 明寫 no SLA on speed）、要 fast interactive 推論的人（純 CPU streaming 模式不適合 chat latency 敏感場景）、想跑 dense model 的人（multitier 主要 optimize MoE 專家調度）。
  - **How：** 兩步：(1) **Get colibri**：從 [Releases](https://github.com/JustVugg/colibri/releases) 下載對應平台 archive，解開後 `python3 coli info` 驗證 engine ready；或 `git clone && cd colibri/c && ./setup.sh` 從 source 編（gcc + OpenMP）。要 `coli` 在 PATH：`pip install -e .`（editable，engine 仍在 `c/`）。(2) **Get the model**：HF `mastouri/GLM-5.2-colibri-int4-g64-with-int8-mtp` 約 372 GB（**gs64 build 是 quality gate**，不是舊的 per-row int4 mirrors），或 `./coli convert --model /nvme/glm52_i4` shard-by-shard 自轉（從 FP8 source，不需 756 GB 全在 disk）。
- **安裝方式：**
  - **下載 prebuilt release（官方主推，無 compiler）：** 從 <https://github.com/JustVugg/colibri/releases> 取對應平台的 `.tar.gz`（Linux x86_64 / macOS / Windows），解開後 `python3 coli info` 確認 engine ready。**依賴**：Python 3（launcher 用）+ C runtime（macOS / Linux 內建）；engine 本身 **zero deps**。
  - **從 source build：** `git clone https://github.com/JustVugg/colibri && cd colibri/c && ./setup.sh`（自動檢 gcc + OpenMP + build + self-tests）。要 `coli` 全域可用：`pip install -e .`（**editable install**，不是 PyPI wheel）。
  - **Model 取得（Hugging Face）：** `huggingface-cli download mastouri/GLM-5.2-colibri-int4-g64-with-int8-mtp`（約 372 GB int4，**int8 MTP head** 是 acceptance gate）。或 `./coli convert --model /nvme/glm52_i4` 自 FP8 source shard-by-shard 轉。
  - **硬體要求（**硬性**）：能跑 744B int4 MoE 的硬體——主要是 disk 空間（372 GB）+ RAM 夠 streaming buffer + （optional）VRAM 加速；CPU 模式要 AVX2/AVX-512。**README 明列**：int8 MTP head 必須 int8（int4 → 0% acceptance）、必須用 gs64 build（per-row int4 mirrors 9pp quality regression）。
  - **License：Apache-2.0（標準 SPDX）**，對主人 horo-agent / horo-webui air-gapped downstream 是 case-A1，含專利授權。**Air-Gapped warning**：自架 model 容器後所有 inference 都在本地，不需外網；HF download 是一次性。
- **近期 release：** **v1.11.0 — "colibri v1.11.0"，published 2026-09-13 13:44:10 UTC（昨天）**。release 觸發 Trending rank 1 + 868 stars/day momentum。前 8 個 release cadence（從 GitHub API）：v1.11.0（09-13）/ v1.10.x / v1.9.x / v1.8.x / ... / v0.x 起步至 7 月初 `created` 2026-07-01——**兩個半月從 0 到 30k stars + 11 個 minor versions**，是 5 個 repo 裡 momentum 與年輕度反差最大者（Y combinator–style growth 但 Rust-free、純 C、單人主要 maintainer JustVugg）。

### `ever-co/ever-gauzy`

- **Repo 摘要：** Ever® Gauzy™ — 開源 **Business Management Platform（ERP / CRM / HRM / ATS / PM）** for collaborative / on-demand / sharing economies，TypeScript monorepo（NestJS API + Angular / Nx 客戶端 + React Native Expo mobile + desktop Electron apps）。5,373★、TypeScript、260 MB、**AGPL-3.0**、`created` 2019-06-02、`pushed_at` 2026-09-14 02:33 UTC（今天仍在 push）。最新 release `v111.0.11` 是 2026-07-04——**release cadence 偏慢（~2 個月）但 main branch 每天 push**，是「released-stable + main-active」雙軌專案的典型型態。背後公司 ever.co 是個**多 product 開源工作室**（Gauzy 是其中一個，還有 Ever Teams / Ever Works）。
- **3W1H：**
  - **What：** 一個 monorepo（`apps/` + `packages/` + `api/` + `desktop/` + `mobile/`）打包整個商業營運 stack：HRM（員工 / 工時 / 績效 / 招募 / on-boarding）、CRM（聯絡人 / sales pipeline / proposal / 發票）、ERP（inventory / supply chain / 生產）、PM（任務 / 目標 / KPI）、財務會計（多幣別 / 發票 / 付款 / 收支）、desktop timer app、knowledge base、reporting。Demo 站 <https://demo.gauzy.co>（admin@ever.co / admin），Docker Compose 一鍵跑（含 PostgreSQL / OpenSearch / MinIO / Redis / Cube / Zipkin / Jitsu / Pgweb 全套 infra）。
  - **Why：** 「**open-source business management for on-demand / sharing economy**」是一個 niche — SaaS（Kommo、Octadesk、Intercom 等）鎖資料 + 月費，**AGPL-3.0 開源 + self-host + white-label** 的選擇很少；Gauzy 對 freelancer agency、studio、on-demand marketplace、in-house team 想自架 ERP 的人是 one of the few choices。**差異化**：HRM + Time-Tracking + Activity Tracking + Equipment Sharing（on-demand economy 核心需求）+ Multi-org / Multi-currency / Multi-lingual，**不是只做 CRM 或只做 HR**。
  - **Who：** freelancer / agency / studio / in-house team 想自架商業平台的人、on-demand marketplace operator、需要 white-label ERP 給客戶的 SaaS reseller、做 sharing economy 的公司（短租、共用工作空間、共用設備）。**不適用於**：只想做「簡單 CRM」（HubSpot / Pipedrive 比 Gauzy 簡單 10×）、不想自架 PostgreSQL + OpenSearch + MinIO 的小團隊（Gauzy infra dependencies **重**，demo 起碼 5 個 container）、Windows-only team（README 強烈建議 Docker + Linux/macOS）。
  - **How：** 三條路：(a) **Demo**：<https://demo.gauzy.co>（每日 reset），admin@ever.co / admin。(b) **Docker Compose（推薦）**：`git clone` → `.env.compose` 改 DB 設定 → `docker-compose -f docker-compose.yml up -d`，起 PostgreSQL + OpenSearch + MinIO + Redis + Cube + Zipkin + Jitsu + API + Web UI，UI 在 <http://localhost:4200>、API 在 <http://localhost:3000/api>。(c) **手動 build（進階）**：Node 22+ / Yarn 1.22.x → `yarn bootstrap` → `.env.local` 改設定 → `yarn start`，UI / API 同 docker-compose。
- **安裝方式：**
  - **Docker Compose（官方主推，快速 demo + dev）：** `git clone https://github.com/ever-co/ever-gauzy && cd ever-co/ever-gauzy` → 編 `.env.compose`（DB type / credentials）→ `docker-compose -f docker-compose.yml up -d`（起 API + Web UI + PostgreSQL + OpenSearch + MinIO + Redis + Cube + Zipkin + Jitsu，Pgweb 在 :8081、OpenSearch Dashboards 在 :5601）。**首次啟動**：DB 自動 seed fake data，要等。
  - **Demo 站（最快，無安裝）：** <https://demo.gauzy.co>，admin@ever.co / admin 登入後體驗全部功能；DB 每天 reset。
  - **手動 build（開發者）：** Node 22 LTS + Yarn 1.22.x → `git clone` → `yarn bootstrap`（= install + workspace setup）→ `yarn prepare:husky`（git hooks）→ `.env.local` 改 DB / 設定 → `yarn start`（同時啟 API + UI）。要重置 DB：`yarn seed`（warning: unsafe for production）；要 fake data：`yarn seed:all`（warning: takes very long）。
  - **Desktop App：** 從 <https://gauzy.co/downloads> 下載對應平台（Windows / macOS / Linux）的 Desktop Timer / 全功能 app。
  - **從 source（deep dev）：** monorepo Nx workspace，`apps/gauzy`（Angular web）/ `apps/api`（NestJS）/ `apps/desktop-timer`（Electron）/ `packages/contracts` / `packages/ui-core` 等。
  - **未找到明確 pip/npm 套件安裝方式**（AGPL-3.0 + 自家 hosting 為主，未 publish 至 PyPI 或 npm registry 作為單一 package）——這是「open-core self-hosted platform」典型 pattern，不希望被當成 library 嵌入第三方。
  - **License：AGPL-3.0（含 copyleft + 網路服務條款）**——對主人 horo-agent / horo-webui downstream 是 **case-H warning**：可以讀、可以自架、可以改、**但若 modified 版本對外提供網路服務必須 source-available 整個 modified stack**。自架 + air-gapped + 單機使用 OK；要商業閉源 fork 或對外 SaaS 用 AGPL 程式碼觸礁。
- **近期 release：** **v111.0.11 — "v111.0.11"，published 2026-07-04 20:25:10 UTC（約 2.5 個月前）**。`v111.x` major 命名 convention 是「release cycle 版本號」（不是 semver），每 ~2 個月推一版。前 release cadence（從 GitHub API）：v111.0.11（07-04）/ v110.x / v109.x... 等 historic。**release tag 慢但 main branch 每天 push**（`pushed_at` 今天 + `updated_at` 今天）——這是 mature enterprise platform 的「stable tagged releases + continuously integrated main」雙軌 pattern。**未找到 GitHub 官方 change log 給定 release notes 摘要**，README 主要寫 What's New 段而不是 inline 在 release tag。

### `calesthio/OpenMontage`

- **Repo 摘要：** OpenMontage —「**世界首個 open-source, agentic video production system**」，把 AI coding assistant（Claude Code / Cursor / Copilot / Windsurf / Codex）變成 full video production studio：12 條 production pipelines、100+ tools、700+ agent skill & production-knowledge files，agent 從研究 / 寫腳本 / 生資產 / 剪輯 / 配音 / 字幕到 render 全部自己來。58,683★、Python、76 MB、**AGPL-3.0**、`created` 2026-03-29、`pushed_at` 2026-09-06 05:02 UTC、`updated_at` 2026-09-14。**未找到 GitHub release**（無 tag / pre-release），但 README 寫著「🏆 #1 Repository of the Day on GitHub Trending」徽章——**trending 動能不靠 release cadence 撐，靠 video demo + 6 個完整 sample films**（「SIGNAL FROM TOMORROW」、「THE LAST BANANA」$1.33 成本、「OBJECTS IN OVERDRIVE」自 Blender 物理）。
- **3W1H：**
  - **What：** 一個 Python + Remotion (Node.js / React) hybrid 系統——`pipeline_defs/` 是 12 條 production pipelines（image-based / real-footage / 3D / vertical transformation / cinematic trailer 等）、`skills/pipelines/` 是 stage director skills（agent 在每個 pipeline stage 用）、`tools/tool_registry` 統一所有 tools（`registry.discover()` + `registry.support_envelope()` + `registry.provider_menu()` 三個 introspection API 給 agent 看）、`remotion-composer/` 是 Remotion 影片組裝渲染。**支援 providers**：FLUX / Google Veo / Kling v3 / MiniMax video / Recraft / Atlas Cloud / Suno / ElevenLabs / OpenAI TTS / Pexels / Pixabay / Unsplash / Blender Eevee/Cycles / FFmpeg。**完整 self-review pipeline**：ffprobe validation / frame sampling / audio level analysis / subtitle checks / 7-dimension provider scoring + auditable decision log / every creative decision 要 user approve。
  - **Why：** 「**AI coding assistant 變 creative production studio**」這條路線在 2026 Q3 才剛成形——Sora / Veo / Kling 把生成成本壓低，但「**端到端 video production with creative control**」仍要人手拼 prompt / provider / cut。OpenMontage 把這堆 SOP 做成「agent + provider menu + approval gates」三層，且支援「real footage」path（用 Pexels / Pixabay / Unsplash 開源素材，不用 AI 生成），對不喜歡 AI 生成「fake-looking video」的人是真需求。**對主人 SOUL「對遊戲/視覺型任務採視覺優先哲學」直接命中**：「邊做邊看」是 OpenMontage 的 first-class design（每個 creative decision 都要 user approve，不黑盒）。
  - **Who：** AI video producer / content creator / 個人創作者想用 Claude Code / Cursor agent 當 video editor / 對 AI 生成 video quality 不滿意想走 hybrid 路線的人 / 想「agentic creative pipeline」教學示範的研究者。**不適用於**：完全不想用 AI provider（必須有 FAL / Atlas Cloud / Kling / Suno / ElevenLabs API key 才能跑完整 pipeline）、不想裝 Node.js / FFmpeg / Python 3.10+（4 個硬依賴）、要 production SaaS（這是 self-host agentic framework，不是 hosted service）。
  - **How：** `git clone` → `cd OpenMontage` → `make setup`（macOS/Linux），或 Windows PowerShell：`python3 -m venv .venv && .\.venv\Scripts\Activate.ps1 && python -m pip install -r requirements.txt && cd remotion-composer && npm install && cd .. && python -m pip install piper-tts && Copy-Item .env.example .env`。然後**在 Claude Code / Cursor / Copilot / Windsurf / Codex 開啟此資料夾**，輸入：`"Make a 60-second animated explainer about how neural networks learn"`——agent 自己跑完整 pipeline。**Agent 開發者**：`python -c "from tools.tool_registry import registry; import json; registry.discover(); print(json.dumps(registry.support_envelope(), indent=2))"` 看 capability envelope。
- **安裝方式：**
  - **Make（macOS / Linux 官方主推）：** `git clone https://github.com/calesthio/OpenMontage && cd OpenMontage && make setup`。Make 內部會建 venv、pip install requirements、cd remotion-composer && npm install、pip install piper-tts、cp .env.example .env。
  - **手動（無 Make）：** `python3 -m venv .venv && source .venv/bin/activate && python -m pip install -r requirements.txt && cd remotion-composer && npm install && cd .. && python -m pip install piper-tts && cp .env.example .env`。**Windows PowerShell 版本**：把 `&&` 改 `;`、用 `py -3 -m venv .venv`、用 `Copy-Item .env.example .env`。
  - **Windows `npm install` 故障排除：** 若出現 `ERR_INVALID_ARG_TYPE`，改用 `npx --yes npm install`。
  - **前置依賴（**硬性**）：** Python 3.10+ + FFmpeg（`brew install ffmpeg` / `sudo apt install ffmpeg`）+ Node.js 18+ + AI coding assistant（Claude Code / Cursor / Copilot / Windsurf / Codex 任一）。
  - **API Keys（optional, more keys = more tools）：** `.env` 填 `FAL_KEY`（FLUX images + Veo / Kling / MiniMax video）/ `ATLASCLOUD_API_KEY` / `KLING_API_KEY` / `PEXELS_API_KEY` / `PIXABAY_API_KEY` / `UNSPLASH_ACCESS_KEY` / `SUNO_API_KEY` / `ELEVENLABS_API_KEY` / `OPENAI_API_KEY` / `XAI_API_KEY` 等——README 完整列出。
  - **未找到明確 pip/npm 套件安裝方式**（OpenMontage 本質是 self-host agentic framework，不是 library，不上 PyPI / npm）。
  - **License：AGPL-3.0（含 copyleft + 網路服務條款）**——對主人 horo-agent / horo-webui downstream 是 **case-H warning**：自架 + air-gapped + 改 fork 內部使用 OK，但若對外提供網路服務觸礁。
- **近期 release：** **未找到 GitHub release**（無 tag、無 pre-release）。`pushed_at` 是 2026-09-06（8 天前），README 仍有活——「release-less repo 但 active development」的典型 pattern：OpenMontage 走「video demo 為主要 release artifact」而不是 git tag。8 天前 push 對這類 agentic framework 來說仍 active（CI / doc updates / sample video 更新），但相比 `colibri`（昨天 release）或 `langwatch`（昨天 release）明顯不是 release-driven momentum。**如要 release 訊號：8 天前最後 main commit** —— 不是 release 暫缺，而是「release 這個動作在這個 repo 不重要」。

### `langwatch/langwatch`

- **Repo 摘要：** LangWatch — **open-core LLM observability + agent testing + AI governance platform**，把「AI 在 production」拆成四象限：**LLM Ops**（trace / 評測 / prompt management / dataset）/ **Coding Agents**（cost per PR / per team、privacy controls）/ **AI Gateway**（OpenAI / Anthropic 兼容 endpoint、virtual keys with budgets、routing）/ **AI Governance**（Copilot Studio / Claude Cowork 合規 ingestion、anomaly rules、誰用什麼工具）。4,770★、TypeScript、640 MB、**Apache-2.0 core + Enterprise extension**（open-core）、`created` 2023-09-09、`pushed_at` 2026-09-14 05:55 UTC（今天仍在 push）、**2026-09-13 一天內釋出 5 個 packages**（typescript-sdk@v1.15.0、agent-plugin@v1.2.0、mcp-server@v2.1.0、langevals@v3.1.0、clickhouse-serverless@v0.4.0 等）——**monorepo 多 package 同步 release 是這類 observability platform 的典型 pattern**。
- **3W1H：**
  - **What：** 一個 monorepo（多個 `sdks/python/` + `sdks/typescript/` + `sdks/go/` + `mcp-server/` + `agent-plugin/` + `langevals/` + `clickhouse-serverless/` + 主 LangWatch app + ClickHouse 圖形 backend）。三大 user surface：(1) **LangWatch Cloud**（<https://app.langwatch.ai>，hosted SaaS），(2) **Self-host**：`npx @langwatch/server`（**只要 Node.js**），(3) **Coding agent CLI**：`npx langwatch claude` / `codex` / `copilot` / `opencode`（一行 attach observability）。
  - **Why：** 「**AI 在 production**」四象限（developer observability / coding-agent cost / model routing / enterprise governance）從前要湊 4 個 SaaS（Langfuse / Helicone / Portkey / Vellum 之類），LangWatch 想把它們整合成 one platform——open-source core + Enterprise 進階。**對主人 SOUL「多代理偏好 default+Qwen 協調 + GPT審查」直接命中**：`npx langwatch claude` 是把 coding agent 接到 observability 的一行 CLI，是主人 Hermes / horo-agent 內部用得上的 tool；`sdks/python/integrations/dspy`（DSPy）+ `sdks/typescript/integrations/mastra` 等整合覆蓋主人工作鏈主流 framework。
  - **Who：** 想 observability 自己 LLM call 的 developer、需要管 coding agent 成本 / token budget 的 engineering manager、需要 routing（OpenAI ↔ Anthropic ↔ Bedrock ↔ 自架 vLLM）的 platform team、需要 governance（Copilot Studio usage / Claude Cowork compliance）的 IT/Compliance team、想做 agent simulation testing 的 AI engineer。**不適用於**：完全不用 LLM 的人、想自架完整 observability + governance 但不需要 Enterprise 的小團隊（核心 Apache-2.0 已夠，要 Enterprise 走 sales）。
  - **How：** (a) **Cloud**：sign up <https://app.langwatch.ai>，裝 SDK `pip install langwatch` 或 `npm install langwatch`，auto-tracing LLM call。(b) **Self-host**：`npx @langwatch/server`（**只要 Node.js 22+，其他 self-host infra 都內建**），production 完整版見 <https://langwatch.ai/docs/self-hosting/overview>。(c) **Coding agent attach**：`npx langwatch claude` 一行包 Claude Code → sessions / cost / token 都進 LangWatch。
- **安裝方式：**
  - **自架 Server（最簡，**只要 Node.js**）：** `npx @langwatch/server`（從 npx 啟 LangWatch self-host，Node 22+ 即可；production 完整版見 <https://langwatch.ai/docs/self-hosting/overview>）。
  - **Coding agent CLI attach（**主人重點**）：** `npx langwatch claude`（或 `codex` / `copilot` / `opencode`）——把 coding agent 接到 LangWatch observability。
  - **pip（Python SDK）：** `pip install langwatch`（badge 顯示 PyPI 已 publish，monthly downloads 顯示活躍）。
  - **npm（TypeScript SDK）：** `npm install langwatch`（badge 顯示 npm 已 publish）。
  - **MCP server：** `langwatch` 同時提供 [MCP integration](https://langwatch.ai/docs/integration/mcp) — 把 LangWatch 工具暴露給任何 MCP-compatible agent（Claude Desktop / Cursor / VS Code 等）。
  - **Cloud（最快體驗）：** sign up <https://app.langwatch.ai>，不用自架 infra。
  - **官方分發 package（**自我參照**）：** `agent-plugin@v1.2.0`（2026-09-13）/ `mcp-server@v2.1.0`（2026-09-12）/ `langevals@v3.1.0`（2026-09-12）/ `sdks/go@v1.0.0`（2026-09-09）/ `typescript-sdk@v1.15.0`（2026-09-13）——monorepo 多 package，每個獨立 semver。
  - **License：Apache-2.0 core + Enterprise extension（open-core）**——核心 `langwatch` Python / npm package 是 Apache-2.0，但 self-host 完整功能 / Enterprise SSO / compliance ingestion 等進階功能需要付費 license。對主人 horo-agent / horo-webui air-gapped downstream 是 **case-A1**（核心可商用）+ 注意 Enterprise 部分 self-host 條款要查官方 license matrix。
- **近期 release：** **typescript-sdk@v1.15.0 — "typescript-sdk: v1.15.0"，published 2026-09-13 12:20:58 UTC（昨天）**。**agent-plugin@v1.2.0 — "agent-plugin: v1.2.0"，同日同時間 published**。前 8 個 release cadence（從 GitHub API）：typescript-sdk@v1.15.0 + agent-plugin@v1.2.0（09-13）/ typescript-sdk@v1.14.0 + mcp-server@v2.1.0 + langevals@v3.1.0 + clickhouse-serverless@v0.4.0 + clickhouse-serverless-image@v0.4.0（09-12）/ sdks/go@v1.0.0（09-09）/ typescript-sdk@v1.13.0 + skills@v1.4.0（09-06）——**多 package 同步 release 模式，每週 ~2–4 個 package bump**，是 5 個 repo 裡 release frequency 對 JS/TS SDK 來說屬中上。

### `modelcontextprotocol/typescript-sdk`

- **Repo 摘要：** **官方 MCP TypeScript SDK** — 給 Model Context Protocol（Anthropic 發起的 LLM-to-tools 標準）實作 server / client library，支援 Node.js / Bun / Deno。13,389★、TypeScript、18.9 MB、license `NOASSERTION`（GitHub API 無法解析 SPDX，repo 內 `LICENSE` 檔案是 MIT，per README 徽章顯示 `MIT licensed`）、`created` 2024-09-24、`pushed_at` 2026-09-14 04:09 UTC（今天仍在 push）。**v2.0.0 對應 2026-07-28 MCP spec 的 stateless revision**——**是 MCP 自 2024-11 推出以來最大 protocol revision**，同時把 `@modelcontextprotocol/server`、`@modelcontextprotocol/client`、`@modelcontextprotocol/node`、`@modelcontextprotocol/express`、`@modelcontextprotocol/fastify`、`@modelcontextprotocol/hono`、`@modelcontextprotocol/codemod`、`@modelcontextprotocol/core` 共 9 個 packages 升 v2.0.0（2026-07-27 同步 publish）。
- **3W1H：**
  - **What：** 兩個 core packages + 一組 middleware adapter 的 monorepo：(1) **`@modelcontextprotocol/server`** — 註冊 MCP tools / resources / prompts 的 high-level API + Streamable HTTP / stdio transports + auth helpers；(2) **`@modelcontextprotocol/client`** — transports、high-level helpers、OAuth helpers；(3) **Middleware**：Express / Fastify / Hono / Node.js HTTP 的薄 adapters。**Tool/prompt schema 用 [Standard Schema](https://standardschema.dev/)**——可帶 Zod v4 / Valibot / ArkType 任一個，**不再 lock 在 Zod**。
  - **Why：** MCP 在 2024-11 由 Anthropic 推出後到 2026-07-28 RC 是「largest revision of the protocol since launch」——**移除 session layer（initialize/initialized handshake 拿掉、Mcp-Session-Id header 拿掉）**，MCP servers 可跑在普通 HTTP infra 上，不需要 session 狀態保存。**對主人 SOUL「企業 hermes-webui + air-gapped」直接命中**：stateless MCP 可直接掛在 Cloudflare Workers / Lambda / FaaS 後面，是主人 enterprise-lite + air-gapped downstream 想做 multi-tenant MCP server 的關鍵 enable。**v1.x 仍 receive bug fixes / security updates 至少 6 個月**，v2 不強制 break。
  - **Who：** 想 build MCP server / client 的 developer、用 Express / Fastify / Hono / Node http 的 backend engineer、想 migrate v1.x → v2.x 的早期 MCP adopter、研究 LLM-to-tools protocol 標準的人。**不適用於**：完全不在 LLM / agent ecosystem（這 SDK 專門給 LLM tool-call protocol）、仍在用舊 stdio-only protocol 的客戶端（雖然 `server-legacy@2.0.0` package 仍 support）。
  - **How：** `npm install @modelcontextprotocol/server`（或 bun add / deno add）。起一個 minimal MCP server：`import { McpServer } from '@modelcontextprotocol/server'; import { StdioServerTransport } from '@modelcontextprotocol/server/stdio'; server.registerTool('greet', { description: '...', inputSchema: z.object({ name: z.string() }) }, async ({ name }) => ({ content: [{ type: 'text', text: \`Hello, ${name}!\` }] }))`。完整 tutorial：<https://ts.sdk.modelcontextprotocol.io/v2/>。
- **安裝方式：**
  - **npm（官方主推）：** `npm install @modelcontextprotocol/server`（建 server）/ `npm install @modelcontextprotocol/client`（建 client）。Optional middleware：`npm install @modelcontextprotocol/node` / `@modelcontextprotocol/express express` / `@modelcontextprotocol/fastify fastify` / `@modelcontextprotocol/hono hono`。
  - **bun：** `bun add @modelcontextprotocol/server`（或 `client` / 任一 middleware package）。
  - **deno：** `deno add npm:@modelcontextprotocol/server`（或 `client` / 任一 middleware package）。
  - **v1.x legacy：** `npm install @modelcontextprotocol/server-legacy`（v1.x 仍 receive bug fixes / security updates 至少 6 個月）。
  - **Tool schema 用 Standard Schema：** import Zod v4 `import * as z from 'zod/v4'` 或 Valibot / ArkType 任選——SDK 不再 lock 在 Zod。
  - **License：MIT（per README 徽章 + LICENSE 檔案；GitHub API `NOASSERTION` 因 auto-detection 失敗，正確 SPDX 是 MIT）**——對主人 horo-agent / horo-webui downstream 是 case-A1，**Air-Gapped warning**：MCP server 本身 stateless、可 air-gapped，但 MCP server 通常需要連 LLM provider（OpenAI / Anthropic / Azure / Vertex AI / Bedrock）才能運作——主人 air-gapped 環境下需確認 LLM endpoint 是否走自架 vLLM / Ollama。
- **近期 release：** **1.30.0 — "1.30.0"，published 2026-07-27 17:54:36 UTC（約 1.5 個月前，v1.x bug-fix line）**。**@modelcontextprotocol/server@2.0.0 + @modelcontextprotocol/client@2.0.0 + @modelcontextprotocol/core@2.0.0 + @modelcontextprotocol/node@2.0.0 + @modelcontextprotocol/express@2.0.0 + @modelcontextprotocol/fastify@2.0.0 + @modelcontextprotocol/hono@2.0.0 + @modelcontextprotocol/codemod@2.0.0 + @modelcontextprotocol/server-legacy@2.0.0 — 共 9 個 v2.0.0 packages 同步 published 2026-07-27（after MCP 2026-07-28 spec release）**。**release cadence**：v2.0.0 major（07-27）+ v1.30.0（07-27 同步）+ v1.29.x 系列 bug-fix——**v2 是 stable，v1 仍 receive patch**——典型「major protocol revision 但 backward-compat window」pattern。**release 對 ecosystem 影響**：`addEventListener('blogpost', mcp-2026-07-28-stateless-spec)` ——MCP mainline spec 從 stateful 變 stateless 是 2026 Q3 最大的 protocol 事件。

---

## 重點觀察

- **Tier-A 主題分佈罕見地廣：三條完全不同路線（推論引擎 / 商業平台 / 視訊製作）+ Tier-B 兩條互補路線（LLM observability / MCP SDK）= 5 個 repo 幾乎無 domain 重疊。** Trending rank 1–6 fresh pick 抓到的 3 個：`colibri`（pure C 推論引擎，2.5 個月從 0 到 30k stars）、`ever-gauzy`（7 年老牌 AGPL ERP/CRM/HRM，`v111.x` major 命名）、`OpenMontage`（agentic video production，58k stars 無 release tag）。Tier-B web 抓到的 2 個：`langwatch`（LLM observability open-core）、`typescript-sdk`（canonical MCP SDK，v2 對 stateless protocol）。**5 個 repo 在 domain map 上分屬 5 個不同象限**——是 9 月以來最分散的一次（之前 daily 經常兩三個在 AI agent / orchestrator 路線上 cluster）。**Tier-A 三條路線的共同點**：都是「**單一 take-it-or-leave-it 整套 solution**」（推論引擎 / ERP / video production），不是 library / framework / 工具集——這反映 trending 容易被「完整 demo + 一句話定位」吸走。Tier-B 兩條互補：observability 看 runtime、SDK 看 protocol——是 infrastructure layer 視角。
- **Tier-B 雙聚焦（MCP SDK + LLM observability）對主人 enterprise-lite + air-gapped 主線最直接可借鑑——尤其「**MCP 2026-07-28 stateless spec**」是今日最重 protocol news。** `modelcontextprotocol/typescript-sdk` v2.0.0 對應 MCP spec 2026-07-28——stateless revision 把 session layer 整個拿掉、MCP server 可跑在普通 HTTP infra（FaaS / Cloudflare Workers / Lambda）。**對主人 enterprise-lite + air-gapped downstream 是「把 MCP server 變 stateless」關鍵 enable**——目前主人 horo-agent / horo-webui 若想 expose MCP server 給 multi-tenant 客戶端，stateless pattern 省下 session affinity / sticky routing / server-side state store 的 design cost。**LangWatch 對主人 Hermes / horo-agent 內部直接可用**：`npx langwatch claude` 一行 attach observability 到 Claude Code 呼叫，`npx @langwatch/server` 一行 self-host（**只要 Node.js**）——主人現有 8100 lmstudio-council / 8000 ASMR Foley Lab 已經在用 FastAPI + SSE，把 LangWatch self-host 串進去觀察 token / cost / routing 是 low-friction add。**兩個 tier-B 加起來對今日的 SOP「用模型挑、今天可能要換」命中**——與 09-12 的「use-agent-os + book-to-skill」（agent runtime + skill ecosystem）和 09-13 的「microsoft/agent-framework + superset-sh/superset」（framework + orchestration IDE）形成**「agent infrastructure 三部曲：runtime / orchestrator / observability + protocol」**三階段遞進——主人 enterprise-lite 路線剛好踩中這三條主線。
- **License spectrum 今天拉到 4 段：strict-permissive（2/5: MIT + MIT）→ open-core（1/5: Apache-2.0 + Enterprise）→ copyleft（2/5: AGPL-3.0 + AGPL-3.0）。** 5 個 repo 細分：`colibri` 是 **Apache-2.0**（case-A2，含專利授權）；`typescript-sdk` 是 **MIT**（case-A1）；`langwatch` 是 **Apache-2.0 core + Enterprise extension**（case-A1-OC open-core）；`ever-gauzy` 是 **AGPL-3.0**（case-H copyleft + 網路服務條款）；`OpenMontage` 是 **AGPL-3.0**（case-H copyleft + 網路服務條款）。**主人 `horo-agent` / `horo-webui` air-gapped downstream 必須把 AGPL 兩個視為 case-H**：`ever-gauzy` + `OpenMontage` 可讀、可自架、可 air-gapped 改 fork 內部使用，**但若對外提供網路服務必須 source-available 整個 modified stack**——AGPL 觸礁風險。**商業閉源 fork 不可能**。`colibri` + `typescript-sdk` + `langwatch`（核心）是 permissive，可併入下游商用。**今日最值得注意的 license insight**：`colibri` 是 Apache-2.0 + 純 C + zero deps——**air-gapped + 自主可控**的 inference engine 對主人 SOUL「保守減法、保留已驗證 runtime」是極少見的 license 與技術雙重 permissive case。
- **5 個 repo 語言光譜：C / TypeScript ×3 / Python —— Rust、Go、Rust 各零個，**與 09-13（Rust 1 / TS 1 / Python 3）對比 Rust 退出。**Python 1（OpenMontage）+ TypeScript 3（gauzy / langwatch / typescript-sdk）+ C 1（colibri）—— TS 主流、C 唯一**。**今日 C 出現是訊號**：pure C inference engine 在 2026 Q3 是「**拒絕 PyTorch 依賴鏈的刻意選擇**」——主人 SOUL 偏好「保守裁切 runtime + 自主可控」與 colibri 哲學對齊（zero engine deps + 自己讀 / 改 / 量測 inference engine）。`colibri` 也是 5 個 repo 裡 **唯一一個不依賴 Python ecosystem 的 runtime**——其他 4 個（gauzy / OpenMontage / langwatch / typescript-sdk）至少有 Python 或 TS 工具鏈綁定。C 的可移植性（從 MCU 到 HPC 都跑）對主人 air-gapped 想跨硬體部署有意義。
- **對主人的實用度排序 + 跨 repo 觀察：** `modelcontextprotocol/typescript-sdk` 是**主人 enterprise-lite + hermes-webui 主軸最直接可研究**——MCP 2026-07-28 stateless spec 是 2026 Q3 最大的 protocol 事件，主人 horo-agent / horo-webui 若想做 multi-tenant MCP server 給客戶端用，stateless pattern 是必須抄的；v2 對 v1 的 backward-compat window（≥6 個月 bug fix）是 enterprise 採納關鍵訊號；`langwatch` 是**主人 Hermes / horo-agent 內部用得上的 tool**——`npx langwatch claude` 一行 attach，`npx @langwatch/server` 一行 self-host（只要 Node.js），與主人 8100 lmstudio-council / 8000 ASMR Foley Lab FastAPI + SSE stack 整合 low-friction，給主人「**用 OpenAI / Anthropic 兼容 endpoint + virtual keys + budgets + routing**」四象限 observability 一次收齊；`JustVugg/colibri` 對主人 SOUL「**自主可控 inference + air-gapped + permissive license**」是 engine-level reference——純 C + Apache-2.0 + zero deps + 「no SLA on speed, hard guarantee on semantics」哲學對齊主人「保守裁切 runtime」心法，但主人沒有 MoE 模型 + 372 GB 容器放本地，**purely study / borrow philosophy**，不是 replace Qwen3.8-27B 那條線；`ever-co/ever-gauzy` 短期對主人無 direct call（AGPL + 整套 ERP infra dependencies 重），但**它的「HRM + Time-Tracking + Activity Tracking」on-demand economy 設計 + AGPL-3.0 + SaaS-alternative 商業模式**對主人若想開源 agent-as-business-platform 是 reference 案例（不是抄 code，是抄 positioning）；`calesthio/OpenMontage` 對主人 SOUL「**對遊戲/視覺型任務採視覺優先哲學**」直接命中——「agentic + provider menu + creative decision approval gates + real-footage path」是主人如果想做 visual-first skill library 的 general pattern reference（不是抄 video production，是抄「AI coding assistant + structured approval gate」這層 pattern），且**它 README 寫著「If You're An OpenClaw Agent Reading This, Congratulations」——主人 OpenClaw 經驗意外和這條路線撞合**，值得 pattern-level 觀察。
